# Unworked Review Issues

**Run:** 2026-05-19 11:57:41
**Task:** TASK-040
**Total:** 82 (0 critical, 1 major, 81 minor)

## Major

1. [ ] **architecture-alignment-checker** | `examples/empty_response_example.cpp:21` | adr-violation
   The example directly includes `<microhttpd.h>` and uses the raw MHD symbol `MHD_RF_HEAD_ONLY_RESPONSE` on line 37. The architecture doc (§4.3 http_response, §3.1 overview) explicitly states that public headers must never include `<microhttpd.h>` and that the consumer translation unit is insulated from the MHD backend. Exposing an MHD enum constant in a user-facing example teaches consumers to depend on the MHD ABI directly, which contradicts the PIMPL isolation goal and will break if MHD renames or renumbers the flag in a future release.
   *Recommendation:* Replace the `MHD_RF_HEAD_ONLY_RESPONSE` argument with the libhttpserver-defined equivalent (e.g., `httpserver::response_flag::head_only` or similar constant), or remove the flag entirely and add a comment explaining HEAD semantics. Remove the `#include <microhttpd.h>` line so the example compiles using only the installed `<httpserver.hpp>` umbrella header, as verified by `scripts/verify-installed-examples.sh`.

## Minor

2. [ ] **architecture-alignment-checker** | `examples/allowing_disallowing_methods.cpp:27` | pattern-violation
   `hello_world_resource::render()` is declared without the `override` specifier. The architecture doc (§4.4 http_resource) requires subclasses to override `render` or the method-specific `render_get` / `render_post` / etc. Established v2.0 pattern in the rest of the suite (benchmark_*.cpp, url_registration.cpp) uses `override` consistently. Missing `override` is a code-quality gap that can mask a signature mismatch silently.
   *Recommendation:* Add `override` to the `render(...)` declaration to match the pattern established in all other resource subclasses in the examples suite.

3. [ ] **architecture-alignment-checker** | `examples/args_processing.cpp:38` | pattern-violation
   `args_resource::render()` is declared without `override`, same issue as the two preceding findings. All three files were present in the changed set and should have been uniformly updated.
   *Recommendation:* Add `override` to the `render(...)` declaration.

4. [ ] **architecture-alignment-checker** | `examples/binary_buffer_response.cpp:22` | pattern-violation
   The comment on lines 22–24 says 'using string_response' (a v1 class name) when the actual API used is `http_response::string(...)` (the v2.0 factory). This is a documentation inconsistency: the code itself is correct, but the comment teaches a wrong class name to readers who might then search for `string_response` in the v2.0 headers.
   *Recommendation:* Replace 'using string_response' / 'string_response works' in the comment with 'using `http_response::string(...)` factory' / 'the string factory works' to match the v2.0 naming.

5. [ ] **architecture-alignment-checker** | `examples/custom_error.cpp:35` | pattern-violation
   `hello_world_resource::render()` is declared without `override`, identical issue to allowing_disallowing_methods.cpp. The benchmark examples in the same commit do use `override` on their `render()` declarations, making this an inconsistency within the changed set.
   *Recommendation:* Add `override` to the `render(...)` declaration.

6. [ ] **architecture-alignment-checker** | `examples/custom_error.cpp:35` | pattern-violation
   hello_world_resource::render() is declared without the override specifier. This is the same pattern as service.cpp finding above. Consistent use of override across the example suite helps users understand the v2.0 class-form pattern correctly.
   *Recommendation:* Add override: httpserver::http_response render(const httpserver::http_request&) override {

7. [ ] **architecture-alignment-checker** | `examples/deferred_with_accumulator.cpp:56` | pattern-violation
   The deferred_resource class overrides render_get but does not declare it with the override specifier. shared_state.cpp (the canonical class-form example) correctly uses override. Architecture §4.4 and the task description call for v2.0 idioms throughout the example suite; override is a C++11 (and thus C++20, per DR-001) best practice that the reference example should model.
   *Recommendation:* Add override: httpserver::http_response render_get(const httpserver::http_request&) override {

8. [ ] **architecture-alignment-checker** | `examples/deferred_with_accumulator.cpp:59` | pattern-violation
   std::shared_ptr<std::atomic<int>> is constructed with raw new (new std::atomic<int>(counter++)) rather than std::make_shared. The rest of the v2.0 examples and the architecture (§4.4, §13) consistently use std::make_shared / std::make_unique. While not a correctness defect, it is inconsistent with the smart-pointer factory idiom that TASK-040 is explicitly promoting. This minor was present in iter 1 and the fixer did not resolve it.
   *Recommendation:* Replace: std::shared_ptr<std::atomic<int>> closure_data(new std::atomic<int>(counter++));
With:    auto closure_data = std::make_shared<std::atomic<int>>(counter++);

9. [ ] **architecture-alignment-checker** | `examples/deferred_with_accumulator.cpp:65` | pattern-violation
   `std::shared_ptr<std::atomic<int>> closure_data(new std::atomic<int>(counter++))` uses the raw `new` form of shared_ptr construction. The architecture doc (§4.4 http_resource Lifetime) and TASK-040 task definition both require smart-pointer ownership without raw-pointer patterns. The preferred v2.0 idiom is `std::make_shared<std::atomic<int>>(counter++)`, which is also exception-safe.
   *Recommendation:* Replace `std::shared_ptr<std::atomic<int>>(new std::atomic<int>(counter++))` with `std::make_shared<std::atomic<int>>(counter++)` to be consistent with the smart-pointer ownership pattern demonstrated across the rest of the suite.

10. [ ] **architecture-alignment-checker** | `examples/deferred_with_accumulator.cpp:65` | pattern-violation
   The deferred callback returns `static_cast<ssize_t>(max)` unconditionally after a 1-second sleep when reqid < 5, which means the lambda never terminates for eligible connections — the accumulator loop is unbounded. This is a logic issue in the example code rather than an architecture violation, but examples serve as documentation and this pattern may confuse users about proper deferred-response termination idioms.
   *Recommendation:* Add a finite iteration counter inside the lambda (e.g. cap at N cycles) so the example demonstrates both streaming and clean termination, matching the v2.0 pattern guidance in §13 documentation deliverables.

11. [ ] **architecture-alignment-checker** | `examples/service.cpp:44` | pattern-violation
   All eight render_* overrides in service_resource are declared in the class body without the override specifier (render_get, render_put, render_post, render, render_head, render_options, render_connect, render_delete). The canonical class-form example (shared_state.cpp) demonstrates override on every virtual method. This was noted in iter 1 and was not addressed by the fixer.
   *Recommendation:* Add override to each render_* method declaration in the class definition.

12. [ ] **code-quality-reviewer** | `examples/README.md:39` | code-readability
   The README lists `args_processing.cpp`, `custom_error.cpp`, `allowing_disallowing_methods.cpp` under 'HTTP basics' and several other files under other sections, but these files are also listed as targets in examples/Makefile.am (`noinst_PROGRAMS`). However, these .cpp files are not among the changed files in this task. If they are pre-existing and unchanged, the README references may be stale (pointing to v1 files not yet ported). This is outside the scope of this PR's diff but worth a follow-up audit.
   *Recommendation:* Verify that args_processing.cpp, custom_error.cpp, allowing_disallowing_methods.cpp, and other README-listed files not in the diff are already on v2.0 idioms, or file a follow-up task to port them.

13. [ ] **code-quality-reviewer** | `examples/allowing_disallowing_methods.cpp:32` | code-readability
   The `render` override lacks the `override` keyword, while the newer examples (shared_state.cpp, url_registration.cpp, benchmarks) consistently mark their virtual overrides. The missing specifier means a signature mismatch would silently shadow rather than error.
   *Recommendation:* Add `override` to the `render` declaration: `httpserver::http_response render(const httpserver::http_request&) override {`.

14. [ ] **code-quality-reviewer** | `examples/centralized_authentication.cpp:57` | code-elegance
   The trailing usage comment block (lines 57-68) mixes C++ source code with shell command documentation. While useful, placing shell invocations inside a .cpp file is unconventional; readers must switch mental modes mid-file. This pattern does not appear in any other example.
   *Recommendation:* Move the curl usage examples into the README.md entry for centralized_authentication.cpp, or into a standalone shell script alongside the example. If kept in the .cpp, wrap in a clearly delineated `/* Usage: ... */` block consistent with the top-of-file license header style.

15. [ ] **code-quality-reviewer** | `examples/deferred_with_accumulator.cpp:34` | code-elegance
   The global `std::atomic<int> counter` is file-scope mutable state used only by `deferred_resource::render_get`. In the context of an examples suite that now uniformly prefers encapsulating state inside the resource class, this pattern is inconsistent with shared_state.cpp and contradicts the comment on line 29 of that file that explains why class-level state is the right shape.
   *Recommendation:* Move `counter` as a member of `deferred_resource` (using `std::atomic<int>` there too). This makes the example self-consistent with the teaching narrative and avoids global mutable state.

16. [ ] **code-quality-reviewer** | `examples/deferred_with_accumulator.cpp:36` | code-elegance
   Global std::atomic<int> counter is not reset between server restarts and is mutable from outside the class. Placing state at global scope rather than as a member variable makes the class less self-contained.
   *Recommendation:* Move counter into deferred_resource as a member (std::atomic<int> counter_{0};). For an example this is low priority, but it would better illustrate encapsulation.

17. [ ] **code-quality-reviewer** | `examples/deferred_with_accumulator.cpp:58` | code-readability
   `render_get` override in `deferred_resource` lacks the `override` keyword, inconsistent with updated examples.
   *Recommendation:* Add `override` to the `render_get` declaration.

18. [ ] **code-quality-reviewer** | `examples/deferred_with_accumulator.cpp:59` | code-elegance
   closure_data is constructed with `new std::atomic<int>(counter++)` wrapped in std::shared_ptr, rather than std::make_shared. The raw `new` is benign here (exception safety is not a concern since the ctor cannot throw), but it is inconsistent with the v2.0 idiom and project style visible in every other example file.
   *Recommendation:* Replace `std::shared_ptr<std::atomic<int>>(new std::atomic<int>(counter++))` with `std::make_shared<std::atomic<int>>(counter.fetch_add(1))` to align with the project's smart-pointer conventions.

19. [ ] **code-quality-reviewer** | `examples/deferred_with_accumulator.cpp:63` | code-readability
   The comment '// sleep(1); ==> adapted for C++11 on non-*Nix systems' is leftover migration noise from a prior implementation; it references removed code and adds no value.
   *Recommendation:* Remove the commented-out sleep line and its associated migration annotation.

20. [ ] **code-quality-reviewer** | `examples/empty_response_example.cpp:29` | code-readability
   render_delete comment 'Return a 204 No Content response with no body' is redundant noise — the method name and return value are self-documenting.
   *Recommendation:* Remove or shorten the comment; the render_head block comment is useful because it explains a non-obvious v2.0 idiom, so keep that one.

21. [ ] **code-quality-reviewer** | `examples/file_upload.cpp:29` | code-readability
   The `render_get` and `render_post` overrides in `file_upload_resource` lack the `override` keyword. This is inconsistent with shared_state.cpp and with the iterative changes to other examples that were touched in this same commit.
   *Recommendation:* Add `override` to both `render_get` and `render_post` declarations.

22. [ ] **code-quality-reviewer** | `examples/file_upload_with_callback.cpp:30` | code-readability
   Same as file_upload.cpp: `render_get` and `render_post` overrides lack `override`.
   *Recommendation:* Add `override` to both `render_get` and `render_post` declarations.

23. [ ] **code-quality-reviewer** | `examples/hello_world.cpp:9` | code-elegance
   The `// NOLINT(build/namespaces)` comment suppresses a linter warning that only applies if cpplint is run against the examples directory. There is no evidence in the repository that cpplint is actually run here, making the NOLINT annotation dead noise for most readers.
   *Recommendation:* Either wire cpplint into the examples check so the NOLINT comment is load-bearing and readers understand why it is there, or remove the NOLINT suffix and rely solely on the prose comment explaining the `using namespace` rationale.

24. [ ] **code-quality-reviewer** | `examples/pipe_response_example.cpp:54` | code-readability
   The write return value is cast to void with a comment `(void)ret;` after storing in `ret`. This is an unusual two-step pattern. The more common idiom is `(void)write(fd, msg, strlen(msg));` directly, making the suppression intent immediately obvious.
   *Recommendation:* Remove the `ret` variable and write `(void)write(fd, msg, strlen(msg));` directly.

25. [ ] **code-quality-reviewer** | `examples/pipe_response_example.cpp:54` | code-elegance
   `write()` is called with `(void)ret;` discarding the return value. While this is an example and brevity is valued, the comment says the return is suppressed, which teaches users that ignoring write errors is acceptable. On partial writes (short write or EINTR) the pipe data would be silently truncated.
   *Recommendation:* Either handle the error (even just logging it to stderr and closing the pipe) or add a comment explaining that in production code the return value of write() must be checked in a loop to handle partial writes.

26. [ ] **code-quality-reviewer** | `examples/service.cpp:149` | code-readability
   Three block comments ('Use builder to define...', 'Create webserver...', 'Create and register...', 'Start and block...') state what the next line of code already makes obvious, violating the 'don't add obvious noise' comments rule.
   *Recommendation:* Remove the block comments in main(); the code is sufficiently self-explanatory at this level of abstraction.

27. [ ] **code-quality-reviewer** | `examples/service.cpp:29` | code-elegance
   `bool verbose = false;` is a non-const, non-static file-scope variable accessed from both `log_if_verbose` and `main`. The design rules flag global mutable state as a code smell (opacity, fragility). For an examples file that is meant to teach patterns, a cleaner shape would be to pass `verbose` as a parameter or capture it in a lambda.
   *Recommendation:* Declare `verbose` as `static bool verbose = false;` to at least limit the linkage, or refactor `log_if_verbose` to accept a boolean flag parameter and remove the global. This is a minor nit in the context of a legacy kitchen-sink example.

28. [ ] **code-quality-reviewer** | `examples/shared_state.cpp:56` | code-readability
   The private member `n_` uses a trailing-underscore style that is common in Google style but is inconsistent with the rest of the example codebase (other examples have no member variables, so this is the only instance). The naming choice itself is fine but worth noting for future contributors who extend the example.
   *Recommendation:* No change required; document the style convention in the file comment if you expect contributors to add more members.

29. [ ] **code-quality-reviewer** | `scripts/check-examples.sh:106` | code-readability
   The bidirectional Makefile.am coverage check uses three nested for-loops with manual found/in_artifacts flags. While correct, the O(n*m) inner loop is harder to follow than a single awk/grep pass would be.
   *Recommendation:* Low priority for a CI script; acceptable as-is, but could be simplified with 'echo "$programs" | grep -qw "$base"' to reduce nesting.

30. [ ] **code-quality-reviewer** | `scripts/check-examples.sh:117` | test-coverage
   The Makefile.am coverage check extracts `noinst_PROGRAMS` with a single-line awk pattern. If `noinst_PROGRAMS` uses continuation lines (backslash-newline), the awk strips the trailing `\` but does not join lines, so programs on continuation lines are missed. The current Makefile.am uses a single long line, so this does not bite today — but it is a latent fragility.
   *Recommendation:* Either document the single-line assumption in the script comment, or handle multi-line continuation by joining lines in the awk program before parsing program names.

31. [ ] **code-quality-reviewer** | `scripts/check-examples.sh:27` | code-readability
   `set -u` is present but `set -e` (exit on error) is absent. The script uses explicit `fail()` calls for each check, which is a deliberate and reasonable choice; but a subshell error (e.g. from `awk`) would silently continue without `-e`. This is a low risk given the short script but is inconsistent with secure shell script practices.
   *Recommendation:* Add `set -e` or `set -eu` so unexpected subprocess failures surface immediately.

32. [ ] **code-quality-reviewer** | `scripts/check-examples.sh:69` | test-coverage
   The LOC-counting awk script in check-examples.sh does not handle the `NOLINT` comment that hello_world.cpp uses on its `using namespace` line (line 9). If the NOLINT suffix is ever changed to a multi-word form that tricks the comment stripper, the LOC count could shift silently. This is a fragile coupling between the script logic and the specific comment text in the example.
   *Recommendation:* Add a brief comment in check-examples.sh explaining which line in hello_world.cpp the `using namespace` sits on, or add a regression assertion: count known-good LOC against the current file and fail if the result is not in [5,10].

33. [ ] **code-quality-reviewer** | `scripts/verify-installed-examples.sh:26` | code-readability
   Same as check-examples.sh: `set -u` only, no `set -e`. Subprocess failures in the install step or compiler invocations might leave the script in an inconsistent state.
   *Recommendation:* Add `set -e` or use the already-present `|| fail` error guards consistently on every external command.

34. [ ] **code-quality-reviewer** | `scripts/verify-installed-examples.sh:66` | code-readability
   The HAVE_* feature detection in verify-installed-examples.sh uses four consecutive case statements, one per feature, where each case only has a single pattern match. This is repetitive; a single grep pass against the cxxflags line extracting all defines at once would be more concise and easier to extend.
   *Recommendation:* Optionally consolidate the four `case "$cxxflags_line" in ... esac` blocks into a single `grep -o` loop or a set of `echo "$cxxflags_line" | grep -q` calls, reducing vertical repetition.

35. [ ] **code-simplifier** | `examples/benchmark_select.cpp:26` | naming
   `#define PATH "/plaintext"` and `#define BODY "Hello, World!"` — using preprocessor defines for named constants in C++17/20 code is the wrong tool. This pattern is replicated identically in `benchmark_threads.cpp` and `benchmark_nodelay.cpp`.
   *Recommendation:* Replace both `#define` lines with `constexpr const char* kPath = "/plaintext";` and `constexpr const char* kBody = "Hello, World!";` (or `constexpr std::string_view`). Apply consistently to all three benchmark files.

36. [ ] **code-simplifier** | `examples/binary_buffer_response.cpp:54` | code-structure
   The comment block on lines 54-55 (`// Lambda form: stateless handlers need no class. generate_png_data()\n// stays a plain static function; the lambda calls it per request.`) explains implementation motivation — why a lambda was chosen over a class. This is meta-commentary about a refactoring decision that no longer needs to be captured in the example code; it distracts from what the example is teaching (binary buffer responses).
   *Recommendation:* Remove the two-line motivational comment. The lambda usage is self-evident; the `generate_png_data()` function is already documented above at its definition.

37. [ ] **code-simplifier** | `examples/centralized_authentication.cpp:57` | code-structure
   Usage comments placed after `main()` at the bottom of the file (lines 57-68) are block-comment documentation. These 'Usage:' comments are better placed in the file header alongside the existing comment block, or in the README, rather than after the closing brace of main.
   *Recommendation:* Move the usage curl examples to the file-level comment block (before `#include`) or remove them and rely on the README. Trailing comment blocks after main() break the 'separate concepts vertically' and 'related code vertically dense' rules.

38. [ ] **code-simplifier** | `examples/client_cert_auth.cpp:65` | code-structure
   `std::set<std::string> allowed_fingerprints` is a file-scope global that is never populated anywhere in main() (the comment says 'In a real application, this would be loaded from...'), making the allowlist branch dead code. The `time_t now = time(nullptr)` mixing with `std::int64_t` is also a narrowing comparison on platforms where `time_t` is 64-bit signed vs 32-bit.
   *Recommendation:* Either populate `allowed_fingerprints` with a documented example value in main() so the branch is exercised, or remove the dead allowlist branch and replace it with a comment explaining the extension point. The time comparison should use `static_cast<std::int64_t>(now)` for clarity and portability.

39. [ ] **code-simplifier** | `examples/deferred_with_accumulator.cpp:37` | comments
   The comment on line 37 (`// closure_data is always non-null when called from the lambda`) was added to suppress what was previously a null-check finding but it describes a constraint that is structural (shared_ptr is only reachable through the lambda capture) rather than documenting intent. It reads as defensive commentary rather than clarifying information.
   *Recommendation:* Remove the comment on line 37 — the non-null guarantee is self-evident once test_callback is inlined or once closure_data is replaced with a plain int capture.

40. [ ] **code-simplifier** | `examples/deferred_with_accumulator.cpp:60` | naming
   The variable `initial` holds the string `"cycle callback response"` and is captured by the lambda as the first-call body. The name `initial` is ambiguous — it could refer to an initial value, an initial state, or an initializer. The content it holds (`"cycle callback response"`) is more specifically a 'preamble' or 'header_body'.
   *Recommendation:* Rename `initial` to `preamble` (or `header_chunk`) to make the intent obvious without needing to read the value.

41. [ ] **code-simplifier** | `examples/deferred_with_accumulator.cpp:63` | code-structure
   Commented-out code `// sleep(1); ==> adapted for C++11 on non-*Nix systems` is scaffolding left in the file. Clean code rules say: don't comment out code, just remove.
   *Recommendation:* Delete lines 63-64 entirely. The `std::this_thread::sleep_for` call on the next line is self-explanatory and the comment explaining why sleep(1) was replaced is no longer needed.

42. [ ] **code-simplifier** | `examples/empty_response_example.cpp:22` | code-structure
   `#include <microhttpd.h>` appears in this example, pulling in a libmicrohttpd implementation detail (`MHD_RF_HEAD_ONLY_RESPONSE`) into what should be a pure libhttpserver consumer example. The README describes this example as 'bodyless responses for DELETE / HEAD', implying the public API should be sufficient.
   *Recommendation:* Check whether `MHD_RF_HEAD_ONLY_RESPONSE` can be replaced with a libhttpserver-native constant or flag. If libhttpserver intentionally exposes this as part of the v2.0 API then document it in the file comment; if not, route through a wrapper constant so the example does not depend on the underlying MHD header directly.

43. [ ] **code-simplifier** | `examples/file_upload.cpp:30` | code-structure
   HTML is assembled by repeated `+= ` string concatenation on a `std::string`. Using a raw string literal (`R"html(...)html"`) or a single `return http_response::string(R"html(...)", "text/html")` is more readable and avoids the visual noise of dozens of append operations.
   *Recommendation:* Replace the `get_response` string-append chain with a single raw string literal in `render_get`. Apply the same to `render_post` and to `file_upload_with_callback.cpp`.

44. [ ] **code-simplifier** | `examples/hello_world.cpp:1` | code-structure
   The `hello_world.cpp` file intentionally omits the LGPL license header (present on nearly every other file) in favour of a task-specific comment. This is fine for the hello_world file (the license header would push it over the LOC budget), but a brief one-line note in the file comment about the omission would help readers understand it is intentional rather than an accident.
   *Recommendation:* Add one sentence to the existing file comment: 'The LGPL header is omitted to keep this file within the 10-LOC budget.' This is optional polish — the omission is already explained in context by the LOC constraint.

45. [ ] **code-simplifier** | `examples/pipe_response_example.cpp:53` | code-structure
   `auto ret = write(fd, msg, strlen(msg)); (void)ret;` suppresses the return value of `write()` without explanation. In an example, silently discarding the write result teaches a bad habit. Even a comment explaining 'ignoring for brevity in this example' would improve clarity.
   *Recommendation:* Add a brief inline comment: `// return value ignored in this demo; production code must check it`. This is a one-line change that teaches the right instinct without adding error-handling code that would obscure the teaching point.

46. [ ] **code-simplifier** | `examples/service.cpp:23` | dependencies
   `#include <cstdio>` is present but no C stdio functions (printf, scanf, fopen, etc.) are used anywhere in the file.
   *Recommendation:* Remove the `#include <cstdio>` line. All I/O in this file goes through `std::cout`.

47. [ ] **code-simplifier** | `examples/service.cpp:41` | code-structure
   The explicit `= default` constructor and destructor declarations were added by the fixer but add no value over omitting them entirely. When a class needs no user-defined constructor or destructor body, the idiomatic C++ style is to omit the declarations and let the compiler generate them implicitly. The `= default` form is useful when you need to control access (e.g., `protected`) or when a base class would suppress the default — neither applies here.
   *Recommendation:* Remove the `service_resource() = default;` and `~service_resource() = default;` declarations. The compiler will generate them implicitly and the class definition becomes shorter and clearer.

48. [ ] **housekeeper** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-040/examples/README.md:71` | documentation-stale
   README.md documents client_cert_auth.cpp as 'not wired into noinst_PROGRAMS', which is correct. However there is no EXTRA_DIST or similar stanza in examples/Makefile.am to explicitly include client_cert_auth.cpp in the source distribution, which means it may be silently excluded from tarballs produced by make dist.
   *Recommendation:* Consider adding client_cert_auth.cpp to an EXTRA_DIST line in examples/Makefile.am so it is included in release tarballs as the documentation artifact the README describes.

49. [ ] **performance-reviewer** | `examples/client_cert_auth.cpp:112` | memory-allocation
   render_get in secure_resource builds a multi-line response body with six separate += appends on a std::string, with no initial reserve. The four string_view-to-string copies at lines 80-83 are correct and necessary (as the comment explains), but the response construction itself is gratuitously allocate-heavy for an example intended to show certificate auth, not string building.
   *Recommendation:* Reserve capacity before the += chain, or use an ostringstream. A minor issue in a doc-only file (client_cert_auth is excluded from noinst_PROGRAMS), but still visible to users reading source.

50. [ ] **performance-reviewer** | `examples/file_upload.cpp:70` | memory-allocation
   reserve(post_response.size() + 512) is called after the raw-string literal is already assigned to post_response, which is correct, but the reserved amount (512 bytes) covers only one or two file rows. For uploads with many files the buffer will still reallocate per file. This is a documentation / example quality issue rather than a real performance bug (upload responses are cold path), but the comment promises 'per-append reallocations avoided' which is only partially true.
   *Recommendation:* Either increase the reserve hint (e.g., reserve 512 * estimated_file_count) or remove the comment claim and let the optimizer handle it; the existing code is functionally fine.

51. [ ] **performance-reviewer** | `examples/file_upload_with_callback.cpp:98` | memory-allocation
   Inside the file_cleanup_callback lambda (invoked once per uploaded file), std::string dest = permanent_dir + "/" + filename constructs two temporary std::string objects: one for permanent_dir + "/" and another for the full concatenation. For the example's intended use this is negligible, but as reference code it teaches users to use + chaining in callbacks that may be called frequently.
   *Recommendation:* Use a single reserve + append pattern: std::string dest; dest.reserve(permanent_dir.size() + 1 + filename.size()); dest = permanent_dir; dest += '/'; dest += filename; Alternatively, use std::format (C++20) which avoids temporaries.

52. [ ] **performance-reviewer** | `examples/hello_with_get_arg.cpp:33` | memory-allocation
   The response is constructed as `"Hello: " + std::string(req.get_arg("name"))`. This copies the arg (a string_view into request storage) into a temporary std::string, then concatenates it with a literal, producing another temporary. For a hello-world example that users will copy as the idiomatic way to echo a query arg, this is worth fixing.
   *Recommendation:* If the API supports http_response::string(string_view), compose the response without the intermediate std::string: build with std::string{"Hello: "}.append(req.get_arg("name")), which avoids the extra copy and temporary. Or use std::format("Hello: {}", req.get_arg("name")).

53. [ ] **performance-reviewer** | `examples/minimal_deferred.cpp:40` | memory-allocation
   In test_callback, the return value is computed as `return std::string(buf).size()` after snprintf writes into buf. This constructs a full std::string from buf on every callback invocation (which is called repeatedly by MHD until it returns -1) solely to get the length, when strlen(buf) or a constant would suffice.
   *Recommendation:* Replace `return std::string(buf).size();` with `return strlen(buf);` or compute the length directly from snprintf's return value (which is the number of characters that would have been written). The temporary std::string allocation is gratuitous and will mislead users reading deferred-response examples.

54. [ ] **performance-reviewer** | `examples/url_registration.cpp:59` | memory-allocation
   The parametric-segment lambda concatenates three std::string values per request: `"ARGS: " + std::string(req.get_arg("arg1")) + " and " + std::string(req.get_arg("arg2"))`. Each + creates a temporary string. The same pattern appears at line 65-67 for the constrained-arg endpoint. This is also present in hello_with_get_arg.cpp line 33.
   *Recommendation:* Use an ostringstream or fmt/std::format (C++20) for multi-part string assembly in handlers, or at minimum document that the + chain creates temporaries. A single-format-call idiom would be idiomatic v2.0 usage and teach users the right pattern.

55. [ ] **performance-reviewer** | `examples/url_registration.cpp:62` | memory-allocation
   The two parametric-arg lambda handlers build their response body with multiple + concatenations of std::string temporaries: "ARGS: " + std::string(req.get_arg("arg1")) + " and " + std::string(req.get_arg("arg2")). Each + creates a temporary heap allocation. The same pattern appears on lines 72-75. As reference code, users will copy this idiom into higher-traffic handlers.
   *Recommendation:* Use ostringstream, std::format (C++20), or reserve + append: std::string r; r.reserve(/* hint */); r += "ARGS: "; r += req.get_arg("arg1"); r += " and "; r += req.get_arg("arg2"); return http_response::string(std::move(r));

56. [ ] **performance-reviewer** | `examples/websocket_echo.cpp:37` | memory-allocation
   on_message copies msg (a string_view of the incoming frame) into a std::string via std::string(msg) in order to append it to the "Echo: " prefix, creating a temporary copy of potentially large messages. For the echo demo this is expected behaviour, but users adapting this pattern for a high-message-rate service will unnecessarily copy every frame.
   *Recommendation:* Use std::string reply; reply.reserve(6 + msg.size()); reply = "Echo: "; reply += msg; session.send_text(reply); This avoids the intermediate temporary and is equally readable.

57. [ ] **performance-reviewer** | `scripts/verify-installed-examples.sh:104` | missing-batching
   The for loop on line 104 compiles each example sequentially. With 28+ examples each requiring a full compile+link, the total build time on a multicore machine is needlessly high. The script has no parallelism.
   *Recommendation:* Run compilations in parallel using a background subshell with a wait-based job limit, or replace the loop with a GNU parallel / xargs -P invocation. Even simple `&` + `wait` with a semaphore would halve wall-clock time on a 4-core host. This is a CI-time issue, not a runtime issue, so it is minor.

58. [ ] **security-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-040/examples/basic_authentication.cpp:37` | sensitive-data
   Credentials 'myuser'/'mypass' are still hardcoded as string literals in the comparison at line 37. The comment at line 27 warns against this, but unlike centralized_authentication.cpp, the code itself still uses compile-time strings. Example consumers are more likely to copy the code pattern than read the comment (CWE-798).
   *Recommendation:* Load expected credentials from environment variables inside the lambda, or add a startup check in main() that reads AUTH_USER/AUTH_PASS from the environment and captures them into the lambda, mirroring centralized_authentication.cpp. This makes the secure pattern the default rather than an aspirational note.

59. [ ] **security-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-040/examples/digest_authentication.cpp:47` | sensitive-data
   The password 'mypass' is still hardcoded as a string literal passed to check_digest_auth() (line 47). Although the comment at line 32 warns that credentials should come from environment variables, the code does not demonstrate this pattern, unlike centralized_authentication.cpp which does use getenv(). As example code copied by users, this propagates the anti-pattern of embedding secrets in source (CWE-798).
   *Recommendation:* Load the password from an environment variable (e.g., std::getenv("DIGEST_PASS")) with a null check and error exit, mirroring the pattern in centralized_authentication.cpp. The existing comment is good but insufficient without a code demonstration.

60. [ ] **security-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-040/examples/file_upload.cpp:74` | injection
   User-supplied file metadata (file key name at line 75, uploaded filename at line 77, content-type at line 83, transfer-encoding at line 85) is concatenated directly into an HTML response body without HTML-entity encoding. A crafted multipart upload with a filename like '<script>alert(1)</script>' would inject arbitrary HTML/JS into the response rendered by the browser (CWE-79, reflected/stored XSS).
   *Recommendation:* HTML-encode all user-controlled strings before inserting them into the HTML table. At minimum, replace '<', '>', '&', '"', and '\'' with their named entities. Alternatively, respond with JSON and let the browser render it safely, or add a comment noting that this is a demo and production code must encode output.

61. [ ] **security-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-040/examples/file_upload_with_callback.cpp:57` | injection
   The original uploaded filename (files.first, user-controlled) is inserted raw into the HTML response body at line 57, and the same value is used unvalidated to build the destination path at line 98 ('permanent_dir + "/" + filename'). A filename containing path-traversal sequences such as '../../etc/cron.d/evil' could write outside the intended permanent directory if std::rename() is not restricted to the target directory tree (CWE-22). The HTML injection risk mirrors file_upload.cpp finding #1 (CWE-79).
   *Recommendation:* For the path: strip the directory component from the filename (keep only basename) before constructing the destination path, and validate the resolved canonical path is still under permanent_dir. For the HTML: HTML-encode the filename before inserting it into the <li> element.

62. [ ] **security-reviewer** | `examples/custom_access_log.cpp:31` | logging-failures
   The access log callback writes the raw URL to stdout without sanitization. If the URL contains terminal escape sequences (ANSI codes, carriage returns), this can corrupt terminal output or in some terminal emulators trigger terminal injection (CWE-117). This is a minor risk for an example but teaches an unsafe logging pattern.
   *Recommendation:* Add a comment noting that production access logs should sanitize or percent-encode the URL before writing to a terminal or log file. A simple approach: strip or replace control characters before logging.

63. [ ] **security-reviewer** | `examples/digest_authentication.cpp:37` | insecure-design
   The nonce timeout is set to 300 seconds (5 minutes) which is generous. The `stale_nonce` parameter (the last 0 argument) is set to 0, meaning stale nonces and invalid nonces receive the same rejection response. While the comment at line 22-23 notes this is intentional to avoid leaking nonce state, no comment explains the nonce lifetime choice or its security trade-off.
   *Recommendation:* Add a comment explaining the nonce lifetime choice and the security trade-off between usability (longer nonces allow more retries without re-auth) and replay window (shorter nonces limit replay attack window). The current 300s is not unreasonable but should be explained.

64. [ ] **security-reviewer** | `examples/iovec_response_example.cpp:43` | missing-security-headers
   The JSON response sets `Content-Type: application/json` but does not set `X-Content-Type-Options: nosniff`. While this is an example rather than production code, the absence of this pattern in reference code means developers will not adopt it. Minor risk.
   *Recommendation:* As an example of best practice, add `.with_header("X-Content-Type-Options", "nosniff")` to show the pattern. A comment can note it is a defense-in-depth header.

65. [ ] **security-reviewer** | `examples/minimal_https_psk.cpp:52` | insecure-design
   The TLS priority string `NORMAL:-VERS-TLS-ALL:+VERS-TLS1.2:+PSK:+DHE-PSK` forces TLS 1.2, which is acceptable, but the example does not explain why TLS 1.3 with PSK requires different handling (external PSK in TLS 1.3 has different semantics). Without this context developers may copy the TLS 1.2 lock-in unnecessarily or miss that TLS 1.3 PSK support needs different configuration.
   *Recommendation:* Add a comment explaining that this priority string locks to TLS 1.2 because external PSK in TLS 1.3 requires session ticket based resumption or the `NORMAL:+PSK` without version restriction on newer GnuTLS. Direct readers to GnuTLS documentation.

66. [ ] **security-reviewer** | `scripts/verify-installed-examples.sh:29` | insecure-design
   The install prefix `$(mktemp -d -t libhttpserver-installed-examples-XXXXXX)` is created by mktemp, which is safe, but the directory is never cleaned up on exit. If the script is run repeatedly in CI it will leave many directories behind. This is not a security vulnerability per se, but the predictable-prefix pattern `libhttpserver-installed-examples-` could make the temp dir easier to guess on low-entropy systems. Minor CWE-377 concern.
   *Recommendation:* Add a `trap 'rm -rf "$PREFIX"' EXIT` immediately after setting PREFIX, or document that cleanup is the caller's responsibility when `INSTALL_PREFIX` is explicitly provided.

67. [ ] **security-reviewer** | `scripts/verify-installed-examples.sh:47` | insecure-design
   The make install step silently discards stderr: `>/dev/null 2>&1`. If the install fails for a non-obvious reason (e.g., permission error, disk full), the only diagnostic is the generic 'make install into $PREFIX failed' message. This makes security-relevant failures (e.g., files installed with wrong permissions) invisible.
   *Recommendation:* Consider capturing stderr to a temp file and displaying it on failure, or at minimum run with `2>&1 | tee /tmp/install.log` so that failures are diagnosable. The current `set -u` at the top is good.

68. [ ] **spec-alignment-checker** | `examples/:null` | ears-requirement
   PRD-HDL-REQ-006: 'When a user wants to register a handler with the HTTP method known only at runtime then the system shall provide a generic webserver::route(http_method, const std::string& path, handler) entry point'. TASK-040's Related Requirements list PRD-HDL-REQ-001..006, and the audit action item says to port examples to v2.0 idioms. No example demonstrates route(); only on_get/on_post are shown (handlers.cpp covers exactly two methods). The absence is not directly called out in the acceptance criteria but the PRD-HDL-REQ-006 capability is left undocumented.
   *Recommendation:* Add a short example — or a section in url_registration.cpp — showing route() used for table-driven registration (e.g., iterating a vector of {method, path, handler} tuples). This closes the only PRD-HDL-REQ coverage gap in the example suite.

69. [ ] **spec-alignment-checker** | `examples/binary_buffer_response.cpp:22` | ears-requirement
   PRD-HDL-REQ per §3.4 audit action: comment on lines 22-23 and 67 references 'string_response' — the removed v1 subclass — to explain the example. The actual code correctly uses http_response::string() factory (v2.0 idiom), but the prose tells readers the v1 class name is the mechanism. After PRD-RSP-REQ-006 removes string_response from the public API, this comment is misleading: new users reading the example will look for a non-existent type.
   *Recommendation:* Update the introductory comment and inline comment in binary_buffer_response.cpp to use the v2.0 terminology 'http_response::string()' rather than 'string_response'. Replace 'Despite its name, string_response works with arbitrary binary content' with a note about http_response::string() accepting std::string which can hold arbitrary bytes.

70. [ ] **spec-alignment-checker** | `examples/deferred_with_accumulator.cpp:58` | action-item
   deferred_with_accumulator.cpp declares render_get without 'override', consistent with the pattern observed in service.cpp, file_upload.cpp, and file_upload_with_callback.cpp. Same reasoning as findings 2 and 3.
   *Recommendation:* Add 'override' to render_get.

71. [ ] **spec-alignment-checker** | `examples/deferred_with_accumulator.cpp:65` | action-item
   Action item 'Audit existing examples; port each to v2.0 (with_* chains, smart-ptr resources, snake_case render_*, http_response::factory(...) returns)': line 65 uses the C++11 style 'std::shared_ptr<std::atomic<int>>(new std::atomic<int>(counter++))' rather than std::make_shared. This is inside a callback lambda (not a resource-registration site), so it is not a v1 API pattern per se, but it is inconsistent with the v2.0 modern-C++ idiom the audit was supposed to enforce across all examples.
   *Recommendation:* Replace 'std::shared_ptr<std::atomic<int>>(new std::atomic<int>(counter++))' with 'std::make_shared<std::atomic<int>>(counter++)' to stay consistent with the make_shared idiom used everywhere else in the ported examples.

72. [ ] **spec-alignment-checker** | `examples/empty_response_example.cpp:null` | specification-gap
   The file correctly uses the v2.0 http_response::empty() idiom and does not include <microhttpd.h>. No remaining violations of PRD-HDR-REQ-001 or the v2.0 idiom audit action item were found. Score docked 2 points only because the comment on line 34-37 explains an internal library detail (body stripping) that is implementation-level noise for an example file; this is a cosmetic concern only.
   *Recommendation:* Consider trimming the inline comment to a single line ('libhttpserver strips the body automatically for HEAD requests') to keep the example concise.

73. [ ] **spec-alignment-checker** | `examples/file_upload.cpp:29` | action-item
   file_upload.cpp and file_upload_with_callback.cpp declare render_get and render_post without the 'override' keyword, inconsistent with the v2.0 idiom used in shared_state.cpp. Like service.cpp above, the missing 'override' is a consistency issue from the audit action item.
   *Recommendation:* Add 'override' to render_get and render_post in both file_upload.cpp and file_upload_with_callback.cpp.

74. [ ] **spec-alignment-checker** | `examples/service.cpp:44` | action-item
   service.cpp declares render_get, render_put, render_post, render, render_head, render_options, render_connect, and render_delete without the 'override' keyword. The task action item 3 requires porting examples to v2.0 idioms including snake_case render_* — using 'override' on virtual overrides is a contemporary C++ idiom that shared_state.cpp and allowing_disallowing_methods.cpp already follow. The omission means the compiler cannot catch signature mismatches. This is a consistency gap rather than a functional defect.
   *Recommendation:* Add 'override' to each render_* declaration and definition in service.cpp to match the pattern established by shared_state.cpp.

75. [ ] **test-quality-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-040/scripts/check-examples.sh:148` | missing-test
   The disk-to-Makefile.am glob (line 148) uses a bare `*.cpp` pattern without bash `nullglob`. If examples/ were empty, bash would pass the literal glob string as `src`, causing a confusing error message rather than a clean FAIL. This is not a false-pass but produces a misleading diagnostic.
   *Recommendation:* Either set `shopt -s nullglob` before the glob loop and add a post-loop check that `unlisted` or `found` counters were exercised at all, or add an explicit guard: `[ -n "$(ls "$REPO_ROOT"/examples/*.cpp 2>/dev/null)" ] || fail "no .cpp files found in examples/"`.

76. [ ] **test-quality-reviewer** | `scripts/check-examples.sh:1` | missing-test
   check-examples.sh uses `set -u` but not `set -e` or `set -o pipefail`. If a `grep -q` or `grep -Eq` call exits non-zero due to a system error (e.g., permission denied, out-of-memory) rather than no-match, the surrounding `||fail` guards would still fire correctly — but the positive-match assertions (lines 89-103 in shared_state checks) use `|| fail`, while any awk invocation error in count_loc would cause `loc` to be empty, potentially triggering the `[ $loc -lt 5 ]` false-fail rather than a meaningful error message.
   *Recommendation:* Add `set -eo pipefail` at the top of the script. Validate that `loc` is a valid integer after the count_loc call before numeric comparison.

77. [ ] **test-quality-reviewer** | `scripts/check-examples.sh:106` | missing-test
   check-examples.sh checks that shared_state.cpp calls `register_path(..., std::make_unique<...>())` by grepping for 'register_path[[:space:]]*(.*std::make_unique'. The regex is satisfied by any line containing both tokens — including a comment. It does not verify the call is outside a comment block or that the type argument is a concrete derived class. This is a weak heuristic, but it is the same approach used throughout the script and is acceptable given the static-check nature of the tool. However, the parallel check on hello_world.cpp for raw-pointer registration (line 86) similarly is not applied to shared_state.cpp, leaving open the possibility that shared_state.cpp could pass the smart-pointer check while also using `new` elsewhere.
   *Recommendation:* Add a raw-pointer registration check for shared_state.cpp mirroring lines 85-88, or document explicitly in the script header that shared_state.cpp is not checked for raw-pointer registration because the positive smart-pointer assertion is considered sufficient.

78. [ ] **test-quality-reviewer** | `scripts/verify-installed-examples.sh:29` | missing-test
   When `INSTALL_PREFIX` is not set, the script creates a mktemp directory but never cleans it up on success. Each successful run leaks a directory under /tmp. On CI, this accumulates across runs.
   *Recommendation:* Add a trap: `trap 'rm -rf "$PREFIX"' EXIT` immediately after the PREFIX assignment, guarded by a flag that skips cleanup when INSTALL_PREFIX was provided externally (the caller owns externally-specified directories).

79. [ ] **test-quality-reviewer** | `scripts/verify-installed-examples.sh:43` | naming-convention
   The local `fail()` function (line 43) calls `exit 1` immediately. This contradicts the script's own stated goal (line 24: 'Reports all compile failures before exiting non-zero'). For infrastructure failures (missing build dir, missing installed header/library) immediate exit is intentional, but the function is named `fail` identically to the one in check-examples.sh which also exits immediately — making it easy to accidentally call `fail` in the compile loop where the collect-and-continue pattern is required. The compile loop correctly uses the counter pattern and does not call `fail`, so this is not a current bug, but the naming is misleading.
   *Recommendation:* Rename the hard-abort function to `abort_early` or `fatal` to distinguish it from a per-example failure; add a comment noting it is reserved for infrastructure errors.

80. [ ] **test-quality-reviewer** | `scripts/verify-installed-examples.sh:47` | missing-test
   The `make install` output is suppressed with `>/dev/null 2>&1` but the error output is discarded too. If the install step fails, the `fail` message only says 'make install into $PREFIX failed' with no detail about why. On CI, this requires re-running with verbose flags to diagnose install failures.
   *Recommendation:* Capture stderr to a temp file and cat it on failure: `( cd ... && make install ... ) 2>"$install_log" || { cat "$install_log" >&2; fail "..."; }`.

81. [ ] **test-quality-reviewer** | `scripts/verify-installed-examples.sh:65` | non-deterministic
   The AM_CXXFLAGS detection uses `grep -E '^AM_CXXFLAGS' | head -1` on the generated Makefile. If autotools changes the variable name casing, quoting, or splits it across continuation lines (e.g., `AM_CXXFLAGS = \ \n  -DHAVE_GNUTLS`), the grep will silently miss the flags and all feature-gated examples will be skipped rather than compiled. The script then exits 0 with all examples 'skipped', which is a false pass.
   *Recommendation:* Add an assertion that at least one example was actually compiled (`[ $ok -gt 0 ] || fail 'no examples were compiled; flag detection may have failed'`) so a broken feature-flag parse surfaces as a failure rather than a silent all-skip.

82. [ ] **test-quality-reviewer** | `scripts/verify-installed-examples.sh:67` | implementation-coupling
   AM_CXXFLAGS detection uses `grep -E '^AM_CXXFLAGS' | head -1`, which assumes the variable is always on a single line and never uses Automake continuation lines (backslash-newline). Generated Makefiles can emit the variable across multiple lines with leading whitespace on continuations. If the pattern ever misses, all four HAVE_* flags stay 0, all conditional examples are skipped, and the new ok==0 guard on line 154 catches it — but only if skipped>0, making the diagnostic confusing. The comment on line 152 acknowledges 'continuation lines' as a known failure mode, yet the detection code does not handle it.
   *Recommendation:* Use `grep -A5 '^AM_CXXFLAGS'` or join continuation lines with awk before the case statements, so multi-line AM_CXXFLAGS values are handled correctly. Alternatively, switch to reading config.status or config.h which have stable single-line HAVE_ macro definitions.
