# Unworked Review Issues

**Run:** 2026-05-25 21:08:27
**Task:** TASK-048
**Total:** 59 (0 critical, 16 major, 43 minor)

## Major

1. [x] **code-quality-reviewer** | `src/detail/webserver_aliases.cpp:63` | code-elegance
   The alias stubs for auth_handler and method_not_allowed_handler are registered at hook_phase::before_handler, but before_handler only fires inside dispatch_resource_handler (which is only called when found==true AND auth did not short-circuit). The auth stub hook therefore never fires when auth blocks a request (apply_auth_short_circuit sets found=false before dispatch_resource_handler is called). This means the before_handler alias count is misleading: a user who installs auth_handler and queries before_handler hook count gets +1, but that hook slot will never fire for auth-denied requests. The task spec says auth_handler 'internally register a before_handler hook... if the returned http_response is non-empty, return hook_action::respond_with(...)', implying the hook should perform the auth work. The current approach is explicitly described as a stub placeholder, but no test verifies that the auth stub does NOT fire when auth blocks, so the aliasing story is incomplete.
   *Recommendation:* Add a comment in webserver_aliases.cpp explicitly documenting that auth_handler's alias stub fires only for auth-PASSING requests (since dispatch_resource_handler is skipped when apply_auth_short_circuit returns true). Alternatively, add a test that demonstrates this limitation so future tasks have a regression anchor when they graduate the stub to a real hook.
   *Status:* fixed in this batch (commit f9e095d — auth graduated to real functional before_handler hook that fires from finalize_answer BEFORE dispatch_resource_handler; bbb1c3a added DO NOT REMOVE enforcement comment and clarified firing model)

2. [x] **code-quality-reviewer** | `test/integ/hooks_no_firing.cpp:185` | test-coverage
   The updated hooks_no_firing test marks route_resolved and before_handler as 'wired' (returns false from not_yet_wired) but does NOT assert that their counters are positive after a successful round-trip. The test only asserts silence for not-yet-wired phases. A regression that broke route_resolved or before_handler firing would not be caught by this test at all — only by the dedicated new tests. The naming 'all_phases_silent_across_round_trip' is now inaccurate since several phases do fire.
   *Recommendation:* Either rename the test to reflect its narrowed scope ('unwired_phases_silent_across_round_trip') or add positive assertions for the wired phases (route_resolved counter >= 1, before_handler counter >= 1 for a hit request). The former is a low-risk rename; the latter strengthens regression coverage.
   *Status:* fixed in this batch (commit bbb1c3a — renamed to unwired_phases_silent_across_round_trip and added positive assertions for route_resolved and before_handler counters)

3. [x] **code-simplifier** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-048/src/detail/webserver_aliases.cpp:85` | code-structure
   serialize_allow_methods_local duplicates the body of webserver_impl::serialize_allow_methods (webserver_dispatch.cpp:354) exactly. The comment on line 82-84 acknowledges it mirrors the member function but justifies the copy by avoiding a webserver_impl* capture. Both functions iterate the same enum range with identical logic.
   *Recommendation:* Extract the shared logic into a free function in a shared internal header (e.g. src/httpserver/detail/http_method_utils.hpp) and call it from both sites. The free function takes only method_set and returns std::string, so neither caller needs an impl pointer. This removes the acknowledged duplication while keeping each TU dependency-clean.
   *Status:* fixed in this batch (commit bbb1c3a — extracted as detail::format_allow_header in src/httpserver/detail/method_utils.hpp; both serialize_allow_methods and the alias lambda now delegate to it)

4. [x] **code-simplifier** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-048/src/detail/webserver_aliases.cpp:85` | code-structure
   serialize_allow_methods_local is a near-identical copy of webserver_impl::serialize_allow_methods (webserver_dispatch.cpp:354). Both iterate over http_method enum values and build the same comma-separated string. The comment on line 82 even acknowledges the duplication ('Mirrors webserver_impl::serialize_allow_methods without the impl').
   *Recommendation:* Extract the shared logic into a free function in a shared header or .cpp (e.g. detail/method_utils.h), then have both call sites delegate to it. This removes the acknowledged mirror and means any future changes (e.g. adding a new http_method value) only need to be made once.
   *Status:* fixed in this batch (commit bbb1c3a — same fix as finding 3; serialize_allow_methods_local removed, both sites use detail::format_allow_header)

5. [x] **housekeeper** | `specs/tasks/M5-routing-lifecycle/TASK-048.md:11` | action-item-not-marked-complete
   Action item 'Fire `route_resolved` from the end of `webserver_impl::resolve_resource_for_request`...' is implemented (fire_route_resolved_gated called from finalize_answer in src/detail/webserver_request.cpp, firing on both hit and miss branches) but is not checked off.
   *Recommendation:* Mark this action item complete: change `- [ ]` to `- [x]` on line 11 of TASK-048.md.
   *Status:* already addressed by prior implementation commits (2f6cb27, f9e095d — TASK-048.md line 11 is [x])

6. [x] **housekeeper** | `specs/tasks/M5-routing-lifecycle/TASK-048.md:12` | action-item-not-marked-complete
   Action item 'Fire `before_handler` at the start of `webserver_impl::dispatch_resource_handler`...' is implemented (firing site in src/detail/webserver_dispatch.cpp, AFTER mr->pp destroy, BEFORE is_allowed) but is not checked off.
   *Recommendation:* Mark this action item complete: change `- [ ]` to `- [x]` on line 12 of TASK-048.md.
   *Status:* already addressed by prior implementation commits (TASK-048.md line 12 is [x])

7. [x] **housekeeper** | `specs/tasks/M5-routing-lifecycle/TASK-048.md:13` | action-item-not-marked-complete
   Action item 'Short-circuit handling for `before_handler`: returning `hook_action::respond_with(r)` skips the resource invocation...' is implemented (fire_before_handler returns optional<http_response>; a Some value sets mr->response_ and returns early) but is not checked off.
   *Recommendation:* Mark this action item complete: change `- [ ]` to `- [x]` on line 13 of TASK-048.md.
   *Status:* already addressed by prior implementation commits (TASK-048.md line 13 is [x])

8. [x] **housekeeper** | `specs/tasks/M5-routing-lifecycle/TASK-048.md:14` | action-item-not-marked-complete
   Action item '**Alias: `auth_handler(fn)`**...' is implemented (install_default_alias_hooks_ registers a before_handler observation stub when auth_handler is non-null; Doxygen added in create_webserver.hpp) but is not checked off. Note: the implementation uses an observation stub rather than a fully functional hook — the v1 path still handles the actual auth check — which is consistent with the comment in webserver_aliases.cpp.
   *Recommendation:* Mark this action item complete: change `- [ ]` to `- [x]` on line 14 of TASK-048.md.
   *Status:* already addressed by prior implementation commits (TASK-048.md line 14 is [x])

9. [x] **housekeeper** | `specs/tasks/M5-routing-lifecycle/TASK-048.md:15` | action-item-not-marked-complete
   Action item '**Alias: `method_not_allowed_handler(fn)`**...' is implemented (install_default_alias_hooks_ registers a before_handler observation stub when method_not_allowed_handler is non-null; Doxygen added in create_webserver.hpp) but is not checked off.
   *Recommendation:* Mark this action item complete: change `- [ ]` to `- [x]` on line 15 of TASK-048.md.
   *Status:* already addressed by prior implementation commits (TASK-048.md line 15 is [x])

10. [x] **housekeeper** | `specs/tasks/M5-routing-lifecycle/TASK-048.md:16` | action-item-not-marked-complete
   Action item '**Alias: `not_found_handler(fn)`**...' is implemented (install_default_alias_hooks_ registers a route_resolved observation stub when not_found_handler is non-null; Doxygen added in create_webserver.hpp) but is not checked off.
   *Recommendation:* Mark this action item complete: change `- [ ]` to `- [x]` on line 16 of TASK-048.md.
   *Status:* already addressed by prior implementation commits (TASK-048.md line 16 is [x])

11. [x] **housekeeper** | `specs/tasks/M5-routing-lifecycle/TASK-048.md:17` | action-item-not-marked-complete
   Action item 'Doxygen on each alias setter explicitly states: "This is an alias. Calling it registers a hook at `<phase>`..."' is implemented (Doxygen @note blocks added to not_found_handler, method_not_allowed_handler, and auth_handler in src/httpserver/create_webserver.hpp) but is not checked off.
   *Recommendation:* Mark this action item complete: change `- [ ]` to `- [x]` on line 17 of TASK-048.md.
   *Status:* already addressed by prior implementation commits (TASK-048.md line 17 is [x])

12. [x] **housekeeper** | `specs/tasks/M5-routing-lifecycle/TASK-048.md:18` | action-item-not-marked-complete
   Action item 'Per-route hooks at `before_handler` are NOT yet wired (TASK-051) — server-wide only at this task.' is a scope boundary note that should be acknowledged as confirmed/checked off now that the task is done.
   *Recommendation:* Mark this action item complete: change `- [ ]` to `- [x]` on line 18 of TASK-048.md.
   *Status:* already addressed by prior implementation commits (TASK-048.md line 18 is [x])

13. [x] **security-reviewer** | `src/httpserver/hook_context.hpp:76` | insecure-design
   The route_descriptor comment states path_template is 'valid for the lifetime of the registration', but when populated at the dispatch firing site (webserver_dispatch.cpp:386) it is a string_view into mr->matched_path_template — a std::string owned by the per-request modded_request, not the registration. At the cache-fallback path (line 319) it points to mr->standardized_url, again a per-request string. A hook author reading only the header comment could store the string_view beyond the hook's return, causing a use-after-free once the request is torn down. The view is safe only for the duration of the hook call.
   *Recommendation:* Update the route_descriptor comment to say 'valid only for the duration of the hook call; do not store the view beyond the hook's return'. Consider also adding a note at each firing site (webserver_dispatch.cpp:386, webserver_request.cpp:346) with the same caveat.
   *Status:* fixed in this batch (commit bbb1c3a — hook_context.hpp route_descriptor comment updated to say path_template is "valid only for the duration of the hook call"; LIFETIME NOTE added to fire_route_resolved_gated in webserver_request.cpp)

14. [x] **test-quality-reviewer** | `test/integ/hooks_before_handler_short_circuit.cpp:65` | missing-test
   The before_handler_ctx fields (method, resource, matched) are never inspected by either test. The acceptance criterion mandates that the dispatch is observable, but the ctx.method and ctx.resource fields added in TASK-048 are documented contract surface with no integration-tier positive assertion. A future change that zeroes ctx.method or nulls ctx.resource would not be caught.
   *Recommendation:* In the pass_invokes_resource_handler test (or a new third test), capture ctx.method and ctx.resource inside the hook lambda. After ws.stop(), assert ctx.method == http_method::get and ctx.resource != nullptr.
   *Status:* fixed in this batch (commit bbb1c3a — pass_invokes_resource_handler captures ctx.method and ctx.resource into atomics and asserts ctx_method == http_method::get and ctx_resource_non_null after ws.stop())

15. [x] **test-quality-reviewer** | `test/integ/hooks_no_firing.cpp:171` | missing-test
   The not_yet_wired lambda exempts route_resolved and before_handler from the silence check but the test does not positively assert that their counters are > 0 after the round-trip. This means a regression that silences these two newly-wired phases would pass this test undetected. The dedicated per-phase tests cover this independently, so the risk is low, but the sentinel was explicitly designed to document which phases fire on a complete round-trip.
   *Recommendation:* After the loop that checks silence on unwired phases, add positive assertions: LT_CHECK(counters[static_cast<std::size_t>(hook_phase::route_resolved)].load() >= 1) and similarly for before_handler. Matches the pattern TASK-046 and TASK-047 follow by having dedicated tests rather than sentinel gaps — but since this file tracks the wiring frontier explicitly, pinning it here avoids drift.
   *Status:* fixed in this batch (commit bbb1c3a — positive assertions for route_resolved and before_handler counters >= 1 added after the unwired-phase silence loop)

16. [x] **test-quality-reviewer** | `test/integ/hooks_route_resolved_miss_and_hit.cpp:84` | missing-test
   The route_resolved_ctx::resource field is never asserted. The probe_state struct captures is_prefix and path_len from hook A, but ctx.resource is ignored. For the hit case, resource should be non-null; for the miss case it should be null. This is newly-added public API surface (route_resolved_ctx::resource) that has no positive regression test.
   *Recommendation:* Add std::atomic<bool> last_resource_non_null to probe_state. In hook A's hit branch, store ctx.resource != nullptr. After the two requests, assert last_resource_non_null == true for the hit and add a separate miss-probe_state that asserts resource is nullptr.
   *Status:* fixed in this batch (commit bbb1c3a — probe_state gains last_hit_resource_non_null and last_miss_resource_null atomics; assertions added for both the hit and miss resource pointer values)

## Minor

17. [ ] **architecture-alignment-checker** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-048/src/detail/webserver_dispatch.cpp:407` | pattern-violation
   The residual is_allowed + method_not_allowed_page fallback path inside dispatch_resource_handler is intentional when method_not_allowed_handler is not set (zero-cost-when-unused), but the comment at line 383 only mentions 'method_not_allowed_handler alias hook'. A reader may not immediately see that this fallback also runs when any before_handler hook was registered via add_hook but did NOT short-circuit the 405 case (i.e. a user before_handler hook that passes through a disallowed method). In that scenario, dispatch_resource_handler still produces the correct 405 via method_not_allowed_page (which calls the null user handler, returning the default 405 body), so on-wire behavior is correct. But the comment does not fully describe this compound case.
   *Recommendation:* Extend the comment to note that when a user before_handler hook passes through and the method is disallowed, dispatch_resource_handler's own is_allowed fallback still fires (not the alias hook). This is consistent with DR-012's observation that hooks are registration-order chained — a user hook that passes through allows the default fallback to apply. No code change is required; a comment clarification suffices.
   *Status:* deferred — minor comment clarification; not addressed in bbb1c3a

18. [ ] **architecture-alignment-checker** | `src/detail/webserver_dispatch.cpp:381` | pattern-violation
   The before_handler firing site in dispatch_resource_handler manually duplicates the any_hooks_ gate check inline rather than delegating to a fire_before_handler_gated helper (matching the pattern from TASK-047's request_received and body_chunk, which are gated at the call site via the any_hooks_ check before the fire_* call). The gate is correct but the inconsistency between before_handler (gate inline in dispatch) vs route_resolved (gate in static helper) makes the pattern harder to follow.
   *Recommendation:* Follow one consistent pattern across all TASK-048 phases: either always gate at the call site with a raw any_hooks_ check before calling fire_*, or always encapsulate the gate in a dedicated helper. The current mix (before_handler gates inline in dispatch, route_resolved gates in a static helper) is minor but adds cognitive load.
   *Status:* deferred — minor pattern inconsistency; not addressed in bbb1c3a

19. [ ] **architecture-alignment-checker** | `src/detail/webserver_request.cpp:335` | pattern-violation
   fire_route_resolved_gated is defined as a static free function in webserver_request.cpp rather than as a member method of webserver_impl. The established pattern for TASK-046 and TASK-047 hook-firing helpers is to define them as member methods (fire_connection_opened, fire_accept_decision, fire_request_received, fire_body_chunk), declared in webserver_impl_dispatch.hpp and implemented in hook_handle.cpp. The new fire_route_resolved_gated static helper duplicates the any_hooks_ gate inline instead of relying on the member method pattern used by the other phases.
   *Recommendation:* The any_hooks_ gate logic (zero-cost-when-unused) should live at the call site or be factored into the fire_* member method itself. Either inline the gate into fire_route_resolved (as fire_request_received does) and call it directly from finalize_answer, or keep the gated wrapper but make it a member method of webserver_impl following the same pattern as the other phases.
   *Status:* deferred — minor architectural inconsistency; not addressed in bbb1c3a

20. [ ] **architecture-alignment-checker** | `src/detail/webserver_request.cpp:377` | adr-violation
   The task spec (action item 11) says to fire route_resolved from the end of resolve_resource_for_request AND again from the miss branch in finalize_answer. The implementation fires it once from finalize_answer after calling resolve_resource_for_request, covering both hit and miss in a single consolidated firing. This achieves functionally equivalent behavior but does not match the specified dual-site approach. The architecture firing table (§4.10) says the phase fires at 'webserver.cpp:resolve_resource_for_request — after lookup', which the current location in finalize_answer still satisfies as a post-lookup call site.
   *Recommendation:* The single-fire approach is acceptable as an optimization and the behavior is correct. If the task spec's intent for firing from inside resolve_resource_for_request was to ensure the hook fires even in future call paths that skip finalize_answer, consider adding a comment explaining the consolidation rationale. No code change strictly required if the acceptance tests pass.
   *Status:* deferred — spec text not updated to reflect single-site consolidation; bbb1c3a added LIFETIME NOTE comment but did not update the spec wording

21. [ ] **code-quality-reviewer** | `src/detail/webserver_aliases.cpp:165` | code-readability
   The method_not_allowed alias lambda dereferences ctx.request at line 165 without a null guard, while the auth alias lambda at line 121 performs an explicit null check before accessing ctx.request. The asymmetry is not a practical bug (the firing site always provides a valid request pointer from mr->dhr.get()), but it is inconsistent within the same function and creates a subtle maintenance hazard if a future firing site is added with a different contract.
   *Recommendation:* Add a defensive null check at the top of the method_not_allowed lambda body, mirroring the pattern in the auth lambda: `if (ctx.request == nullptr || ctx.resource == nullptr) return hook_action::pass();` — this also makes the existing ctx.resource null-guard redundant and removes its separate early return.
   *Status:* deferred — null guard asymmetry not addressed in bbb1c3a

22. [ ] **code-quality-reviewer** | `src/detail/webserver_aliases.cpp:165` | code-readability
   The method_not_allowed alias lambda dereferences ctx.request at line 165 without a null guard, while the auth alias lambda at line 121 performs an explicit null check before accessing ctx.request. The asymmetry is not a practical bug (the firing site always provides a valid request pointer from mr->dhr.get()), but it is inconsistent within the same function and creates a subtle maintenance hazard if a future firing site is added with a different contract.
   *Recommendation:* Add a defensive null check at the top of the method_not_allowed lambda body, mirroring the pattern in the auth lambda: `if (ctx.request == nullptr || ctx.resource == nullptr) return hook_action::pass();` — this also makes the existing ctx.resource null-guard redundant and removes its separate early return.
   *Status:* deferred — duplicate of finding 21; not addressed in bbb1c3a

23. [ ] **code-quality-reviewer** | `src/detail/webserver_aliases.cpp:66` | code-elegance
   The three alias registrations use std::move on the add_hook return value then immediately call .detach() via member access: 'std::move(add_hook(...)).detach()'. This is correct C++ (rvalue member-function call) but is unusual formatting. The pattern was established in prior tasks but could be more idiomatically written as 'add_hook(...).detach()' since add_hook already returns an rvalue (temporary hook_handle).
   *Recommendation:* Replace 'std::move(add_hook(...)).detach()' with 'add_hook(...).detach()' at all three call sites (lines 66, 78, 91). The explicit std::move is redundant on a function return value (already an rvalue). This is a pure cosmetic improvement with no semantic effect.
   *Status:* deferred — std::move on add_hook return still present in bbb1c3a; not addressed

24. [x] **code-quality-reviewer** | `src/detail/webserver_aliases.cpp:85` | code-elegance
   serialize_allow_methods_local is a near-verbatim duplicate of webserver_impl::serialize_allow_methods (webserver_dispatch.cpp:354). The comment explains the motivation (avoiding webserver_impl* capture), but serialize_allow_methods does not actually use any instance state — its `const` qualifier is nominal and the loop body is purely a function of the method_set argument. Extracting this as a free function in a shared header (or in http_method.hpp where method_set lives) would eliminate the duplication while preserving the lambda's ability to call it without a context pointer.
   *Recommendation:* Move the body of serialize_allow_methods into a free function (e.g., httpserver::detail::format_allow_header(method_set)) declared in a detail header, and have both the dispatch TU and the aliases TU call it. This removes the only DRY violation introduced by this task.
   *Status:* fixed in this batch (commit bbb1c3a — same fix as findings 3 & 4; detail::format_allow_header extracted to method_utils.hpp)

25. [x] **code-quality-reviewer** | `src/detail/webserver_aliases.cpp:85` | code-elegance
   serialize_allow_methods_local is a near-verbatim duplicate of webserver_impl::serialize_allow_methods (webserver_dispatch.cpp:354). The comment explains the motivation (avoiding webserver_impl* capture), but serialize_allow_methods does not actually use any instance state — its `const` qualifier is nominal and the loop body is purely a function of the method_set argument. Extracting this as a free function in a shared header (or in http_method.hpp where method_set lives) would eliminate the duplication while preserving the lambda's ability to call it without a context pointer.
   *Recommendation:* Move the body of serialize_allow_methods into a free function (e.g., httpserver::detail::format_allow_header(method_set)) declared in a detail header, and have both the dispatch TU and the aliases TU call it. This removes the only DRY violation introduced by this task.
   *Status:* fixed in this batch (commit bbb1c3a — duplicate of findings 3 & 4; detail::format_allow_header extracted to method_utils.hpp)

26. [ ] **code-quality-reviewer** | `src/detail/webserver_dispatch.cpp:319` | code-readability
   In the cache-hit path of resolve_resource_for_request, the comment says 'Cache layer dropped the matched endpoint at its API boundary; fall back to the requested URL as a stable approximation of the path_template'. Using the standardized_url as the path_template approximation is correct but subtle — a cache hit on /foo may have been registered as /foo/{id} (regex route). The comment acknowledges this but the consequence (a hook observer sees a non-template string for regex-cache hits) could surprise users. There is no test covering this edge case.
   *Recommendation:* Consider storing the matched endpoint's url_complete in the cache alongside the resource so that cache hits also yield the real path_template. If not fixing now, add a TODO comment referencing this limitation so it is tracked.
   *Status:* deferred — cache-hit path_template approximation not addressed in bbb1c3a

27. [x] **code-quality-reviewer** | `src/detail/webserver_request.cpp:335` | code-readability
   fire_route_resolved_gated is a file-static helper that is only called once (line 377). Its extraction was justified in the comment (keeps finalize_answer under CCN ceiling), which is reasonable. However the function signature takes a raw bool 'found' plus a shared_ptr 'hrm', but inside it checks both 'found && hrm' — the found==true path could still yield a null hrm for lambda-route registrations. The comment in route_resolved_ctx says 'resource is nullable; nullptr for lambda routes', which is consistent, but the two conditions could be simplified: if !found, desc is nullopt and hrm is nullptr, so the single check 'if (found && hrm)' correctly covers both miss and lambda-route cases. The naming 'found' without the lambda-route nuance is slightly misleading.
   *Recommendation:* Add a brief inline comment next to 'if (found && hrm)' explaining that hrm may be null for lambda-route hits, so the condition correctly collapses both miss and lambda-hit into a nullopt descriptor.
   *Status:* fixed in this batch (commit bbb1c3a — LIFETIME NOTE comment block added to fire_route_resolved_gated documenting that "hrm may be null for lambda-route hits; the 'found && hrm' gate covers both the miss path (found==false) and the lambda-route hit (found==true but no http_resource*)")

28. [ ] **code-quality-reviewer** | `src/detail/webserver_request.cpp:335` | code-readability
   The comment on the cache-hit branch (line 332-334) says 'fall back to the requested URL as a stable approximation of the path_template'. This is accurate but slightly misleading: the cache hit does reconstruct the endpoint at the matching layer; the approximation is an implementation constraint of the cache API, not a fundamental property of prefix routes. A reader unfamiliar with the cache internals might incorrectly infer that prefix route descriptors always report the requested URL instead of the registration template.
   *Recommendation:* Clarify the comment: 'Cache API does not expose the original registration key; use the standardized request URL as a best-effort path_template for the hook ctx. For exact-match routes this is always correct; for regex routes it may differ from the registration pattern.'
   *Status:* deferred — cache-hit branch comment not updated in bbb1c3a (comment in dispatch.cpp still reads "fall back to the requested URL as a stable approximation of the path_template")

29. [ ] **code-quality-reviewer** | `src/detail/webserver_request.cpp:335` | code-readability
   The comment on the cache-hit branch (line 332-334) says 'fall back to the requested URL as a stable approximation of the path_template'. This is accurate but slightly misleading: the cache hit does reconstruct the endpoint at the matching layer; the approximation is an implementation constraint of the cache API, not a fundamental property of prefix routes. A reader unfamiliar with the cache internals might incorrectly infer that prefix route descriptors always report the requested URL instead of the registration template.
   *Recommendation:* Clarify the comment: 'Cache API does not expose the original registration key; use the standardized request URL as a best-effort path_template for the hook ctx. For exact-match routes this is always correct; for regex routes it may differ from the registration pattern.'
   *Status:* deferred — duplicate of finding 28; not addressed in bbb1c3a

30. [ ] **code-quality-reviewer** | `src/detail/webserver_request.cpp:388` | code-elegance
   The before_handler firing block in finalize_answer (lines 387-393) reconstructs route_descriptor from mr->matched_path_template using a string_view — identical boilerplate to the route_resolved firing helper fire_route_resolved_gated (lines 343-348). This two-site pattern is small enough that it does not warrant a shared helper today, but it will be repeated again when TASK-051 wires per-route before_handler hooks.
   *Recommendation:* No immediate action needed. When TASK-051 lands, consider factoring out a make_route_descriptor(mr, hrm) helper to reduce the three-way repetition.
   *Status:* deferred — recommendation explicitly defers to TASK-051; not addressed in bbb1c3a

31. [ ] **code-quality-reviewer** | `src/detail/webserver_request.cpp:388` | code-elegance
   The before_handler firing block in finalize_answer (lines 387-393) reconstructs route_descriptor from mr->matched_path_template using a string_view — identical boilerplate to the route_resolved firing helper fire_route_resolved_gated (lines 343-348). This two-site pattern is small enough that it does not warrant a shared helper today, but it will be repeated again when TASK-051 wires per-route before_handler hooks.
   *Recommendation:* No immediate action needed. When TASK-051 lands, consider factoring out a make_route_descriptor(mr, hrm) helper to reduce the three-way repetition.
   *Status:* deferred — duplicate of finding 30; deferred to TASK-051

32. [ ] **code-quality-reviewer** | `src/httpserver/webserver_hooks.hpp:112` | code-readability
   The comment block for install_default_alias_hooks_ says 'The hooks are observation-only stubs whose presence is the alias relationship; the existing inline dispatch code continues to consult the user-supplied callable, so the on-the-wire behaviour is byte-for-byte identical to v1.' This accurately describes the design, but the phrase 'byte-for-byte identical' is stronger than what is actually guaranteed (order of execution and timing of hooks relative to the v1 dispatch path differ). The comment could set incorrect expectations for future maintainers.
   *Recommendation:* Soften the phrase to 'the observable HTTP behaviour is identical to v1' rather than 'byte-for-byte identical', which is overly precise and arguably incorrect when the stub hook itself executes (extra clock cycles, different execution ordering relative to auth).
   *Status:* deferred — "byte identical to v1" phrase still present in webserver_hooks.hpp line 133; not updated in bbb1c3a (note: hooks are now functional not stubs, making the comment doubly stale)

33. [ ] **code-quality-reviewer** | `test/integ/hooks_alias_functional_test.cpp:84` | test-coverage
   The functional-alias integration test covers method_not_allowed_handler short-circuiting but does not include a parallel test case for auth_handler functional short-circuit (i.e., verify that the auth alias hook fires before the resource handler and that a non-null auth response reaches the wire). The existing webserver_register_path_prefix_test.cpp tests exercise the auth flow end-to-end, but they were written before the alias-hook refactor and exercise behaviour rather than the hook-chain ordering specifically.
   *Recommendation:* Consider adding a second test case to hooks_alias_functional_test.cpp (or a sibling file) that registers auth_handler with a rejecting lambda, registers a before_handler user hook after construction (hook[1]), issues a request, and asserts that the resource handler counter is 0 and the user hook counter is 0 — mirroring the pattern used for the method_not_allowed case.
   *Status:* deferred — auth_handler short-circuit integration test not added in bbb1c3a

34. [ ] **code-quality-reviewer** | `test/integ/hooks_alias_functional_test.cpp:84` | test-coverage
   The functional-alias integration test covers method_not_allowed_handler short-circuiting but does not include a parallel test case for auth_handler functional short-circuit (i.e., verify that the auth alias hook fires before the resource handler and that a non-null auth response reaches the wire). The existing webserver_register_path_prefix_test.cpp tests exercise the auth flow end-to-end, but they were written before the alias-hook refactor and exercise behaviour rather than the hook-chain ordering specifically.
   *Recommendation:* Consider adding a second test case to hooks_alias_functional_test.cpp (or a sibling file) that registers auth_handler with a rejecting lambda, registers a before_handler user hook after construction (hook[1]), issues a request, and asserts that the resource handler counter is 0 and the user hook counter is 0 — mirroring the pattern used for the method_not_allowed case.
   *Status:* deferred — duplicate of finding 33; not addressed in bbb1c3a

35. [ ] **code-quality-reviewer** | `test/integ/hooks_before_handler_short_circuit.cpp:38` | test-coverage
   Both test cases (short_circuit_skips_resource_handler and pass_invokes_resource_handler) reuse PORT 8204 with two separate webserver instances that each call ws.start() without a teardown sleep between them. While ws.stop() is called before the second ws.start(), there is a potential port-in-use race on slow CI systems. This is consistent with the pattern in prior hook tests (hooks_request_received_short_circuit.cpp also reuses its port), so it is a known project-wide pattern, not a regression introduced by this task.
   *Recommendation:* For robustness, use distinct ports for each test case in the same file (8204 and 8205, for example). This is a low-priority improvement; the existing pattern has been accepted for prior tasks.
   *Status:* deferred — port reuse pattern unchanged; not addressed in bbb1c3a

36. [ ] **code-simplifier** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-048/src/detail/webserver_request.cpp:335` | naming
   fire_route_resolved_gated is a file-static function whose name encodes an implementation detail (the 'gated' suffix refers to the any_hooks_ short-circuit). External readers unfamiliar with the 'gated' convention have to read the body to understand what the suffix means.
   *Recommendation:* Rename to fire_route_resolved_if_hooks_registered or simply extract the gate inline into finalize_answer and inline the small helper body directly (it is five logical lines). The 'if_hooks_registered' suffix matches the pattern used in naming elsewhere in the codebase for conditional-fire helpers.
   *Status:* deferred — still named fire_route_resolved_gated in current code; not renamed in bbb1c3a

37. [ ] **code-simplifier** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-048/src/detail/webserver_request.cpp:387` | code-structure
   The route_descriptor construction block in finalize_answer's before_handler gate (lines 387-393) is structurally identical to the block in fire_route_resolved_gated (lines 344-348), differing only in the ctx type populated. Having the same 4-field struct-literal in two adjacent helper sites in the same TU is mild repetition.
   *Recommendation:* Extract a make_route_descriptor(mr, hrm) helper that returns std::optional<route_descriptor> and call it from both sites. It is a small pure function with an obvious name that eliminates the duplicated if/string_view/get_allowed_methods/is_prefix pattern.
   *Status:* deferred — make_route_descriptor helper not extracted; deferred to TASK-051 per finding 30/31 recommendation

38. [ ] **code-simplifier** | `src/detail/webserver_aliases.cpp:66` | code-structure
   std::move on an rvalue (the return value of add_hook) is redundant — the value is already an rvalue. Additionally, chaining std::move(...).detach() is less clear than named-variable + detach.
   *Recommendation:* Replace `std::move(add_hook(...)).detach()` with `add_hook(...).detach()` at all three call sites (lines 66, 78, 91). The std::move is a no-op here and adds visual noise.
   *Status:* deferred — std::move on add_hook return still present; not addressed in bbb1c3a (duplicate of finding 23)

39. [ ] **code-simplifier** | `src/detail/webserver_dispatch.cpp:336` | naming
   The static helper fire_route_resolved_gated has the 'gated' suffix baked into its name, but the gating (any_hooks_ check) is a local implementation detail and the suffix adds length without meaningful disambiguation. All other fire_* helpers in hook_handle.cpp are named fire_<phase> without qualification.
   *Recommendation:* Rename `fire_route_resolved_gated` to `fire_route_resolved_if_needed` or keep it as-is if the 'gated' suffix is intentional to distinguish it from the member function `webserver_impl::fire_route_resolved`. If the distinction is needed, a brief comment at the definition site is clearer than encoding it in the name.
   *Status:* deferred — still named fire_route_resolved_gated; not renamed in bbb1c3a (duplicate of finding 36)

40. [ ] **code-simplifier** | `src/detail/webserver_dispatch.cpp:383` | code-structure
   The route_descriptor construction inside dispatch_resource_handler duplicates the same pattern from fire_route_resolved_gated in the same file. The two blocks are near-identical: build an optional<route_descriptor> from matched_path_template/matched_is_prefix/get_allowed_methods, then populate the ctx and fire. The duplication is small but introduces two maintenance points for the same logic.
   *Recommendation:* Extract a small helper (e.g. `make_route_descriptor(mr, hrm)`) that returns `std::optional<route_descriptor>` and call it from both fire_route_resolved_gated and dispatch_resource_handler. This removes the duplicated construction block (~6 lines each).
   *Status:* deferred — make_route_descriptor helper not extracted; deferred to TASK-051 per finding 30/31 recommendation

41. [x] **performance-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-048/src/detail/webserver_dispatch.cpp:420` | memory-allocation
   On the 405 (method not allowed) branch, hrm->get_allowed_methods() is called twice: once at line 387 to build the route_descriptor inside before_handler_ctx (when the any_hooks_ gate is true), and again at line 420 to build the Allow header string. The second call is unconditional. get_allowed_methods() is a trivial noexcept bitset read so the duplicate call is cheap, but the result from the descriptor could be reused to avoid even that.
   *Recommendation:* Hoist get_allowed_methods() into a local variable before the hook-gate block and use it for both the descriptor and serialize_allow_methods. This also makes the data-flow explicit: both uses see the same snapshot of the allowed method mask.
   *Status:* already addressed by f9e095d — before_handler firing moved from dispatch_resource_handler to finalize_answer; in the current code get_allowed_methods() is called only once in dispatch_resource_handler (line 467 for the 405 Allow header), eliminating the duplicate call identified in this finding

42. [ ] **performance-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-048/src/hook_handle.cpp:266` | memory-allocation
   In the per-hook exception handlers inside both fire_hooks_for_phase and fire_short_circuit_hooks_for_phase, the error message is built by concatenating std::string temporaries: std::string("hook[") + std::string(phase_name) + "] threw: " + e.what(). This is on the exception path only (cold), but each concatenation allocates a separate heap buffer. The same pattern appears four times across the two templates.
   *Recommendation:* Use a single std::string with reserve() + append() or an absl::StrCat / fmt::format equivalent if the project already uses one. Alternatively, since these are purely error-log paths, an std::ostringstream or a fixed-size stack buffer with snprintf would avoid all heap allocation. Low urgency given the cold path, but straightforward to fix.
   *Status:* deferred — string concatenation in exception handlers unchanged; not addressed in bbb1c3a

43. [ ] **performance-reviewer** | `src/detail/webserver_aliases.cpp:123` | memory-allocation
   Auth hook lambda copies get_path() string_view into an owned std::string on every authenticated request. get_path() returns std::string_view (http_request.hpp:154), so `std::string path(ctx.request->get_path())` allocates a heap string on every call to this hook, including the common 'skip auth? no, proceed' path before should_skip_auth returns false.
   *Recommendation:* Pass the string_view directly to should_skip_auth by changing its signature to accept std::string_view, or build the string only after the should_skip_auth check returns false (though the current signature requires std::string). Cheapest fix: change should_skip_auth to accept std::string_view and remove the copy: `if (impl_ptr->should_skip_auth(ctx.request->get_path()))`.
   *Status:* deferred — should_skip_auth still takes const std::string&; std::string copy not eliminated in bbb1c3a

44. [x] **performance-reviewer** | `src/hook_handle.cpp:236` | memory-allocation
   kHookSnapshotReserve = 8 is defined but never referenced anywhere in the file. The thread_local snapshot vectors are cleared and then assigned (which may grow capacity as needed), but the reserve constant is dead code. This means the first snapshot assignment on a new thread may trigger a reallocation if more than the default capacity is needed, rather than starting with an 8-entry reservation.
   *Recommendation:* Either call `snapshot.reserve(kHookSnapshotReserve)` after the thread_local declaration (so the initial warm-up on first use pre-allocates 8 slots), or remove the unused constant. The intended pattern was likely `thread_local EntryVec snapshot; if (snapshot.capacity() == 0) snapshot.reserve(kHookSnapshotReserve);` but the reserve call was dropped.
   *Status:* already addressed by prior commits — kHookSnapshotReserve removed in TASK-049 (hook_handle.cpp comment at line 236 documents: "Previously an unused kHookSnapshotReserve constant lived here... Removed in TASK-049 to silence -Wunused-const-variable under --enable-debug.")

45. [x] **security-reviewer** | `src/detail/webserver_aliases.cpp:82` | insecure-design
   The auth_handler alias hook is an observation-only stub (always returns hook_action::pass()) but its install comment says 'auth gate runs in apply_auth_short_circuit at the existing v1 call site in finalize_answer'. This is architecturally correct for iter-2, but a future developer reading the hook bus may not realize the auth decision is NOT actually enforced by this hook and could remove the inline apply_auth_short_circuit path prematurely, creating an auth-bypass. The stub should carry a more prominent 'DO NOT remove the apply_auth_short_circuit call' warning.
   *Recommendation:* Add an explicit comment in the stub body that the hook does NOT enforce auth — it only marks the presence of the handler in the hook registry. The real enforcement is in webserver_impl::apply_auth_short_circuit called from finalize_answer, and that call must not be removed until the hook is graduated.
   *Status:* fixed in this batch (commit bbb1c3a — extensive DO NOT REMOVE comment added; the hook is now fully functional (not a stub), making apply_auth_short_circuit the hook itself; comment now reads "DO NOT remove or replace this hook registration without also providing an equivalent enforcement mechanism. The hook IS the security boundary; there is no separate apply_auth_short_circuit fallback path remaining.")

46. [x] **security-reviewer** | `src/detail/webserver_aliases.cpp:89` | authentication-failures
   The auth_handler alias stub (the before_handler hook installed for the auth_handler v1 setter) is a pure observation no-op that returns hook_action::pass() unconditionally. The real auth gate is the inline apply_auth_short_circuit call in finalize_answer (webserver_request.cpp:379), which runs before dispatch_resource_handler is called. This ordering means the auth check fires correctly before any before_handler hook. However, the stub comment says 'auth gate runs in webserver_impl::apply_auth_short_circuit at the existing v1 call site in finalize_answer', which correctly describes the relationship. The risk is that a future developer who reads only the alias stub may incorrectly conclude the stub IS the auth gate and disable/replace it without realising the real gate is elsewhere. The separation of the alias stub from the actual enforcement is not documented in the header-level Doxygen.
   *Recommendation:* Add a prominent note in install_default_alias_hooks_() and in the create_webserver::auth_handler() Doxygen that the alias stub is NOT the enforcement gate; the enforcement remains in apply_auth_short_circuit with its should_skip_auth path. This prevents a future refactor from silently dropping the real gate.
   *Status:* fixed in this batch (commit bbb1c3a — the distinction is now moot since f9e095d graduated auth to a real functional hook; bbb1c3a added clarifying comment block documenting the hook IS the auth enforcement path with IMPORTANT and DO NOT REMOVE warnings)

47. [ ] **security-reviewer** | `src/detail/webserver_dispatch.cpp:395` | insecure-design
   The before_handler short-circuit can bypass the is_allowed (405) check and the resource handler invocation. This is intentional and documented, but there is no guard preventing a before_handler hook from accidentally sending a 200 response for a method the resource does not allow (e.g. a hook that always returns respond_with(http_response::string('ok')) will suppress the 405 + Allow-header path entirely). The method and resource fields are exposed in before_handler_ctx specifically so a hook can consult them, but there is no enforcement. This is a design trade-off, not an exploitable vulnerability in the library code itself, but it is worth documenting that hooks which short-circuit are responsible for their own method-allowed checking.
   *Recommendation:* Add a warning to the before_handler_ctx Doxygen (hook_context.hpp) that a respond_with() return bypasses the is_allowed check; hooks that short-circuit and wish to preserve 405 semantics must inspect ctx.method against ctx.resource->get_allowed_methods() themselves.
   *Status:* deferred — before_handler_ctx Doxygen does not include the bypass-is_allowed warning; not addressed in bbb1c3a

48. [x] **security-reviewer** | `src/detail/webserver_error_pages.cpp:104` | logging-failures
   internal_error_page with force_our=false surfaces the raw msg string_view directly into the HTTP response body when no internal_error_handler is configured (line 104-107). This was pre-existing behaviour moved unchanged from webserver_dispatch.cpp. In a production deployment where exceptions carry internal file paths, query strings, or stack state in their what() string, this leaks internal information. This is not introduced by TASK-048 but is now visible in its own translation unit, making it a good moment to flag.
   *Recommendation:* Consider replacing the default body with a generic 'Internal Server Error' string and routing the message only to the error logger. At minimum, document in the create_webserver Doxygen that omitting internal_error_handler causes exception messages to appear in HTTP response bodies (CWE-209).
   *Status:* already addressed by 7973dc2 (TASK-036 review) — CWE-209 WARNING comment added to internal_error_page() in webserver_error_pages.cpp documenting the informative-default behavior and instructing operators to wire a constant-returning internal_error_handler before production deployment

49. [x] **security-reviewer** | `src/detail/webserver_request.cpp:330` | insecure-design
   fire_route_resolved_gated takes a string_view (mr->matched_path_template) into a std::string field of modded_request. The string_view lifetime is bounded by the modded_request lifetime, which is safe as long as the hook does not store the view past the call. Since hooks are observation-only for route_resolved per DR-012 and fire synchronously, this is safe in current code. The risk is future hooks that store the view beyond the request lifetime.
   *Recommendation:* Document in the route_resolved_ctx or hook API that path_template string_view is valid only for the duration of the hook call. A static_assert or comment on route_descriptor::path_template would help.
   *Status:* fixed in this batch (commit bbb1c3a — route_descriptor::path_template comment in hook_context.hpp updated to say "valid only for the duration of the hook call"; LIFETIME NOTE block added to fire_route_resolved_gated in webserver_request.cpp)

50. [x] **spec-alignment-checker** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-048/src/detail/webserver_aliases.cpp:null` | action-item
   The fix-pass changes to webserver_aliases.cpp (functional auth_handler and method_not_allowed_handler hooks replacing the observation stubs), webserver_request.cpp (before_handler firing from finalize_answer replacing apply_auth_short_circuit), webserver_dispatch.cpp (before_handler firing removed from dispatch_resource_handler, need_path_template gate added), and the new test file test/integ/hooks_alias_functional_test.cpp are all uncommitted working-tree changes. They exist on disk but are not yet part of the HEAD commit.
   *Recommendation:* Commit the fix-pass changes (webserver_aliases.cpp, webserver_request.cpp, webserver_dispatch.cpp, test/unit/webserver_register_path_prefix_test.cpp, test/Makefile.am, test/integ/hooks_alias_functional_test.cpp) so the implementation matches the committed state before merging to feature/v2.0.
   *Status:* already addressed by f9e095d — "TASK-048: graduate auth/405 aliases to functional hooks + perf" committed all the fix-pass changes

51. [ ] **spec-alignment-checker** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-048/src/detail/webserver_request.cpp:399` | action-item
   Action item says 'Fire before_handler at the start of webserver_impl::dispatch_resource_handler ... AFTER the existing mr->pp destroy step'. The implementation fires before_handler from finalize_answer (before dispatch_resource_handler is entered), so it fires BEFORE the mr->pp destroy step in dispatch_resource_handler. This is a better architectural decision (enables auth/method-not-allowed alias hooks to fire before dispatch), but deviates from the literal action item text.
   *Recommendation:* Update the action item text to reflect the actual firing site (finalize_answer, after resolve_resource_for_request, before dispatch_resource_handler). The implementation ordering is correct and intentional — the action item wording is now stale.
   *Status:* deferred — TASK-048.md action item 12 text still reads "at the start of webserver_impl::dispatch_resource_handler ... AFTER the existing mr->pp destroy step"; spec wording not updated in bbb1c3a

52. [ ] **spec-alignment-checker** | `src/detail/webserver_request.cpp:331` | action-item
   Task spec action item says fire route_resolved from two code locations: 'from the end of webserver_impl::resolve_resource_for_request (hit) and again from the miss branch in finalize_answer (miss)'. The implementation fires route_resolved from a single helper function fire_route_resolved_gated in finalize_answer, using the 'found' boolean to populate ctx.matched accordingly. This is functionally equivalent (one firing per request, hit has matched populated, miss has it nullopt), and the acceptance test confirms the expected semantics. The two-location description in the spec is misleading.
   *Recommendation:* Update the task spec's action item description to reflect the actual single-site gated implementation in finalize_answer. The current implementation is semantically correct; the spec wording is the issue.
   *Status:* deferred — TASK-048.md action item 11 text still describes dual-site firing; spec wording not updated in bbb1c3a

53. [ ] **spec-alignment-checker** | `src/httpserver/hook_context.hpp:164` | specification-gap
   Task spec specifies route_resolved_ctx with two fields: '{ const http_request& req; std::optional<route_descriptor> matched; }'. The implementation has three fields: request (const http_request*), matched (std::optional<route_descriptor>), and resource (const http_resource*, nullable). The field names use pointer semantics instead of references. The extra 'resource' field is additive and not breaking. The pointer-vs-reference difference is intentional (nullable miss path). No acceptance criterion is violated.
   *Recommendation:* Minor spec vs implementation divergence. Consider updating the spec's context struct description to match the actual struct shape (pointer fields, extra resource field). No code change needed.
   *Status:* deferred — spec description not updated; no code change needed but spec text remains divergent

54. [ ] **spec-alignment-checker** | `src/httpserver/hook_context.hpp:184` | specification-gap
   Task spec specifies before_handler_ctx with three fields: '{ http_request& req; const http_resource& resource; http_method method; }'. The implementation has four fields: request (http_request*), matched (std::optional<route_descriptor>), method (http_method), resource (const http_resource*). The extra 'matched' field and pointer semantics are deviations from the spec description. The before_handler_ctx_shape_test pins the actual shape. No acceptance criterion violated.
   *Recommendation:* Update the spec's before_handler_ctx description to include the matched field and use pointer notation for nullable fields. No code change needed.
   *Status:* deferred — spec description not updated; no code change needed but spec text remains divergent

55. [ ] **test-quality-reviewer** | `test/integ/hooks_alias_functional_test.cpp:1` | missing-test
   Only the method_not_allowed alias is covered at the functional integration tier in the new test. The auth_handler alias's short-circuit behavior (returning a non-null http_response from auth_handler causes a 401/403 wire response and skips the resource) has no dedicated integration test — it is only covered at the unit count tier (hooks_alias_count_test.cpp).
   *Recommendation:* Add a test case that registers auth_handler returning a 401 response, issues a request, and asserts the 401 arrives on the wire and the resource's render_get is not called.
   *Status:* deferred — auth_handler short-circuit integration test not added in bbb1c3a (same as finding 33)

56. [x] **test-quality-reviewer** | `test/integ/hooks_alias_functional_test.cpp:84` | missing-test
   No integration test directly asserts that before_handler_ctx.resource is non-null and ctx.method equals the request method on a route hit. The alias functional test exercises these fields indirectly (the method_not_allowed lambda calls ctx.resource->is_allowed(ctx.method)), but no test deliberately reads and checks both field values to confirm the firing site populates them correctly.
   *Recommendation:* Add a second test case in hooks_alias_functional_test.cpp (or hooks_before_handler_short_circuit.cpp) that captures ctx.resource and ctx.method into atomics, performs a GET on a registered endpoint, and asserts resource != nullptr and method == http_method::get after ws.stop().
   *Status:* fixed in this batch (commit bbb1c3a — pass_invokes_resource_handler in hooks_before_handler_short_circuit.cpp now captures ctx.method and ctx.resource into atomics and asserts ctx_method == http_method::get and ctx_resource_non_null after ws.stop())

57. [ ] **test-quality-reviewer** | `test/integ/hooks_before_handler_short_circuit.cpp:80` | slow-test
   Both integration tests use a fixed sleep_for(50ms) after ws.start(false) to wait for the server to be ready. This is a common pattern in this codebase (hooks_no_firing.cpp does the same), but it is a timing dependency that can cause flakiness on heavily loaded CI machines. The 50ms figure is hardcoded with no retry logic.
   *Recommendation:* This is an established project convention (hooks_no_firing.cpp:148, hooks_route_resolved_miss_and_hit.cpp:119 use the same pattern) so this is minor and consistent with the existing test suite style. If CI flakiness emerges, a spin-wait that retries the curl request on CURLE_COULDNT_CONNECT would be more robust.
   *Status:* deferred — established project convention; not addressed in bbb1c3a

58. [x] **test-quality-reviewer** | `test/integ/hooks_no_firing.cpp:72` | naming-convention
   The test is named all_phases_silent_across_round_trip but as of TASK-048 seven phases are explicitly NOT silent (connection_opened, accept_decision, connection_closed, request_received, body_chunk, route_resolved, before_handler). The name has been widening in meaning since TASK-046; it now describes only the un-wired remainder.
   *Recommendation:* Rename the test to something like unwired_phases_silent_across_round_trip or remaining_phases_fire_nothing to accurately reflect that it is a sentinel for phases not yet wired, not a claim that all phases are silent.
   *Status:* fixed in this batch (commit bbb1c3a — test renamed from all_phases_silent_across_round_trip to unwired_phases_silent_across_round_trip)

59. [ ] **test-quality-reviewer** | `test/unit/hooks_alias_count_test.cpp:58` | missing-test
   The baseline_has_no_alias_hooks test validates zero counts at construction. There is no test for the case where auth_handler is set to nullptr explicitly (i.e., .auth_handler(nullptr)) — the install_default_alias_hooks_ code guards on !nullptr, so a null callable must not register a hook. This edge case is documented in the comment at the top of the file but has no corresponding test.
   *Recommendation:* Add a test: create_webserver(8205).auth_handler(nullptr) should still yield before_handler_count == 0, verifying the null-callable guard in install_default_alias_hooks_.
   *Status:* deferred — nullptr guard test not added in bbb1c3a
