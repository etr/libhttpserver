# Unworked Review Issues

**Run:** 2026-05-25 11:49:27
**Task:** TASK-047
**Total:** 33 (0 critical, 6 major, 27 minor)

## Major

1. [x] **code-quality-reviewer** | `src/detail/webserver_body_pipeline.cpp:83` | correctness
   The `offset` field in `body_chunk_ctx` is populated with `mr->dhr->get_content().size()` BEFORE the chunk is appended, which is correct for a monotone offset. However, if `put_processed_data_to_content` is false AND `mr->pp != nullptr`, `grow_content` is not called (line 197-199), so on the NEXT chunk invocation `get_content().size()` will still reflect the pre-append size — the offset will be wrong (not accumulating). The acceptance test only exercises the case where `mr->pp == nullptr` (plain POST body), so this mismatch is not caught by the current test suite.
   *Recommendation:* Maintain a separate `std::uint64_t mr->body_bytes_seen` counter (incremented every time a chunk passes through the hook, regardless of whether `grow_content` is called) and use that for `ctx.offset` instead of `mr->dhr->get_content().size()`.
   *Status:* Resolved — `body_bytes_seen` field added to `modded_request.hpp:93`; `fire_and_maybe_short_circuit_body_chunk` now sources `ctx.offset` from it and increments unconditionally (`webserver_body_pipeline.cpp:87,89`), with the inline comment documenting the correctness rationale exactly as recommended.

2. [x] **code-simplifier** | `src/detail/webserver_body_pipeline.cpp:126` | code-structure
   The any_hooks_ gate in requests_answer_first_step is accessed with an inline static_cast<std::size_t>(hook_phase::request_received) expression at the call site, while the comment above the gate already documents the pattern. The same pattern is repeated at line 180 for body_chunk. Both gate checks embed the same verbose index expression that also appears in webserver_callbacks_lifecycle.cpp for TASK-046 phases, making the pattern harder to read and change consistently.
   *Recommendation:* Extract a small inline helper, e.g. bool webserver_impl::has_hooks_for(hook_phase p) const noexcept { return any_hooks_[static_cast<std::size_t>(p)].load(std::memory_order_relaxed); }, and replace each gate at its call site with if (has_hooks_for(hook_phase::request_received)). This matches the project's existing pattern of extracting one-liner complexity into named helpers and makes the guard readable without requiring the reader to parse the cast.
   *Status:* Resolved — `has_hooks_for(hook_phase)` inline helper added in `webserver_impl.hpp:307-310` with exactly the recommended signature; both body-pipeline call sites (`webserver_body_pipeline.cpp:136,188`) now use it.

3. [x] **code-simplifier** | `src/hook_handle.cpp:121` | code-structure
   hook_handle::remove() repeats erase_if_found(...) + reset_gate_if_empty(...) for every case in an 11-arm switch. The two lambdas are defined once, but each arm is two lines that must be updated in lock-step whenever a new phase is added. The pattern is mechanical enough to invite an omission (see the count_ arm, which already diverges). The TASK-048..051 phases will each add another arm.
   *Recommendation:* Introduce a per-phase accessor that maps hook_phase to the matching (hook_vec&, atomic_bool&) pair — e.g. a small lookup table or a helper method — and replace the switch with a single two-line call site. This makes adding a new phase a one-line table entry rather than a two-line switch arm.
   *Fixed:* Collapsed the two-lambda pattern (`erase_if_found` + `reset_gate_if_empty`) into a single `erase_and_reset(vec)` lambda that pairs the erase with the gate-clear. Each switch arm is now a one-liner. The fully-typed switch is retained (one-line comment block explains why a table lookup would need `std::visit` over a tuple-of-vectors and is deferred until the phase set stabilises post-TASK-051).

4. [x] **code-simplifier** | `src/hook_handle.cpp:248` | patterns
   fire_hooks_for_phase and fire_short_circuit_hooks_for_phase share identical structure: snapshot-under-lock with reserve(kHookSnapshotReserve), iterate with inner try/catch logging to log_dispatch_error, outer catch for snapshot failure. The only differences are the entry function signature (void vs hook_action) and whether the loop breaks early. This duplication means any change to the shared skeleton (e.g. lock type, reserve size, error message format) must be applied twice.
   *Recommendation:* Extract the snapshot-under-lock step into a small helper: template<typename Entry> static std::vector<Entry> snapshot_hooks(webserver_impl* impl, std::vector<Entry>& hook_vec). Then fire_hooks_for_phase and fire_short_circuit_hooks_for_phase each call it and only own the iteration policy. Alternatively, collapse into a single template that accepts a Visitor callable — the visitor returns bool (true = stop) so the void variant always returns false.
   *Fixed:* Extracted the three log-formatting blocks (`hook[…] threw: …`, `hook[…] threw unknown exception`, `…[…]: snapshot copy failed`) into free helpers `log_hook_threw`, `log_hook_threw_unknown`, `log_snapshot_failed`. Both templates now share the same one-line catch arms — one allocation per log call, no per-instantiation divergence. The two outer template skeletons stay intact because of the thread_local snapshot optimisation (TASK-048) and the void/hook_action signature divergence; a full visitor-based collapse is the right next step but needs a focused refactor task. The handler_exception variant in the same file intentionally stays open-coded due to its const-ctx + alias tail; its strings are unaffected by this change.

5. [x] **code-simplifier** | `test/integ/hooks_request_received_short_circuit.cpp:48` | needless-repetition
   The writefunc curl write callback and PORT macro are duplicated across at least three of the five new TASK-047 test files (hooks_request_received_short_circuit.cpp, hooks_body_chunk_short_circuit_no_leak.cpp, hooks_body_chunk_observes_progress.cpp). Each defines its own identical writefunc and a different-valued PORT #define, producing per-TU boilerplate that is identical except for the port number.
   *Recommendation:* Extract writefunc into the shared test/integ/consumer_fixture.cpp (or a new test/integ/curl_helpers.hpp) that the TASK-046/047 test files already partially share. Port numbers should be defined in a single port-allocation header to prevent future collisions when new tests are added.
   *Fixed:* Added `test/integ/curl_helpers.hpp` exporting `httpserver_test::writefunc`. The three TASK-047 integ tests now `#include "test/integ/curl_helpers.hpp"` and `using httpserver_test::writefunc;` in their local anonymous namespace; the per-TU body of `writefunc` is gone. Listed in `test/Makefile.am` `noinst_HEADERS` for dist hygiene. PORT allocation pre-dates this scope and remains a per-TU `#define` (a unified port-allocation header is a separate cross-cutting cleanup applied to the entire integ suite, ~20 files).

6. [x] **performance-reviewer** | `src/hook_handle.cpp:319` | memory-allocation
   fire_short_circuit_hooks_for_phase() always heap-allocates the snapshot vector via snapshot.reserve(kHookSnapshotReserve=8) on every invocation, including body_chunk which fires once per MHD chunk delivery. On slow networks chunks may arrive one byte at a time, making this potentially thousands of heap allocations per request. The any_hooks_ atomic gate is correctly checked by the CALLER before entering the template, but the template itself has no fast-empty-path check before the allocation.
   *Recommendation:* Add if (hook_vec.empty()) return std::nullopt; at the top of fire_short_circuit_hooks_for_phase() before the snapshot construction (likewise in fire_hooks_for_phase). Also consider calling snapshot.reserve(hook_vec.size()) instead of the fixed constant 8 to avoid over-allocating, or use a stack-allocated small-buffer for the common single-hook case (e.g. a fixed std::array<...,4> falling back to heap only when count > 4).
   *Status:* Resolved — TASK-048 perf rework removed `kHookSnapshotReserve` entirely (see comment at `hook_handle.cpp:269-273`) and replaced the per-call reserve with a `thread_local EntryVec snapshot` that is `clear()`ed and reused. Both fire templates now early-exit under the shared_lock when `hook_vec.empty()` (`hook_handle.cpp:299, 371`), so the warm-path body_chunk firing causes no heap allocations after the first call per thread.

## Minor

7. [x] **architecture-alignment-checker** | `specs/architecture/04-components/hooks.md:11` | pattern-violation
   The table entry for `request_received` lists the firing site as `webserver_body_pipeline.cpp:requests_answer_first_step`. The actual function fires only when `any_hooks_[request_received].load(relaxed)` is true (atomic gate present), so the description 'post header-parse, pre body-read' is accurate. However, the table does not mention the `any_hooks_` gate caveat for these two phases while the other phases column is silent too — this is consistent across all rows so no inconsistency.
   *Recommendation:* No action required; the table is internally consistent. If future maintainers add per-phase firing-condition notes they should include the atomic gate pattern for all phases uniformly.
   *Status:* No action — finding explicitly states no inconsistency; the architecture doc still uses the uniform pattern described and the §4.10 narrative below the table documents `any_hooks_` as the shared gating mechanism for every phase.

8. [ ] **architecture-alignment-checker** | `src/detail/webserver_body_pipeline.cpp:83` | pattern-violation
   The body_chunk_ctx is constructed with is_final=false unconditionally for every chunk. Per the hook_context.hpp docstring, is_final is meant to signal the last chunk. MHD's upload callback is invoked with *upload_data_size > 0 for each non-final chunk and *upload_data_size == 0 to signal completion; since the body_chunk hook is only fired when *upload_data_size > 0 (the second_step guards on this), is_final is always false here. The architecture doc §4.10 does not specify how is_final is determined, but the hook_context.hpp comment implies it should be set to true on the final chunk. This is a minor gap — the field is always false and users relying on it to detect the final chunk will be misled.
   *Recommendation:* Either detect the final chunk (e.g., by comparing offset + chunk.size() against the declared Content-Length, or by a flag set when MHD calls back with size==0) and set is_final accordingly, or update the hook_context.hpp docstring to explicitly document that is_final is currently always false and will be implemented in a future task.
   *Status:* deferred — `is_final` remains hardcoded false in `webserver_body_pipeline.cpp:88` and the public `hook_context.hpp:197` docstring does not yet warn callers about this MHD limitation. The accepted relaxation in `hooks_body_chunk_observes_progress.cpp:129` documents the workaround at the test layer but the public-API caveat still needs to be added.

9. [x] **code-quality-reviewer** | `src/detail/webserver_body_pipeline.cpp:126` | code-elegance
   The `any_hooks_` atomic gate check in `requests_answer_first_step` (lines 126-137) is done with a verbose `static_cast<std::size_t>` index expression. The equivalent guard in `requests_answer_second_step` (lines 180-188) uses the same pattern. The project already has a helper pattern for this in TASK-046 code; a named helper function like `has_hooks_for(hook_phase)` would reduce noise and potential indexing errors.
   *Recommendation:* Add a small inline helper `bool has_hooks_for(hook_phase p) const noexcept` to `webserver_impl` that encapsulates the `any_hooks_[static_cast<std::size_t>(p)].load(std::memory_order_relaxed)` expression, consistent with the DRY principle.
   *Status:* Resolved — same as item #2: `webserver_impl::has_hooks_for` exists at `webserver_impl.hpp:307-310` and both gate sites use it.

10. [ ] **code-quality-reviewer** | `src/detail/webserver_body_pipeline.cpp:144` | readability
   Long compound boolean expression for the post-encoding check (lines 148-150) spans three lines with deep nesting. The logic for matching the two MIME types is hard to parse at a glance and would benefit from a named predicate or helper variable that says 'is_form_encoded_content_type'.
   *Recommendation:* Extract the strncasecmp pair into a named bool 'is_form_content_type' before the outer if, reducing nesting and making the intent immediately obvious.
   *Status:* wontfix — cosmetic/style preference, no functional impact; pure readability polish on legacy code; not on critical path

11. [ ] **code-quality-reviewer** | `src/detail/webserver_body_pipeline.cpp:22` | comments
   The file-level block comment (lines 22-31) is longer than needed and partially restates the task history that belongs in the commit message or task spec, not in the source. The comment for `fire_and_maybe_short_circuit_body_chunk` (lines 66-73) is a multi-line block that largely restates what the function name and return type already convey.
   *Recommendation:* Trim file-level comment to the single-sentence architectural justification. Shorten the helper comment to a one-liner: `// Returns true iff a hook short-circuited; also tears down mr->pp.`
   *Status:* deferred — both comments are still in their original verbose form (`webserver_body_pipeline.cpp:21-30, 68-75`). The wider repo convention has kept TASK-tag rationale in code; trimming should batch with item #17.

12. [ ] **code-quality-reviewer** | `src/detail/webserver_body_pipeline.cpp:84` | code-readability
   `/*is_final=*/false` is hardcoded unconditionally. The comment in `hook_context.hpp` acknowledges MHD does not always signal is_final in the upload callback, but `is_final` is always `false` here — the field appears in the public API but is never set to `true` in any firing site, which may mislead hook authors.
   *Recommendation:* Either remove `is_final` from `body_chunk_ctx` for now (breaking change, needs task), or document clearly in the firing-site comment that `is_final` is always `false` in the current implementation and is reserved for a future MHD integration, so hook authors know not to rely on it.
   *Status:* deferred — duplicate of #8. The firing site (`webserver_body_pipeline.cpp:88`) and the public `body_chunk_ctx` definition (`hook_context.hpp:193-198`) still lack the explicit "currently always false" caveat. Tracked alongside #8.

13. [ ] **code-quality-reviewer** | `test/integ/hooks_body_chunk_observes_progress.cpp:129` | test-coverage
   The comment at line 129 says 'MHD does not always signal is_final in the upload-callback; we relax to last entry offset+size equals body.size()'. The is_final field on body_chunk_ctx is therefore never verified to be true in any test, leaving its behaviour untested. This is an acknowledged limitation but worth noting.
   *Recommendation:* If future MHD versions or configurations do set is_final reliably, add an optional assertion. For now, the comment-driven relaxation is acceptable; document in the task spec that is_final testing is deferred.
   *Status:* deferred — relaxation comment remains at `hooks_body_chunk_observes_progress.cpp:129-131`; no `is_final == true` assertion exists. Tied to #8/#12 — once the firing-site sets `is_final` correctly the test can be tightened.

14. [ ] **code-quality-reviewer** | `test/integ/hooks_request_received_short_circuit.cpp:136` | test-coverage
   The `small_body_passes_through` sub-test creates a second `webserver` on the same PORT (8200) immediately after the first `ws.stop()`. There is no inter-test delay or port-reuse teardown guarantee; on a loaded CI host the OS TIME_WAIT on that port may cause the second test's `ws.start()` to silently fail or bind to a different interface, making the assertions vacuously pass.
   *Recommendation:* Either use a different port for each sub-test (e.g. 8200 / 8203) or add a brief `SO_REUSEADDR`-style note confirming the server is configured with port reuse. The pattern in TASK-046 tests uses a per-file unique port.
   *Status:* deferred — both sub-tests still share PORT 8200 (`hooks_request_received_short_circuit.cpp:79,139`). Low-impact CI flake risk; better addressed as part of a wider test-suite port-allocation pass (alongside #5).

15. [ ] **code-quality-reviewer** | `test/integ/hooks_request_received_short_circuit.cpp:76` | code-elegance
   The two test cases (oversize_body_short_circuits_before_body_read and small_body_passes_through) duplicate the webserver setup, hook registration, resource wiring, curl initialization, and curl teardown code almost verbatim. The only differences are the body size and the expected HTTP status code.
   *Recommendation:* Extract a shared helper (e.g. a local lambda or a free function) that accepts body size and expected status, reducing the duplicated CURL boilerplate. The existing test framework (littletest) may not support parameterized tests natively, but the curl block at minimum could be a helper function.
   *Status:* deferred — both sub-tests still inline their identical setup. Same scope-cluster as #5/#14 (integ-suite-wide helper extraction).

16. [ ] **code-simplifier** | `src/detail/webserver_body_pipeline.cpp:126` | code-structure
   requests_answer_first_step contains an inline TASK-047 comment block (lines 119-125) that explains the MHD short-circuit flow in detail. While accurate, the comment describes the mechanism rather than the intent, and is longer than the code it annotates (6 comment lines for 5 code lines). The code itself is clear from the variable names (skip_handler, fire_request_received, response_.emplace).
   *Recommendation:* Trim to a single-line intent comment: // request_received hook: short-circuit before body is read. The detailed MHD callback-sequencing description belongs in the design document (TASK-047.md), not inline.
   *Status:* deferred — the long comment block remains at `webserver_body_pipeline.cpp:127-135`. Same comment-trimming cluster as #11/#17.

17. [ ] **code-simplifier** | `src/detail/webserver_body_pipeline.cpp:21` | comments
   The 11-line block comment at the top of webserver_body_pipeline.cpp (lines 21-31) explains the file's extraction history in terms of TASK numbers. While useful during the task, TASK references as primary documentation are opaque to future readers who lack the task tracker context. The comment mixes 'why this file exists' (useful) with 'which task moved it here' (redundant with git history).
   *Recommendation:* Trim the comment to the functional rationale only: 'Two body-pipeline stages extracted from webserver_request.cpp to keep that TU under FILE_LOC_MAX after the hook firing sites were added.' The TASK references belong in the git commit message, not the source file.
   *Status:* deferred — file-top block at `webserver_body_pipeline.cpp:21-30` still references TASK numbers. The TASK-021 review pass elected to keep similar TASK-tag context in other files; revisit globally rather than per-file.

18. [x] **code-simplifier** | `src/detail/webserver_body_pipeline.cpp:36` | patterns
   The include list contains <chrono>, <cstdint>, <cstddef>, <iostream>, <optional>, <span>, <string>, <utility> alongside <cstring> and <strings.h>. The <iostream> include is only used inside a #ifdef DEBUG block (line 191). All other headers are used in production code. Keeping <iostream> unconditionally included adds a compile-time cost to all non-debug builds.
   *Recommendation:* Move #include <iostream> inside #ifdef DEBUG ... #endif so it is only pulled in when the debug path is active. This matches the conditional use and the pattern used for the DEBUG block itself.
   *Status:* Resolved — `#include <iostream>` is now wrapped in `#ifdef DEBUG ... #endif` at `webserver_body_pipeline.cpp:43-45`, matching the recommendation exactly.

19. [x] **code-simplifier** | `src/detail/webserver_body_pipeline.cpp:80` | patterns
   The span construction in fire_and_maybe_short_circuit_body_chunk uses reinterpret_cast<const std::byte*>(upload_data) with upload_data_size. The upload_data pointer is a const char*; the cast and size are split across two lines, making the span construction verbose. The same pattern does not appear elsewhere in the changed files (it is unique to this firing site), but it is more verbose than idiomatic C++20 span-from-range.
   *Recommendation:* Use std::as_bytes on a std::span<const char>: std::as_bytes(std::span<const char>(upload_data, upload_data_size)). This removes the reinterpret_cast entirely and makes the intent (treat the char buffer as bytes) explicit via a named standard-library function.
   *Status:* Resolved — the construction now uses `std::as_bytes(std::span<const char>(upload_data, upload_data_size))` at `webserver_body_pipeline.cpp:86`; no `reinterpret_cast` remains in the file.

20. [ ] **code-simplifier** | `src/hook_handle.cpp:74` | naming
   The function fire_and_maybe_short_circuit_body_chunk is descriptive but long. The 'and_maybe' portion is implicit from the return type (bool) and the optional short-circuit pattern already established by fire_short_circuit_hooks_for_phase. The name is not wrong, but it is one of the longer function identifiers in the codebase.
   *Recommendation:* Consider renaming to apply_body_chunk_hook or fire_body_chunk_with_short_circuit to match the naming style of other internal helpers in this file. This is purely a readability polish; functionality is unaffected.
   *Status:* deferred — function still named `fire_and_maybe_short_circuit_body_chunk` (anon-ns helper at `webserver_body_pipeline.cpp:76`). Naming-only polish, no behaviour impact.

21. [x] **housekeeper** | `specs/tasks/_index.md:6` | documentation-stale
   The task index 'Last updated' date remains 2026-05-18, which predates the TASK-047 implementation and the iter2 doc-fix commit. This is cosmetic and does not obscure any tracking information.
   *Recommendation:* Optionally bump 'Last updated' to today's date when making the next doc-only commit to keep the header current.
   *Status:* Resolved — `specs/tasks/_index.md:4` now reads `**Last updated:** 2026-05-27` (post-TASK-047 review work).

22. [x] **performance-reviewer** | `src/hook_handle.cpp:255` | memory-allocation
   fire_hooks_for_phase() (void variant for connection_opened/accept_decision/connection_closed) also always calls snapshot.reserve(kHookSnapshotReserve=8) without checking hook_vec.empty() first. The any_hooks_ gate in callers guards this normally, but a defensive empty-check inside the template would eliminate the allocation in any race between gate-clear and vector-erase, and makes the template safe to call directly in tests.
   *Recommendation:* Add if (hook_vec.empty()) return; before the snapshot allocation in fire_hooks_for_phase(), mirroring the same fix recommended for fire_short_circuit_hooks_for_phase().
   *Status:* Resolved — `fire_hooks_for_phase` now performs `if (hook_vec.empty()) return;` under the shared_lock at `hook_handle.cpp:299`; mirror fix in the short-circuit variant at line 371. Same TASK-048 perf rework also removed the fixed-size reserve in favour of a `thread_local` snapshot buffer.

23. [x] **performance-reviewer** | `src/hook_handle.cpp:264` | memory-allocation
   log_dispatch_error() path inside both fire helpers constructs a std::string via multiple concatenations (std::string("hook[") + std::string(phase_name) + "] threw: " + e.what()), creating three or four temporaries per exception. While exception paths are not hot, this pattern is inconsistent with a library that explicitly targets hot-path allocation reduction.
   *Recommendation:* Build the error string with a single reserve-and-append: std::string msg; msg.reserve(64); msg += "hook["; msg += phase_name; msg += "] threw: "; msg += e.what(); impl->log_dispatch_error(msg);
   *Status:* Resolved — TASK-049 review reworked both fire helpers to use `std::string("hook[").append(phase_name).append("] threw: ").append(e.what())` (`hook_handle.cpp:309-311, 313-315, 382-384, 386-388`), eliminating the chain of intermediate temporaries. Comment at line 306 explicitly references the review note.

24. [ ] **security-reviewer** | `examples/early_413.cpp:35` | input-validation
   Content-Length is read as a raw header string and parsed with std::stoull. The catch-all `catch(...)` correctly handles malformed values, but std::stoull on a syntactically valid but astronomically large value (e.g. '99999999999999999999') will throw std::out_of_range which is caught by the catch block — correct. However, a value of '0' passes the `> kMaxUploadBytes` test and falls through to pass(), which is correct but not explicitly tested. The silent pass for overflow is the documented fallback ('let the normal pipeline produce a 400 elsewhere'), which may not hold if the pipeline does not independently validate Content-Length. Low exploitability since MHD validates its own framing, but the implicit reliance on a downstream 400 is undocumented.
   *Recommendation:* Add a comment clarifying that overflow/underflow from std::stoull is intentionally delegated to MHD's own framing validation. Consider adding a test case for a malformed Content-Length string to make the fallback behavior explicit. CWE-20 (Improper Input Validation).
   *Status:* deferred — example comment at `examples/early_413.cpp:42-45` still reads only "Malformed Content-Length: let the normal pipeline produce a 400 elsewhere"; the CWE-20 caveat about delegating to MHD framing isn't called out. Documentation polish on an example, no security impact (the example is illustrative; library has its own `content_size_limit` setting).

25. [ ] **security-reviewer** | `examples/early_413.cpp:38` | input-validation
   Content-Length enforcement relies solely on a hook registered at application startup with no server-side cap enforced by the library itself. If a caller forgets the hook (or a future version of the example changes `kMaxUploadBytes`), the library will buffer arbitrarily large bodies limited only by memory. The example is not part of the library's enforcement path — it is documentary. The comment in hook_context.hpp correctly warns about chunk-level hooks being per-chunk, but there is no in-library default cap backstop.
   *Recommendation:* Document clearly in hook_context.hpp (and the reference manual) that request_received hook-based Content-Length gating is advisory; applications that require a hard cap should also configure the webserver's own content_size_limit (set_content_size_limit) as a defense-in-depth layer. The example should show both: the early-reject hook AND the library-level size limit. CWE-400 (Uncontrolled Resource Consumption).
   *Status:* deferred — `examples/early_413.cpp` still only demonstrates the hook path; the `create_webserver::content_size_limit(...)` defense-in-depth layer exists in the library (`create_webserver.hpp:144`) but isn't shown alongside. Documentation/example enhancement.

26. [ ] **security-reviewer** | `src/hook_handle.cpp:325` | concurrency
   fire_short_circuit_hooks_for_phase snapshots the hook vector under a shared_lock and then iterates the snapshot outside the lock. This correctly allows add_hook / remove() re-entrancy during hook execution (per the design comment). However, if a hook calls handle.remove() on its own slot_id (self-removal), the removal succeeds against the live vector while the snapshot iterator still holds a copy of the entry. The hook may fire once even after self-removal — this is likely the intended 'fire the current snapshot once' semantics but it is not documented. If a hook expects that calling remove() inside the hook's own body prevents further invocations of that same hook within the same firing batch, that expectation is violated for multi-hook chains.
   *Recommendation:* Document explicitly in hook_handle.hpp and hook_context.hpp that self-removal from within a hook body takes effect for future firing batches, not the currently executing batch. CWE-362 (Race Condition) — low severity since the consequence is at most one extra invocation, not a security exploit.
   *Status:* deferred — the snapshot-under-lock comment at `hook_handle.cpp:260-267` documents the re-entrancy semantics but does not explicitly state the "self-removal does not affect the currently executing batch" rule for public consumers; `hook_handle.hpp` / `hook_context.hpp` still lack the caveat. Low-risk doc gap.

27. [ ] **security-reviewer** | `src/httpserver/hook_context.hpp:156` | insecure-design
   body_chunk_ctx::chunk is std::span<const std::byte> aliasing MHD-owned memory. The header comment correctly warns 'only valid for the duration of the hook call; copy into owned storage if it must outlive this firing.' However, the compiler cannot enforce this — a hook that captures the span by value and stashes it in a shared variable (e.g., a member field) will produce a dangling reference after the MHD callback returns. Since hook lambdas are user-written arbitrary code, this is a latent UAF surface if the documentation is missed. The same concern applies to request_received_ctx::request — the mutable pointer remains valid across the hook call but has no documented upper bound on its validity beyond 'during the callback'.
   *Recommendation:* Strengthen the header comment to explicitly state this is UB if the span escapes the hook call. Consider marking body_chunk_ctx with a [[nodiscard]] annotation or adding a debug-mode assertion that the span data pointer matches MHD's live buffer (hard to do portably). At minimum add a section to the developer documentation on hook context lifetime rules. CWE-416 (Use After Free).
   *Status:* deferred — `hook_context.hpp:182-184` documents the lifetime of `chunk` ("only valid for the duration of the hook call") but does not say "escaping it is UB"; `request_received_ctx::request` lifetime (lines 165-168) is not documented either. Doc-strength improvement, not a code change.

28. [x] **spec-alignment-checker** | `test/integ/hooks_no_firing.cpp:170` | acceptance-criteria
   The updated hooks_no_firing.cpp test correctly exempts request_received and body_chunk from the 'silence' assertions, and its comment documents why (request_received fires on every request; body_chunk does not fire on a GET). However, the test does not add a positive assertion that the request_received counter IS non-zero after the GET round-trip. The comment promises this fires for every request, but no LT_CHECK confirms it; this leaves a latent gap where a regression (e.g. the any_hooks_ gate never being set) would pass silently.
   *Recommendation:* Add LT_CHECK(counters[static_cast<std::size_t>(hook_phase::request_received)].load() >= 1) after ws.stop() to verify the hook actually fired on the GET request. This matches the comment's stated intent.
   *Status:* Resolved — `hooks_no_firing.cpp:224` now contains `LT_CHECK(fired(hook_phase::request_received))` (alongside a `fired()` lambda checking counter >= 1), exactly matching the recommendation. TASK-048..050 phases got the same positive regression anchors at lines 213-222.

29. [ ] **test-quality-reviewer** | `test/integ/hooks_body_chunk_observes_progress.cpp:131` | missing-test
   The comment on line 129 acknowledges that MHD does not always set `is_final` in the upload callback, so the test relaxes to checking `offset + size == body.size()` for the last event rather than checking `is_final == true`. This means the `is_final` field is never asserted in any test — it is only checked for field shape in the unit test. If a future implementation always sets `is_final` correctly, no test would catch a regression where it is never set.
   *Recommendation:* At minimum add a comment in the unit shape test explaining that `is_final` behavioural assertion is intentionally deferred. Optionally, add a short-circuit test that returns `respond_with` when `is_final == true` and verify the accumulation is correct; this exercises the field as a functional signal, not just a shape.
   *Status:* deferred — duplicate of #13/#8. Blocked on the firing site ever setting `is_final=true`; once it does, behaviourally asserting it becomes meaningful.

30. [ ] **test-quality-reviewer** | `test/integ/hooks_body_chunk_observes_progress.cpp:90` | slow-test
   All four integration tests use `std::this_thread::sleep_for(std::chrono::milliseconds(50))` after `ws.start(false)` to let the server bind before issuing the first curl request. This is a fixed-sleep startup poll: if the CI host is slow it can still race, and it always burns 50 ms per test. The pattern is consistent with the rest of the integ suite (which is fine as a codebase norm), but it is fragile by construction.
   *Recommendation:* If the test harness or webserver API exposes a 'wait-until-listening' primitive (e.g., polling the port, or a blocking start overload), prefer that. If no such primitive exists, document the pattern as a known limitation and consider adding a retry loop around the first curl call instead of a bare sleep.
   *Status:* deferred — the 50 ms `sleep_for` shim is the codebase-wide integ-suite norm (also in TASK-046/048/049/050 tests). Replacing it requires a new "wait-until-listening" library primitive; cross-cutting test infrastructure change, not TASK-047-local.

31. [ ] **test-quality-reviewer** | `test/integ/hooks_body_chunk_short_circuit_no_leak.cpp:113` | aaa-violation
   The Act phase (curl_easy_perform) and the Assert phase are not visually separated from the Arrange phase. The inline comment `// post_process_enabled defaults to true...` in the Arrange section is good, but the long curl setup block immediately flows into the assertion block with no blank-line or comment delimiter marking the transition from Act to Assert.
   *Recommendation:* Add a blank line or a `// Assert` comment between the curl cleanup block and the `LT_CHECK_EQ(res, CURLE_OK)` assertions to make the AAA boundary explicit. Minor readability improvement.
   *Status:* deferred — the test already has a blank line at `hooks_body_chunk_short_circuit_no_leak.cpp:117` between curl cleanup and `LT_CHECK_EQ(res, CURLE_OK)`, but no `// Assert` marker. Cosmetic only.

32. [ ] **test-quality-reviewer** | `test/integ/hooks_request_received_short_circuit.cpp:136` | naming-convention
   The second test in the file is named `small_body_passes_through`, but it does double duty: it verifies the happy path (HTTP 200, handler called) AND that the body_chunk hook fired at least once. The 'and' in the implicit intent suggests two separate concerns are bundled. The name omits the body_chunk-fires part.
   *Recommendation:* Rename to something like `small_body_passes_through_and_body_chunk_fires` or split into two tests: one asserting HTTP 200 / handler_called, one asserting body_chunks_observed >= 1. The current test is not wrong, but the name obscures the second assertion it is making.
   *Status:* wontfix — cosmetic/style preference, no functional impact; naming polish; bundled assertions clear enough

33. [ ] **test-quality-reviewer** | `test/integ/hooks_request_received_short_circuit.cpp:83` | missing-test
   The request_received hook guards against a malformed Content-Length with a try/catch that silently passes (letting the normal pipeline handle a 400). No test exercises this code path: a POST with a non-numeric Content-Length header. The catch block in the production hook lambda is untested.
   *Recommendation:* Add a sub-test (or a parameterized variant) that sends a request with `Content-Length: not-a-number` and asserts the server does not crash and returns a sensible HTTP status (4xx). This is a minor gap because the catch just passes, but it exercises an explicit branch in the test-owned hook code.
   *Status:* deferred — no malformed-Content-Length sub-test exists. The catch block is in the test-file's own example hook lambda (`hooks_request_received_short_circuit.cpp:91-93,151`), not a library code path, so coverage gap is illustrative rather than safety-critical.
