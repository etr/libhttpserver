# Unworked Review Issues

**Run:** 2026-06-22 18:53:47
**Task:** TASK-083
**Total:** 39 (0 critical, 8 major, 31 minor)

## Major

1. [ ] **code-quality-reviewer** | `test/bench_hook_overhead.cpp:132` | code-elegance
   sort_and_median and p99_of_sorted are still duplicated verbatim across bench_hook_overhead.cpp (lines 132-141), bench_route_lookup.cpp (lines 109-118), and bench_warm_path.cpp (lines 119-127). These three TUs now share bench_harness.hpp for do_not_optimize but each still carries its own private copy of the two stat helpers. This is the same DRY violation flagged in iteration 1 as a major finding and it persists unchanged.
   *Recommendation:* Move sort_and_median and p99_of_sorted into bench_harness.hpp alongside do_not_optimize and run_bench_median. All three bench TUs already include bench_harness.hpp so no include changes are needed; just delete the three local copies.

2. [ ] **code-quality-reviewer** | `test/bench_route_lookup.cpp:109` | code-elegance
   sort_and_median and p99_of_sorted are duplicated verbatim in bench_route_lookup.cpp (lines 109-118) and in bench_hook_overhead.cpp (lines 132-141) and again in bench_warm_path.cpp (lines 119-127). bench_harness.hpp already exists as the shared harness and even provides run_bench_median, but these two helpers were not moved there. This violates the DRY principle and means a future bug fix in one copy will not propagate to the others.
   *Recommendation:* Add sort_and_median and p99_of_sorted to bench_harness.hpp alongside run_bench_median. Then remove the three local copies. The signature is the same in all three files so the move is mechanical.

3. [ ] **code-quality-reviewer** | `test/bench_route_lookup.cpp:109` | code-elegance
   Duplicate of sort_and_median / p99_of_sorted per finding 1. bench_route_lookup.cpp defines its own copies of both helpers (lines 109-118) even though bench_harness.hpp is now included at line 71. The inline copies also slightly diverge in commenting style from bench_hook_overhead.cpp, introducing inconsistency.
   *Recommendation:* Remove the local definitions once the helpers are promoted to bench_harness.hpp (see finding 1).

4. [ ] **code-quality-reviewer** | `test/bench_route_lookup.cpp:122` | code-elegance
   measure_median_ns is defined locally in bench_route_lookup.cpp (lines 122-156) and again nearly identically in bench_warm_path.cpp (lines 132-164). run_bench_median in bench_harness.hpp does almost the same job but without the warmup pre-loop and without printing. A shared helper that accepts a label and prints would eliminate this duplication.
   *Recommendation:* Extend bench_harness.hpp with a print-and-return variant of run_bench_median that includes the 10 000-iteration warmup and the label/printf output, then replace both local copies. Alternatively, keep run_bench_median as-is and add a thin measure_and_print wrapper in the header.

5. [ ] **code-simplifier** | `test/bench_hook_overhead.cpp:132` | patterns
   sort_and_median and p99_of_sorted are still duplicated across bench_hook_overhead.cpp (lines 132-141), bench_route_lookup.cpp (lines 109-118), and bench_warm_path.cpp (lines 119-127). This was flagged in iteration 1 and the fixer pass did not address it. These two helpers are identical in all three files.
   *Recommendation:* Move sort_and_median and p99_of_sorted into bench_harness.hpp alongside run_bench_median. The harness is already the canonical home for shared bench utilities; the three TUs all include it. measure_median_ns is TU-local because its warmup loop semantics differ, but the sort/median helpers have no variation.

6. [ ] **code-simplifier** | `test/bench_hook_overhead.cpp:143` | patterns
   measure_gate_cost in bench_hook_overhead.cpp (lines 143-184) and measure_median_ns in bench_route_lookup.cpp (lines 123-156) and bench_warm_path.cpp (lines 132-164) all implement the same outer-rounds / inner-iterations timing pattern with nearly identical structure. This was flagged in iteration 1 and persists.
   *Recommendation:* A shared measure_median_ns template in bench_harness.hpp that accepts a pre-round callback (for the cache-invalidation case in bench_route_lookup (b)) would eliminate the repetition. Alternatively, document explicitly why the three variants must stay separate if the per-round-callback approach would entangle the harness.

7. [ ] **code-simplifier** | `test/bench_route_lookup.cpp:109` | code-structure
   sort_and_median and p99_of_sorted are copy-pasted verbatim from bench_hook_overhead.cpp and bench_warm_path.cpp. The identical pair appears in all three files with identical signatures, identical bodies, and identical precondition comments. bench_harness.hpp is the canonical shared header for bench helpers, so this logic belongs there rather than being replicated three times.
   *Recommendation:* Move sort_and_median and p99_of_sorted into bench_harness.hpp (alongside run_bench_median). The three .cpp files can then remove their local definitions. This eliminates six duplicate function bodies without any behavior change; the precondition on p99_of_sorted should travel with the definition.

8. [ ] **code-simplifier** | `test/bench_route_lookup.cpp:254` | code-structure
   The radix_pure measurement block (lines 253-290) hand-rolls the same outer/inner timing loop that measure_median_ns (lines 122-156) already provides. The only difference is the invalidate_route_cache() call inserted before the inner loop. This structural divergence means the two measurement paths are no longer maintained in lockstep, and the hand-rolled block is longer than it needs to be.
   *Recommendation:* Extract the cache-invalidation step into a pre-round hook. One minimal approach: add an optional pre-round callable to measure_median_ns (default = no-op), or provide a measure_median_ns_with_preround helper in bench_harness.hpp. The existing measure_median_ns signature is simple enough that a two-argument extension (warmup fn + pre-round fn) would keep both measurement sites identical in structure. Either approach eliminates the ~35 lines of duplicated timing machinery while preserving the invalidation-before-each-outer-round semantic that makes the cold-cache measurement correct.

## Minor

9. [ ] **architecture-alignment-checker** | `specs/architecture/09-testing.md:14` | adr-violation
   The architecture testing document (09-testing.md §9 item 7) describes performance acceptance gates as belonging to EXTRA_PROGRAMS running under 'make bench' and says they 'do not run under sanitizer CI'. The TASK-083 implementation correctly follows the EXTRA_PROGRAMS / opt-in make bench pattern, but the architecture document's description of the gated metrics is now partially stale: it mentions only bench_get_headers.cpp (get_headers >= 10x) and bench_sizeof_http_resource.cpp (sizeof shrink) as the two gated benchmarks, with no mention of bench_hook_overhead, bench_route_lookup, or bench_warm_path. The implementation adds three new gates not listed in the architecture spec.
   *Recommendation:* Update 09-testing.md §9 item 7 to enumerate all five bench_targets (bench_get_headers, bench_sizeof_http_resource, bench_hook_overhead, bench_route_lookup, bench_warm_path) and describe the gate semantics (absolute ceiling for hook overhead, dual cache-warm/radix-cold ceilings for route lookup, per-platform baseline regression for warm path). This is a documentation gap, not a code error — the implementation is correct.

10. [ ] **architecture-alignment-checker** | `test/bench_route_lookup.cpp:109` | pattern-violation
   bench_route_lookup.cpp defines its own local sort_and_median() and p99_of_sorted() helpers (lines 109-118) that are functionally identical to run_bench_median() already consolidated in bench_harness.hpp. The file includes bench_harness.hpp but does not use run_bench_median() for the cache-warm measurement (it calls measure_median_ns instead, a third private helper). This leaves three measurement patterns across the file: run_bench_median (unused), measure_median_ns, and a bespoke outer loop for the radix-cold path. The consolidation goal of TASK-083 (centralising do_not_optimize into bench_harness.hpp) is met, but the stat-utility duplication is not cleaned up.
   *Recommendation:* Lift sort_and_median and p99_of_sorted into bench_harness.hpp alongside run_bench_median so all bench TUs share a single implementation. bench_route_lookup.cpp can then call these shared helpers directly instead of redefining them. This is consistent with the harness-consolidation intent stated in the bench_harness.hpp header comment.

11. [ ] **architecture-alignment-checker** | `test/bench_warm_path.cpp:119` | pattern-violation
   bench_warm_path.cpp also defines its own local sort_and_median() and p99_of_sorted() (lines 119-127) rather than using shared helpers from bench_harness.hpp. Same issue as bench_route_lookup.cpp: the pattern is self-consistent within each TU, but duplicates logic that belongs in the shared harness header.
   *Recommendation:* Same resolution as finding 1: move sort_and_median and p99_of_sorted into bench_harness.hpp and remove the per-TU copies.

12. [ ] **code-quality-reviewer** | `test/bench_baseline.hpp:65` | code-readability
   The Linux and MSVC baseline arms carry TODO(TASK-084) placeholder values that are documented as 'deliberately loose so the gate never produces a false failure'. While the intent is clearly stated in the header comment, these arms make the CI gate on Linux and MSVC essentially a no-op against real regressions until TASK-084 lands. The risk of forgetting TASK-084 is real, since a later commit could silently regress Linux performance without the bench catching it.
   *Recommendation:* Add a compile-time #warning (or a runtime printf at bench startup) on Linux and MSVC noting that the baselines are uncalibrated placeholders, so the bench output clearly communicates reduced gating confidence. This makes the placeholder status visible in CI logs even before TASK-084 runs.

13. [ ] **code-quality-reviewer** | `test/bench_baseline.hpp:84` | code-readability
   The #else branch ends with a #error that fires for any platform that is not __APPLE__, linux+libstdc++, or _Win32. A libc++-on-Linux build (e.g. Ubuntu with clang+libc++) matches none of the three arms and triggers this hard error. The comment in the file header mentions only 'libstdc++ on Linux', not 'any Linux', so a libc++-on-Linux CI runner would break the build with no easy workaround until TASK-084 adds a fourth arm.
   *Recommendation:* Widen the __linux__ arm to cover both __GLIBCXX__ and _LIBCPP_VERSION, or add a separate libc++ arm with the same conservative placeholder values as the libstdc++ arm. Alternatively, document the libc++-on-Linux gap explicitly in the #error message.

14. [ ] **code-quality-reviewer** | `test/bench_harness.hpp:59` | code-readability
   The MSVC-only inline variable do_not_optimize_sink is declared as 'volatile const void* volatile'. The double volatile (once on the pointee type, once on the pointer itself) is correct and intentional, but there is no brief comment at the declaration explaining why both qualifiers are needed. Readers familiar with MSVC optimisation rules may not immediately see that the pointer itself must be volatile to force the store.
   *Recommendation:* Add a one-line comment at the declaration: '// pointer is volatile so the store to it is an observable side effect; pointee is const void* to accept any address'.

15. [ ] **code-quality-reviewer** | `test/bench_hook_overhead.cpp:134` | code-readability
   The median index expression v[v.size() / 2] assumes v is non-empty and that integer division of an even-sized vector yields an acceptable middle element, but there is no assertion or comment documenting the OUTER-must-be-odd precondition at the call site in sort_and_median. The same pattern appears in bench_route_lookup.cpp line 111 and bench_warm_path.cpp line 121.
   *Recommendation:* Add a static_assert or runtime assert at the top of sort_and_median (or in bench_harness.hpp if consolidated) that v.size() > 0, and add a brief comment referencing the OUTER-must-be-odd convention already documented in bench_harness.hpp.

16. [ ] **code-quality-reviewer** | `test/bench_route_lookup.cpp:277` | code-readability
   The idx variable used to rotate through kManyPaths in the warmup loop (line 277) and in the timed inner loop (line 288) is declared before the warmup block and carries state across both. Its value after the warmup loop feeds directly into the timed loop, which is intentional but not commented. A reader might expect the timed loop to start from idx=0.
   *Recommendation:* Add a brief comment before the timed loop (e.g. 'idx continues from warmup so the first timed iteration does not restart at path 0') to make the intent explicit.

17. [ ] **code-quality-reviewer** | `test/bench_warm_path.cpp:119` | code-readability
   bench_warm_path.cpp defines its own sort_and_median / p99_of_sorted (lines 119-127) and also a full measure_median_ns template (lines 132-164) that is not shared with the other two bench TUs. While measure_median_ns differs slightly (it includes warmup and prints a summary line), the stat helpers are identical to the copies in bench_hook_overhead.cpp and bench_route_lookup.cpp.
   *Recommendation:* Consolidate stat helpers into bench_harness.hpp. measure_median_ns could also move there if the warmup + print behaviour is acceptable for all callers, but that is a lower-priority cleanup.

18. [ ] **code-quality-reviewer** | `test/bench_warm_path.cpp:316` | test-coverage
   The comment tag '(performance-reviewer-iter1-1)' appears on lines 316 and 358 referencing a prior review finding. This tag is a review artifact that leaked into production source. It adds noise without adding value to future readers who will not have the original review thread.
   *Recommendation:* Remove the '(performance-reviewer-iter1-1)' parenthetical tags from the comments and keep only the substantive description of the design change.

19. [ ] **code-quality-reviewer** | `test/bench_warm_path.cpp:350` | code-readability
   do_not_optimize(buf5) at line 350 (and the equivalent do_not_optimize(buf6) at line 393) prevents the compiler from eliding the backing buffer, but the comment says 'prevent compiler from eliding the buffer'. For buf5 the real risk is that the compiler does not actually materialise the zero-initialised bytes because no pointer to them escapes; however, the monotonic_buffer_resource constructed on each lambda invocation takes buf5.data() as its first argument, which already gives the compiler a reason to keep buf5 live. The do_not_optimize call is therefore defence-in-depth rather than strictly necessary. The comment slightly overstates the need.
   *Recommendation:* Tighten the comment to 'defence-in-depth: ensures the compiler treats buf5 as potentially read by the monotonic_buffer_resource' to avoid misleading future readers into thinking the resource construction alone is insufficient.

20. [ ] **code-simplifier** | `test/bench_harness.hpp:50` | patterns
   Two consecutive #if defined(_MSC_VER) blocks (lines 50-52 for the #include and lines 54-60 for the sink declaration) can be merged into one guarded block with a blank line separating the include from the declaration. Minor readability polish.
   *Recommendation:* Merge into a single #if defined(_MSC_VER) ... #endif block containing both the #include <intrin.h> and the inline volatile sink declaration.

21. [ ] **code-simplifier** | `test/bench_hook_overhead.cpp:226` | naming
   const double HOOK_BASELINE_NS = median_a; uses ALL_CAPS for a non-constant local variable. ALL_CAPS is the project convention for macros and compile-time constants. A regular local should use snake_case.
   *Recommendation:* Rename to hook_baseline_ns (or baseline_ns) to follow the local-variable convention used everywhere else in the bench files.

22. [ ] **code-simplifier** | `test/bench_hook_overhead.cpp:92` | code-structure
   The kSanitizerBuild constexpr block (lines 92-110) is copy-pasted identically in bench_hook_overhead.cpp, bench_route_lookup.cpp, and bench_warm_path.cpp. All three files define the same symbol with the same preprocessor logic. If a new sanitizer macro needs to be added in the future (e.g. HWASAN on a new platform), it must be updated in three places.
   *Recommendation:* Move the kSanitizerBuild constant into bench_harness.hpp. The constant is already named at file scope and has no dependencies on file-local types, so the move is mechanical. This consolidates the sanitizer-detection logic in one place, consistent with the rationale that already moved do_not_optimize into bench_harness.hpp.

23. [ ] **housekeeper** | `test/bench_baseline.hpp:66` | documentation-stale
   Two TODO(TASK-084) comments in bench_baseline.hpp (lines 66 and 76) reference placeholder values for Linux/libstdc++ and MSVC/Windows warm-path baselines. These are intentional and correctly cross-referenced to TASK-084 (which exists as a Backlog task with its own spec file). However, the bench_baseline.hpp comment header (lines 30-37) already documents the refresh procedure thoroughly, so the inline TODO annotations are both accurate and well-supported. No missing documentation, but the TODO markers will persist until TASK-084 lands.
   *Recommendation:* No action required for this commit. When TASK-084 is implemented, the two TODO(TASK-084) arms should be updated with real measured values and the TODO annotations removed.

24. [ ] **performance-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-083/test/bench_route_lookup.cpp:244` | missing-caching
   iter-1-1 RESOLVED (comment-only fix, adequate): The first <=256 iterations per outer round partially warm the cache rather than measuring pure radix latency. The fix added a comment at lines 244-251 quantifying the warm-up window as ~0.25% of INNER_RADIX=100K iterations and explaining it makes the gate conservatively stricter rather than looser. This is an adequate resolution: the comment accurately characterises the residual bias, the bias is self-limiting in magnitude (0.25%), and correcting it structurally would require per-iteration cache invalidation with non-trivial overhead that would distort the signal more than the warm-up itself. No measurement validity concern remains.
   *Recommendation:* No action required. The comment at bench_route_lookup.cpp lines 244-251 is accurate and sufficient. A future TASK could explore per-iteration invalidation if tighter purity is desired, but that is an enhancement, not a defect.

25. [ ] **performance-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-083/test/bench_warm_path.cpp:349` | memory-allocation
   iter-1-2 RESOLVED (structural fix, correct): buf5 (measurement 5) and buf6 (measurement 6) are now declared as std::array<std::byte,65536> outside the measured lambda, zero-initialised exactly once via aggregate-init, and pinned live with do_not_optimize(). The lambda constructs a fresh monotonic_buffer_resource from the same backing storage each call (cheap: resets two internal pointers, does not re-zero bytes), and uses null_memory_resource() as the upstream so overflow is a hard error rather than a silent heap fallback. The per-iteration 64 KiB memset that dominated the iter-1 signal is gone. The fix correctly models the production lifecycle (fresh arena per request, same backing slab). No residual measurement-validity concern.
   *Recommendation:* No action required. The fix is structurally sound and correctly eliminates the per-iteration zeroing overhead while preserving the zero-heap-alloc invariant the bench is meant to prove.

26. [ ] **performance-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-083/test/bench_warm_path.cpp:429` | missing-caching
   iter-1-3 RESOLVED (structural fix, correct): kAbsoluteNoiseFloorNs = 5.0 ns is added at line 429. The gate ceiling for each measurement is now max(baseline * kAllowedRegressionRatio, baseline + kAbsoluteNoiseFloorNs). For the sub-10 ns should_skip_auth_empty baseline (2.0 ns on Apple silicon), the floor ceiling is 7.0 ns while the ratio ceiling would have been only 2.1 ns (below steady_clock resolution), so the floor takes effect and eliminates false-fail risk. For the larger baselines (130 ns, 40 ns, 650 ns, 640 ns) the ratio ceiling dominates, preserving the 5% regression intent. The fix matches the pattern already used in bench_hook_overhead.cpp's kRelativeGateNoiseFloorNs. No false-fail or false-pass risk identified.
   *Recommendation:* No action required. The dual-ceiling pattern is correct. One optional future hardening: document the 5.0 ns value as tied to steady_clock resolution on the reference platform, so TASK-084 reviewers know to revisit it if the runner changes to a platform with coarser clock resolution.

27. [ ] **performance-reviewer** | `test/bench_harness.hpp:122` | missing-caching
   run_bench_median allocates std::vector<double> samples_ns on each call and reserves `outer` elements. For bench contexts where this function is called many times (not the current case), this would cause repeated heap allocation. In the current usage (called once or twice per bench binary) this is inconsequential.
   *Recommendation:* No change required for the current call pattern. If run_bench_median is reused in inner loops in a future bench, accept a pre-allocated vector by reference.

28. [ ] **performance-reviewer** | `test/bench_hook_overhead.cpp:199` | missing-caching
   OUTER=51 with INNER=1,000,000 means each outer round iterates 1M atomic loads. The warmup (10,000 iterations before the timed loop) is dwarfed by the first outer round which also effectively warms the branch predictor. The first outer round therefore carries a slightly different branch-predictor state than subsequent rounds, but since median of 51 is used (26th sorted value), and the first round is at most 1 outlier in the top or bottom of the distribution, this is statistical noise of < 2%. No action required, but the warmup comment 'dwarfs this explicit pre-loop' is accurate.
   *Recommendation:* No change required. The 51-round median is robust to 1-2 warm-up outliers.

29. [ ] **performance-reviewer** | `test/bench_warm_path.cpp:200` | missing-caching
   OUTER=11 for bench_warm_path vs OUTER=51 for bench_hook_overhead and bench_route_lookup. With only 11 outer rounds, the median is the 6th sorted value. The p99 is 10th out of 11, which is actually max(). On noisy CI runners with 1-2 high-outlier rounds due to OS scheduling, a 11-round median is noticeably less stable than a 51-round median. The warm_path bench is the one with the tightest absolute gate (5% regression) so it most benefits from more rounds.
   *Recommendation:* Consider increasing OUTER to at least 21 (median is 11th value, p99 is 20th) or 31 for the warm_path bench. The cost at 1M inner iterations is ~1 second per extra 10 rounds — acceptable for a bench that is explicitly not part of `make check`.

30. [ ] **security-reviewer** | `test/bench_harness.hpp:105` | insecure-design
   The unknown-compiler fallback branch declares 'static volatile const void* volatile fallback_sink = nullptr' as a function-local static. When bench_harness.hpp is included by multiple TUs compiled by the same unknown compiler, each TU gets its own copy of the local static (function-local statics are not the same as inline namespace-scope variables). This is harmless for correctness in test code (the sink is never read), but the inconsistency with the MSVC branch (which uses an inline variable giving one definition) is a latent ODR deviation if ever compiled with a C++17 compiler that also happens to enter the else branch.
   *Recommendation:* Mirror the MSVC pattern: declare 'inline volatile const void* volatile fallback_sink = nullptr;' at namespace scope (guarded by an #else block), then write through it in do_not_optimize. This gives a single definition across all TUs regardless of compiler, matching the MSVC branch's intent.

31. [ ] **security-reviewer** | `test/bench_route_lookup.cpp:184` | buffer-handling
   make_radix_paths() writes into a fixed char buf[64] via std::snprintf. The format string '/a/u%04zu/b/v%04zu/c/w%04zu/d/x%04zu' with i up to count-1 (currently 319) produces at most '/a/u0319/b/v0319/c/w0319/d/x0319' = 33 characters plus NUL, well within 64 bytes. However the buffer size is not tied to the format: if count were ever raised past 9999 (five-digit i), the rendered string would grow to 37 chars, still safe; past 99999 (six-digit) it reaches 41 chars, still safe; the buffer would not overflow until i reached roughly 10^13. The risk is currently zero but the coupling is implicit.
   *Recommendation:* Add a static_assert or a runtime assertion documenting the invariant, e.g. static_assert(kNumPaths <= 9999, "buf[64] in make_radix_paths is sized for at most 4-digit path indices"); This makes the constraint explicit and prevents a silent overflow if kNumPaths is ever raised past the safe range.

32. [ ] **spec-alignment-checker** | `test/bench_baseline.hpp:65` | acceptance-criteria
   bench_warm_path AC states 'fails when warm-path latency regresses >=5% vs a versioned per-platform baseline header'. The baseline header exists and is correctly named per-platform, but the Linux/libstdc++ and MSVC arms carry conservative placeholder values (~3x the Apple-silicon medians) explicitly labelled TODO(TASK-084). These arms cannot detect a real 5% regression on their target platforms because the baselines are intentionally loose. The spec says 'versioned per-platform baseline', which implies calibrated values; on Linux/MSVC the gate is nominally wired but effectively toothless until TASK-084 lands.
   *Recommendation:* This is a known and documented deferral (TASK-084 is in Backlog). No code change required here, but the TASK-083 acceptance criterion text could note 'Linux/MSVC arms carry temporary conservative placeholders pending TASK-084' to accurately reflect that the gate is only calibrated on Apple-silicon at this stage.

33. [ ] **spec-alignment-checker** | `test/bench_harness.hpp:null` | acceptance-criteria
   Acceptance criterion 4 states 'MSVC sink survives /O2 (verified by disassembly snippet in the commit message)'. The commit message (HEAD 318b214) contains a detailed textual argument and godbolt cross-reference but no actual disassembly snippet (no `mov`/`lea` instruction lines pasted inline). The text says 'Verify on godbolt with MSVC v19.3x /O2 /std:c++20' and describes the expected output, but does not include the disassembly output itself.
   *Recommendation:* Append a short MSVC /O2 disassembly snippet (2-4 instruction lines from godbolt) to the commit message or to test/PERFORMANCE.md to satisfy the 'snippet' wording of the criterion. Alternatively, if the team accepts prose+godbolt-URL as sufficient evidence, update the task acceptance criterion wording to 'verified by godbolt cross-reference' to match what was actually delivered.

34. [ ] **spec-alignment-checker** | `test/bench_hook_overhead.cpp:235` | specification-gap
   The implementation adds a 2 ns additive noise floor (`kRelativeGateNoiseFloorNs`) to the relative gate formula: `relative_gate_ns = 2 * HOOK_BASELINE_NS + 2.0`. This component is not mentioned in the TASK-083 task spec or in the PRD §3.8 acceptance criterion ('within 2x microbench noise of the pre-hook-system baseline'). The noise floor is a reasonable implementation choice, but it widens the gate beyond the literal '2x HOOK_BASELINE_NS' the spec describes.
   *Recommendation:* Either document in the task file that the additive noise floor was intentionally added to handle sub-ns sampling jitter, or ensure the task spec acceptance wording is updated to reflect the '2 * baseline + 2 ns floor' formula actually used.

35. [ ] **test-quality-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-083/test/bench_baseline.hpp:68` | non-deterministic
   WARM_SHOULD_SKIP_AUTH_EMPTY_NS = 2.0 ns is still very tight even with the new max(ratio_ceiling, baseline + 5ns floor) gate. The effective ceiling is max(2.0*1.05, 2.0+5.0) = max(2.1, 7.0) = 7.0 ns — the floor now dominates, which means the gate passes anything under 7 ns regardless of how much the 2 ns path has regressed. This is technically safe against false-positives, but the tight baseline makes the gate nearly toothless for this measurement: a 3.5x regression (2 ns → 7 ns) would still pass.
   *Recommendation:* This is a known consequence of the TASK-084 baseline-refresh gap, already flagged in the header comments. No immediate action needed — TASK-084 will tighten it. Consider documenting the 7 ns effective ceiling explicitly in bench_baseline.hpp next to the 2.0 ns constant so future readers understand why the absolute floor dominates.

36. [ ] **test-quality-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-083/test/bench_route_lookup.cpp:262` | implementation-coupling
   kNumPaths = 320 is hardcoded against ROUTE_CACHE_MAX_SIZE (256). The comment acknowledges this coupling and instructs a manual update if the LRU capacity changes, but there is no static_assert or compile-time check to catch a mismatch. If ROUTE_CACHE_MAX_SIZE is ever raised above 320 the radix_pure gate silently reverts to measuring a cache-warm mix instead of pure radix latency, defeating the fix.
   *Recommendation:* Add a static_assert (or a runtime assertion guarded by webserver_test_access) that kNumPaths > the actual LRU capacity constant. This converts a prose-only coupling into a compile-time or startup-time failure, making the gate self-enforcing.

37. [ ] **test-quality-reviewer** | `test/bench_baseline.hpp:84` | missing-test
   The #else branch at line 84 is a #error that blocks compilation on any platform not matching __APPLE__, Linux+libstdc++, or _WIN32 (e.g. FreeBSD, musl-libc Linux, or a Linux build with libc++). This means a developer or CI runner on a non-matching platform cannot run `make bench` at all — not even with a deliberately loose gate — until they manually add a platform arm. The bench_hook_overhead and bench_route_lookup benches have no such hard compile-time barrier; they run on any platform because their ceilings are hardcoded. The asymmetry creates a silent difference in `make bench` portability that is not documented in the Makefile or PERFORMANCE.md.
   *Recommendation:* Add a fallback arm (before the #else/#error) that uses conservative placeholder values matching the Linux/MSVC arm, with a runtime std::fprintf warning that the baseline is uncalibrated. This lets `make bench` complete on unknown platforms while the gate still exists. Alternatively, document in EXTRA_DIST/PERFORMANCE.md that bench_warm_path is platform-gated at compile time.

38. [ ] **test-quality-reviewer** | `test/bench_route_lookup.cpp:245` | implementation-coupling
   kNumPaths = 320 is justified as "> ROUTE_CACHE_MAX_SIZE (256)", but ROUTE_CACHE_MAX_SIZE is a private implementation constant that is not exposed or static_asserted anywhere in the bench. If ROUTE_CACHE_MAX_SIZE is ever changed (e.g. doubled to 512 for a cache capacity increase), kNumPaths would silently become too small, the LRU would not be overflowed, and the 'radix_pure' measurement would revert to measuring a cache-warm mix rather than pure radix-tier latency — the exact flaw the comment at lines 37-38 says TASK-083 fixed. The gate would still pass (the ceiling is generous) but the measurement label would become misleading and the cache-cold invariant would be violated without any diagnostic.
   *Recommendation:* Either: (a) pull ROUTE_CACHE_MAX_SIZE into a shared header and add a static_assert(kNumPaths > ROUTE_CACHE_MAX_SIZE) in the bench, or (b) add a comment explicitly naming the magic number (320) relative to the current value (256) with a TODO to update if the capacity changes. At minimum, document in the bench's source comment that kNumPaths must be manually kept above the LRU capacity.

39. [ ] **test-quality-reviewer** | `test/bench_warm_path.cpp:403` | logic-in-test
   The check lambda at line 403 contains conditional logic (an if branch inside a lambda that is called six times). This is a minor instance of logic-in-test: the lambda's pct computation is used only for the FAIL printf but is always computed; more importantly, the lambda accumulates rc via closure mutation which makes the control flow non-local and requires reading main() end-to-end to confirm all six checks contribute to rc independently. This is readable as written but the lambda's stateful mutation of rc via capture could silently produce a false pass if the lambda were ever called in a short-circuit context.
   *Recommendation:* Minor structural note only: the current usage is correct (all six check() calls are unconditional). No urgent change required, but the lambda could be made a free function or its return value used to OR into rc to make the independence more visually explicit.
