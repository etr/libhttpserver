# Unworked Review Issues

**Run:** 2026-05-26 00:00:00
**Task:** TASK-051
**Total:** 44 (0 critical, 3 major, 41 minor)

## Major

1. [ ] **architecture-alignment-checker** | `src/detail/webserver_dispatch.cpp:404` | adr-violation
   The per-route handler_exception chain fires AFTER the server-wide chain, and if the server-wide chain short-circuits the per-route chain is skipped. This is correct per DR-012 ordering. However, when neither chain produces a response, the code falls through to emit the hardcoded 500 directly (line 417-419) rather than allowing the per-route chain another chance. The handler_exception case is the one phase where DR-012 §5.6 says 'the handler_exception chain continues to the next hook when one of its hooks throws' — for the per-route handler_exception chain in resource_hook_table::fire_handler_exception, throwing hooks are logged and the chain continues within the per-route scope. This behavior is consistent with the spec but differs subtly from webserver_impl::fire_handler_exception which also has a last-position alias slot. On further analysis the implementation correctly follows DR-012 and DR-009 §5.2. The subtle interaction is not explicitly commented.
   *Recommendation:* No code change required. Consider a follow-up to make the per-route handler_exception fallback behavior explicit in arch §5.6 and the code comment at line 415-419.

2. [ ] **code-simplifier** | `src/resource_hook_table.cpp:135` | code-structure
   The five append_* methods (append_before_handler, append_handler_exception, append_after_handler, append_response_sent, append_request_completed) are structurally identical: fetch_add next_slot_id_, take unique_lock, push_back, store any_hooks_ gate, return id. The only differences are the target vector member and the hook_phase value. This is needless repetition (five copies of the same 8-line body) that inflates the file and means any future change to the append protocol must be applied five times.
   *Recommendation:* Extract a private template helper `template<hook_phase P, typename Sig> uint64_t append_impl(std::vector<entry<Sig>>& vec, std::function<Sig> fn)` that contains the shared body. Each public overload becomes a one-liner that delegates to append_impl. This mirrors the existing pattern of fire_short_circuit_impl / fire_void_impl in the same file.

3. [ ] **code-simplifier** | `test/integ/hooks_per_route_invalid_phase_throws.cpp:60` | code-structure
   Six nearly identical test cases (request_received_tag_throws, connection_opened_tag_throws, accept_decision_tag_throws, body_chunk_tag_throws, route_resolved_tag_throws, connection_closed_tag_throws) repeat the same pattern: declare a trivial_resource, set `threw = false`, call add_hook inside a try/catch for std::invalid_argument, assert threw == true. Only the phase tag and the overload signature differ. This is 90 lines of duplicated boilerplate.
   *Recommendation:* Extract a local helper `template<typename FnT> bool add_hook_throws(hook_phase p, FnT fn)` that returns true iff add_hook throws std::invalid_argument. Each test then reduces to a single LT_CHECK_EQ call. Alternatively, consolidate all six into one test that iterates over a list of {phase, fn} pairs.

## Minor

4. [ ] **architecture-alignment-checker** | `src/httpserver/http_resource.hpp:283` | adr-violation
   DR-012 and TASK-051 action item specify per-resource storage as a `std::array<std::vector<...>, count_> + std::array<std::atomic<bool>, count_> + std::shared_mutex` embedded directly on http_resource. The implementation uses a PIMPL pattern instead: a lazy shared_ptr<detail::resource_hook_table> that is nullptr until first add_hook() call. The choice is sound architecturally (avoids fixed-array cost on resources that never register hooks, consistent with 'zero-cost when unused') but the deviation from the spec's primary design is not documented in an ADR update.
   *Recommendation:* Document the PIMPL shape as the chosen implementation of the 'fallback if sizeof is tight' parenthetical from the task spec, either in a brief note in specs/architecture/04-components/http-resource.md §4.4 or in DR-012 consequences.

5. [ ] **architecture-alignment-checker** | `src/resource_hook_table.cpp:57` | pattern-violation
   The fire_short_circuit_impl template in resource_hook_table.cpp allocates a local Vec snapshot (not thread_local) for each invocation, unlike the server-wide fire_short_circuit_hooks_for_phase template in hook_handle.cpp which uses a thread_local snapshot buffer for zero-heap-allocation on the warm path. The per-route firing helpers therefore allocate on every invocation even on the hot path.
   *Recommendation:* Consider using thread_local snapshot buffers in resource_hook_table's fire_short_circuit_impl and fire_void_impl helpers, mirroring the TASK-048 perf optimization in the server-wide path.

6. [ ] **architecture-alignment-checker** | `src/http_resource.cpp:79` | pattern-violation
   The ensure_table function uses std::atomic_load_explicit / std::atomic_compare_exchange_strong_explicit on a std::shared_ptr<detail::resource_hook_table> slot. While correct for C++17, the deprecated std::atomic_load/atomic_compare_exchange free functions on shared_ptr are marked deprecated in C++20. The project targets C++17+ but if targeting C++20 consumers, the std::atomic<std::shared_ptr<T>> class should be used instead.
   *Recommendation:* Track the migration to std::atomic<std::shared_ptr<resource_hook_table>> as a C++20 follow-up. Document it as a known C++20 deprecation in the code comment at line 79 or in specs/architecture/12-open-questions.md.

7. [ ] **architecture-alignment-checker** | `src/detail/webserver_finalize.cpp:73` | interface-contract
   The per_route_table helper function checks mr->resource_weak_.expired() before calling lock(). This is a TOCTOU pattern: expired() can return false and then lock() can return nullptr due to a concurrent destroy between the two calls. The code then checks if res_out is non-null (the safe check). However the expired() check is redundant and slightly misleading. The correct pattern is to call lock() directly and check the resulting shared_ptr for null.
   *Recommendation:* Simplify to: `res_out = mr->resource_weak_.lock(); if (!res_out) return nullptr; return res_out->hook_table_raw_();` — removing the redundant expired() check.

8. [ ] **architecture-alignment-checker** | `src/httpserver/detail/resource_hook_table.hpp:130` | interface-contract
   The resource_hook_table class uses a single shared_mutex covering all five hook vectors, matching the architecture doc. However the class documentation says the storage is 'same shape' to the spec while the spec says std::array<std::vector<...>, count_> and the impl has five named vectors. This is a documentation inconsistency; the named vector approach is actually more efficient for the compile/template path but is not the documented layout.
   *Recommendation:* Update the class comment to note that the implementation uses five named vectors rather than a single std::array<std::vector<...>, count_> for type safety at the append_*/fire_* call sites.

9. [ ] **code-quality-reviewer** | `src/http_resource.cpp:61` | code-readability
   check_phase(requested, expected) throws when requested != expected, but the single-argument validation for 'empty callable' (check_fn) is named differently and takes a bool instead of the callable itself. The asymmetry in the two helper signatures makes the pair harder to scan at a glance.
   *Recommendation:* Consider naming the callable check 'check_not_empty' and passing the std::function reference directly (letting the helper evaluate !fn internally), which would make both helpers symmetric.

10. [ ] **code-quality-reviewer** | `src/http_resource.cpp:45` | code-readability
    throw_wrong_phase takes both 'requested' and 'expected' phase parameters but the error message only makes sense when the mismatch is overload-level. Consider renaming 'expected' to 'overload_phase' to make the parameter semantics clearer.
    *Recommendation:* Cosmetic rename; current message is comprehensive and well-structured.

11. [ ] **code-quality-reviewer** | `src/resource_hook_table.cpp:135` | code-elegance
    The five append_* methods are structurally identical (also flagged by code-simplifier as major finding #2). The server-side hook_handle.cpp already uses fire_hooks_for_phase and fire_short_circuit_hooks_for_phase templates for exactly the same reason.
    *Recommendation:* Consider a private template: `template<typename Sig, hook_phase Phase> uint64_t append_impl(std::vector<entry<Sig>>& vec, std::function<Sig> fn)`. Each public append_* would become a one-liner delegation.

12. [ ] **code-quality-reviewer** | `src/resource_hook_table.cpp:202` | code-elegance
    remove_slot's per-case body repeats the erase_if_found + reset_if_empty pair for all five phases. The lambda helpers defined inside remove_slot (erase_if_found, reset_if_empty) are a good start; the switch cases could be collapsed to single-line calls per case.
    *Recommendation:* Minimal remaining repetition (two-call pattern per case); acceptable as-is.

13. [ ] **code-quality-reviewer** | `src/detail/webserver_finalize.cpp:73` | code-readability
    per_route_table() helper in the anonymous namespace accepts modded_request* and a shared_ptr<http_resource>& out-parameter, returning a raw resource_hook_table*. The out-parameter is necessary to keep the resource alive during firing, but the function name 'per_route_table' does not hint at the lifetime-extension side-effect.
    *Recommendation:* Consider renaming to 'lock_per_route_table' or adding a brief comment at the call sites explaining that 'res_out' is intentionally kept alive to prevent the table pointer from dangling mid-firing.

14. [ ] **code-quality-reviewer** | `src/detail/webserver_finalize.cpp:90` | code-readability
    fire_before_handler_gated has a local variable named 'per_route_table' (line 97) that shadows the anonymous-namespace helper function of the same name declared at line 73. The local variable and the helper function are both present in the same TU; while it compiles cleanly, the shadowing makes the function harder to read.
    *Recommendation:* Rename the local pointer variable to 'per_route_hook_tbl' or 'rtable' (the name already used in fire_after_handler_gated at line 148) to eliminate the shadowing.

15. [ ] **code-quality-reviewer** | `src/detail/webserver_finalize.cpp:236` | code-readability
    fire_request_completed_gated duplicates the expired-check + lock pattern for fetching the per-route resource (lines 238-243) instead of calling the per_route_table() helper that was defined for exactly this purpose at line 73. fire_after_handler_gated (line 147) and fire_response_sent_gated (line 189) both use the helper consistently.
    *Recommendation:* Replace the inline expanded pattern in fire_request_completed_gated with a call to per_route_table(mr, res), matching the style of fire_after_handler_gated and fire_response_sent_gated.

16. [ ] **code-quality-reviewer** | `test/integ/hooks_per_route_order.cpp:77` | test-coverage
    The hooks_per_route_order test covers response_sent order (A→B). There is no integ test exercising the server-wide → per-route ordering for before_handler or after_handler, even though those phases have the same chaining contract and different short-circuit semantics.
    *Recommendation:* Consider adding a test case for before_handler order (server-wide fires and passes, then per-route fires) to confirm that the ordering contract also holds for the short-circuit-capable phases.

17. [ ] **code-quality-reviewer** | `test/integ/hooks_per_route_order.cpp:95` | test-coverage
    The test registers both hooks BEFORE calling ws.start(). It does not exercise registering a per-route hook while the server is running (mid-flight registration). The TSan test covers the lock discipline but external mid-flight registration from a separate thread is not covered.
    *Recommendation:* Low-priority gap since the TSan test covers the lock discipline. A future test that registers a per-route hook from a concurrent non-handler thread would complete coverage.

18. [ ] **code-quality-reviewer** | `src/httpserver/detail/resource_hook_table.hpp:114` | code-elegance
    The log_fn typedef (std::function<void(std::string_view)>) is repeated in resource_hook_table.hpp. The server-wide equivalent uses std::function directly at all call sites with no typedef. The inconsistency creates two parallel conventions within the same codebase.
    *Recommendation:* Either adopt the log_fn typedef in both places, or remove it from resource_hook_table and pass the lambda inline as the server-wide path does.

19. [ ] **code-quality-reviewer** | `src/httpserver/http_resource.hpp:285` | code-readability
    The HTTPSERVER_COMPILATION-gated hook_table_raw_() method has a trailing underscore in its name (hook_table_raw_()), which is the project's convention for internal/private members but is unusual for a method name. Looks like an accidental typo or leftover from a renaming.
    *Recommendation:* Consider renaming to 'hook_table_raw()' (without trailing underscore) since the HTTPSERVER_COMPILATION guard already restricts access.

20. [ ] **code-quality-reviewer** | `src/detail/webserver_dispatch.cpp:384` | code-readability
    handle_dispatch_exception now has a TASK-051 block that reads mr->resource_weak_.lock() followed by res->hook_table_raw_() inside the same scope. This lock + raw-pointer pattern is correct but could leave rtable pointing at freed memory if res is the only remaining reference and goes out of scope before rtable is used. The code is safe here because res lives to the end of the if block, but the pattern requires careful reading.
    *Recommendation:* Add a brief comment like '// res keeps the resource alive while rtable is in use' to clarify the lifetime relationship.

21. [ ] **code-simplifier** | `src/detail/webserver_finalize.cpp:236` | code-structure
    In fire_request_completed_gated the weak_ptr check is done with an explicit expired() call followed by lock(): `if (!mr->resource_weak_.expired()) { res = mr->resource_weak_.lock(); if (res) { ... } }`. The inner `if (res)` guard makes the outer `expired()` redundant — lock() already returns null when expired.
    *Recommendation:* Remove the outer `expired()` guard and keep only `res = mr->resource_weak_.lock(); if (res) { ... }`. This is the same two-liner used in per_route_table() (line 75-78).

22. [ ] **code-simplifier** | `src/detail/webserver_finalize.cpp:97` | naming
    In fire_before_handler_gated there is a local variable named `per_route_table` (line 97) that shadows the free function also named `per_route_table` defined in the anonymous namespace at line 73 of the same file. While it compiles cleanly, the shadowing is confusing when reading the function.
    *Recommendation:* Rename the local variable to `route_tbl` or `rtable` (consistent with the naming used in fire_after_handler_gated, fire_response_sent_gated, and fire_request_completed_gated in the same file).

23. [ ] **code-simplifier** | `src/http_resource.cpp:45` | code-structure
    throw_wrong_phase takes both `requested` and `expected` as parameters, but the error message it generates could be misleading when the user passes a wrong phase: passing `handler_exception` to the `before_handler` overload yields a message saying the 'expected' phase is before_handler, which does not clearly explain the user picked the wrong overload.
    *Recommendation:* Minor: consider dropping the `expected` parameter and replacing its mention in the message with the phrase 'this overload accepts only the phase matching its callable signature'. Alternatively, keep the current approach — the existing message is comprehensive enough.

24. [ ] **code-simplifier** | `test/integ/hooks_per_route_resource_destroyed_first.cpp:67` | code-structure
    Three of the four test cases end with `LT_CHECK_EQ(true, true)` as a sentinel for 'no crash'. This assertion always passes regardless of any condition and adds noise rather than expressing intent.
    *Recommendation:* Remove the tautological LT_CHECK_EQ(true, true) lines. A comment is clearer than a vacuous assertion.

25. [ ] **housekeeper** | `specs/architecture/04-components/http-resource.md` | architecture-not-updated
    The http-resource.md component doc describes http_resource's responsibilities and API surface but makes no mention of add_hook(), the per-resource hook table PIMPL, or the hook-related includes added to http_resource.hpp (hook_handle.hpp, hook_phase.hpp, hook_action.hpp, hook_context.hpp). The hooks.md file already covers the per-route API (§4.10) so the gap is minor.
    *Recommendation:* Consider adding a brief note to specs/architecture/04-components/http-resource.md mentioning that http_resource now also hosts a per-resource hook bus (add_hook, hook_table_ PIMPL) as documented in §4.10.

26. [ ] **performance-reviewer** | `src/detail/webserver_finalize.cpp:75` | race-condition
    per_route_table() helper calls mr->resource_weak_.expired() then mr->resource_weak_.lock() as two separate operations. Between the two calls the weak_ptr can become expired (resource destroyed by another thread), producing a redundant but harmless null-return. Same pattern repeated at lines 238-239 in fire_request_completed_gated.
    *Recommendation:* Remove the .expired() pre-check and rely solely on .lock(): `res_out = mr->resource_weak_.lock(); if (!res_out) return nullptr;`. A single atomic lock() is cheaper than expired()+lock().

27. [ ] **performance-reviewer** | `src/resource_hook_table.cpp:64` | memory-allocation
    fire_short_circuit_impl and fire_void_impl both allocate a fresh Vec snapshot on every call (`Vec snapshot;`), whereas the server-wide equivalents in hook_handle.cpp use a `thread_local` snapshot buffer that is `.clear()`ed and reused. Under high traffic on resources with per-route hooks, the two helpers will allocate and free a std::vector on every fired phase.
    *Recommendation:* Adopt the same thread_local snapshot pattern used by fire_hooks_for_phase / fire_short_circuit_hooks_for_phase (hook_handle.cpp lines 262-265, 330-333): declare `thread_local Vec snapshot;` and call `snapshot.clear();` before copying into it.

28. [ ] **performance-reviewer** | `src/detail/webserver_finalize.cpp:122` | memory-allocation
    fire_before_handler_gated and fire_after_handler_gated each construct a std::function log lambda inline at the call site. Each construction heap-allocates a closure object. These lambdas are passed as `const log_fn&` into the per-route table firing helpers, so they are created on every request that has per-route hooks, even when the hook vector is empty after the any_hooks_ gate check.
    *Recommendation:* Hoist the lambda out of the gate branch and construct it once per gated-fire function scope, or pass a pre-constructed `resource_hook_table::log_fn` stored on the impl (single allocation at server startup).

29. [ ] **security-reviewer** | `src/resource_hook_table.cpp:137` | concurrency
    next_slot_id_ is incremented with memory_order_relaxed, and any_hooks_[phase] is stored with memory_order_release inside the unique_lock. The slot_id is computed outside the critical section. In practice there is no observable bug (the unique_lock provides the fence before the push_back that uses the id), but the intent is clearer and the pattern is safer if the fetch_add is either moved inside the critical section or issued with memory_order_acq_rel.
    *Recommendation:* Move `next_slot_id_.fetch_add(1, std::memory_order_relaxed)` inside the unique_lock scope, or change the order to acquire: `next_slot_id_.fetch_add(1, std::memory_order_acq_rel)`.

30. [ ] **security-reviewer** | `src/detail/webserver_finalize.cpp:75` | concurrency
    per_route_table() checks `mr->resource_weak_.expired()` and then calls `mr->resource_weak_.lock()` as two separate operations. Although `modded_request` is single-threaded per request, the pattern of `expired()` + `lock()` is a well-known TOCTOU anti-pattern with weak_ptr and should be replaced by a direct `lock()` call with null check.
    *Recommendation:* Replace the `if (mr->resource_weak_.expired()) return nullptr; res_out = mr->resource_weak_.lock();` sequence with `res_out = mr->resource_weak_.lock(); if (!res_out) return nullptr;`.

31. [ ] **security-reviewer** | `src/detail/webserver_finalize.cpp:238` | concurrency
    fire_request_completed_gated repeats the `expired()` + `lock()` anti-pattern: `if (!mr->resource_weak_.expired()) { res = mr->resource_weak_.lock(); ... }`. Same reasoning as above.
    *Recommendation:* Replace with `res = mr->resource_weak_.lock(); if (res) { rtable = res->hook_table_raw_(); }`.

32. [ ] **security-reviewer** | `src/resource_hook_table.cpp:106` | concurrency
    resource_hook_table::any_hooks() uses memory_order_relaxed for the gate load (documented as intentional for the hot-path zero-cost check). The acquire chain from ensure_table() through hook_table_raw_() is correct. However a future refactor that moves the hrm acquisition earlier could break the acquire-chain.
    *Recommendation:* Add a comment at fire_before_handler_gated::97 explaining that hook_table_raw_() is safe because hrm was acquired from resolve_resource_for_request under the route_table shared_lock (which provides the acquire barrier for any_hooks_ and the hook table pointer itself).

33. [ ] **security-reviewer** | `src/httpserver/detail/resource_hook_table.hpp:131` | insecure-design
    resource_hook_table uses a single shared_mutex covering all five hook vectors. In a DoS scenario where an attacker causes many concurrent requests to hold shared_locks simultaneously, a legitimate hook deregistration (e.g., from a security-update code path) could be delayed. The snapshot copy itself drops the lock, limiting the window.
    *Recommendation:* Document the known contention scenario in the class header. If hook deregistration latency becomes a concern, consider per-phase mutexes. Current single-mutex design is acceptable for the expected workload.

34. [ ] **spec-alignment-checker** | `src/http_resource.cpp:61` | specification-gap
    The check_phase helper error message always names the specific expected phase rather than the broader constraint. For example, passing hook_phase::body_chunk to the before_handler overload produces a message that says '...expected before_handler...' -- technically accurate but the message body then lists all five permitted phases. Minor UX ambiguity; both the rejected phase and the constraint are present, so this is a phrasing consistency note, not a violation.
    *Recommendation:* Consider restructuring the error message to clearly distinguish 'you passed phase X' from 'this overload requires phase Y (one of the five permitted phases)'.

35. [ ] **spec-alignment-checker** | `src/detail/webserver_finalize.cpp:193` | specification-gap
    In fire_response_sent_gated, the guard condition is '!server_gate && !route_gate && !log_access_alias_' -- meaning if only the log_access alias is set but no per-route gate, the elapsed computation is skipped. This means the alias-using caller sees elapsed=0 nanoseconds. The spec does not require elapsed to be correct for alias callers -- log_access is a v1 alias -- so this is a minor spec interpretation note already covered by an existing comment.
    *Recommendation:* Consider adding a note that log_access alias callers should use ctx.duration_hint_ns==0 as a signal that timing was not measured, or document it in the ctx field comment.

36. [ ] **spec-alignment-checker** | `test/integ/hooks_per_route_concurrent_registration.cpp:107` | acceptance-criteria
    The TSan concurrency test exercises concurrent add_hook from inside a handler on Q while requests to R fire the per-route chain. However, the test does not exercise the handle.remove() path concurrently. The write->remove concurrent path is absent but not listed as required by the acceptance criterion.
    *Recommendation:* Consider adding a second worker thread that removes the oldest handle from the shared handles vector while the primary workers are adding, to give TSan more material on the contention between append and remove_slot paths.

37. [ ] **test-quality-reviewer** | `test/integ/hooks_per_route_invalid_phase_throws.cpp:60` | naming-convention
    The cross-mismatch test at line 155 is named `cross_mismatch_within_valid_phases_throws` — while accurate, its name could be confused with a missing-phase test.
    *Recommendation:* Rename to `before_handler_overload_rejects_response_sent_phase_tag` to make the scenario (overload-vs-phase mismatch among permitted phases) immediately clear without reading the body.

38. [ ] **test-quality-reviewer** | `test/integ/hooks_per_route_resource_destroyed_first.cpp:67` | aaa-violation
    `LT_CHECK_EQ(true, true)` is a sentinel assertion used across four tests in this file (lines 67, 86, 102, 122). It adds no value — it will pass even if the assertion machinery is broken, and it obscures that the actual contract is 'no crash/UAF reaching this point'.
    *Recommendation:* Remove the sentinel `LT_CHECK_EQ(true, true)` assertions and rely on the comment, or use a dedicated macro like `LT_SUCCEED()` if the framework provides one.

39. [ ] **test-quality-reviewer** | `test/unit/hook_api_shape_test.cpp:180` | aaa-violation
    `LT_CHECK_EQ(true, true)` sentinel at line 180 (double_remove_is_noop) has the same issue as above — it passes trivially. The test's only meaningful assertion is 'the second remove() does not crash'.
    *Recommendation:* Same as previous finding: remove the sentinel assertion or use an explicit succeed marker.

40. [ ] **test-quality-reviewer** | `test/integ/hooks_per_route_early_413_per_endpoint.cpp:83` | missing-test
    The test verifies three combinations (2KB to small=413, 2KB to large=200, 500B to small=200) but does not cover the boundary case: exactly 1024 bytes to /upload-small should return 200 (at-limit), while 1025 bytes should return 413. Boundary tests are high-value for this kind of policy hook.
    *Recommendation:* Add `post_bytes(PORT, "/upload-small", 1024)` asserting 200 and `post_bytes(PORT, "/upload-small", 1025)` asserting 413 to pin the inclusive/exclusive semantics of the `> 1024` condition.

41. [ ] **test-quality-reviewer** | `test/integ/hooks_per_route_order.cpp:77` | missing-test
    The acceptance criterion requires 'A request to another resource fires only A.' The second part is tested, but there is no test verifying that B does NOT fire for /s after a request to /r at the per-element level. The existing `LT_CHECK_EQ(firing_order[0], std::string("A"))` does effectively test the absence of B.
    *Recommendation:* Informational only; the existing assertion is sufficient. No change required.

42. [ ] **test-quality-reviewer** | `test/integ/hooks_per_route_concurrent_registration.cpp:107` | slow-test
    The concurrent registration test runs for 800ms (`kRunDuration`) with 8 threads. This is intentional for TSan coverage but will add ~1s to every normal (non-TSan) CI run.
    *Recommendation:* Consider gating the long duration behind a `#ifdef __SANITIZE_THREAD__` or an environment variable, reducing to e.g. 100ms for standard runs and 800ms under TSan.

43. [ ] **test-quality-reviewer** | `test/integ/hooks_per_route_concurrent_registration.cpp:171` | missing-test
    The test only asserts `q_count > 0` and `r_count > 0` to prove the traffic ran. It does not assert that no requests returned an unexpected status code. If the server crashes mid-test, `curl_easy_perform` returns a non-OK code and the counters may still be >0 from earlier iterations, making the crash silent.
    *Recommendation:* Track failure counts per thread and assert `failure_count == 0` after joining, or at least check that the ratio of successful to attempted requests is above a reasonable floor.

44. [ ] **test-quality-reviewer** | `test/integ/hooks_per_route_early_413_per_endpoint.cpp:93` | excessive-setup
    Both `before_handler` lambdas contain a `ctx.request == nullptr` null-guard and a `cl.empty()` guard plus try/catch around `stoul`. The null guard is almost certainly never true in a real dispatch, and the guards partially obscure what the test is actually exercising.
    *Recommendation:* If null request and missing Content-Length are not realistic for this test scenario, remove the guards so the test fails loudly on unexpected precondition violations rather than silently returning `hook_action{}`.
