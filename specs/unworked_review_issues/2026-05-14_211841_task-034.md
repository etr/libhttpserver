# Unworked Review Issues

**Run:** 2026-05-14 21:18:41
**Task:** TASK-034
**Total:** 32 (0 critical, 5 major, 27 minor)

## Major

1. [ ] **architecture-alignment-checker** | `src/httpserver/webserver.hpp:504` | interface-contract
   register_ws_resource takes a raw websocket_handler* pointer. §4.1 (webserver component spec) and DR-010 both specify the signature as register_ws_resource(path, unique_ptr<websocket_handler>) plus a shared_ptr overload. The raw-pointer form was present in the feature/v2.0 base and was carried forward unchanged by this task. This task's stated scope was only removing the #ifdef HAVE_WEBSOCKET guard, but the raw-pointer contract violation now surfaces in the unconditionally-visible public API and is introduced to consumers who previously never saw this method on HAVE_WEBSOCKET-off builds.
   *Recommendation:* Add unique_ptr<websocket_handler> and shared_ptr<websocket_handler> overloads as specified in §4.1 and DR-010. The raw-pointer overload can be removed or kept as a deprecated shim. DR-010 explicitly states: 'Raw-pointer overload removed (extending PRD-HDL-REQ-005).'

2. [ ] **code-quality-reviewer** | `src/webserver.cpp:253` | test-coverage
   The webserver constructor guards HAVE_BAUTH (basic_auth_enabled throws feature_unavailable) and HAVE_GNUTLS (use_ssl throws feature_unavailable) but has no matching guard for HAVE_DAUTH. Enabling digest_auth on a HAVE_DAUTH-off build silently constructs the server instead of throwing feature_unavailable. The task spec (§7) says every build-disabled feature invoked at construction time should throw. The tests in create_webserver_test.cpp cover the TLS and basic_auth throw paths but there is no corresponding #ifndef HAVE_DAUTH guard and no test for digest_auth_true on a HAVE_DAUTH-off build.
   *Recommendation:* Add `#ifndef HAVE_DAUTH if (digest_auth_enabled) { throw feature_unavailable("digest_auth", "HAVE_DAUTH"); } #endif` in the webserver constructor body alongside the HAVE_BAUTH and HAVE_GNUTLS checks, and add a parallel `#ifndef HAVE_DAUTH` test case in create_webserver_test.cpp covering the throw and the false-is-ok paths.

3. [ ] **code-simplifier** | `src/webserver.cpp:270` | patterns
   The features() body uses eight separate #ifdef / #else / #endif pairs to initialise four struct fields, producing 24 preprocessor lines for what is logically four conditional booleans. Each pair is spelled identically (true in the #ifdef branch, false in the #else), which makes the pattern verbose and fragile to extend.
   *Recommendation:* Collapse each field into a single conditional expression: `return { !!(0 HAVE_BAUTH_token), !!(0 HAVE_DAUTH_token), ... };` is one common idiom, or define four compile-unit-local constexpr bools just before the return and initialise the struct from them. For example:
  constexpr bool have_bauth =
#ifdef HAVE_BAUTH
      true;
#else
      false;
#endif
  // ...repeat for the other three...
  return {have_bauth, have_dauth, have_tls, have_websocket};
This keeps one #ifdef per feature (not two) and makes the struct-field-to-macro mapping obvious without the interleaved /*comment*/ hints.

4. [ ] **code-simplifier** | `src/websocket_handler.cpp:176` | patterns
   The !HAVE_WEBSOCKET block defines five identical one-liner methods on websocket_session (send_text, send_binary, send_ping, send_pong, close) — each is a single-statement body `throw feature_unavailable("websocket", "HAVE_WEBSOCKET");`. The repetition means the throw arguments must be kept in sync manually across five sites.
   *Recommendation:* Extract a private static helper (or a file-local inline function in the !HAVE_WEBSOCKET block) that constructs and throws the exception, then call it from each of the five methods. For example:
  static void throw_ws_unavailable() {
      throw feature_unavailable("websocket", "HAVE_WEBSOCKET");
  }
This consolidates the feature name and macro string into one location (needless repetition, clean-code §code-smells).

5. [ ] **test-quality-reviewer** | `test/unit/webserver_features_test.cpp:70` | logic-in-test
   The single runtime test `fields_match_build_flags` contains four `#ifdef / #else / #endif` preprocessor branches. While these are preprocessor directives rather than runtime `if` statements, they have the same structural effect: the test body branches on a condition that is always true in any given build, making each alternate branch dead code. In the TLS-off build, the `LT_CHECK_EQ(f.tls, true)` arm (line 83) is never compiled and can never catch a regression. In practice this means a four-way guard block tests only one side of each conditional per build.
   *Recommendation:* The static_asserts above already pin the struct shape. For the runtime values, split into four separate tests — one per flag — each unconditional: `LT_CHECK_EQ(f.tls, webserver::features().tls)` is circular, so the test must still use `#ifdef` or pass the expected value as a compile-time constant. The cleaner alternative is to declare `constexpr bool kExpectedTls = ...` outside the test body and assert inside a single flat test without branching, making the intent explicit: `LT_CHECK_EQ(f.tls, kExpectedTls);`. This removes the internal branching while keeping the test linear.

## Minor

6. [ ] **architecture-alignment-checker** | `src/httpserver.hpp:24` | adr-violation
   The umbrella header guards with '#if __cplusplus < 201703L' (C++17), but DR-001 mandates C++20 as the floor for libhttpserver v2.0. The build system (configure.ac line 47: AX_CXX_COMPILE_STDCXX([20],...)) correctly enforces C++20, making this guard a misleading under-claim that could be confusing for readers and packagers. This is pre-existing relative to this task's diff but remains an alignment gap.
   *Recommendation:* Update the guard to '#if __cplusplus < 202002L' to match the C++20 requirement documented in DR-001 and enforced by the build system.

7. [ ] **architecture-alignment-checker** | `src/webserver.cpp:253` | adr-violation
   The constructor throws feature_unavailable for use_ssl(true) on !HAVE_GNUTLS and for basic_auth_enabled==true on !HAVE_BAUTH, but does NOT throw for digest_auth_enabled==true on !HAVE_DAUTH. The §7 table footnote says basic_auth(true) throws 'consistent with other feature flags', implying digest_auth(true) should also throw when HAVE_DAUTH is off. The asymmetry means a consumer that calls create_webserver().digest_auth(true) on a DAUTH-off build silently succeeds at construction time and receives empty sentinels at runtime instead of an early, actionable error.
   *Recommendation:* Add '#ifndef HAVE_DAUTH / if (digest_auth_enabled) throw feature_unavailable("digest_auth", "HAVE_DAUTH"); / #endif' in the webserver constructor after the HAVE_BAUTH check. Also add a corresponding test case analogous to the basic_auth_true_throws_feature_unavailable_when_no_bauth test.

8. [ ] **architecture-alignment-checker** | `src/webserver.cpp:258` | pattern-violation
   Pre-existing (iter-1): §7 table entry for HAVE_DAUTH originally specified only a sentinel-return for check_digest_auth and empty return for get_digested_user, with no explicit mention of a construction-time throw for digest_auth(true) on HAVE_DAUTH-off builds. The new throw was added (security-reviewer-iter1-1) and is architecturally correct and symmetric with HAVE_BAUTH and HAVE_GNUTLS guards, but the §7 table itself was not updated to document it. This is a documentation gap, not a code defect.
   *Recommendation:* Update specs/architecture/07-feature-availability.md §7 table row for HAVE_DAUTH to add: 'create_webserver::digest_auth(true) throws feature_unavailable at webserver construction time (consistent with HAVE_BAUTH and HAVE_GNUTLS)' — mirroring the existing wording for the HAVE_BAUTH row.

9. [ ] **code-quality-reviewer** | `src/create_webserver.cpp:54` | code-readability
   The comment block at lines 54-65 (above digest_auth_default()) is longer and more detailed than the equivalent comment above basic_auth_default() (lines 43-52). The asymmetry is not harmful but slightly inconsistent: one function gets a rationale comment of 2 lines, the other gets 5 lines.
   *Recommendation:* Trim the digest_auth_default() comment to match the brevity of basic_auth_default()'s comment, or extend the latter to the same level of detail. Minor style consistency issue.

10. [ ] **code-quality-reviewer** | `src/webserver.cpp:270` | code-elegance
   The features() implementation uses a chain of #ifdef / #else / #endif blocks for each of the four fields. While the comment explains the name collision reason, the pattern is verbose and mechanical. A constexpr helper or a single HAVE_* → bool mapping table inside the TU would communicate the same intent with less noise.
   *Recommendation:* Consider four constexpr bool locals (e.g. `constexpr bool k_bauth = ...`) derived from the HAVE_* macros via conditional expressions, then use them in the aggregate initialiser. This eliminates the four repeated #ifdef / #else / #endif stanzas while keeping the translation-unit locality that the existing comment documents as intentional.

11. [ ] **code-quality-reviewer** | `src/webserver.cpp:279` | code-readability
   The struct webserver::features webserver::features() definition requires the elaborated-type-specifier workaround because the nested type and the static member function share the same spelling 'features'. This is a known C++ ambiguity and is handled correctly, but the comment at line 278-284 is longer than the actual code needed to express the intent. The naming collision is survivable but worth noting for future readers.
   *Recommendation:* Consider renaming the nested struct to feature_set or build_features in a follow-up task to eliminate the elaborated-type-specifier ceremony. Not a blocker for this task, but noted for backlog.

12. [ ] **code-quality-reviewer** | `src/webserver.cpp:285` | code-elegance
   The webserver::features() body uses four interleaved #ifdef / #else / #endif blocks to build the return aggregate initializer. While correct and intentional (library-build-time flags, not consumer-time), this is the only place in the codebase using this style for return-value construction. An alternative using constexpr booleans defined once would be slightly more readable.
   *Recommendation:* Low priority. Could predefine 'constexpr bool have_bauth = ...;' local constants at the top of the function body to separate the flag probing from the aggregate construction, similar to how Python boolean expressions are sometimes extracted for readability. Not worth changing alone but a candidate for cleanup.

13. [ ] **code-quality-reviewer** | `test/consumer_fixture.cpp:72` | test-coverage
   touch_ws() silently swallows ALL exceptions (catch (...)). This means a regression where register_ws_resource throws something other than feature_unavailable (e.g. std::logic_error) on a HAVE_WEBSOCKET-on build would pass the fixture without notice. The fixture's stated purpose is compile-and-link proof, but a tighter catch would also guard against unexpected runtime errors.
   *Recommendation:* On the HAVE_WEBSOCKET-on path, let the null-handler throw propagate or catch only httpserver::feature_unavailable specifically. For the fixture's purpose a catch-all is acceptable, but a comment explaining which exceptions are expected would improve clarity for future readers.

14. [ ] **code-quality-reviewer** | `test/unit/webserver_dauth_unavailable_test.cpp:56` | test-coverage
   The throw-path test (ctor_throws_feature_unavailable_when_digest_auth_requested) and its bauth counterpart follow the same structure as the existing webserver_ws_unavailable_test. However, neither the dauth nor the bauth unavailable tests assert the exception type's what() string contains both tokens when running on a HAVE_DAUTH-off build with .digest_auth(false) (the non-throwing case). The negative complementary case is covered only by 'default_ctor_does_not_throw_on_dauth_off', which exercises the default constructor path but not an explicit .digest_auth(false) call.
   *Recommendation:* Add a third test case: explicit .digest_auth(false) on a HAVE_DAUTH-off build must NOT throw — this pins that the sentinel-false default is not the only non-throwing value, and that .digest_auth(false) is an explicit opt-out. Low priority but would give complete coverage of the setter's three states (default-off, explicit-false, explicit-true).

15. [ ] **code-quality-reviewer** | `test/unit/webserver_dauth_unavailable_test.cpp:81` | test-coverage
   The second test (default_ctor_does_not_throw_on_dauth_off) constructs a real webserver on port 8092, which attempts to bind a socket. If the port is in use in CI the test fails for a reason unrelated to the feature under test. The analogous bauth test (webserver_ws_unavailable_test.cpp line 47) does the same on port 8090. These tests require no-listen-socket semantics to be port-safe.
   *Recommendation:* Add .listen_socket(false) to the create_webserver builder call in both webserver_dauth_unavailable_test.cpp and webserver_ws_unavailable_test.cpp so the 'does not throw / construction succeeds' tests do not require a free port to pass. The throw-path tests (which never reach start()) are already fine.

16. [ ] **code-simplifier** | `src/http_request.cpp:816` | code-structure
   The HAVE_DAUTH-off #else block in check_digest_auth_digest (lines 817-824) has six `(void)` suppressor lines, one per parameter, which is more verbose than the equivalent block in check_digest_auth (five suppressors). The asymmetry is caused by the extra `userdigest_size` parameter. This is not wrong but slightly inconsistent in density with the sister function above it.
   *Recommendation:* No change required; the pattern is correct and the asymmetry is inherent to the different parameter lists. Noted only for completeness.

17. [ ] **code-simplifier** | `src/http_request.cpp:924` | code-structure
   get_pass() has no #else comment to explain the sentinel, unlike the equivalent get_user() at line 918 which has `// TASK-034 §7 sentinel: BAUTH disabled at build time -> empty view.`. The #else branch at line 931 silently returns an empty view without explaining the intent.
   *Recommendation:* Add the same one-line comment to the get_pass() #else branch for consistency:
  // TASK-034 §7 sentinel: BAUTH disabled at build time -> empty view.

18. [ ] **code-simplifier** | `src/http_request.cpp:935` | code-structure
   get_digested_user() contains a redundant early-return branch: when connection_ is null and digested_user is empty, the function falls through the `if (!impl_->digested_user.empty())` guard and reaches `if (impl_->connection_ == nullptr) { return impl_->digested_user; }`. This second null-connection check (line 942) is only reached when digested_user IS empty — the path is correct but the extra guard adds noise.
   *Recommendation:* Move the null-connection check before the emptiness check, or hoist both into a single early-return:
  if (impl_->connection_ == nullptr || !impl_->digested_user.empty()) {
      return impl_->digested_user;
  }
This is equivalent (empty view for unset test requests, cached value for live requests), eliminates the duplicate guard, and makes the flow read top-to-bottom without a second detour.

19. [ ] **code-simplifier** | `src/http_request.cpp:950` | code-structure
   In get_digested_user() the early-return guard `if (impl_->connection_ == nullptr) return impl_->digested_user;` (line 955) is reached only when digested_user is already empty (the preceding `if (!impl_->digested_user.empty()) return` already handled non-empty). The null-connection branch therefore unconditionally returns an empty string and could be written as a simple `return EMPTY;` — the current form is not wrong but requires a second mental pass to confirm the two guards are not contradictory.
   *Recommendation:* Replace the inner null-connection guard with `return std::string_view{};` (matching the HAVE_DAUTH-off sentinel comment) so the intent is self-evident without tracing back to the outer check. Behaviour is identical.

20. [ ] **housekeeper** | `:null` | documentation-stale
   No specs/unworked_review_issues/TASK-034.md file exists. Per prior task pattern (TASK-031..033), if the validation loop produced any findings that were intentionally left unworked, they should be recorded in this file. If all validation findings were resolved, this is not required, but the absence should be confirmed intentional.
   *Recommendation:* If validation produced any unworked findings, create specs/unworked_review_issues/TASK-034.md documenting them. If all findings were resolved, no action is needed.

21. [ ] **performance-reviewer** | `src/http_request.cpp:762` | missing-caching
   check_digest_auth calls get_digested_user() after the null-conn guard, which does an MHD IPC call plus a pmr::string assign on the first invocation. If a handler calls check_digest_auth more than once per request the second call is already O(1) due to the non-empty fast-path in get_digested_user(), so this is only a concern when calling both get_digested_user() and check_digest_auth independently on the same request.
   *Recommendation:* No change needed for the TASK-034 scope. The existing lazy-cache in get_digested_user() (lines 950-957) already handles the common repeated-call case. Document in the public API that callers should avoid mixing direct get_digested_user() calls with check_digest_auth() on the same request, or note that the result is already cached.

22. [ ] **performance-reviewer** | `src/webserver.cpp:270` | missing-caching
   webserver::features() is a static function returning compile-time constants but is not declared constexpr or inline, so each call-site pays an out-of-line function call. The four booleans are resolved entirely at compile time; the function body has no run-time work.
   *Recommendation:* Declare the function constexpr (or at minimum inline) in webserver.hpp so the compiler can fold call-sites to a zero-cost aggregate literal. Because the values must reflect the library's build flags (not the consumer's), the body must remain in webserver.cpp, but marking it inline there and adding a matching inline declaration in the header achieves both goals. Alternatively, expose the struct as a constexpr free constant: `inline constexpr webserver::features k_features{...};`.

23. [ ] **security-reviewer** | `src/http_request.cpp:875` | insecure-design
   get_arg_flat() falls through to MHD_GET_ARGUMENT_KIND lookup via get_connection_value() when the key is not found in unescaped_args. On the test-request path (connection_ == nullptr) get_connection_value() correctly returns EMPTY for MHD_GET_ARGUMENT_KIND (the default branch in the switch returns nullptr). However the code path is subtle: a caller who populates unescaped_args via set_arg() on the test path and then calls get_arg_flat() for a non-existent key will silently receive an empty string_view that aliases EMPTY[] (a static const char[]). The alias is safe today but could become a dangling-view issue if the call order changes (e.g. populate_args() triggered on a live connection after the test-request branch is exercised). No immediate exploitability, but the code is fragile.
   *Recommendation:* After the populate_args() call on the live path, return http_request::EMPTY instead of delegating to get_connection_value() for the MHD_GET_ARGUMENT_KIND fallback on test-request paths. Or add an explicit connection_ != nullptr guard before the MHD lookup in get_arg_flat().

24. [ ] **security-reviewer** | `src/webserver.cpp:1701` | insecure-design
   When no internal_error_handler is configured, internal_error_page() returns a 500 response whose body is the raw exception message (e.what()). For a feature_unavailable exception this body will be 'feature \'X\' unavailable: built without HAVE_Y', which leaks library build configuration to HTTP clients. The TASK-034 spec (webserver.hpp line 99, point 5) explicitly states that feature_unavailable lands as a generic 500 like any other std::exception, but does not address the body content. CWE-209 (Information Exposure Through an Error Message).
   *Recommendation:* The default no-handler path should emit an empty body (or a generic 'Internal Server Error' string) rather than e.what(). The operator can configure a custom internal_error_handler to expose whatever detail is appropriate for their deployment. Consider changing the default body to the empty string: http_response::empty().with_status(http_utils::http_internal_server_error).

25. [ ] **security-reviewer** | `src/webserver.cpp:719` | insecure-design
   register_ws_resource on a HAVE_WEBSOCKET-off build correctly throws feature_unavailable before touching any state (lines 727-733). However, on a HAVE_WEBSOCKET-on build, a null handler pointer is rejected with std::invalid_argument (line 721-723), which is correct. The inconsistency is that the null check only exists on the HAVE_WEBSOCKET-on branch. On the HAVE_WEBSOCKET-off branch the (void)handler cast silences the unused-parameter warning and the null check is unreachable because the throw comes first — this is not exploitable, but the asymmetry is confusing and could become a problem if the branch order is changed. CWE-476 (documentation concern only).
   *Recommendation:* Minor: document explicitly (or add a comment) that the null check is intentionally only in the HAVE_WEBSOCKET-on path because the off-path always throws before any state is mutated. No code change is required unless the ordering could be reversed in a future refactor.

26. [ ] **spec-alignment-checker** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-034/src/webserver.cpp:258` | specification-gap
   The digest_auth(true) throw is placed inside webserver::webserver() (construction time) rather than inside create_webserver::digest_auth(bool), which is the builder setter. This is consistent with how use_ssl(true) was implemented (throws at construction, not at setter time), but the task action item says 'create_webserver::use_ssl(true) ... throws at webserver construction time' — so the pattern is intentionally mirrored. No spec violation; noted for clarity.
   *Recommendation:* No change needed. The mirror pattern (throw at webserver construction, not at setter) is consistent with use_ssl and explicitly stated in action item 4 of TASK-034.

27. [ ] **spec-alignment-checker** | `src/httpserver/webserver.hpp:504` | specification-gap
   register_ws_resource still takes a raw `websocket_handler*` pointer. PRD-HDL-REQ-003 (TASK-023 scope) requires `std::unique_ptr` and `std::shared_ptr` overloads for register_ws_resource; the raw-pointer overload is supposed to be gone per PRD-HDL-REQ-005. That requirement belongs to a sibling task, but since TASK-034 touches websocket registration directly the gap is worth flagging. The TASK-034 spec itself only says 'register_ws_resource on non-WS build throws feature_unavailable', which is satisfied — so this is an informational carry-forward note rather than a TASK-034 failure.
   *Recommendation:* Track in TASK-035 (which blocks on TASK-034) to replace the raw-pointer overload with unique_ptr/shared_ptr per PRD-HDL-REQ-003 and PRD-HDL-REQ-005. No action required for TASK-034 approval.

28. [ ] **spec-alignment-checker** | `test/unit/webserver_features_test.cpp:null` | acceptance-criteria
   The acceptance criterion 'TLS-off build asserts webserver.features().tls == false and use_ssl(true).build() throws with what() naming tls and HAVE_GNUTLS' is tested across two separate files: webserver_features_test.cpp for the features() assertion, and create_webserver_test.cpp (lines 614-631) for the throw path. Both tests are correctly implemented and conditionally compiled with #ifndef HAVE_GNUTLS. The split is fully adequate but slightly harder to navigate than a single consolidated test suite for the acceptance criterion.
   *Recommendation:* No code change required; the test coverage is correct. Consider a cross-reference comment in webserver_features_test.cpp pointing to create_webserver_test.cpp for the construction-throw half of the acceptance criterion.

29. [ ] **test-quality-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-034/test/unit/webserver_dauth_unavailable_test.cpp:77` | test-isolation
   The `default_ctor_does_not_throw_on_dauth_off` test constructs a webserver on port 8092 and calls `ws.stop()` without ever calling `ws.start()`. While this is safe because stop() is documented as a no-op when not running, the test implicitly relies on the webserver constructor not binding the port during construction (only during start()). If that contract ever changed, the test would still pass for the wrong reason. More importantly, the test leaks the `ws` object by declaring it inside the try block but calling stop() on it -- if `ws` construction succeeds (HAVE_DAUTH-on build, empty suite), the stop() call is inside the try block only on HAVE_DAUTH-off. The logic is correct but slightly fragile: on a HAVE_DAUTH-off build the ws object is constructed and stop() called, which is fine; but the test name says 'does_not_throw' and the check is `LT_CHECK(!threw)` which is correct. No actual defect, but clarity would be improved by just constructing `ws` without calling stop(), since the webserver constructor does not start the daemon.
   *Recommendation:* Remove the `ws.stop()` call from the `default_ctor_does_not_throw_on_dauth_off` test body since the webserver constructor does not bind any port or start any daemon. The test intent is purely 'construction does not throw', and stop() adds unnecessary noise without testing anything meaningful.

30. [ ] **test-quality-reviewer** | `test/unit/create_test_request_test.cpp:306` | multiple-concerns
   The `full_chain` test (lines 306-346) exercises every builder setter in a single test and then asserts on every getter. This is a kitchen-sink test that touches method, path, version, content, headers, footers, cookies, args, querystring, user/pass (both ifdef branches), and requestor. Any single getter regression causes the test to fail with no indication of which setter is at fault. All individual setters already have focused tests above (build_method_path, build_headers, build_basic_auth, etc.), making full_chain partially redundant.
   *Recommendation:* Either remove `full_chain` as a redundant integration of already-covered individual tests, or keep it as a smoke test but rename it `full_chain_smoke` and trim the assertions to a minimal subset (e.g., just confirm the build() call succeeds and one representative getter works), leaving the per-feature assertions to the individual tests.

31. [ ] **test-quality-reviewer** | `test/unit/create_webserver_test.cpp:645` | missing-test
   The `#ifndef HAVE_BAUTH` block (lines 645-668) tests that `basic_auth(true)` throws `feature_unavailable` and that `basic_auth(false)` does not throw — mirroring the GnuTLS pattern precisely. However, there is no corresponding `#ifdef HAVE_BAUTH` block that tests the *success* path: when HAVE_BAUTH is defined, constructing `webserver{create_webserver(N).basic_auth(true)}` should succeed without throwing. The GnuTLS success path is implicitly covered by integration-level start/stop tests, but the basic-auth success path has no such coverage (basic_auth is not exercised in ws_start_stop.cpp).
   *Recommendation:* Add a `#ifdef HAVE_BAUTH` guarded test in create_webserver_test.cpp that asserts `httpserver::webserver{create_webserver(8085).basic_auth(true)}` does not throw. This mirrors the implicit guarantee that enabling an available feature is always valid and protects against a regression where the feature_unavailable guard fires for both true and false values.

32. [ ] **test-quality-reviewer** | `test/unit/webserver_ws_unavailable_test.cpp:45` | naming-convention
   The test `register_ws_resource_throws_feature_unavailable` uses a manual `bool caught` + `std::string msg` pattern (lines 47-58) rather than a structured exception assertion helper, which is consistent with how the GnuTLS test in create_webserver_test.cpp (lines 615-631) is written. However, three separate `LT_CHECK` assertions on `caught` and the two substrings are spread across three lines without grouping comments, making the intent slightly harder to scan. The pattern is not wrong but is less readable than the helper used elsewhere in create_webserver_test.cpp (`throws_invalid_argument_with`).
   *Recommendation:* If the littletest framework lacks an `LT_CHECK_THROWS_WITH` macro, consider extracting a `throws_feature_unavailable_with(op, feature, flag)` helper (analogous to `throws_invalid_argument_with`) and use it in both the webserver_ws_unavailable_test.cpp and the `#ifndef HAVE_GNUTLS` / `#ifndef HAVE_BAUTH` blocks in create_webserver_test.cpp to reduce duplication and improve readability.
