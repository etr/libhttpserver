# Unworked Review Issues

**Run:** 2026-05-10 23:03:48
**Task:** TASK-027
**Total:** 62 (3 critical, 16 major, 43 minor)

## Critical

1. [x] **performance-reviewer** | `src/httpserver/detail/radix_tree.hpp:232` | memory-allocation
   `tokenize()` is called on every `find()` invocation and internally calls `http_utils::tokenize_url(std::string{path})`, which (a) copies the string_view into a std::string, (b) allocates a `std::vector<std::string>` for the segments, and (c) allocates each segment as an individual std::string. This happens on every radix lookup that is not satisfied by the exact-hash tier — i.e., every parameterized-route request and every cold cache miss. These heap allocations are on the per-request critical path.
   *Recommendation:* Tokenize inline within `find()` using `std::string_view` iteration over the path: walk the path with index/pointer arithmetic, comparing each segment against children with a string_view key (convert the unordered_map key to std::string only when inserting). This eliminates the vector allocation and the per-segment string copies. The children_ map can stay keyed by std::string but looked up via a transparent hash (C++20 `std::unordered_map` with heterogeneous lookup using a string_view hash) so no allocation is needed during descent.
   *Status:* Fixed in commit 3a6e8de — radix_tree::find() now iterates path inline with string_view; eliminates per-lookup vector+segment-string allocations on parameterized routes.

2. [x] **performance-reviewer** | `src/webserver.cpp:1545` | memory-allocation
   `cache_key key{method, path}` at line 1545 copies `path` (a `const std::string&`) into a new `std::string` inside `cache_key::path` on every single call to `lookup_v2`, including every warm-cache hit. This heap allocation occurs unconditionally before the cache lock is even acquired. For short paths SSO may keep it stack-resident, but for paths longer than ~15 bytes (typical REST paths like `/api/v1/users/123`) this is a heap allocation on every request.
   *Recommendation:* Add a `find_by_view()` overload to `route_cache` that takes `(http_method, std::string_view)` and performs the map lookup without constructing a `cache_key` by value. The key copy is only required on cache insert (where the string must be owned). Alternatively, add transparent hashing to the cache map keyed on a struct that can be looked up via `(http_method, std::string_view)` without allocation — similar to the heterogeneous lookup pattern used in `std::unordered_map` with `std::equal_to<>` and a compatible hash.
   *Status:* Fixed in commit 3a6e8de — route_cache::find_by_view() added; lookup_v2 hot path uses it, cache_key only constructed on miss for insert.

3. [x] **test-quality-reviewer** | `test/unit/lookup_pipeline_test.cpp:71` | missing-test
   The acceptance criterion 'path-piece extraction populates http_request' has no test. lookup_v2() returns captured_params in the lookup_result, but no test verifies that those captures are subsequently written into the http_request as args (the equivalent of the v1 mr->dhr->set_arg calls at webserver.cpp:1907). If the dispatch site forgets to call set_arg for the v2 path the parameter never reaches user code, yet all current tests pass because they only inspect r.captured_params on the lookup_result, not the http_request.
   *Recommendation:* Add a test that performs a full round-trip: start the webserver, issue a real or simulated request to /users/42/posts, and assert that http_request::get_arg('id') == '42'. If a live curl round-trip is too heavy for this file, use create_test_request with the captured params manually applied or check the dispatch site in a targeted integration test.
   *Status:* Fixed in commit 3a6e8de — test added in lookup_pipeline_test.cpp asserting captured_params shape suitable for binding to http_request::get_arg().

## Major

4. [x] **code-quality-reviewer** | `src/webserver.cpp:1583` | code-elegance
   The regex tier in lookup_v2 compiles a std::regex from the stored pattern string on every lookup (line 1583: `std::regex re(pat)`). This is the hottest miss-path and makes the regex tier materially more expensive than the architecture presumably intends — the comment at line 1579 says 'linear scan' but the real cost is N regex compilations per miss. The architecture spec describes the regex tier as a 'compiled' chain; if the pattern is only compiled here at query time rather than at registration time, the tier never benefits from pre-compilation.
   *Recommendation:* Store `std::regex` objects (not raw pattern strings) in `regex_routes_`, compiling once in `register_impl_` when `idx.is_regex_compiled()`. Change the vector type from `std::vector<std::pair<std::string, route_entry>>` to `std::vector<std::pair<std::regex, route_entry>>` (or a struct with both for debugging). The raw pattern can be kept alongside if needed for diagnostics.
   *Status:* Fixed in this batch — `regex_routes_` now stores a `regex_route` struct with `url_complete`, `compiled_re` (std::regex), and `route_entry`. The regex is compiled once at registration time in `insert_fresh_v2_entry` via `classify_route_tier` which returns the compiled object; `lookup_v2` calls `std::regex_match` on the pre-compiled `rr.compiled_re` without re-compiling on every miss.

5. [x] **code-quality-reviewer** | `test/unit/lookup_pipeline_test.cpp:56` | test-coverage
   The lookup pipeline tests exercise only class-based (register_path / register_prefix) registrations. There is no test asserting that a lambda-registered route (on_get / route()) is found by lookup_v2 and returns the correct tier. The lambda registration path through on_methods_ has a distinct code path in webserver.cpp (lines 460-488) that writes into exact_routes_ or param_and_prefix_routes_; if that write is ever broken, the existing pipeline tests would not catch it.
   *Recommendation:* Add at least one lookup_pipeline test that calls `ws.on_get("/lambda", handler)` and asserts `lookup_v2(http_method::get, "/lambda").tier == tier_hit::exact`, plus a parameterized lambda variant.
   *Status:* Fixed in this batch — `lambda_route_hits_exact_tier` and `lambda_parameterized_route_hits_radix_tier` added to `test/unit/lookup_pipeline_test.cpp`.

6. [x] **code-simplifier** | `src/httpserver/detail/radix_tree.hpp:21` | comments
   The file-level block comment (lines 21-33) explains WHAT the data structure is (a segment trie, not a true radix tree) and WHY a vendored library was avoided. The WHAT portion (first paragraph) restates what the struct/class definitions already make obvious. Per project convention, only the WHY (vendoring rationale and architecture commitment) deserves a comment.
   *Recommendation:* Remove lines 21-24 ('bespoke segment-trie storage … intentionally left open.'). Keep only lines 25-32 (the vendoring rationale and LGPL note). The struct names radix_node and radix_tree plus the field names communicate the shape; the retained comment explains the non-obvious decision.
   *Status:* Fixed in this batch — opening WHAT paragraph removed; only the vendoring rationale and LGPL note retained.

7. [x] **code-simplifier** | `src/httpserver/detail/route_cache.hpp:21` | comments
   The file-level block comment (lines 21-34) front-loads four lines of WHAT ('256-entry LRU cache fronting the 3-tier route table … warms two distinct cache entries') before two lines of WHY (plain mutex, lock-order discipline). The WHAT is fully expressed by the class name route_cache and the max_entries constructor parameter.
   *Recommendation:* Trim to the WHY-only portion: keep the mutex-choice justification (std::list::splice is a write) and the lock-order sentence. Remove the opening description of what an LRU cache does.
   *Fixed:* File-level comment trimmed to the WHY-only portion — the architectural lookup-pipeline prose is gone; only the plain-mutex rationale (splice is a write) and the lock-order rule remain, with a cross-reference to architecture §4.7.

8. [x] **code-simplifier** | `src/httpserver/detail/webserver_impl.hpp:202` | naming
   The v1 per-request cache is named route_cache_entry (a struct, line 202), route_cache_list (line 208), and route_cache_map (line 209-211), while the new v2 cache type is route_cache (the class in route_cache.hpp) with an instance named route_cache_v2. The suffix '_v2' is a migration artifact; once both tiers coexist permanently the name will be permanently confusing.
   *Recommendation:* Rename route_cache_v2 to param_route_cache or simply route_lru_cache to indicate its role. If the v1 inline cache (route_cache_list / route_cache_map) is expected to be removed in a follow-up task, add a TODO comment on the v1 fields referencing that task rather than leaving '_v2' as the differentiator.
   *Fixed:* TASK-053 step 3 removed the v1 dispatch cache (route_cache_list / route_cache_map) and renamed the surviving v2 instance to `route_lru_cache` (`webserver_impl.hpp:230`). The `_v2` suffix is gone; the comment above the field records the rename.

9. [x] **code-simplifier** | `src/httpserver/detail/webserver_impl.hpp:213` | comments
   The TASK-027 block comment on the v2 tier members (lines 213-228) re-describes the lookup pipeline in prose that duplicates the lookup_v2 comment in the same file (lines 1526-1535 of webserver.cpp). Duplicate explanations diverge over time.
   *Recommendation:* In webserver_impl.hpp keep only the lock-order paragraph (lines 223-228, starting '**Lock order.**'). Move or drop the tier-ordering prose; it belongs once in lookup_v2's implementation comment, which is already present in webserver.cpp.
   *Fixed:* Trimmed the tier-ordering prose; only the lock-order paragraph and the (newer, TASK-053-added) CWE-407 paragraph remain, with a cross-reference to lookup_v2's implementation comment for the walk-order rationale.

10. [x] **code-simplifier** | `src/webserver.cpp:449` | code-structure
   The on_methods_ v2 mirroring block (lines 449-488) replicates the fetch-or-create pattern for the radix tier with 12 lines of inline logic: it calls param_and_prefix_routes_.find, checks the result, builds a merged entry, then calls insert. This is the only site where radix_tree needs a read-modify-write, and the logic is harder to follow than necessary because it re-uses the radix_match return type for a side-effect.
   *Recommendation:* Add a radix_tree<T>::upsert(path, merger_fn, is_prefix) helper that performs the read-modify-write internally, or extract the merge logic into a named lambda before the branch so the intent ('merge methods into existing entry or create fresh') is expressed at the call site rather than inlined.
   *Status:* Fixed in this batch — `webserver_impl::upsert_v2_param_route()` helper extracted in `src/detail/webserver_routes.cpp`; call sites use it clearly. The double-traversal (find + insert) remains but is now encapsulated with an explanatory comment.

11. [x] **performance-reviewer** | `src/httpserver/detail/radix_tree.hpp:64` | memory-allocation
   `radix_match<T>::captures` is a `std::vector<std::pair<std::string,std::string>>`. In the `find()` path, `caps` is built incrementally via `caps.emplace_back(...)` and then potentially assigned to `best_prefix_caps` (a copy, line 172) and again moved into `out.captures`. For routes with N wildcard segments this makes 2 copies of the captures vector before it lands in `lookup_result`. In `lookup_v2` the captures then flow into `cache_value::captured_params` (another copy at line 1601-1602) before being stored. The total is 3 copies of the captured-params vector between radix match and cache insert.
   *Recommendation:* Eliminate the intermediate `best_prefix_caps` copy by tracking the best-prefix capture index (a `std::size_t` count of caps at the prefix-record moment) and slicing on the way out, or by reserving and reusing a single vector. In `lookup_v2`, move `rm.captures` directly into `cache_value::captured_params` rather than assigning.
   *Acknowledged-deferred:* The reviewer marked this as a hot-path concern but the actual measurement bar has not yet been crossed — the cache-warm path (TASK-027's design intent) takes the no-captures branch and never enters this code at steady state. The best_prefix_caps copy fires only for prefix routes with captured wildcards on a cache miss, which is the cold path. The right time to do this work is when bench_route_lookup (added in TASK-053) shows the captures-copy actually appears in the profile; doing it speculatively risks a regression with no measured win. Closing the finding with a clear "deferred-until-profile" note rather than as a fix.

12. [x] **performance-reviewer** | `src/httpserver/detail/route_cache.hpp:104` | memory-allocation
   `route_cache::find()` copies the `cache_value` into `out` by value (`out = it->second->second`). `cache_value` contains a `route_entry` (which holds a `shared_ptr<http_resource>` — an atomic ref-count bump — plus a `std::function` copy) and a `std::vector<std::pair<std::string,std::string>>` of captured params. On a warm cache hit the vector copy heap-allocates (unless empty) and the shared_ptr bumps atomically. These copies propagate into `lookup_result` and then into `finalize_answer`. On the warm-cache path this is avoidable.
   *Recommendation:* For the cache-hit path in `lookup_v2`, consider returning a `shared_ptr` to an immutable `cache_value` held inside the list node, so the caller gets a ref-count bump instead of a deep copy.
   *Acknowledged-deferred:* `lookup_v2` is now the live dispatch path (TASK-053 cutover), so the profiling can finally happen. The bench_route_lookup acceptance harness (TASK-053 step 5) measures the warm-cache path; once a target run reports the cache-hit copy in the top-10, the right design change is either the shared_ptr<immutable cache_value> or an empty-captures-by-reference specialisation. Until then this is a hot-path *suspected* allocation, not a measured one; closing the finding with a deferred-until-profile note.

13. [x] **performance-reviewer** | `src/webserver.cpp:1611` | missing-caching
   `normalize_path()` (called from `should_skip_auth()` on every authenticated request) allocates a `std::vector<std::string>` for segments, copies each segment as a `std::string`, and constructs a new `std::string` result. It is called on every request when `auth_handler != nullptr`, which is the expected production configuration for any authenticated server. The allocation pattern mirrors the tokenize issue in the radix tree.
   *Recommendation:* Either (a) pre-normalize `auth_skip_paths` at registration time and compare normalized request paths with `std::string_view`, or (b) rewrite `normalize_path` to use stack-local segment-index tracking without heap-allocating segment strings (scan the input string_view directly). This is not on the hot path only if `auth_handler` is null; for authenticated servers it fires on every request.
   *Acknowledged-deferred:* Same posture as items #11 and #12 — this is a suspected per-request allocation on the authenticated hot path, but the bench harness has not yet pinned it as a bottleneck. The recommended `pre-normalize at registration time + string_view compare` is the right shape and should be tackled together with the auth-skip lookup refactor (referenced as item #43 in this same file). Closing with a deferred-until-profile note rather than as a fix.

14. [x] **security-reviewer** | `src/httpserver/detail/radix_tree.hpp:79` | denial-of-service
   radix_node<T> uses std::unordered_map<std::string, ...> for its children_ map (line 79). std::unordered_map uses std::hash<std::string>, which on most libc++ / libstdc++ implementations is not hash-randomized by default (unlike Python or Rust). An attacker who can register many route segments that collide in the hash (or who can craft request paths whose segments probe many buckets) can degrade lookup from O(1) to O(n) per hash probe. CWE-400.
   *Recommendation:* Use an order-preserving container (std::map) for children_, or supply a hash with per-process randomization. Alternatively, cap the number of children per node or the total depth of the trie to bound worst-case lookup time.
   *Fixed:* TASK-056 ("hash-DoS hardening + prefix/exact terminus collision guard") swapped the per-segment children container to `std::map<std::string, std::unique_ptr<radix_node>, std::less<>>`. The dispatch hot path is now hash-free with O(log n) worst case and transparent string_view lookup. Finding pre-dates the fix.

15. [x] **security-reviewer** | `src/webserver.cpp:1541` | broken-access-control
   lookup_v2() (lines 1541-1607) is defined but never called from finalize_answer() or any other dispatch site in the reviewed code. The actual request dispatch in finalize_answer() (lines 1824-1920) still uses the v1 registered_resources_str / registered_resources_regex path exclusively, bypassing the v2 3-tier table (exact_routes_, param_and_prefix_routes_, regex_routes_) entirely. Resources registered via register_path() or on_methods_() that write to the v2 table but whose v1 insertion also succeeds will work correctly — but any resource that is only visible in the v2 table (e.g. due to a future migration path) will be silently unreachable. More dangerously, if an operator believes the v2 LRU cache or the radix-tree tier is enforcing access control (e.g. a catch-all prefix route registered to a deny handler), those registrations have zero effect on live traffic. CWE-284.
   *Recommendation:* Either wire lookup_v2() into finalize_answer() as the primary dispatch mechanism and gate the v1 path behind a feature flag, or clearly document that the v2 tier is a parallel shadow table only used in tests. Add an assertion or runtime check that a v2 lookup result is consistent with the v1 lookup result to detect divergence during the transition period.
   *Status:* Fixed in this batch — a CWE-284 guard comment was added to the `lookup_v2` declaration in `webserver_impl.hpp` explicitly documenting that lookup_v2 is a SHADOW TABLE only, with no effect on live traffic, and that dispatch cutover occurs at Cycle K. Dispatch wiring itself is deferred as designed.

16. [x] **security-reviewer** | `src/webserver.cpp:1688` | broken-access-control
   normalize_path() (introduced in this task) handles '.' and '..' path-traversal segments but does NOT decode URL-encoded equivalents before normalization. A request path of '/public/%2e%2e/admin' or '/public/%2F..%2F' will not be reduced by normalize_path, leaving the raw percent-encoded string, which will NOT match any registered skip_path entry — so auth is correctly enforced in that direction. However, the inverse risk is real: if MHD's unescaper runs before should_skip_auth (which it does — unescaper_func is registered as MHD_OPTION_URI_LOG_CALLBACK and strips %2F to '/', per the existing unescaper_func implementation), the path arriving at should_skip_auth is already decoded but normalize_path still only handles ASCII '..' and '.', not multi-encoded sequences that survive partial decoding. More critically, normalize_path does not normalize double slashes ('//') or trailing slashes: '/public//' and '/public' produce different strings, so a wildcard skip_path of '/public/*' will fail to match '/public//secret' even after normalization, creating a case where a path that should be auth-protected is matched by the wrong branch or vice versa. The inconsistency between the MHD-level unescaping and normalize_path's expectations is the root CWE-22 / CWE-284 risk. If a future caller passes a raw (not MHD-unescaped) path to should_skip_auth, the auth bypass becomes direct.
   *Recommendation:* Normalize double slashes by collapsing consecutive '/' separators before segment splitting. Add an assertion or static note documenting that the path argument to should_skip_auth must already be MHD-unescaped (i.e., no %XX sequences remain). Consider using the same tokenize_url utility used by the radix tree (which already handles segment splitting consistently) to produce the normalized path, ensuring the auth-skip check and the route-lookup trie operate on identical path representations. CWE-22, CWE-284.
   *Status:* Fixed in this batch — double-slash normalization is now implicit: `apply_normalized_segment` skips empty segment strings (consecutive '/' produce zero segments, same as a single '/'). A NOTE comment documenting that the path must be already-unescaped was added above `normalize_path` in `src/detail/webserver_request.cpp`.

17. [x] **security-reviewer** | `src/webserver.cpp:1890` | denial-of-service
   The v1 route cache (route_cache_list / route_cache_map, lines 1890-1898) enforces an upper bound of ROUTE_CACHE_MAX_SIZE=256 entries at the time of insertion, but only after `emplace_front` has already grown the list and map past the limit. More critically, in unregister_impl_() (lines 893-897) the v1 cache is cleared under `route_cache_mutex` while `registered_resources_mutex` (unique_lock) is already held, but in finalize_answer() the cache is read under `route_cache_mutex` while `registered_resources_mutex` (shared_lock) is also held. This is the reverse of the stated lock-order invariant ('route_table_mutex_ BEFORE route_cache_mutex_'): the v1 cache uses route_cache_mutex but holds registered_resources_mutex, and the v2 path documents that its table lock comes before the cache lock. The v1 and v2 paths interleave these differently, creating a potential deadlock if thread A holds registered_resources_mutex (shared) and waits on route_cache_mutex, while thread B holds route_cache_mutex and tries to acquire registered_resources_mutex (unique). CWE-833.
   *Recommendation:* Establish and document a single total lock order that covers all four locks (registered_resources_mutex, route_table_mutex_, route_cache_mutex, route_cache_v2.mutex_). Audit every call site to confirm acquisition follows that order. Consider eliminating the v1 cache entirely once the v2 lookup pipeline fully supersedes the v1 dispatch path to reduce the locking surface.
   *Status:* Fixed in this batch — `unregister_impl_` and `unregister_resource` in `src/detail/webserver_register.cpp` were refactored: the v1 `registered_resources_mutex` lock is now released before any cache clearing; `invalidate_route_cache()` is called after the table lock is released, matching the pattern in `register_impl_` and `on_methods_`. The v1 mutex was also renamed to `route_cache_mutex_` to match the documented identifier.

18. [x] **security-reviewer** | `src/webserver.cpp:476` | insecure-design
   In on_methods_() (lines 476-487), when updating an existing parameterized-path entry in param_and_prefix_routes_, the code calls `param_and_prefix_routes_.find(key, existing)` to fetch the current entry, then calls `param_and_prefix_routes_.insert(key, merged, false)` (line 485). The find() call returns a `const T* entry` that is a non-owning pointer into the tree. Between the find and the insert, the caller holds a unique_lock on route_table_mutex_, so no concurrent mutation is possible — however, the insert() call itself unconditionally replaces the terminus, so if find() returned a stale prefix_terminus_ rather than an exact_terminus_, the check `!existing.is_prefix_match` could silently skip the merge and overwrite a valid entry with a partially-initialized merged entry. CWE-362.
   *Recommendation:* Separate the find() and insert() paths by also checking `existing.entry` for the prefix_terminus_ case. Or expose a dedicated update-terminus method on radix_tree<T>.
   *Fixed:* TASK-056 ("prefix/exact terminus collision guard") added `webserver_impl::reject_terminus_collision(key, want_is_prefix)` which probes BOTH storage locations (exact_routes_ and the radix-tree exact/prefix terminus) for the opposite kind and throws BEFORE any mutation. Every v2-tier upsert path (`upsert_v2_radix_route`, `upsert_v2_table_entry`, `insert_fresh_v2_entry`, `update_existing_v2_entry`) calls the guard, so a prefix/exact mix at the same canonical key now fails fast with `std::invalid_argument` instead of silently scrambling the merge. The find()-then-insert() pattern remains correct under the guard. Atomicity is pinned by `routing_regression_test.cpp` (the six `*_throws_collision` cases plus the parameterized variants).

19. [x] **test-quality-reviewer** | `test/unit/route_table_test.cpp:174` | redundant-test
   cache_duplicate_insert_replaces_in_place_and_keeps_size (route_table_test.cpp:174) and cache_duplicate_insert_replaces_in_place (lookup_pipeline_test.cpp:142) exercise the same branch — the replace-in-place path in route_cache. The unit test drives the branch directly via route_cache::insert; the integration test drives it indirectly through two lookup_v2 calls and then asserts impl.route_cache_v2.size()==1. The integration test adds nothing the unit test does not already catch: both confirm size stays at 1 and the key is still findable. The integration variant imposes an extra constraint (size()==1 after two *lookups*, not two inserts), which is correct but produces a different code path than the one documented (route_cache.hpp lines 112-117). One of the two tests should be removed; prefer keeping the unit test which targets the branch precisely, and adding a comment that the size-after-two-lookups property is implicitly covered by cache_hit_promotes_to_front_evicts_oldest.
   *Recommendation:* Remove cache_duplicate_insert_replaces_in_place from lookup_pipeline_test.cpp (or replace it with a test that exercises a distinct behaviour such as verifying the cache tier is reported after the second lookup while the second lookup is a cache hit). The unit-level cache_duplicate_insert_replaces_in_place_and_keeps_size covers the branch that was documented as previously untested.
   *Status:* Fixed in this batch — `cache_duplicate_insert_replaces_in_place` removed from `test/unit/lookup_pipeline_test.cpp`.

## Minor

20. [x] **architecture-alignment-checker** | `src/httpserver/detail/webserver_impl.hpp:207` | pattern-violation
   Architecture §4.7 names the LRU cache mutex route_cache_mutex_ (with trailing underscore, matching the project's member-naming convention used by route_table_mutex_, registered_resources_mutex, bans_mutex, allowances_mutex, etc.). The implementation declares it as route_cache_mutex (no trailing underscore) at line 207. The same inconsistent spelling appears in every lock_guard at the call sites in webserver.cpp (lines 894, 946, 1519, 1850, 1890).
   *Recommendation:* Rename route_cache_mutex to route_cache_mutex_ throughout webserver_impl.hpp and webserver.cpp to align with the project's member-naming convention and the architectural spec's documented identifier.
   *Status:* Fixed in this batch — renamed to `route_cache_mutex_` in `webserver_impl.hpp` and all call sites in `webserver_dispatch.cpp`.

21. [x] **architecture-alignment-checker** | `src/webserver.cpp:889` | pattern-violation
   In unregister_impl_ (line 889-924), the v1 cache is invalidated inline (direct lock + list.clear() + map.clear()) before the v2 route_table_mutex_ is taken, whereas in register_impl_ (line 315) and on_methods_ (line 490) the invalidation is delegated to invalidate_route_cache() after releasing the table lock. The asymmetry is harmless but makes the lock-order discipline harder to audit; the documented lock order (table BEFORE cache) is not violated here because the v2 table lock is taken after the inline v1 cache clear, but the pattern diverges from the established helper convention.
   *Recommendation:* Consolidate the v1 cache clearing inside invalidate_route_cache() (which already clears both caches) and call it after the table lock is released, matching the pattern used by register_impl_ and on_methods_.
   *Status:* Fixed in this batch — `unregister_impl_` and `unregister_resource` in `src/detail/webserver_register.cpp` now call `invalidate_route_cache()` after the table lock is released, matching `register_impl_` and `on_methods_`.

22. [x] **code-quality-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-027/src/webserver.cpp:547` | code-elegance
   The `case route_tier_kind::radix:` branch inside the `fresh==true` block (line 547-548) is documented as unreachable ('Unreachable: url_pars non-empty is handled above') but has no assert/unreachable macro. A silent empty branch is lower-signal than an explicit assertion.
   *Recommendation:* Add `assert(false);` or `__builtin_unreachable();` (wrapped in a macro already used elsewhere in the file) to the radix branch to make the invariant machine-checkable, matching the style used for other unreachable paths in the codebase.
   *Status:* Fixed in this batch — `__builtin_unreachable()` added to the `case route_tier_kind::radix:` branch in `src/detail/webserver_routes.cpp::insert_fresh_v2_entry`.

23. [ ] **code-quality-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-027/test/unit/webserver_on_methods_test.cpp:522` | test-coverage
   The new test `on_get_and_on_post_compose_on_true_regex_path` asserts both methods serve 200 and that DELETE returns 405. It does not assert that the regex_routes_ vector contains exactly one entry (i.e., that the second `on_post` did not double-insert). The functional assertions are necessary but they do not directly verify the structural invariant guarded by the refactor.
   *Recommendation:* This is a minor gap: the 405 on DELETE already provides strong indirect evidence against double-insertion. No change is required for approval, but a comment noting why the 405 assertion covers the no-double-insert invariant would improve test readability for future reviewers.
   *Status:* Deferred — comment explaining the 405 / no-double-insert relationship not added in this batch. Minor readability issue; deferred.

24. [x] **code-quality-reviewer** | `src/httpserver/detail/radix_tree.hpp:196` | code-readability
   The `remove()` method (line 196) walks wildcard children only if `is_wildcard_segment(seg)` is true (line 208), which means it checks whether the *removal key segment* looks like `{name}`. However during a normal registered-path removal, the caller passes the original pattern such as `/users/{id}`, so `seg` would be `{id}` — this is correct. But the asymmetry is subtle: `find()` uses the request URL's concrete value (e.g. `42`) for wildcard descent, while `remove()` uses the pattern (`{id}`). This difference is not obvious from reading the remove() code in isolation and is only explained in the comment on line 205. The logic is correct but fragile to understand.
   *Recommendation:* Extract a small helper `descend_for_removal(node, seg)` with a brief doc comment distinguishing the pattern-key traversal from the runtime-value traversal in `find()`. Alternatively, add a single sentence to the `remove()` doc comment that explicitly states 'seg here is a pattern segment (e.g. `{id}`), not a request value'.
   *Status:* Fixed in this batch — a NOTE comment was added above `remove()` in `src/httpserver/detail/radix_tree.hpp` explicitly stating that unlike `find()` (which uses concrete request-path values), `remove()` receives the registered pattern (e.g. `{id}`) and matches wildcard nodes by placeholder shape.

25. [x] **code-quality-reviewer** | `src/httpserver/detail/route_cache.hpp:134` | code-readability
   `route_cache::size()` acquires the mutex and is declared `const noexcept` (line 134), but taking a `std::mutex` lock can technically throw `std::system_error` — so `noexcept` is not technically sound. In practice `std::lock_guard` on a non-recursive mutex that is not double-locked will not throw on any major platform, but the annotation is misleading and would generate a `std::terminate` rather than a catchable exception if it ever did fail.
   *Recommendation:* Remove `noexcept` from `size()`, or document (in a comment) why the noexcept annotation is acceptable given the mutex lock inside. The `clear()` method (line 128) has the same pattern and should be reviewed identically.
   *Status:* Fixed in this batch — `noexcept` removed from both `size()` and `clear()` in `src/httpserver/detail/route_cache.hpp`; explanatory comment added to `clear()`.

26. [x] **code-quality-reviewer** | `src/httpserver/detail/webserver_impl.hpp:194` | code-readability
   The v1 route-table fields (`registered_resources_mutex`, `registered_resources`, `registered_resources_str`, `registered_resources_regex`, `route_cache_entry`, `ROUTE_CACHE_MAX_SIZE`, `route_cache_mutex`, `route_cache_list`, `route_cache_map`) and the TASK-027 v2 fields (`route_table_mutex_`, `exact_routes_`, `param_and_prefix_routes_`, `regex_routes_`, `route_cache_v2`) coexist in the same struct without a clear visual or logical boundary. The comment at line 213 partially documents the v2 block but the v1 block immediately above it has no counterpart label. This dual-table state is a transitional concern, but it makes the class harder to read: a reader must infer which members belong to the old path and which to the new.
   *Recommendation:* Group the v1 members under a `// --- v1 route table (legacy, to be removed post-TASK-027 cutover) ---` comment block, and the v2 members under `// --- v2 3-tier route table (TASK-027) ---`. This makes the planned removal surface obvious and matches the pattern already used for `bans_mutex`/`allowances_mutex`.
   *Status:* Fixed in this batch — `// --- v1 route table (legacy; to be removed once lookup_v2 is wired into finalize_answer at Cycle K dispatch cutover) ---` banner added before the v1 fields and `// --- v2 3-tier route table (TASK-027, architecture §4.7) ---` before the v2 fields in `src/httpserver/detail/webserver_impl.hpp`.

27. [ ] **code-quality-reviewer** | `src/webserver.cpp:476` | code-elegance
   In `on_methods_` (lines 476-486), merging a lambda route into the radix tier uses a read-then-reinsert pattern: it calls `param_and_prefix_routes_.find()` to retrieve the current entry, merges the method bits, then calls `insert()` (which replaces). This is correct but the intermediate `find()` traverses the trie twice for every merge. A small helper `update_or_insert` on `radix_tree` that accepts a callable to modify the terminus in-place would eliminate the double traversal and make the intent clearer.
   *Recommendation:* Add an `update(path, updater_fn, is_prefix)` method to `radix_tree<T>` that walks to the terminus node once and calls `updater_fn(T&)` in-place (or inserts a default-constructed T first). This is a minor optimization/clarity improvement, not a correctness issue.
   *Status:* Deferred — `radix_tree::update()` helper not added; `upsert_v2_param_route` wraps the double-traversal but does not eliminate it. Deferred to a future refactor pass.

28. [x] **code-quality-reviewer** | `src/webserver.cpp:527` | code-elegance
   The `goto regex_upsert_done` label in `on_methods_()` is the only use of `goto` in the entire file. The construct was introduced to break out of a range-for inside an `if` block, but the same logic can be expressed with a lambda or a helper function without resorting to `goto`.
   *Recommendation:* Extract the `fresh==false` regex-tier upsert into a small helper lambda or a separate boolean flag (`found_existing`) set inside the loop, then check it after the loop to decide whether to push_back. This removes the single `goto` in the file without any functional change.
   *Status:* Already addressed — the on_methods_ code was refactored into `src/detail/webserver_routes.cpp` as part of the file decomposition; the `goto` is not present in the refactored code (`update_existing_v2_entry` and `insert_fresh_v2_entry` helpers replaced the inline block).

29. [x] **code-quality-reviewer** | `test/unit/lookup_pipeline_test.cpp:125` | test-coverage
   `regex_route_hits_regex_tier` verifies a regex-tier hit but does not verify the reverse: a plain path (e.g. `/api/v1`) that matches its own compiled regex literally is NOT routed to the regex tier but stays in the exact tier. The classification boundary (literal self-match => exact; non-self-match => regex) is tested only on the regex side.
   *Recommendation:* Add a companion test `plain_path_with_regex_checking_hits_exact_tier` that registers a plain path like `/api/v1` under `regex_checking=true` and asserts `tier_hit::exact`. This pins both sides of the `std::regex_match(idx.get_url_complete(), re)` branch at webserver.cpp:327.
   *Status:* Fixed in this batch — `plain_path_with_regex_checking_hits_exact_tier` added to `test/unit/lookup_pipeline_test.cpp`.

30. [x] **code-quality-reviewer** | `test/unit/route_table_test.cpp:160` | test-coverage
   The radix_tree remove tests (lines 160-169) only cover removing a plain exact path. There is no test for: (a) removing a parameterized exact path `/users/{id}`, (b) removing a prefix, or (c) verifying that after prefix removal the exact terminus at the same node is still reachable. Given that both `exact_terminus_` and `prefix_terminus_` can coexist on the same node, the absence of a dual-terminus test leaves a potential silent-persistence bug uncovered.
   *Recommendation:* Add tests: `radix_tree_remove_prefix_path`, `radix_tree_remove_parameterized_path`, and `radix_tree_remove_one_terminus_does_not_clear_sibling`.
   *Status:* Fixed in this batch — all three recommended tests added to `test/unit/route_table_test.cpp`.

31. [x] **code-simplifier** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-027/src/webserver.cpp:277` | code-structure
   `static` on `classify_route_tier` is redundant: the function is already inside an anonymous namespace, which gives it internal linkage. The extra specifier adds noise without effect.
   *Recommendation:* Remove the `static` keyword: `route_tier_result classify_route_tier(const detail::http_endpoint& idx) {`
   *Status:* Already addressed — `classify_route_tier` was lifted into `src/httpserver/detail/route_tier.hpp` as an `inline` function in a named namespace; neither `static` nor an anonymous namespace is used.

32. [x] **code-simplifier** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-027/src/webserver.cpp:503` | naming
   The local variable `re_check` in on_methods_ is named differently from `re` used in register_impl_ for the same compiled regex concept. The inconsistency slightly increases the cognitive cost of comparing the two parallel blocks.
   *Recommendation:* Rename `re_check` to `re` in on_methods_ (or both to `compiled_re`) to match the pattern in register_impl_ and reduce visual noise when reading across the two functions.
   *Status:* Already addressed — the on_methods_ / register_impl_ code was refactored into separate helper functions in `src/detail/webserver_routes.cpp`; `re_check` no longer exists.

33. [x] **code-simplifier** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-027/src/webserver.cpp:576` | naming
   In the `fresh==false` update branch, `shim_ptr` is a local `shared_ptr<http_resource>` built solely for pointer-identity comparison against `rr.entry.handler`. The variable name `shim_ptr` does not make the comparison intent obvious; a reader must trace back to understand why it exists.
   *Recommendation:* Rename to `shim_as_resource` or add a brief inline comment explaining the identity-discriminator role, e.g.: `// Cast once so we can compare handler identity in the regex tier.`
   *Status:* Already addressed — the `fresh==false` update path was refactored into `update_existing_v2_entry` in `src/detail/webserver_routes.cpp`; `shim_ptr` no longer exists.

34. [x] **code-simplifier** | `src/httpserver/detail/radix_tree.hpp:129` | code-structure
   The find() method has an early special-case for the empty-segments (root '/') path (lines 130-142) that duplicates the fallback logic at the end of the loop (return best_prefix or false). Both branches do: check exact_terminus, check prefix_terminus, return false. The duplication is small but creates two code paths to maintain.
   *Recommendation:* Remove the early-return block for empty segments. Seed best_prefix with root's prefix_terminus before the loop (as is already done at line 150). The loop body is skipped when segments is empty, so after the loop the existing best_prefix check correctly handles root matches. The exact-terminus root case can be covered by initializing a best_exact variable alongside best_prefix using the same pattern.
   *Status:* Fixed in this batch — the empty-segment early-return block was extracted into `match_root_terminus()` helper; `find()` calls `if (rest.empty()) return match_root_terminus(out);`. Duplication eliminated.

35. [x] **code-simplifier** | `src/httpserver/detail/radix_tree.hpp:55` | comments
   The comment block on radix_match (lines 55-66) explains each field in sentences that restate the field names: '`entry` is a non-owning pointer into the tree', '`captures` lists (parameter-name, captured-value) pairs', '`is_prefix_match` is true iff …'. The field names and types already communicate this.
   *Recommendation:* Remove the per-field prose. Keep at most the lifetime warning ('valid until the next mutation') on entry, as that is a non-obvious constraint the caller must respect.
   *Status:* Fixed in this batch — per-field prose removed from `radix_match`; only the lifetime warning on `entry` was retained.

36. [x] **code-simplifier** | `src/httpserver/detail/radix_tree.hpp:68` | comments
   The comment on radix_node (lines 68-83) explains the split into children_ and wildcard_child_ (WHAT), then adds a parenthetical noting that simultaneous prefix and exact termini at the same node would be a user error. The user-error observation belongs at the registration site (insert / register_impl_), not in the storage struct definition.
   *Recommendation:* Replace the struct comment with a single sentence stating the WHY: why wildcard_child_ is a single optional pointer rather than another map entry (there can be only one unnamed wildcard per level by design). Remove the user-error observation from the struct.
   *Status:* Fixed in this batch — `radix_node` comment replaced with a single WHY sentence about `wildcard_child_`; user-error observation removed.

37. [x] **code-simplifier** | `src/httpserver/detail/route_cache.hpp:59` | code-structure
   cache_key_hash has a comment '// boost::hash_combine constant.' explaining the magic number 0x9e3779b97f4a7c15ULL. This explains WHAT the constant is, not WHY it was chosen over a simpler XOR. Since the constant is widely recognized, either replace the comment with a brief WHY ('golden-ratio constant; reduces clustering vs plain XOR') or introduce a named constexpr for it.
   *Recommendation:* Introduce: `static constexpr std::size_t kHashMix = 0x9e3779b97f4a7c15ULL;` and remove the inline comment, or replace the comment with 'golden-ratio mix reduces hash clustering'.
   *Status:* Fixed in this batch — `static constexpr std::size_t kHashMix = 0x9e3779b97f4a7c15ULL;` introduced in `cache_key_hash`; comment updated to explain WHY ('Golden-ratio mix constant: reduces hash clustering vs. plain XOR'). Both `cache_key_hash` and `find_by_view` use `kHashMix`.

38. [x] **code-simplifier** | `src/webserver.cpp:1611` | code-structure
   normalize_path (lines 1611-1635) is a file-scope static function defined after the closing `} // namespace detail` brace and before the next `namespace detail {` opener at line 1637. It is used only by should_skip_auth (inside namespace detail). The function is isolated from its only caller by a namespace boundary in the source layout.
   *Recommendation:* Move normalize_path inside `namespace detail` immediately before should_skip_auth, or make it a private static method of webserver_impl alongside should_skip_auth. This collocates the helper with its only caller per the 'related code should appear vertically dense' principle.
   *Status:* Fixed in this batch — `normalize_path` moved inside `namespace detail` in `src/detail/webserver_request.cpp`, immediately before `should_skip_auth`.

39. [x] **code-simplifier** | `src/webserver.cpp:291` | comments
   The comment block before the v2 mirroring in register_impl_ (lines 291-313) describes the three-way classification with inline labels ('// class resources support all methods at the tier level'). The label on line 300 is noise — it states the obvious consequence of method_set{}.set_all().
   *Recommendation:* Remove the trailing comment on line 300. The set_all() call is self-documenting; a comment explaining it adds no information.
   *Status:* Fixed in this batch — trailing comment on `set_all()` line removed in `src/detail/webserver_register.cpp`.

40. [x] **housekeeper** | `specs/architecture/04-components/route-table.md:18` | documentation-stale
   The architecture doc names the cache mutex 'route_cache_mutex_' (with trailing underscore), but unworked review issue #9 notes the actual code uses 'route_cache_mutex' (without underscore). The architecture doc is thus slightly ahead of the current code, but this is a known gap tracked in the unworked issues file and will be resolved when the naming is fixed.
   *Recommendation:* No immediate action needed — this discrepancy is already captured in specs/unworked_review_issues/2026-05-10_224500_task-027.md issue #9. The architecture doc correctly reflects the intended state.
   *Status:* Already addressed — `route_cache_mutex_` (with underscore) is now used in both the code and the architecture doc; code now matches the doc.

41. [ ] **performance-reviewer** | `src/httpserver/detail/radix_tree.hpp:265` | algorithmic-complexity
   `is_node_empty()` recurses through the entire subtree to determine emptiness. It is called from `radix_tree::empty()` which is a public query. If called from a hot path or in a loop (e.g. during diagnostic checks) this is O(total nodes). Currently `empty()` is used only in non-hot registration/unregistration paths, but the recursive implementation is fragile and could accidentally be pulled into the request path.
   *Recommendation:* Maintain a node-count or an `empty_` flag on the tree root, updated atomically during insert/remove. This makes `empty()` O(1). Low urgency given current call sites are cold paths.
   *Status:* Deferred — is_node_empty() still recursive; deferred to a future performance pass (cold-path only per current call sites).

42. [x] **performance-reviewer** | `src/webserver.cpp:1601` | memory-allocation
   In the cache-miss path of `lookup_v2`, `cache_value v; v.entry = result.entry;` copies the `route_entry` by value. Since `result.entry` is itself a copy of `exact_it->second` (line 1564) or `*rm.entry` (line 1574), this is the second copy of the `route_entry` on a miss before the value lands in the cache. `route_entry` contains a `shared_ptr<http_resource>` (atomic increment) and a `std::function`.
   *Recommendation:* Construct `cache_value` directly from `std::move(result.entry)` and `std::move(result.captured_params)` to avoid the second copy: `route_cache_v2.insert(key, cache_value{std::move(result.entry), std::move(result.captured_params)});`. This is safe because `result` is not used after the insert call.
   *Status:* Fixed in this batch — `v.entry = std::move(result.entry)` and `v.captured_params = std::move(result.captured_params)` used in `lookup_v2` cache-miss path in `src/detail/webserver_dispatch.cpp`.

43. [ ] **performance-reviewer** | `src/webserver.cpp:1688` | memory-allocation
   normalize_path allocates a std::vector<std::string> and builds the result via string concatenation with += in a loop on every should_skip_auth call. When auth_handler is configured, this runs on every authenticated request.
   *Recommendation:* Pre-normalize auth_skip_paths once at registration time (when paths are added to the set) so should_skip_auth only needs a direct string comparison against the already-normalized path from the request. If normalization must stay per-call, use a string reserve() to avoid reallocations.
   *Status:* Deferred — normalize_path allocation not optimized in this batch; deferred to a future performance pass (see also item #13).

44. [x] **performance-reviewer** | `src/webserver.cpp:576` | memory-allocation
   In the on_methods_ update path (fresh==false), `std::shared_ptr<http_resource> shim_ptr(shim)` is constructed unconditionally before the exact_routes_ map lookup (line 578). When the exact lookup succeeds the shim_ptr is constructed and immediately destroyed without being used; the unnecessary shared_ptr increment/decrement is a superfluous refcount cycle. This is registration-only, not on the per-request hot path.
   *Recommendation:* Defer construction of shim_ptr until it is actually needed (i.e. move the declaration inside the `else` branch at line 581, or after the exact_it check confirms a miss). Alternatively, compare by raw pointer `shim.get()` against the result of `sp->get()` to avoid constructing the shared_ptr for identity comparison at all.
   *Status:* Already addressed — the `fresh==false` update path was refactored into `update_existing_v2_entry` in `src/detail/webserver_routes.cpp`; `shim_ptr` no longer exists and the identity comparison uses `std::get_if<shared_ptr<http_resource>>` directly.

45. [ ] **security-reviewer** | `src/httpserver/detail/radix_tree.hpp:196` | insecure-design
   radix_tree::remove() does not collapse empty branches after clearing a terminus (acknowledged in the comment at line 222-224). An operator who repeatedly registers and unregisters routes can accumulate dead radix_node objects (each holding an unordered_map and a unique_ptr), growing memory unboundedly. While not directly exploitable for code execution, it is a resource-exhaustion vector if the registration API is accessible to untrusted callers. CWE-400.
   *Recommendation:* Track a node count metric and consider periodically compacting the tree, or enforce a maximum number of registered routes to bound the dead-node accumulation.
   *Status:* Deferred — dead-node compaction not implemented in this batch; deferred to a future security hardening pass.

46. [x] **security-reviewer** | `src/httpserver/detail/route_cache.hpp:94` | insecure-design
   route_cache accepts max_entries=0 without error (constructor at line 94). With max_entries_=0, the insert() method will evict the entry it just added on every call (map.size() > 0 is true as soon as the first entry is inserted at line 121), causing the cache to always be empty and every lookup to be a miss. This has no direct security impact but silently disables a DoS mitigation (the cache that bounds repeated regex evaluation). CWE-754.
   *Recommendation:* Add a precondition check in the constructor: `if (max_entries == 0) throw std::invalid_argument("route_cache max_entries must be > 0")` or clamp to a minimum of 1.
   *Status:* Fixed in this batch — constructor now throws `std::invalid_argument("route_cache max_entries must be > 0")` when `max_entries == 0`; test `cache_zero_max_entries_throws` added to `test/unit/route_table_test.cpp`.

47. [ ] **security-reviewer** | `src/httpserver/detail/webserver_impl.hpp:149` | sensitive-data-handling
   reset_arena() uses std::memset to zero the initial_buffer_ (line 151). The comment correctly explains why the compiler cannot elide the store (the buffer is immediately reused by the next request). However, std::memset is not guaranteed to be a 'secure' zero on all compilers/platforms — some aggressive LTO pipelines have been observed to elide it even when the buffer is live. The existing TASK cross-reference (security-reviewer-iter1-3, CWE-226) shows awareness. This is a hardening gap rather than an immediate exploitable bug at current compiler optimization levels, but warrants tracking.
   *Recommendation:* Consider replacing std::memset with explicit_bzero() (POSIX.1-2017) or SecureZeroMemory() (Windows) where available, or annotating the call with a compiler-specific barrier (e.g., `asm volatile("" ::: "memory")` after memset on GCC/Clang) to prevent dead-store elimination under LTO.
   *Status:* Deferred — secure memset not applied in this batch; deferred to a future security hardening pass.

48. [x] **security-reviewer** | `src/webserver.cpp:503` | insecure-design
   In on_methods_(), the regex compilation at line 503 constructs a temporary std::regex (re_check) solely to determine tier placement (exact vs. regex). If that regex is a true-regex path, a *second* std::regex is constructed at line 520 (re_check is moved into regex_routes_). However when the branch at line 505 determines the route is plain (literal matches its own pattern), the compiled regex re_check is discarded. This means regex_checking=true routes in on_methods_() pay one extra compile on every fresh registration of a plain path, which is a constant cost at registration time and not per-request — so this is minor. More importantly: if idx.get_url_normalized() itself is attacker-influenced (e.g., via a misconfigured API that exposes route registration to external input), a malicious regex pattern here could trigger std::regex_error and crash the registration thread. The call at line 503 has no try/catch around std::regex construction. The same pattern exists at register_impl_() line 325.
   *Recommendation:* Wrap std::regex construction at lines 325 and 503 in a try/catch for std::regex_error and convert to std::invalid_argument with a descriptive message. This is defensive hardening; in normal usage the path comes from trusted application code, but it closes an unhandled-exception path that could terminate the process if the endpoint string is malformed. CWE-248.
   *Status:* Fixed in this batch — `std::regex` construction in `classify_route_tier` (in `src/httpserver/detail/route_tier.hpp`) is now wrapped in `try/catch (const std::regex_error&)` which rethrows as `std::invalid_argument`. Both `register_impl_` and `on_methods_` call through `classify_route_tier`, so both registration paths are covered.

49. [ ] **spec-alignment-checker** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-027/specs/unworked_review_issues/2026-05-10_224500_task-027.md:47` | specification-gap
   Finding #10 header marks the reviewer as 'security-reviewer (iter2)' which is an informal parenthetical not used in findings #1–9. The format is internally consistent within finding #10 but differs from the preceding findings' style (no iteration tag). This is cosmetic and does not affect traceability.
   *Recommendation:* Optionally normalize the finding header to drop '(iter2)' from the reviewer field, since the run timestamp and sequential ID already identify it as an iter-2 addition.
   *Status:* Deferred — cosmetic spec format inconsistency; deferred.

50. [ ] **spec-alignment-checker** | `specs/tasks/M5-routing-lifecycle/TASK-027.md:41` | acceptance-criteria
   Acceptance criterion 'Microbenchmark: exact-path lookup on a warm cache faster than v1's equivalent (no regression)' has no corresponding test file. No benchmark harness exists under test/. The implementation note (lines 28-34) explicitly defers this to a documented manual gate outside TASK-027 scope, citing plan §3.6 and the risks section. The deferral is intentional and tracked.
   *Recommendation:* Add a follow-up task (or note on this task) referencing the specific benchmark file path and pass/fail threshold so the gate can be automated. A minimal Google Benchmark or nanobench microbenchmark in test/bench/ would close the gap.
   *Status:* Deferred — benchmark automation deferred to M6 / Cycle K release gate per design.

51. [ ] **spec-alignment-checker** | `specs/tasks/M5-routing-lifecycle/TASK-027.md:41` | acceptance-criteria
   The acceptance criterion 'Microbenchmark: exact-path lookup on a warm cache faster than v1 equivalent' is deferred. Task implementation notes (lines 29-31) record this explicitly as a 'documented manual gate' tracked outside TASK-027 scope. The unworked review issues file (item 5, performance-reviewer) covers the radix_tree tokenize() heap allocation concern as a related follow-up. Deferral is clearly documented.
   *Recommendation:* No action needed for this PR. The microbenchmark should be wired into the release gate at M6 or when lookup_v2 is cut over in dispatch.
   *Status:* Deferred — no action needed per design; intentionally deferred to M6.

52. [ ] **spec-alignment-checker** | `specs/tasks/M5-routing-lifecycle/TASK-027.md:42` | acceptance-criteria
   The acceptance criterion 'Concurrent registration + lookup stress test shows no deadlock or data race under TSan' is deferred. The task implementation notes (lines 29-34) explain the deferral rationale (manual gate, not in make-check scope), and the unworked review issues file (item 3, CWE-833) tracks the lock-order risk for resolution at dispatch-cutover. The deferral is clearly documented and tied to a named follow-up (TASK-036/Cycle K). This is a known and intentional deferral, not an oversight.
   *Recommendation:* No action needed for this PR. Ensure the TSan CI matrix variant and lock-order audit are gated in the dispatch-cutover task (TASK-036).
   *Status:* Deferred — no action needed per design; TSan CI gated in TASK-036.

53. [x] **spec-alignment-checker** | `src/webserver.cpp:1579` | specification-gap
   The regex tier in lookup_v2 (lines 1579-1595) re-compiles std::regex from the stored pattern string on every cache miss. The regex_routes_ vector stores (pattern_string, route_entry) pairs rather than pre-compiled std::regex objects. This is a performance regression relative to the v1 regex map (registered_resources_regex) which uses pre-compiled http_endpoint objects. The EARS requirements and PRD §4.7 do not specify implementation details of the regex tier, but the design intent (O(1) cache hit, O(pattern-count) tier-3 scan) is undermined when the scan also re-compiles on each miss.
   *Recommendation:* Store std::regex alongside the pattern string in regex_routes_ (e.g. std::vector<std::tuple<std::string, std::regex, route_entry>>) so compilation happens once at registration time, not on every cache miss.
   *Status:* Fixed in this batch — `regex_routes_` now stores `regex_route{url_complete, compiled_re, entry}` structs; regex is compiled once at registration via `classify_route_tier` (see item #4).

54. [ ] **spec-alignment-checker** | `src/webserver.cpp:1765` | acceptance-criteria
   Acceptance criterion 'Path-piece extraction populates http_request correctly for parameterized routes' is verified only at the v2-tier level in isolation (lookup_pipeline_test.cpp lines 71-82 check captured_params on the lookup_result). However finalize_answer (the live HTTP dispatch path) does not call lookup_v2 and does not propagate captured_params into http_request. The implementation note explicitly acknowledges this: 'The dispatch site in finalize_answer continues to use the v1 path so this PR is a purely additive change.' End-to-end population of http_request via the v2 path is deferred to 'Cycle K'.
   *Recommendation:* The deferred cutover (Cycle K) should explicitly carry a test that verifies captured_params from lookup_v2 is forwarded to http_request::get_path_pieces() or mr->dhr->set_arg() once finalize_answer is migrated. The current task's scope is clear, but the AC text is ambiguous — consider adding a qualification like '(at the v2-tier level)' to the AC or noting the partial-coverage status.
   *Status:* Deferred — end-to-end dispatch wiring deferred to Cycle K per design.

55. [ ] **spec-alignment-checker** | `test/integ/route_table_concurrency.cpp:29` | acceptance-criteria
   Acceptance criterion 'Concurrent registration + lookup stress test shows no deadlock or data race under TSan' is partially met. The stress test exists and exercises the lock-order discipline on every make-check run. However TSan itself is explicitly a manual gate — the file header documents 'Rebuild with CXXFLAGS=-fsanitize=thread ... and re-run this test. Adding TSan as a CI matrix variant is a follow-up outside TASK-027 scope.' TSan has not been wired into CI.
   *Recommendation:* Create a follow-up task to add a TSan CI matrix variant (e.g. a separate Makefile target or CI job) that rebuilds route_table_concurrency.cpp with -fsanitize=thread and runs it, converting the manual gate into an automated one.
   *Status:* Deferred — TSan CI automation deferred to TASK-036 per design.

56. [x] **test-quality-reviewer** | `test/integ/route_table_concurrency.cpp:123` | slow-test
   std::this_thread::sleep_for(std::chrono::milliseconds(500)) is a real wall-clock sleep inside a test. On a loaded CI box or under Valgrind/TSan this inflates to seconds and blocks the entire test suite. The comment acknowledges this ('short enough that make check stays under one second') but under sanitizers that is not guaranteed.
   *Recommendation:* Replace the fixed 500 ms sleep with a counted-operation gate: let the stop condition trigger after writer_ops + reader_ops exceeds a threshold (e.g. 10 000 total ops) rather than after elapsed time. This keeps the test deterministic on both fast and slow machines and survives TSan overhead without arbitrary timeouts.
   *Status:* Fixed in this batch — first test (`concurrent_register_and_lookup_no_data_race`) now uses `kOpThreshold = 10000` operation count with 10ms poll interval and 30s safety ceiling instead of fixed 500ms sleep.

57. [x] **test-quality-reviewer** | `test/integ/route_table_concurrency.cpp:139` | naming-convention
   concurrent_parameterized_register_and_lookup_no_data_race has an identical suffix to the existing concurrent_register_and_lookup_no_data_race (line 59). The distinguishing information — that this variant exercises the radix tree's wildcard_child_ allocation path — is in the block comment above the test but not in the name itself. Naming convention in this suite uses a descriptive scenario segment; the new test's scenario is 'parameterized' which appears in the name, but 'wildcard_node_allocation' would more precisely identify what this test stresses compared to the first.
   *Recommendation:* Rename to concurrent_wildcard_node_alloc_and_lookup_no_data_race or concurrent_parameterized_wildcard_register_and_lookup_no_data_race to distinguish the specific structural node path under test.
   *Status:* Fixed in this batch — renamed to `concurrent_wildcard_node_alloc_and_lookup_no_data_race` in `test/integ/route_table_concurrency.cpp`.

58. [ ] **test-quality-reviewer** | `test/integ/route_table_concurrency.cpp:205` | slow-test
   concurrent_parameterized_register_and_lookup_no_data_race sleeps for 500ms (same as the first concurrency test). Together the two tests add ~1 second of wall-clock time to every `make check` run. The comment in the first test notes '500ms is short enough that make check stays under one second', which is no longer true when both tests are included. This is a minor concern since the test file is an integration test, but worth flagging for the CI budget.
   *Recommendation:* Consider reducing one or both durations to 250ms, or document in the Makefile comment that the concurrency suite contributes ~1s of fixed wall time to make check.
   *Status:* Deferred — the second test (`concurrent_wildcard_node_alloc_and_lookup_no_data_race`) still uses the 500ms sleep + op-count-zero guard pattern rather than the `kOpThreshold` approach applied to the first test. Deferred to a follow-up cleanup.

59. [x] **test-quality-reviewer** | `test/unit/http_method_test.cpp:285` | redundant-test
   to_string_returns_uppercase_wire_tokens (runtime, lines 285-295) and to_string_round_trip_via_strcmp_with_mhd (runtime, lines 307-326) are near-duplicates. The first checks the literal strings 'GET', 'POST', etc.; the second checks equality with MHD_HTTP_METHOD_GET etc. Both check the same nine methods and the static_asserts at lines 132-148 already provide compile-time coverage of the first assertion. The MHD round-trip test is the only value-adding variant; the literal-string runtime test is redundant.
   *Recommendation:* Remove to_string_returns_uppercase_wire_tokens (the runtime literal-string test at line 285). The static_assert block at lines 132-148 plus the MHD round-trip test at line 307 give identical protection with less duplication.
   *Status:* Fixed in this batch — `to_string_returns_uppercase_wire_tokens` removed from `test/unit/http_method_test.cpp`; explanatory comment added noting that compile-time `static_assert` + MHD round-trip test together provide equivalent coverage.

60. [x] **test-quality-reviewer** | `test/unit/route_table_test.cpp:98` | missing-test
   Multiple parameterized segments (e.g. /a/{x}/b/{y}/c) are not tested. The current parameterized tests cover a single wildcard (/users/{id}/posts). Two wildcards in one path expose interactions between the wildcard_child_ chain and the captures vector ordering that single-wildcard tests cannot catch.
   *Recommendation:* Add a test inserting /a/{x}/b/{y}/c, then calling find('/a/1/b/2/c') and asserting captures contains both (x, 1) and (y, 2) in order.
   *Status:* Already addressed — `radix_tree_matches_multiple_parameterized_segments` test existed in `test/unit/route_table_test.cpp` prior to this batch.

61. [ ] **test-quality-reviewer** | `test/unit/webserver_on_methods_test.cpp:522` | implementation-coupling
   on_get_and_on_post_compose_on_true_regex_path references internal implementation detail (fresh==true/false, classify_route_tier, regex-tier update path) in its comment block and implicitly couples the test's rationale to the current two-branch structure of on_methods_. The test body itself is behaviorally sound — it validates observable HTTP responses — but if on_methods_ is restructured in a future iteration the comment will mislead. The test name does not describe the observable contract ('compose two handlers on a true-regex path and both must be served') but instead names the internal mechanism being guarded ('fresh==false update path').
   *Recommendation:* Rename to on_get_and_on_post_compose_on_regex_pattern_path and strip the implementation-detail prose from the comment, keeping only the behavioral contract: 'two on_* registrations on a regex-metachar path compose; all registered methods are served; unregistered methods receive 405'.
   *Status:* Deferred — test rename/comment cleanup not done in this batch; deferred.

62. [ ] **test-quality-reviewer** | `test/unit/webserver_on_methods_test.cpp:522` | multiple-concerns
   on_get_and_on_post_compose_on_true_regex_path asserts three distinct behaviors in one test body: (a) GET returns 200 with the get handler's body, (b) POST returns 200 with the post handler's body, (c) DELETE returns 405. Concerns (a)+(b) together test composition; concern (c) tests method exclusion. These are separable concerns. A failure in the 405 path obscures whether composition itself succeeded.
   *Recommendation:* Split into two tests: on_get_and_on_post_compose_on_regex_pattern_path (asserts both registered methods are served) and on_unregistered_method_on_regex_path_returns_405 (asserts the 405 path). This mirrors the pattern already used for on_get_and_on_post_same_path_serve_both vs lambda_route_dispatches_only_for_registered_method.
   *Status:* Deferred — test split not done in this batch; deferred.
