# Unworked Review Issues

**Run:** 2026-05-20 11:50:13
**Task:** TASK-044
**Total:** 28 (0 critical, 5 major, 23 minor)

## Major

1. [x] **code-quality-reviewer** | `scripts/check-parallel-install.sh:195` | error-handling
   The v1-pattern grep uses an unquoted variable in `grep -E "$V1_PATTERN"`. On Darwin, V1_PATTERN is `^libhttpserver\.[01]\.dylib` — this works as written, but the unquoted `$v1_hits` expansion on line 201 will word-split on spaces and glob-expand filenames. If a library filename ever contains spaces, or if `ls` output contains unexpected entries, the `for f in $v1_hits` loop would misbehave. The pattern also only matches SONAME .0 and .1, not .so.0 or any other v1 variant.
   *Recommendation:* Quote `$v1_hits` where it is iterated: use a `while IFS= read -r f; do ... done <<< "$v1_hits"` construct. Also consider whether the V1_PATTERN range `[01]` is intentionally exclusive of higher v1 digits (e.g. libhttpserver.so.1.x).
   *Status:* fixed in this batch — changed `for f in $v1_hits` to `while IFS= read -r f; do ... done <<< "$v1_hits"` in Phase 3. Phase 3 also now prefers strict exact-filename matching from .la (see item 5).

2. [x] **code-quality-reviewer** | `scripts/check-parallel-install.sh:94` | code-elegance
   The RESOLVED_PREFIX extraction block (config.status parsing via tr/grep/sed pipeline) is copy-pasted verbatim from check-soversion.sh lines 74-75. This is a DRY violation and means any future fix must be applied in two places.
   *Recommendation:* Extract the prefix-resolution logic into a shared helper script (e.g. scripts/lib/resolve-prefix.sh) sourced by both gate scripts, or accept the duplication with a comment noting it is intentional for standalone-invocability reasons.
   *Status:* fixed in this batch — extracted to `scripts/lib/resolve-prefix.sh`; both scripts now `source "$(dirname "$0")/lib/resolve-prefix.sh"`.

3. [x] **code-simplifier** | `scripts/check-parallel-install.sh:90` | code-structure
   The config.status prefix-resolution block (lines 90-96) is copy-pasted verbatim from check-soversion.sh (lines 67-81), including the identical comment 'same trick as check-soversion.sh'. The duplication spans 7 lines of non-trivial parsing logic involving tr, grep, head, and sed. If the resolution method needs to change (e.g. the --prefix= parsing regex), it must be updated in two places.
   *Recommendation:* Extract the block into a shared sourced helper, e.g. scripts/lib/resolve-prefix.sh, and source it from both scripts: `source "$(dirname "$0")/lib/resolve-prefix.sh"`. The helper would set RESOLVED_PREFIX and LIBDIR, accepting BUILD_DIR and CONFIG_STATUS as inputs. Alternatively, since both scripts already require a built tree, the parsed value could be stored in a stamp file during configure.
   *Status:* fixed in this batch — same fix as item 2; shared helper created.

4. [x] **code-simplifier** | `scripts/check-soversion.sh:147` | code-structure
   The chase_symlink function (lines 147-160) reimplements readlink -f portability handling inline inside the check, mid-script, after the A4 assertion comment. It is defined just before its single call site, which is reasonable, but the function name does not appear anywhere in the header documentation or assertion list, making it invisible to a reader scanning the A1-A8 contract. More importantly, on macOS, `readlink -f` IS available via coreutils (brew install coreutils provides greadlink), and the script already guards with `command -v` for other tools; the portability concern could be handled with a simpler two-branch approach.
   *Recommendation:* Move chase_symlink to the top of the script alongside fail/pass/skip so helpers are grouped together rather than scattered between assertion blocks. Optionally rename to resolve_symlink_chain to better express intent. The current placement between 'A4: dev symlink' setup and the actual check makes the flow harder to follow.
   *Status:* fixed in this batch — moved function to top alongside fail/pass, renamed to `resolve_symlink_chain`, removed inline definition.

5. [x] **test-quality-reviewer** | `scripts/check-parallel-install.sh:106` | missing-test
   The V1_PATTERN on Linux is `^libhttpserver\.so\.[01]` and on Darwin `^libhttpserver\.[01]\.dylib`. This matches either SONAME major 0 OR 1, but never validates which one actually comes from the master branch. If master ships SONAME 0 (likely for the legacy 0.x line) the assertion passes trivially; if master unexpectedly shipped SONAME 2 already, the pattern would still match 2 but only via the fall-through grep. The coexistence assertion should capture the actual V1 SONAME from the v1 build's .la or readelf/otool output and assert the exact filename, not a broad character-class pattern. As written, the test will not catch a mis-configured v1 that installs the SAME SONAME as v2.
   *Recommendation:* After the v1 install succeeds, extract the actual v1 SONAME basename from `$V1_BUILD/.libs/libhttpserver.la` (the `library_names` field) and assert that exact filename exists in $STAGE_LIB rather than relying on the `[01]` glob. Alternatively, use `readelf -d` / `otool -L` on the installed v1 binary to read its embedded SONAME and compare against the expected value.
   *Status:* fixed in this batch — Phase 3 now reads `library_names=` from `$V1_BUILD/.libs/libhttpserver.la` and does a strict exact-filename check; also asserts v1 and v2 names differ. Falls back to V1_SONAME_PATTERN grep only when the .la is absent.

## Minor

6. [x] **architecture-alignment-checker** | `scripts/check-parallel-install.sh:9` | pattern-violation
   The script header comment says Darwin produces 'libhttpserver.2.0.0.dylib' as the versioned artefact, but later in the script body (line 111) and in check-soversion.sh (line 113) the correct Darwin output is documented as 'libhttpserver.2.dylib' only (libtool -version-number A:B:C on Mach-O emits only the major-numbered .N.dylib). The header comment is internally inconsistent with the rest of the script and the architecture doc (§8, check-soversion.sh line 11-12).
   *Recommendation:* Update the script's opening block comment on line 9 to read 'libhttpserver.2.dylib' (not 'libhttpserver.2.0.0.dylib') for consistency with the actual Darwin libtool behaviour documented elsewhere in the same file and in check-soversion.sh.
   *Status:* fixed in this batch — corrected header comment from `libhttpserver.2.0.0.dylib` to `libhttpserver.2.dylib`.

7. [x] **code-quality-reviewer** | `Makefile.am:338` | code-elegance
   The `check-soversion` and `check-parallel-install` targets are not wired into `check-local` (intentionally documented). However, they are not listed in the top-level `.PHONY` declaration at line 351 — they ARE listed there, so this is fine. The EXTRA_DIST at line 44 correctly includes both scripts. No issue here — this finding is retracted.
   *Recommendation:* No action needed.
   *Status:* already addressed — retracted by reviewer; no change required.

8. [x] **code-quality-reviewer** | `scripts/check-parallel-install.sh:169` | code-readability
   The Homebrew-specific CPPFLAGS/LDFLAGS baked into the v1 configure invocation (`-I/opt/homebrew/include`, `-L/opt/homebrew/lib`) are hardcoded macOS-local paths. On Linux or on non-Homebrew macOS installs this silently adds non-existent paths. The paths are harmless but reflect environment assumptions that should be documented.
   *Recommendation:* Gate the Homebrew flags on `[ "$PLATFORM" = "Darwin" ]`, or accept them unconditionally but add a comment explaining they are best-effort Darwin hints that are harmless when absent.
   *Status:* fixed in this batch — gated behind `if [ "$PLATFORM" = "Darwin" ]`; dynamically detects Homebrew prefix via `brew --prefix` (items 8, 15, 26 together).

9. [x] **code-quality-reviewer** | `scripts/check-soversion.sh:44` | code-readability
   `set -uo pipefail` omits `-e`. Both scripts rely on explicit conditional error handling (`|| fail ...`) for every significant command, so this is intentional. However, without `-e`, a silent intermediate command failure (e.g. inside a subshell that is not guarded) could be swallowed. The omission is not documented.
   *Recommendation:* Add a brief comment at the `set -uo pipefail` line explaining why `-e` is omitted (explicit per-command error handling pattern), to avoid future maintainers adding `-e` and breaking the intentional design.
   *Status:* fixed in this batch — added explanatory comment above `set -uo pipefail` in both scripts.

10. [x] **code-quality-reviewer** | `scripts/check-soversion.sh:75` | code-readability
   The sed expression `s/^'\?--prefix=//;s/'\$//'` uses a `\?` quantifier which is a GNU sed extension. On macOS/BSD sed this fails silently or matches incorrectly. The script is intended to run on Darwin.
   *Recommendation:* Use a POSIX-portable alternative such as `sed 's/^["'"'"]*--prefix=//; s/["'"'"]*$//'` or switch to parameter expansion after capturing the grep output. Alternatively, use `python3 -c` or `awk` for the extraction to remain portable.
   *Status:* fixed in this batch — the shared `scripts/lib/resolve-prefix.sh` uses POSIX-portable `'*` instead of GNU `'\?`.

11. [x] **code-quality-reviewer** | `scripts/check-soversion.sh:93` | test-coverage
   The `make install` subshell on line 92-93 redirects stdout to a log file but not via a named-pipe approach that would allow streaming progress. For large builds this means the CI console goes silent. This is a UX issue, not a correctness issue.
   *Recommendation:* Consider using `tee` to stream output while still capturing it: `( cd "$BUILD_DIR" && make install DESTDIR="$STAGE" ) 2>&1 | tee "$STAGE/.install.log" || fail ...`. Note this changes the exit-code semantics; use `${PIPESTATUS[0]}` on bash.
   *Status:* fixed in this batch — A1 install now uses `tee` with `${PIPESTATUS[0]}` exit-code check.

12. [x] **code-quality-reviewer** | `specs/tasks/_index.md:1` | readability
   The 'Last updated' header field at line 4 still reads '2026-05-18' but this change was made after that date; keeping it stale makes it slightly misleading.
   *Recommendation:* Update 'Last updated' to reflect the date of this edit (2026-05-20) so the metadata stays accurate.
   *Status:* fixed in this batch — updated `Last updated` to 2026-05-20.

13. [x] **code-simplifier** | `scripts/check-parallel-install.sh:101` | naming
   The variable V1_PATTERN (lines 106, 113) uses a regex string that is matched with grep -E, but neither the variable name nor any nearby comment makes clear whether this pattern is anchored, what it matches on Darwin vs Linux, or why [01] covers all relevant historic v1 sonumbers. A reader must mentally simulate the regex to understand the intent.
   *Recommendation:* Add a one-line inline comment on each branch explaining the reasoning: e.g. `# matches libhttpserver.so.0 (legacy 0.x) or .so.1 (hypothetical 1.x bump)`. The variable name V1_SONAME_PATTERN would also be slightly more self-documenting than V1_PATTERN.
   *Status:* fixed in this batch — renamed to `V1_SONAME_PATTERN`; added explanatory comments on each platform branch.

14. [x] **code-simplifier** | `scripts/check-parallel-install.sh:147` | code-structure
   Lines 147-150 unconditionally attempt `git worktree remove --force` followed by `rm -rf` for the stale-worktree cleanup before adding, and the cleanup trap at line 86 does the same. The two blocks are nearly identical but differ only in context (pre-add cleanup vs EXIT trap). The duplication is small but the intent of each could be made explicit with a single remove_master_worktree helper function used by both.
   *Recommendation:* Extract a remove_master_worktree() shell function called from both the pre-add guard (line 147) and the trap (line 82). This removes three lines of duplication and makes the trap registration read as `trap remove_master_worktree EXIT`.
   *Status:* fixed in this batch — extracted `remove_master_worktree()` helper; both the stale-cleanup guard and the EXIT trap now call it.

15. [x] **code-simplifier** | `scripts/check-parallel-install.sh:168` | code-structure
   The v1 configure step (lines 168-175) hardcodes Homebrew paths (-I/opt/homebrew/include, -L/opt/homebrew/lib) unconditionally. On Linux CI hosts these paths do not exist and are silently ignored, but they make the script's assumptions about the build environment visible in a way that could mislead contributors running on Linux who see these flags and wonder if Homebrew is required.
   *Recommendation:* Guard the Homebrew flags behind `if [ "$PLATFORM" = "Darwin" ]`, or document in a comment that these are Darwin-only hints that are harmless no-ops on Linux. The current approach works but violates the 'be consistent' understandability principle — platform-specific logic is already gated by case statements elsewhere in both scripts.
   *Status:* fixed in this batch — same fix as items 8 and 26; Darwin-only block with dynamic `brew --prefix` detection.

16. [x] **code-simplifier** | `scripts/check-soversion.sh:74` | code-structure
   The prefix-resolution one-liner (lines 74-75) is a multi-stage pipeline (config.status | tr | grep | head | sed) that is difficult to read at a glance. The surrounding comment 'Resolve prefix/libdir/includedir by asking config.status' is helpful, but the pipeline itself mixes field splitting, filtering, and stripping in one expression.
   *Recommendation:* No structural change required given the single call site, but splitting into two variables would improve readability: capture the raw --prefix= token first, then strip the quotes. This matches the 'explanatory variables' clean code principle and makes each transformation step visible.
   *Status:* fixed in this batch — the shared helper `scripts/lib/resolve-prefix.sh` splits the pipeline across multiple lines with per-step comments.

17. [x] **housekeeper** | `specs/tasks/_index.md:129` | task-not-marked-complete
   The _index.md fix (TASK-044 status 'Not Started' -> 'Done') is present as an unstaged working-tree change rather than a committed change. The task branch still has this edit uncommitted.
   *Recommendation:* Stage and commit the _index.md change before merging into feature/v2.0 so the task index matches the committed state of the implementation.
   *Status:* already addressed by TASK-044 merge commit `f73aca5`; TASK-044 is committed as Done in _index.md on feature/v2.0.

18. [x] **housekeeper** | `specs/tasks/_index.md:6` | documentation-stale
   The 'Last updated' field in specs/tasks/_index.md reads '2026-05-18' but the commit date for this work is 2026-05-20. Preceding tasks (TASK-042, TASK-043) also updated the index without bumping this date.
   *Recommendation:* Update the 'Last updated' date in specs/tasks/_index.md to 2026-05-20 when fixing the TASK-044 status entry.
   *Status:* fixed in this batch — same fix as item 12; updated to 2026-05-20.

19. [x] **performance-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-044/scripts/check-parallel-install.sh:178` | missing-parallelization
   v1 build uses make -j4 (hard-coded parallelism) rather than inheriting the job count from the parent make invocation or the environment
   *Recommendation:* Replace `make -j4` with `make ${MAKEFLAGS:--j$(nproc 2>/dev/null || sysctl -n hw.logicalcpu 2>/dev/null || echo 4)}` or simply `make -j$(nproc 2>/dev/null || echo 4)` to use all available cores. On large machines -j4 is significantly slower than full parallelism; on 2-core CI machines it is harmless but misleading.
   *Status:* fixed in this batch — replaced `make -j4` with dynamic `make -j"${_NJOBS}"` where `_NJOBS` is detected via `nproc` / `sysctl -n hw.logicalcpu` / fallback 4.

20. [x] **security-reviewer** | `scripts/check-parallel-install.sh:169` | injection
   The v1 configure invocation expands '${CPPFLAGS:-}', '${LDFLAGS:-}', and '${LIBS:--lgnutls}' without quoting them before passing into the subshell. If a maintainer's environment contains whitespace or special characters in CPPFLAGS/LDFLAGS, the values will be word-split by the shell before they reach configure, potentially altering the flag list in unexpected ways. CWE-88 (argument injection via uncontrolled environment variable).
   *Recommendation:* Double-quote all environment-variable expansions passed to configure: 'CPPFLAGS="${CPPFLAGS:-} -I/opt/homebrew/include ..." LDFLAGS="${LDFLAGS:-} ..." LIBS="${LIBS:--lgnutls}" ../configure --prefix="$RESOLVED_PREFIX"'.
   *Status:* already addressed — the `CPPFLAGS=`, `LDFLAGS=`, and `LIBS=` environment variable assignments are already inside double-quotes in the original code; word-splitting does not occur for shell variable assignments. The homebrew paths are now gated to Darwin-only and composed from `_HOMEBREW_CPPFLAGS` / `_HOMEBREW_LDFLAGS` variables.

21. [x] **security-reviewer** | `scripts/check-parallel-install.sh:201` | injection
   The loop variable 'f' in 'for f in $v1_hits' iterates over an unquoted command-substitution result. If any filename contains whitespace or glob characters, the shell will word-split and glob-expand $v1_hits before iteration. In this specific script the paths come from 'ls | grep', so a crafted filename in STAGE_LIB (an attacker-controlled DESTDIR path) could introduce unexpected expansion. CWE-78 (word-splitting leading to unintended shell interpretation).
   *Recommendation:* Store matches in an array or use 'while IFS= read -r f; do ... done <<< "$v1_hits"' to iterate safely over newline-separated entries. For example: 'while IFS= read -r f; do echo "    $f"; done <<< "$v1_hits"'.
   *Status:* fixed in this batch — same fix as item 1; now uses `while IFS= read -r f`.

22. [x] **security-reviewer** | `scripts/check-parallel-install.sh:93` | insecure-design
   The RESOLVED_PREFIX is extracted from config.status output by piping through 'tr', 'grep', 'head', and 'sed'. The result is never sanitised before being used in path construction (e.g., LIBDIR="$RESOLVED_PREFIX/lib", STAGE_LIB="$SHARED_STAGE$LIBDIR"). If a maintainer has configured a prefix containing shell-significant characters or path-traversal sequences (e.g., '../secret'), these flow into 'rm -rf', 'mkdir -p', and 'make install DESTDIR='. This is the same pattern in check-soversion.sh (line 74). Since config.status is trusted build infrastructure the practical risk is low, but the code is defence-in-depth deficient. CWE-22 (path traversal).
   *Recommendation:* After extracting RESOLVED_PREFIX, validate that it is an absolute path starting with '/': 'case "$RESOLVED_PREFIX" in /*) ;; *) fail "resolved prefix is not absolute: $RESOLVED_PREFIX";; esac'. This is a one-liner guard against accidental or adversarial misconfiguration.
   *Status:* fixed in this batch — `scripts/lib/resolve-prefix.sh` includes an absolute-path guard after resolving the prefix.

23. [ ] **spec-alignment-checker** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-044/Makefile.am:288` | acceptance-criteria
   Acceptance criterion 1 ('`./configure && make && make install DESTDIR=$tmp` produces libhttpserver.so.2.0.0 and the expected symlinks') is verified by `make check-soversion` / `scripts/check-soversion.sh`, but check-soversion is intentionally NOT wired into `make check`. The task explicitly documents this as intentional ('NOT wired into make check — it does its own make install and is intentionally invoked standalone'). The script itself is comprehensive (assertions A1-A8), but the criterion is only verified when the developer remembers to run the standalone target; a normal `make check` invocation will not catch a regression.
   *Recommendation:* Consider adding a lightweight variant (e.g. a filename-only check without a full staged install) to check-local so a basic SOVERSION regression is caught in CI, while keeping the full check-soversion as the pre-tagging gate.
   *Status:* deferred — requires a design decision: the current "not in make check" policy is intentional per task docs. A lightweight filename check in check-local would require a staged install (make install) as a prerequisite, which conflicts with the keep-CI-fast goal. Adding to a CI matrix step is the correct fix, not a code change here.

24. [x] **spec-alignment-checker** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-044/Makefile.am:347` | acceptance-criteria
   Acceptance criterion 3 ('A test installs both libhttpserver1 and libhttpserver2 into the same prefix and confirms both .so.1 and .so.2 coexist — or document the test as manual if CI can't reasonably do this') is met by check-parallel-install.sh, which correctly exits 0 with a SKIP when the v1 build cannot be bootstrapped. The task-definition text acknowledges this acceptable fallback. However, the script uses V1_PATTERN='^libhttpserver\.so\.[01]' (Linux) and '^libhttpserver\.[01]\.dylib' (Darwin), which implicitly assumes the v1 major SOVERSION is 0 or 1. If the master branch SOVERSION turns out to be something other than 0 or 1 (e.g. the pre-bump value was 0.20 which maps to major=0 under -version-number), the pattern still matches 0 — this is fine for the current state but is undocumented.
   *Recommendation:* Add a brief comment in check-parallel-install.sh explaining why [01] covers the known v1 SONAME majors (master used MAJOR_VERSION=0, so the .so.0 / .0.dylib SONAME applies).
   *Status:* fixed in this batch — renamed to V1_SONAME_PATTERN and added per-platform comments explaining the [01] range; also added strict exact-filename check from .la file (item 5).

25. [x] **spec-alignment-checker** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-044/configure.ac:326` | specification-gap
   The `-version-number libhttpserver_LDF_VERSION` is appended to the global `LDFLAGS` rather than to a per-target `libhttpserver_la_LDFLAGS`. This means the version-number flag is technically injected into LDFLAGS for all link steps in the build (executables, test runners, etc.), not only for the shared library. Libtool ignores the flag for non-library link steps, so in practice this causes no problem, but it is a non-idiomatic pattern that could confuse future maintainers. The TASK-044 spec says 'Update Makefile.am install rules if the .so symlink chain needs adjusting' and notes no install-rule edits were required — but the version flag placement is in configure.ac's global LDFLAGS rather than src/Makefile.am's libhttpserver_la_LDFLAGS.
   *Recommendation:* Move `-version-number ${libhttpserver_LDF_VERSION}` from configure.ac's global LDFLAGS to `libhttpserver_la_LDFLAGS` in src/Makefile.am so it is scoped to the shared-library link step only. This is a cleanup, not a correctness issue for the current build.
   *Status:* fixed in this batch — removed from global LDFLAGS in configure.ac; added `LHT_LDF_VERSION` AC_SUBST; `src/Makefile.am` `libhttpserver_la_LDFLAGS` now includes `-version-number @LHT_LDF_VERSION@`.

26. [x] **test-quality-reviewer** | `scripts/check-parallel-install.sh:168` | non-deterministic
   The v1 configure step hardcodes Homebrew paths (`-I/opt/homebrew/include`, `-L/opt/homebrew/lib`, `-L/opt/homebrew/opt/gnutls/lib`). On non-macOS Linux CI runners (or macOS machines without Homebrew at /opt/homebrew), these paths are silently added to CPPFLAGS/LDFLAGS but don't exist, which is harmless but can mislead diagnostics. More critically, on macOS machines using a non-standard Homebrew prefix (e.g. Intel Macs with /usr/local), the actual include/lib paths are missing and the configure step fails, triggering a SKIP rather than an honest failure. The script is thus non-deterministic across environments — the same v1 source may build on arm64 macOS and be skipped on x86_64 macOS.
   *Recommendation:* Use `$(brew --prefix)/include` and `$(brew --prefix)/lib` dynamically (if brew is on PATH), or detect the correct prefix with `pkg-config --cflags gnutls` / `pkg-config --libs gnutls`. If brew is not available, fall through to the existing SKIP path. This makes the diagnostic accurate rather than silently appending wrong paths.
   *Status:* fixed in this batch — same fix as items 8 and 15; uses `brew --prefix` dynamically; falls back to /opt/homebrew when brew unavailable; Linux gets no Homebrew flags.

27. [x] **test-quality-reviewer** | `scripts/check-soversion.sh:170` | missing-test
   On Linux, assertion A5 reads the SONAME via `readelf -d` but uses a loose case-match: `*"[$SONAME_BASENAME]"*|*"$SONAME_BASENAME"*`. This tolerates a SONAME of the form `libhttpserver.so.2` appearing anywhere in the readelf line, including as a substring of a longer SONAME like `libhttpserver.so.20`. A strict equality check on the extracted SONAME field would be more protective.
   *Recommendation:* Extract just the SONAME value with `sed` or `awk` from the `readelf -d` output (the bracketed field) and compare it with `[ "$extracted" = "$SONAME_BASENAME" ]` rather than a substring match.
   *Status:* fixed in this batch — A5 now extracts the bracketed SONAME via `sed 's/.*\[\(.*\)\].*/\1/'` and uses strict `[ "$extracted_soname" = "$SONAME_BASENAME" ]` equality.

28. [x] **test-quality-reviewer** | `scripts/check-soversion.sh:243` | missing-test
   Assertion A7 checks that libhttpserver.la contains the SONAME basename string, but does not verify the `library_names` field specifically — a match anywhere in the file (e.g. in a comment or the `dlname` field) would satisfy the grep. If the SONAME appears in the wrong field, downstream libtool linking could still fail.
   *Recommendation:* Narrow the grep to the `library_names=` line: `grep -E "^library_names=.*$SONAME_BASENAME" "$LA_FILE"`. This ensures the SONAME is listed where libtool expects it.
   *Status:* fixed in this batch — A7 grep narrowed to `^library_names=.*$SONAME_BASENAME` with anchored match.
