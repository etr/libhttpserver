# Unworked Review Issues

**Run:** 2026-05-29 18:34:34
**Task:** TASK-055
**Total:** 17 (0 critical, 0 major, 17 minor)

> **Minor sweep status (2026-06-02):** all 17 minor findings triaged.
> 11 addressed, 6 acknowledged-no-action or pre-existing.

## Minor — Closed / Moot

| # | File | Category | Disposition |
|---|------|----------|-------------|
| 1 | create-webserver.md | interface-contract | **Fixed:** added `expose_exception_messages (TASK-055 / DR-009 Revision 1)` paragraph to §4.9 documenting the security-opt-in shape, alongside the existing `expose_credentials_in_logs` note. |
| 2 | webserver.md | interface-contract | **Fixed:** added a Key-design-notes bullet to §4.1 referencing the fixed `"Internal Server Error"` default body, the `log_error`-still-surfaces-`e.what()` rule, and the `expose_exception_messages` opt-in, cross-referencing §5.2. |
| 3 | constants.hpp:62 | code-elegance | **Fixed:** replaced the `GENERIC_ERROR` comment with explicit v1-API-parity language pointing at `INTERNAL_SERVER_ERROR` as the live constant. |
| 4 | webserver_impl_dispatch.hpp:63 | code-readability | **Fixed:** rewrote the third bullet in the `internal_error_page` declaration comment to describe the dual-branch behaviour ("fixed string Internal Server Error" / `@p msg` only when `expose_exception_messages` is true). |
| 5 | basic.cpp:2073 (and :1512, :1541) | code-readability | **Fixed:** replaced the renamed test-name references in three comment blocks (post-2127, line 1509, line 1539) with the current `dr009_default_body_is_fixed_string` / `dr009_default_body_is_fixed_string_for_non_std_exception` names and the verbose-opt-in counterparts. |
| 6 | webserver_error_pages.cpp:95 | code-structure | **Fixed:** extracted `const auto status = http_utils::http_internal_server_error;` ahead of the expose-flag branch so both return paths share the same status binding — the dimension that must never diverge. |
| 7 | constants.hpp:62 | naming | **Fixed:** same edit as #3 — comment on `GENERIC_ERROR` now distinguishes it from `INTERNAL_SERVER_ERROR`. |
| 9 | webserver_error_pages.cpp:100 | memory-allocation | **Acknowledged-deferred:** reviewer notes this is consistent with the pre-existing `NOT_FOUND_ERROR` / `METHOD_ERROR` convention and the 5xx path is cold relative to 2xx traffic; the `string_view`-overload migration is a `http_response::string` API change that belongs in a separate cross-cutting task. |
| 10 | webserver_error_pages.cpp:123 | memory-allocation | **Acknowledged-pre-existing:** reviewer confirms "not introduced by TASK-055"; the `log_error_ptr` signature widening is a separate API task. |
| 11 | README.md:668 (and :1719) | insecure-design | **Fixed:** replaced both code examples — the lambda body now returns `"Internal Server Error"` and the comment explicitly says `// CWE-209: 'what' may contain file paths, SQL fragments, or attacker-influenced input. Log it internally; do NOT echo it to the HTTP client.` with an `(void)what;` placeholder for a logger call. |
| 12 | webserver_error_pages.cpp:104 | logging-failures | **Acknowledged-documented:** reviewer marks this a pre-existing documented limitation; no action required for the revision. A future `log_sanitizer` callback slot would be the right shape. |
| 13 | create_webserver.hpp:282 | insecure-design | **Acknowledged-deferred:** reviewer's mitigation (drop the default argument or annotate `[[deprecated]]` on the no-arg overload) is a public-API change with cross-cutting implications for `expose_credentials_in_logs` and any future security-opt-in flag. Both flags already document the security implication at their declaration; tracked as a candidate API hardening pass. |
| 14 | webserver_impl_dispatch.hpp:63 | specification-gap | **Fixed:** same edit as #4. |
| 15 | basic.cpp:3413 | redundant-test | **Fixed:** removed the redundant `s.find("unknown exception") == npos` check; the preceding `LT_CHECK_EQ(s, "Internal Server Error")` already provides full coverage. |
| 16 | basic.cpp:3531 | naming-convention | **Moot:** the explanatory block comment immediately above `dr009_feature_unavailable_lands_as_generic_500` already documents that the verbose body opt-in is part of the test's contract; the rename suggestion is a cosmetic preference. |
| 17 | create_webserver_test.cpp:122 | implementation-coupling | **Acknowledged-no-action:** reviewer confirms the integration suite (`dr009_default_body_is_fixed_string` and the verbose counterparts) already exercises the behavioral propagation; the unit test correctly stays as a compile/no-crash check on the builder itself. |

## Minor — Deferred

8. **code-simplifier** | `test/integ/basic.cpp:3227` | code-structure
   Restore small named port-offset constants (e.g. `PORT_DR009_FIXED_BODY = PORT + 80`, or keep `PORT_DR009_BASE` and address each test as `PORT_DR009_BASE + N`). Current shape spreads `PORT + 80..89` literals across eight test bodies; the comment table at line 3287 is the source of truth but is not mechanically enforced. Deferred because the table-of-truth comment is well-maintained and the alternative (10 `constexpr int` lines) is at the boundary of net-positive value.
