# Unworked Review Issues

**Run:** 2026-05-25 10:28:01
**Task:** TASK-046
**Total:** 24 (0 critical, 0 major, 24 minor)

## Minor

1. [ ] **architecture-alignment-checker** | `specs/architecture/04-components/hooks.md:9` | pattern-violation
   The §4.10 firing-site map lists `connection_opened` and `connection_closed` at `webserver.cpp:connection_notify` and `accept_decision` at `webserver.cpp:policy_callback`, but those symbols live in `src/detail/webserver_callbacks_lifecycle.cpp` (a split that predates TASK-046 for `webserver_callbacks.cpp` and is explicitly required by this task). The stale reference is a documentation issue, not a code defect — TASK-046 notes the split explicitly in the file header and in the task definition — but the architecture spec table is not updated to reflect the actual physical location.
   *Recommendation:* Update the §4.10 firing-site table to use `detail/webserver_callbacks_lifecycle.cpp:connection_notify` and `detail/webserver_callbacks_lifecycle.cpp:policy_callback` to keep the spec current. Low priority since the logical symbol names are correct and the split is documented at the call site.

2. [ ] **code-quality-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-046/src/detail/webserver_callbacks_lifecycle.cpp:149` | code-readability
   The any_hooks_ gate in connection_notify is split across four lines inside the if-condition, creating visual noise. The same multi-line pattern is repeated twice in the function (lines 149-152 and 162-165). While each occurrence is correct, readers have to mentally parse the static_cast<std::size_t>() indexing pattern every time to confirm which phase is being gated.
   *Recommendation:* Consider a small inline helper (lambda or static free function) such as `auto gate = [impl](hook_phase p) { return impl->any_hooks_[static_cast<std::size_t>(p)].load(std::memory_order_relaxed); };` defined at the top of connection_notify, then write `if (impl != nullptr && gate(hook_phase::connection_opened))`. This reads directly as the intent without requiring manual re-parsing of the indexing arithmetic.

3. [ ] **code-quality-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-046/src/hook_handle.cpp:220` | code-organization
   The fire_* methods are defined in hook_handle.cpp, which already has a conceptually distinct role: out-of-line bodies for hook_handle (move, dtor, remove, detach) and peer_address::to_string. Adding the firing dispatch helpers here creates a file with two unrelated responsibilities. The file comment at line 21 still says 'hook_handle and peer_address' and does not mention the fire_* methods, compounding the surprise for a new reader.
   *Recommendation:* Either move the fire_* bodies to webserver_callbacks_lifecycle.cpp (they are lifecycle machinery and belong alongside connection_notify/policy_callback conceptually) or add them to a dedicated webserver_hooks.cpp. Update the file-level comment if they stay in hook_handle.cpp.

4. [ ] **code-quality-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-046/src/hook_handle.cpp:235` | code-elegance
   The three fire_* helpers (fire_connection_opened, fire_accept_decision, fire_connection_closed) are structurally identical: reserve snapshot, acquire shared_lock, copy vector, release lock, iterate with try/catch, catch outer failure. The only variation is the vector member name and the error string literal. This is the first of what will be eleven such helpers (one per hook_phase), making this a preview of significant needless repetition as TASK-047..051 land.
   *Recommendation:* Extract a private template helper such as `fire_phase_snapshot(auto& vec, const auto& ctx, std::string_view phase_name) noexcept` that performs the snapshot-lock-iterate-catch pattern. Each fire_* call site becomes a one-liner delegating to it. This is the right time to establish the template before nine more identical copies accumulate.

5. [ ] **code-quality-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-046/test/integ/hooks_connection_lifecycle.cpp:185` | test-coverage
   The assertion `snapshot.back().phase_name == 'connection_closed'` relies on a single curl round-trip producing exactly one accept_decision event, one connection_opened event, and one connection_closed event — and that connection_closed is the last in the shared log vector. When the test machine is under load or curl reuses keep-alive with multiple requests, the log may contain interleaved events where connection_closed is not at back(). The comment at line 29 acknowledges MHD callback ordering is platform-dependent for the first event, but the last-event assertion is equally subject to race between the ws.stop() call and the MHD internal teardown thread writing to the shared log.
   *Recommendation:* Soften the bookend assertion to: verify that at least one connection_closed event exists in the log, and that no event with phase_name 'connection_opened' appears after any 'connection_closed' for the same implicit connection. The simpler fix is to just assert `closed_count >= 1` (already asserted) and drop the `.back()` ordering pin, which is too fragile for a multithreaded integration test.

6. [ ] **code-quality-reviewer** | `src/detail/webserver_callbacks_lifecycle.cpp:222` | code-elegance
   The `accept_decision` gate in `policy_callback` is written as an inline atomic index/cast expression (lines 222-224) rather than reusing the `hooks_armed` lambda defined in `connection_notify`. This is a minor inconsistency: every other call site uses `hooks_armed(phase)`, but this one site spells out the full expression again. The `hooks_armed` lambda is local to `connection_notify`, so it is not accessible here — but the same pattern could be centralised into a free helper or a one-liner member call if desired.
   *Recommendation:* Consider adding a small `is_phase_armed(hook_phase)` method on `webserver_impl` (or a free function in the anonymous namespace) so both `connection_notify` and `policy_callback` can share the same idiom. This removes the only hand-rolled atomic index/cast expression remaining after the iter-1 `hooks_armed` lambda was introduced in `connection_notify`.

7. [ ] **code-quality-reviewer** | `src/hook_handle.cpp:262` | code-elegance
   The error message construction inside `fire_hooks_for_phase` concatenates three `std::string` temporaries (`std::string("hook[") + std::string(phase_name) + ...`). Converting a `std::string_view` to `std::string` just to concatenate is unnecessary; `std::string("hook[") + std::string(phase_name)` could be written as `"hook[" + std::string(phase_name)` which saves one temporary. Minor allocation concern in error paths that are already exceptional.
   *Recommendation:* Replace `std::string("hook[") + std::string(phase_name)` with `"hook[" + std::string(phase_name)` (implicit conversion from string literal to std::string). Applies to both the inner catch lines (262-263, 265-267) and the outer catch (274-276).

8. [ ] **code-quality-reviewer** | `test/integ/hooks_connection_lifecycle.cpp:185` | test-coverage
   The lifecycle ordering assertion `LT_CHECK_EQ(snapshot.back().phase_name, std::string("connection_closed"))` only checks that `connection_closed` is last, but does not assert that `connection_opened` fired before `connection_closed`. If MHD fires NOTIFY_CLOSED without a preceding NOTIFY_STARTED (edge case) the test would still pass. The comment correctly notes MHD callback ordering is platform-dependent for accept vs open, but the open-before-close invariant is always required.
   *Recommendation:* Add a check that `connection_opened` appears before `connection_closed` in the snapshot, e.g. by recording the first index of each phase and comparing them. This strengthens the lifecycle-ordering pin without over-specifying the accept/open ordering.

9. [ ] **code-simplifier** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-046/src/detail/webserver_callbacks_lifecycle.cpp:222` | code-structure
   policy_callback does not use the hooks_armed lambda that connection_notify defines; it inlines the same atomic-gate pattern (impl->any_hooks_[static_cast<std::size_t>(...)].load(std::memory_order_relaxed)) at line 222-224. This is a small but direct inconsistency with the fix applied in connection_notify.
   *Recommendation:* Define hooks_armed at the top of webserver_impl (e.g. as a free helper or a member) so both connection_notify and policy_callback can call hooks_armed(hook_phase::accept_decision) instead of repeating the cast/load inline.

10. [ ] **code-simplifier** | `src/detail/webserver_callbacks_lifecycle.cpp:127` | naming
   The variable `impl` is obtained from `ws->impl_.get()` but the outer `webserver_impl::connection_notify` is itself a method on webserver_impl. Using a local `impl` pointer for `ws->impl_` while the method's own `this` is a different webserver_impl* (the static trampoline pattern) is correct but potentially confusing: a reader may conflate `impl` with `this`. A more explicit name such as `ws_impl` or `owner_impl` would make the indirection immediately clear.
   *Recommendation:* Rename the local variable from `impl` to `owner_impl` (or `ws_impl`) to distinguish the owning webserver's impl from the static trampoline's implicit this context.

11. [ ] **code-simplifier** | `src/detail/webserver_callbacks_lifecycle.cpp:132` | code-structure
   resolve_peer is a lambda capturing `connection` that is defined unconditionally at the top of connection_notify. In the NOTIFY_CLOSED arm it is never called (connection_close_ctx is constructed only if impl is non-null AND hooks are armed, but the lambda itself is allocated eagerly regardless). The lambda object itself is trivial so there is no runtime cost, but declaring a callable that is conditionally unused makes the reader wonder whether NOTIFY_CLOSED is missing a peer population.
   *Recommendation:* Either call resolve_peer() in the NOTIFY_CLOSED arm when firing the hook (to populate connection_close_ctx.peer, which is the symmetrical thing to do), or add a brief comment noting that close context intentionally receives a zeroed peer_address pending a future lookup. The former is the cleaner option and makes the two arms symmetric.

12. [ ] **code-simplifier** | `src/hook_handle.cpp:116` | code-structure
   The switch in hook_handle::remove() repeats the two-step pattern `erase_if_found(impl->hooks_X_); reset_gate_if_empty(impl->hooks_X_);` for every phase. Adding the three new lifecycle phases (connection_opened, connection_closed) follows the same mechanical repetition. While this is an existing pattern rather than newly introduced code, TASK-046 extended it and the extension is ripe for a table-driven approach.
   *Recommendation:* Consider a small helper that takes a (vector_ref, phase) pair and performs erase + gate reset in one call, then iterate over a std::array of such pairs. This is a low-urgency polish item since the switch is already constrained to the number of hook phases, but it reduces the per-phase addition cost from 3 lines to 1 in a future task.

13. [ ] **housekeeper** | `specs/architecture/04-components/hooks.md:9` | architecture-not-updated
   The hooks architecture doc (§4.10) still shows the firing-site column referencing 'webserver.cpp:connection_notify' and 'webserver.cpp:policy_callback'. These symbols now live in 'src/detail/webserver_callbacks_lifecycle.cpp' (split out per the task's action item 1). The file:symbol cross-reference is stale.
   *Recommendation:* Update the 'Fires at (file:symbol)' column for connection_opened, accept_decision, and connection_closed phases to reference 'webserver_callbacks_lifecycle.cpp' rather than 'webserver.cpp'. This is a minor doc-accuracy issue since the public contract (phase semantics) is unchanged.

14. [ ] **housekeeper** | `specs/tasks/_index.md:null` | documentation-stale
   Architecture doc cross-references (housekeeper-iter1-2) were deliberately deferred and are not re-raised per instructions.
   *Recommendation:* Deferred — no action required this iteration.

15. [ ] **performance-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-046/src/detail/webserver_callbacks_lifecycle.cpp:132` | missing-caching
   resolve_peer() is a lambda defined on every connection_notify invocation that calls MHD_get_connection_info(). When hooks are registered it is called once per STARTED event and once per CLOSED event. The lambda itself is zero-cost (stack only, captures by reference), but MHD_get_connection_info with MHD_CONNECTION_INFO_CLIENT_ADDRESS is a function call into libmicrohttpd on every hooked connection. This is unavoidable given the hook contract, but callers should be aware that enabling connection_opened or connection_closed hooks adds one MHD API call per connection lifecycle event.
   *Recommendation:* No code change required — the cost is inherent to the hook contract. Document in the public hook API that connection_opened/connection_closed hooks trigger one MHD_get_connection_info call per event so users can make an informed registration decision. The current relaxed-atomic short-circuit (zero hooks = zero cost) already provides the right escape hatch.

16. [ ] **performance-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-046/src/hook_handle.cpp:238` | memory-allocation
   Each fire_* helper unconditionally calls snapshot.reserve(8) before the copy, allocating a heap vector of capacity 8 even when the phase vector holds only 1 or 2 entries. For very high connection rates with a small number of registered hooks this allocates and frees a std::vector on every connection lifecycle event.
   *Recommendation:* Consider reserving the exact snapshot size by reading hooks_*_.size() under the lock before reserving: snapshot.reserve(hooks_connection_opened_.size()). This avoids over-allocation when hook counts are low and avoids a realloc when they exceed 8. Alternatively, a small_vector<8> type (e.g. absl or boost::container::small_vector) would eliminate the heap allocation for the common <= 8 case entirely.

17. [ ] **performance-reviewer** | `src/hook_handle.cpp:252` | memory-allocation
   snapshot.reserve(kHookSnapshotReserve) unconditionally heap-allocates 8 slots every time fire_hooks_for_phase is entered, even when hook_vec is empty. The zero-hooks fast-path is currently protected by the call-site gate in connection_notify (and policy_callback), but fire_hooks_for_phase itself has no internal early-exit before the allocation. Any future caller that bypasses the external gate — including unit tests calling fire_connection_opened directly — will pay the allocator round-trip per event.
   *Recommendation:* Add an internal early-exit after acquiring the shared_lock: check hook_vec.empty() while the lock is held and return immediately without reserving or copying if true. This makes the function self-protecting and shaves the allocator call in the already-guarded path when the vector is empty at fire time (e.g., last hook was just removed). Example: { std::shared_lock lock(impl->hook_table_mutex_); if (hook_vec.empty()) return; snapshot = hook_vec; }

18. [ ] **security-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-046/src/hook_handle.cpp:243` | error-handling
   fire_hooks_for_phase is declared without noexcept but the comment says callers are noexcept and std::terminate is the observable outcome. The outer catch(...) at line 270 absorbs snapshot-copy failures, so no exception can propagate in practice. However, the function signature itself is not noexcept, meaning a compiler cannot statically verify the no-throw contract; if any future code path is accidentally added that throws past the catch, the noexcept callers would std::terminate without a meaningful diagnostic. The risk is purely theoretical in current code since the outer catch is exhaustive.
   *Recommendation:* Consider marking fire_hooks_for_phase noexcept (or a conditional noexcept) so the compiler enforces the no-throw guarantee statically and documents the intent unambiguously. The current outer catch makes this safe to add.

19. [ ] **security-reviewer** | `src/hook_handle.cpp:248` | error-handling
   fire_connection_opened, fire_accept_decision, and fire_connection_closed are declared noexcept, but they call log_dispatch_error which is not noexcept. log_dispatch_error constructs std::string(msg) before calling the user logger; a bad_alloc there would propagate out of log_dispatch_error and, since the caller is noexcept, trigger std::terminate. Under normal operation this is harmless; under a severe OOM attack or allocator failure it becomes an uncontrolled process termination (CWE-391).
   *Recommendation:* Declare log_dispatch_error noexcept (it already swallows the user callback's exceptions; adding noexcept makes the OOM path go through std::terminate at the call site rather than propagating unexpectedly). Alternatively, add a try/catch around the std::string(msg) construction inside log_dispatch_error and silently drop the log line on allocation failure.

20. [ ] **security-reviewer** | `src/hook_handle.cpp:256` | error-handling
   The outer catch-all in fire_connection_opened (and sibling fire_* functions) is labeled 'snapshot copy failed' but it can also be triggered by a bad_alloc inside the inner catch handler's std::string concatenation (e.g. 'hook[connection_opened] threw: ' + e.what()). Under severe memory pressure, the misleading message makes triage harder without indicating the actual failure site.
   *Recommendation:* Either mark the string-building in the inner catch with a separate try/catch that passes a fixed literal to log_dispatch_error, or split the outer catch message to distinguish 'snapshot failure' from 'error-handler failure'. Example: wrap the std::string(...) + e.what() construction in its own try block with a fixed-string fallback.

21. [ ] **spec-alignment-checker** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-046/test/integ/hooks_connection_lifecycle.cpp:185` | specification-gap
   The acceptance criterion states hooks fire in order '(connection_opened, accept_decision{accepted=true}, connection_closed)' but the test explicitly relaxes the ordering constraint between connection_opened and accept_decision, acknowledging MHD fires policy_callback before NOTIFY_STARTED. The test comment at line 28-29 says 'the natural order is accept_decision first, but the test should not pin a specific MHD callback ordering'. This is a deliberate and correct engineering decision given MHD's documented callback ordering, but it means the test cannot assert the precise three-phase order stated in the acceptance criterion — only that connection_closed is last.
   *Recommendation:* Consider updating the acceptance criterion text in TASK-046.md to reflect the relaxed ordering (accept_decision or connection_opened first, connection_closed last) as was implemented, so the spec matches the actual testable behavior.

22. [ ] **test-quality-reviewer** | `test/integ/hooks_accept_decision_throwing.cpp:72` | multiple-concerns
   The two sub-tests (`banned_with_throwing_hook_still_rejected` and `unbanned_with_throwing_hook_still_accepted`) share the same PORT constant base (8201 / 8202). If both tests run in the same process concurrently the port assignment is correct, but the comment in the file says the tests run sequentially via the AUTORUN macro. No isolation issue, but re-using `PORT + 1` for the second test creates an implicit coupling that is easy to break if a third test is added to this file.
   *Recommendation:* Assign each test an independent named constant (e.g. `PORT_BANNED = 8201`, `PORT_ACCEPTED = 8202`) rather than arithmetic on the same base, to make port assignments self-documenting and independent.

23. [ ] **test-quality-reviewer** | `test/integ/hooks_no_firing.cpp:167` | logic-in-test
   The lambda `not_yet_wired` contains a switch/case inside the test body. This is logic in test code — the set of 'wired' phases is hardcoded knowledge that could diverge silently from the actual implementation. The if-guarded skip means a phase that starts firing early (before its TASK lands) produces no assertion failure, only silence.
   *Recommendation:* Replace the lambda+switch with a flat set of explicit phase-ordinal checks or a static constexpr bitset outside the test body. Alternatively, assert that each of the three wired phases fired at least once (mirroring what hooks_connection_lifecycle already does) so both halves are enforced: wired phases fire, unwired phases stay silent.

24. [ ] **test-quality-reviewer** | `test/unit/hooks_accept_ctx_shape_test.cpp:49` | naming-convention
   The single runtime test is named `runtime_construction`, which describes mechanism rather than the observable contract being pinned. The static_asserts above it carry the real intent; the runtime test's name gives no indication of what would fail or why.
   *Recommendation:* Rename to `default_accepted_true_reason_nullopt_and_aggregate_init_with_banned` (or a shorter equivalent) so the name communicates both the default-construction invariant and the aggregate-init-with-decision variant exercised.
