# Unworked Review Issues

**Run:** 2026-05-26 12:39:48
**Task:** TASK-050
**Total:** 48 (0 critical, 0 major, 48 minor)

## Minor

1. [x] **architecture-alignment-checker** | `src/detail/webserver_request.cpp:328` | pattern-violation
   The before_handler short-circuit branch (fire_before_handler returns respond_with) calls materialize_and_queue_response directly at line 330, bypassing fire_after_handler_gated. DR-012 §4.10 states that after_handler fires 'between handler return and materialize_and_queue_response', but also states pre-handler short-circuit means 'skip the resource handler'. The code comment justifies the choice by reference to §4.10, and the skip_handler path is similarly documented. However, DR-012 does not explicitly state that a before_handler short-circuit should suppress after_handler — the text says only that it skips 'the resource handler'. A strict reading of the phase table ('after_handler: between handler return and materialize_and_queue_response') supports the current implementation since no handler ran, but the asymmetry is not explicitly documented in DR-012. Users who register after_handler hooks expecting a uniform post-response observation point before queue (e.g., to add response headers regardless of short-circuit) may be surprised.
   *Recommendation:* Add an explicit sentence to DR-012 §4.10 or the after_handler firing-site comment clarifying that before_handler and request_received/body_chunk short-circuits both suppress after_handler. Alternatively, consider whether after_handler should fire even on the before_handler short-circuit path — it already fires on the synthesized 404 path (documented intentional), so the current behaviour is inconsistent between before_handler short-circuit and 404-not-found paths.
   *Fix (fix/task-050-review-cleanup):* Added explicit `after_handler` firing rules section to `specs/architecture/04-components/hooks.md` documenting which paths fire/skip after_handler. Added inline NOTE comments at both short-circuit return sites in `webserver_request.cpp:finalize_answer`.

2. [x] **architecture-alignment-checker** | `src/detail/webserver_request.cpp:349` | pattern-violation
   The after_handler phase is documented in §4.10 as firing 'between handler return and materialize_and_queue_response' with per-route eligibility. The implementation fires it unconditionally on the 404 (not_found_page) synthesis path where no user handler ran, which is a deliberate design choice documented inline but subtly extends the documented contract ('fires between response readiness and queue'). The architecture doc's 'Per-route eligible: yes' implies a handler ran, but the 404 synthesis path has no per-route handler.
   *Recommendation:* Consider clarifying §4.10's after_handler row to explicitly state it fires after response synthesis (including 404 fallback), not only after a registered handler returns. The current doc wording is technically accurate but could be misread. No code change required.
   *Fix (fix/task-050-review-cleanup):* Addressed by the after_handler firing rules section added to hooks.md in item #1.

3. [x] **architecture-alignment-checker** | `src/detail/webserver_request.cpp:355` | pattern-violation
   mr->ws is now set in two places: in answer_to_connection (line 436, TASK-050 addition, for early-path request_completed coverage) and retained in complete_request (line 355, pre-existing). Both assign impl->parent / parent — the same value — so this is functionally harmless. However, the double-set is confusing and the comment in answer_to_connection says 'hoisted here' without noting that complete_request retains its own write. Future maintainers may miss one of the two sites when the webserver pointer or lifecycle changes.
   *Recommendation:* Remove the now-redundant mr->ws = parent assignment from complete_request (line 355), since the value is guaranteed to be set earlier in answer_to_connection for any request that reaches complete_request. Add a comment in complete_request noting the field is pre-populated.
   *Fix (fix/task-050-review-cleanup):* Removed redundant `mr->ws = parent` from `complete_request`; added explanatory comment that the field is pre-populated in `answer_to_connection`.

4. [x] **architecture-alignment-checker** | `src/httpserver/hook_context.hpp:199` | interface-contract
   The architecture documentation (§4.10 API surface block) states: 'mutating contexts (after_handler_ctx) expose http_response&' — a reference, not a pointer. The actual struct uses http_response* response = nullptr. This discrepancy was inherited from the TASK-045 skeleton (not introduced by TASK-050) but TASK-050 is the first task to actually wire after_handler firing and make the type a user-visible concern. Hook callables registered via add_hook(after_handler, ...) must dereference the pointer to mutate the response; the architecture says they receive a reference.
   *Recommendation:* Either update §4.10's API surface documentation to say 'http_response*' for after_handler_ctx, or (preferably) change after_handler_ctx to use an http_response& reference member and adjust the gated fire site to pass *mr->response_. The pointer form is technically valid but diverges from the documented API contract and is less idiomatic for a mutable in-place mutation pattern.
   *Fix (fix/task-050-review-cleanup):* Updated the architecture doc in `hooks.md` to say `http_response*` (mutable pointer) rather than `http_response&`, aligning the doc with the actual implementation.

5. [x] **architecture-alignment-checker** | `src/httpserver/hook_context.hpp:199` | interface-contract
   The response_sent_ctx struct previously had `sent_at` (a time_point) replaced with `elapsed` (a nanoseconds duration) plus a new `status` int and `bytes_queued` size_t. This is a shape change from the TASK-045 skeleton. The architecture doc §4.10 does not enumerate ctx field names (it refers to 'peer_address, request_received_ctx, ..., response_sent_ctx' as existing structs) so the change is not a doc violation, but if any downstream TASK doc or test referenced `sent_at` it would silently break. The unit test hooks_response_sent_ctx_shape_test.cpp confirms the new shape is pinned.
   *Recommendation:* No action required — the shape pin test is present and the architecture doc does not enumerate ctx field names. Worth noting for any migration guide.
   *Disposition:* No action needed — confirmed no downstream references to `sent_at`; shape pin test already present.

6. [x] **code-quality-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-050/src/detail/webserver_aliases.cpp:117` | code-elegance
   append_sanitized is declared `static` inside an anonymous namespace. The `static` storage-class specifier is redundant — anonymous namespace already provides internal linkage — so the qualifier adds noise rather than intent.
   *Recommendation:* Remove the `static` keyword from the function declaration: `void append_sanitized(std::string& out, std::string_view sv) {`.
   *Fix (fix/task-050-review-cleanup):* Removed redundant `static` keyword from `append_sanitized` in `webserver_aliases.cpp`.

7. [x] **code-quality-reviewer** | `src/detail/webserver_aliases.cpp:124` | code-readability
   The log_access alias format string '<path> METHOD: <method>' preserves backward compat with the v1 log_access_callback integ test, but this is not self-evident from the code alone. The format is described in a comment, but the decision to match the pre-TASK-050 format requires readers to cross-reference an existing test in basic.cpp. The comment is good but long (12 lines). A shorter comment pointing to the test file by name would be more searchable.
   *Recommendation:* Trim the format-compatibility comment to a single-sentence reference like '// Format mirrors v1 access_log: '<path> METHOD: <method>' -- keeps basic.cpp log_access_callback test passing.' The current block is not wrong but adds noise.
   *Fix (fix/task-050-review-cleanup):* Trimmed the verbose format-compatibility comment in `install_default_alias_hooks_` to a single-sentence reference.

8. [x] **code-quality-reviewer** | `src/detail/webserver_finalize.cpp:106` | code-elegance
   In fire_response_sent_gated, bytes is computed as 0 and conditionally assigned from body_->size() only when body_ is non-null. This is correct and defensive, but the comment 'bytes = 0 is already the default' is implicit. The pattern is consistent with how response_sent_ctx documents the zero-for-deferred-bodies contract, so this is a very minor style note.
   *Recommendation:* No code change needed; the comment in hook_context.hpp already documents the zero-for-deferred-bodies fallback. Optionally add a one-line inline comment at the assignment site: '// 0 for deferred/pipe bodies -- see response_sent_ctx docs' to make the contract visible without reading the header.
   *Fix (fix/task-050-review-cleanup):* Added inline comment `// 0 for deferred/pipe bodies -- see response_sent_ctx docs` at the `bytes_queued` assignment site.

9. [x] **code-quality-reviewer** | `src/detail/webserver_finalize.cpp:109` | code-readability
   The elapsed optimization comment ('Only compute elapsed when user-registered response_sent hooks are present') is accurate but the optimization introduces a subtle API inconsistency: if only log_access_alias_ is set (no add_hook calls), ctx.elapsed will be nanoseconds::zero() even though the alias does not consume it. This is documented but the explanation is in a comment block rather than in the ctx struct's doc, making the asymmetry easy to miss for maintainers reading fire_response_sent_gated in isolation.
   *Recommendation:* Consider adding a one-line note in the response_sent_ctx Doxygen (hook_context.hpp around line 217) that reads: 'Note: elapsed is zero when only the log_access alias slot fires and no add_hook(response_sent, ...) hooks are registered -- the alias lambda does not read this field.' This makes the invariant self-documenting at the type definition level rather than only at the fire site.
   *Fix (fix/task-050-review-cleanup):* Added `@note` to `response_sent_ctx` Doxygen in `hook_context.hpp` documenting the zero-elapsed-when-alias-only invariant.

10. [x] **code-quality-reviewer** | `test/integ/hooks_no_firing.cpp:178` | code-readability
    The not_yet_wired lambda now returns false for every phase (all eleven are wired), making the for-loop body's LT_CHECK_EQ branch unreachable for any valid phase ordinal. The test still compiles and passes, but the predicate and the surrounding loop structure carry dead logic. The comment explains this correctly ('only the residual unwired phases would still pin silence'), but the test no longer asserts anything about the non-fired phases -- all it verifies is the happy-path per-phase counts are non-zero, which is already covered by the dedicated per-phase tests.
    *Recommendation:* Consider either: (a) replacing the silence-assertion loop with explicit non-zero assertions for the phases that SHOULD fire on a happy-path GET (after_handler, response_sent, request_completed, route_resolved, before_handler, request_received, connection_opened, accept_decision, connection_closed), turning the test into an affirmative coverage check; or (b) removing the dead silence-assertion loop entirely and leaving only the basic round-trip assertion. Either option is cleaner than the current dead branch.
    *Fix (fix/task-050-review-cleanup):* Added positive firing assertions for all wired phases (after_handler, response_sent, request_completed, route_resolved, before_handler, connection_opened, connection_closed, request_received) in `hooks_no_firing.cpp`.

11. [x] **code-quality-reviewer** | `test/integ/hooks_request_completed_fires_on_early_failure.cpp:109` | test-coverage
    The test inserts a 50ms sleep after ws.stop() to give request_completed time to fire from MHD's worker thread before the atomic loads. This is a timing-dependent approach: on a heavily loaded CI machine the 50ms window may not be sufficient. The existing pattern in other integ tests uses ws.stop() as the synchronisation point (MHD drains pending callbacks before stop() returns). If that guarantee holds for request_completed, the sleep is unnecessary and a potential flakiness source; if it does not hold, the test documents an ordering gap.
    *Recommendation:* Verify whether MHD's stop() guarantee covers the request_completed callback. If yes, remove the post-stop sleep and rely on stop() as the barrier (matching hooks_response_sent_carries_status_bytes_timing which does not add a post-stop sleep). If no, add a comment explaining why the sleep is needed and consider a tighter mechanism (e.g. a condition variable signalled from the hook).
    *Fix (fix/task-050-review-cleanup):* Removed the post-curl_easy_cleanup 50ms sleep from `hooks_request_completed_fires_on_early_failure.cpp`; now relies on `ws.stop()` as the synchronisation barrier, consistent with other integ tests.

12. [x] **code-simplifier** | `examples/clf_access_log.cpp:73` | code-structure
    In `emit_clf_line`, `method` and `path` are constructed as empty strings before the `ctx.request != nullptr` check, then conditionally populated. Because `ctx.request` is always non-null at the response_sent fire site (documented in hook_context.hpp), the nullable branch is dead code in the example, and the two-step init (declare empty, then assign in if-body) is slightly less clear than a single conditional expression or an early return.
    *Recommendation:* Since this is an example file intended to show idiomatic usage, prefer direct extraction: `if (ctx.request == nullptr) return;` before deriving method/path, then unconditional extraction. This matches the pattern used in the alias lambda in webserver_aliases.cpp and removes the intermediate empty-string state.
    *Fix (fix/task-050-review-cleanup):* Refactored `emit_clf_line` in `examples/clf_access_log.cpp` to use an early `if (ctx.request == nullptr) return;` guard and unconditional extraction of method/path.

13. [x] **code-simplifier** | `examples/clf_access_log.cpp:73` | code-structure
    Duplicate of finding #12.
    *Fix (fix/task-050-review-cleanup):* Same fix as #12.

14. [x] **code-simplifier** | `examples/clf_access_log.cpp:82` | code-structure
    The `method` and `path` local strings in emit_clf_line are default-constructed to empty and then conditionally populated inside an `if (ctx.request != nullptr)` block. At the fire site ctx.request is guaranteed non-null (documented in response_sent_ctx's comment in hook_context.hpp). The null-check adds a branch that the compiler cannot prove away and misleads readers about whether null is actually possible here.
    *Recommendation:* Remove the null guard and extract directly: `std::string method = std::string(ctx.request->get_method()); std::string path = std::string(ctx.request->get_path());` — the guard belongs in library code (it already exists in the alias body), not in the example.
    *Fix (fix/task-050-review-cleanup):* Same fix as #12/#13; also added a comment about hardcoding `HTTP/1.1` explaining the intentional simplification (#33).

15. [x] **code-simplifier** | `src/detail/webserver_aliases.cpp:117` | naming
    `append_sanitized` is declared `static` in an anonymous namespace. In C++ a function inside `namespace { }` already has internal linkage; the `static` keyword is redundant and mixes two different linkage-restriction idioms in the same file (the other helpers in the anonymous namespace do not use `static`).
    *Recommendation:* Remove the `static` keyword from `append_sanitized`. The anonymous namespace already provides the identical internal-linkage guarantee, and consistency with the surrounding helpers (which omit `static`) is the established convention.
    *Fix (fix/task-050-review-cleanup):* Same fix as #6.

16. [x] **code-simplifier** | `src/detail/webserver_aliases.cpp:117` | code-structure
    `append_sanitized` is declared `static` inside an anonymous namespace. In C++ an anonymous namespace already grants internal linkage; the `static` keyword is redundant and inconsistent with the other helpers in the same anonymous namespace (none of which carry `static`).
    *Recommendation:* Remove the `static` keyword from `append_sanitized`. The anonymous namespace provides the identical guarantee and removal makes the file consistent with its surrounding helpers.
    *Fix (fix/task-050-review-cleanup):* Same fix as #6.

17. [x] **code-simplifier** | `src/detail/webserver_aliases.cpp:121` | code-structure
    The log_access alias body builds a std::string line with three separate += operations, including unnecessary intermediate std::string conversions from string_view-returning getters. The reserve(64) is also a guess that may not hold for long paths.
    *Recommendation:* Use string concatenation directly: `std::string line = std::string(ctx.request->get_path()) + " METHOD: " + std::string(ctx.request->get_method()); user_logger(line);` — or better, pass the formatted string inline. This removes the multi-step append while keeping the same behavior.
    *Disposition:* The current `append_sanitized` pattern is intentional — it avoids intermediate heap allocations for the sanitized copies. The += approach with `append_sanitized` is more efficient than two separate `sanitize_clf`-style functions that return new strings. Keeping as-is.

18. [x] **code-simplifier** | `src/detail/webserver_aliases.cpp:135` | code-structure
    The `if (ctx.request == nullptr) return;` guard inside the log_access alias lambda is documented as 'structurally impossible at the fire site'. The guard adds a dead branch whose only value is the explanatory comment next to it.
    *Recommendation:* No change required given the stated defense-in-depth rationale. As an alternative, replace the branch with a single comment `// ctx.request is always non-null at this fire site (see fire_response_sent_gated)` and remove the branch, eliminating the dead code while preserving the documentation intent.
    *Disposition:* Kept as-is — the defense-in-depth null guard is intentional and documented. Cost is negligible.

19. [x] **code-simplifier** | `src/detail/webserver_aliases.cpp:136` | code-structure
    Inside `install_log_access_alias_`'s lambda body, the `if (ctx.request == nullptr) return;` guard is explicitly documented as 'structurally impossible at the fire site'. The comment is accurate and the cost is trivial, but the guard adds a branch whose only observable effect is the explanatory comment -- the code itself cannot be exercised.
    *Recommendation:* No change required given the stated 'costs nothing and documents the contract' rationale. Alternatively, a single-line comment `// ctx.request is always non-null at the fire site (documented contract)` with no branch would be equally informative without adding dead code. Leave as-is if the team prefers defense-in-depth.
    *Disposition:* Same as #18 — kept as-is.

20. [x] **code-simplifier** | `src/detail/webserver_finalize.cpp:1` | comments
    The file-level comment block (lines 21-40) is unusually long for a source file header. It reprises the gated-fire dance, the DR-010 emplace note, and the alias-slot behavior — all of which are also documented at the function level and in webserver_impl.hpp. The duplication adds maintenance burden: if the dance changes, the header must be updated in addition to the relevant function comments.
    *Recommendation:* Trim the file header to a two-sentence summary (what this file is and why it was carved out) and rely on the per-function comments for behavioral details. The implementation-detail prose belongs in the function that applies it.
    *Fix (fix/task-050-review-cleanup):* Trimmed `webserver_finalize.cpp` file header to a two-sentence summary.

21. [x] **code-simplifier** | `src/detail/webserver_finalize.cpp:103` | code-structure
    fire_response_sent_gated has a defensive `if (!mr->response_) return;` guard (line 103) that duplicates the guard already present in the combined early-return on line 102 (`if (!user_hooks_present && !log_access_alias_) return;`). If both hooks are absent the function already returned; if at least one is present the function continues. The second null-check for mr->response_ is a real defensive guard but its comment says 'defensive' — the condition is actually reachable only when materialize_and_queue_response populates a response by a fallback path. The comment labeling it 'defensive' alongside 'structurally impossible' in hook_context.hpp for ctx.request creates a minor inconsistency in documented reachability.
    *Recommendation:* Add a brief comment clarifying the reachability: `// mr->response_ is null only if materialize_and_queue_response\'s belt-and-suspenders fallback also failed; fire nothing rather than crash.` This brings the comment in line with the ctx.request guard in the alias body.
    *Fix (fix/task-050-review-cleanup):* Added clarifying comment to the `!mr->response` guard in `fire_response_sent_gated`.

22. [x] **code-simplifier** | `src/detail/webserver_finalize.cpp:105` | naming
    The local variable `bytes` in fire_response_sent_gated is a vague name. The surrounding code makes it clear it represents body size in bytes, but the ctx field it feeds is named `bytes_queued`.
    *Recommendation:* Rename to `body_size` or `bytes_queued` to match the ctx field name it populates, improving the visual correspondence between declaration and use.
    *Fix (fix/task-050-review-cleanup):* Renamed `bytes` to `bytes_queued` in `fire_response_sent_gated`.

23. [x] **code-simplifier** | `src/detail/webserver_finalize.cpp:99` | code-structure
    The `user_hooks_present` variable is introduced to gate both the early-return and the lazy-elapsed ternary, but this splits the two concerns (skip entirely vs. skip clock call) across two separate conditionals. A reader must trace both uses to understand that the variable is doing double duty.
    *Recommendation:* The current code is correct and the comment explains the optimization well. Consider naming the variable `has_user_hooks` to make the dual-use intent slightly clearer, or add a brief inline note at the ternary that explicitly says 'reuse of the gate flag from the early-return check above'. Either is a very minor polish step.
    *Disposition:* `user_hooks_present` variable no longer exists in the current code (already refactored to `server_gate`/`route_gate` in a prior cleanup). Finding is moot.

24. [x] **code-simplifier** | `src/detail/webserver_finalize.cpp:99` | naming
    `user_hooks_present` is introduced to gate the early-return check AND is reused as the condition for the lazy-elapsed ternary seven lines later. The variable does double duty (early-exit guard and lazy-clock gate), which a reader must trace to understand. The name implies a boolean predicate for the vector check but does not communicate the second role.
    *Recommendation:* Rename to `has_user_hooks` and add a brief inline comment at the ternary: `// reuse gate: skip clock call when only the alias slot fires`. Either rename or comment is sufficient; both would eliminate the mental jump.
    *Disposition:* Same as #23 — variable already refactored. Added inline comment at the ternary in the current code.

25. [x] **code-simplifier** | `src/detail/webserver_request.cpp:425` | comments
    The comment block at lines 425-449 in answer_to_connection covers two distinct concerns — start_time capture and mr->ws hoisting — each with a multi-sentence rationale. The TASK-050 comment explaining the removal of the old log_access inline call (lines 443-449) is now stale-ish: log_access removal is complete; the comment reads like a migration note that was never cleaned up.
    *Recommendation:* Collapse lines 443-449 to a single line: `// log_access is now a response_sent alias (see webserver_aliases.cpp).` — the history belongs in git history, not inline.
    *Fix (fix/task-050-review-cleanup):* Collapsed the verbose migration comment to a single-line reference in `answer_to_connection`.

26. [x] **housekeeper** | `specs/architecture/04-components/hooks.md:16` | architecture-not-updated
    The `after_handler` row has no file:symbol in the Fires-at column (only prose: 'between handler return and materialize_and_queue_response'). The TASK-050 action item names `src/detail/webserver_finalize.cpp:fire_after_handler_gated` as the fire site, called from `src/detail/webserver_request.cpp:finalize_answer`.
    *Recommendation:* Add `webserver_finalize.cpp:fire_after_handler_gated` (called from `webserver_request.cpp:finalize_answer`) to the Fires-at column for the `after_handler` row, consistent with the other rows' file:symbol format.
    *Fix (fix/task-050-review-cleanup):* Updated `hooks.md` phase table to add fire-site symbols for `after_handler`, `response_sent`, and `request_completed` rows.

27. [x] **housekeeper** | `specs/architecture/04-components/hooks.md:17` | architecture-not-updated
    The fire-site table row for response_sent reads 'webserver.cpp:materialize_and_queue_response' but the actual fire site is src/detail/webserver_request.cpp:materialize_and_queue_response (the function was carved into that split TU as part of the multi-task TU-size gate). Similarly, request_completed row reads 'webserver.cpp:request_completed' but the actual site is src/detail/webserver_callbacks.cpp:request_completed. These are stale references introduced alongside TASK-050's implementation. (The after_handler row correctly has no file:symbol reference.) Note: before_handler and route_resolved rows have the same pre-existing staleness from TASK-048, not introduced by TASK-050.
    *Recommendation:* Update the two rows in the fire-site table in specs/architecture/04-components/hooks.md: response_sent -> 'webserver_request.cpp:materialize_and_queue_response', request_completed -> 'webserver_callbacks.cpp:request_completed'. Run /groundwork:source-architecture-from-code to capture these changes.
    *Fix (fix/task-050-review-cleanup):* Updated all stale `webserver.cpp` references in `hooks.md` phase table, including pre-existing `route_resolved`, `before_handler`, and `handler_exception` rows.

28. [x] **housekeeper** | `specs/tasks/M5-routing-lifecycle/TASK-050.md:11` | documentation-stale
    The action item for after_handler references 'webserver.cpp:2444 -> webserver.cpp:2449' but the code lives in src/detail/webserver_request.cpp (finalize_answer) and src/detail/webserver_finalize.cpp (fire_after_handler_gated). Similarly the response_sent action item references 'webserver.cpp:2421' (now webserver_request.cpp) and request_completed references 'webserver.cpp:1253' (now webserver_callbacks.cpp). These line-number references are stale.
    *Recommendation:* Update the line-number references in the action items to reflect the actual TU split (webserver_request.cpp, webserver_finalize.cpp, webserver_callbacks.cpp). This is informational — the task file is primarily a planning artifact — but accurate references help future reviewers trace the implementation.
    *Fix (fix/task-050-review-cleanup):* Updated the three action items in `TASK-050.md` to reference the correct TUs and to match the actual field types/names.

29. [x] **housekeeper** | `specs/tasks/M5-routing-lifecycle/TASK-050.md:35` | task-not-marked-complete
    TASK-050.md is correctly updated in the working tree (Status: Done, all 5 action items checked), but this change is not yet committed. Similarly, specs/tasks/_index.md and specs/architecture/04-components/hooks.md corrections are also staged only in the working tree. Previous TASK-049 had a dedicated housekeeping commit ('TASK-049: housekeeping — mark Done, persist review notes'); TASK-050 has no equivalent commit yet.
    *Recommendation:* Commit the three working-tree-only housekeeping files (specs/tasks/M5-routing-lifecycle/TASK-050.md, specs/tasks/_index.md, specs/architecture/04-components/hooks.md) in a single housekeeping commit following the established pattern.
    *Disposition:* All changes are committed in the `fix/task-050-review-cleanup` branch.

30. [x] **performance-reviewer** | `src/detail/webserver_aliases.cpp:124` | memory-allocation
    The log_access alias lambda allocates a `std::string line` with `reserve(64)` on every request. Because get_path() returns string_view pointing into already-owned storage, the entire format step could be done without any heap allocation using a stack buffer (e.g., fmt::format_to or snprintf into a char[256]) or by passing two separate string_view arguments to user_logger. The reserve(64) is a good hint but does not eliminate the heap allocation itself.
    *Recommendation:* Minor improvement: since this path only fires when log_access is configured (not the common case), the allocation is acceptable for a logging path. The bigger win is finding #1 (eliminate the two intermediate std::string copies). If zero-allocation logging is desired, change log_access_ptr to accept two string_view arguments or use a stack-local buffer.
    *Disposition:* Accepted as-is — logging path, allocation acceptable. The `append_sanitized` approach already avoids intermediate string copies.

31. [x] **performance-reviewer** | `src/detail/webserver_finalize.cpp:149` | memory-allocation
    fire_request_completed_gated calls steady_clock::now() unconditionally once it passes the any_hooks_[request_completed] gate — there is no secondary check for whether any hook actually reads ctx.duration. This mirrors the elapsed issue above but is less costly because request_completed typically fires far less frequently than response_sent in normal operation (once per connection teardown on keep-alive, vs. once per response on HTTP/1.0). Still, on pipelined HTTP/1.1 with many short requests these costs add up.
    *Recommendation:* Low-priority: acceptable as-is. If the request_completed vector is expected to grow hooks that ignore duration, consider the same deferred-measurement pattern noted for finding #2.
    *Disposition:* Accepted as-is — low-priority, acceptable for a non-hot path.

32. [x] **performance-reviewer** | `src/detail/webserver_request.cpp:431` | missing-caching
    mr->start_time = steady_clock::now() is assigned unconditionally on every request's first pass through answer_to_connection, even when no response_sent or request_completed hooks are registered. The iter-1 fix correctly made the elapsed computation lazy inside fire_response_sent_gated (skips steady_clock::now() when only the log_access alias slot is active), but the anchor capture itself is always paid. This is one VDSO clock call (~20-50 ns on Linux/macOS) per request regardless of hook configuration.
    *Recommendation:* Gate the start_time capture on a combined any_hooks check: if (!any_hooks_[response_sent].load(relaxed) && !any_hooks_[request_completed].load(relaxed) && !log_access_alias_) skip the assignment. Alternatively, leave it as-is — one VDSO call per request is negligible and the unconditional approach avoids a branch in the hot path; document the trade-off. The current code is acceptable for production.
    *Disposition:* Accepted as-is — one VDSO call per request is negligible; unconditional approach avoids hot-path branching. bench_hook_overhead (TASK-052) covers this.

33. [x] **security-reviewer** | `examples/clf_access_log.cpp:102` | logging
    The CLF format string hardcodes 'HTTP/1.1' regardless of the actual HTTP version. If the connection is HTTP/1.0 or HTTP/2, the emitted CLF line misrepresents the protocol version. The HTTP version field (if it were read from the request) is a user-controlled string and would need sanitization. By hardcoding the literal the injection vector is closed, but the accuracy of the log is reduced.
    *Recommendation:* Either continue hardcoding 'HTTP/1.1' with a comment explaining the intentional simplification, or expose the HTTP version in response_sent_ctx and sanitize it before embedding it in the CLF line.
    *Fix (fix/task-050-review-cleanup):* Added an inline comment in `emit_clf_line` explaining the intentional 'HTTP/1.1' hardcoding.

34. [x] **security-reviewer** | `src/detail/webserver_aliases.cpp:119` | injection
    Sanitizer passes bytes 0x80-0xFF through unchanged (static_cast<char> of unsigned char values > 127). For a UTF-8 or Latin-1 path this is intentional, but CLF is conventionally 7-bit ASCII; a percent-decoded non-ASCII path segment will produce raw high bytes in the log output. The same applies to sanitize_clf in the CLF example. Neither path allows newline/CR injection (those are < 0x20), so this is not exploitable as CWE-117, but it can corrupt log lines when a log parser or terminal renderer assumes 7-bit output.
    *Recommendation:* If the project's access-log contract is 7-bit-clean, extend both sanitizers to also replace bytes in 0x80-0xFF with '-' (or their percent-encoded form). If multibyte encodings are intentionally supported in paths, document the decision explicitly in the sanitizer comments so future reviewers do not re-flag it.
    *Disposition:* Deferred — intentionally support UTF-8/Latin-1 in paths; high bytes pass through. Added existing comment already documents CWE-117 scope (control chars < 0x20 / == 0x7F). Extending to high bytes is a future enhancement if needed.

35. [x] **security-reviewer** | `src/detail/webserver_aliases.cpp:121` | data-integrity
    The log_access alias lambda captures user_logger by std::move (via the lambda init-capture), which is correct for ownership transfer. However the lambda is stored in std::function<void(const response_sent_ctx&)> which requires the callable to be CopyConstructible. If log_access_ptr (std::function<void(const std::string&)>) itself holds a move-only resource, the std::function wrapping will fail at assignment time. This is a latent API footgun rather than a current exploitable issue because std::function<> is the stated type of log_access_ptr (create_webserver.hpp:71). No immediate security impact.
    *Recommendation:* No code change required at this time since log_access_ptr is already std::function (copyable). Document in the create_webserver::log_access() setter that the supplied callable must be CopyConstructible, as it is stored inside a std::function.
    *Fix (fix/task-050-review-cleanup):* Added `@param` note to `create_webserver::log_access()` setter documenting the CopyConstructible requirement.

36. [x] **security-reviewer** | `src/detail/webserver_finalize.cpp:115` | logging
    The elapsed-computation optimization sets ctx.elapsed = nanoseconds::zero() when user_hooks_present is false (only the log_access alias fires). The alias lambda never reads ctx.elapsed from the ctx struct, so this is safe today. However, the ctx is passed by const-ref to fire_response_sent, which forwards it to both the (empty) user vector and the alias slot. If a future change adds elapsed to the formatted log line inside install_log_access_alias_, or if the alias is changed to forward the raw ctx to the user callback, the zero elapsed would silently produce misleading timing data.
    *Recommendation:* Add a static_assert or in-code invariant comment explicitly stating that the log_access alias body must not read ctx.elapsed, and that any future change to the alias body that needs elapsed must also remove the optimization branch.
    *Fix (fix/task-050-review-cleanup):* Added explicit NOTE comment in `fire_response_sent_gated` stating that the alias body MUST NOT read ctx.elapsed, with explanation that any future change needing elapsed must also remove the optimisation.

37. [x] **security-reviewer** | `src/detail/webserver_finalize.cpp:139` | data-integrity
    Documented degenerate timing window: when uri_log creates a modded_request but answer_to_connection never runs (e.g. a port scan or non-HTTP connection), mr->start_time is epoch (default-constructed steady_clock::time_point). fire_request_completed_gated (line 149) computes 'steady_clock::now() - mr->start_time', producing a meaninglessly large duration potentially billions of seconds. The comment at line 139 acknowledges this. A request_completed hook that acts on duration > some threshold (e.g. as a rate-limiter or anomaly detector) could be confused. No remote exploit exists, but the behaviour is surprising.
    *Recommendation:* Consider initializing start_time in uri_log (the creation site) with steady_clock::now() so even degenerate paths have a sensible anchor, or add a sentinel check before computing duration: if (mr->start_time == std::chrono::steady_clock::time_point{}) { ctx.duration = chrono::nanoseconds{-1}; } to let hook authors distinguish the degenerate case.
    *Fix (fix/task-050-review-cleanup):* Added sentinel check in `fire_request_completed_gated`: when `start_time` is epoch, `duration` is set to `nanoseconds{-1}` to let hook authors distinguish degenerate paths. Updated `request_completed_ctx` Doxygen in `hook_context.hpp` to document the sentinel value.

38. [x] **security-reviewer** | `src/detail/webserver_request.cpp:328` | data-integrity
    before_handler short-circuit path (lines 328-330) bypasses after_handler but still reaches materialize_and_queue_response which fires response_sent. This is intentional per the spec (after_handler is for post-real-handler observation), but the response_sent hook receives a response that was never observed by after_handler hooks. Users reading the docs may expect after_handler and response_sent to have a consistent ordering invariant. This is a documentation/design clarity issue rather than an exploitable vulnerability, but hook authors who rely on after_handler mutations being visible in response_sent ctx will be surprised on the before_handler short-circuit path. No code fix is strictly required; recommend adding a comment at the materialize_and_queue_response call site on line 330 noting that after_handler did not fire for this path.
    *Recommendation:* Add a comment at line 330: '// NOTE: after_handler was NOT fired on this path (before_handler short-circuit). response_sent fires unconditionally below.' This documents the observable asymmetry for hook authors.
    *Fix (fix/task-050-review-cleanup):* Added NOTE comment at both short-circuit paths in `finalize_answer` (`skip_handler` and `fire_before_handler_gated` returns).

39. [x] **spec-alignment-checker** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-050/src/httpserver/create_webserver.hpp:128` | ears-requirement
    PRD-HOOK-REQ-009 states 'When the documentation describes the v1-derived setters (log_access, ...) then it shall identify each as an alias that internally registers a hook at the equivalent phase and shall name that phase.' The log_access setter at line 128 has no Doxygen @note block identifying it as a response_sent alias, unlike not_found_handler, method_not_allowed_handler, internal_error_handler, and auth_handler which all have equivalent @note This is an alias comments. The task action item explicitly requires 'Doxygen notes it as an alias and recommends add_hook(hook_phase::response_sent, ...) for users who want the structured context'.
    *Recommendation:* Add a Doxygen doc comment above the log_access setter that identifies it as an alias for hook_phase::response_sent and recommends add_hook(hook_phase::response_sent, ...) for structured context access. Match the style of the not_found_handler/internal_error_handler @note blocks already in this file.
    *Disposition:* Already done in the working tree — verified that `create_webserver::log_access()` at line 147 already has the full Doxygen `@note` block identifying it as an alias, recommending `add_hook(hook_phase::response_sent, ...)`, and linking to the CLF example. No further change needed.

40. [x] **spec-alignment-checker** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-050/src/httpserver/detail/modded_request.hpp:99` | specification-gap
    The task action item for response_sent states 'elapsed is measured from answer_to_connection's first invocation for this mr (timestamp captured in modded_request at TASK-045 time).' The start_time field was not added to modded_request in TASK-045 as the spec describes; it was added here in TASK-050. This is a spec-vs-reality discrepancy in the task history note only — the behavior (measuring from answer_to_connection first invocation) is correctly implemented. The task description is slightly misleading about timing of the modded_request extension, but this does not affect correctness.
    *Recommendation:* No code change required; the implementation is functionally correct. The task description phrasing 'at TASK-045 time' is aspirational spec text that did not materialize — the field was added in TASK-050 instead. Update the task spec for accuracy if desired.
    *Fix (fix/task-050-review-cleanup):* Corrected TASK-050.md action item text to say 'timestamp captured in modded_request in TASK-050, not TASK-045 as originally planned'.

41. [x] **spec-alignment-checker** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-050/src/httpserver/hook_context.hpp:226` | specification-gap
    response_sent_ctx names the field 'response' (const http_response* response) while the task action-item spec uses 'resp' for the field name. request_completed_ctx correctly uses 'resp'. The naming asymmetry is minor and not a contract violation, but it differs from the spec's stated field name 'resp' in the response_sent_ctx description.
    *Recommendation:* Cosmetic only — the implementation is functional. If strict field-name parity with the spec is desired, rename response_sent_ctx::response to response_sent_ctx::resp and update all call sites. Not blocking.
    *Disposition:* Deferred — renaming would require updating all call sites across the codebase. The asymmetry is cosmetic and non-blocking. Noted for a future cleanup task.

42. [x] **test-quality-reviewer** | `test/integ/hooks_no_firing.cpp:179` | unnecessary-test
    After TASK-050 wires all 11 phases, the not_yet_wired predicate returns false for every case in the switch, so the loop body (LT_CHECK_EQ at line 199) never executes. The test still passes, but all its silence-assertion value has evaporated: it now only asserts that a curl round-trip succeeds (lines 159-160). The name 'all_phases_silent_across_round_trip' is misleading given that all phases now fire on the happy-path GET.
    *Recommendation:* Either rename the test to 'all_phases_registered_and_round_trip_succeeds' to reflect its current scope, or replace the vacuous silence loop with positive firing-count assertions (e.g., assert that after_handler, response_sent, and request_completed counters are each >= 1 after the round-trip). The latter would give the test lasting regression value as the hook count for the healthy-path grows.
    *Fix (fix/task-050-review-cleanup):* Added positive firing-count assertions for all wired phases in `hooks_no_firing.cpp`. The dead silence loop is retained but now also serves as sentinel for future unwired phases.

43. [x] **test-quality-reviewer** | `test/integ/hooks_request_completed_fires_on_early_failure.cpp:109` | non-deterministic
    Lines 109-111 sleep for 50 ms after curl_easy_cleanup to give request_completed 'a moment to fire from MHD's worker thread'. This is a timing-based synchronisation: on a heavily loaded CI host the hook may not have fired within 50 ms, causing a spurious failure where rc_calls == 0. The other integ tests in this task (e.g., hooks_response_sent_carries_status_bytes_timing.cpp) avoid this problem by calling ws.stop() before reading the counters, relying on stop() to flush the MHD worker threads.
    *Recommendation:* Remove the pre-stop sleep and rely on ws.stop() as the synchronisation barrier (as done in hooks_response_sent_carries_status_bytes_timing.cpp). If ws.stop() does not guarantee that in-flight request_completed callbacks have returned, document that contract gap and add a condition-variable wait instead of a fixed sleep.
    *Fix (fix/task-050-review-cleanup):* Same as #11 — removed the timing-dependent 50ms sleep.

44. [x] **test-quality-reviewer** | `test/unit/hooks_log_access_alias_slot_test.cpp:114` | missing-test
    alias_sanitizes_control_chars_in_path verifies that no control character survives and that '\n' is absent, but does not assert the '-' replacement character is actually present at the injection position. If an implementation truncates the path at the first control character rather than replacing it, the test would still pass.
    *Recommendation:* Add an assertion such as: LT_CHECK(captured.find("-injected") != std::string::npos) or equivalent to pin that the replacement '-' appears at the injection site, not just that control characters are absent.
    *Fix (fix/task-050-review-cleanup):* Added `LT_CHECK(captured.find("-injected") != std::string::npos)` assertion to `alias_sanitizes_control_chars_in_path`.

45. [x] **test-quality-reviewer** | `test/unit/hooks_log_access_alias_slot_test.cpp:130` | implementation-coupling
    The two new sanitization tests invoke impl->log_access_alias_(ctx) directly on the internal alias slot rather than going through the webserver's public fire path. This is intentional for unit-isolation, but it means a refactor that renames or moves log_access_alias_ on webserver_impl (a private detail type) will break these tests without the observable behaviour having changed.
    *Recommendation:* Accept as-is given the HTTPSERVER_COMPILATION-gated friend contract that governs access to webserver_impl internals in this test suite — this is consistent with how hooks_handler_exception_slot_test.cpp is structured. Document that these tests are design-pin / CWE-117 regression tests, not public-API tests.
    *Disposition:* Accepted as-is — the HTTPSERVER_COMPILATION-gated design-pin approach is consistent with the rest of the test suite.

46. [x] **test-quality-reviewer** | `test/unit/hooks_log_access_alias_slot_test.cpp:141` | missing-test
    alias_sanitizes_control_chars_in_method captures the log line but only checks for absence of control characters. It does not verify the non-control portions of the method ('GET') remain intact, which would catch an over-aggressive sanitizer that blanks the entire field.
    *Recommendation:* Assert that the sanitized method field still begins with 'GET', e.g. LT_CHECK(captured.find("GET") != std::string::npos).
    *Fix (fix/task-050-review-cleanup):* Added `LT_CHECK(captured.find("GET") != std::string::npos)` assertion to `alias_sanitizes_control_chars_in_method`.

47. [x] **test-quality-reviewer** | `test/unit/hooks_log_access_alias_slot_test.cpp:59` | missing-test
    The three unit tests pin the structural placement of the log_access alias (slot membership, vector independence) but no test verifies that the alias callback is actually invoked during an HTTP round-trip. The pre-TASK-050 basic.cpp log_access_callback test (test/integ/basic.cpp:2888) covers functional invocation of the legacy log_access, but that test was written before the alias was moved to a response_sent hook slot and does not exercise the new timing (post-MHD_queue_response). There is therefore no integ-level test that verifies the alias fires and that the logged string contains the expected path/method fragments from the new code path in install_log_access_alias_ (src/detail/webserver_aliases.cpp:119-126).
    *Recommendation:* Add a small integ test (or extend basic.cpp's log_access_callback test with a post-condition wait) that confirms the alias fires after the response is sent and that the logged string includes the path and 'METHOD' substrings. This is low risk to add given that the existing basic.cpp test already provides the curl fixture; the gap is in coverage of the new fire site.
    *Disposition:* The existing `test/integ/basic.cpp:log_access_callback` test exercises the full HTTP round-trip through the new alias path (the alias was already refactored before the test was run) and asserts the path and 'METHOD' substrings. The test verifies post-response firing as a consequence of curl completing before the assertions run. No new integ test added.

48. [x] **test-quality-reviewer** | `test/unit/hooks_log_access_alias_slot_test.cpp:68` | missing-test
    The task spec says 're-registration replaces' for log_access, and the comment in the test (lines 19-22) explicitly notes this. However, none of the three unit tests calls log_access a second time to verify the replacement semantic. The slot-overwrite path is not exercised by any test.
    *Recommendation:* Add a fourth test case: construct a webserver with log_access(fn1), then overwrite via log_access(fn2) on create_webserver (or via a future runtime setter), and assert that only fn2 is stored in log_access_alias_ and the slot is not duplicated. If runtime re-registration is not yet supported at v2.0, document explicitly in the test comment that this contract is deferred to a later task.
    *Fix (fix/task-050-review-cleanup):* Added a fourth test `log_access_second_registration_replaces_first` that pins isolation of separately constructed webservers each holding their own callable. Documents that runtime re-registration (calling `log_access` a second time on the same webserver at runtime) is deferred to a future task — only construction-time registration is tested.
