# Unworked Review Issues

**Run:** 2026-05-27 00:00:00
**Task:** TASK-052
**Total:** 21 (0 critical, 0 major, 21 minor)

## Minor

1. [ ] **architecture-alignment-checker** | `RELEASE_NOTES.md:105` | interface-contract
   The 'What's new' hook-bus bullet lists the eleven phases in a non-canonical order: 'connection_opened, accept_decision, connection_closed, request_received, body_chunk, ...' — placing connection_closed third instead of last. DR-012, §4.10 (the hooks.md phase table), and hook_phase.hpp all define connection_closed as the eleventh and final phase in firing order.
   *Recommendation:* Reorder the parenthetical to match the canonical firing order: connection_opened, accept_decision, request_received, body_chunk, route_resolved, before_handler, handler_exception, after_handler, response_sent, request_completed, connection_closed.

2. [ ] **architecture-alignment-checker** | `README.md:1535` | interface-contract
   The README 'Phases' table rows are presented in the same non-canonical order (connection_closed appears at row 3, before request_received). The canonical firing order defined in DR-012/§4.10 and reflected in hook_phase.hpp places connection_closed last (after request_completed).
   *Recommendation:* Reorder the table rows to match canonical firing order: connection_opened, accept_decision, request_received, body_chunk, route_resolved, before_handler, handler_exception, after_handler, response_sent, request_completed, connection_closed.

3. [ ] **code-quality-reviewer** | `test/integ/threadsafety_stress.cpp:105` | readability
   The HookBag struct comment says 'shared_ptr to keep the test mutator obvious in handler captures' but HookBag is passed and used everywhere as a raw pointer (HookBag*) to a stack-allocated struct. There is no shared_ptr in the implementation. The comment describes a design that was not adopted.
   *Recommendation:* Replace the comment with accurate wording, e.g. 'Raw pointer (passed to driver_body) to a stack-allocated bag; lifetime is the test body, so no ownership transfer is needed.'

4. [ ] **code-quality-reviewer** | `examples/per_route_auth.cpp:83` | code-readability
   The hook lambda at line 83 guards against ctx.request == nullptr and returns hook_action::pass() in that branch. The hook_context.hpp Doxygen for before_handler_ctx does not say request can be null for normal dispatches; the null guard looks defensive but may mislead readers into thinking null is a plausible runtime case when calling add_hook on a resource that is always dispatched after route resolution.
   *Recommendation:* Add a one-line comment explaining the null check: either 'belt-and-braces; request should never be null at before_handler on a matched route' or remove the guard and add an assertion, depending on whether null is truly possible here.

5. [ ] **code-quality-reviewer** | `test/bench_hook_overhead.cpp:119` | code-readability
   median_of_sorted and p99_of_sorted both take the vector by non-const reference even though they only read it (after the sort). The sort in median_of_sorted is a side-effect that destroys the original ordering; p99_of_sorted relies on this side-effect via the comment 'v already sorted'. This implicit ordering dependency between two functions violates the clean-code principle that functions should have no hidden side effects on their arguments.
   *Recommendation:* Either (a) have median_of_sorted take a copy and sort internally while returning both median and p99 in a single pass, or (b) rename the function to sort_and_return_median to make the mutation explicit, and pass a pre-sorted reference to p99_of_sorted with a clearer API contract.

6. [ ] **code-quality-reviewer** | `src/httpserver/hook_context.hpp:124` | code-readability
   The accept_ctx struct has both a Doxygen /** @brief ... */ block AND a trailing block of inline // comments (lines 124-138) that repeat much of the same information in a different style. The two documentation styles are inconsistent with every other context struct in the file, which uses only the /** */ Doxygen form.
   *Recommendation:* Consolidate the inline // TASK-046 comment block into the Doxygen /** */ block above accept_ctx, matching the style of request_completed_ctx.

7. [ ] **code-quality-reviewer** | `src/httpserver/hook_context.hpp:168` | code-readability
   body_chunk_ctx has the same mixed-documentation pattern: a Doxygen /** */ block followed by a lengthy // comment block. This is repeated for response_sent_ctx (lines 271-293) and request_completed_ctx (lines 308-334). The // blocks contain important detail (lifetime constraints, null conditions, the 'offset' counter semantics) that should be in the @brief / @param sections of the Doxygen block instead.
   *Recommendation:* Migrate the detail from the // comment blocks into the respective Doxygen /** */ blocks as @note, @param, or @attention tags.

8. [ ] **code-simplifier** | `test/bench_hook_overhead.cpp:119` | naming
   `median_of_sorted` sorts its argument in place (a side effect) but is named as if it only reads. The caller relies on this mutation before calling `p99_of_sorted`, creating an invisible ordering dependency. The modulo in `install_random_hook`'s switch covers 11 phases; if a phase is added later the modulo constant must be kept in sync manually.
   *Recommendation:* Rename to `sort_and_median` to make the mutation explicit, or fold both statistics into a single helper that sorts once and returns a struct `{ double median; double p99; double min; double max; }`.

9. [ ] **code-simplifier** | `test/integ/threadsafety_stress.cpp:127` | code-structure
   `install_random_hook`'s switch covers 11 phases (case 0..9 + default) using `phase_idx % 11u`, but the modulo and the case count must be kept in sync manually. If a phase is added later the modulo is the only thing that changes, which is easy to miss.
   *Recommendation:* Add a static_assert or a comment explicitly tying the modulo constant to the number of hook_phase enumerators, so a future phase addition triggers a visible reminder to update both.

10. [ ] **code-simplifier** | `test/integ/threadsafety_stress.cpp:114` | code-structure
    The `noop_resource::render_get` body splits its `return` statement across three lines with an awkward line break after `return` and before `ht::http_response::string("ok")`. The surrounding codebase uses single-line returns for trivial bodies.
    *Recommendation:* Collapse to a single line: `return ht::http_response::string("ok");`

11. [ ] **performance-reviewer** | `test/bench_hook_overhead.cpp:127` | algorithmic-complexity
    median_of_sorted sorts the vector in-place and p99_of_sorted then relies on it already being sorted, but the call to std::min_element / std::max_element happens BEFORE the sort in measure_gate_cost. p99_of_sorted silently assumes the vector is sorted when called — there is no assertion or documentation guarding this contract inside the function itself.
    *Recommendation:* Add a comment inside p99_of_sorted (e.g. `// precondition: v is sorted ascending`) or an assert(std::is_sorted(v.begin(), v.end())) under a debug flag.

12. [ ] **performance-reviewer** | `test/integ/threadsafety_stress.cpp:254` | algorithmic-complexity
    When the HookBag reaches capacity (kCap=256), the cap-overflow path calls erase(begin()) on a std::vector, which is O(n). With kCap=256 the cost is negligible but the pattern would degrade if kCap were raised.
    *Recommendation:* Document the O(n) cost in the comment so future maintainers who raise kCap understand the trade-off, or switch to std::deque<hook_handle> which provides O(1) pop_front.

13. [ ] **security-reviewer** | `examples/per_route_auth.cpp:87` | insecure-design
    HTTP Basic authentication example transmits credentials over plain HTTP (port 8080, no TLS configured). The example does not warn in-code that Basic auth over unencrypted HTTP exposes credentials in plaintext on the wire. The SECURITY NOTE comment correctly warns against hardcoding credentials but says nothing about transport security (CWE-523).
    *Recommendation:* Add a brief in-source caveat: '// TRANSPORT NOTE: HTTP Basic auth sends credentials base64-encoded but NOT encrypted. // In production, serve only over TLS (use create_webserver().use_ssl(...)).'

14. [ ] **security-reviewer** | `examples/per_route_auth.cpp:89` | authentication
    Credential comparison uses `std::string_view::operator==`, which short-circuits on the first differing byte, creating a timing side-channel (CWE-208). For a demonstration example the risk is low, but the pattern may be copied into real auth code without the timing-safe caveat.
    *Recommendation:* Add a comment noting that constant-time comparison is required in production (e.g., using `CRYPTO_memcmp`), or replace the comparison with a simple constant-time helper to model the correct pattern directly.

15. [ ] **security-reviewer** | `examples/per_route_auth.cpp:84` | insecure-design
    The hook callback returns `hook_action::pass()` when `ctx.request == nullptr` (fail-open pattern, CWE-636). In practice the before_handler context should never have a null request pointer, but treating a null pointer as permission-granted pass-through is a dangerous precedent.
    *Recommendation:* Fail closed on unexpected null: return `hook_action::respond_with(http_response::unauthorized(...))` when `ctx.request` is null.

16. [ ] **spec-alignment-checker** | `test/bench_hook_overhead.cpp:117` | acceptance-criteria
    The TASK action item states the CI gate for (a) is 'within 2x microbench noise of the pre-hook-system baseline'. The implementation uses an absolute fixed ceiling of kMaxGateNsPerCall = 50.0 ns. There is no stored HOOK_BASELINE_NS constant; the comment at line 48 mentions 'within 2x of HOOK_BASELINE_NS' but that constant does not exist.
    *Recommendation:* Either introduce a compile-time HOOK_BASELINE_NS constant and gate at 2x that value, or update the comment to accurately say the gate is a fixed 50 ns ceiling. The current ceiling is generous and correct in spirit but differs from the specified 2x-relative gate.

17. [ ] **spec-alignment-checker** | `test/bench_hook_overhead.cpp:57` | acceptance-criteria
    bench_hook_overhead is wired under EXTRA_PROGRAMS / make bench only, not make check. Automated CI running make check will not execute the benchmark gate.
    *Recommendation:* Consistent with bench_sizeof_http_resource and bench_get_headers (same EXTRA_PROGRAMS treatment). If CI enforcement is desired, add bench_hook_overhead to check_PROGRAMS with the existing sanitizer-skip guard. Otherwise update the acceptance criterion wording to say 'passes gate when run under make bench'.

18. [ ] **test-quality-reviewer** | `test/integ/threadsafety_stress.cpp:268` | implementation-coupling
    Case 5 (hook remove) calls hooks->handles.back().remove() followed by pop_back(). If remove() throws, pop_back is skipped and the handle stays in the bag, but the hook is already deregistered — the subsequent destructor call would hit the already-removed guard.
    *Recommendation:* Mirror the case-4 recycle pattern: move the back handle into a local, pop_back first, then call remove() on the local.

19. [ ] **test-quality-reviewer** | `test/bench_hook_overhead.cpp:127` | naming-convention
    median_of_sorted and p99_of_sorted mutate (sort) their vector argument but the parameter is a non-const reference. The sort-in-place side-effect is why min/max must be captured before calling these functions; the parameter name and signature do not communicate the mutation.
    *Recommendation:* Either rename the parameter to signal mutation (e.g. samples_inout) or add a brief comment in the function signature noting the sort-in-place contract.

20. [ ] **test-quality-reviewer** | `test/integ/threadsafety_stress.cpp:380` | missing-test
    The final assertions check only combined sums (hook_add_ok + hook_remove_ok > 0) rather than each counter individually. A subtle bug silently suppressing one of the two ops would be masked by the combined-sum check.
    *Recommendation:* Split into two separate checks: `LT_CHECK_GT(counters.hook_add_ok.load(), 0)` and `LT_CHECK_GT(counters.hook_remove_ok.load(), 0)`.

21. [ ] **test-quality-reviewer** | `test/bench_hook_overhead.cpp:172` | excessive-setup
    OUTER=11 rounds (later increased to 51) × INNER=1,000,000 iterations with only 10,000 warmup iterations may be proportionally under-warmed (0.09% warmup of total). The warmup is insufficient to reliably pre-train branch predictors on some hosts.
    *Recommendation:* Consider increasing warmup to 100,000 iterations or discarding the first 2 outer rounds from the sample. At minimum, add a comment explaining why 10,000 warmup iterations are sufficient relative to the OUTER/INNER parameters.
