# Unworked Review Issues

**Run:** 2026-05-30 23:50:01
**Task:** TASK-057
**Total:** 26 (0 critical, 1 major, 25 minor)

## Major

1. [x] **code-simplifier** | `src/http_request.cpp:430` | code-structure
   operator<< has four symmetric if/else blocks over `expose`. Each pair (user/pass, headers, footers, cookies) duplicates the same branch pattern. The two header/footer cases are structurally identical: the only difference between the redacted and non-redacted path is which helper is called. This creates four call sites to maintain instead of two (one dispatch table or one lambda per section).
   *Recommendation:* Extract a single inline dispatcher: `auto dump_headers = expose ? http::dump_header_map : dump_header_map_redacted; auto dump_cookies = expose ? http::dump_header_map : dump_cookie_map_redacted;` then collapse the if/else pairs to unconditional calls. For the pass field: `os << "pass:\"" << (expose ? r.get_pass() : kRedacted) << "\"";` (ternary). This removes the four branching blocks and makes the redaction policy a one-liner per field.
   *Fixed:* Collapsed the four if/else blocks into a function-pointer dispatch (`header_dumper`) selected once per call, plus a ternary for the pass field. Since `http::dump_header_map` is overloaded for both `header_view_map` and `header_map`, the dispatcher is statically cast to the `header_view_map` signature to disambiguate.

## Minor

2. [ ] **architecture-alignment-checker** | `specs/architecture/:null` | adr-violation
   The expose_credentials_in_logs flag has no corresponding update to the architecture docs. The expose_exception_messages precedent (DR-009 Revision 1) was formally documented in §5.2 cross-cutting, the create-webserver component spec (§4.9), and the decision record itself. TASK-057 adds an analogous security-opt-in flag that is present in the Doxygen and task spec but absent from specs/architecture/05-cross-cutting.md, specs/architecture/04-components/create-webserver.md, specs/architecture/04-components/http-request.md, and has no new or revised ADR entry. The unworked_review_issues file 2026-05-29_183434_task-055.md already flagged the same gap for expose_exception_messages in component docs, and TASK-057 repeats the pattern without closing it.
   *Recommendation:* Add a sentence to specs/architecture/05-cross-cutting.md under a 'Security opt-in flags' note (or extend §5.2's error-propagation section with a parallel §5.x for diagnostic redaction) documenting that http_request::operator<< redacts credential fields by default (CWE-312/CWE-532) and that create_webserver::expose_credentials_in_logs(true) restores the verbose form for development. Optionally add a short note to the create-webserver (§4.9) and http-request (§4.2) component specs mirroring the expose_exception_messages treatment in DR-009.

3. [ ] **architecture-alignment-checker** | `src/httpserver/http_request.hpp:531` | pattern-violation
   set_expose_credentials_in_logs(bool v) is declared in the private section of the installed public header src/httpserver/http_request.hpp without an HTTPSERVER_COMPILATION gate. While the private access specifier prevents external call, it is visible in the installed header to any consumer reading the API surface. Other internal-only mutators in the same private section (set_arg, set_content, set_path, etc.) follow the same convention without gating, so this is self-consistent within the class. However, the analogous propagation path for expose_exception_messages does not need a similar setter because webserver_impl accesses webserver::expose_exception_messages directly as a const member. The asymmetry — a private setter on http_request vs direct field access on webserver — is not a violation but is worth noting for future maintainers.
   *Recommendation:* Consider whether the expose_credentials_in_logs_ field on http_request_impl could be set via the existing friend access path (webserver_impl is already a friend of http_request via detail::webserver_impl friendship) rather than through a named setter, which would bring the pattern in line with how expose_exception_messages is consumed. This is a minor stylistic alignment, not a correctness issue.

4. [ ] **code-quality-reviewer** | `RELEASE_NOTES.md:221` | readability
   The sentence 'this flag is intended for development only and must not be set in production deployments' is advisory prose rather than a machine-checkable constraint, which matches the style of the analogous expose_exception_messages paragraph. No action required, but a future improvement could link to a DR or security note for traceability.
   *Recommendation:* Optionally append a parenthetical reference such as '(see DR-009)' or create a dedicated DR for the credential-redaction policy so the rationale is traceable from the release notes.

5. [ ] **code-quality-reviewer** | `src/http_request.cpp:375` | code-elegance
   is_authorization_header_key() reinvents a case-insensitive comparison (inline lambda `ieq`) that the project's existing `http::header_comparator` already provides. Because `header_view_map` uses `header_comparator` as its comparator, iterating over a `header_view_map` and calling `is_authorization_header_key(kv.first)` is a double case-fold — the map already stores keys in canonical casing order, but the comparison lambda tolowers again. The function is also inlined into the anonymous namespace of http_request.cpp and cannot be reused or unit-tested independently.
   *Recommendation:* Replace the inline lambda with a comparison against two `constexpr std::string_view` constants using the project's existing `http::header_comparator{}` functor, or simply use the map's `find()` with `header_comparator`'s `is_transparent` lookup. This removes the bespoke tolower loop and ties the redaction logic to the same comparator the map itself uses, making it impossible for them to diverge.

6. [ ] **code-quality-reviewer** | `src/http_request.cpp:399` | code-readability
   dump_header_map_redacted() and dump_cookie_map_redacted() accept `prefix` by `const std::string&`, but every call site passes a string literal. The project's existing `http::dump_header_map()` signature (defined in http_utils.hpp) uses `const std::string&` for the same parameter, so this is consistent with current convention, but accepting `std::string_view` would avoid a string construction on every call since the prefix is always a compile-time constant.
   *Recommendation:* Change the `prefix` parameter type to `std::string_view` in both helper functions, matching what modern C++ style recommends for read-only string parameters. This is purely a style/micro-perf alignment; the impact is negligible but it aligns with the broader migration toward `std::string_view` visible throughout the request API.

7. [ ] **code-quality-reviewer** | `test/unit/http_request_operator_stream_test.cpp:49` | test-coverage
   The test suite covers the default-redaction and opt-in-expose paths but does not include a case where no credentials are present (no `user`, no `pass`, no Authorization header, no cookies). This edge case verifies that the redaction path still emits a well-formed `pass:"<redacted>"` token rather than `pass:""` when Basic-auth is absent on a HAVE_BAUTH-off build, and that the dump does not accidentally emit the `<redacted>` token in headers/cookies sections that are simply empty. It also ensures that the no-cookie path is exercised without the redacted cookie section appearing at all (per the `if (map.empty()) return;` guard in dump_cookie_map_redacted).
   *Recommendation:* Add a third test case `operator_stream_no_credentials` that streams a minimal request (method, path, no auth, no cookies) and asserts that (a) the output contains `pass:"<redacted>"`, (b) no `<redacted>` appears inside a Cookies section, and (c) `hunter2` or any other placeholder secret does not appear.

8. [ ] **code-simplifier** | `src/create_test_request.cpp:109` | code-structure
   The propagation of expose_credentials_in_logs goes through a public setter (req.set_expose_credentials_in_logs) in create_test_request::build(), while other impl fields (headers_local, requestor_ip, etc.) are set by direct member access via impl_. The indirection through the setter is consistent with encapsulation but is inconsistent with the direct-access pattern used for every other field in the same function.
   *Recommendation:* Either make all impl-field assignments in build() go through the setter layer (preferred for consistency) or set expose_credentials_in_logs_ directly alongside the other impl_ member writes: `req.impl_->expose_credentials_in_logs_ = _expose_credentials_in_logs;`. Pick one pattern and apply it uniformly.

9. [ ] **code-simplifier** | `src/http_request.cpp:375` | code-structure
   is_authorization_header_key performs a manual case-insensitive comparison via a local lambda `ieq`. The lambda is defined inside the function, captures nothing, and is used only twice. The same case-insensitive matching idiom is used nowhere else in the new code, but the implementation is a hand-rolled loop rather than using std::equal with std::tolower, making it slightly harder to read at a glance.
   *Recommendation:* Consider hoisting the loop body into a small named static helper `static bool iequal(string_view a, string_view b)` shared between the two sites, or using std::equal with a case-fold comparator. Either form is more idiomatic and avoids the nested-lambda style.

10. [ ] **code-simplifier** | `src/http_request.cpp:399` | naming
   dump_header_map_redacted and dump_cookie_map_redacted have the same function signature and almost identical bodies: the only difference is that dump_header_map_redacted calls is_authorization_header_key to decide whether to redact, while dump_cookie_map_redacted redacts unconditionally. The duplication of the loop scaffolding (prefix print, bracket open/close, endl) is minor but real.
   *Recommendation:* Unify into a single helper that takes a callable `ShouldRedact` predicate: `void dump_headerlike_map_redacted(ostream&, string_view prefix, const header_view_map&, Predicate should_redact)`. Cookie call site passes a lambda returning `true`; header call site passes `is_authorization_header_key`. This collapses ~27 lines to ~14 with no behaviour change.

11. [ ] **code-simplifier** | `src/http_request.cpp:399` | code-structure
   dump_header_map_redacted and dump_cookie_map_redacted share identical stream-output boilerplate (empty-guard, prefix line, per-entry key/quote/close, closing bracket). Only the value expression differs: one selects between kRedacted and kv.second, the other always emits kRedacted. The duplication will drift if the diagnostic format changes.
   *Recommendation:* Extract the shared loop into a single helper that accepts a callable to resolve the value, e.g.: `void dump_map_redacted(std::ostream& os, const std::string& prefix, const http::header_view_map& map, auto value_for)`. Then: `dump_header_map_redacted` calls it with `[](auto& kv){ return is_authorization_header_key(kv.first) ? kRedacted : kv.second; }` and `dump_cookie_map_redacted` calls it with `[](auto&){ return kRedacted; }`. This collapses ~26 lines to ~14 and ensures both functions stay in sync if the diagnostic format ever changes.

12. [ ] **code-simplifier** | `src/http_request.cpp:433` | code-structure
   The pass-field branch in operator<< repeats the surrounding `user:"..."` prefix and the closing `"` twice — once for the expose path and once for the redact path. Only the value differs, so the branching is wider than necessary.
   *Recommendation:* Hoist the value into a variable: `const std::string_view pass_out = expose ? std::string_view(r.get_pass()) : kRedacted;` then emit `os << "user:\"" << r.get_user() << "\" pass:\"" << pass_out << "\"";` once. Reduces the branch to the minimum differing part and makes it easier to see that the user field is unconditional.

13. [ ] **code-simplifier** | `src/httpserver/detail/http_request_impl.hpp:253` | code-structure
   The expose_credentials_in_logs_ field is a plain bool with a Doxygen block but no #ifdef guard, even though the fields it affects (password, username, digested_user) are gated on HAVE_BAUTH / HAVE_DAUTH. The flag is always stored and always read by operator<<, but on a HAVE_BAUTH-off build get_pass() returns an empty sentinel, so setting the flag to true has no visible effect on the pass field. This is fine behaviourally but the comment about 'four credential surfaces' is inaccurate on auth-off builds.
   *Recommendation:* Update the comment to clarify that on HAVE_BAUTH-off / HAVE_DAUTH-off builds some credential surfaces (pass, digested-user) are absent by design, so expose_credentials_in_logs(true) only affects Authorization headers and cookies on those configurations. No code change required.

14. [ ] **housekeeper** | `/Users/etr/progs/libhttpserver/specs/architecture/04-components/create-webserver.md:25` | architecture-not-updated
   The create-webserver component doc lists the auth_handler_ptr shape (TASK-054) and references PRD-CFG-REQ-001..004, but does not mention the expose_credentials_in_logs(bool) builder setter added by TASK-057. For consistency with the expose_exception_messages pattern (which is described in RELEASE_NOTES.md and referenced in the architecture), the new security-relevant builder flag deserves at least a one-line reference in the component doc. The observability section (10-observability.md) also says nothing about the redaction policy on operator<< diagnostic dumps.
   *Recommendation:* Add a short note to specs/architecture/04-components/create-webserver.md documenting expose_credentials_in_logs(bool) (analogous to how expose_exception_messages is referenced). Optionally extend specs/architecture/10-observability.md with a sentence on credential-redaction in diagnostic output. Run /groundwork:source-architecture-from-code to capture these changes.

15. [ ] **performance-reviewer** | `src/http_request.cpp:381` | missing-caching
   The `ieq` lambda is re-defined on every call to `is_authorization_header_key`. In the default (redacting) path `dump_header_map_redacted` calls `is_authorization_header_key` once per header entry. The lambda is trivially inlined by any optimising compiler and carries zero allocation cost, so this is purely a style observation on the code shape — not a measurable issue on a diagnostic path.
   *Recommendation:* No action required. If the function is ever moved to a hot path, hoisting the lambda to a file-scope static or converting it to a plain `strncasecmp`-style helper would clarify intent. For the current diagnostic use, the current form is fine.

16. [ ] **performance-reviewer** | `src/http_request.cpp:399` | memory-allocation
   Both `dump_header_map_redacted` and `dump_cookie_map_redacted` accept `prefix` as `const std::string&`, matching the existing `http::dump_header_map` signatures. This is consistent but means call-sites that pass string literals (`"Headers"`, `"Footers"`, `"Cookies"`) will construct a temporary `std::string` on each call. In small-string-optimisation (SSO) builds this is stack-only and cost-free; it is included only for completeness.
   *Recommendation:* No action needed for a diagnostic path. If the existing `http::dump_header_map` signatures are ever modernised to `std::string_view`, the new helpers should follow suit for consistency.

17. [ ] **security-reviewer** | `src/detail/webserver_body_pipeline.cpp:200` | A09:Logging Failures
   Under #ifdef DEBUG, std::cout streams the raw upload_data body bytes verbatim. If a client posts credentials in a form body (application/x-www-form-urlencoded with a `password=` field) or sends a raw Basic-auth body, those bytes will appear in stdout in debug builds. This is a pre-existing issue not introduced by TASK-057, but it is directly relevant to the task's credential-redaction scope.
   *Recommendation:* Consider redacting or suppressing this debug emit, or at minimum add a comment noting it is unsafe for production builds and must remain guarded by #ifdef DEBUG / NDEBUG. The current guard already prevents release builds from leaking, but the comment should explicitly call out the credential-body risk so future reviewers do not accidentally widen the scope.

18. [ ] **security-reviewer** | `src/httpserver/create_test_request.hpp:127` | A09:Logging Failures
   The expose_credentials_in_logs(bool enable = true) builder setter uses a default argument of true, meaning expose_credentials_in_logs() (no argument) enables credential exposure. This is intentional for ergonomic test code but is asymmetric with the webserver-side setter which also defaults to true. Both are opt-in (default-false field), so the security property holds, but a developer who copies the call pattern without reading documentation may enable exposure unintentionally. No code path defaults the flag to true.
   *Recommendation:* No change required for security correctness (the field default is false). Consider adding an inline comment on the declaration noting that the no-argument form is shorthand for enable=true and must not appear outside test scope, to reinforce the development-only intent.

19. [ ] **security-reviewer** | `src/httpserver/http_request.hpp:554` | A09:Logging Failures
   The Doxygen block on operator<< explicitly states 'Query-string arguments are streamed verbatim. Callers that put credential material in query parameters (?token=...) should sanitize before constructing the request URL; see the warning in the class-level block'. This is a deliberate design decision, but the warning is referenced to a different location (the class-level block of http_request) rather than being visible inline on the operator<< declaration. A user reading only the operator<< docs may not follow the cross-reference and inadvertently log token= / api_key= parameters.
   *Recommendation:* Repeat the warning inline in the @warning section of the operator<< Doxygen block: 'Query-string arguments including any ?token= or ?api_key= parameters are emitted verbatim regardless of the expose_credentials_in_logs flag; avoid routing credential material through query strings, or strip them before the request reaches the logging path.'

20. [ ] **spec-alignment-checker** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-057/RELEASE_NOTES.md:214` | specification-gap
   RELEASE_NOTES.md states that v1 emitted 'digested_pass:"<plaintext>"' into the operator<< stream. Inspection of the pre-TASK-057 operator<< (git show 371b7bc:src/http_request.cpp) shows it only ever emitted the Basic-auth 'pass' field — there was never a 'digested_pass' field in the diagnostic dump. The RELEASE_NOTES entry slightly overstates the v1 behaviour.
   *Recommendation:* Remove 'digested_pass:"<plaintext>",' from the RELEASE_NOTES description of what v1 emitted, or qualify it as 'not present in the v1 operator<< output'. This is informational documentation only and does not affect the security fix.

21. [ ] **spec-alignment-checker** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-057/specs/tasks/v2-deferred-backlog-plan.md:289` | specification-gap
   Action item 1 says 'Same treatment for digested_pass and any other authentication secret on the stream.' The pre-TASK-057 operator<< never emitted a digested_pass field, so there is nothing to redact. The implementation correctly handles digest credentials via the case-insensitive Authorization/Proxy-Authorization header redaction in dump_header_map_redacted. The action item text is ambiguous but the implementation resolves the ambiguity correctly by protecting the Authorization header that carries digest challenge responses.
   *Recommendation:* No code change needed. The action item checkbox is correctly marked done. For future clarity the spec could note that digested_pass was never a separate emit field and that digest credential protection is achieved via the Authorization header redaction.

22. [ ] **spec-alignment-checker** | `src/http_request.cpp:null` | specification-gap
   The task action item says 'Same treatment for digested_pass and any other authentication secret on the stream.' The field digested_pass does not exist anywhere in the codebase (confirmed by exhaustive grep), and the original operator<< at 371b7bc never emitted it. There is therefore nothing to redact. The implementation is correct as-is, but the task spec's mention of digested_pass appears to be a speculative/stale reference that was never reconciled with the actual codebase shape.
   *Recommendation:* No code change needed. Update the TASK-057 entry in v2-deferred-backlog-plan.md to remove the 'digested_pass' clause, or note that it was evaluated and confirmed absent, so the redaction surface is limited to: pass, Authorization/Proxy-Authorization header values, and cookie values.

23. [ ] **spec-alignment-checker** | `test/unit/http_request_operator_stream_test.cpp:39` | acceptance-criteria
   The task spec names the required test 'http_request_test::operator_stream_redacts_credentials'. The implementation uses the suite name 'http_request_operator_stream_suite', giving a fully-qualified name of 'http_request_operator_stream_suite::operator_stream_redacts_credentials'. The test name 'operator_stream_redacts_credentials' matches exactly; only the suite name differs. Every other unit test in the project follows the <domain>_suite convention (e.g. create_webserver_suite, auth_handler_optional_signature_suite), so the implementation is consistent with project convention. The required test behavior is fully covered.
   *Recommendation:* No code change needed. The task spec's 'http_request_test' was likely a freeform description, not a binding suite name. Optionally update the task spec to read 'http_request_operator_stream_suite::operator_stream_redacts_credentials' for precision.

24. [ ] **test-quality-reviewer** | `test/unit/http_request_operator_stream_test.cpp:105` | implementation-coupling
   The assertion on line 105 embeds a nested-quote literal `"Digest username=\"admin\", response=\"deadbeef\""` inside the expected output string. The outer stream format uses `key:"value"`, so the expected substring contains an unescaped double-quote mid-value: `Proxy-Authorization:"Digest username="admin", response="deadbeef""`. This is checking the exact serialisation format of the operator<< implementation rather than the observable security property (that the plaintext digest challenge is present). If the serialisation quoting strategy changes (e.g. escaping embedded quotes) the test will fail even though the security behaviour is unchanged.
   *Recommendation:* Split the assertion into two independent observable properties: (1) `out.find("Proxy-Authorization:") != npos` to confirm the key is present, and (2) `out.find("deadbeef") != npos` to confirm the actual credential payload is streamed verbatim when expose_credentials_in_logs is set. This avoids coupling to the exact quoting format while still confirming the credential is present.

25. [ ] **test-quality-reviewer** | `test/unit/http_request_operator_stream_test.cpp:49` | missing-test
   The default-redaction test (operator_stream_redacts_credentials) does not verify that query-string arguments are streamed verbatim (i.e. not accidentally redacted). The production code in http_request.cpp line 449 calls `http::dump_arg_map` unconditionally regardless of the expose flag. A test that adds a non-credential query arg and checks it appears in output would guard against an accidental over-redaction regression. This is a low-risk gap because query args are already excluded from the redaction branch, but a guard would improve protection against future changes to the operator<< body.
   *Recommendation:* Add `.arg("page", "2")` to the arrange phase of operator_stream_redacts_credentials and assert that the string `page` (or the formatted arg) is present in `out`. This is a quick one-liner that pins the 'query args are never redacted' invariant.

26. [ ] **test-quality-reviewer** | `test/unit/http_request_operator_stream_test.cpp:49` | missing-test
   There is no test exercising the Footer redaction path. The production implementation in http_request.cpp (lines 446-447) calls `dump_header_map_redacted` for Footers as well, but neither test adds a `Proxy-Authorization` or `Authorization` entry via the `footer()` builder setter to confirm footers containing authorization-class keys are also redacted. Given that the footers and headers share the same redaction helper, this is a low risk gap, but it is a distinct code path from headers.
   *Recommendation:* Add a small dedicated test, or extend operator_stream_redacts_credentials with `.footer("Authorization", "Bearer footertoken")` and assert that `footertoken` does not appear in `out`. This is particularly relevant because the class-level doc block for operator<< explicitly mentions footers as a redaction surface.
