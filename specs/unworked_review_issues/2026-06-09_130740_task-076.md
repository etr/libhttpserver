# Unworked Review Issues

**Run:** 2026-06-09 13:07:40
**Task:** TASK-076
**Total:** 28 (0 critical, 2 major, 26 minor)

## Major

1. [x] **code-simplifier** | `test/integ/ws_start_stop.cpp:760` | patterns
   LT_SKIP_IF(true, ...) is always-true conditional skip used in catch blocks across many TLS tests (ipv6_webserver, dual_stack_webserver, bind_address_ipv6_string, https_webserver, tls_session_getters, client_cert_no_certificate, client_cert_with_certificate, client_cert_dn_extraction, client_cert_fingerprint, client_cert_no_cn, client_cert_untrusted, client_cert_accessors_known_values, sni_callback_setup). Because the catch block is only entered when an exception was thrown, the condition is always true — LT_SKIP_IF(true, reason) is exactly equivalent to LT_SKIP(reason) and the boolean literal obscures that equivalence.
   *Recommendation:* Replace every catch-block occurrence of `LT_SKIP_IF(true, ...)` with `LT_SKIP(...)`. The macro already captures __FILE__/__LINE__ and the intent ('we caught an exception, skip the test') is clearer without the redundant true literal. Example: replace `LT_SKIP_IF(true, std::string("TLS start failed: ") + e.what())` with `LT_SKIP(std::string("TLS start failed: ") + e.what())`.
   *Worked 2026-07-07 (chore/unworked-major-issues):* All 14 catch-block LT_SKIP_IF(true, ...) occurrences converted to LT_SKIP(...); the 13 genuinely conditional LT_SKIP_IF sites left as-is.

2. [x] **code-simplifier** | `test/integ/ws_start_stop.cpp:912` | patterns
   Duplicated curl setup scaffolding (curl_global_init, curl_easy_init, four curl_easy_setopt calls for URL/HTTPGET/WRITEFUNC/WRITEDATA, curl_easy_perform, curl_easy_cleanup) appears verbatim in at least 25 test functions. This is pure copy-paste: any change to the scaffolding (e.g. adding a timeout option) requires hunting all sites. The pattern also intermixes test-specific URL/response variables with boilerplate, making each function longer than necessary.
   *Recommendation:* Extract a small helper — either a free function or a struct — that owns the curl lifecycle and accepts the url and a response-string reference. A simple signature like `CURLcode curl_get(const std::string& url, std::string& out, bool verify_ssl = true)` would replace the ~8 lines of boilerplate at each of the ~25 call sites. SSL-specific tests can pass `verify_ssl = false`. This reduces each TLS test body by roughly 40% and makes the actual assertion (`LT_CHECK_EQ(s, "OK")`) the visually dominant line.
   *Worked 2026-07-07 (chore/unworked-major-issues):* curl_get(url, out, verify_ssl) helper added; 37 plain-GET/TLS sites converted; 10 genuinely divergent handles (IPRESOLVE, CUSTOMREQUEST, live-handle getinfo, mTLS cert options) intentionally left inline.
## Minor

3. [ ] **architecture-alignment-checker** | `test/integ/ws_start_stop.cpp:760` | pattern-violation
   LT_SKIP_IF(true, ...) is used in 13 catch blocks as an unconditional skip. The condition is always `true`, which means the macro's conditional evaluation adds no information — it is effectively a more verbose LT_SKIP(). The architecture comment in littletest.hpp (line 301) says LT_SKIP_IF is for 'when an environment-conditional setup step fails for an expected, non-broken reason', implying the condition should be a real predicate. Using `true` as the condition is a minor style inconsistency; it does not affect correctness because the exception-catching context means the condition semantically is always true when reached.
   *Recommendation:* Replace `LT_SKIP_IF(true, reason)` with the unconditional `LT_SKIP(reason)` in all 13 catch-block sites. This better expresses intent: control only reaches the catch body when the exception fired, which is inherently always-true. Using LT_SKIP directly also matches how the five psk_connection_* tests correctly use `LT_SKIP_IF(!has_gnutls_cli(), ...)` with a real predicate.

4. [ ] **architecture-alignment-checker** | `test/littletest.hpp:97` | pattern-violation
   The `SKIP` constant (#define SKIP 3) is defined alongside WARN/CHECK/ASSERT, but `LT_SWITCH_MODE` does not handle the `case(SKIP):` branch. This is intentional — LT_SKIP bypasses LT_SWITCH_MODE entirely by throwing skip_unattended directly — but the gap between the SKIP constant definition and the switch's coverage creates a latent confusion risk for maintainers who may attempt to pass SKIP as a mode argument to LT_SIMPLE_EV or similar internal macros. If `SKIP` were accidentally passed as __lt_mode__ to LT_SWITCH_MODE, the switch would fall through silently, recording a success instead of a skip.
   *Recommendation:* Add a comment on the `#define SKIP 3` line (or on LT_SWITCH_MODE) making explicit that SKIP is NOT a valid mode argument to LT_SWITCH_MODE and that it has a separate code path via LT_SKIP/skip_unattended. Alternatively add `case(SKIP): throw littletest::skip_unattended(__lt_ss__.str());` to LT_SWITCH_MODE as a defensive guard, which would make the constant self-consistent even if accidentally misused.

5. [ ] **code-quality-reviewer** | `test/integ/ws_start_stop.cpp:1676` | code-readability
   The comment block explaining why cmd_exit != 127 is the chosen assertion for psk_connection_success is 8 lines long and detailed but the core invariant (the gnutls-cli binary must be invokable) can be expressed more concisely. The comment also repeats information already present in the LT_SKIP_IF gate at line 1648.
   *Recommendation:* Trim the comment to 2-3 lines: state the assertion target and note that 127 means command-not-found. The probe/exec race edge-case explanation can be dropped — it adds complexity without guiding a future reader toward a fix.

6. [ ] **code-quality-reviewer** | `test/integ/ws_start_stop.cpp:760` | code-readability
   LT_SKIP_IF(true, ...) is used inside catch blocks where the condition is always true (the exception was caught). Using LT_SKIP(reason) directly would be cleaner and more honest — LT_SKIP_IF(true,...) looks like the condition was accidentally hardcoded rather than intentionally unconditional. This pattern appears 14 times across the TLS and IPv6 skip sites.
   *Recommendation:* Replace LT_SKIP_IF(true, reason) with LT_SKIP(reason) in all catch blocks where the skip is unconditional. Reserve LT_SKIP_IF for sites where the condition is genuinely runtime-variable (e.g. !has_gnutls_cli(), is_psk_unsupported_error(e)).

7. [ ] **code-quality-reviewer** | `test/integ/ws_start_stop.cpp:849` | code-elegance
   The bind_address_ipv6_string refactor uses a shared_ptr<webserver> to work around the non-movable type, which is reasonable. However, if the construction catch fires and LT_SKIP_IF(true,...) throws skip_unattended, control leaves the test body before line 857 (ws_ptr->register_path). This is safe but subtle — the null shared_ptr is never dereferenced only because skip_unattended is thrown first. A comment noting this invariant would prevent a future reader from adding code between the catch and line 857 that dereferences ws_ptr on a failure path.
   *Recommendation:* Add a one-line comment after the closing brace of the construction try-catch (line 855) stating that reaching this point guarantees ws_ptr is non-null, since LT_SKIP_IF throws on failure.

8. [ ] **code-quality-reviewer** | `test/unit/littletest_skip_semantics_test.cpp:45` | test-coverage
   Cycle 1.c claims to test LT_SKIP_IF(true,...) but the test body (observe_prior_skip_was_counted) only asserts the cumulative skip counter is >= 1, which is already satisfied by Cycle 1.a's LT_SKIP call. LT_SKIP_IF(true,...) is never actually invoked in the file, so its distinct code path (the do{ if(cond){ LT_SKIP(...); } }while(0) wrapper) is not directly exercised by this TU.
   *Recommendation:* Add a dedicated test case that calls LT_SKIP_IF(true, "intentional") and verifies via get_skips() that the skip counter incremented by exactly 1. This ensures the conditional dispatch in the macro is covered independently of the unconditional LT_SKIP path.

9. [ ] **code-simplifier** | `test/integ/ws_start_stop.cpp:1484` | naming
   The helper `is_psk_unsupported_error` performs a two-stage check: first it returns false if neither 'PSK' nor 'psk' appears, then it checks for one of four 'unavailability' tokens. The inner logic is split across a negated early-return and a positive return expression, making it slightly harder to read at a glance.
   *Recommendation:* Consolidate into a single expression: `bool has_psk = msg.find("PSK") != npos || msg.find("psk") != npos; bool has_unsupported = ...; return has_psk && has_unsupported;`. The two booleans with descriptive names make the intent immediately legible without reading the control flow.

10. [ ] **code-simplifier** | `test/integ/ws_start_stop.cpp:1533` | code-structure
   The psk_handler_setup, psk_handler_empty, and psk_no_handler tests share an identical tail block: `if (ws.is_running()) { ws.stop(); } LT_CHECK_EQ(ws.is_running(), false);`. The conditional stop followed by the is_running assertion is a three-line pattern repeated three times.
   *Recommendation:* The guard `if (ws.is_running()) { ws.stop(); }` can be simplified: calling `ws.stop()` on a non-running server is likely a no-op or idempotent. Verify that, then replace the conditional with an unconditional `ws.stop(); LT_CHECK_EQ(ws.is_running(), false);`. This removes one level of nesting and keeps the assertion visible.

11. [ ] **code-simplifier** | `test/integ/ws_start_stop.cpp:1562` | patterns
   Five PSK catch blocks (psk_handler_setup, psk_handler_empty, psk_no_handler, psk_connection_success, psk_connection_unknown_user, psk_connection_empty_handler, psk_connection_invalid_hex, psk_connection_no_handler) each contain an identical 4-line comment block followed by the same two lines of code: `LT_SKIP_IF(is_psk_unsupported_error(e), ...) + throw;`. The comment text is near-verbatim repeated 8 times ('TASK-076: see psk_handler_setup — typed SKIP on PSK-unsupported builds; re-throw to fail loudly otherwise.'). This is the Needless Repetition code smell.
   *Recommendation:* Replace the repeated comment with a single brief inline comment at each call site, e.g. `// PSK-unsupported builds: SKIP; implementation errors: re-throw`. The full rationale is already in the block comment above `is_psk_unsupported_error` at line 1493-1510 — cross-referencing once in psk_handler_setup is sufficient; the seven subsequent call sites need only the one-liner.

12. [ ] **code-simplifier** | `test/integ/ws_start_stop.cpp:769` | naming
   The local URL variable in `ipv6_webserver` is named `ipv6_url` while the analogous variable in `dual_stack_webserver` (line 803) is named `ds_url`. All other tests in the file use the plain name `url` for the single curl URL variable. Inconsistent naming within the same semantic slot makes the code harder to scan.
   *Recommendation:* Rename both `ipv6_url` and `ds_url` to `url` to match the naming convention used in every other test in the file. The variables are test-local and there is no ambiguity.

13. [ ] **code-simplifier** | `test/integ/ws_start_stop.cpp:853` | code-structure
   In `bind_address_ipv6_string`, after the construction try/catch emits `LT_SKIP_IF(true, ...)` and throws `skip_unattended`, the lines immediately below (857-861) dereference `ws_ptr` unconditionally. If the construction catch fires, `ws_ptr` remains null and the runner catches `skip_unattended` — so in practice the null deref is never reached. However, the code reads as though `ws_ptr` might be null when `ws_ptr->register_path` is called on line 857, which is misleading. The comment on line 845-848 acknowledges the design but the code structure does not make the invariant obvious.
   *Recommendation:* Add a null-guard assertion or early return after the construction try/catch to make the non-null invariant explicit: `if (!ws_ptr) return;` — or restructure so the registration and start are both inside the `try` block and only the start-failure path branches to a second catch. This makes the flow obvious without relying on a reader knowing that `LT_SKIP_IF(true,...)` always throws.

14. [ ] **code-simplifier** | `test/littletest.hpp:38` | code-structure
   The SKIP constant (value 3) is defined alongside WARN/CHECK/ASSERT but is never passed to LT_SWITCH_MODE — the switch in LT_SWITCH_MODE has no case for SKIP (3) and would simply fall through silently if it were passed. The comment on line 38 explains the architectural reason (skip_unattended is thrown directly from LT_SKIP, not via LT_SWITCH_MODE), but the constant itself is dead — no code path reads the value 3 from any variable.
   *Recommendation:* Either remove the `#define SKIP 3` constant since it is unused at the call-site level (LT_SKIP never calls LT_SWITCH_MODE), or add a comment immediately above it noting explicitly that it is a reserved/documentary value not passed to LT_SWITCH_MODE, to prevent a future contributor from wiring it in and hitting the silent fall-through. Removal is cleaner.

15. [ ] **code-simplifier** | `test/unit/littletest_skip_semantics_test.cpp:42` | code-structure
   LT_CHECK_EQ(1 + 1, 2) on line 42 is a compile-time constant expression — exactly the kind of tautological assertion the TASK-076 was designed to remove. Its stated intent is to verify that 'control returns from LT_SKIP_IF', but that is already demonstrated by the fact that the skip-counter assertions on lines 39-40 execute and pass. The arithmetic check adds no information.
   *Recommendation:* Remove line 42 (`LT_CHECK_EQ(1 + 1, 2);`). The two lines above it (before/after counter delta) already prove that LT_SKIP_IF(false,...) is a no-op and that control flows through; a constant-expression check contributes nothing and would itself be flagged by the tautological-assert gate if it were in ws_start_stop.cpp.

16. [ ] **housekeeper** | `specs/architecture/09-testing.md:null` | documentation-stale
   The architecture testing doc (09-testing.md) enumerates the project's seven named test surfaces but does not mention the littletest skip/exit-77 semantics added by TASK-076. The new LT_SKIP / LT_SKIP_IF contract and the tls-no-cli CI lane are material additions to the project's test infrastructure. The omission does not block any consumer, but the doc is now slightly stale relative to what the framework actually supports.
   *Recommendation:* Consider adding a brief entry to specs/architecture/09-testing.md noting the LT_SKIP / LT_SKIP_IF SKIP primitive and the exit-77 / Automake SKIP contract, and the tls-no-cli CI lane added in TASK-076. Run /groundwork:source-architecture-from-code to regenerate if preferred.

17. [ ] **performance-reviewer** | `test/integ/ws_start_stop.cpp:1485` | blocking-io
   has_gnutls_cli() calls system("which gnutls-cli > /dev/null 2>&1") — a blocking subprocess fork — every time it is invoked. It is called at the top of five consecutive test bodies (psk_connection_*), so the fork+exec is repeated five times sequentially during a single test binary run.
   *Recommendation:* Memoize the result with a file-scope static or a once-flag so the subprocess is spawned at most once per process: `static bool cached = (system(...) == 0); return cached;`. The five tests run in the same process so all subsequent calls hit the cache. This is a test-runtime micro-optimization; in CI it saves ~5 fork/exec round-trips per test run.

18. [ ] **performance-reviewer** | `test/littletest.hpp:290` | memory-allocation
   LT_SKIP always constructs a std::stringstream and calls make_streamable even on the hot 'always skip' path. The stringstream heap allocation is unconditional regardless of whether any observer reads the message.
   *Recommendation:* This is in a test-only macro invoked rarely (on skip, which is the exceptional path), so the allocation cost is negligible in practice. No change required unless skip-heavy test suites are profiled and show overhead here.

19. [ ] **security-reviewer** | `scripts/test_check_littletest_skip_exit_code.sh:59` | injection
   The CXX variable is read from the environment without quoting in the compile invocations on lines 59 and 95 ("$CXX" -std=c++20 ...). While CXX is double-quoted correctly, the script also passes $REPO_ROOT as a bare -I path (line 59: -I"$REPO_ROOT/test"), which is double-quoted correctly. The main risk is that an attacker who controls the environment in which this script runs (e.g. a poisoned CI workspace) could inject compiler flags via a crafted CXX value. In this project's CI context $CXX is set from the matrix or is the default 'c++', so actual exploitability is very low (CWE-78, CWE-454).
   *Recommendation:* The script is already defensively written (set -euo pipefail, TMPDIR_ is local). Optionally add a CXX value sanity check: if [[ "$CXX" =~ [^a-zA-Z0-9+_./-] ]]; then echo 'ERROR: suspicious CXX value' >&2; exit 1; fi. Not required for this PR.

20. [ ] **security-reviewer** | `test/integ/ws_start_stop.cpp:1495` | insecure-design
   is_psk_unsupported_error() uses permissive substring matching on the exception message to decide whether to SKIP or re-throw. The function checks for 'PSK'/'psk' AND any of 'not supported'/'unsupported'/'not available'/'disabled'. The design intent (documented in the comment) is intentionally permissive so that a changed MHD error message falls through to re-throw rather than silently masking a real failure. This is correct and the existing re-throw path (line 1529, 1559, etc.) is present on every call site. The risk flagged by the focus area is real but minor: a hypothetical future MHD error that contains e.g. 'PSK cipher disabled due to policy' but indicates a genuine implementation break would be masked as a SKIP. Since the condition requires BOTH a PSK token AND a support-negation token, the false-positive space is narrow.
   *Recommendation:* Consider tightening the 'unsupported' token list to require 'PSK' in the same clause as the negation token (e.g. msg.find('PSK') + ('not supported'/'unsupported') in the same substring), rather than checking them in separate OR branches. A helper unit test that asserts is_psk_unsupported_error correctly classifies known benign messages vs known broken-build messages would add regression protection. This is a quality-of-test concern rather than a production security issue.

21. [ ] **security-reviewer** | `test/integ/ws_start_stop.cpp:1627` | injection
   Five system() calls pass a shell command string assembled with snprintf, where the only dynamic value is a hardcoded integer port (PORT + constant). The integer is not attacker-controlled and cannot be injected. However the command string is still passed to sh -c via system(), which inherits the test process environment including $PATH and $IFS. The has_gnutls_cli() guard (line 1484) only validates whether gnutls-cli is present at probe time, not at exec time. This is a test-only code path and the inputs are compile-time constants, so exploitability is nil; it is noted for completeness under CWE-78.
   *Recommendation:* For belt-and-suspenders hygiene consider replacing the system()/sh-based invocations with execvp() (fork+exec directly) or, where that is impractical, verifying that the command string is entirely composed of compile-time literals with no runtime-variable interpolation beyond the integer port — which is already the case here. No change is required for security.

22. [ ] **spec-alignment-checker** | `.github/workflows/verify-build.yml:1002` | acceptance-criteria
   The canary verification step for the 'with gnutls-cli present' baseline uses a grep pattern '^\ [SKIP\ ].*psk_connection'. The LT_SKIP output format is '[SKIP] (file:line) - skipping "testname": reason', so the testname 'psk_connection_success' etc. will appear in the line. This is correct. However, the canary only fires on 'ubuntu / basic / gcc / nodebug / dynamic / classic' — it does NOT fire on the macOS lanes or other Linux compiler lanes. If gnutls-cli detection regressed on those lanes, it would not be caught. This is a narrower-than-ideal canary scope, but the task text says 'a representative healthy gnutls-bin-installed lane' so it meets the criterion.
   *Recommendation:* Low priority: consider extending the canary to at least one additional lane (e.g. gcc-14 extra/dynamic). The current single-lane baseline is adequate per the task spec.

23. [ ] **spec-alignment-checker** | `test/integ/ws_start_stop.cpp:920` | specification-gap
   Several TLS start-failure catch blocks use 'LT_SKIP_IF(true, ...)' (unconditional skip) rather than a conditional predicate. For tests like 'https_webserver', 'tls_session_getters', 'client_cert_*', and 'sni_callback_setup', any exception from ws.start() unconditionally skips the test. This is correct for the task's intent (environment may lack certs or library), but it means a broken TLS implementation that throws a different unexpected exception (e.g. a null-deref surfaced as std::bad_alloc) would also be silently SKIPped rather than FAILed. The task spec says 'distinguish configuration not supported from configuration broken'; the distinction is only made for PSK (via is_psk_unsupported_error), not for the generic TLS-cert tests.
   *Recommendation:* Consider applying the same typed-catch + is_tls_cert_missing_error() predicate used in the PSK path to the cert-based TLS tests. For now, LT_SKIP_IF(true, ...) in a catch meets the task goal of 'not reporting PASS', so this is a minor improvement opportunity only.

24. [ ] **test-quality-reviewer** | `.github/workflows/verify-build.yml:976` | implementation-coupling
   The tls-no-cli verification step (line 976) greps for `'^\ [SKIP\]\ .*gnutls-cli'` — the pattern matches only SKIP lines whose reason string contains 'gnutls-cli'. This is correct for the five `psk_connection_*` tests. However, if the LT_SKIP reason string for those tests is later rephrased (e.g. 'gnutls-cli tool not found' changed to 'gnutls CLI tool absent'), the grep pattern would stop matching and the CI step would incorrectly report 0 SKIPs and fail. The verification correctness depends on keeping the grep pattern synchronized with the reason string passed to LT_SKIP_IF in the C++ source.
   *Recommendation:* Either anchor the grep to the test name rather than the reason string (grep for 'psk_connection' in the SKIP lines), or add a comment in both the workflow and the LT_SKIP_IF call noting this coupling so a future rephrasing of the reason string triggers a corresponding workflow update.

25. [ ] **test-quality-reviewer** | `scripts/test_check_littletest_skip_exit_code.sh:1` | missing-test
   The shell script verifies (a) all-skip → exit 77 and (b) mixed pass+skip → exit 0 and checks for [SKIP] and '-> N skipped' output. It does NOT verify the failure path: a binary with at least one failure should exit non-zero (the failure count), not 77 — i.e., `failures_counter` must dominate. Without this case, a future regression that returned 77 on any failure would not be caught by this script.
   *Recommendation:* Add a case (c): compile a TU with one failing test and one skipping test, run it, and assert the exit code is neither 0 nor 77 (specifically > 0 and not 77). This pins the priority: failures win over skips.

26. [ ] **test-quality-reviewer** | `test/integ/ws_start_stop.cpp:1527` | naming-convention
   The four is_psk_unsupported_error unit tests are placed inside ws_start_stop_suite rather than a dedicated helper-predicate suite, which makes the test naming slightly misleading (suite is named after webserver start/stop lifecycle, not predicate logic). This is a minor organisational concern.
   *Recommendation:* No change required — the placement is pragmatic since the helper and its consumers are in the same TU. A comment noting these are predicate unit tests (already present) is sufficient.

27. [ ] **test-quality-reviewer** | `test/unit/littletest_skip_semantics_test.cpp:49` | test-isolation
   observe_prior_skip_was_counted relies on test execution order: it reads the cumulative skip counter incremented by skip_macro_increments_skip_counter (Cycle 1.a). If the runner ever changes ordering, the GTE(skips, 1) assertion could pass vacuously because a different earlier test in the suite also called LT_SKIP. The comment acknowledges this but labels it an acceptable invariant rather than a design constraint.
   *Recommendation:* Consider capturing `get_skips()` before and after a single LT_SKIP call within the same test body using a counter-delta approach — this would not require cross-test state. If the runner ordering guarantee is stable (LT_BEGIN_AUTO_TEST registers in source order), add a comment explicitly documenting that invariant so future readers know the dependency is intentional.

28. [ ] **test-quality-reviewer** | `test/unit/littletest_skip_semantics_test.cpp:59` | naming-convention
   Test name `observe_prior_skip_was_counted` (line 59) encodes execution-order dependency in the name ('prior'), coupling it to test registration order. The test asserts `get_skips() >= 1` after the earlier skipping test ran. This is correct but fragile: if the test file is reorganized and `skip_macro_increments_skip_counter` runs after this test, the assertion would fail spuriously. The `>= 1` bound partially mitigates the ordering concern, but the name implies a specific ordering contract.
   *Recommendation:* Rename to `skip_counter_nonzero_after_skip_runs` to drop the ordering implication. Better still, consider resetting the skip counter between tests (if the runner supports it) so each test can use a tight delta assertion rather than a cumulative lower bound.
