# Unworked Review Issues

**Run:** 2026-07-02 10:24:53
**Task:** TASK-088
**Total:** 21 (0 critical, 0 major, 21 minor)

## Minor

1. [ ] **architecture-alignment-checker** | `specs/architecture/09-testing.md:15` | pattern-violation
   The doc says each matrix entry invokes `make check-<tool>` where the context implies <tool>=memcheck/helgrind/drd, yielding `make check-memcheck` etc. The actual workflow command is `make check-valgrind-${TOOL}` (i.e. `check-valgrind-memcheck`). The check-valgrind-lane.sh structural gate (assertion d) explicitly confirms that bare `make check-${TOOL}` without the `valgrind-` prefix does not exist and must fail the gate, so the doc notation is imprecise. The correct phrasing would be `make check-valgrind-<tool>` (tool=memcheck/helgrind/drd).
   *Recommendation:* In 09-testing.md item 8, change `make check-<tool>` to `make check-valgrind-<tool>` (where tool is memcheck, helgrind, or drd) to match the AX_VALGRIND_CHECK-generated target names and the workflow's `make "check-valgrind-${TOOL}"` invocation.

2. [ ] **cloud-infrastructure-reviewer** | `.github/workflows/verify-build.yml:200` | iac-anti-pattern
   [OPS-02 Operational Excellence — Config Clarity] The valgrind matrix `include` entry sets `debug: nodebug`, but the configure step at line 914 always passes `--enable-debug` when `BUILD_TYPE=valgrind`, making the matrix `debug` key a non-functional label for this lane. A reader scanning the matrix could incorrectly infer that Valgrind runs without debug symbols. Adding an inline comment clarifying that the configure branch overrides the env var would prevent future confusion.
   *Recommendation:* Add a comment on the `debug: nodebug` line such as `# configure step passes --enable-debug unconditionally for the valgrind lane` to match the pattern used on other workaround fields in the matrix.

3. [ ] **cloud-infrastructure-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-088/.github/workflows/verify-build.yml:895` | iac-anti-pattern
   [OE — Documentation Accuracy] The inline YAML comment on the 'Run valgrind-lane structural gate' step says '`make check-valgrind` still invoked' but TASK-088 changed the step to invoke `make "check-valgrind-${TOOL}"` (per-tool target). The gate script itself (check-valgrind-lane.sh assertion d) correctly describes the new invocation, so only the YAML comment is stale.
   *Recommendation:* Update the YAML comment from '`make check-valgrind` still invoked' to '`make "check-valgrind-${TOOL}"` (per-tool AX_VALGRIND_CHECK target) invoked' to match the actual CI step at line 1215 and the gate script's assertion d.

4. [ ] **code-quality-reviewer** | `scripts/check-valgrind-lane.sh:81` | code-elegance
   The valgrind gate's Python block checks `if not valgrind` (at least one entry required) but does not check `if len(valgrind) > 1` the way check-msan-lane.sh does for the msan entry. A future accidental duplicate valgrind include entry would pass the gate silently. The asymmetry makes the valgrind gate slightly less strict than its msan mirror.
   *Recommendation:* Add `if len(valgrind) > 1: print('(b) more than one valgrind include entry found', file=sys.stderr); sys.exit(5)` after the `if not valgrind` check to match check-msan-lane.sh's exactness contract.

5. [ ] **code-quality-reviewer** | `scripts/check-valgrind-lane.sh:96` | code-readability
   Assertion (c) uses `grep -qF -- "$flag" "$WF"` to detect residual disable flags anywhere in the workflow file. This is a whole-file grep; any future comment in the YAML that mentions a historical flag string (e.g., '# we removed --disable-valgrind-helgrind') would cause a false-positive gate failure. The msan gate has no symmetric grep-for-absence check, making this an asymmetric brittleness in the gate idiom.
   *Recommendation:* Either restrict the search to the configure branch stanza (e.g., pipe the valgrind elif block through awk first, then grep within that slice), or add a comment documenting that future YAML comments about these flags must avoid the exact --disable-valgrind-{helgrind,drd,sgcheck} strings.

6. [ ] **code-quality-reviewer** | `scripts/test_check_valgrind_lane.sh:101` | test-coverage
   Test 5 strips both test-suite-helgrind.log and test-suite-drd.log from the fixture in a single sed substitution and asserts exit 1. This covers the case where both are absent but leaves a blind spot: a regression that dropped the (e1) helgrind check while keeping (e2) drd — or vice versa — would not be caught by the self-test. The msan test (test_check_msan_lane.sh) also omits a split-assertion test, but here there are two distinct grep checks (e1/e2) that each need independent coverage.
   *Recommendation:* Add two fixture variants: one where only test-suite-drd.log is absent (assert exit 1 from e2), and one where only test-suite-helgrind.log is absent (assert exit 1 from e1). This ensures each branch of the e-assertion is exercised independently.

7. [ ] **code-quality-reviewer** | `scripts/test_check_valgrind_lane.sh:163` | readability
   Comment says 'write_good already uses check-${TOOL}' but write_good (line 72) actually uses `make "check-valgrind-${TOOL}"`. The comment appears to describe the previous state before the fix but reads as a description of current code, which is the opposite of what write_good does.
   *Recommendation:* Rephrase to: 'the old write_good (iter-1) used bare check-${TOOL}, which the old gate incorrectly accepted; the current write_good uses check-valgrind-${TOOL}' to make the historical reference explicit.

8. [ ] **code-quality-reviewer** | `scripts/test_check_valgrind_lane.sh:19` | test-coverage
   There is no test that passes a non-existent file path to check-valgrind-lane.sh and asserts exit 1 (the `if [ ! -f "$WF" ]` guard at line 43-46 of the gate). The msan self-test (test_check_msan_lane.sh) also omits this test, so this is consistent with the mirror, but it is a small blind spot in the gate's coverage.
   *Recommendation:* Add a test that invokes the script with a path to a file that does not exist and asserts the script exits 1. This ensures the early-exit guard is exercised.

9. [ ] **code-quality-reviewer** | `specs/architecture/09-testing.md:15` | readability
   Architecture doc says each matrix entry invokes `make check-<tool>` but the actual make target is `make check-valgrind-<tool>` (with the valgrind- prefix). This is exactly the distinction Test 7 guards against — a reader following the spec literally would write a broken CI step.
   *Recommendation:* Change `make check-<tool>` to `make check-valgrind-<tool>` in the Valgrind lane description.

10. [ ] **code-simplifier** | `.github/workflows/verify-build.yml:228` | code-structure
   The inline comment `# configure step passes --enable-debug unconditionally for valgrind-* lanes` is repeated verbatim on all three matrix entries (lines 218, 228, 238). The first entry already carries the explanation; the second and third copies are noise because the three entries are visually grouped under the block comment above them, which already explains the configure behavior.
   *Recommendation:* Remove the inline comment from the valgrind-helgrind and valgrind-drd entries (lines 228 and 238), keeping it only on the first (valgrind-memcheck) entry where a reader first encounters the discrepancy between `debug: nodebug` and the actual configure flag.

11. [ ] **code-simplifier** | `scripts/check-valgrind-lane.sh:109` | patterns
   grep -qE is used for the literal string 'make check-valgrind', which contains no ERE metacharacters. Assertions (c), (e1), and (e2) in the same script consistently use grep -qF for literal matching, and the msan mirror uses plain grep -q for analogous literal checks.
   *Recommendation:* Change 'grep -qE' to 'grep -qF' on line 109 to match the literal-matching style used on lines 97, 117, and 122: if grep -qF 'make check-valgrind' "$WF"; then

12. [ ] **code-simplifier** | `scripts/check-valgrind-lane.sh:4` | naming
   The one-line banner comment reads "Re-enable helgrind / drd / sgcheck in the valgrind CI lane", but sgcheck is intentionally left disabled (Valgrind 3.20 removed it, ubuntu-latest ships 3.22). The body (line 12) clarifies this, but the title creates a false expectation for anyone scanning script headers.
   *Recommendation:* Revise the banner to reflect the actual outcome: "Re-enable helgrind + drd in the valgrind CI lane (sgcheck intentionally off — removed upstream in Valgrind 3.20)".

13. [ ] **code-simplifier** | `scripts/test_check_valgrind_lane.sh:163` | naming
   The comment at lines 163-165 says "write_good already uses check-${TOOL} (which is what the old gate incorrectly accepted)" but `write_good` actually uses `check-valgrind-${TOOL}` — the correct form. This is a stale/inverted description that could mislead a reader into thinking the test-fixture helper is broken or that the correct target is `check-${TOOL}`.
   *Recommendation:* Replace the misleading sentence with a brief accurate statement, e.g.: "The fixture uses the bare `check-${TOOL}` target (missing the `valgrind-` prefix) — this target does not exist in AX_VALGRIND_CHECK and must cause assertion (d) to fail."

14. [ ] **housekeeper** | `.github/workflows/verify-build.yml:895` | documentation-stale
   The gate step comment says '`make check-valgrind` still invoked' but the actual 'Run Valgrind checks' step (and assertion (d) in check-valgrind-lane.sh) now use `make "check-valgrind-${TOOL}"` — the per-tool form. The bare `check-valgrind` target no longer exists in the workflow.
   *Recommendation:* Update the comment to read `make "check-valgrind-${TOOL}"` (per-tool invocation asserted by check-valgrind-lane.sh assertion (d)) instead of `make check-valgrind`.

15. [ ] **performance-reviewer** | `.github/workflows/verify-build.yml:196` | missing-caching
   Three parallel valgrind jobs (valgrind-memcheck, valgrind-helgrind, valgrind-drd) each run a full configure + make of libhttpserver independently. The libmicrohttpd dependency is shared via the actions/cache step (same key across all three since OS and compiler are identical), so that cost is not tripled. The libhttpserver build itself is compiled three times, consuming roughly 3x the runner-minutes for the compile phase.
   *Recommendation:* Consider adding ccache (e.g. `uses: hendrikmuhs/ccache-action`) with a shared cache key for the valgrind lanes. This would let the second and third runner hits avoid recompiling unchanged translation units, cutting per-job compile time significantly without affecting correctness. This is a runner-cost optimisation only — wall-clock is already capped at the slowest single tool by the parallel split — so it is not a blocker.

16. [ ] **spec-alignment-checker** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-088/.github/workflows/verify-build.yml:1218` | specification-gap
   The 'Print Valgrind results' step (always()) iterates all three log files (memcheck/helgrind/drd) on every valgrind-* job, but each individual job only produces one log. The conditional `if [ -f ... ]` guards correctly so no error occurs, but on a helgrind job the step silently skips the memcheck/drd logs with no output — which is expected but may confuse readers of CI logs who see a 'Print Valgrind results' header that emits nothing for two of three files.
   *Recommendation:* Consider echoing a note when a log file is absent (e.g. 'not produced by this lane') to make each job's output self-explanatory. Low priority — functionally correct.

17. [ ] **spec-alignment-checker** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-088/.github/workflows/verify-build.yml:218` | specification-gap
   All three valgrind-* matrix entries carry `debug: nodebug` in the matrix YAML with an inline comment explaining configure passes --enable-debug unconditionally. This creates a mild inconsistency: the matrix field says nodebug but the effective build is debug-enabled. A reader who looks only at the matrix summary (not the configure step) will be misled about whether debug symbols are present.
   *Recommendation:* Either rename the field value to something like `valgrind-debug` to accurately reflect the configured state, or document in a comment block at the matrix level that valgrind-* lanes override this field. Low priority — functionally correct, the configure step always uses --enable-debug for valgrind-* patterns.

18. [ ] **spec-alignment-checker** | `scripts/check-valgrind-lane.sh:null` | specification-gap
   The structural gate (check-valgrind-lane.sh) checks only the workflow YAML for no --disable-valgrind-* flags and for helgrind/drd log surfacing. It does not assert that configure.ac contains AX_VALGRIND_DFLT([sgcheck],[off]) before AX_VALGRIND_CHECK. A future configure.ac regression that re-enables sgcheck probing (causing configure to error on ubuntu-latest / Valgrind 3.22 which no longer ships sgcheck) would pass the lint gate but break the Valgrind lane.
   *Recommendation:* Add an assertion (f) to check-valgrind-lane.sh that greps configure.ac for 'AX_VALGRIND_DFLT\(\[sgcheck\]' and verifies it precedes 'AX_VALGRIND_CHECK', matching the existing gate's idiom of validating all wiring points. Also update test_check_valgrind_lane.sh with a corresponding failure fixture.

19. [ ] **spec-alignment-checker** | `test/valgrind-helgrind.supp:null` | action-item
   Action items 2 and 3 specify 'Run the suite locally under helgrind / drd: triage every finding. Fixes go in source; intentional racing patterns get a narrow entry [...] with a comment.' Both suppression files shipped with zero entries and their header comments say 'suppressions are added only when a concrete third-party finding surfaces during CI-side triage on the Linux lane (Valgrind is Linux-only and cannot run on the darwin dev/CI runners).' This is a coherent rationale (macOS dev machines cannot run Valgrind), but it shifts the triage to post-merge rather than pre-PR as the action items imply. If there is existing third-party noise (libmicrohttpd worker threads, glibc, libstdc++) the first CI run with helgrind/drd enabled will surface it and may fail until suppressions are added reactively.
   *Recommendation:* Accept the empty-ships approach as intentional given the Linux-only Valgrind constraint, but document explicitly in the task or PR description that CI-side triage on the Linux lane is the intended completion step for action items 2 and 3, and that the lane may require one follow-up PR to add narrow third-party suppressions. Alternatively, run `valgrind --tool=helgrind` inside a Docker Linux container locally before merging and pre-populate any needed entries.

20. [ ] **test-quality-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-088/scripts/test_check_valgrind_lane.sh:101` | missing-test
   Test 5 removes both test-suite-helgrind.log and test-suite-drd.log in a single fixture, exercising assertions (e1) and (e2) together. The gate checks these via two independent grep calls. If either grep were accidentally removed from the gate, the remaining grep would still make Test 5 pass (exit 1 either way), so the combined fixture does provide detection coverage. However, isolated fixtures — one removing only helgrind.log, one removing only drd.log — would make each sub-assertion's necessity independently verifiable.
   *Recommendation:* Optionally split Test 5 into two fixtures: one with only test-suite-drd.log absent and one with only test-suite-helgrind.log absent. This is low-priority given the current combined fixture still catches removal of either check, but would make the self-test a strict mirror of the gate's two-part (e) assertion.

21. [ ] **test-quality-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-088/scripts/test_check_valgrind_lane.sh:164` | naming-convention
   The comment on line 164 reads 'write_good already uses check-${TOOL}' but write_good in fact uses 'check-valgrind-${TOOL}'. The sentence appears to describe a historical state of write_good before the gate was tightened, and now misleads readers about what the good fixture actually contains.
   *Recommendation:* Replace lines 163-165 with a comment that accurately describes the fixture: 'The fixture is built independently with make "check-${TOOL}" (bare form, missing the valgrind- prefix) to ensure Test 7 is decoupled from write_good and tests only assertion (d).'
