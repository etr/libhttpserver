# Unworked Review Issues

**Run:** 2026-05-19 10:25:55
**Task:** TASK-039
**Total:** 40 (0 critical, 4 major, 36 minor)

## Major

1. [ ] **code-quality-reviewer** | `test/bench_get_headers.cpp:140` | test-coverage
   Median index calculation uses OUTER/2 = 5 (integer division), which is the 6th element (0-indexed) of a 10-element sorted array. For an even-count sample the canonical median is the mean of elements [4] and [5]. The discrepancy is small (one rep of noise), but the PERFORMANCE.md documents 'median' and the test criterion is tight enough that a worst-case jitter spike in element [5] could shift the reported number vs the true median.
   *Recommendation:* Either document that 'median' here means the upper-of-center sample for even N (acceptable engineering choice), or compute `(samples_ns[OUTER/2 - 1] + samples_ns[OUTER/2]) / 2.0` and add a comment explaining the choice.

2. [ ] **code-quality-reviewer** | `test/v1_baseline/measure_v1_get_headers.cpp:152` | code-readability
   The `do_not_optimize` overload in measure_v1_get_headers.cpp takes `T const& value` but passes `value` (not `&value`) to the asm clobber — `"r,m"(value)` vs `"r,m"(&value)` in bench_get_headers.cpp. The v1 measurement file passes the map by value to the asm constraint, which passes a copy of the map object (the entire std::map layout) as an input. For a non-trivial type that is actually a correctness/portability risk; the benchmark file correctly passes a pointer. These two `do_not_optimize` helpers are inconsistent.
   *Recommendation:* Align the v1 measurement TU's `do_not_optimize` to use `"r,m"(&value)` (same as bench_get_headers.cpp), and add a comment noting the pointer-vs-value distinction so future readers understand the intent.

3. [ ] **code-simplifier** | `test/bench_get_headers.cpp:123` | code-structure
   The timing loop structure (reserve vector, outer/inner loops, sort, take median at OUTER/2) is duplicated verbatim between bench_get_headers.cpp (lines 123-140) and measure_v1_get_headers.cpp (lines 163-178). Both use the same OUTER=10, INNER=1'000'000, and std::chrono::steady_clock. This makes the two programs structurally identical except for what is timed.
   *Recommendation:* Factor the harness into a shared header (e.g. test/bench_harness.hpp) providing a run_bench(callable, outer, inner) -> double function that returns the median ns/call. Each TU would call it with a lambda, eliminating the duplicated scaffolding. Since measure_v1_get_headers.cpp is not built by make, it can still use the header.

4. [ ] **code-simplifier** | `test/bench_get_headers.cpp:62` | code-structure
   do_not_optimize() is defined identically (same template body, same asm volatile, same MSVC fallback) in both bench_get_headers.cpp (lines 61-70) and measure_v1_get_headers.cpp (lines 150-153, with a minor variant: the bench uses "r,m"(&value) while the measure uses "r,m"(value)). Duplicating this helper is a code smell; if the sink is ever changed (e.g. to support MSVC better), both copies must be updated in lockstep.
   *Recommendation:* Extract do_not_optimize into a shared bench utility header, e.g. test/bench_utils.hpp. The v1 baseline TU ships in EXTRA_DIST and is never built by make, so it cannot link against the bench infrastructure, but it can #include the same header-only helper. The slight difference between the two copies (pointer vs value) should also be reconciled — the bench_get_headers.cpp version (passing &value) is correct for a reference return, and should be used in both.

## Minor

5. [ ] **architecture-alignment-checker** | `test/v1_baseline/v1_constants.hpp:57` | pattern-violation
   V1_HTTP_RESOURCE_SIZEOF is hard-coded to 32 (the libc++/macOS value), but the comment acknowledges libstdc++ would give 56. The bench_sizeof_http_resource.cpp static_assert is algebraically valid on both stdlibs (as demonstrated in PERFORMANCE.md), but the constant's comment says 'We commit the libc++ value because that is the baseline-measurement stdlib' — this makes the constant contingent on the maintainer's host ABI, which could silently yield a looser bound on a libstdc++ CI host. The architecture's §9 testing strategy does not forbid ABI-specific literal constants, so this is not a violation, but it is a fragility that a future maintainer updating only the constant without re-checking the algebra could inadvertently weaken the assertion.
   *Recommendation:* Add a brief note in v1_constants.hpp (or PERFORMANCE.md) warning that if V1_HTTP_RESOURCE_SIZEOF is updated to the libstdc++ value (56), V1_STD_MAP_STRING_BOOL_SIZEOF must also be updated to the libstdc++ value (48) simultaneously, since the static_assert's pass condition depends on both being from the same ABI.

6. [ ] **code-quality-reviewer** | `test/PERFORMANCE.md:189` | code-readability
   The Known noise sources table references 'Median (not mean) over 10 reps' for the 'Other tenants on the host' row, but bench_get_headers.cpp now uses OUTER=11 reps. The table text was not updated to say '11 reps'.
   *Recommendation:* Change '10 reps' to '11 reps' in the noise-sources table to match the actual bench code.

7. [ ] **code-quality-reviewer** | `test/bench_get_headers.cpp:92` | code-readability
   The `; // NOLINT(whitespace/semicolon)` comment at the end of the kSanitizerBuild initializer suggests cpplint is active, but this is the only NOLINT suppression in the added files. The constexpr bool initialization pattern with the semicolon on a separate line is an unusual formatting choice specific to multi-line conditional initializers; an alternative is to wrap the whole expression in a helper constexpr function to avoid the awkward trailing semicolon.
   *Recommendation:* Consider a `constexpr bool detect_sanitizer_build() { ... return false; }` helper to avoid the unusual semicolon placement and the need for the NOLINT comment.

8. [ ] **code-quality-reviewer** | `test/bench_sizeof_http_resource.cpp:52` | code-readability
   The comment block algebra says `sizeof(method_set) * 2` captures 'the new member's own size and the worst-case alignment padding it forces'. The static_assert on line 72 is consistent with this, but the PERFORMANCE.md section 'Methodology — sizeof(http_resource) check' shows the same formula as `sizeof(method_set) * 2` with method_set=4 giving 8. The comment in the .cpp says the factor-2 accounts for 'its own size plus up to one alignment unit of slack', which is a reasonable bound, but the explanation could be tighter to help a future reader understand that this is a worst-case padding allowance, not a measurement.
   *Recommendation:* Add a sentence clarifying that the factor-of-2 is a conservative upper bound (own_size + max_padding <= 2 * own_size when alignment <= sizeof(member)), not an exact count.

9. [ ] **code-quality-reviewer** | `test/bench_sizeof_http_resource.cpp:83` | test-coverage
   The binary's main() returns 0 unconditionally after the compile-time static_asserts. The bench rule in test/Makefile.am runs all bench_targets and checks their exit code (`|| exit $$?`). Since the only meaningful check is compile-time, the runtime exit code carries no signal. This is by design but could confuse a maintainer who adds a runtime check and forgets the static_assert is the only gate. A brief comment in main() would help.
   *Recommendation:* Add a comment in main() such as `// All checks are compile-time static_asserts above; this binary succeeds by compiling.` to make the intent explicit.

10. [ ] **code-quality-reviewer** | `test/v1_baseline/measure_v1_get_headers.cpp:163` | code-readability
   The v1 measurement TU still uses OUTER=10 (even), so `samples_ns[OUTER/2]` at line 179 selects index 5, which is not the true median of 10 elements (the median of an even-length array is the average of indices 4 and 5). The bench_get_headers.cpp comment explicitly motivates OUTER=11 for a true median, but the v1 counterpart was not updated to match. This is a documentation/methodological inconsistency: the committed constant was measured with an even-count median, not the true median.
   *Recommendation:* Either change OUTER to 11 in measure_v1_get_headers.cpp for methodological consistency, or add a comment explaining the deliberate difference (e.g. 'this is a one-shot measurement TU; even-count median is acceptable here since the value is only committed once and reviewed by eye').

11. [ ] **code-quality-reviewer** | `test/v1_baseline/measure_v1_get_headers.cpp:67` | code-elegance
   The global mutable arrays `g_keys[16]` and `g_vals[16]` (non-const `const char*` pointers initialized from `StaticHeaders::g_static_headers` at static-init time) are a fragile global-init ordering dependency. If the static init order were ever changed, `g_keys`/`g_vals` could be read before `g_static_headers` initializes them. The measurement TU is documentation-only and never built by the build system, but the pattern is worth noting as a latent smell for future maintainers who might adapt it.
   *Recommendation:* Consider initializing the pointer arrays inside `StaticHeaders::StaticHeaders()` directly (they are already there at lines 89-92), but storing the `StaticHeaders` object itself as the single source of truth and accessing via method rather than naked globals. This is minor since the TU is EXTRA_DIST only.

12. [ ] **code-quality-reviewer** | `test/v1_baseline/v1_constants.hpp:74` | code-readability
   The README.md (test/v1_baseline/README.md, line 93) instructs maintainers to commit the 'rounded lower end of the observed range' as a conservative number, but v1_constants.hpp line 70-74 says 'We commit the rounded median as a conservative number'. The two descriptions are contradictory: the README says lower-end-of-range; the header comment says 'rounded median'. The actual committed value (760.0) is between the observed min (756) and median (~768), so it is actually closer to the lower end. The README description appears to be correct; the header comment is misleading.
   *Recommendation:* Correct the comment in v1_constants.hpp line 70-74 to match the README: 'We commit the rounded lower end of the observed range (756 ns rounded to 760 ns) as a conservative number.'

13. [ ] **code-quality-reviewer** | `test/v1_baseline/v1_constants.hpp:76` | code-readability
   The comment block on V1_GET_HEADERS_NS_PER_CALL still reads '10 outer reps x 1,000,000 inner iterations' (line 76-78), but the PERFORMANCE.md methodology section now documents OUTER=11 for bench_get_headers. Since v1_constants.hpp describes the v1 measurement (which uses OUTER=10), the file itself is internally consistent, but the cross-file picture is slightly confusing. A one-line note clarifying 'this constant was produced with OUTER=10; bench_get_headers uses OUTER=11' would remove any ambiguity.
   *Recommendation:* Add a brief parenthetical to the V1_GET_HEADERS_NS_PER_CALL comment noting that OUTER=10 was used for the v1 measurement (to distinguish from the v2 bench's OUTER=11).

14. [ ] **code-simplifier** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-039/test/v1_baseline/v1_constants.hpp:179` | naming
   In measure_v1_get_headers.cpp (OUTER=10 baseline TU), the local variable `median` at line 179 shadows the meaning well but is inconsistent with the bench_get_headers.cpp pattern that names it `v2_median_ns`. The v1 side names the variable plainly `median`, which is fine in isolation but slightly inconsistent across the two timing files.
   *Recommendation:* Rename `median` to `v1_median_ns` in measure_v1_get_headers.cpp line 179 to match the naming style of the v2 bench file and make the symmetry explicit.

15. [ ] **code-simplifier** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-039/test/v1_baseline/v1_constants.hpp:59` | code-structure
   The `#if defined(__GLIBCXX__) / #else` block for platform-specific sizeof constants is correct and necessary, but the `#else` branch has no explicit guard comment for what happens on MSVC or other non-GCC, non-libc++ toolchains. A future porter targeting Windows with MSVC would silently fall through to the libc++ values (which may or may not be correct for that ABI).
   *Recommendation:* Add a `#warning` or a `static_assert` in the `#else` branch noting that the values are validated only for libc++ and that a different stdlib may require re-measurement. This is a documentation/safety gap, not a functional bug.

16. [ ] **code-simplifier** | `test/bench_get_headers.cpp:101` | comments
   The inline comment block at lines 101-106 (starting '// Build a 16-header request via create_test_request...') describes internal implementation details of create_test_request and http_request_impl that are irrelevant to a bench reader. Comments like '...flows through the connection_ == nullptr branch in ensure_headerlike_cache, builds the view map, and sets headers_cache_built_=true' describe the implementation, not the intent.
   *Recommendation:* Replace the implementation-detail comments with intent-focused comments: '// Build a 16-header request; first call to get_headers() warms the cache.' and '// Subsequent calls exercise the warm (memoised) path — the path PRD-REQ-REQ-001 claims is >=10x faster.'

17. [ ] **code-simplifier** | `test/bench_get_headers.cpp:140` | naming
   The variable v2_median_ns (line 140) is named with an explicit 'v2' prefix while the corresponding v1_ns (line 141) uses a 'v1' prefix. In measure_v1_get_headers.cpp the equivalent variable is named median (line 179) without a version qualifier. The naming convention is inconsistent across the two files.
   *Recommendation:* In bench_get_headers.cpp, 'v2_median_ns' is clear and accurate — keep it. In measure_v1_get_headers.cpp, rename 'median' to 'median_ns' for consistency with the reported unit (ns/call) and to match the output format string that uses '%.3f' for nanoseconds.

18. [ ] **code-simplifier** | `test/bench_get_headers.cpp:20` | comments
   The file-level comment block (lines 20-41) partially duplicates information already in test/PERFORMANCE.md: the 10x threshold, the warmup rationale, the sanitizer skip logic, and the EXTRA_PROGRAMS wiring are all described in detail there. The comment is useful as a quick orientation but re-states specifics (e.g. '10 outer reps x 1,000,000 inner iterations') that are also expressed in the named constants OUTER and INNER immediately below in main(). Per clean-code principles, if the code is self-documenting (named constants), the comment is partly redundant noise.
   *Recommendation:* Trim the file comment to the 'why' (PRD requirement, skip rationale, not-part-of-make-check), and remove the detailed re-statement of iteration counts. Let the named constants speak for themselves.

19. [ ] **code-simplifier** | `test/bench_sizeof_http_resource.cpp:44` | comments
   The long comment block at lines 44-73 (PRD-REQ-REQ-003 algebra) largely duplicates the explanation already given in test/PERFORMANCE.md under '## Methodology — sizeof(http_resource) check'. Keeping an abbreviated version here is reasonable, but the full algebraic re-derivation with example numbers for both stdlibs (24 lines of comments for a 3-line static_assert) tips into noise.
   *Recommendation:* Keep the one-sentence PRD citation and the algebra formula (5-6 lines), remove the inline worked examples (the macOS and Linux number-crunching), and add a 'See test/PERFORMANCE.md for the full derivation.' pointer.

20. [ ] **code-simplifier** | `test/v1_baseline/measure_v1_get_headers.cpp:163` | naming
   In main(), the variable names r (outer loop counter, line 167) and i (inner loop counter, line 169) are single-letter names with no contextual meaning. The bench_get_headers.cpp counterpart uses the same names but has OUTER/INNER constants next to them, which helps. In the v1 measure program there are no constants — the literal values 10 and 1'000'000 appear directly in the loop bounds, making r and i especially opaque.
   *Recommendation:* Either name the constants OUTER and INNER as in bench_get_headers.cpp, or at minimum rename the loop variables to outer_rep and inner_iter. The latter eliminates the 'what does r count?' question without requiring structural change.

21. [ ] **code-simplifier** | `test/v1_baseline/measure_v1_get_headers.cpp:75` | code-structure
   StaticHeaders (lines 75-94) is a private struct defined solely to initialise g_keys/g_vals through a global constructor, then immediately instantiated as g_static_headers. The pattern (struct with a constructor, single global instance) is a common C++ global-init workaround, but here it could be replaced with a simpler free function called at the top of main() that fills g_keys and g_vals directly, avoiding the need for a named type at all.
   *Recommendation:* Remove StaticHeaders and g_static_headers. Instead, at the top of main(), call a local init_synthetic_headers() free function that fills g_keys and g_vals using the same loop body. This eliminates the struct-as-constructor-workaround pattern and makes the initialization flow explicit in main().

22. [ ] **housekeeper** | `specs/architecture/09-testing.md:null` | architecture-not-updated
   Architecture §9 testing strategy lists 6 test surfaces (header hygiene, build-flag invariance, move semantics, SBO invariant, routing regression, thread-safety) but does not mention the TASK-039 performance acceptance gates (get_headers >=10x speedup, sizeof(http_resource) shrink) as a named testing item, even though TASK-039 is referenced in the _index.md PRD coverage table as implementing PRD-REQ-REQ-001 and PRD-REQ-REQ-003 numeric §3.6 acceptance criteria.
   *Recommendation:* Consider adding a 7th item to §9 testing strategy: 'Performance acceptance gates (PRD §3.6): make bench verifies get_headers() >=10x speedup vs v1 and sizeof(http_resource) shrink by at least the empty-map footprint.' Run /groundwork:source-architecture-from-code to capture this if a full architecture refresh is desired.

23. [ ] **housekeeper** | `test/PERFORMANCE.md:189` | documentation-stale
   The 'Known noise sources / mitigations' table says 'Median (not mean) over 10 reps' but the bench uses OUTER=11 repetitions. The methodology section above (lines 66-73) correctly states '11 outer repetitions' and explains that OUTER=11 (odd) ensures an unambiguous median. The noise table was not updated to match.
   *Recommendation:* Change 'over 10 reps' to 'over 11 reps' in the Known noise sources table row for 'Other tenants on the host'.

24. [ ] **performance-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-039/test/v1_baseline/measure_v1_get_headers.cpp:163` | algorithmic-complexity
   measure_v1_get_headers.cpp still uses OUTER=10 (even), so samples_ns[OUTER/2]=samples_ns[5] is the 6th element of a 10-element sorted array, not the true median (which for an even-length array should be the average of indices 4 and 5). This is a one-off documentation/measurement tool and the resulting constant is committed manually, so it has no runtime correctness impact on the pass/fail bench gate.
   *Recommendation:* Change OUTER to 11 in measure_v1_get_headers.cpp for methodological consistency with bench_get_headers.cpp, or document the median-index convention explicitly. Since this tool is EXTRA_DIST only and the committed constant already has conservative rounding, the impact is negligible.

25. [ ] **performance-reviewer** | `test/PERFORMANCE.md:186` | missing-caching
   CPU frequency scaling mitigation is documented as 'not enforced'. On a laptop running on battery or a CI runner with aggressive power management, per-call ns could easily be 2-3x higher than the baseline host, narrowing the 230x headroom. With a 10x gate and 230x measured headroom the gate would still pass (76ns threshold vs ~10ns actual at 3x slowdown), but the methodology note should acknowledge that the gate is robust to this specific noise source rather than just saying 'not enforced'.
   *Recommendation:* Add a note quantifying the robustness: 'Even at 10x clock throttling (v2 degrades from 3ns to 30ns), the ratio remains >25x, well above the 10x gate.' This would give future maintainers confidence that the bench is not fragile to power management.

26. [ ] **performance-reviewer** | `test/bench_get_headers.cpp:141` | missing-caching
   The V1 baseline (V1_GET_HEADERS_NS_PER_CALL=760.0) is a committed constant measured once on one machine. The bench passes on any machine where v2 ns/call < 76ns. Since v2's warm-path cost is a branch + reference return (~3 ns on a modern out-of-order CPU), the gate will pass even on slow machines. However, the stated methodology does not validate that the v2 benchmark is being measured at the same optimization level (-O3) as the v1 baseline measurement. If the build system runs 'make bench' in a debug build, v2 ns/call could be 20-50x higher than the release-mode 3 ns, potentially failing the 760/v2 >= 10 gate. The bench has no compile-time or runtime check for the optimization level.
   *Recommendation:* Add a build-system guard to 'make bench' that verifies the build was configured with optimization (e.g., assert that NDEBUG is defined, or check that -O2/-O3 is in CXXFLAGS). Alternatively, document in PERFORMANCE.md that 'make bench' must be run from a release build directory.

27. [ ] **performance-reviewer** | `test/v1_baseline/measure_v1_get_headers.cpp:170` | algorithmic-complexity
   The v1 baseline measurement allocates and destroys a std::map with 16 std::string nodes in every one of the 10,000,000 inner iterations. This is correct for measuring v1's per-call cost, but the global allocator will be under sustained pressure throughout the 10-second measurement window. On systems with a thread-local allocator cache (jemalloc, tcmalloc, mimalloc) the steady-state cost after warmup will be lower than cold first-call cost. The warmup of 10,000 iterations is sufficient to reach allocator steady state, so this is not a methodology flaw, but it means the measured V1 number is the allocator-hot cost, not the first-call cost. This is actually more conservative (lower v1 baseline) for the gate.
   *Recommendation:* No change needed; this is an acceptable methodology choice. The comment in the file could be expanded to note that the measurement represents allocator-hot steady-state cost.

28. [ ] **security-reviewer** | `test/bench_get_headers.cpp:62` | insecure-design
   The `do_not_optimize` helper uses `asm volatile` with `"r,m"(&value)` (address-of-reference). While this is an established benchmark pattern (Google Benchmark uses it), on non-GCC/Clang platforms the MSVC fallback stores into `volatile const void*` — a `volatile` write that is technically observable but may still be elided by aggressive MSVC optimisers on newer standards. Not a security issue per se (CWE-1059: insufficient technical documentation), but worth noting for correctness under the platform diversity the library targets.
   *Recommendation:* Document the MSVC fallback limitation in a comment and consider using `_ReadWriteBarrier()` or `__iso_volatile_store64` for a more robust MSVC-specific sink. For security purposes no action required.

29. [ ] **security-reviewer** | `test/v1_baseline/measure_v1_get_headers.cpp:144` | insecure-design
   The MHD stub function `MHD_get_connection_values` is declared `extern "C"` and will shadow the real symbol if this TU is ever accidentally linked with real libmicrohttpd (the EXTRA_DIST placement means it is NOT built by `make bench`, only shipped for documentation, so the risk is vestigial). Additionally, the `reinterpret_cast<void*>(&headers)` on a `header_view_map*` is safe here because MHD immediately casts it back to the same type via the callback `cls` parameter, but combining a `reinterpret_cast` with an extern-C stub is a code-smell pattern (CWE-704). The comment block correctly explains the strategy.
   *Recommendation:* Add a compile-time `#error` or prominent `#warning` guard (e.g., `#ifdef REAL_MHD_LINKED`) inside the stub or a static_assert that the TU is never linked with real MHD. Alternatively, rename the stub to a non-clashing name and invoke it indirectly via a function pointer to make the isolation explicit. Since this file is EXTRA_DIST-only and not included in any build target, the practical risk is negligible.

30. [ ] **spec-alignment-checker** | `test/PERFORMANCE.md:30` | specification-gap
   The 'Baseline values' table (lines 30-34) lists only the libc++ reference-host numbers (sizeof(http_resource)=32, sizeof(map)=24). The libstdc++ baseline values (56 and 48) are described only in the methodology algebra section (lines 113-117) but not in the summary table. The task acceptance criterion says 'Both numbers documented in test/PERFORMANCE.md with the v1 baseline they were measured against'; a reader scanning the table alone would not see the Linux/libstdc++ baseline.
   *Recommendation:* Add a second row or a note to the 'Baseline values' table explicitly recording the libstdc++ values (sizeof(http_resource)=56, sizeof(map)=48) alongside the host triple or stdlib tag so the table is self-contained for both platforms.

31. [ ] **spec-alignment-checker** | `test/bench_get_headers.cpp:140` | specification-gap
   The benchmark measures only the warm-path (cache-hit) cost of get_headers(), not the cold-path (first-call) cost. The PRD §3.6 acceptance criterion says 'per-call cost vs v1' without specifying warm vs cold. The PERFORMANCE.md methodology section explains the choice: v2.0's improvement is in the steady-state cost, since real consumers call get_headers() many times per request. This interpretation is reasonable but is not made explicit in the PRD §3.6 wording itself.
   *Recommendation:* No code change needed. Consider adding a parenthetical note to PRD §3.6 to clarify that 'per-call cost' refers to the steady-state (warm) cost. This avoids ambiguity for future maintainers.

32. [ ] **spec-alignment-checker** | `test/bench_sizeof_http_resource.cpp:72` | action-item
   The action item specifies `static_assert(sizeof(http_resource) <= sizeof_v1_http_resource - sizeof(std::map<std::string, bool>))` but the implementation uses a relaxed formula: `static_assert(sizeof(http_resource) + V1_STD_MAP_STRING_BOOL_SIZEOF <= V1_HTTP_RESOURCE_SIZEOF + sizeof(method_set) * 2)`. The deviation is acknowledged and justified in test/PERFORMANCE.md under 'Why not the literal task formulation' — the literal formula fails on every stdlib because the new `method_set` member consumes some of the savings. The corrected algebra still enforces that the map-removal saving dominates the new field's cost, satisfying the PRD §3.6 contract.
   *Recommendation:* No code change needed. The relaxation is correctly documented. Consider updating the TASK-039 action-item wording in the task doc to match the implemented formula to keep specs and code in sync.

33. [ ] **spec-alignment-checker** | `test/v1_baseline/measure_v1_get_headers.cpp:179` | specification-gap
   The v1 measurement tool uses OUTER=10 (even) and takes `samples_ns[OUTER/2]` = `samples_ns[5]`, which is the upper-middle element, not the true statistical median (which would be the average of [4] and [5] for an even-length array). PERFORMANCE.md documents the observed range as 756-784 ns and the committed constant as 760 ns described as 'rounded lower end'. The upper-middle element of a sorted 10-sample array is a reasonable proxy, but the description in PERFORMANCE.md says 'median of 10 reps', which is slightly inaccurate. This does not affect correctness of the acceptance gate since the constant was directly cross-checked against the observed range.
   *Recommendation:* Either change OUTER to 11 in measure_v1_get_headers.cpp for a true odd-length median consistent with bench_get_headers.cpp, or update the PERFORMANCE.md description to say 'upper-middle element of 10 sorted samples' instead of 'median of 10 reps'.

34. [ ] **spec-alignment-checker** | `test/v1_baseline/v1_constants.hpp:57` | specification-gap
   The committed V1_HTTP_RESOURCE_SIZEOF = 32 is the libc++ (macOS) value. The libstdc++ (Linux) value would be 56. The static_assert in bench_sizeof_http_resource.cpp is validated against the libc++ constant; on a libstdc++ host the assertion still passes (16+48=64 <= 56+8=64 per the comment) but only because the algebra accommodates both stdlibs through the `sizeof(method_set)*2` slack term. There is no CI lane that verifies the bench compiles on a libstdc++ host. This is a documentation and CI gap, not a correctness failure.
   *Recommendation:* The PERFORMANCE.md documents the Linux / libstdc++ numbers. Adding a Linux lane to `make bench` CI (not `make check`) would catch any future stdlib-specific regression. Not required for this task's acceptance.

35. [ ] **spec-alignment-checker** | `test/v1_baseline/v1_constants.hpp:65` | specification-gap
   The platform-detection uses `#if defined(__GLIBCXX__)` / `#else`. The `#else` branch silently applies the libc++ constants (32/24) on any stdlib that is neither libstdc++ nor libc++ (e.g., MSVC STL, stdlibcpp). On such a host the static_assert algebra would be evaluated against wrong baseline numbers, potentially passing when the v2 size has not actually shrunk by the required amount. The risk is low given the project targets macOS/Linux, but the guard is not robust.
   *Recommendation:* Add an explicit `#elif defined(_LIBCPP_VERSION)` for libc++ and a `#else #error` or `#pragma message` fallback so an unknown stdlib fails loudly rather than silently applying macOS numbers.

36. [ ] **test-quality-reviewer** | `test/PERFORMANCE.md:111` | implementation-coupling
   The algebra comment in PERFORMANCE.md (line 111) writes `sizeof(method_set) * 2` as `sizeof(method_set) * 2 = 4*2 = 8`, giving `40 <= 40`, but in the bench_sizeof_http_resource.cpp comment block (line 65) the same example is shown correctly. The PERFORMANCE.md line says `32 + 4*2 = 40` which is arithmetic for `V1_HTTP_RESOURCE_SIZEOF + sizeof(method_set)*2`, consistent with the static_assert. This is fine. However, the description 'sizeof(method_set) * 2 captures both the new member's own size and the worst-case alignment padding' is explained only in the source comment — PERFORMANCE.md's condensed version (`32 + 4*2 = 40`) does not mention that `sizeof(method_set)*2` accounts for both the field size and alignment slack, which could mislead a future re-measurer into tightening the formula incorrectly.
   *Recommendation:* Add a one-sentence note in PERFORMANCE.md under the sizeof check methodology explaining that the `*2` factor accounts for the new field's own size plus worst-case alignment padding, matching the detailed explanation in bench_sizeof_http_resource.cpp lines 53-58.

37. [ ] **test-quality-reviewer** | `test/bench_get_headers.cpp:140` | non-deterministic
   Median index `samples_ns[OUTER / 2]` with OUTER=10 yields index 5 (the 6th of 10 sorted samples, i.e. the upper-middle element). True median for an even count is the average of indices 4 and 5. Using the upper-middle is biased high, making the ratio slightly conservative, but it means the reported 'median' is not technically the statistical median. The same pattern is mirrored in measure_v1_get_headers.cpp (line 179), so both sides of the ratio use the same index, which cancels the bias in the ratio — making this a naming/documentation issue rather than a correctness flaw in the gate.
   *Recommendation:* Either document that 'median' here means 'upper-middle sample of 10 sorted reps' (acceptable given both sides use it), or increase OUTER to an odd number (e.g. 11) so `OUTER/2` is the true median index. The mismatch between measure_v1_get_headers.cpp and bench_get_headers.cpp would become visible if OUTER values differ in a future refactor, so aligning them is safer.

38. [ ] **test-quality-reviewer** | `test/bench_get_headers.cpp:95` | missing-test
   The sanitizer skip exits 0 at runtime, but there is no compile-time or documentation note for CI maintainers that bench_get_headers is meaningless on debug/sanitizer builds even if someone adds it to `make check` in the future. The current Makefile.am correctly keeps it in EXTRA_PROGRAMS only; however, the skip path in main() is reached only if someone invokes the binary directly on a sanitizer build, which the Makefile bench rule would do if the binary is ever promoted. There is no test that validates the skip message is emitted — the skip is silent to the build system (exit 0), so a misconfigured CI that calls bench on a sanitizer build would silently pass without validating anything.
   *Recommendation:* Consider printing a machine-parseable prefix like 'SKIP:' that a harness can distinguish from a genuine PASS, and document in test/PERFORMANCE.md that bench results from sanitizer builds must be ignored. The current 'skipped (sanitizer build…)' message is a good start; adding a 'SKIP:' sentinel prefix would make scripted detection unambiguous.

39. [ ] **test-quality-reviewer** | `test/bench_sizeof_http_resource.cpp:83` | naming-convention
   The file is named bench_sizeof_http_resource.cpp and is listed under the `bench` target, but its sole content is two static_asserts verified at compile time and an `int main() { return 0; }`. Calling this a 'bench' is a misnomer — it is purely a compile-time regression guard. Running it under `make bench` adds no runtime measurement value: the binary always exits 0 as long as it links, even if the static_asserts were somehow bypassed (they cannot be, but the intent is unclear to future contributors).
   *Recommendation:* Either rename the file to reflect its compile-time-only nature (e.g. check_sizeof_http_resource_static.cpp) and move it to check_PROGRAMS / TESTS so it runs on every `make check`, or add a comment in Makefile.am clarifying that bench_sizeof_http_resource exercises only compilation, not runtime measurement. Moving it to check_PROGRAMS would be the stronger choice — the static_assert is a regression guard that should run in CI, not only on release-mode bench runs.

40. [ ] **test-quality-reviewer** | `test/v1_baseline/measure_v1_get_headers.cpp:179` | non-deterministic
   The one-off v1 measurement tool still uses OUTER=10 (even), so samples_ns[OUTER/2] = samples_ns[5] selects the upper of the two middle elements, not a true median. This is a pre-existing issue in a documentation-only TU (not built by make bench or make check), and the committed constant (760 ns) is already conservative relative to the observed range (756-784 ns), so there is no functional impact on the acceptance gate.
   *Recommendation:* Consider changing OUTER to 11 in measure_v1_get_headers.cpp for consistency with bench_get_headers.cpp, but this is low priority since the TU only runs when a maintainer explicitly re-measures v1 and the result is reviewed manually before committing any updated constant.
