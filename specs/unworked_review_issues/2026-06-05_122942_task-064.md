# Unworked Review Issues

**Run:** 2026-06-05 12:29:42
**Task:** TASK-064
**Total:** 69 (0 critical, 11 major, 58 minor)

## Major

1. [x] **code-quality-reviewer** | `src/cookie.cpp:160` | code-elegance
   The `split_cookie_token` helper (lines 160-185) is defined in the anonymous namespace of cookie.cpp but is never called anywhere. The `parse_cookie_header` function duplicates the same splitting logic inline rather than calling this helper. This is dead code that creates confusion about which parsing path is authoritative.
   *Recommendation:* Either delete `split_cookie_token` entirely and keep the inline logic in `parse_cookie_header`, or refactor `parse_cookie_header` to call `split_cookie_token` and remove the duplicated logic. The former is simpler since `parse_cookie_header` is the only consumer.
   *Worked 2026-07-07 (chore/unworked-major-issues):* Already resolved in the current tree: split_cookie_token no longer exists in src/cookie.cpp.

2. [x] **code-quality-reviewer** | `src/http_response.cpp:232` | test-coverage
   The legacy `do_set_cookie` shim uses `cookies_.insert_or_assign` (map semantics: overwrites duplicate name) but `structured_cookies_.push_back` (vector semantics: appends, allowing duplicates). Calling `with_cookie("sid", "old"); with_cookie("sid", "new")` via the deprecated string path will produce two entries in `structured_cookies_`, emitting two `Set-Cookie: sid=...` headers on the wire — a behavioral change from v1 which silently overwrote. The existing `overwrite_cookie` test in `http_response_test.cpp` only checks `get_cookie()` (which reads the legacy map and sees the overwrite), masking the wire-level regression.
   *Recommendation:* Add a test that calls the legacy `with_cookie` setter twice with the same name and asserts that `get_cookies_parsed().size() == 1` (or 2, if the decision is to allow duplicate Set-Cookie headers). Then either document the new wire behavior explicitly in the header or change `do_set_cookie` to replace rather than append when the name already exists in `structured_cookies_`.
   *Worked 2026-07-07 (chore/unworked-major-issues):* do_set_cookie now replaces the same-name structured cookie in place (case-insensitive via http::header_comparator, matching the legacy map), restoring v1 overwrite wire semantics; unit test overwrite_cookie_single_structured_entry added.

3. [x] **code-quality-reviewer** | `test/unit/cookie_render_test.cpp:457` | test-coverage
   render_throws_when_parsed_name_contains_semicolon is a disjunctive tautology. The test passes IFF (threw == true) OR (no cookie has ';' in its name). Because the parser splits on ';', the second branch of the disjunction is always true regardless of whether the guard fires, so the test can never fail even if the injection guard is removed. The test provides zero enforcement of the security invariant it purports to cover.
   *Recommendation:* Remove the disjunctive lambda entirely. Use parse_cookie_header with a cookie whose name will actually contain a forbidden byte that survives the parser (as render_throws_on_name_with_low_byte_bypassed_by_parser does with space), or document clearly that this test is intentionally a parser-behavior assertion rather than a guard assertion and rename it accordingly.
   *Worked 2026-07-07 (chore/unworked-major-issues):* The disjunctive lambda was already removed (15d5d70); the parser-splits-on-';' property is now mechanically pinned by the new parser_splits_on_semicolon_so_name_cannot_contain_it test.

4. [x] **code-quality-reviewer** | `test/unit/cookie_render_test.cpp:477` | test-coverage
   render_throws_on_name_with_low_byte_bypassed_by_parser relies on parse_cookie_header trimming whitespace from the name token but then leaving the interior space intact ("a b" -> name="a b"). The comment says 'parser trims whitespace around = key, giving name "a b"', but the parser calls trim_ws on the entire token before splitting on '=', so leading/trailing whitespace is removed but an interior space survives. This behaviour is not explicitly documented in parse_cookie_header, meaning the test depends on an undocumented implementation detail that could silently change. If trim_ws were extended to strip all whitespace, the test would silently stop being a useful guard test.
   *Recommendation:* Add a comment referencing the exact parse_cookie_header behaviour relied upon, or build the cookie name directly using a friend/internal mechanism or a different forbidden byte (e.g. TAB or a low control character that is never a delimiter) whose survival through the parser is more obviously guaranteed.
   *Worked 2026-07-07 (chore/unworked-major-issues):* Comment added citing the exact trim_ws() behavior relied on (strips only edge ' '/'\t' per token and around the name; interior bytes preserved).

5. [x] **code-quality-reviewer** | `test/unit/cookie_render_test.cpp:539` | test-coverage
   render_guard_rejects_semicolon_in_parsed_value does not actually test the render guard for ';' in value_. The test body only exercises the with_value setter (which rejects ';' at construction time), not the to_set_cookie_header render-time guard. The test name promises render-guard coverage but delivers setter coverage. The 80-line comment block honestly explains this cannot be achieved via parse_cookie_header, but the name creates a false assurance.
   *Recommendation:* Rename the test to reflect what it actually tests: e.g. with_value_rejects_semicolon_setter_path. The render-guard for ';' in value_ is structurally unreachable via the public API (parse_cookie_header always splits on ';'), so either document this as a known gap with a correctly named test, or add a comment-only placeholder. The current mismatch between name and body is the principal quality issue remaining in this cycle.
   *Worked 2026-07-07 (chore/unworked-major-issues):* Renamed to with_value_rejects_semicolon_setter_path with an honest short note; the render guard's ';'-in-value branch documented as unreachable via the public API.

6. [x] **test-quality-reviewer** | `test/unit/cookie_deprecation_sentinel_test.cpp:48` | missing-test
   The acceptance criterion 'deprecated string-blob path still compiles with a [[deprecated]] warning' is confirmed at compile time by the pragma suppression, but the test comment explicitly acknowledges that the negative side (the warning IS emitted on a flag-free compile) is not asserted by any automated check. This is a gap in the stated AC: there is no compile-negative test, XFAIL, or CI step shown in the test corpus that mechanically verifies the warning fires. If someone accidentally removes [[deprecated]] from the two overloads, nothing in the test suite would catch it.
   *Recommendation:* Add a CMake/Autoconf test target (e.g. a try_compile that checks the compiler exit code when -Werror -Wdeprecated-declarations is supplied WITHOUT the pragma suppression) or an explicit XFAIL negative-compile sentinel similar to the header_hygiene pattern already used in this test suite. The existing cookie_deprecation_sentinel_test.cpp correctly documents the gap in its comments — close it with a mechanism.
   *Worked 2026-07-07 (chore/unworked-major-issues):* Closed with scripts/check-deprecated-cookie-overload.sh: negative-compile gate (-Werror=deprecated-declarations, umbrella-only TU) with a positive control, wired into check-local as lint-deprecated-cookie-overload; mutation-tested by stripping the attribute.

7. [x] **test-quality-reviewer** | `test/unit/cookie_deprecation_sentinel_test.cpp:48` | missing-test
   Iter1 gap still open. The test file's own comment (lines 29-32) acknowledges that the negative compile check — confirming the [[deprecated]] warning IS emitted on a flag-free build — is not mechanically verified. If someone removes [[deprecated]] from the two overloads, nothing in the automated test suite detects it. This acceptance criterion is documented but not enforced.
   *Recommendation:* Add a CMake try_compile() check (or an autoconf AC_COMPILE_IFELSE equivalent) that compiles a minimal TU calling with_cookie(string, string) without -Wno-deprecated-declarations and with -Werror, asserting compilation fails. The existing cookie_header_sentinel_test.cpp pattern of compile-only sentinels shows the project already has infrastructure for this class of test.
   *Worked 2026-07-07 (chore/unworked-major-issues):* Same closure as finding 6: lint-deprecated-cookie-overload negative-compile gate.

8. [x] **test-quality-reviewer** | `test/unit/cookie_render_test.cpp:244` | missing-test
   render_same_site_none_forces_secure (line 244) verifies that SameSite=None auto-coerces Secure when the user did NOT set it. There is no symmetric test confirming that when the user has already called with_secure(true) and then sets SameSite=None, the output does not emit '; Secure; Secure' (double Secure attribute). The production code in append_flag_attributes uses `secure_ || (same_site_ == none)`, which is correct, but the double-emit scenario is a distinct code-path exercise.
   *Recommendation:* Add a test: cookie{}.with_name('sid').with_value('x').with_secure(true).with_same_site(same_site_mode::none).to_set_cookie_header() must contain exactly one occurrence of '; Secure', not two.
   *Worked 2026-07-07 (chore/unworked-major-issues):* Covered by same_site_none_with_explicit_secure_emits_single_secure_token (exact 'sid=x; Secure; SameSite=None' string + count==1 assertion).

9. [x] **test-quality-reviewer** | `test/unit/cookie_render_test.cpp:261` | missing-test
   Iter1 gap still open. render_same_site_none_forces_secure only covers the case where secure_ is false when SameSite=None is set. There is no test for cookie{}.with_name('sid').with_value('x').with_secure(true).with_same_site(same_site_mode::none).to_set_cookie_header(), which exercises the `secure_ || (same_site_ == none)` short-circuit. If a refactor accidentally changes that expression to `secure_ | (same_site_ == none)` (bit-or) or emits the Secure token for each true clause independently, a double-emit would appear — and this test suite would not catch it.
   *Recommendation:* Add: cookie{}.with_name('sid').with_value('x').with_secure(true).with_same_site(same_site_mode::none).to_set_cookie_header() must equal exactly 'sid=x; Secure; SameSite=None' — one '; Secure' token, not two. A std::count of '; Secure' substrings is a simple assertion.
   *Worked 2026-07-07 (chore/unworked-major-issues):* Same coverage as finding 8.

10. [x] **test-quality-reviewer** | `test/unit/http_request_cookies_parsed_test.cpp:97` | missing-test
   The 'never allocates after the first call' acceptance criterion is tested only by checking pointer identity (data() and capacity()) across two calls on the same object (second_call_does_not_reallocate). This detects no-reallocation when the cached state is clean, but it does NOT verify that get_cookies_parsed() makes zero heap allocations when called on a request built from a live MHD connection (the non-test-request path). The cache-built guard (cookies_parsed_cache_built_) on the live path is untested; if the live path erroneously rebuilds the vector on each call, these tests still pass.
   *Recommendation:* Add a test that calls get_cookies_parsed() twice in succession and asserts that no heap allocation happened between the two calls. This can be approximated portably by installing a custom std::pmr memory resource around the second call and asserting it is never invoked. Alternatively, document in the test comment that the live-MHD path is covered by the integration test, and adjust the acceptance criterion language accordingly.
   *Worked 2026-07-07 (chore/unworked-major-issues):* Documented per option (a): the test comment scopes the live-MHD cookies_parsed_cache_built_ branch to integration coverage (comment was present at lines 98-104; verified guard exists in http_request_impl.cpp).

11. [x] **test-quality-reviewer** | `test/unit/http_request_cookies_parsed_test.cpp:97` | missing-test
   Iter1 gap still open. second_call_does_not_reallocate verifies pointer/capacity identity between two calls on a test-request (create_test_request path), which populates cookies_parsed_cache_ via the builder, not through the live MHD_get_connection_values path. The cookies_parsed_cache_built_ guard on the live (non-test) path in detail/http_request_impl.cpp is not exercised by any test. If the live path erroneously drops the built flag or rebuilds the vector on every call, all existing tests still pass.
   *Recommendation:* Either: (a) document explicitly in a test comment that the live-MHD path is only covered by integration tests and annotate the acceptance criterion accordingly, OR (b) add a test that verifies the cache-guard flag is set after the first call. A portable approach is to call get_cookies_parsed() twice on a live request stub and assert that the cookie* data() pointer is identical, mirroring the existing pattern but using a path that exercises the MHD-iteration branch.
   *Worked 2026-07-07 (chore/unworked-major-issues):* Same documentation closure as finding 10.
## Minor

12. [ ] **architecture-alignment-checker** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-064/src/cookie.cpp:160` | pattern-violation
   The `split_cookie_token` / `cookie_token_split` helper (lines 160-185) is defined in the anonymous namespace but is never called. `parse_cookie_header` (line 370) re-implements the same split inline rather than delegating to this helper. The dead code is never compiled out of the TU because it sits in an anonymous namespace, not in a separate translation unit, so it will still be compiled and may confuse future readers who assume the helper is authoritative.
   *Recommendation:* Either remove `cookie_token_split` / `split_cookie_token` entirely, or refactor `parse_cookie_header` to call it so it serves as the single source of truth for the name/value splitting logic. The former (deletion) is simpler and aligns with the project's CCN ceiling policy; the redundancy is the issue, not the algorithm itself.

13. [ ] **architecture-alignment-checker** | `src/http_response.cpp:233` | pattern-violation
   In do_set_cookie, the legacy cookies_ map is populated via insert_or_assign using std::move(key) and std::move(value), but the key string was already passed into new_cookie.with_name(key) / with_value(value) by const-ref on line 233. The move is safe because with_name/with_value take string_view and the std::string lifetime extends to the move — but the arrangement is subtle and relies on the fact that with_name/with_value do not move from their arguments. A future maintainer could accidentally reorder the calls and introduce use-after-move. A local comment pinning the ordering (or passing copies to with_name/with_value and then moving into the map) would harden the invariant without any runtime cost.
   *Recommendation:* Add a brief inline comment after the with_name/with_value calls stating 'key and value are still valid here; the move below is safe because with_name/with_value take string_view', or restructure as: cookie new_cookie; new_cookie.with_name(key).with_value(value); cookies_.insert_or_assign(key, value); structured_cookies_.push_back(std::move(new_cookie)); — passing by value (copies) to insert_or_assign to make the sequence order-independent.

14. [ ] **code-quality-reviewer** | `src/cookie.cpp:313` | code-readability
   The render-time guard loop in to_set_cookie_header() duplicates the logic of validate_name() (both iterate name_ and call is_invalid_name_byte). A small private helper such as check_name_for_render() that both paths could call would eliminate the duplication and make future changes (e.g. extending is_invalid_name_byte to also reject '(') automatically apply to both the setter and the render guard.
   *Recommendation:* Extract a private (or anonymous-namespace) check_name_bytes(std::string_view name) helper that throws with the render-time error message, and call it from to_set_cookie_header(). The setter-level validate_name() can remain separate since it has a different error message contextualising the call site.

15. [ ] **code-quality-reviewer** | `src/cookie.cpp:41` | code-elegance
   `kForbiddenCtrlChars` (line 41) is only used by `validate_attr_param` (line 83). The `validate_name` and `validate_value` functions perform equivalent checks inline with explicit character comparisons rather than reusing this constant, diverging from the DRY principle the codebase otherwise follows. The inconsistency also means future additions to the forbidden-char set would need to be updated in multiple places.
   *Recommendation:* Unify the three validators to share `kForbiddenCtrlChars`. The name validator can additionally check `c <= 0x20` for whitespace control, and the value validator can additionally reject `;`, keeping the base set common.

16. [ ] **code-quality-reviewer** | `src/cookie.cpp:56` | code-readability
   validate_name now accepts a default argument ctx = "cookie::with_name". Default arguments on non-virtual free functions in an anonymous namespace are acceptable, but the parameter type is std::string_view while the body does std::string(ctx) for concatenation. This is correct but a minor inconsistency: the caller context string is always a narrow literal, so storing it as std::string_view is fine, though the repeated conversion in the hot path of every setter call adds a tiny allocation.
   *Recommendation:* Consider accepting ctx as const char* (all callers pass string literals) to avoid the std::string(ctx) heap allocation on every validation failure path. This is a minor optimization and clarity improvement, not a bug.

17. [ ] **code-quality-reviewer** | `src/cookie.cpp:82` | code-elegance
   validate_attr_param now performs two sequential linear scans over the input string_view: one for kForbiddenCtrlChars (find_first_of with a 3-char needle) and one for ';' (find). These could be merged into a single pass with a combined reject-set, or alternatively the ';' check could be folded into a 4-byte kForbiddenAttrChars constant. This is a micro-optimization and readability consideration, not a bug.
   *Recommendation:* Consider combining the two into a single find_first_of call with a 4-byte needle ("\r\n\0;", 4), matching the style of kForbiddenCtrlChars. Alternatively the current two-pass form is acceptable if the separation of concerns (ctrl vs. injection guard) is deliberate.

18. [ ] **code-quality-reviewer** | `src/cookie.cpp:99` | code-elegance
   validate_attr_param now checks control bytes, then semicolons, then commas in three separate if-blocks, each constructing a std::string(setter) for the error prefix. The string construction is repeated three times in a hot validation path. While not a correctness issue, it creates mild duplication. The kForbiddenCtrlChars check already uses find_first_of; the semicolon and comma checks could be combined into a single find_first_of call with a combined reject set to eliminate the repeated string construction.
   *Recommendation:* Consider combining the ';' and ',' checks into a single find_first_of(";,") branch with a combined error message listing both characters, or at minimum refactor to build the std::string(setter) prefix once before the three checks.

19. [ ] **code-quality-reviewer** | `src/detail/http_request_impl.cpp:361` | code-readability
   In `ensure_cookies_parsed_cached`, the test-request path (lines 350-368) builds a synthetic `name=value` string and passes it through `cookie::parse_cookie_header` to construct each cookie entry. The comment says this avoids round-tripping through a wire-formatted string, but it actually does exactly that by constructing `k + "=" + v` and parsing it. The result is more convoluted than directly setting `c.name_` / `c.value_` via the bypass pattern used for parsing, and the round-trip through the parser silently drops cookies whose name contains `=` (which the test builder permits via `cookies_local`).
   *Recommendation:* Assign directly via the two-field bypass (`c.name_ = k; c.value_ = v; cookies_parsed_cached_.push_back(c)`) as the inline comment suggests was the intent, rather than going through the string-format-then-parse detour. Add a test for a cookie name containing `=` on the test-request path to pin this behavior.

20. [ ] **code-quality-reviewer** | `src/http_response.cpp:233` | code-readability
   In do_set_cookie, the structured cookie is constructed with lvalue copies of key and value (new_cookie.with_name(key).with_value(value)) and then key and value are std::moved into insert_or_assign on the next line. This pattern is correct but slightly non-obvious: the copy into the cookie and the move into the map both succeed, but a reader may initially wonder why key/value are not moved into with_name/with_value. The trailing comment 'throws before any map mutation' explains the order constraint but not the copy-then-move choice.
   *Recommendation:* Add a brief comment clarifying that with_name/with_value take by-value and validate — moving key/value into them would leave them in an unspecified state if the map mutations needed to roll back. Alternatively, restructure to copy key/value up front: 'std::string name_copy = key; ...' to make the copy intent explicit.

21. [ ] **code-quality-reviewer** | `src/httpserver/http_response.hpp:408` | code-readability
   The large comment block at lines 399-428 describes the v1 string-blob cookie API design rationale and deferred decision — this is historical context that has now been superseded by TASK-064. The v2 structured-cookie design makes most of this note obsolete, yet it remains in the header and could mislead future readers into thinking the string-pair shape is still the primary API.
   *Recommendation:* Remove or replace the stale v1 cookie API design note with a brief forward-pointer to the TASK-064 structured cookie type and the deprecation timeline.

22. [ ] **code-quality-reviewer** | `test/unit/cookie_render_test.cpp:436` | code-readability
   The block comment preceding render_throws_when_parsed_name_contains_semicolon (lines 432-435) describes the intent of the test correctly but the test body then takes a much wider path that contradicts it: the comment says 'to_set_cookie_header() must detect this and throw', but the test accepts either outcome (throw OR no forbidden name). The mismatch between the intent comment and the implementation increases cognitive load for future readers.
   *Recommendation:* Update the leading comment to accurately describe what the test actually asserts, or fix the test logic to match the stated intent.

23. [ ] **code-quality-reviewer** | `test/unit/cookie_render_test.cpp:511` | test-coverage
   render_guard_rejects_lf_in_parsed_value omits precondition checks (cookies.empty() == false and cookies[0].name() == 'sid') that are present in the CR variant (lines 498-500). If the parser splits on LF (which it does not currently, but might after refactoring), the test would silently pass with threw==false and LT_CHECK_EQ(threw, true) would correctly fail — but with a confusing message. Adding the same preconditions as the CR test would make failures self-diagnosing.
   *Recommendation:* Add the same two precondition assertions (cookies not empty, first cookie name is 'sid') to the LF and NUL variants to match the CR and space-in-name test patterns.

24. [ ] **code-quality-reviewer** | `test/unit/cookie_render_test.cpp:539` | code-readability
   The comment block inside render_guard_rejects_semicolon_in_parsed_value is 22 lines long and reads as a stream-of-consciousness design exploration (multiple abandoned approaches described inline). This violates the clean-code comments rule against redundant noise and makes the test body harder to scan.
   *Recommendation:* Collapse the comment to 3-4 lines that state the conclusion: the parser splits on ';' so ';' cannot appear in value_ via the parse path; the render guard for ';' is verified by code inspection; this test covers the setter path instead. Remove the failed-approach narrative.

25. [ ] **code-quality-reviewer** | `test/unit/cookie_render_test.cpp:570` | test-coverage
   same_site_none_with_explicit_secure_emits_single_secure_token asserts both an exact string equality (LT_CHECK_EQ on out) and then recounts '; Secure' occurrences in a loop. The string equality check already makes the loop redundant — if the exact string matches, the count must be 1. This violates the single-assert-per-test guideline and adds noise.
   *Recommendation:* Remove the while-loop counting block. The exact string match is sufficient and more readable as a single pin.

26. [ ] **code-quality-reviewer** | `test/unit/http_response_cookie_wire_test.cpp:1` | test-coverage
   The acceptance criterion in TASK-064 specifies the exact fluent chain `http_response::ok().with_cookie(cookie{}.with_name("sid").with_secure(true).with_same_site(same_site_mode::strict))` must compile and emit a well-formed `Set-Cookie`. The tests use `http_response::string("body")` rather than `http_response::ok()` (which does not exist — the factory is `http_response::string`). This is benign since `string` is semantically equivalent for this test, but worth noting that there is no `ok()` factory alias as the AC text implies.
   *Recommendation:* Minor: verify the AC phrasing is updated in TASK-064.md to match the actual factory name (`string` or `empty` rather than `ok()`), or add an `ok()` factory alias if desired for readability.

27. [ ] **code-quality-reviewer** | `test/unit/http_response_cookie_wire_test.cpp:226` | test-coverage
   The regression test for the partial-mutation fix (legacy_with_cookie_bad_key_leaves_maps_clean) covers a bad key ('bad;key') but there is no symmetric test for a bad value containing ';'. The new do_set_cookie implementation routes through with_value() before mutating maps, so a semicolon in the value would also throw before any map mutation — but this path is untested at the integration level.
   *Recommendation:* Add a companion test case, e.g. legacy_with_cookie_bad_value_leaves_maps_clean, that calls r.with_cookie("good_key", "bad;value"), asserts it throws, and then asserts both get_cookie("good_key") and get_cookies_parsed() are empty.

28. [ ] **code-simplifier** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-064/src/cookie.cpp:94` | code-structure
   validate_attr_param now performs three sequential find() scans over the same string_view (kForbiddenCtrlChars check at line 83, semicolon check at line 94, comma check at line 105). All three could be collapsed into a single pass with a combined forbidden-character set, reducing the number of string scans from three to one.
   *Recommendation:* Define a combined forbidden set for attribute parameters — e.g., constexpr std::string_view kForbiddenAttrChars(";,\r\n\0", 5) — and scan once with find_first_of. Split the error message based on which character was found if the per-character diagnostic is important, or use a single combined message. This removes two redundant passes over the string.

29. [ ] **code-simplifier** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-064/test/unit/cookie_render_test.cpp:472` | naming
   The test name render_throws_on_name_with_low_byte_bypassed_by_parser is inaccurate: space (0x20) is not a 'low byte' in the commonly understood sense — it is a printable ASCII character. The comment inside the test correctly identifies it as a space (0x20). 'low_byte' is ambiguous and likely means 'control character' or 'byte <= 0x20' to the author but reads as a value qualifier to a reviewer.
   *Recommendation:* Rename to render_throws_on_name_with_space_bypassed_by_parser or render_guard_rejects_space_in_parsed_name for clarity.

30. [ ] **code-simplifier** | `src/cookie.cpp:113` | code-structure
   In format_imf_fixdate, the variables wday, mon, year, mday, hour, minute, sec are all extracted from tm into separate locals before being passed to snprintf. While this avoids calling tm members multiple times, the intermediate locals add 7 lines without aiding clarity — the snprintf call already names the format fields, and the expressions like tm.tm_wday + 1900 are simple. The bounds-clamp logic for wday/mon (lines 113-114) is the only genuinely complex part and could be commented inline.
   *Recommendation:* This is a minor point — the current approach is safe and explicit. If the function is ever touched, consider inlining the trivial extractions (year, mday, hour, minute, sec) directly into the snprintf arguments and keeping only the clamped wday/mon as named intermediates:
  const int wday = (tm.tm_wday >= 0 && tm.tm_wday < 7) ? tm.tm_wday : 0;
  const int mon  = (tm.tm_mon  >= 0 && tm.tm_mon  < 12) ? tm.tm_mon  : 0;
  std::snprintf(buf, sizeof(buf), "%s, %02d %s %04d %02d:%02d:%02d GMT",
                kDayNames[wday], tm.tm_mday, kMonthNames[mon],
                tm.tm_year + 1900, tm.tm_hour, tm.tm_min, tm.tm_sec);

31. [ ] **code-simplifier** | `src/cookie.cpp:314` | code-structure
   The render-time value_ guard in to_set_cookie_header() duplicates the exact same four-byte loop body that validate_value() already implements. The only difference is the error message context string. A validate_value(std::string_view v, std::string_view ctx) overload (the same pattern just applied to validate_name) would unify both sites and keep the rejection set in one place.
   *Recommendation:* Give validate_value a ctx parameter with a default of "cookie::with_value", just like validate_name was updated. Then replace the open-coded loop in to_set_cookie_header with validate_value(value_, "cookie::to_set_cookie_header"). The error message wording for the render-time path would need a small update (currently it says 'do not reflect cookies returned by parse_cookie_header() without re-constructing them via with_value()'), but that message can be kept by either passing it as a third parameter or accepting a minor wording change.

32. [ ] **code-simplifier** | `src/detail/http_request_impl.cpp:360` | code-structure
   In ensure_cookies_parsed_cached (test-request path), the round-trip through parse_cookie_header to build a single-entry vector from a local key+value pair (line 361: cookie::parse_cookie_header(k + std::string("=") + v)) allocates a temporary concatenated string, invokes the full parser on a trivially-structured single-token string, and then picks only the first element. The comment acknowledges this is a workaround but the inefficiency and cognitive overhead of re-encoding and re-parsing a known-good pair is notable.
   *Recommendation:* Directly populate via the private members (as do_set_cookie_struct does) or provide a named constructor/factory for (name, value) pairs that bypasses wire-format parsing. Since http_request_impl is an internal class with access to cookie's private members via friendship or a dedicated factory, a direct assignment (c.name_ = k; c.value_ = v) would be cleaner and faster. If direct access is unavailable, at minimum extract the concatenation into a helper to document the intent.

33. [ ] **code-simplifier** | `src/http_response.cpp:233` | naming
   In do_set_cookie the local variable 'c' is a single-letter name for a cookie object. The surrounding code (do_set_cookie_struct, ensure_cookies_parsed_cached) uses longer names like 'cookie c' or 'parsed'. While 'c' is a common C++ idiom for a cookie in a tight loop, inside do_set_cookie it is the primary value being constructed and the name obscures intent.
   *Recommendation:* Rename to 'new_cookie' for self-documentation:
  cookie new_cookie;
  new_cookie.with_name(std::move(key)).with_value(std::move(value));
  structured_cookies_.push_back(std::move(new_cookie));

34. [ ] **code-simplifier** | `src/http_response.cpp:509` | code-structure
   challenge.append(" realm=\"", 8) still uses the explicit byte-count form. This is the only remaining explicit-count append call after the iter1 cleanup in cookie.cpp. The count (8) is silently wrong if the literal is ever edited and provides no performance benefit—the compiler derives the same length from the literal at compile time.
   *Recommendation:* Replace with the single-argument form: challenge.append(" realm=\""); — identical behaviour, no magic number.

35. [ ] **code-simplifier** | `test/unit/cookie_render_test.cpp:494` | code-structure
   The three render-guard tests for CR, LF, and NUL in parsed value (render_guard_rejects_cr_in_parsed_value, render_guard_rejects_lf_in_parsed_value, render_guard_rejects_nul_in_parsed_value) are structurally identical: parse a crafted header, assert non-empty, assert name matches, then assert throw. The only variation is the injected byte. This is ~15 lines of boilerplate repeated three times. While test duplication is sometimes acceptable for pin tests, the repetition is notable.
   *Recommendation:* Consider extracting a local helper lambda or a parameterised helper function within the test file: e.g., auto assert_render_guard_throws = [](std::string header) { auto v = cookie::parse_cookie_header(header); LT_CHECK_EQ(!v.empty(), true); bool threw = false; try { (void)v[0].to_set_cookie_header(); } catch (const std::invalid_argument&) { threw = true; } LT_CHECK_EQ(threw, true); }; Each of the three tests then becomes two lines. This does not change the assertions, only reduces repetition.

36. [ ] **code-simplifier** | `test/unit/cookie_render_test.cpp:539` | code-structure
   render_guard_rejects_semicolon_in_parsed_value has a 20-line comment block explaining why a ';' cannot actually be injected through parse_cookie_header, followed by a test that only exercises the with_value setter path. The comment is accurate but it obscures the actual assertion being made. The test name ('render_guard_rejects_semicolon_in_parsed_value') implies a render-guard test, but the body tests the setter, creating a misleading mismatch.
   *Recommendation:* Rename the test to with_value_rejects_semicolon_setter_path (or similar) to match what it actually tests, and condense the comment to one or two lines explaining the coverage gap. The existing with_value_rejects_semicolon test (line 118) already covers this path, so consider whether this test adds coverage or is redundant.

37. [ ] **housekeeper** | `specs/architecture/04-components/http-request.md:11` | documentation-stale
   The 'Interfaces' list in http-request.md still names `get_cookies()` (the deprecated legacy accessor) in the plain-accessor bullet alongside `get_header`, `get_cookie`, etc., without flagging it as deprecated. The new `get_cookies_parsed()` is correctly described in the dedicated paragraph two lines below, but a reader skimming the interface list sees the old name without any deprecation signal.
   *Recommendation:* Annotate the `get_cookies()` entry in the interface list with a `[[deprecated]]` note or remove it in favour of the prose description of `get_cookies_parsed()` to match the actual public API.

38. [ ] **performance-reviewer** | `src/cookie.cpp:370` | missing-caching
   parse_cookie_header() re-implements the cookie-token splitting logic (find '=', trim name, strip DQUOTE from value) inline rather than calling the split_cookie_token() helper that exists in the same anonymous namespace (line 166). This is not a runtime cost issue but means any future optimization to split_cookie_token() would need to be duplicated. The live-request caching path itself (ensure_cookies_parsed_cached) is correct — parse is called once and cached.
   *Recommendation:* Refactor parse_cookie_header() to call split_cookie_token() for the per-token decomposition step, eliminating the duplicate logic. This also reduces the branch count of parse_cookie_header and makes future correctness fixes apply to both code paths automatically.

39. [ ] **performance-reviewer** | `src/cookie.cpp:82` | memory-allocation
   validate_attr_param now performs two find operations on the string_view (one for kForbiddenCtrlChars via find_first_of, one for ';' via find). Both are O(n) over the attribute length, which is tiny in practice (domain/path strings are short). No allocation is involved on the success path. On the failure path, std::string(setter) allocates to build the error message — acceptable since exceptions are rare.
   *Recommendation:* No action required. The two-pass scan is correct and the strings are short. If micro-benchmarking proves these two scans measurable (unlikely), they could be merged into a single pass with a combined reject set, but this would obscure the CWE-113 commentary.

40. [ ] **performance-reviewer** | `src/cookie.cpp:85` | memory-allocation
   validate_attr_param() constructs `std::string(setter) + ": attribute value contains ..."` on the error path. The `setter` parameter is a std::string_view; constructing a std::string from it allocates on every call that hits the error branch. This is error-path-only and therefore not hot, but the allocation is avoidable.
   *Recommendation:* Use a string literal concatenation approach or pass the complete message as a single string_view literal array. Alternatively, use fmt-style or throw with a pre-built std::string assembled via snprintf into a stack buffer. Since this only fires on invalid input, the impact is negligible — document it as accepted cost.

41. [ ] **performance-reviewer** | `src/detail/http_request_impl.cpp:361` | memory-allocation
   Test-request path in ensure_cookies_parsed_cached() synthesizes a wire-format string via `k + std::string("=") + v` and then calls cookie::parse_cookie_header(), which allocates a std::vector<cookie> and returns it, just so the caller can move-out its first element. Two transient heap objects (the concatenated string + the returned vector) are created per cookie entry on the test-request path. While the live-request path (line 378) is clean — it calls parse_cookie_header once on the raw Cookie header and caches the result directly — the test path is unnecessarily allocation-heavy.
   *Recommendation:* On the test-request branch, construct the cookie directly without the round-trip through wire formatting: `cookie c; c.name_ = k; c.value_ = v; cookies_parsed_cached_.push_back(std::move(c));`. Because name_/value_ are private, either add a static factory `cookie cookie::from_name_value(string_view name, string_view value)` that bypasses validators (matching the existing comment about bypassing strict validators for test input), or promote the two-field direct-assign into a private static helper used only by this path. This eliminates the two heap allocations per entry on the test path.

42. [ ] **performance-reviewer** | `src/detail/http_request_impl.cpp:361` | memory-allocation
   ensure_cookies_parsed_cached (test-request path) builds a temporary wire-format string via k + std::string("=") + v for each cookie entry, then parses it back with parse_cookie_header which allocates a std::vector<cookie> per call. This double-alloc-and-parse pattern runs once per unique test cookie key, but only on the first call to get_cookies_parsed() on a test request. Not introduced by the two fix commits — pre-existing — and only affects test infrastructure, not production paths.
   *Recommendation:* Not a production concern. A future cleanup could build cookie objects directly (assigning c.name_ / c.value_ via direct member assignment, as the live-request path does) to avoid the round-trip string formatting and re-parse. Tracked for informational purposes only.

43. [ ] **performance-reviewer** | `src/detail/webserver_request.cpp:274` | memory-allocation
   decorate_mhd_response() calls c.to_set_cookie_header() for every structured cookie on every response that is materialized. to_set_cookie_header() always allocates a new std::string (reserve + appends). For a response with N cookies being sent at high throughput, this is N allocations per response cycle. The rendered string is immediately passed to MHD_add_response_header and then discarded; there is no caching of the rendered header value on the cookie struct itself.
   *Recommendation:* For the common single-cookie case this is acceptable. If profiling reveals it as hot, consider adding a mutable cached_header_ field on cookie that is populated lazily by to_set_cookie_header() and invalidated by any setter call — or, since cookie is a value type, let the caller cache the rendered string. For the current v2.0 scope this is a minor improvement opportunity rather than a blocking issue.

44. [ ] **performance-reviewer** | `src/http_response.cpp:232` | memory-allocation
   do_set_cookie (legacy path) now constructs a temporary cookie object via with_name/with_value before insert_or_assign, adding one stack-allocated cookie construction plus two std::string moves on the happy path. For the common case this is negligible, but callers who use the deprecated with_cookie(name, value) overload in a tight loop (e.g. appending dozens of session cookies per response) now pay validation + cookie construction + push_back on every call rather than the previous insert_or_assign-only cost. The push_back into structured_cookies_ will trigger a vector reallocation once capacity is exceeded.
   *Recommendation:* Acceptable trade-off for correctness (the partial-mutation bug was worse). If profiling shows contention on the cookie-append path, consider reserving structured_cookies_ capacity at construction time or deferring the structured vector push until the response is serialized. For new code, callers should migrate to with_cookie(cookie{}.with_name(...).with_value(...)) which takes ownership directly and skips the legacy mirror entirely.

45. [ ] **security-reviewer** | `src/cookie.cpp:351` | insecure-design
   SameSite=None auto-coerces Secure at render time (append_flag_attributes, line 353-355) but the is_secure() getter still returns false if the caller did not explicitly call with_secure(true). This is a surprising discrepancy: a cookie object where is_secure() == false can nonetheless emit 'Secure' on the wire. Code inspecting is_secure() for security policy enforcement (e.g., 'only transmit this cookie over TLS') will get a false negative for SameSite=None cookies. The draft-west-cookie-incrementalism requirement is satisfied on the wire, but not in the object model.
   *Recommendation:* Consider two options: (1) have with_same_site(none) also set secure_ = true so is_secure() accurately reflects the effective state, or (2) add a is_effective_secure() accessor that returns (secure_ || same_site_ == same_site_mode::none) and document that callers should use this instead of is_secure() when checking effective Secure status. Option 1 is simpler and avoids the discrepancy entirely.

46. [ ] **security-reviewer** | `src/cookie.cpp:370` | insecure-design
   parse_cookie_header bypasses all cookie validators (comment at lines 407-412 acknowledges this). On the request side this is correct by design -- request cookies are untrusted input and the parser must be lenient. However, parsed cookies can be re-emitted via to_set_cookie_header() if a caller reflects parsed request cookies back into a response (e.g., cookie mirroring). In that case, a malicious client could craft a Cookie: header containing ';' in a cookie name (e.g. 'a;Secure=') and have it reflected into a Set-Cookie response header, since parse_cookie_header stores the raw bytes into name_ without sanitization (line 411). The rendering code does not re-validate name_ before appending it.
   *Recommendation:* Add a note in the parse_cookie_header documentation warning that parsed cookies bypass validation and MUST NOT be passed directly back to http_response::with_cookie without first re-validating via cookie::with_name/with_value. Alternatively, have to_set_cookie_header() re-run name validation before rendering. This is a design documentation gap that could produce a real injection if callers mirror cookies naively.

47. [ ] **security-reviewer** | `src/cookie.cpp:83` | injection
   validate_attr_param checks for kForbiddenCtrlChars (CR, LF, NUL) in domain and path attribute values, but does not check for the comma character. RFC 6265 §4.1 defines the path-value and domain-value productions as *av-octet, where av-octet excludes CTLs and ';'. A comma in a Domain value is unusual but syntactically legal per RFC 6265 yet can confuse older HTTP/1.0 multi-value header parsers that split on commas. More importantly, the Domain attribute RFC 6265 §4.1.2.3 says it must be a dot-prefixed domain string; free-form input like 'evil.com, other.com' is not a valid domain but passes the current validator.
   *Recommendation:* Consider adding domain-specific validation that checks the value is a syntactically valid hostname (or dot-prefixed hostname). At minimum, document that the caller is responsible for ensuring the domain value is a well-formed hostname. This is a defense-in-depth hardening, not an exploitable injection under current rendering since the only forbidden character in Set-Cookie av-value is ';'.

48. [ ] **security-reviewer** | `test/unit/cookie_render_test.cpp:539` | injection
   render_guard_rejects_semicolon_in_parsed_value does not exercise the render-time guard for ';' in value_ via the parse-then-render reflection path. The parser splits Cookie headers on ';' before constructing any cookie object, so parse_cookie_header() cannot produce a cookie with ';' in value_ via its normal code path. The test therefore falls back to verifying that with_value("ab;cd") throws (setter path), which only confirms the existing validate_value() guard — not the new render-time guard added in to_set_cookie_header(). The three companion tests (CR/LF/NUL) do correctly drive the render guard via parse-then-render reflection and cover the same loop body, so the guard code is exercised. The ';' branch of the same loop is not independently pinned by a reflection test. Exploitation is not possible via the normal API (value_ cannot reach the render guard containing ';' through parse_cookie_header), so this is a defense-in-depth gap only.
   *Recommendation:* Add a comment to the test explicitly acknowledging that the ';' render-guard branch in to_set_cookie_header() is not reachable via parse_cookie_header() in normal operation (the parser always splits on ';'), and is therefore verified by code inspection. Alternatively, add a dedicated unit test that directly sets c.value_ via a friend or test-only accessor containing ';' and confirms to_set_cookie_header() throws, to mechanically pin the guard for all four forbidden bytes in value_. The existing CR/LF/NUL tests provide sufficient assurance that the loop runs correctly.

49. [ ] **spec-alignment-checker** | `specs/tasks/M7-v2-cleanup/TASK-064.md:22` | acceptance-criteria
   The acceptance criterion example uses `http_response::ok().with_cookie(...)` but `http_response::ok()` is not a public factory; the correct equivalent is `http_response::string("").with_cookie(...)` or `http_response::empty().with_cookie(...)`. All tests use the correct factories, so this is a spec-text-only imprecision rather than an implementation gap.
   *Recommendation:* Update the acceptance criterion text in TASK-064.md to use `http_response::string("body")` (or `http_response::empty()`) instead of `http_response::ok()`, which does not exist in the v2 factory set.

50. [ ] **spec-alignment-checker** | `src/cookie.cpp:238` | ears-requirement
   PRD-CKY-REQ-007 states that http_response::with_cookie(cookie) shall append the structured cookie 'without copying the cookie object'. The implementation does move the cookie object (structured_cookies_.push_back(std::move(c))), satisfying the no-copy requirement on the cookie object itself. However, do_set_cookie_struct also calls cookies_.insert_or_assign(c.name(), c.value()) before the move, which copies the name and value strings into the legacy mirror map. This is a copy of the cookie's string members (not the cookie object itself), so it is technically spec-compliant, but a reader of PRD-CKY-REQ-007 might expect zero-copy on all string content too. The legacy mirror is necessary to back the deprecated get_cookie/get_cookies accessors per PRD-CKY-REQ-009.
   *Recommendation:* No code change required. Optionally add a comment in do_set_cookie_struct noting that the string copy into cookies_ is the intentional cost of maintaining the deprecated-accessor mirror, and is distinct from the PRD-CKY-REQ-007 'without copying the cookie object' constraint which refers to the cookie value type, not its string members.

51. [ ] **spec-alignment-checker** | `src/cookie.cpp:370` | specification-gap
   The parse_cookie_header implementation in cookie.cpp duplicates the DQUOTE-stripping and trim_ws logic inline (lines 394-403) instead of reusing the split_cookie_token() helper defined in the same anonymous namespace (lines 166-185). Both paths are functionally equivalent, but maintaining two slightly different implementations of the same logic creates a future divergence risk.
   *Recommendation:* Refactor parse_cookie_header to call split_cookie_token() directly, replacing the inline DQUOTE-strip and name-trim logic with the dedicated helper to keep the parsing logic in a single place.

52. [ ] **spec-alignment-checker** | `src/httpserver/http_request.hpp:316` | specification-gap
   The API-CKY 'In scope' narrative says 'legacy get_cookie(string), get_cookies(), and with_cookie(string, string) are [[deprecated]]' without qualifying which class, which could be read as covering http_request::get_cookie() and http_request::get_cookies() in addition to the response-side methods. PRD-CKY-REQ-009 resolves the ambiguity explicitly (lists only http_response methods), so the omission of [[deprecated]] on http_request::get_cookie and http_request::get_cookies is spec-compliant. However, the 'In scope' prose remains misleading because a reader who stops there would expect both surfaces to be deprecated.
   *Recommendation:* Clarify the 'In scope' transitional-policy bullet in specs/product_specs.md §3.5.1 to explicitly scope the deprecation to http_response: 'legacy http_response::get_cookie(string_view), http_response::get_cookies(), and http_response::with_cookie(string, string) are [[deprecated]]'. No code change required.

53. [ ] **test-quality-reviewer** | `test/unit/cookie_header_sentinel_test.cpp:45` | naming-convention
   cookie_is_class_type contains a static_assert and then LT_CHECK_EQ(std::is_class_v<cookie>, true) — the runtime check is redundant with the compile-time static_assert. If the static_assert fires, the TU does not compile; if it does not fire, the runtime check trivially passes. The pattern adds noise without catching any additional bugs.
   *Recommendation:* Remove the redundant LT_CHECK_EQ(std::is_class_v<cookie>, true) line (and its equivalents in same_site_mode_enum_values_distinct, cookie_is_copy_and_move_constructible, cookie_is_default_constructible_noexcept). Keep only the static_asserts. This applies to all four tests in cookie_header_sentinel_test.cpp that follow this pattern.

54. [ ] **test-quality-reviewer** | `test/unit/cookie_header_sentinel_test.cpp:45` | redundant-test
   Iter1 gap still open (minor). Four tests (cookie_is_class_type, same_site_mode_enum_values_distinct, cookie_is_copy_and_move_constructible, cookie_is_default_constructible_noexcept) each contain both a static_assert and an LT_CHECK_EQ(true, true) or LT_CHECK_EQ(std::is_class_v<cookie>, true). If the static_assert fires, the TU does not compile and the runtime check is never reached; if it does not fire, the runtime check trivially passes and adds nothing. The pattern adds maintenance noise without catching any additional bugs.
   *Recommendation:* Remove the redundant LT_CHECK_EQ runtime assertions from all four tests. Keep only the static_asserts. If the littletest framework requires at least one runtime check per test, use LT_CHECK_EQ(true, true) with a comment explaining it is a framework-required no-op.

55. [ ] **test-quality-reviewer** | `test/unit/cookie_header_sentinel_test.cpp:45` | redundant-test
   Iter2 gap still open (minor). Four tests (cookie_is_class_type, same_site_mode_enum_values_distinct, cookie_is_copy_and_move_constructible, cookie_is_default_constructible_noexcept) each contain both a static_assert and a redundant LT_CHECK_EQ(true, true) or LT_CHECK_EQ(std::is_class_v<cookie>, true). The runtime check adds nothing beyond the static_assert.
   *Recommendation:* Remove the redundant LT_CHECK_EQ runtime assertions from the four tests. Keep only the static_asserts. If the littletest framework requires at least one runtime check per test, replace with LT_CHECK_EQ(true, true) and a comment explaining it is a framework-required no-op.

56. [ ] **test-quality-reviewer** | `test/unit/cookie_header_sentinel_test.cpp:45` | redundant-test
   Iter3-8 still open. cookie_is_class_type, same_site_mode_enum_values_distinct, cookie_is_copy_and_move_constructible, and cookie_is_default_constructible_noexcept each contain a static_assert (the real check) followed by a redundant LT_CHECK_EQ(true, true) or LT_CHECK_EQ(std::is_class_v<cookie>, true). The runtime check adds no regression protection beyond the static_assert.
   *Recommendation:* Remove the redundant runtime assertions or replace with a single LT_CHECK_EQ(true, true) no-op if the littletest framework requires at least one runtime check per test, with a comment explaining it is framework-required.

57. [ ] **test-quality-reviewer** | `test/unit/cookie_render_test.cpp:287` | missing-test
   render_throws_when_name_empty tests that to_set_cookie_header() throws when name is empty. There is no symmetric test that with_name('') (empty string passed to the setter) either throws or is accepted — the validator in validate_name only rejects bytes <= 0x20, 0x7F, ';', and '=', which means an empty name string would pass validation but still fail at render time. This two-stage behaviour is surprising and worth a test pin.
   *Recommendation:* Add a test: cookie{}.with_name('') should either throw std::invalid_argument immediately OR result in a cookie whose to_set_cookie_header() throws, with the test documenting which invariant the implementation guarantees.

58. [ ] **test-quality-reviewer** | `test/unit/cookie_render_test.cpp:299` | missing-test
   RFC 6265 round-trip test coverage focuses on the exact §4.1 example (SID + Path + Secure + HttpOnly). There is no round-trip test for a cookie with all time-related attributes — specifically Expires + Max-Age together, round-tripped through parse_cookie_header. The combination is tested for rendering (render_both_expires_and_max_age), but the request-side parse of the resulting name=value token after the first semicolon is only implicitly covered.
   *Recommendation:* Add a round-trip test that: (1) renders a cookie with Expires + Max-Age, (2) extracts the first semicolon-delimited token, (3) feeds it to parse_cookie_header, and (4) asserts name and value survive intact. The existing roundtrip_first_token_is_name_value test covers the shape but not this attribute combination.

59. [ ] **test-quality-reviewer** | `test/unit/cookie_render_test.cpp:436` | logic-in-test
   render_throws_when_parsed_name_contains_semicolon contains a for-loop, a lambda with a for-loop inside it, and a conditional short-circuit expression as the assertion argument. Even setting aside the always-green problem (finding 1), this structure violates the no-logic-in-tests rule and makes the test intent hard to read.
   *Recommendation:* If the test is retained after fixing finding 1, extract the precondition check and the expected-throw check into linear, named steps with one assertion per concern.

60. [ ] **test-quality-reviewer** | `test/unit/cookie_render_test.cpp:45` | naming-convention
   with_name_returns_lvalue_ref_on_lvalue and with_name_returns_rvalue_ref_on_rvalue follow the same redundant static_assert + LT_CHECK_EQ(true, true) pattern. The runtime body adds nothing: the compile-time static_assert is the real test.
   *Recommendation:* Replace the LT_CHECK_EQ(true, true) stubs with a single static_assert-only test body. If the littletest framework requires at least one runtime assertion per test, use a compile-time sentinel comment explaining why.

61. [ ] **test-quality-reviewer** | `test/unit/cookie_render_test.cpp:539` | naming-convention
   render_guard_rejects_semicolon_in_parsed_value is named as a render-guard test but actually exercises the with_value setter rejection path (cookie{}.with_name('sid').with_value('ab;cd')). The body's own comment admits that a ';' in value_ cannot be injected via parse_cookie_header, so the render-guard code path for ';' is not reached. The test verifies valid behavior (setter rejects semicolon) but under a misleading name that implies render-time guard coverage.
   *Recommendation:* Rename to with_value_rejects_semicolon_setter_path (or similar) and move it next to the other with_value setter tests around line 118. Add a comment documenting that the render-guard ';' branch is verified by code inspection only (same loop as the CR/LF/NUL branches already tested).

62. [ ] **test-quality-reviewer** | `test/unit/cookie_render_test.cpp:580` | logic-in-test
   same_site_none_with_explicit_secure_emits_single_secure_token contains a while-loop (lines 582-586) counting occurrences of '; Secure' in the output string. The exact-string assertion on line 579 (LT_CHECK_EQ(out, std::string('sid=x; Secure; SameSite=None'))) already fully pins the output, making the loop dead confirmation — if the string matches exactly, '; Secure' occurs exactly once by definition. The loop adds no regression protection, violates the no-logic-in-tests rule, and obscures the test's intent.
   *Recommendation:* Remove the while-loop and the count assertion. The exact-string equality assertion on line 579 is sufficient and self-documenting.

63. [ ] **test-quality-reviewer** | `test/unit/http_request_cookies_parsed_test.cpp:63` | missing-test
   one_per_cookie_set_via_builder only asserts v.size() == 2. It does not verify the ordering (that 'a' comes before 'b') or the actual name/value content of each entry. If the implementation re-ordered entries or swapped names and values, this test would pass.
   *Recommendation:* Add LT_CHECK_EQ(v[0].name(), std::string('a')) and LT_CHECK_EQ(v[1].name(), std::string('b')) to pin insertion-order and identity, mirroring the pattern already used in preserves_name_and_value.

64. [ ] **test-quality-reviewer** | `test/unit/http_request_cookies_parsed_test.cpp:63` | missing-test
   Iter1 gap still open (minor). one_per_cookie_set_via_builder asserts only v.size() == 2. It does not verify name or value content of either entry, so a swapped-order or swapped-name-value implementation would still pass.
   *Recommendation:* Add LT_CHECK_EQ(v[0].name(), std::string("a")) and LT_CHECK_EQ(v[1].name(), std::string("b")) to pin insertion order and identity, consistent with the preserves_name_and_value test directly below.

65. [ ] **test-quality-reviewer** | `test/unit/http_request_cookies_parsed_test.cpp:63` | missing-test
   Iter2 gap still open (minor). one_per_cookie_set_via_builder asserts only v.size() == 2. It does not verify name or value content of either entry, so a swapped-order or swapped-name-value implementation would still pass.
   *Recommendation:* Add LT_CHECK_EQ(v[0].name(), std::string("a")) and LT_CHECK_EQ(v[1].name(), std::string("b")) to pin insertion order and identity, consistent with the preserves_name_and_value test directly below.

66. [ ] **test-quality-reviewer** | `test/unit/http_request_cookies_parsed_test.cpp:63` | missing-test
   Iter3-7 still open. one_per_cookie_set_via_builder asserts only v.size() == 2. It does not verify name or value content of either entry, so a swapped-order or swapped-name-value implementation would still pass. The directly following preserves_name_and_value test covers the single-cookie name/value contract but not the two-cookie insertion-order case.
   *Recommendation:* Add LT_CHECK_EQ(v[0].name(), std::string("a")) and LT_CHECK_EQ(v[1].name(), std::string("b")) (and matching value checks) to pin insertion order and identity.

67. [ ] **test-quality-reviewer** | `test/unit/http_response_cookie_wire_test.cpp:81` | redundant-test
   with_cookie_struct_lvalue_returns_lvalue_ref and with_cookie_struct_rvalue_returns_rvalue_ref follow the same static_assert-only pattern as the cookie_header_sentinel tests: static_assert is the real check; LT_CHECK_EQ(true, true) adds no coverage.
   *Recommendation:* Same as finding 5: remove the LT_CHECK_EQ(true, true) stubs or replace with a comment acknowledging the framework requirement.

68. [ ] **test-quality-reviewer** | `test/unit/http_response_cookie_wire_test.cpp:81` | redundant-test
   Iter2 gap still open (minor). with_cookie_struct_lvalue_returns_lvalue_ref and with_cookie_struct_rvalue_returns_rvalue_ref follow the same static_assert-only pattern as the cookie_header_sentinel tests: static_assert is the real check; LT_CHECK_EQ(true, true) adds no coverage.
   *Recommendation:* Same as finding 8: remove the LT_CHECK_EQ(true, true) stubs or replace with a comment acknowledging the framework requirement.

69. [ ] **test-quality-reviewer** | `test/unit/http_response_cookie_wire_test.cpp:81` | redundant-test
   Iter3-9 still open. with_cookie_struct_lvalue_returns_lvalue_ref, with_cookie_struct_rvalue_returns_rvalue_ref, and get_cookies_parsed_return_type_is_const_vector_ref follow the same static_assert-only pattern: static_assert is the real check; LT_CHECK_EQ(true, true) adds no coverage.
   *Recommendation:* Same as finding 4: remove or replace with a documented no-op comment.
