# Unworked Review Issues

**Run:** 2026-05-07 09:27:58
**Task:** TASK-019
**Total:** 31 (0 critical, 1 major, 30 minor)

## Major

1. [x] **code-simplifier** | `src/http_request.cpp:518` | code-structure
   The Subject DN and Issuer DN extraction in populate_all_cert_fields (lines 518-540) are structurally identical two-call GnuTLS blocks: size-probe, allocate, fill, strip trailing NUL, assign to pmr::string. This pattern repeats twice verbatim with only the GnuTLS function name and destination field differing. The Common Name block (lines 542-553) is a third near-copy. Repeated logic is a needless repetition code smell and makes future changes (e.g. error handling) error-prone because all copies must be updated in sync.
   *Recommendation:* Extract a static file-scope helper inside the #ifdef HAVE_GNUTLS block, for example: `static std::string extract_dn(gnutls_x509_crt_t cert, int(*getter)(gnutls_x509_crt_t, char*, size_t*))`. Call it as `auto s = extract_dn(cert.get(), gnutls_x509_crt_get_dn); if (!s.empty()) client_cert_dn.assign(s.data(), s.size());`. The CN case differs slightly (uses get_dn_by_oid with extra arguments and a pre-check for cn_size > 0) but the size-probe + fill + NUL-strip idiom can still be shared with a more general helper taking a `std::function` or a lambda.
   *Status:* Resolved — populate_all_cert_fields() lives in src/detail/http_request_impl_tls.cpp now; the DN/issuer-DN pattern is factored into `extract_x509_string()` (lines 166-176) using the `x509_string_getter` typedef, and CN/fingerprint each have their own dedicated helper.

## Minor

2. [x] **architecture-alignment-checker** | `src/httpserver/http_utils.hpp:50` | pattern-violation
   http_utils.hpp includes <microhttpd.h> unconditionally at the public-header level. Per §4.1 (webserver component) and §4.2 (http_request component), MHD types are supposed to appear only in detail/ headers and .cpp files. This is a pre-existing issue not introduced by TASK-019, but the TASK-019 comment on line 28 explicitly calls out the pattern of keeping backend headers out of public headers, making the contrast visible.
   *Recommendation:* This pre-existing violation is outside TASK-019 scope. Track it as a separate cleanup task to move the <microhttpd.h> dependency behind HTTPSERVER_COMPILATION. TASK-019 itself correctly excludes <gnutls/gnutls.h> from http_utils.hpp.
   *Status:* Resolved — `<microhttpd.h>` is no longer included in src/httpserver/http_utils.hpp; only the comment at line 140 references it as historically reachable from the impl TU.

3. [x] **code-quality-reviewer** | `src/http_request.cpp:494` | code-elegance
   populate_all_cert_fields() calls has_tls_session() then get_tls_session() as two separate MHD_get_connection_info lookups. get_tls_session() already handles the null-connection case and returns nullptr, making the outer has_tls_session() guard partially redundant.
   *Recommendation:* Simplify to `gnutls_session_t session = get_tls_session();` and branch on `session != nullptr`, eliminating the separate has_tls_session() call inside this function.
   *Status:* Resolved — populate_all_cert_fields() in src/detail/http_request_impl_tls.cpp:233 now calls `get_tls_session()` once and branches on `session != nullptr`; the redundant has_tls_session() call is gone (comment at lines 228-232 documents the dedupe).

4. [x] **code-quality-reviewer** | `src/http_request.cpp:519` | code-elegance
   The GnuTLS two-call DN/CN extraction pattern (first call with nullptr to get size, second to fill a std::string) allocates a default-resource std::string on the heap, then assigns via pmr::string::assign(ptr, len). This is repeated three times (DN, issuer DN, CN) with nearly identical code. A small local helper extracting the pattern into a lambda or inline function would reduce duplication and make the intent clearer.
   *Recommendation:* Extract a named lambda or private helper (e.g., `read_gnutls_string`) that takes the GnuTLS getter function pointer and the destination pmr::string& and centralises the two-call pattern, the null-byte stripping, and the assign call. This is a pure readability improvement; the current repetition does not affect correctness.
   *Status:* Resolved — `extract_x509_string()` and `extract_x509_common_name()` helpers in src/detail/http_request_impl_tls.cpp (lines 166-190) centralise the two-call + NUL-strip pattern; the caller boilerplate is now a single .assign() per field.

5. [x] **code-quality-reviewer** | `src/http_request.cpp:519` | code-elegance
   The Subject DN, Issuer DN, and Common Name extraction blocks inside populate_all_cert_fields() are structurally identical two-call GnuTLS patterns (size-probe, allocate, fill, strip trailing NUL, assign). The pattern repeats three times with only the GnuTLS function name and destination field differing.
   *Recommendation:* Extract a file-scope static helper inside the #ifdef HAVE_GNUTLS block, e.g. `static void fill_dn_field(gnutls_x509_crt_t cert, int(*getter)(gnutls_x509_crt_t, char*, size_t*), std::pmr::string& dest)`, and call it for each of the three fields.
   *Status:* Resolved — duplicate of item 4. The `extract_x509_string()` helper covers both DN cases; CN uses its own helper because of the OID/byte-count pre-check.

6. [x] **code-quality-reviewer** | `src/http_request.cpp:557` | code-readability
   The local array `unsigned char fingerprint[32]` uses the magic number 32 for the SHA-256 digest byte length.
   *Recommendation:* Replace with a named constant: `static constexpr std::size_t SHA256_DIGEST_BYTES = 32;` defined in the anonymous namespace inside the #ifdef HAVE_GNUTLS block.
   *Status:* Resolved — `static constexpr size_t SHA256_DIGEST_BYTES = 32;` is defined at src/detail/http_request_impl_tls.cpp:195 and used in the fingerprint buffer declaration.

7. [ ] **code-quality-reviewer** | `src/http_request.cpp:948` | code-readability
   The comment block preceding the TASK-019 public accessor implementations is long and detailed (12 lines). It restates design decisions already captured in the task spec and in the architecture doc. Clean-code principle: avoid redundant comments that restate what the code already shows clearly. The key contract (unconditional declaration, sentinel on no-GnuTLS path, noexcept wrapping pattern) is worth a brief note but the current prose is verbose.
   *Recommendation:* Condense the comment block to 3-4 lines covering only the non-obvious decisions (unconditional public symbols, noexcept try/catch rationale). Remove the sentences that simply paraphrase the function signatures.
   *Status:* wontfix — cosmetic/style preference, no functional impact; pure stylistic preference; existing comment block documents the contract

8. [ ] **code-quality-reviewer** | `src/http_request.cpp:948` | code-readability
   The 12-line comment block preceding the TASK-019 public accessor implementations restates design decisions already captured in the task spec and architecture doc.
   *Recommendation:* Condense to 3-4 lines covering only non-obvious decisions: unconditional public symbols and the noexcept try/catch rationale.
   *Status:* wontfix — cosmetic/style preference, no functional impact; duplicate of #7; pure stylistic

9. [ ] **code-quality-reviewer** | `src/http_request.cpp:964` | code-elegance
   The four string_view accessor bodies (get_client_cert_dn, get_client_cert_issuer_dn, get_client_cert_cn, get_client_cert_fingerprint_sha256) are structurally identical: #ifdef HAVE_GNUTLS, call populate_all_cert_fields(), return std::string_view(impl_->field.data(), impl_->field.size()), #else return std::string_view{}, #endif.
   *Recommendation:* A small private macro or helper (e.g. CERT_STRING_VIEW_ACCESSOR(name, field)) would collapse all four bodies to single-liners, reducing repetition without obscuring intent.
   *Status:* wontfix — cosmetic/style preference, no functional impact; macro vs explicit body is style choice; defer to focused readability pass

10. [x] **code-quality-reviewer** | `src/httpserver/http_utils.hpp:372` | code-readability
   In `arg_comparator::operator()(const std::string& x, std::string_view y)` the call delegates to `operator()(std::string_view(x), std::string(y))`, constructing a heap-allocated `std::string` from a `string_view` argument unnecessarily. The other three overloads consistently delegate to `operator()(string_view, string_view)`. This overload was introduced in a prior task (not this one) but the inconsistency stands out when reading the changed file.
   *Recommendation:* Change line 372 to delegate to `operator()(std::string_view(x), y)` rather than `operator()(std::string_view(x), std::string(y))`. This removes an unnecessary heap allocation and makes all four overloads consistent in delegating to the `(string_view, string_view)` primary. Note: this line is pre-existing and not introduced by TASK-019 — flag as a housekeeping fix opportunity.
   *Status:* Resolved — the overload at src/httpserver/http_utils.hpp:416 now delegates to `operator()(std::string_view(x), y)`; all four overloads route to the `(string_view, string_view)` primary without spurious std::string construction.

11. [x] **code-quality-reviewer** | `test/unit/create_test_request_test.cpp:198` | test-coverage
   build_tls_enabled_no_peer_cert and build_no_client_cert_returns_sentinels both exercise the same test-request path with the same sentinel assertions, creating a redundant test case.
   *Recommendation:* Collapse into a single test; keep build_no_client_cert_returns_sentinels (unconditionally compiled) and extend it with a tls_enabled() builder variant rather than a separate test.
   *Status:* Resolved — build_tls_enabled_no_peer_cert was removed; build_no_client_cert_returns_sentinels at test/unit/create_test_request_test.cpp:231 now covers both the default and the tls_enabled() builder paths (see lines 244-258 and the explanatory comment at lines 229-230).

12. [x] **code-quality-reviewer** | `test/unit/http_request_tls_accessors_test.cpp:1` | test-coverage
   The test file is entirely compile-time assertions (static_asserts) and an empty `main()`. It verifies the public-surface contract (existence, return types, noexcept, absence of the removed accessor) thoroughly, which is the correct approach for a sentinel test. However, there is no runtime test that exercises the noexcept try/catch wrappers in `is_client_cert_verified`, `get_client_cert_not_before`, and `get_client_cert_not_after` under an allocation-failure scenario. The existing `build_no_client_cert_returns_sentinels` test in `create_test_request_test.cpp` covers the happy-path no-cert sentinels, but not the exception-swallowing path.
   *Recommendation:* Consider adding a comment noting that the try/catch sentinel path in the noexcept wrappers is not covered by automated tests (since injecting a bad_alloc in GnuTLS context is impractical in CI). Alternatively, extract the noexcept wrapper logic into a testable helper. This is low priority since the sentinel values are trivially correct on the else-branch.
   *Status:* Resolved — the recommended explanatory comment is now at test/unit/http_request_tls_accessors_test.cpp:129-136, documenting that the bad_alloc sentinel path is not exercised by CI and explaining why the residual risk is low.

13. [x] **code-quality-reviewer** | `test/unit/http_request_tls_accessors_test.cpp:1` | test-coverage
   No runtime test covers the noexcept try/catch sentinel path in is_client_cert_verified, get_client_cert_not_before, and get_client_cert_not_after under an allocation-failure scenario.
   *Recommendation:* Add a comment in the test file noting that the try/catch sentinel path is not covered by automated tests because injecting std::bad_alloc in a GnuTLS context is impractical in CI.
   *Status:* Resolved — duplicate of item 12; the explanatory NOTE block is present at test/unit/http_request_tls_accessors_test.cpp:129-136.

14. [x] **code-simplifier** | `src/http_request.cpp:494` | code-structure
   In populate_all_cert_fields (lines 494-501), the code first calls has_tls_session() then separately calls get_tls_session(). But get_tls_session() already checks connection_ == nullptr and returns nullptr on miss, making the outer has_tls_session() guard partially redundant. The intent is correct but the two-step read-twice pattern obscures that get_tls_session() is already null-safe.
   *Recommendation:* Simplify to `gnutls_session_t session = get_tls_session();` directly. get_tls_session() returns nullptr when connection_ is null or when MHD has no session, which is exactly what the subsequent `if (session != nullptr)` check relies on. This removes the has_tls_session() call from populate_all_cert_fields entirely — has_client_certificate() already demonstrates this direct-call pattern (lines 477-479).
   *Status:* Resolved — duplicate of item 3. populate_all_cert_fields() at src/detail/http_request_impl_tls.cpp:224 now calls `get_tls_session()` exactly once.

15. [x] **code-simplifier** | `src/http_request.cpp:557` | naming
   The local array `unsigned char fingerprint[32]` uses a magic number 32 for the SHA-256 digest size. Although http_utils.hpp already defines `static constexpr size_t sha256_digest_size = 32` (under HAVE_DAUTH), no equivalent constant exists for the GnuTLS path.
   *Recommendation:* Replace the literal 32 with a named constant, either by adding `static constexpr size_t SHA256_DIGEST_BYTES = 32;` inside the anonymous namespace at the top of the #ifdef HAVE_GNUTLS block, or by reusing `GNUTLS_MAX_HASH_SIZE` if already available. This makes the intent explicit and avoids the magic number code smell.
   *Status:* Resolved — duplicate of item 6. `SHA256_DIGEST_BYTES` constant exists at src/detail/http_request_impl_tls.cpp:195 and replaces the magic 32.

16. [ ] **code-simplifier** | `src/http_request.cpp:964` | code-structure
   The four string_view accessor bodies (get_client_cert_dn, get_client_cert_issuer_dn, get_client_cert_cn, get_client_cert_fingerprint_sha256 at lines 964-1001) are structurally identical: `#ifdef HAVE_GNUTLS`, call populate_all_cert_fields(), return `std::string_view(impl_->field.data(), impl_->field.size())`, `#else` return `std::string_view{}`, `#endif`. Repeating the same 6-line skeleton four times adds maintenance surface without adding clarity.
   *Recommendation:* A small private helper on http_request or a file-scope lambda (gated inside the HAVE_GNUTLS block) that takes a `const std::pmr::string&` and returns `std::string_view` would collapse all four bodies to a one-liner: `return to_sv(impl_->client_cert_dn);`. Since only the field reference differs, this is purely mechanical deduplication with no behaviour change. Alternatively, a single-macro `CERT_STRING_VIEW_ACCESSOR(name, field)` matches the existing COMPARATOR macro style in http_utils.hpp.
   *Status:* wontfix — cosmetic/style preference, no functional impact; duplicate of #9; style preference for accessor body dedup

17. [x] **housekeeper** | `specs/unworked_review_issues/2026-05-07_000000_task-019.md:5` | documentation-stale
   The review record header reads 'Total: 20 (0 critical, 1 major deferred, 19 minor)' but the file contains 21 numbered items (1 major + items 2–21 = 20 minor). The count of minor items in the header (19) is off by one — there are 20 minor items (items 2–21).
   *Recommendation:* Update the header to read '20 (0 critical, 1 major deferred, 20 minor)' so the count matches the actual body. Minor discrepancy only; does not affect tracking or approvals.
   *Status:* Resolved — the earlier review file at specs/unworked_review_issues/2026-05-07_000000_task-019.md:5 now reads `Total: 21 (0 critical, 1 major deferred, 20 minor)` matching the body count.

18. [ ] **performance-reviewer** | `src/http_request.cpp:467` | missing-caching
   has_client_certificate() calls gnutls_certificate_get_peers() on every invocation with no caching. If a handler or middleware calls this predicate more than once per request (e.g. in a guard followed by an accessor), the peer-certificate list is fetched from GnuTLS twice. The analogous has_tls_session() likewise re-calls MHD_get_connection_info on every call.
   *Recommendation:* Add a mutable tri-state (unset / true / false) cache field (e.g. mutable std::optional<bool> client_cert_present_cached_) to http_request_impl. Populate it on the first call to has_client_certificate() and return the cached value thereafter. Alternatively, populate it as part of populate_all_cert_fields() so the single cert-init path sets all predicates at once.
   *Status:* deferred — only `populate_all_cert_fields()` is cached via `client_cert_fields_cached`; `has_client_certificate()` and `has_tls_session()` still re-hit MHD/GnuTLS on every call. Low-impact (predicates are cheap) and outside TASK-019 scope, but a real optimisation backlog item.

19. [x] **performance-reviewer** | `src/http_request.cpp:495` | resource-leak
   populate_all_cert_fields() calls has_tls_session() (which calls MHD_get_connection_info internally) and then get_tls_session() (which calls MHD_get_connection_info again) as two separate MHD lookups inside what is already a once-cached function. The double MHD round-trip is unnecessary.
   *Recommendation:* Inline the session retrieval: call get_tls_session() once, assign to a local gnutls_session_t session, and check for null to infer the has_tls_session() condition. This halves the MHD_get_connection_info calls in populate_all_cert_fields() from two to one.
   *Status:* Resolved — duplicate of items 3/14. populate_all_cert_fields() at src/detail/http_request_impl_tls.cpp:233 calls `get_tls_session()` exactly once.

20. [ ] **performance-reviewer** | `src/http_request.cpp:561` | memory-allocation
   The SHA-256 fingerprint is assembled into a default-heap std::string (hex_fingerprint) using snprintf in a 32-iteration loop, then .assign()'d into the arena-backed pmr::string client_cert_fingerprint_sha256. This causes one extra heap allocation and a byte-by-byte copy that could be avoided by writing directly into the pmr::string.
   *Recommendation:* Reserve client_cert_fingerprint_sha256 to 64 chars upfront (fingerprint_size * 2) and write hex digits directly into it using a scratch buffer or std::format / std::to_chars, bypassing the intermediate std::string entirely. Example: client_cert_fingerprint_sha256.reserve(fingerprint_size * 2); for each byte, snprintf into a 3-char stack buf, then client_cert_fingerprint_sha256.append(hex, 2);
   *Status:* deferred — `extract_x509_fingerprint_sha256()` at src/detail/http_request_impl_tls.cpp:197-212 still builds an intermediate `std::string hex` and then .assign()s it; one extra 64-byte heap allocation per cert init. Low impact; deferred.

21. [x] **security-reviewer** | `examples/client_cert_auth.cpp:103` | insecure-design
   The example's time validity check (lines 103-109: now < not_before and now > not_after) is correctly guarded by the earlier has_client_certificate() check (line 72) and is_client_cert_verified() check (line 87), so the -1 sentinels are never reached in the secure_resource. However, the example does not document this guard dependency, and a developer copying only the time-checking snippet would be vulnerable: when no cert is present not_after is -1 (a large negative int64), and any non-negative now would satisfy now > not_after, resulting in a '"Certificate has expired"' response rather than a '"no cert"' denial. This is not a bug in the library but a documentation/example footgun for the time accessors.
   *Recommendation:* Add an inline comment at line 99 explicitly noting that the time checks must only be reached after has_client_certificate() returns true (the -1 sentinel is not a valid Unix epoch value and the comparison would be misleading if evaluated without first confirming cert presence).
   *Status:* Resolved — examples/client_cert_auth.cpp:97-109 now carries an IMPORTANT comment block explicitly documenting that the time-validity checks are only safe after has_client_certificate() and is_client_cert_verified() succeed, and explaining the -1 sentinel footgun.

22. [ ] **security-reviewer** | `src/http_request.cpp:1108` | logging-failures
   The operator<< diagnostic formatter logs the Basic Auth password in plaintext ('pass:\"...\"). This is pre-existing behavior not introduced by TASK-019, but TASK-019 did not add the TLS-session cert fields (DN, CN, fingerprint, verification status) to the formatter output. The omission is appropriate — the cert fields carry PII and should not be in diagnostic logs by default — but the existing password logging is a pre-existing A09 concern.
   *Recommendation:* Pre-existing issue outside TASK-019 scope; note that the new cert fields (DN, CN, fingerprint) were correctly excluded from the formatter.
   *Status:* deferred — pre-existing A09 concern outside TASK-019 scope; src/http_request.cpp:370 still emits `pass:"..."` plaintext. Cert fields remain correctly excluded. Real backlog item to add a redaction policy / opt-in for diagnostic logging.

23. [x] **security-reviewer** | `src/http_request.cpp:492` | insecure-design
   In populate_all_cert_fields(), client_cert_fields_cached is set to true (line 492) before any cert field is populated (lines 510-581). If std::bad_alloc is thrown during the two-pass DN/CN string allocation (e.g., std::string dn(dn_size, '\0')), the cached flag is already true. Subsequent calls return immediately and all string_view accessors return the empty default sentinels — which is the correct sentinel value. However, the noexcept wrappers (is_client_cert_verified, get_client_cert_not_before/after) catch the first-call exception correctly and return false/-1, while the four string_view accessors (get_client_cert_dn etc.) let the first call propagate and then return empty on all subsequent calls. This is not exploitable but creates an asymmetric exception contract between the two groups of accessors: the first call to a string_view accessor can throw even after a noexcept accessor has already silently set the cache. The semantics are correct (all sentinels are appropriate defaults) but the interleaving may surprise callers who call noexcept accessors first, observe false/-1, then call a string_view accessor and get an exception.
   *Recommendation:* Consider moving client_cert_fields_cached = true to after all field population is complete (or after the early-exit for !cert.is_valid()), and restructure populate_all_cert_fields() so that a bad_alloc on one field does not prevent population of subsequent fields (e.g., catch per-field allocation failures individually). Alternatively, add try/catch in the four string_view accessors to match the noexcept accessors' contract.
   *Status:* Accepted — the asymmetric contract is deliberate and now documented: the comment block at src/http_request_auth.cpp:184-199 explicitly states "The four string_view accessors deliberately omit `noexcept` so the allocator failure mode is observable". The `client_cert_fields_cached = true` early-set at src/detail/http_request_impl_tls.cpp:225-226 was kept intentionally; on any post-cache bad_alloc the string fields revert to their (already-default-constructed) empty sentinels, which is the correct contract.

24. [x] **spec-alignment-checker** | `src/httpserver/create_test_request.hpp:115` | ears-requirement
   PRD-FLG-REQ-001 states 'When a public header is parsed then the system shall not gate any declaration on HAVE_BAUTH, HAVE_DAUTH, HAVE_GNUTLS, or HAVE_WEBSOCKET.' The `create_test_request.hpp` public header still gates the `tls_enabled()` builder method on `#ifdef HAVE_GNUTLS` (lines 115 and 143). TASK-019 removed the GNUTLS gate from the primary `http_request.hpp` cert accessors, which is its direct scope, but `create_test_request.hpp` contains a remaining violation of PRD-FLG-REQ-001.
   *Recommendation:* Either (a) make `tls_enabled()` unconditional in `create_test_request.hpp` (the call is a no-op setter that stores a bool; the impl guard in http_request.cpp is sufficient), or (b) note this as deferred to the API-FLG milestone task that addresses HAVE_BAUTH/HAVE_DAUTH gates still in http_request.hpp. The latter approach should be tracked explicitly.
   *Status:* Resolved — `tls_enabled()` at src/httpserver/create_test_request.hpp:118 is now unconditional (path (a)); the TASK-034 comment at lines 115-117 documents that the value is silently dropped on HAVE_GNUTLS-off builds.

25. [x] **spec-alignment-checker** | `src/httpserver/http_request.hpp:126` | specification-gap
   The `http_request.hpp` public header still has `#ifdef HAVE_BAUTH` gates around `get_user()` and `get_pass()` (lines 126-154) and `#ifdef HAVE_DAUTH` around `get_digested_user()` and `check_digest_auth()` (lines 136-446). PRD-FLG-REQ-001 requires all public declarations to be ungated. TASK-019 correctly removed the HAVE_GNUTLS gate from the TLS accessors (its direct scope) but did not address the remaining HAVE_BAUTH/HAVE_DAUTH gates. These are presumably deferred to the API-FLG milestone task, but no tracking note exists in the file.
   *Recommendation:* Add a TODO comment in http_request.hpp cross-referencing the API-FLG milestone task (or create a dedicated TASK for removing HAVE_BAUTH/HAVE_DAUTH gates) so the remaining PRD-FLG-REQ-001 violation is not forgotten.
   *Status:* Resolved — TASK-034 ungated the auth declarations; src/httpserver/http_request.hpp no longer contains any HAVE_BAUTH/HAVE_DAUTH/HAVE_GNUTLS gates (only the HTTPSERVER_COMPILATION include guards remain). PRD-FLG-REQ-001 is satisfied for this header.

26. [x] **spec-alignment-checker** | `test/integ/ws_start_stop.cpp:1057` | acceptance-criteria
   Acceptance criterion 3 says the additional test 'verifies returned DN/CN/fingerprint'. The `client_cert_with_certificate` test checks only that `CN:Test Client` appears (line 1058) and that a fingerprint field is present (line 1059), not a specific known fingerprint value. The `client_cert_fingerprint` test (line 1140) verifies fingerprint length is 64 hex chars but not the exact value against a known-good certificate. The criterion text implies a known-value check.
   *Recommendation:* Either document that length+format verification against a known cert is sufficient for the criterion, or add a test fixture that embeds the expected SHA-256 fingerprint of the test certificate and asserts equality.
   *Status:* Resolved — the new `client_cert_accessors_known_values` test at test/integ/ws_start_stop.cpp:1246 asserts exact known values: DN contains "O=Test Org" and "CN=Test Client" (lines 1309-1310), issuer == subject for the self-signed fixture (line 1314), CN equals exactly "Test Client" (line 1317), and the fingerprint is verified as 64 lowercase hex chars (lines 1322-1325). Path (a) from the recommendation (length+format check is the documented acceptance) applies to the fingerprint, with explicit DN/CN known-value checks supplementing it.

27. [x] **test-quality-reviewer** | `test/integ/ws_start_stop.cpp:0` | logic-in-test
   The fingerprint validation loop `for (char c : fp)` with an `if`-based validity check inside `client_cert_accessors_known_values` introduces control flow logic in test code. The task spec requires hex-encoded lowercase output, but the loop tests each character individually rather than using a single regex/pattern match or a table-driven approach.
   *Recommendation:* Replace the character loop with a single assertion using `std::all_of` and a lambda, or a regex match against `^[0-9a-f]{64}$`. This removes branching from the test body while preserving the same behavioral check: `LT_CHECK(std::all_of(fp.begin(), fp.end(), [](char c){ return (c>='0'&&c<='9')||(c>='a'&&c<='f'); }));`
   *Status:* Resolved — test/integ/ws_start_stop.cpp:1323-1325 now uses the exact `std::all_of` form recommended; the explicit character-loop has been removed.

28. [x] **test-quality-reviewer** | `test/integ/ws_start_stop.cpp:0` | missing-test
   The new `client_cert_accessors_known_values` integration test silently skips all assertions when `ws.start(false)` throws (the catch block just does `LT_CHECK_EQ(1,1)` and returns). In environments where TLS cert files are present but ssl setup still fails, the test will report as passing with zero accessor assertions exercised. This also means CI could have a configuration regression that goes undetected.
   *Recommendation:* Instead of catching and silently passing, consider emitting a dedicated skip signal via the test framework (if supported) or at minimum printing a warning to stderr before returning. If the `LT_CHECK_EQ(1,1)` idiom is the project's convention for explicit skip, add a comment explaining it so reviewers understand the intent.
   *Status:* Resolved — the catch block at test/integ/ws_start_stop.cpp:1257-1265 now carries an explanatory comment documenting the LT_CHECK_EQ(1,1) project skip convention.

29. [x] **test-quality-reviewer** | `test/unit/create_test_request_test.cpp:198` | redundant-test
   `build_tls_enabled_no_peer_cert` (lines 198-209, gated by `#ifdef HAVE_GNUTLS`) and `build_no_client_cert_returns_sentinels` (lines 217-232, ungated) both exercise the same code path: a test-built request with no live peer cert must return empty/false/-1 sentinels for all nine accessors. The only difference is that the first also checks `has_tls_session()==true` for the tls_enabled() builder variant. The unconditional test subsumes the gated one's sentinel assertions entirely.
   *Recommendation:* Collapse the two tests: keep `build_no_client_cert_returns_sentinels` (ungated, broader coverage) and expand it with a second `create_test_request().tls_enabled().build()` block to cover the `has_tls_session()==true` case. Remove `build_tls_enabled_no_peer_cert` to eliminate the redundant sentinel assertions.
   *Status:* Resolved — duplicate of item 11. The tls_enabled() builder variant is folded into build_no_client_cert_returns_sentinels at test/unit/create_test_request_test.cpp:244-258; build_tls_enabled_no_peer_cert no longer exists.

30. [x] **test-quality-reviewer** | `test/unit/create_test_request_test.cpp:217` | naming-convention
   `build_no_client_cert_returns_sentinels` uses a try/catch block wrapping all assertions. The test framework (`littletest`) typically catches exceptions at its own harness level, so the manual try/catch is unnecessary and obscures the linear AAA structure of the test without adding value.
   *Recommendation:* Remove the try/catch wrapper and let the framework handle unexpected exceptions. If the intent is to document that no exception should be thrown, add a comment instead of structural catch-all logic.
   *Status:* Resolved — build_no_client_cert_returns_sentinels at test/unit/create_test_request_test.cpp:231 no longer contains any try/catch wrapper; the body is a linear AAA sequence of LT_CHECK assertions.

31. [x] **test-quality-reviewer** | `test/unit/http_request_tls_accessors_test.cpp:0` | missing-test
   The compile-time test file verifies the public surface of `http_request` but has no corresponding no-GNUTLS compile target in `test/Makefile.am`. The HAVE_GNUTLS=off sentinel path is contractually guaranteed by the implementation (the `#else` branches in `http_request.cpp`) but there is no build target that compiles `http_request_tls_accessors_test.cpp` without `HAVE_GNUTLS` defined to verify the declarations remain available and the noexcept specifiers hold in that configuration.
   *Recommendation:* Add a second Makefile.am entry `http_request_tls_accessors_nognutls` compiled with `-UHAVE_GNUTLS` (or equivalent configure-flag) so the CI matrix exercises the no-TLS surface. Alternatively, document in the test comment that the no-GNUTLS path is covered by the unconditional runtime sentinels in `create_test_request_test.cpp`.
   *Status:* Resolved (alternative path) — test/unit/http_request_tls_accessors_test.cpp:150-158 carries the explanatory NOTE documenting that the no-GNUTLS runtime sentinel contract is exercised by the ungated `build_no_client_cert_returns_sentinels` test in create_test_request_test.cpp; per the recommendation, this in-file documentation satisfies the alternative resolution path.
