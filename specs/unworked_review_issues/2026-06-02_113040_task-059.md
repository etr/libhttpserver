# Unworked Review Issues

**Run:** 2026-06-02 11:30:40
**Task:** TASK-059
**Source:** cloud-infrastructure-reviewer iter1
**Total:** 3 major (all out of scope for TASK-059)

TASK-059 scope is narrowly the PMD analyzer sha256 pin in
`.github/workflows/verify-build.yml`. The three findings below
concern pre-existing lines that TASK-059 does not touch. They are
recorded here for a future dedicated supply-chain hardening task.

## Major

1. [ ] **cloud-infrastructure-reviewer iter1 / finding 1** | `.github/workflows/verify-build.yml:357` | iac-anti-pattern
   [SEC-04 Supply-Chain Integrity] GitHub Actions are pinned to mutable tag references (`actions/checkout@v4`, `actions/cache@v4`, `msys2/setup-msys2@v2`, `codecov/codecov-action@v5`) rather than immutable commit SHAs. A tag can be force-pushed by the upstream maintainer, or compromised, to silently inject malicious code into every CI run. TASK-059 correctly pins PMD by digest but the same discipline is not applied to the Actions themselves, which execute with full runner permissions.
   *Out of scope for TASK-059.* These `uses:` lines were not added or modified by TASK-059.
   *Recommendation:* Pin every `uses:` reference to the full 40-character commit SHA, e.g. `actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683` (v4.2.2). Maintain a renovate.json or Dependabot `github-actions` ecosystem config to automate SHA bump PRs.

2. [ ] **cloud-infrastructure-reviewer iter1 / finding 2** | `.github/workflows/verify-build.yml:509` | iac-anti-pattern
   [SEC-04 Supply-Chain Integrity] The macOS curl tarball (`curl-7.75.0.tar.gz`) is downloaded from an S3 bucket (`libhttpserver.s3.amazonaws.com`) over HTTPS but is NOT verified against a pinned sha256 digest before it is compiled and installed. Lines 514-515 only log the observed hash; they do not fail the step on mismatch. A compromised or tampered S3 object would be installed silently. A pre-existing TODO comment on lines 510-514 already acknowledges this gap.
   *Out of scope for TASK-059.* This curl download step was not added or modified by TASK-059.
   *Recommendation:* Replace the `echo` log line with a hard assertion identical to the PMD and libmicrohttpd patterns already used elsewhere in this file: `echo "<expected-sha256>  curl-7.75.0.tar.gz" | shasum -a 256 -c`. Record the expected digest by downloading the file once from a trusted context, running `shasum -a 256`, and committing the result. Also add `-f` / `--fail` to the bare `curl` invocation (it currently lacks these flags, unlike the PMD step).

3. [ ] **cloud-infrastructure-reviewer iter1 / finding 3** | `.github/workflows/verify-build.yml:483` | iac-anti-pattern
   [SEC-04 Supply-Chain Integrity] The IWYU build clones `--branch clang_18` from GitHub (`include-what-you-use/include-what-you-use.git`), which is a mutable branch reference. The existing TODO comment on lines 476-478 correctly identifies this risk but the fix has not been applied: the clone has no pinned commit SHA and no post-clone integrity check. Any adversarial push to that branch would be compiled and installed with `sudo make install` on the runner.
   *Out of scope for TASK-059.* This IWYU clone step was not added or modified by TASK-059.
   *Recommendation:* Replace the mutable branch clone with `git clone` followed by `git checkout <immutable-SHA>` and a `git rev-parse --verify HEAD` assertion. Obtain the verified SHA from the upstream release/tag page at https://github.com/include-what-you-use/include-what-you-use/tags and commit it as a comment alongside the SHA.

## Minor (in-scope, fixed in TASK-059 validation iter1)

4. [x] **cloud-infrastructure-reviewer iter1 / finding 4** | `.github/workflows/verify-build.yml:445` | iac-anti-pattern
   [OPS-01] The PMD rotation comment did not mention an independent verification path (e.g. the pmd-dist-<ver>-bin.zip.sha256 sidecar or Maven Central attestation) as a cross-check against a potentially compromised GitHub release page.
   *Fixed in iter1:* Rotation comment extended with a defence-in-depth cross-check note.

5. [x] **cloud-infrastructure-reviewer iter1 / finding 5** | `.github/workflows/verify-build.yml:455` | iac-anti-pattern
   [SEC-03] `unzip -o` (overwrite mode) on the PMD extraction step could, if the sha256 gate were ever accidentally removed, allow a malicious archive to overwrite arbitrary files without warning.
   *Fixed in iter1:* Removed the `-o` flag from `sudo unzip -q /tmp/pmd.zip -d /opt`.
