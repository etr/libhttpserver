# Unworked Review Issues

**Run:** 2026-05-09 19:21:03
**Task:** TASK-022
**Total:** 13 (0 critical, 0 major, 13 minor)

## Minor

1. [x] **architecture-alignment-checker** | `src/httpserver/http_resource.hpp:56` | interface-contract
   The return type of all render_* methods (and the render fallback) remains std::shared_ptr<http_response> rather than http_response by value. TASK-022 action item states 'Update return type to http_response by value' but also notes this is 'coupled with TASK-036's full handler-return refactor'. Leaving shared_ptr for now is the explicitly documented intent, so no violation — but the action item checkbox is not ticked.
   *Recommendation:* No action required in this task; confirm TASK-036 is tracked to complete the return-type migration. The existing task dependency (Blocks: TASK-036) is correct.
   *Status:* Resolved — TASK-036 landed; all render_* now return `http_response` by value (src/httpserver/http_resource.hpp:88-173).

2. [x] **code-quality-reviewer** | `README.md:563` | documentation
   render_patch is absent from the README method list (lines 564-572 enumerate 8 of the 9 render_* methods; render_patch is not listed). This gap pre-existed the v2.0 branch but the task touched this section of the README and is an opportunity to fix it.
   *Recommendation:* Add a bullet for render_patch alongside the other eight render_* methods in the 'The Resource Object' section.
   *Status:* Resolved — README.md:724 lists `render_patch` alongside the other eight render_* bullets.

3. [x] **code-quality-reviewer** | `src/httpserver/http_resource.hpp:62` | readability
   Doxygen comment block still uses inconsistent indentation (mix of 5-space and 6-space leading asterisks across method docs), a pre-existing style issue carried into the renamed methods.
   *Recommendation:* Align comment block indentation to a single consistent style (e.g. always one space after the opening /**).
   *Status:* Resolved — current header uses uniform indentation across all render_* Doxygen blocks (src/httpserver/http_resource.hpp:78-173).

4. [ ] **code-quality-reviewer** | `test/integ/basic.cpp:69` | code-readability
   Virtual render method overrides in integration test resource classes are missing the 'override' specifier (e.g., render_get, render_post in simple_resource, large_post_resource_last_value, etc.). This was also missing on the old camelCase names before this task, making it a pre-existing issue, but the rename is an opportunity to add override for correctness and readability.
   *Recommendation:* Add 'override' to all render_get/render_post/etc. declarations in test resource classes. The unit test http_resource_test.cpp already sets the correct example with 'override' on line 38.
   *Status:* wontfix — cosmetic/style preference, no functional impact; pre-existing test-style gap; pure safety/style sweep, no functional impact

5. [x] **code-quality-reviewer** | `test/unit/http_resource_test.cpp:35` | test-coverage
   Only render_get is tested via simple_resource; other renamed virtual methods (render_post, render_put, render_delete, etc.) have no direct unit test coverage confirming they delegate to render().
   *Recommendation:* Consider adding a parameterised or per-method test asserting each render_<verb> falls back to render() so regressions are caught if a future override breaks the delegation chain.
   *Status:* Resolved — integration test `all_methods_fallthrough_to_render` (test/integ/basic.cpp:2951-3050) drives every render_* (POST/PUT/DELETE/PATCH/HEAD/OPTIONS/TRACE) end-to-end through the dispatch path and asserts the base render() result.

6. [x] **code-simplifier** | `README.md:563` | naming
   render_patch is absent from the README's bullet-list of extensible methods (lines 564-572), even though render_patch exists in the header and the webserver dispatch table. The list now documents eight methods; the API exposes nine. This pre-existed in the base branch (render_PATCH was also absent there), but the rename task was an opportunity to fix it.
   *Recommendation:* Add `* _**std::shared_ptr<http_response>** http_resource::render_patch(**const http_request&** req):_ Invoked on an HTTP PATCH request.` after the render_put bullet so the documented API matches the implementation.
   *Status:* Resolved — README.md:724 now documents `render_patch` (return-by-value form, consistent with TASK-036).

7. [ ] **code-simplifier** | `test/integ/basic.cpp:69` | code-structure
   The rename was applied throughout test/integ/basic.cpp but the `override` keyword was not added. The only file in this task's change set that gained `override` was test/unit/http_resource_test.cpp (line 35). All ~95 render_* definitions in test/integ/basic.cpp, test/integ/authentication.cpp, and most other test/integ files lack `override`, making it inconsistent. The missing keyword silently allows typos to create new virtual functions rather than overriding the base.
   *Recommendation:* Add the `override` specifier to all render_get / render_post / etc. definitions in test files, matching the pattern already established in http_resource_test.cpp. This is purely a consistency and safety improvement — no behavior change.
   *Status:* wontfix — cosmetic/style preference, no functional impact; duplicate of #4; broad style sweep deferred

8. [x] **housekeeper** | `specs/tasks/M4-handlers/TASK-022.md:37` | documentation-stale
   Related Decisions references '§3.7, §4.4' using section-number notation. §4.4 is now a split-directory file at specs/architecture/04-components/http-resource.md. The reference is still navigable but uses an older section-number convention rather than a file path link, consistent with other tasks in the same milestone.
   *Recommendation:* No immediate action required; section-number references are used consistently across M4 task files and remain resolvable. Can be normalized to file-path links in a future documentation sweep.
   *Status:* No action — reviewer's own recommendation explicitly accepts the existing notation as consistent across M4 task files; normalisation is a global docs sweep, out of scope for TASK-022.

9. [x] **performance-reviewer** | `src/webserver.cpp:1594` | algorithmic-complexity
   The elif chain that resolves HTTP method strings to function-pointer+enum is O(n) over nine branches on every request. This predates the task and is unchanged, but the rename is a natural moment to note it. GET is checked first (most common), which is the only ordering optimization present.
   *Recommendation:* Not a regression introduced by this task. A future optimisation could use a flat lookup table keyed on the first character or a minimal perfect hash, but the chain is short (9 entries) and each strcmp is on a handful of bytes, so the practical cost per request is negligible. Leave as-is unless profiling shows this path as a hot spot.
   *Status:* Resolved — elif chain replaced by a flat table-driven dispatch in src/detail/webserver_request.cpp:428-453 (CCN reduced from ~10 to ~3 per source comment).

10. [x] **spec-alignment-checker** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-022/src/httpserver/http_resource.hpp:39` | specification-gap
   The detail helper `detail::empty_render(const http_request&)` returns `std::shared_ptr<http_response>` (pointer-based). This is the existing return type for all nine `render_*` virtuals and aligns with the deferred TASK-036 scope per the clarified spec. No action needed for TASK-022, but the pointer return is noted as a known carry-forward.
   *Recommendation:* No change needed in TASK-022 scope. TASK-036 will address the return-type change to http_response by value.
   *Status:* Resolved — TASK-036 landed; `detail::empty_render` helper is gone and the default render() returns `http_response{}` sentinel directly (src/httpserver/http_resource.hpp:88-92).

11. [x] **spec-alignment-checker** | `specs/tasks/M4-handlers/TASK-022.md:39` | specification-gap
   Task status field in TASK-022.md was not updated to 'Done' after implementation. The _index.md also still shows 'Not Started' for TASK-022 (line 107 of specs/tasks/_index.md). This is a bookkeeping omission — the status reflects the state before the branch was worked.
   *Recommendation:* Update 'Status: Not Started' to 'Status: Done' in both specs/tasks/M4-handlers/TASK-022.md and specs/tasks/_index.md.
   *Status:* Resolved — `**Status:** Done` in specs/tasks/M4-handlers/TASK-022.md:39 and `| TASK-022 | … | M4 | Done |` in specs/tasks/_index.md:115.

12. [ ] **test-quality-reviewer** | `test/integ/basic.cpp:3041` | naming-convention
   Comment inside `all_methods_fallthrough_to_render` reads "not render_get/POST/etc." — the POST part is still uppercase, inconsistent with the renamed snake_case convention applied everywhere else in this change.
   *Recommendation:* Change the comment to "not render_get/render_post/etc." to be consistent with the new naming convention.
   *Status:* wontfix — cosmetic/style preference, no functional impact; trivial comment-case fix; bundle into next test-style sweep

13. [ ] **test-quality-reviewer** | `test/integ/basic.cpp:778` | missing-test
   The CONNECT method block in both `complete` (line 774-783) and `only_render` (line 838-849) tests is commented out with no substitute. `complete_test_resource` defines `render_connect` but no integration test ever exercises the `render_connect` dispatch path end-to-end through the HTTP stack. The renamed virtual `render_connect` in the header and its dispatch wiring in `webserver.cpp` are therefore untested at the integration level.
   *Recommendation:* Either add a CONNECT dispatch integration test (even a minimal one using `CURLOPT_CUSTOMREQUEST, "CONNECT"` pointed at a local resource) or add a comment explicitly recording that CONNECT is intentionally excluded and why (e.g. libcurl tunneling semantics make it impractical). This is a pre-existing gap that the rename did not fix.
   *Status:* deferred — pre-existing CONNECT integration-test gap; libcurl CONNECT semantics (tunnel) make a meaningful direct-dispatch test non-trivial. test/integ/basic.cpp:2946-2948 records the exclusion; render_connect dispatch is covered by the unit-level decltype assertion (test/unit/http_resource_test.cpp:101-104). Track as a real backlog item for a follow-up MHD-only CONNECT test.
