# Unworked Review Issues

**Run:** 2026-07-03 22:58:49
**Task:** TASK-091
**Total:** 21 (0 critical, 0 major, 21 minor)

## Minor

1. [ ] **architecture-alignment-checker** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-091/scripts/check-readme.sh:318` | pattern-violation
   The S3 tab-detection awk block (both in check-readme.sh and check-release-notes.sh) still uses the old toggle-style fence tracking (/^```/ { in_block = !in_block }) rather than the new ordered state machine introduced in scripts/lib/check-fence-balance.sh. This mixes two fence-detection strategies within the same scripts. The toggle approach is logically safe for the tab-only check (S3 is not gating structural balance, which S1 now handles), but creates a maintenance inconsistency: a future developer editing the S3 block might not realize it uses a weaker model than S1.
   *Recommendation:* Consider extracting a shared awk snippet from check-fence-balance.sh that the S3 tab check can reuse, or add a comment to the S3 awk block explicitly noting that structural balance is validated separately by S1 and that the toggle approach is intentionally retained for the tab-only scan.

2. [ ] **architecture-alignment-checker** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-091/scripts/check-readme.sh:340` | pattern-violation
   The markdownlint invocation uses `markdownlint -q "$README" 2>&1` — the `2>&1` redirect merges stderr into stdout, sending markdownlint diagnostics to the script's stdout stream. The code comment says 'findings go to stderr so they are visible in CI logs', which is inaccurate: after the redirect they appear on stdout, not stderr. The same pattern is in check-release-notes.sh line 367. In practice CI logs capture both streams, so visibility is not impaired, but the comment mis-states the stream.
   *Recommendation:* Remove the `2>&1` redirect and let markdownlint write to its natural streams, or change the redirect to `>&2` (stdout to stderr) if the intent is to keep findings on stderr. Update the comment to match the actual behavior.

3. [ ] **architecture-alignment-checker** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-091/scripts/lib/v1-no-setters.txt:1` | pattern-violation
   scripts/lib/ previously contained only sourced bash scripts (resolve-prefix.sh, skip-or-fail.sh). v1-no-setters.txt is the first static data file placed there. While reasonable and well-documented, it slightly expands the implied contract of the lib/ directory beyond executable helpers.
   *Recommendation:* No action required; the file is well-documented with its derivation and purpose. If the lib/ convention is tightened in the future, consider moving static data to scripts/data/ or similar. The current placement is not a blocker.

4. [ ] **code-quality-reviewer** | `scripts/check-readme.sh:339` | code-elegance
   The markdownlint block (lines 339-348 in check-readme.sh and lines 366-375 in check-release-notes.sh) is near-identical across both scripts — same advisory-vs-strict logic, same env-var names, same advisory NOTE message. This is the highest-risk point for a future out-of-sync regression (one script gets updated, the other does not). The fence-balance check was successfully extracted to a shared helper (scripts/lib/check-fence-balance.sh) using the same pattern.
   *Recommendation:* Consider extracting a scripts/lib/check-markdownlint.sh helper that accepts the target file and a caller label (e.g. 'check-readme' or 'check-release-notes'), mirroring the check-fence-balance.sh extraction. Both scripts would then source or invoke the helper, keeping the advisory logic in one place.

5. [ ] **code-quality-reviewer** | `scripts/check-release-notes.sh:167` | readability
   The comment 'Note: check-readme.sh may carry additional tokens not required in RELEASE_NOTES.md' is directionally wrong. In practice check-release-notes.sh has ONE MORE token (\bhttpserver::constants\b at line 203) than check-readme.sh. A future maintainer reading this comment may add a token to check-readme.sh thinking they are filling a gap, when the gap is in the other direction.
   *Recommendation:* Correct to: 'Note: check-release-notes.sh may carry additional tokens not required in README.md (e.g. tokens that appear in release-notes prose but not in code examples). When adding a new v2 surface, update both scripts.'

6. [ ] **code-quality-reviewer** | `scripts/lib/check-fence-balance.sh:20` | readability
   The header Usage/Exit comment says 'non-zero = unbalanced (with a diagnostic on stderr)' without distinguishing the two exit codes: exit 2 (info-string fence inside an open block, i.e. consecutive openers) vs exit 3 (unclosed block at EOF). Both callers treat any non-zero identically today, but the distinction aids debugging and documents the protocol for future callers.
   *Recommendation:* Extend the 'Exit:' line to: 'Exit: 0 = balanced; 2 = info-string fence inside an open block (consecutive openers); 3 = unclosed block at EOF. Callers may treat any non-zero as unbalanced.'

7. [ ] **code-simplifier** | `scripts/check-readme.sh:339` | code-structure
   The markdownlint advisory block (lines 339-348 in check-readme.sh, lines 366-375 in check-release-notes.sh) was hardened in this PR from opt-in strict to opt-out advisory, and the same logic was added to both scripts. The two blocks are structurally identical — LIBHTTPSERVER_MARKDOWNLINT_ADVISORY=1 or MARKDOWNLINT_STRICT=no downgrades to advisory, otherwise fail. Only the NOTE message prefix differs ('check-readme:' vs 'check-release-notes:').
   *Recommendation:* A shared function `run_markdownlint_advisory <script_prefix> <file>` sourced from scripts/lib/ would deduplicate this. Alternatively, since the two scripts are standalone (no common sourced lib currently), a simpler improvement is to extract the advisory condition into a named boolean at the top of each script: `MARKDOWNLINT_ADVISORY=$([ "${LIBHTTPSERVER_MARKDOWNLINT_ADVISORY:-0}" = "1" ] || [ "${MARKDOWNLINT_STRICT:-yes}" = "no" ] && echo yes || echo no)` — then the block collapses to a single if/else. This makes the intent visible at the call site. Low priority since both blocks are small and clearly commented.

8. [ ] **code-simplifier** | `scripts/check-readme.sh:83` | code-structure
   The v1-no-setters.txt loading loop (8 lines, lines 83-90) is duplicated verbatim in check-release-notes.sh (lines 116-122). Both blocks were added in this PR as part of moving to the shared data file. Only the guard message differs. Since the shared data file abstraction was already introduced, the loading pattern is the remaining duplication.
   *Recommendation:* Extract the loading loop into scripts/lib/load-v1-no-setters.sh that sources into the caller's environment (sets V1_NO_SETTERS and validates it). Both scripts already use the sourced-lib pattern (check-parallel-install.sh sources resolve-prefix.sh and skip-or-fail.sh), so this fits the established convention. The guard message can accept a label parameter: `load_v1_no_setters <label>`. Only worth doing if a third consumer emerges; with two sites, inline duplication is tolerable.

9. [ ] **housekeeper** | `specs/tasks/M7-v2-cleanup/_index.md:58` | task-not-marked-complete
   The M7 task-table row for TASK-091 still reads 'Backlog'. TASK-091.md was updated to Status: Done (with all action items checked) in this commit, but _index.md was not touched. The row should be 'In Progress' (or 'Done') to match the task file.
   *Recommendation:* Update the TASK-091 row in specs/tasks/M7-v2-cleanup/_index.md from 'Backlog' to 'In Progress' (the orchestrator will flip it to Done after merge).

10. [ ] **performance-reviewer** | `scripts/check-readme.sh:161` | missing-batching
   A3 required-v2-token check loops over 29 tokens and spawns a separate grep subprocess per token, reading README.md 29 times. By contrast, A2 (the absence check) already batches all v1 tokens into a single alternation grep. The 29-subprocess pattern is harmless at README.md scale, but inconsistent with A2's approach.
   *Recommendation:* If the diagnostic 'which tokens are missing' reporting is needed (it is), the current loop is the correct design. An optional optimisation: do one grep with all 29 patterns ORed to short-circuit the common case (all present), and only fall into the per-token loop on failure. No change required for CI scale.

11. [ ] **performance-reviewer** | `scripts/check-readme.sh:256` | missing-batching
   A4b checks 5 alias names and 4 example filenames against the already-extracted $hooks_body string via 9 separate 'echo | grep' pipelines (each spawning two processes). All 9 patterns could be checked in a single awk pass over the string.
   *Recommendation:* Replace the two inner for-loops with a single awk invocation that checks all 9 patterns in one pass. Not required at current scale.

12. [ ] **performance-reviewer** | `scripts/check-release-notes.sh:54` | missing-batching
   check_tokens_present() spawns one grep per token in a loop. It is called twice: once with ~60 V1 tokens (line 164) and once with ~30 V2 tokens (line 206), totalling ~90 separate grep subprocess invocations against RELEASE_NOTES.md. At the current file size this is negligible, but it is the most process-spawn-heavy loop in the changed scripts.
   *Recommendation:* Same short-circuit approach as finding 1: run a single alternation grep first; only loop for individual diagnostics on failure. For CI correctness the current code is fine as-is.

13. [ ] **security-reviewer** | `scripts/check-examples.sh:134` | injection
   Unquoted word-splitting on `$programs` in `for prog in $programs`. The variable is populated by awk processing of examples/Makefile.am. If a program name ever contains a glob character or space, the loop would misbehave. The same pattern repeats at line 151 and for `$KNOWN_ARTIFACTS` at line 160 (currently empty, but would split on expansion if ever populated).
   *Recommendation:* Collect awk output into a bash array instead of a flat string: `mapfile -t programs < <(awk ... "$MAKEFILE_AM")` and iterate with `for prog in "${programs[@]}"`.

14. [ ] **security-reviewer** | `scripts/check-parallel-install.sh:197` | injection
   `_BREW_PREFIX` is captured from `brew --prefix` output and interpolated unquoted into compiler flag strings (`-I${_BREW_PREFIX}/include`). On a system where `brew --prefix` returns a path containing spaces (uncommon but not impossible on non-standard Homebrew installations), the resulting CPPFLAGS/LDFLAGS would be word-split incorrectly when passed to configure.
   *Recommendation:* Quote the expansions: `_HOMEBREW_CPPFLAGS="-I${_BREW_PREFIX}/include -I${_BREW_PREFIX}/opt/gnutls/include"` is already quoted at assignment; confirm the configure invocation at line 205-209 passes these quoted (it does via `CPPFLAGS="..."` assignment), so this is low risk but add a guard to reject paths with whitespace: `case "$_BREW_PREFIX" in *' '*) _BREW_PREFIX=""; esac`.

15. [ ] **security-reviewer** | `scripts/check-readme.sh:291` | injection
   Relative Markdown link targets extracted from README.md (user-controlled repo content) are joined directly to REPO_ROOT without path-traversal sanitization: `if [ ! -e "$REPO_ROOT/$target" ]`. A crafted link like `[x](../../../etc/shadow)` makes the script test existence of arbitrary filesystem paths. The exposure is read-only (`-e` test, not exec), but the check silently extends outside the repository tree.
   *Recommendation:* Reject or skip targets that begin with `..` or contain `/../`: `case "$target" in *../*|../*)  continue ;; esac` before the existence test.

16. [ ] **security-reviewer** | `scripts/check-release-notes.sh:71` | injection
   In `extract_section_body`, the `heading_re` argument is passed to awk via `-v re="$2"`. The function comment correctly documents that the value must not contain backslashes or double-quotes. All current call sites use hardcoded strings, but this restriction is unenforced. A future caller passing a dynamic regex containing `\` or `"` would silently corrupt the awk program string.
   *Recommendation:* Document the restriction with an assertion or sanitize the value inside the function: `re="${2//\\/\\\\}"` (escape backslashes) before passing via -v, or switch to reading the pattern from a file with ARGV to avoid the quoting hazard entirely.

17. [ ] **security-reviewer** | `scripts/check-soversion.sh:266` | injection
   SONAME_BASENAME (e.g. `libhttpserver.so.2`) is interpolated without escaping into a `grep -E` pattern. The dots in the basename are ERE wildcards, so the pattern `^library_names=.*libhttpserver.so.2` also matches `libhttpserverXsoY2`. Practically harmless in this context but violates least-surprise.
   *Recommendation:* Escape ERE metacharacters before interpolation: use `grep -F` for literal matching, or pre-escape the string with `printf '%s' "$SONAME_BASENAME" | sed 's/[.]/[&]/g'`.

18. [ ] **security-reviewer** | `scripts/verify-installed-examples.sh:28` | injection
   `set -eu` is used instead of `set -euo pipefail`. The feature-flag detection pipeline at line 81 (`grep -E ... | head -1`) can silently suppress a grep non-zero exit under `-eu` but not under pipefail. A build where AM_CXXFLAGS is formatted across continuation lines (already noted in line 82 comment) could yield an empty `cxxflags_line` without failing, causing all HAVE_* flags to default to 0 — meaning gated examples compile without their required defines.
   *Recommendation:* Add `-o pipefail` to the `set` invocation at line 28, and add a guard after `cxxflags_line=` that emits a warning when the line is empty to surface the silent-detection failure path.

19. [ ] **spec-alignment-checker** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-091/scripts/verify-installed-examples.sh:26` | specification-gap
   verify-installed-examples.sh uses `set -eu` (no `-o pipefail`) while the acceptance criterion targets 'all check-*.sh scripts'. This file is not a check-*.sh and is therefore technically out of scope for AC-1, but it is the only shell script in the changed set that lacks pipefail and could silently swallow pipeline failures.
   *Recommendation:* Add `set -euo pipefail` to verify-installed-examples.sh for consistency, or document the intentional omission of pipefail with a comment.

20. [ ] **test-quality-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-091/scripts/test_check_fence_balance.sh:27` | missing-test
   The 'balanced' fixture contains exactly one fenced block. The state machine's reset behaviour (in_block returns to 0 and then opens correctly for a second block) is never exercised. A document with two sequentially balanced blocks (open-close-open-close) would give one additional regression signal that the state machine does not incorrectly latch open after the first close.
   *Recommendation:* Add a second fixture, e.g. two_blocks.md, that contains two properly-balanced code fences separated by prose, and assert it exits 0. This covers the in_block 1 → 0 → 1 → 0 path and guards against accidental latch logic.

21. [ ] **test-quality-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-091/scripts/test_check_fence_balance.sh:27` | missing-test
   All three fixtures use an info-string opener (```cpp). The helper also accepts a bare ``` line as a valid opener (the in_block == 0 branch does not check whether rest is empty). The bare-opener path is untested: a fixture with ``` (no language tag) opening and ``` closing would confirm that the 'bare as opener' branch behaves correctly.
   *Recommendation:* Add a fixture using a bare ``` opener and a bare ``` closer and assert exit 0. This is a one-liner extension of the existing BALANCED fixture and confirms the helper does not accidentally treat a bare opener as a closer when in_block is 0.
