# Unworked Review Issues

**Run:** 2026-06-15 10:37:51
**Task:** TASK-081
**Total:** 29 (0 critical, 1 major, 28 minor)

## Major

1. [x] **code-simplifier** | `test/unit/webserver_ws_available_test.cpp:80` | code-structure
   Tests unique_ptr_null_throws_invalid_argument and shared_ptr_null_throws_invalid_argument duplicate the same dual-bool pattern (caught_invalid_argument + caught_feature_unavailable) that also appears in webserver_register_ws_smartptr_test.cpp (null_unique_ptr_throws_feature_unavailable_on_ws_off, lines 333-344) and webserver_dauth_available_test.cpp (lines 67-77, 87-97). Across the three new files there are six virtually identical try/catch blocks tracking two boolean flags each. This needless repetition (clean-code smell) means any littletest ASSERT_THROW workaround evolution has to be applied in six places.
   *Recommendation:* Extract a file-local helper such as `bool throws_invalid_arg_not_feature_unavailable(auto fn)` that runs the try/catch and returns the boolean verdict. Each test body then becomes a single-expression call + two LT_CHECK lines. The dual-flag idiom is already documented in webserver_register_ws_smartptr_test.cpp (lines 189-194) — the comment belongs once on the helper, not repeated inline each time.
   *Worked 2026-07-07 (chore/unworked-major-issues):* Dual-flag idiom centralized in test/unit/throw_probe.hpp (httpserver_test::probe_throw_type returning a two-flag verdict); all six sites across webserver_ws_available / webserver_register_ws_smartptr / webserver_dauth_available converted; the rationale comment lives once on the helper.
## Minor

2. [ ] **architecture-alignment-checker** | `test/unit/http_request_operator_stream_test.cpp:170` | pattern-violation
   The HAVE_BAUTH-off test `operator_stream_credential_surfaces_absent_on_bauth_off` asserts `out.find("pass:\"<redacted>\"") != std::string::npos` (line 170), documenting in a comment that the pass field is emitted unconditionally with the redaction token even when HAVE_BAUTH is off. However, this assertion binds the test to a subtle implementation detail: on a HAVE_BAUTH-off build, if the field is actually absent from the stream (rather than present with the redaction token), the test would fail in a confusing way. The architecture (07-feature-availability.md) documents that `get_user`/`get_pass` return empty `string_view` on HAVE_BAUTH-off builds; it does not specify whether the `pass:` field line is emitted at all. The test comment says 'the field is emitted unconditionally; the value is the redacted token' — this is an implementation-level assumption that should be confirmed by a cross-reference to the operator<< implementation comment, or a companion assertion on what happens when the field is absent.
   *Recommendation:* Add a brief cross-reference comment citing the specific line in src/http_request.cpp (or equivalent) that guarantees the unconditional emission of the pass field, so reviewers can verify the assertion is tracking the correct invariant rather than an implementation accident. This is a documentation/test-comment improvement only — the test logic itself may be correct.

3. [ ] **code-quality-reviewer** | `test/PORTABILITY.md:173` | code-elegance
   The PORTABILITY.md entry for `http_response_factories_test.cpp` mentions that the `pipe_factory_signature_is_single_arg` static_assert is wrapped inside the `#ifndef _WIN32` block but 'is itself portable; a future Windows port could split the static_assert out'. This is a known improvement opportunity left as a comment in the documentation rather than being acted on, even though it is a pure-compile assertion with no platform dependency.
   *Recommendation:* Extract the `static_assert` inside `pipe_factory_signature_is_single_arg` from the `#ifndef _WIN32` guard in `http_response_factories_test.cpp` so it runs on every platform, including Windows. The surrounding runtime `pipe_factory_kind` test can remain gated. This is a small quick win that is already noted in PORTABILITY.md but not acted on in this task.

4. [ ] **code-quality-reviewer** | `test/unit/body_test.cpp:209` | test-coverage
   The test `file_body_destructor_closes_fd_when_not_materialized` relies on the OS recycling the same fd number after an open/close cycle. This is a POSIX implementation detail: if another thread or OS activity opens an fd between steps 1 and 2 (the close and the `file_body` constructor call) the slot will not be reclaimed by `file_body` and the test will spuriously fail. A comment acknowledges the technique but does not note this race. In a busy process (e.g. parallel `make check`) this can be flaky.
   *Recommendation:* Add a comment warning that the fd-recycling technique is inherently single-threaded and the test should not be run under heavy concurrency. Alternatively, replace the technique with a wrapper that tracks the fd value by inspecting the file_body through the existing friend accessor, if one is available, to avoid the race entirely.

5. [ ] **code-quality-reviewer** | `test/unit/header_hygiene_test.cpp:138` | readability
   The cppcheck-suppress comment on line 142 references `_PTHREAD_H/_PTHREAD_H_` in the suppression rationale text even though those macros were deleted from the detection block. The comment is accurate in intent but slightly misleading as a reader scanning for what macros are actually checked.
   *Recommendation:* Update the inline comment to list only the macros that remain active: `MHD_VERSION`, `GNUTLS_GNUTLS_H`, `_SYS_SOCKET_H/_SYS_SOCKET_H_`, `_SYS_UIO_H/_SYS_UIO_H_`. Drop the `_PTHREAD_H/_PTHREAD_H_/_WINPTHREADS_H` reference since those checks no longer exist.

6. [ ] **code-quality-reviewer** | `test/unit/http_request_operator_stream_test.cpp:141` | code-readability
   The HAVE_BAUTH-off `#else` block spans from line 140 to line 213, with no closing comment on the `#endif` other than `// HAVE_BAUTH`. The asymmetry between the dense comment block on the `#else` branch header (lines 141-151) and the absence of any summary at the `#endif  // HAVE_BAUTH` on line 214 makes it slightly harder to visually locate where the conditional block ends when reading bottom-up.
   *Recommendation:* Minor nit only: the existing pattern in the codebase for other paired tests uses `#endif  // HAVE_BAUTH` or `#endif  // !HAVE_BAUTH` consistently; the current file matches that convention. No change strictly required, but adding a parenthetical like `#endif  // HAVE_BAUTH / else-branch above` would aid navigation in the longer BAUTH-off block.

7. [ ] **code-quality-reviewer** | `test/unit/http_request_operator_stream_test.cpp:151` | test-coverage
   The BAUTH-off complement `operator_stream_credential_surfaces_absent_on_bauth_off` asserts `out.find("admin") == std::string::npos` but does not assert `out.find("user:") != std::string::npos` (i.e. that the user field line is still emitted, just with an empty value). Without that positive assertion, a regression that drops the user field entirely would still pass this test.
   *Recommendation:* Add `LT_CHECK(out.find("user:") != std::string::npos)` to pin that the field is present but holds the empty value on the BAUTH-off path. This symmetrically mirrors the BAUTH-on test's `LT_CHECK(out.find("user:\"admin\"") != std::string::npos)` assertion.

8. [ ] **code-quality-reviewer** | `test/unit/http_response_factories_test.cpp:261` | test-coverage
   The `pipe_factory_signature_is_single_arg` static_assert is inside the `#ifndef _WIN32` block. PORTABILITY.md itself notes this: 'a future Windows port could split the static_assert out so it runs on every lane while the runtime test stays gated.' The compile-time pin is therefore absent on the Windows/MSYS lane.
   *Recommendation:* Move the `static_assert` that checks `pipe_fn_t` above the `#ifndef _WIN32` guard so the signature contract is verified on every CI lane, then keep only the runtime `::pipe()` call inside the guard. This requires the `pipe_factory_signature_is_single_arg` test to still have at least one `LT_CHECK` if littletest requires it, which could be satisfied by the LT_CHECK_EQ(true,true) already present.

9. [ ] **code-quality-reviewer** | `test/unit/webserver_register_ws_smartptr_test.cpp:311` | code-readability
   The `#ifndef HAVE_WEBSOCKET` block at line 310 introduces a second suite (`webserver_register_ws_smartptr_off_suite`) in the same translation unit that already has `webserver_register_ws_smartptr_suite` (defined only under `#ifdef HAVE_WEBSOCKET`). This means there are two `LT_BEGIN_SUITE` / `LT_END_SUITE` blocks in one file. The littletest framework allows this, but it is unusual and may confuse readers expecting one suite per TU.
   *Recommendation:* Consider documenting at the top of the file that this TU intentionally houses two suites for the on/off paths, or extract the off-path tests to a dedicated file (`webserver_register_ws_smartptr_off_test.cpp`) for consistency with the pattern used by `webserver_ws_available_test.cpp` (new file) vs `webserver_ws_unavailable_test.cpp` (existing file).

10. [ ] **code-quality-reviewer** | `test/unit/webserver_ws_available_test.cpp:80` | test-coverage
   The test `unique_ptr_null_throws_invalid_argument` checks two boolean flags (`caught_invalid_argument` and `caught_feature_unavailable`) within a single test body, giving two runtime assertions per test. Clean Code Tests rule #1 (one assert per test) is violated. If the feature_unavailable branch were unexpectedly hit, the assertion on `caught_invalid_argument` still passes false and the failure message is less clear.
   *Recommendation:* Split into two tests: one asserting `caught_invalid_argument == true`, and a second asserting `caught_feature_unavailable == false`. The same applies to `shared_ptr_null_throws_invalid_argument` and to the analogous dual-flag pattern in `webserver_register_ws_smartptr_test.cpp` (lines 332-345) and `webserver_dauth_available_test.cpp` (lines 67-78, 86-98).

11. [ ] **code-quality-reviewer** | `test/unit/webserver_ws_available_test.cpp:82` | test-coverage
   The test constructs `httpserver::webserver ws{httpserver::create_webserver(8190)}` on a fixed port. If the host machine has port 8190 in use (TIME_WAIT, another parallel test run), the constructor may fail or behave unexpectedly. The port is also not using `listen_socket(false)` the way `webserver_dauth_available_test.cpp` does for its construction tests, so this could attempt a real bind.
   *Recommendation:* Follow the pattern established in `webserver_dauth_available_test.cpp` and add `.listen_socket(false)` to the `create_webserver(8190)` calls in `unique_ptr_null_throws_invalid_argument` and `shared_ptr_null_throws_invalid_argument`. These tests only exercise the registration throw path, not any network functionality, so skipping the actual bind makes them port-safe.

12. [ ] **code-simplifier** | `test/unit/http_request_operator_stream_test.cpp:154` | code-structure
   The three HAVE_BAUTH-off tests (operator_stream_credential_surfaces_absent_on_bauth_off, operator_stream_redacts_authorization_header_on_bauth_off, operator_stream_redacts_cookies_on_bauth_off at lines 152-213) each repeat the same ostringstream / operator<< / const string out pattern across independent request builders. The `oss << req; const std::string out = oss.str();` idiom appears three times in four lines — a minimal helper `std::string stream_request(const http_request& r)` would remove the repetition and make the per-test assertion block the focus.
   *Recommendation:* Extract a file-local lambda or inline function `auto dump = [](const httpserver::http_request& r) { std::ostringstream o; o << r; return o.str(); };` at file scope inside the `#else` block. Each of the three tests reduces to `const std::string out = dump(req);` — a seven-line saving and the assertion intent becomes clearer.

13. [ ] **code-simplifier** | `test/unit/webserver_dauth_available_test.cpp:47` | code-structure
   The suite's set_up() and tear_down() bodies are empty. The same empty stubs appear in webserver_ws_available_test.cpp (lines 59-63). Analogous suites in this codebase (e.g. webserver_register_ws_smartptr_test.cpp line 132) omit the empty body from tear_down (written as `void tear_down() {}`on one line) — the new files add unnecessary vertical mass with two-line blank definitions.
   *Recommendation:* Collapse the empty lifecycle hooks to single-line form consistent with the rest of the codebase: `void set_up() {}` / `void tear_down() {}`. This is a purely cosmetic vertical-density fix with zero behavior impact.

14. [ ] **code-simplifier** | `test/unit/webserver_dauth_available_test.cpp:67` | code-structure
   The two 'does not throw' tests (digest_auth_true and explicit_digest_auth_false) share identical try/catch/bool-flag scaffolding differing only in the builder argument (true vs false) and the port number. The pattern is repeated verbatim across 10 lines each.
   *Recommendation:* The tests are intentionally independent per the comment rationale, and littletest lacks EXPECT_NO_THROW, so the duplication is hard to avoid without a helper. If a local lambda or free helper `does_not_throw_feature_unavailable(create_webserver_fn)` were introduced it would shrink each test body to two lines. This is optional polish only — do not change if the added helper would itself require a comment to explain.

15. [ ] **code-simplifier** | `test/unit/webserver_register_ws_smartptr_test.cpp:311` | naming
   The new suite added on HAVE_WEBSOCKET-off builds is named `webserver_register_ws_smartptr_off_suite` (line 311), while its mirror for the on-path is `webserver_register_ws_smartptr_suite` (line 130). This asymmetry in naming means the suite names do not clearly communicate their complementary relationship. The paired TU webserver_ws_available_test.cpp uses `webserver_ws_available_suite` which mirrors `webserver_ws_unavailable_test.cpp`'s suite name — a cleaner convention.
   *Recommendation:* Rename `webserver_register_ws_smartptr_off_suite` to `webserver_register_ws_smartptr_unavailable_suite` to match the `_available` / `_unavailable` naming pattern used by the paired TU suites elsewhere in this task.

16. [ ] **code-simplifier** | `test/unit/webserver_register_ws_smartptr_test.cpp:311` | naming
   The off-suite name webserver_register_ws_smartptr_off_suite (line 311) follows a different suffix convention from all other suite names in this file and the sibling available/unavailable files, which use _suite without an _off infix. The _off infix is unique to this suite.
   *Recommendation:* Rename to webserver_register_ws_smartptr_unavailable_suite to align with the webserver_ws_unavailable / webserver_dauth_unavailable naming pattern used elsewhere, or simply webserver_register_ws_smartptr_ws_off_suite to make the feature-flag axis explicit. Either is clearer than _off_suite which requires reading the surrounding #ifndef to understand the scope.

17. [ ] **code-simplifier** | `test/unit/webserver_ws_available_test.cpp:81` | code-structure
   unique_ptr_null_throws_invalid_argument and shared_ptr_null_throws_invalid_argument (lines 81-112) share identical two-bool flag + try/catch + two LT_CHECK scaffolding, differing only in the smart-pointer type and port number. The same pattern also appears in webserver_register_ws_smartptr_test.cpp null_unique_ptr_throws / null_shared_ptr_throws (lines 195-217) as a single-flag variant.
   *Recommendation:* As with the dauth case, a helper lambda parameterised on the registration call would eliminate the repetition, but is optional given that littletest has no EXPECT_THROW/EXPECT_NO_THROW macros and the tests are already well-named. The two-bool variant (catching both feature_unavailable and invalid_argument) is load-bearing for the throw-type contrast, so collapsing it into a one-bool form would change the contract pinning.

18. [ ] **code-simplifier** | `test/unit/webserver_ws_available_test.cpp:99` | code-structure
   shared_ptr_null_throws_invalid_argument (lines 97-112) and unique_ptr_null_throws_invalid_argument (lines 80-95) each create a fresh `httpserver::webserver ws{...}` on different ports (8190, 8191) solely to call register_ws_resource with a null pointer. Both tests assert exactly the same two booleans with the same logic. The distinction being tested is the call-site type (unique_ptr vs shared_ptr), but because the underlying null-guard is documented as shared (comment lines 184-194 in webserver_register_ws_smartptr_test.cpp), a single test with two successive null-ptr calls on the same webserver instance would cover the same surface at half the port and setup cost.
   *Recommendation:* Consider merging into a single test `null_smartptr_throws_invalid_argument` that calls both overloads in sequence. If the intent is to keep the two throw-type contracts as independently-named regression hooks (which is a valid test-documentation goal), add a one-line comment to each test body cross-referencing the other — the current tests have comments about the unavailable-TU contrast but say nothing about their pair.

19. [ ] **housekeeper** | `specs/tasks/M7-v2-cleanup/TASK-081.md:9` | documentation-stale
   The Goal section still names 'test/unit/webserver_ws_unavailable_test.cpp:30-33 — entire suite empty on HAVE_WEBSOCKET-on builds' and 'test/unit/webserver_dauth_unavailable_test.cpp:33-37 — entire suite empty on HAVE_DAUTH-on builds' as the problem. The chosen resolution was to add paired companion TUs (webserver_ws_available_test.cpp, webserver_dauth_available_test.cpp) rather than inverting the existing guard. The goal text could note the 'add paired TU' approach was chosen over the 'invert guard' approach to keep the resolved design rationale visible, though this is optional.
   *Recommendation:* Optionally update the Goal section to mention the paired-TU strategy chosen, so future readers understand why both the _unavailable and _available TUs exist.

20. [ ] **performance-reviewer** | `test/unit/webserver_ws_available_test.cpp:82` | resource-leak
   Two tests (unique_ptr_null_throws_invalid_argument and shared_ptr_null_throws_invalid_argument) construct a full webserver on ports 8190 and 8191 without listen_socket(false). Unlike the dauth companion which correctly uses .listen_socket(false) to suppress the real bind, these two construct with a live socket intent. If MHD does attempt a bind on those ports during the test binary's lifetime the fd is leaked on the exception path. The constructor throws in this test, so MHD likely never starts, but the asymmetry with the dauth TU's pattern is worth noting.
   *Recommendation:* Add .listen_socket(false) to the create_webserver calls on lines 82 and 99 to match the established port-safe pattern in webserver_dauth_available_test.cpp and keep the intent explicit.

21. [ ] **security-reviewer** | `test/unit/body_test.cpp:338` | error-handling
   deferred_body_trampoline_null_cls_returns_error asserts that trampoline(nullptr, ...) returns MHD_CONTENT_READER_END_WITH_ERROR. This is the correct defensive behaviour. However the test does not cover the complementary case: a non-null cls pointing to a deferred_body whose internal std::function is empty (moved-from or default-constructed). An empty std::function would throw std::bad_function_call at runtime inside an MHD I/O thread, which would terminate the process. The guard for null cls alone is insufficient if deferred_body can be left in a moved-from state after a move-construct/assign.
   *Recommendation:* Add a test case that constructs a deferred_body with a valid callable, moves it away (leaving the source in moved-from state), then calls trampoline on the moved-from body. The trampoline should return MHD_CONTENT_READER_END_WITH_ERROR rather than throwing. If the current implementation already handles this (e.g., by checking !fn_ before calling), add a test to pin that invariant.

22. [ ] **security-reviewer** | `test/unit/http_request_operator_stream_test.cpp:125` | sensitive-data-handling
   The expose-credentials test asserts 'pass:"hunter2"' appears in streamed output (line 125) and 'Authorization:"Basic YWRtaW46aHVudGVyMg=="' appears (line 126). These assertions correctly verify the opt-in development flag is working. However the test does NOT assert that expose_credentials_in_logs() output is blocked from reaching any persistent log sink — it only tests the string format. If a future refactor accidentally wires the expose flag to the default (non-test) webserver path, credentials would silently appear in production logs. This is a test-coverage gap rather than an active vulnerability.
   *Recommendation:* Consider adding a negative-polarity test or a static_assert/comment that confirms expose_credentials_in_logs() is a test-only API (create_test_request scope only) and cannot be set via create_webserver. If create_webserver already lacks this setter, add a static_assert or comment to create_test_request.hpp documenting that the flag is test-infrastructure-only.

23. [ ] **security-reviewer** | `test/unit/http_request_operator_stream_test.cpp:65` | sensitive-data-handling
   The opt-in expose_credentials_in_logs test (operator_stream_exposes_credentials_when_opted_in) hard-codes a real-looking username 'admin' and password 'hunter2', plus a syntactically-valid Base64 credential 'YWRtaW46aHVudGVyMg==' (the Base64 encoding of 'admin:hunter2'). These are obviously test fixtures and are not production credentials, but they appear verbatim in a test that explicitly asserts the value IS written to the log stream under the expose flag. Anyone who searches the codebase for 'hunter2' will find live credential logging demonstrated. The risk is negligible in a test file — no runtime secret exposure occurs — but the value should be clearly annotated as a fictitious fixture so future reviewers do not mistake it for a real credential that leaked.
   *Recommendation:* Add a brief comment on lines 63–65 of the form '// Fictitious test fixture — not a real credential' adjacent to the .user("admin").pass("hunter2") builder calls, matching the annotation style already used at lines 21-27 (the TASK-057 block comment). No code change is strictly required; a comment is sufficient to document intent.

24. [ ] **spec-alignment-checker** | `test/PORTABILITY.md:173` | specification-gap
   The PORTABILITY.md entry for http_response_factories_test.cpp (lines 173-186) notes that the pipe_factory_signature_is_single_arg static_assert is 'wrapped inside the #ifndef _WIN32 block but is itself portable; a future Windows port could split the static_assert out so it runs on every lane while the runtime test stays gated'. The task's action item for body/response/iovec tests explicitly offered 'write Windows-shaped variant OR document the gap in PORTABILITY.md' as alternatives. The documentation path was chosen and the documentation is thorough. This is intentional and acceptable per the task spec; noting it only to flag the improvement hint is recorded but not actioned.
   *Recommendation:* The restoration plan is recorded. Consider splitting the static_assert outside the #ifndef _WIN32 gate in a follow-up cleanup (TASK-085 residual test-smell sweep is a candidate home).

25. [ ] **spec-alignment-checker** | `test/unit/http_request_operator_stream_test.cpp:152` | specification-gap
   The HAVE_BAUTH-off complement tests (operator_stream_credential_surfaces_absent_on_bauth_off, operator_stream_redacts_authorization_header_on_bauth_off, operator_stream_redacts_cookies_on_bauth_off) at lines 152-213 check that authorization headers and cookies are still redacted on a HAVE_BAUTH-off build. The comment at line 148 states 'the builder calls become no-ops at the field level, and the streamed line is user:"" pass:"<redacted>"'. The test for the credential-surfaces-absent case (line 153) checks that 'admin' does not appear in output and 'hunter2' does not appear, which is correct. However, it does not positively verify user:"" (empty string) is emitted. This is a minor gap in the pin but does not misrepresent the contract.
   *Recommendation:* Consider adding LT_CHECK(out.find("user:\"\"") != std::string::npos) to operator_stream_credential_surfaces_absent_on_bauth_off to positively pin the empty user field on HAVE_BAUTH-off builds. Optional improvement, not a blocker.

26. [ ] **spec-alignment-checker** | `test/unit/webserver_ws_available_test.cpp:66` | ears-requirement
   PRD-FLG-REQ-003 requires webserver::features() to return a struct of bool fields reporting basic_auth, digest_auth, TLS, and websocket. The new webserver_ws_available_test.cpp only checks f.websocket; the existing webserver_features_test.cpp already covers the full struct contract. This is correct partitioning of responsibility, but the task description cites PRD-FLG-REQ-003 as a related requirement implying each new suite should contribute coverage of features(). Both webserver_ws_available and webserver_dauth_available do call features() (lines 69 and 57 respectively) and check one relevant field each, which is adequate incremental coverage. No gap; noting for traceability.
   *Recommendation:* No change needed. The features() struct contract is fully pinned by webserver_features_test.cpp; the two new paired suites add the complementary ON-path flag-state pin without duplicating the full struct shape test.

27. [ ] **test-quality-reviewer** | `test/unit/body_test.cpp:179` | implementation-coupling
   file_body_size_known_before_materialize hard-codes the expected file size as 21 bytes ('test content of file\n'). This couples the test to the exact on-disk content of the test_content fixture file. If the fixture is updated during future development, this test will fail with no obvious diagnostic pointing at the fixture file vs. the production code.
   *Recommendation:* Replace the hard-coded 21 with a runtime measurement (e.g., open the file and fstat it independently, then compare b.size() against that value). Alternatively, add a comment immediately above the LT_CHECK_EQ explaining 'test_content is exactly 21 bytes: "test content of file\n" — update this assertion if the fixture file is intentionally changed'. The current test is not wrong, but it is brittle in a way that could mislead future maintainers.

28. [ ] **test-quality-reviewer** | `test/unit/http_request_operator_stream_test.cpp:152` | naming-convention
   The three HAVE_BAUTH-off companion tests (operator_stream_credential_surfaces_absent_on_bauth_off, operator_stream_redacts_authorization_header_on_bauth_off, operator_stream_redacts_cookies_on_bauth_off) each cover a single concern and are well-named individually. However, the concerns were split across three separate tests on the off-lane while the HAVE_BAUTH-on lane covers all of them in two tests (operator_stream_redacts_credentials / operator_stream_exposes_credentials_when_opted_in). This asymmetry means adding a new credential surface requires updating tests in both branches but the structural symmetry is not obvious, increasing maintenance cost.
   *Recommendation:* Low priority — the existing tests are correct and readable as-is. If the file is touched again, consider consolidating the three off-lane tests into two that mirror the on-lane structure (a 'default redaction' test covering pass/Authorization/cookie together, and a second for the expose_credentials path which is a no-op on the off lane). This is a style suggestion, not a correctness issue.

29. [ ] **test-quality-reviewer** | `test/unit/webserver_register_ws_smartptr_test.cpp:331` | redundant-test
   null_unique_ptr_throws_feature_unavailable_on_ws_off (HAVE_WEBSOCKET-off suite) pins the exact same throw-type contract that webserver_ws_unavailable_test.cpp already exercises for the same call site. The comment in the test acknowledges this overlap ('the same contract pinned by webserver_ws_unavailable_test.cpp') but justifies it as a guard against 'a regression in the smartptr-overload throw routing'. That reasoning is valid for the shared_ptr overload, but the unique_ptr shim-conversion path is the only branch distinguishing this TU from the unavailable TU, and both TUs call the same underlying overload. On a HAVE_WEBSOCKET-off build, both register a null unique_ptr via the same public method — the throw-routing regression the comment fears would be caught equally well by either TU alone.
   *Recommendation:* Consider removing null_unique_ptr_throws_feature_unavailable_on_ws_off from the HAVE_WEBSOCKET-off companion and keeping only the features_reports_websocket_off test. The null-arg throw-type contract on the off lane is already fully pinned by webserver_ws_unavailable_test.cpp (which owns that lane's contract). If you want both call sites pinned, add a brief comment documenting the deliberate duplication so future reviewers understand it is intentional cross-TU defence-in-depth rather than an oversight.
