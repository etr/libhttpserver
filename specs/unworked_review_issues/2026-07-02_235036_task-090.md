# Unworked Review Issues

**Run:** 2026-07-02 23:50:36
**Task:** TASK-090
**Total:** 26 (0 critical, 2 major, 24 minor)

## Major

1. [ ] **code-simplifier** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-090/scripts/check-codeql-workflow.sh:92` | code-structure
   Assertion (c) uses a while-read loop that spawns a subshell per uses: line (via `printf '%s\n' "$line" | grep -qE ...`). This is unnecessarily complex when a single `grep -v` invocation would collect all non-matching lines in one pass.
   *Recommendation:* Replace the while-read loop (lines 93-98) with: `bad_pins="$(printf '%s\n' "$uses_lines" | grep -vE '@[0-9a-f]{40}([[:space:]]|$)' || true)"`. Then print with `echo "$bad_pins" | sed 's/^/    /' >&2` instead of `printf '%b\n' "$bad_pins"`. This removes one subshell fork per action step, is idiomatic bash, and avoids the `\n`-in-variable accumulation pattern.

2. [ ] **code-simplifier** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-090/scripts/check-codeql-workflow.sh:92` | code-structure
   Assertion (c) accumulates non-SHA-pinned uses: lines through a while-read loop (lines 92-106) that pipes each line through printf + grep, builds a $bad_pins string with embedded \n escape sequences, and finally emits them with printf '%b'. The [ -z "$line" ] && continue guard inside the loop is dead code: grep never emits empty lines, so $uses_lines cannot contain them. The entire loop can be replaced by a single grep -v pipeline, which also eliminates the printf '%b' trick and removes ~6 lines of accidental complexity. This finding was raised in iter1 and the code is unchanged here.
   *Recommendation:* Replace lines 92-106 with:
  bad_pins="$(printf '%s\n' "$uses_lines" | grep -vE '@[0-9a-f]{40}([[:space:]]|$)' || true)"
  if [ -n "$bad_pins" ]; then
      echo "  (c) uses: line(s) not pinned to a 40-hex SHA:" >&2
      printf '    %s\n' "$bad_pins" >&2
      fail=1
  else
      echo "  (c) every uses: line pinned to a 40-hex commit SHA"
  fi
Behavior is identical; the dead-code empty-line guard disappears naturally.

## Minor

3. [ ] **architecture-alignment-checker** | `Makefile.am:536` | pattern-violation
   lint-codeql-workflow and lint-workflow-pinning both run their companion unit-test script before the real gate (test_check_codeql_workflow.sh / test_check_workflow_pinning.sh), while the pre-existing lint-parallel-install-lane only runs the gate script directly. This makes `make lint-codeql-workflow` self-validating but creates a minor inconsistency: test_check_parallel_install_lane.sh is never invoked from any make target, even though the analogous test_check_codeql_workflow.sh is now always executed when the lint-codeql-workflow make target runs.
   *Recommendation:* Either update lint-parallel-install-lane to also run test_check_parallel_install_lane.sh before the gate (bringing the older target up to the new pattern), or document in the new targets' comments that this is an intentional enhancement over the prior idiom. Either direction is fine; the key is consistency across lane-gate targets.

4. [ ] **cloud-infrastructure-reviewer** | `.github/workflows/codeql-analysis.yml:42` | iac-anti-pattern
   [Operational Excellence — Reliability] No step in codeql-analysis.yml carries a timeout-minutes: guard. A hung dependency build or a stalled CodeQL extractor will hold a runner until GitHub's 6-hour job ceiling. verify-build.yml sets timeout-minutes: 90 on the Valgrind check step as a precedent; applying the same pattern to the build and analyze steps would cap runaway jobs and surface hangs sooner.
   *Recommendation:* Add timeout-minutes: 30 to the 'Install libmicrohttpd dependency', 'Build the library (CodeQL manual mode)', and 'Perform CodeQL Analysis' steps. See https://docs.github.com/en/actions/writing-workflows/workflow-syntax-for-github-actions#jobsjob_idstepstimeout-minutes

5. [ ] **code-quality-reviewer** | `.github/workflows/codeql-analysis.yml:1` | code-elegance
   The adjacent verify-build.yml restricts the default GITHUB_TOKEN to `contents: read` via a top-level `permissions:` block (line 14). codeql-analysis.yml has no permissions block, leaving it on the GitHub default (which grants write to several scopes). For a supply-chain hardening task, adding `permissions: contents: read` globally with a per-job `security-events: write` override for the CodeQL upload would be consistent and defense-in-depth.
   *Recommendation:* Add `permissions: contents: read` at the top level of codeql-analysis.yml, and override with `security-events: write` at the job level (required by `github/codeql-action/analyze` to upload the SARIF result). This mirrors the pattern already established in verify-build.yml.

6. [ ] **code-quality-reviewer** | `scripts/check-codeql-workflow.sh:118` | code-readability
   Assertion (e) checks that `./configure` and `make` each appear somewhere in the workflow file independently, not necessarily in the same step. A workflow that removed the explicit build step but still had `./configure` in a comment and `make` mentioned in a step name would pass the gate. The check is sufficient for the current workflow shape but the limitation is undocumented.
   *Recommendation:* Add a one-line inline comment noting the check is file-wide, e.g. `# (sufficient: both must appear; false-positives from comments are negligible for this file shape)`. No logic change required.

7. [ ] **code-quality-reviewer** | `scripts/check-codeql-workflow.sh:159` | readability
   The failure message for assertion (h) recommends 'echo "<sha256>  libmicrohttpd-1.0.3.tar.gz" | sha256sum -c' with a literal '<sha256>' placeholder. A developer seeing this message would not know which digest value to substitute and could not act on it without reading additional files.
   *Recommendation:* Replace the '<sha256>' placeholder with the actual expected digest (7816b57aae199cf5c3645e8770e1be5f0a4dfafbcb24b3772173dc4ee634126a) or add a pointer such as 'see the rotation comment in codeql-analysis.yml or verify-build.yml for the authoritative digest'.

8. [ ] **code-quality-reviewer** | `scripts/check-codeql-workflow.sh:20` | readability
   The header comment block lists assertions (a) through (f) only. Assertions (g) and (h) added in iteration 2 are not mentioned in that enumeration, so a reader scanning the header gets an incomplete picture of what the gate enforces.
   *Recommendation:* Extend the header list to include '(g) a permissions block with security-events: write must be declared' and '(h) the libmicrohttpd download must be followed by a sha256sum -c verification step', matching the inline comments at lines 141 and 152.

9. [ ] **code-quality-reviewer** | `scripts/check-codeql-workflow.sh:65` | code-readability
   The inline Python snippet uses `yaml.safe_load(open(path))` without a context manager, leaving the file handle open until CPython's garbage collector closes it. This appears identically in check-workflow-pinning.sh (line 69). Functionally harmless in CPython, but deviates from idiomatic Python style.
   *Recommendation:* Replace with `with open(path) as fh: yaml.safe_load(fh)` in both gate scripts. Same change applies to check-workflow-pinning.sh line 69.

10. [ ] **code-quality-reviewer** | `scripts/test_check_codeql_workflow.sh:122` | readability
   Test 6 comment says 'The fixture keeps SHA-pinned refs + a build step so only assertion (a) fires.' After adding assertions (g) and (h), the bad-YAML fixture also lacks the permissions block and sha256sum verification, so (g) and (h) also fire on that fixture. The comment is now stale.
   *Recommendation:* Update the comment to note that the bad-YAML fixture additionally triggers (g) and (h) because no permissions block or sha256sum line is present, or restructure the fixture to carry those elements so only (a) fires as the comment claims.

11. [ ] **code-quality-reviewer** | `scripts/test_check_workflow_pinning.sh:74` | test-coverage
   The test suite covers `fail_ci_if_error: false` (Test 2) and `fail_ci_if_error: true` (Test 1), but there is no test for the third branch in check-workflow-pinning.sh assertion (c): the case where `fail_ci_if_error` is absent entirely from verify-build.yml. That branch also exits 1, but it is untested.
   *Recommendation:* Add a Test 7 fixture that omits the `fail_ci_if_error` key entirely (e.g. `sed '/fail_ci_if_error/d' "$VERIFY_GOOD"`) and asserts the gate exits 1.

12. [ ] **code-simplifier** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-090/scripts/check-codeql-workflow.sh:126` | naming
   `sed 's/.*@//'` to strip the action-path prefix from the grep output is less readable than the equivalent `cut -d@ -f2`. The sed expression is a greedy `.*` match that requires the reader to infer what is being stripped; cut makes the delimiter explicit.
   *Recommendation:* Replace `| sed 's/.*@//'` with `| cut -d@ -f2` on lines 126 and 127. Both produce identical output for strings of the form `codeql-action/init@<sha>`, and cut communicates the intent (split on `@`, take field 2) more directly.

13. [ ] **code-simplifier** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-090/scripts/check-codeql-workflow.sh:126` | naming
   Assertion (f) uses sed 's/.*@//' to strip the action-name prefix and extract the SHA (lines 126-127). The greedy .* makes the intent slightly opaque; the reader must reason that there is exactly one @ in the matched string. cut -d@ -f2 expresses the same extraction more directly: 'take the second field when splitting on @'. This finding was raised in iter1 and the code is unchanged.
   *Recommendation:* Replace:
  init_sha="$(grep -oE 'codeql-action/init@[0-9a-f]{40}' "$WF" | head -1 | sed 's/.*@//')"
  analyze_sha="$(grep -oE 'codeql-action/analyze@[0-9a-f]{40}' "$WF" | head -1 | sed 's/.*@//')"
With:
  init_sha="$(grep -oE 'codeql-action/init@[0-9a-f]{40}' "$WF" | head -1 | cut -d@ -f2)"
  analyze_sha="$(grep -oE 'codeql-action/analyze@[0-9a-f]{40}' "$WF" | head -1 | cut -d@ -f2)"
Pure cosmetic; behavior is identical.

14. [ ] **code-simplifier** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-090/scripts/check-codeql-workflow.sh:65` | code-structure
   `yaml.safe_load(open(path))` in the inline Python snippet leaves the file handle unclosed. The same pattern appears in check-workflow-pinning.sh line 69. While harmless in practice (the process exits moments later), it is non-idiomatic Python and will generate resource-leak warnings under some linters.
   *Recommendation:* Replace `yaml.safe_load(open(path))` with `with open(path) as f: yaml.safe_load(f)` in both gate scripts. The change is two lines instead of one but the `with` statement is the canonical Python idiom for file I/O.

15. [ ] **code-simplifier** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-090/scripts/test_check_codeql_workflow.sh:100` | code-structure
   Test 5 (missing explicit build step) filters `./bootstrap` from the good fixture along with `./configure` and `make ;`. Assertion (e) in the gate only checks for `./configure` and `make` — it does not check for `./bootstrap`. The extra `-e './bootstrap'` filter is dead code that misleads readers into thinking `./bootstrap` is part of the tested invariant.
   *Recommendation:* Remove `-e './bootstrap'` from the grep -v invocation on line 100. The resulting NO_BUILD fixture will still lack `./configure` and `make ;`, which is what assertion (e) checks, so the test outcome is unchanged.

16. [ ] **housekeeper** | `specs/product_specs.md:32` | spec-not-updated
   TASK-090.md 'Related Requirements: PRD §2 supply-chain NFR' references a requirement that does not exist in specs/product_specs.md §2 Non-functional & cross-cutting requirements. The section has no supply-chain bullet covering SHA-pinning of CI actions or Codecov upload failure handling. This is a CI-internal concern so the omission is arguably tolerable, but the cross-reference is dangling.
   *Recommendation:* Either add a supply-chain NFR bullet to specs/product_specs.md §2 (e.g. 'All GitHub Actions in CI workflows shall be pinned to full 40-hex commit SHAs; Codecov upload failures shall break CI'), or change the 'Related Requirements' line in TASK-090.md to 'None' since this task governs internal tooling rather than product behavior. Run /groundwork:source-product-specs-from-code if you choose to add the requirement.

17. [ ] **performance-reviewer** | `.github/workflows/codeql-analysis.yml:55` | missing-caching
   libmicrohttpd dependency is downloaded and compiled from source on every CodeQL run (push to master, PR to master, weekly schedule) with no actions/cache layer. verify-build.yml caches the same artifact under the key 'libmicrohttpd-1.0.3-pre-built-v2'. Each uncached run spends ~2-3 minutes on configure+make for a dependency that does not change between runs.
   *Recommendation:* Add a 'Fetch libmicrohttpd from cache' step using actions/cache@<sha> (keyed on '${{ runner.os }}-libmicrohttpd-1.0.3-codeql') before the 'Install libmicrohttpd dependency' step, and guard the build step on 'steps.cache-libmicrohttpd.outputs.cache-hit != true', mirroring the pattern at lines 695-723 of verify-build.yml.

18. [ ] **security-reviewer** | `.github/workflows/codeql-analysis.yml:57` | insecure-design
   The curl invocation uses `curl https://... -o file` without the `-f` flag. If the server returns a 4xx or 5xx error, curl still exits 0 and writes the HTTP error body into the output file. `tar -xzf` then fails on what it thinks is a corrupted archive, but the error is misleading. All curl calls in verify-build.yml use `-fsSL`. In the CodeQL workflow the risk is lower than the missing checksum (finding 2), but the pattern is still inconsistent with the rest of the CI.
   *Recommendation:* Change the curl invocation to: `curl -fsSL https://s3.amazonaws.com/libhttpserver/libmicrohttpd_releases/libmicrohttpd-1.0.3.tar.gz -o libmicrohttpd-1.0.3.tar.gz`

19. [ ] **security-reviewer** | `scripts/check-codeql-workflow.sh:65` | insecure-design
   The Python heredoc uses `open(path)` without an explicit encoding, which relies on the locale's default. This is not exploitable in CI (the runner locale is UTF-8) but is a latent hazard if the workflow YAML ever contains non-ASCII characters (e.g., in the emoji-bearing comments removed in this task). It is also inconsistent with Python 3 best practice (PEP 597).
   *Recommendation:* Change `yaml.safe_load(open(path))` to `yaml.safe_load(open(path, encoding='utf-8'))` for robustness. The same applies in the corresponding heredoc in check-workflow-pinning.sh.

20. [ ] **spec-alignment-checker** | `.github/workflows/verify-build.yml:null` | specification-gap
   The CI lint-lane steps call the gate scripts directly (`./scripts/check-codeql-workflow.sh`, `./scripts/check-workflow-pinning.sh`) rather than the Makefile lint targets (`lint-codeql-workflow`, `lint-workflow-pinning`). This means the fixture-driven self-tests (test_check_codeql_workflow.sh, test_check_workflow_pinning.sh) are not executed by CI — only by `make lint-codeql-workflow` / `make lint-workflow-pinning` locally. The gate logic itself is exercised by CI; only the self-test of the gate is absent from CI. This is the same pattern used by check-parallel-install-lane.sh (TASK-089) and the other structural gates, so it is consistent with established convention but is a minor observability gap.
   *Recommendation:* Either invoke the Makefile lint targets from CI (so the self-tests also run on CI's lint lane) or accept this as the established convention for structural gate scripts — prior tasks (TASK-088, TASK-089) set the same precedent. No spec text mandates CI execution of the self-tests.

21. [ ] **spec-alignment-checker** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-090/specs/tasks/M7-v2-cleanup/TASK-090.md:34` | specification-gap
   Task status field reads 'In Progress' while all four action items are checked [x] and all acceptance criteria are verifiably met. Convention in the task list is to set Status: Done once implementation is complete.
   *Recommendation:* Update **Status:** from 'In Progress' to 'Done' to keep the task index consistent with the checked-off action items.

22. [ ] **test-quality-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-090/scripts/check-codeql-workflow.sh:20` | naming-convention
   The header comment block listing assertions only enumerates (a)-(f); assertions (g) and (h) added in this iteration are implemented in the body but not listed in the header catalogue. A reader consulting the header to understand what the gate enforces will miss the two new checks.
   *Recommendation:* Extend the assertion list in the header comment to include '(g) a permissions block with security-events: write is declared' and '(h) the libmicrohttpd download is verified with sha256sum -c', matching the style of (a)-(f) already there.

23. [ ] **test-quality-reviewer** | `scripts/test_check_codeql_workflow.sh:107` | naming-convention
   Test 6 (invalid YAML) carefully constructs a fixture with SHA-pinned uses: lines, a './configure ; make ;' run step, and matching init/analyze SHAs so only assertion (a) fires — but unlike the reference idiom in test_check_valgrind_lane.sh:130-133, there is no comment documenting this protective intent. A maintainer editing the fixture without understanding the constraint might inadvertently let another assertion co-fire, making test 6 a partial validator of assertion (a) rather than an independent check.
   *Recommendation:* Add a comment above the BAD_YAML fixture block explaining that SHA-pinned refs and the explicit build step are present so that only assertion (a) fires, mirroring the documented pattern in test_check_valgrind_lane.sh:130-133.

24. [ ] **test-quality-reviewer** | `scripts/test_check_codeql_workflow.sh:67` | missing-test
   Assertion (c) — 'every non-comment uses: line pinned to a full 40-hex commit SHA' — is only exercised indirectly through test 2's @v3 floating tag, which also triggers assertion (b). A uses: line with a short hash like 'uses: actions/checkout@abc1234' (7 hex chars) would fail assertion (c) independently without triggering assertion (b). That specific branch of the 40-hex-length regex in check-codeql-workflow.sh:95 is never tested in isolation.
   *Recommendation:* Add a test that replaces one uses: pin with a shortened hash (e.g., @abc1234) that satisfies the '@[a-f0-9]+' shape but is shorter than 40 chars, and asserts exit 1. This confirms assertion (c) fires independently of assertion (b).

25. [ ] **test-quality-reviewer** | `scripts/test_check_workflow_pinning.sh:110` | missing-test
   The invalid-YAML test (test 6) injects a bad fixture only for the $2 (verify-build) workflow path. There is no symmetric test for invalid YAML in the $1 (codeql) workflow path. The gate's for-loop in check-workflow-pinning.sh:58-78 iterates over both workflows, but only one side is exercised, so a regression that silently skipped the codeql workflow in the loop would not be caught by the test suite.
   *Recommendation:* Add a test 7 that passes a bad-YAML fixture as $1 (codeql) with a valid verify fixture as $2, and assert exit 1. Mirror the same defensive fixture construction used in test 6 — include the 'Invariance exclusion (doxygen)' label and 'fail_ci_if_error: true' text in the verify fixture so only assertion (a) fires.

26. [ ] **test-quality-reviewer** | `scripts/test_check_workflow_pinning.sh:74` | missing-test
   Assertion (c) in check-workflow-pinning.sh has three outcome branches: 'fail_ci_if_error: false' → FAIL (covered by test 2), 'fail_ci_if_error: true' → PASS (covered by test 1 via the good fixture), and 'neither key present' → FAIL (the else clause at check-workflow-pinning.sh:101-104, not covered). If the else branch were accidentally removed or broken, the unit tests would not catch the regression.
   *Recommendation:* Add a test that passes a verify fixture where the 'fail_ci_if_error:' line is absent entirely (not flipped to false, just deleted) and asserts exit 1. This can be derived from the good fixture via 'grep -v fail_ci_if_error'.
