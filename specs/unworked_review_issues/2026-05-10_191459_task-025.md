# Unworked Review Issues

**Run:** 2026-05-10 19:14:59
**Task:** TASK-025
**Total:** 40 (0 critical, 2 major, 38 minor)

## Major

1. [x] **housekeeper** | `:null` | documentation-stale
   Every completed task from TASK-022 onward has a corresponding file in specs/unworked_review_issues/ (e.g. 2026-05-09_192103_task-022.md, 2026-05-10_114120_task-023.md, 2026-05-10_155313_task-024.md). TASK-025 has no such file. Prior validation runs for completed tasks always produce an unworked_review_issues file that captures any non-blocking findings from the review agents so they can be tracked. Without this file the review findings for TASK-025 will be lost, breaking the pattern that feeds TASK-026's 'unworked issues' starting context.
   *Recommendation:* After the validation loop concludes, persist the consolidated non-blocking review findings to specs/unworked_review_issues/ with a filename following the pattern YYYY-MM-DD_HHMMSS_task-025.md, consistent with how TASK-024's findings were persisted.
   *Status:* Addressed — this file IS the persisted unworked_review_issues file for TASK-025 (2026-05-10_191459_task-025.md).

2. [ ] **performance-reviewer** | `src/webserver.cpp:359` | missing-caching
   on_method_() uses std::dynamic_pointer_cast<detail::lambda_resource> on every registration call to distinguish a lambda shim from a class-based resource. This is registration-time only (not per-request), so the cast cost is not a hot-path problem. However, once the lambda_resource shim is inserted into registered_resources as a shared_ptr<http_resource>, the dispatch path (finalize_answer) will also re-traverse it via the virtual render_* mechanism, paying the vtable indirection cost that was avoidable if lambda routes had a separate lookup tier. This is a latent issue that will only matter when TASK-027 wires route_entry into the real dispatch; flagged now so the 3-tier design can accommodate a fast lambda branch.
   *Recommendation:* When TASK-027 integrates route_entry into the dispatch path, maintain a separate fast path for the lambda_handler arm of the variant (std::get<0>(entry.handler)(req)) that bypasses the virtual dispatch chain (render_get -> invoke_ -> slot lookup). This is the main motivation for route_entry carrying a variant in the first place.
   *Status:* Deferred — TASK-027 is complete and wired the v2 3-tier table. The fast lambda variant arm (bypassing virtual dispatch) is a TASK-036 or follow-up concern. The v1 shim dispatch path remains for backward compatibility and is not hot-path.

## Minor

3. [x] **architecture-alignment-checker** | `src/httpserver/detail/lambda_resource.hpp:127` | pattern-violation
   The `invoke_` fallback path calls `detail::empty_render(r)` when a slot is cleared despite the bit being set. The comment acknowledges this is theoretically unreachable via the normal 405 path, but `empty_render` is a detail-layer free function whose existence and contract are not documented in the architecture spec. If it returns an empty/default-constructed response the caller may see a 200 with no body rather than a 500, which is surprising defensive-code behavior. This is not an ADR violation but deviates from the documented pattern of always having valid handler or 405.
   *Recommendation:* Either remove the defensive branch entirely (the comment already says it is unreachable) or replace the call with an explicit assertion/abort so any future regression is immediately visible rather than silently producing a 200.
   *Status:* Addressed — `empty_render` was replaced with `assert(slot)` in a prior pass. Item 28 (security) adds the NDEBUG-safe fallback on top of the assert.

4. [x] **architecture-alignment-checker** | `src/httpserver/detail/lambda_resource.hpp:131` | pattern-violation
   The fixer replaced the defensive `empty_render(r)` fallback in `invoke_()` with `assert(slot)`. In NDEBUG (release) builds the assert is a no-op; if the invariant were ever broken, execution would proceed to call `slot(r)` on an empty `std::function`, causing `std::bad_function_call` rather than a graceful error response. The prior code's `empty_render` path was deliberately defensive against the slot/allow-bit getting out of sync. The architectural pattern for internal invariants in this codebase has been to assert in debug and degrade gracefully in release (as seen in other render fallbacks), so the change trades safety for terseness.
   *Recommendation:* Either restore the `empty_render` fallback under the assert (assert fires in debug, graceful 500-series response in release) or document explicitly that the invariant is considered unbreakable and the assert-only path is intentional. If TASK-036 is expected to remove this shim entirely before any release, the assert-only form is acceptable with that caveat noted.
   *Status:* Fixed — `invoke_()` in `lambda_resource.hpp` now has `assert(slot)` followed by an explicit `if (!slot) return http_response::string(...).with_status(500)` defensive branch. Crash-loudly in debug, safe 500 in release.

5. [x] **architecture-alignment-checker** | `src/webserver.cpp:385` | pattern-violation
   The `is_exact` heuristic (`idx.get_url_pars().empty()`) determines whether to insert into the `registered_resources_str` fast-path map. However, parameterized paths with `{id}` segments are instead inserted into `registered_resources_regex` at line 386 when `idx.is_regex_compiled()` is true. The §4.7 architecture specifies a hash map for exact paths and a radix tree for parameterised paths — the regex chain is the third-tier fallback. Routing parameterized `on_*` registrations into the regex tier rather than a radix tier is a known transitional compromise (TASK-027 will fix this), but the comment at line 320 says the shim uses "the existing v1 three-map shape" without explicitly noting that parameterized on_* routes land in the regex tier instead of the eventual radix tier. There is no semantic violation in v1 behavior, but the discrepancy from §4.7 is worth a comment at the insertion site.
   *Recommendation:* Add a one-line comment at line 385–388 noting that parameterized paths temporarily use the regex tier until TASK-027 wires up the radix tree, mirroring the existing comment at the top of on_method_ (line 319–321).
   *Status:* Addressed — TASK-027 wired the radix tree; parameterized paths now go to `param_and_prefix_routes_` (the radix tier). The v1 insertion via `registered_resources_regex` is still present in parallel but is now correctly described by the TASK-027 code comments.

6. [ ] **code-quality-reviewer** | `src/httpserver/detail/lambda_resource.hpp:120` | code-readability
   The private helper is named `invoke_` with a trailing underscore, which in this codebase is the convention for private *member data* (the webserver private fields use this convention). The helper is a private method, so the trailing underscore is slightly inconsistent with the naming style used for other private methods in the codebase (e.g., `register_impl_`, `on_method_` in webserver — though those also use the trailing underscore). Inside the class itself the render_* virtuals call `invoke_` but the convention difference between the detail class and the parent class private methods may be jarring. This is low priority.
   *Recommendation:* No change required if the codebase conventions for internal-linkage helpers allow trailing underscores on methods — this is largely a style nit. If a style guide is established, ensure it is applied consistently.
   *Status:* wontfix — cosmetic/style preference, no functional impact; recommendation says 'no change required'; trailing underscore consistent with codebase

7. [ ] **code-quality-reviewer** | `src/httpserver/detail/route_entry.hpp:64` | code-elegance
   The NOLINT comment for `whitespace/line_length` is present on the `lambda_handler` type alias line (line 64). The line is 95 characters which is only mildly over a typical 80-char limit. Given the namespace prefixes are unavoidable here this is acceptable, but the suppression comment slightly increases noise.
   *Recommendation:* No change needed. This is the right call given the fully-qualified type names, but worth tracking if a line-length lint threshold is raised to 100 chars.
   *Status:* wontfix — cosmetic/style preference, no functional impact; recommendation says 'no change needed'

8. [x] **code-quality-reviewer** | `src/webserver.cpp:339` | code-readability
   The single_resource check in `on_method_` differs subtly from the one in `register_impl_`. In register_impl_, the check is `single_resource && ((resource != "" && resource != "/") || !family)` where `!family` guards against exact matching. In on_method_, the check is `single_resource && path != "" && path != "/"`. The `!family` part is implicitly always true for on_* (which always uses exact matching), so the logic is equivalent, but the difference in expression makes it harder to verify equivalence at a glance.
   *Recommendation:* Add a brief comment such as `// on_* always uses exact (non-prefix) matching, so the !family arm from register_impl_ is always true here` to make the equivalence explicit.
   *Status:* Fixed — comment added to `on_methods_()` in `src/detail/webserver_routes.cpp` explaining that on_*/route always uses exact matching so the `!family` arm from `register_impl_` is always true and intentionally omitted.

9. [x] **code-quality-reviewer** | `test/unit/webserver_on_methods_test.cpp:201` | test-coverage
   There is no test for calling on_* with an empty (null) std::function, which is validated in on_method_ and throws std::invalid_argument. This guard exists in the implementation but is not exercised.
   *Recommendation:* Add a test that passes a default-constructed (empty) std::function to on_get and asserts std::invalid_argument is thrown.
   *Status:* Already addressed — `on_get_empty_handler_throws_invalid_argument` test exists at line 387 of the test file.

10. [ ] **code-quality-reviewer** | `test/unit/webserver_on_methods_test.cpp:285` | test-coverage
    all_seven_on_methods_serve_their_method uses a single webserver for all seven methods and checks response body but not the 200 status code for GET/POST/PUT/DELETE/PATCH/OPTIONS. If the dispatch were silently failing (e.g. returning a non-200 with empty body) the body check would still pass.
    *Recommendation:* Add a response_code assertion alongside each body check, or rely on the new per-method isolation tests (on_get_dispatches_get through on_head_dispatches_head) which individually assert both code and body. The latter already exist, so this is a low-priority polish note.
    *Status:* Deferred — the seven individual per-method tests (`on_get_dispatches_get` through `on_head_dispatches_head`) already verify dispatch. The omnibus test is described by item 38 as a composition test; its body checks are sufficient for that purpose.

11. [x] **code-quality-reviewer** | `test/unit/webserver_on_methods_test.cpp:366` | test-coverage
    The test `on_get_then_register_path_with_class_throws` only exercises one direction of the mixing guard (lambda first, then class). The symmetric direction (register_path first, then on_*) is covered by `on_method_`'s `dynamic_pointer_cast` check, but there is no test asserting it. The asymmetry means a future refactor of register_impl_ could silently remove the reverse guard.
    *Recommendation:* Add a test `register_path_then_on_get_throws` that calls `register_path` first and then `on_get` on the same path, asserting std::invalid_argument is thrown.
    *Status:* Already addressed — `register_path_then_on_get_same_path_throws` test exists at line 418 of the test file.

12. [ ] **code-quality-reviewer** | `test/unit/webserver_on_methods_test.cpp:367` | test-coverage
    on_get_then_register_path_with_class_throws and register_path_then_on_get_same_path_throws test the conflict path in on_method_ (lambda vs class) but only for register_path, not register_prefix. register_prefix also inserts into registered_resources, so a lambda registered at /x and a register_prefix("/x", ...) call would hit the same conflict-detection code. The symmetry is not exercised.
    *Recommendation:* Add a test case covering register_prefix followed by on_get on the same path (and vice versa), or add a comment in the test file explaining why register_prefix is intentionally out of scope for TASK-025 (e.g., because prefix and exact paths cannot collide by construction at the endpoint level).
    *Status:* Deferred — register_prefix uses `family=true` which creates a distinct `http_endpoint` key from the `family=false` key used by `on_*/register_path`. They do not collide by key construction. Documenting this is low priority; the test scope is intentionally limited to exact-path conflict detection.

13. [x] **code-quality-reviewer** | `test/unit/webserver_on_methods_test.cpp:78` | code-elegance
    header_func constructs a temporary std::string from the kAllowPrefix constant on line 78 solely to call .size() on it: `std::string(kAllowPrefix).size()`. kAllowPrefix is a string literal; strlen or std::char_traits::length would avoid the heap allocation, or the length could be named as a constexpr std::size_t.
    *Recommendation:* Replace `std::string(kAllowPrefix).size()` with `std::strlen(kAllowPrefix)` or introduce `constexpr std::size_t kAllowPrefixLen = 6;` alongside the constant to avoid the temporary string.
    *Status:* Fixed — replaced `std::string(kAllowPrefix).size()` with `std::strlen(kAllowPrefix)` and added `#include <cstring>` in `test/unit/webserver_on_methods_test.cpp`.

14. [ ] **code-simplifier** | `src/httpserver/detail/lambda_resource.hpp:90` | code-structure
    The 7 render_* overrides now carry a block comment explaining they cannot be collapsed without changing the base-class interface. The comment is accurate and the constraint is real. The only residual polish opportunity is that the comment sits before the first override rather than as a class-level doc comment, making it slightly easy to miss when scanning the private section. This is cosmetic.
    *Recommendation:* No change required. The current placement is acceptable. Optionally move the explanation into the class-level doc comment block at line 66 to keep all architectural notes together, but this is not a blocker.
    *Status:* wontfix — cosmetic/style preference, no functional impact; recommendation says 'no change required'; comment placement acceptable

15. [x] **code-simplifier** | `src/webserver.cpp:337` | naming
    The local variable `fresh` is a boolean flag used to gate a `if (fresh)` block that inserts the new shim into the three storage maps. 'fresh' is informal; the intent is 'this is a newly created shim that has not yet been inserted'.
    *Recommendation:* Rename `fresh` to `is_new_entry` or `newly_created` to communicate the intent without ambiguity. The existing `shim` variable name is already clear; the flag should match that precision.
    *Status:* Fixed — renamed `fresh` to `is_new_entry` in `on_methods_()` in `src/detail/webserver_routes.cpp`.

16. [x] **code-simplifier** | `src/webserver.cpp:349` | naming
    The local boolean `fresh` (line 349) is a fine name but `is_new_entry` would more directly express the intent at the insertion site (line 356: `if (fresh)`). This is a very minor naming preference and does not affect clarity meaningfully.
    *Recommendation:* No change required. `fresh` is clear enough in context. Renaming to `is_new_entry` would be a marginal improvement only.
    *Status:* Addressed — renamed as part of item 15 fix.

17. [x] **code-simplifier** | `src/webserver.cpp:356` | code-structure
    The `registered_resources_lock.unlock()` call at the end of on_method_ is explicit, but the lock is a std::unique_lock — it will unlock automatically on scope exit. Explicit unlock before `invalidate_route_cache()` is intentional (releasing the mutex before the cache invalidation, which may acquire its own lock), but this intent is not documented. A reader unfamiliar with the locking protocol may wonder whether the explicit unlock is a mistake or a necessity.
    *Recommendation:* Add a one-line comment: `// Release route-table lock before invalidating the LRU cache (cache has its own lock).` This makes the ordering decision visible and prevents a future refactor from accidentally removing the early unlock.
    *Status:* Fixed — the explicit unlock was replaced by a scoped block in a prior pass. Added a comment to the scoped block in `src/detail/webserver_routes.cpp` explaining the lock-ordering rationale (v1 mutex released before v2/cache mutexes to prevent deadlock).

18. [ ] **code-simplifier** | `src/webserver.cpp:380` | code-structure
    on_delete has a one-line comment explaining the `del` enum spelling (keyword collision). The other six on_* forwarders have no body comments. The asymmetry is harmless but slightly inconsistent. The `del` comment is genuinely useful; no action needed unless a team style decision prefers uniform comment presence.
    *Recommendation:* No change required. The asymmetry is justified because only `del` has a non-obvious spelling. The comment explains intent correctly.
    *Status:* wontfix — cosmetic/style preference, no functional impact; recommendation says 'no change required'; asymmetry justified

19. [ ] **code-simplifier** | `test/unit/webserver_on_methods_test.cpp:133` | code-structure
    The static_assert block for the seven on_* overloads repeats the decltype(std::declval<webserver&>().on_X(...)) pattern seven times with only the method name changing. This is verbose and will drift if a new on_* method is added.
    *Recommendation:* This is a test file and verbose static_asserts are common for clarity, but a helper alias template (e.g. `template<typename Fn, typename R> using on_sig = R`) or a macro could reduce the seven nearly-identical blocks to a table. Since this is test code the impact is low, but it is worth noting for when the set of supported methods grows.
    *Status:* wontfix — cosmetic/style preference, no functional impact; static_assert verbosity in tests acceptable; aids readability

20. [x] **housekeeper** | `specs/architecture/04-components/route-table.md:null` | spec-not-updated
    specs/architecture/04-components/route-table.md (§4.7) documents the route_entry type shape (method_set + variant + is_prefix) but does not note that TASK-025 has shipped the detail::route_entry type in src/httpserver/detail/route_entry.hpp with a lambda_resource shim for the existing v1 three-map storage, while TASK-027 will plug route_entry into the real 3-tier table. The spec currently implies the full 3-tier table and route_entry wiring are a single unit of work, which could confuse a reader checking §4.7 against the current codebase.
    *Recommendation:* Add a brief implementation-status note to route-table.md under §4.7 stating: 'TASK-025 introduces detail::route_entry and the lambda_resource shim into the existing v1 three-map storage. TASK-027 will replace the three-map storage with the full 3-tier table and route_entry wiring described here.' Run /groundwork:source-architecture-from-code if a full re-derive is preferred.
    *Status:* Fixed — added Implementation status note to `specs/architecture/04-components/route-table.md` documenting the TASK-025/TASK-027 staging.

21. [x] **housekeeper** | `specs/architecture/04-components/route-table.md:null` | documentation-stale
    The route-table spec documents the §4.7 `route_entry` type and 3-tier table as a future target. TASK-025 ships the `route_entry` type itself (as a detail header) but storage remains the v1 three-map shape via the lambda_resource shim until TASK-027. The spec does not note this interim state, but the TASK-025.md action item text already captures it clearly ('TASK-025 ships the §4.7-shape detail::route_entry type plus the on_* entry points; storage is the existing v1 three-map shape via a hidden detail::lambda_resource shim. TASK-027 will plumb route_entry into the real 3-tier table.'). This is a minor documentation nuance with no blocking impact.
    *Recommendation:* Optionally add a brief note to specs/architecture/04-components/route-table.md clarifying that route_entry is introduced in TASK-025 as a detail type, with full 3-tier integration deferred to TASK-027. Not blocking.
    *Status:* Fixed — same note as item 20 addresses this.

22. [x] **housekeeper** | `specs/architecture/11-decisions/DR-004.md:null` | spec-not-updated
    DR-004 (handler return type) documents the decision and its consequences (PRD-HDL-REQ-001 amendment) but does not reference the on_* entry points or the lambda_handler type alias that TASK-025 introduced as the first concrete realisation of this decision. The decision record is not wrong, but it predates any implementation and a future reader tracing DR-004 → code has no pointer to where the decision was first implemented.
    *Recommendation:* Add a one-line 'First implemented in:' note under DR-004 Consequences: 'First realised in TASK-025 via detail::lambda_handler (std::function<http_response(const http_request&)>) in src/httpserver/detail/route_entry.hpp.' This is optional polish given DR-004 is already correct.
    *Status:* Fixed — added "First realised in TASK-025" note to `specs/architecture/11-decisions/DR-004.md` Consequences section.

23. [x] **performance-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-025/src/httpserver/detail/lambda_resource.hpp:136` | missing-caching
    iter1-1 re-validation: make_shared<http_response>(slot(r)) is the minimal expression of the DR-004 value-return contract. http_response is move-only (copy constructor is deleted), so the prvalue returned by slot(r) is move-constructed directly into the shared_ptr control block via make_shared — one allocation, one move, no extra copy. No in-scope optimization exists without changing the parent http_resource virtual signature (shared_ptr<http_response> return type), which is out of scope for TASK-025. Documentation-only response accepted.
    *Recommendation:* No action required in TASK-025. TASK-036 (noted in the comment) is the correct venue if the virtual return type is ever changed to a value or a type-erased handle that avoids the per-request heap allocation entirely.
    *Status:* Deferred — recommendation says "No action required in TASK-025".

24. [x] **performance-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-025/src/httpserver/detail/lambda_resource.hpp:136` | missing-caching
    iter1-2 re-validation: the std::function indirect call in invoke_() sits behind two existing vtable dispatches (the v1 per-method render_* virtual and the finalize_answer method-function-pointer dispatch). The incremental overhead of std::function type erasure is noise relative to the virtual dispatch chain already in play. TASK-027 will bypass the lambda_resource shim entirely by dispatching directly through the route_entry variant, eliminating both the virtual calls and the std::function call simultaneously. Deferral to TASK-027 is reasonable and does not block TASK-025 acceptance.
    *Recommendation:* No action required in TASK-025. Confirm TASK-027 removes the shim path from the hot dispatch loop as planned.
    *Status:* Deferred — TASK-027 wired the v2 3-tier dispatch. The shim path still exists for v1 backward compatibility but the fast path now uses route_entry dispatch directly.

25. [ ] **performance-reviewer** | `src/httpserver/detail/lambda_resource.hpp:132` | memory-allocation
    std::array<lambda_handler, count_> slots_{} zero-initialises count_ std::function objects (one per HTTP method enumerator) at lambda_resource construction time. Each default-constructed std::function is empty and cheap, but on most implementations std::function is 32–40 bytes; the array is sized to the full method count even when only one or two methods are ever registered. For a server with many single-method lambda routes this wastes ~(count_ - 1) * sizeof(std::function) bytes per route entry.
    *Recommendation:* Consider storing only the registered slots using a small flat map or a std::optional<lambda_handler> per slot to avoid pre-allocating all method entries. Alternatively, document the current approach as an acceptable trade-off (O(1) slot access at the cost of fixed per-route overhead) since the array is stack-allocated within the shared object.
    *Status:* Deferred — the array is sized to `count_` (9 entries, ~9*32=288 bytes max per shim), a fixed and bounded overhead. O(1) slot access outweighs the modest memory cost for typical servers. TASK-036 may remove the shim entirely.

26. [x] **performance-reviewer** | `src/webserver.cpp:345` | missing-caching
    on_method_() constructs a detail::http_endpoint idx object (including a regex compile if the path is parameterised) before acquiring the mutex, then holds the lock while calling impl_->registered_resources.find(idx) and potentially impl_->invalidate_route_cache() after releasing it. The http_endpoint construction is outside the lock, which is correct, but the route-cache invalidation (impl_->invalidate_route_cache()) is called unconditionally on every on_* call regardless of whether fresh is true or whether the shim was merely updated. Invalidating the cache for every on_method_ call when only fresh routes actually change the routing outcome is a minor waste during startup-time registration bursts.
    *Recommendation:* Call impl_->invalidate_route_cache() only when fresh == true (new route inserted). Updating an existing shim's method slot does not change route topology — the key and shim pointer in all three maps are unchanged — so cache invalidation is unnecessary in the merge branch.
    *Status:* Deferred — in the current architecture `upsert_v2_table_entry` always modifies the methods bitmask in the v2 route_entry (even on a non-fresh call), so the v2 table IS changed by every `on_methods_` call. The cache must be invalidated on both fresh and non-fresh calls to maintain correctness with the v2 tier. The recommendation was based on the v1-only code shape.

27. [ ] **security-reviewer** | `src/httpserver/detail/lambda_resource.hpp:119` | error-handling
    invoke_() at line 129 calls `slot(r)` with no exception guard inside lambda_resource itself. Any exception thrown by a user-supplied lambda propagates out of render_* → finalize_answer. The broad catch(...) at webserver.cpp:1725 does catch it, so the server does not crash, but the recovery path calls internal_error_page — which logs nothing about the originating lambda path or method. An attacker who can reliably trigger a lambda exception therefore causes a silent 500 with no server-side attribution, which complicates incident response. CWE-390 (detection of error condition without action). The issue is minor because the exception does not escape the MHD trampoline; it only degrades observability.
    *Recommendation:* Wrap slot(r) in a try/catch inside invoke_() and either return a generic 500 response directly or rethrow after logging (if a logging hook is available). This constrains the exception boundary to the shim layer, ensures the 500 is attributed to the specific lambda, and avoids depending on the outer catch in finalize_answer as a safety net for user-land exceptions.
    *Status:* Deferred — a logging hook infrastructure is not yet available in the shim layer. Wrapping in try/catch without logging would merely duplicate the outer catch's behavior. Acceptable to address when a logging facility is available (possibly TASK-036 or a separate logging task).

28. [x] **security-reviewer** | `src/httpserver/detail/lambda_resource.hpp:131` | insecure-design
    assert(slot) is compiled away under NDEBUG (CWE-617). In release builds, if the invariant is ever broken — e.g. by a future refactor that calls set_allowing(method, true) without a paired set_slot, or that clears a slot after setting it — invoke_() proceeds to call an empty std::function, which throws std::bad_function_call. That exception propagates up through the MHD callback chain, which libmicrohttpd documents as undefined behavior (MHD expects callbacks never to throw). The net risk is a server crash / process abort, not memory corruption, but it is a silent regression from the previous defensive fallback that returned a safe empty_render response. The invariant is currently well-maintained: set_slot always writes before set_allowing, and no code path clears a slot. However, the comment on line 79 says 'Caller must have already verified' — that is only enforced by the throw in on_method_, which is bypassed if set_slot is ever called directly by future internal code.
    *Recommendation:* Either (a) restore the defensive fallback as the NDEBUG path and keep assert for debug-mode crash-loudly: `assert(slot); if (!slot) return detail::empty_render(r);` — the branch is predicted-not-taken and has negligible cost; or (b) replace assert with an explicit invariant check that throws a std::logic_error, which is recoverable and gives a meaningful error message in all build modes. Option (a) is preferred because it preserves the crash-loudly-in-debug / safe-in-release split that matches this codebase's existing error-handling style.
    *Status:* Fixed — `invoke_()` in `src/httpserver/detail/lambda_resource.hpp` now has `assert(slot)` followed by `if (!slot) return http_response::string(...).with_status(500)`. Debug crashes loudly; release returns a safe 500. Comment updated to explain the CWE-617 rationale.

29. [ ] **security-reviewer** | `src/httpserver/detail/lambda_resource.hpp:132` | insecure-design
    The slots_ array is sized by `http_method::count_` (value 9 per the enum), which includes http_method::connect (index 5) and http_method::trace (index 7). No render_connect or render_trace override is present in lambda_resource, so those slots are allocated but cannot be written to via on_* (no on_connect/on_trace entry points) and are never invoked. This is not currently exploitable, but if http_method gains a new enumerator that is inserted before count_ in the enum body, and lambda_resource is not updated to add the matching render_* override, a dispatch via the base-class default would silently bypass the slot mechanism. CWE-682 (incorrect calculation — incorrect array sizing assumption). Additionally, should someone add render_connect / render_trace overrides in a future commit without also adding the corresponding on_connect / on_trace entry points, the slots would remain zero-initialised (empty std::function), hitting the fallback empty_render path unexpectedly.
    *Recommendation:* Either (a) add a static_assert confirming that the set of render_* overrides in lambda_resource exactly covers all http_method enumerators below count_, or (b) add a note in the class comment explicitly listing the uncovered methods and why they are intentionally omitted. This makes the coverage gap explicit and ensures future enum additions are caught at compile time.
    *Status:* Deferred — the class comment updated in item 30 fix now explicitly documents that render_connect and render_trace are intentionally not overridden. A static_assert to count overrides is complex to express in C++ without reflection; the documentation approach is sufficient.

30. [x] **security-reviewer** | `src/httpserver/detail/lambda_resource.hpp:69` | insecure-design
    lambda_resource overrides only seven render_* methods (get, post, put, delete, patch, options, head). The http_resource base class also exposes render_connect and render_trace. The dispatch table in answer_to_connection (webserver.cpp line 1813-1818) maps CONNECT and TRACE to those two overrides. If a client sends CONNECT or TRACE to a lambda_resource route, the base-class render_connect / render_trace will fire (not invoke_), returning the base-class sentinel. That is not a security vulnerability because the is_allowed gate at line 1699 blocks those methods (disallow_all() in the constructor clears the whole mask, and no on_connect / on_trace API is offered), but the code's comment on line 91-93 says 'These seven overrides are required by the http_resource base-class interface' — that claim is inaccurate and could mislead a future author into thinking the base class only has seven overrides, increasing the risk of a gap when render_connect or render_trace is ever plumbed through.
    *Recommendation:* Update the comment at line 91 to say 'These seven overrides correspond to the seven on_* entry points exposed by webserver. render_connect and render_trace are intentionally not overridden; the is_allowed gate prevents dispatch reaching them on a lambda_resource shim.' This removes the misleading claim and documents the intentional gap.
    *Status:* Fixed — comment in `src/httpserver/detail/lambda_resource.hpp` at the render_* override block updated to accurately describe that the seven overrides correspond to the seven on_* entry points, and that render_connect/render_trace are intentionally not overridden (with explanation of the is_allowed gate).

31. [ ] **security-reviewer** | `src/webserver.cpp:1823` | insecure-design
    The existing comment at line 1823 acknowledges a pre-existing latent bug: for an unrecognised HTTP method verb, mr->callback is left un-initialised (null pointer-to-member). If is_allowed somehow returned true for http_method::count_ (which currently cannot happen because no resource sets that bit), the null callback dereference would be UB. This is a pre-existing issue rather than a regression introduced by TASK-025, but the TASK-025 block comment on line 324-328 restates the same invariant without noting this edge, creating a gap in the documented invariant.
    *Recommendation:* No code change needed for this task, but add a note to the TASK-025 block comment that the invariant also depends on mr->callback being non-null, which is only guaranteed for the nine recognised method strings — the unrecognised-method path relies solely on is_allowed(count_) returning false.
    *Status:* Deferred — the TASK-025 block comment is in `src/detail/webserver_routes.cpp` (refactored from the original location). The latent bug is a pre-existing issue in `src/webserver.cpp:1823` (answer_to_connection dispatch table). Tracking separately.

32. [ ] **security-reviewer** | `src/webserver.cpp:329` | input-validation
    on_method_() validates the single_resource constraint but does not reject a null/empty path argument when single_resource is false. An empty string passed as `path` does not hit the single_resource guard (because empty == '' passes), and is forwarded directly to http_endpoint construction and map insertion. The register_impl_ sibling has the same gap, so the behaviour is consistent, but an empty-path registration from a public on_* call is a silent no-op or an unintended catch-all depending on how http_endpoint normalises the key. An explicit `if (path.empty() && !single_resource) throw` guard (or a shared path-normalisation helper) would make the contract explicit.
    *Recommendation:* Add a path validation step in on_method_() (and in register_impl_() for parity): if (!single_resource && path.empty()) throw std::invalid_argument("path must not be empty"); This mirrors the guard already present in the single_resource branch and prevents silent misregistrations.
    *Status:* Deferred — `register_impl_` and `on_method_` both treat empty path as a valid alias for root "/" per existing behavior. An empty-path registration is intentionally equivalent to a root registration in single_resource mode. Non-single_resource empty-path registration is an existing edge case in v1; changing the behavior is a breaking API change requiring a separate task.

33. [x] **spec-alignment-checker** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-025/src/httpserver/detail/route_entry.hpp:null` | specification-gap
    The `route_entry` struct is defined and pinned via static_assert in the test, but it is not yet plumbed into the actual dispatch path — the on_* plumbing uses the existing v1-shaped three-map storage via a `lambda_resource` shim rather than the route_entry table. The task definition acknowledges this explicitly: 'TASK-027 will plumb route_entry into the real 3-tier table.' This is a known design limitation, not a spec violation.
    *Recommendation:* Confirmed as intentional staging; TASK-027 will complete the integration.
    *Status:* Addressed — TASK-027 is complete and wired route_entry into the full 3-tier dispatch table.

34. [ ] **spec-alignment-checker** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-025/src/httpserver/webserver.hpp:272` | specification-gap
    PRD-NAM-REQ-002 and PRD-NAM-REQ-005 require `sweet_kill` be renamed to `stop_and_wait` and the ip-access verbs collapsed to `block_ip`/`unblock_ip`. These pre-existing v1 names (`sweet_kill`, `ban_ip`, `allow_ip`, `unban_ip`, `disallow_ip`) are still present in the public header. They are not in TASK-025 scope, but they remain unaddressed across the active worktree and will need to be cleaned up before v2.0 ships.
    *Recommendation:* Track as a separate task (API-NAM milestone); no action needed in this PR.
    *Status:* Deferred — out of TASK-025 scope; tracked as separate API-NAM milestone task.

35. [x] **spec-alignment-checker** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-025/src/httpserver/webserver.hpp:null` | specification-gap
    PRD-HDL-REQ-006 requires a generic `webserver::route(http_method, const std::string& path, handler)` entry point. The task definition explicitly defers this to TASK-026 per plan §4.3. The method is not present in this PR, which aligns with the stated deferral. No violation within TASK-025 scope, but confirming the gap is tracked.
    *Recommendation:* Confirmed as deferred to TASK-026; no action needed in this PR.
    *Status:* Addressed — TASK-026 is complete and `webserver::route()` overloads are now present in `src/detail/webserver_routes.cpp`.

36. [ ] **test-quality-reviewer** | `test/unit/webserver_on_methods_test.cpp:216` | naming-convention
    lambda_route_dispatches_only_for_registered_method tests two behaviors: (a) the registered method returns 200 and (b) an unregistered method returns 405 with a correct Allow header. The name only conveys dispatch selectivity, not the 405/Allow-header assertion. The 'and' pattern is a sign the test covers more than one concern.
    *Recommendation:* Consider splitting into lambda_route_returns_200_for_registered_method and lambda_route_returns_405_with_allow_header_for_unregistered_method.
    *Status:* Deferred — splitting the test would require a new port and additional test scaffolding. The test name is descriptive enough and splitting is low-priority polish.

37. [ ] **test-quality-reviewer** | `test/unit/webserver_on_methods_test.cpp:216` | multiple-concerns
    lambda_route_dispatches_only_for_registered_method tests two behaviors in one test body: (1) that a registered GET returns 200, and (2) that an unregistered POST returns 405 with an Allow header. The name implies only the second concern. A failing assertion on line 225 (GET check) would be misleading given the name.
    *Recommendation:* Split into two tests: on_get_registered_path_returns_200 and on_get_registered_path_rejects_post_with_405_and_allow_header. The GET-returns-200 case is already covered by on_get_hello_world and on_get_dispatches_get, so it could simply be removed from this test, leaving it focused solely on the 405/Allow behavior.
    *Status:* Deferred — same as item 36; splitting is low-priority polish. The test is functionally correct and the failure message from LT_CHECK_EQ is clear.

38. [ ] **test-quality-reviewer** | `test/unit/webserver_on_methods_test.cpp:259` | redundant-test
    all_seven_on_methods_serve_their_method overlaps substantially with the seven individual on_*_dispatches_* tests added in iter1. GET, POST, PUT, DELETE, PATCH, OPTIONS are each exercised one-to-one by their own test; the omnibus test adds no additional dispatch path that isn't already covered, except for asserting composition (seven methods registered on the same path at once). If the intent is to test composition, the test name should reflect that; if the intent is individual dispatch verification, the test is redundant.
    *Recommendation:* Either rename the test to reflect its distinctive value (e.g., all_seven_on_methods_compose_on_same_path) and strip the per-method response-body assertions down to just the status-code check for each (to keep it focused on composition, not re-verifying dispatch), or remove it and let the seven individual tests own dispatch verification. The composition angle — that seven concurrent registrations on the same endpoint all work together — is genuinely distinct and worth a single test.
    *Status:* Deferred — test refactoring is low-priority polish. The composition aspect is genuinely distinct; renaming could be done in a follow-up.

39. [x] **test-quality-reviewer** | `test/unit/webserver_on_methods_test.cpp:384` | missing-test
    The reverse of the lambda-vs-class conflict test (on_get_then_register_path_with_class_throws, line 366) is not tested: registering a class-based resource first and then calling on_get on the same path should also throw. Only one direction of the mutual-exclusion invariant is exercised.
    *Recommendation:* Add a test that calls register_path first, then on_get on the same path, and asserts std::invalid_argument.
    *Status:* Already addressed — `register_path_then_on_get_same_path_throws` test exists at line 418 of the test file.

40. [ ] **test-quality-reviewer** | `test/unit/webserver_on_methods_test.cpp:57` | test-isolation
    Tests use hardcoded port constants (PORT=8190 through PORT+7). If any test crashes without reaching ws.stop() the port is not released, and the leaked server can cause the next test run on the same host to fail with EADDRINUSE. This also means tests cannot run in parallel.
    *Recommendation:* Use port 0 and call ws.get_bound_port() to get the OS-assigned port, or register a RAII guard that stops the server. At minimum, document the port-isolation assumption.
    *Status:* Deferred — changing to port 0 requires a `get_bound_port()` API that may not exist. Port-RAII guard would be a significant test infrastructure change. Pre-existing pattern across all unit tests in this codebase. Track as a test infrastructure improvement.
