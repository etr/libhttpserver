# Unworked Review Issues

**Run:** 2026-07-17 21:49:22
**Task:** manual-validation
**Total:** 17 (0 critical, 3 major, 14 minor)

## Major

1. [x] **code-simplifier** | `src/detail/webserver_lifecycle.cpp:195` | patterns
   The pattern `struct MHD_Daemon* d = impl_->daemon_.daemon.load(std::memory_order_acquire); if (d == nullptr) return <sentinel>;` is repeated verbatim across nine consecutive methods (quiesce, get_listen_fd, get_active_connections, get_bound_port, run, run_wait, get_fdset, get_timeout, add_connection, lines 195-279). Before the daemon_lifecycle extraction this may have been acceptable one-off field access; now that the handle lives behind a dedicated collaborator, the repeated acquire-load + null-check is a leftover duplication seam that also scatters the memory-order contract (documented once in daemon_lifecycle.hpp's `daemon` comment) across nine call sites.
   *Recommendation:* Add a small accessor on daemon_lifecycle, e.g. `MHD_Daemon* daemon_lifecycle::get() const noexcept { return daemon.load(std::memory_order_acquire); }`, and have the nine webserver:: methods call `impl_->daemon_.get()` instead of repeating the load + memory-order argument. Behavior-preserving; centralizes the acquire-semantics comment in one place.

2. [x] **code-simplifier** | `src/detail/webserver_register.cpp:178` | code-structure
   The regex-tier sweep-by-key logic (`std::remove_if` over `impl_->routes_.regex_routes_` matching `rr.url_complete == key`, paired with an `exact_routes_.erase(key)`) is duplicated verbatim between `unregister_impl_` (lines 177-186) and `unregister_resource` (lines 212-222). Both functions also reach directly into route_table's three raw containers (`exact_routes_`, `param_and_prefix_routes_`, `regex_routes_`) from outside the collaborator, rather than through a route_table method -- an awkward seam now that route_table already owns encapsulated primitives like `register_v2_route` and `lookup_v2` for the symmetric insert path.
   *Recommendation:* Add a route_table method (e.g. `erase_by_key(const std::string& key, bool erase_prefix_too)`) that performs the exact + regex + (optionally) radix-prefix sweep under its own lock_for_write(), and have both unregister_impl_ and unregister_resource call it. This removes the duplicated std::remove_if lambda and stops webserver_register.cpp from poking route_table's raw members directly, matching the encapsulation level already used for insertion.

3. [x] **housekeeper** | `specs/architecture/04-components/hooks.md:14` | architecture-not-updated
   The hooks component doc's phase-dispatch table cites `webserver_finalize.cpp:fire_before_handler_gated` / `fire_after_handler_gated` (line 16) / `fire_response_sent_gated` (line 17) / `fire_request_completed_gated` (line 18) as the source locations for these hook-firing helpers. Commit 2e73dcb in this range ('refactor: rename three mis-scoped translation units') renamed `src/detail/webserver_finalize.cpp` to `src/detail/webserver_hook_firing.cpp` (confirmed: the four `fire_*_gated` functions now live in webserver_hook_firing.cpp, per `grep -n 'fire_before_handler_gated' src/detail/webserver_hook_firing.cpp:79`). specs/architecture/ was not touched anywhere in this commit range, so the doc still points readers at a file that no longer exists.
   *Recommendation:* Update the four `webserver_finalize.cpp` references in specs/architecture/04-components/hooks.md to `webserver_hook_firing.cpp`. Run /groundwork:source-architecture-from-code to resync the architecture doc's component/file mapping with the current collaborator layout (webserver_hook_firing.cpp, plus the newly extracted hook_bus.cpp/route_table.cpp/daemon_lifecycle.cpp/ip_access_control.cpp/ws_registry.cpp collaborators from this same series, in case other component pages reference the pre-decomposition file layout).

## Minor

4. [ ] **code-quality-reviewer** | `src/detail/daemon_lifecycle.cpp:74` | code-elegance
   The line `webserver* const parent = owner_->parent;` is repeated verbatim at the top of seven methods (add_base_mhd_options, add_tls_mhd_options, add_gnutls_mhd_options, add_extended_mhd_options, add_https_extra_options, compose_transport_flags, compose_runtime_flags, compose_start_flags). Low-impact repetition given the intentional back-pointer design documented in daemon_lifecycle.hpp, but it's the same pattern six-plus times over one file.
   *Recommendation:* Optional: could pass `webserver* parent` as a parameter from build_mhd_option_array/compose_start_flags to the sub-builders instead of each re-deriving it from owner_, trimming one line per method.

5. [ ] **code-quality-reviewer** | `src/detail/hook_bus.cpp:479` | code-elegance
   fire_handler_exception (lines 479-525) re-implements the snapshot/try-catch/log loop that fire_short_circuit_hooks_for_phase (lines 379-415) already provides generically, plus a small alias tail. The duplication is explained in a comment as necessary because the ctx is `const&` here vs `&` in the template and because of the extra alias-invocation tail, but the core per-entry try/catch/log block (lines 490-501) is a near-verbatim copy of lines 398-409.
   *Recommendation:* Consider factoring the inner 'invoke one entry, catch std::exception/... , route through log_hook_threw[_unknown]' step into a small shared helper parameterized on the entry-invoke expression, reused by both the generic templates and this bespoke function, to remove the duplicated catch-block bodies.

6. [ ] **code-quality-reviewer** | `src/detail/route_table.cpp:365` | code-elegance
   insert_fresh_v2_entry uses `assert(!"unreachable..."); __builtin_unreachable();` to mark the radix case unreachable. __builtin_unreachable() is a GCC/Clang-only intrinsic newly introduced by this refactor (grep shows it is the only use in src/); in a release (NDEBUG) build the assert compiles away and only the UB-hint intrinsic remains, with no portable fallback (e.g. MSVC's __assume(false)). Elsewhere in this same file the project shows Windows-awareness (daemon_lifecycle.hpp's MHD_socket comment: 'int on POSIX, SOCKET on Windows'), so this is worth a quick portability check if MSVC/MinGW is a supported toolchain.
   *Recommendation:* If MSVC is a supported target, wrap the unreachable-hint in a small cross-platform macro (or drop it and rely on the assert + a return/throw) instead of the bare Clang/GCC builtin.

7. [ ] **code-quality-reviewer** | `src/httpserver/detail/hook_bus.hpp:228` | code-readability
   Encapsulation posture is inconsistent across the five sibling collaborators extracted in this series. ip_access_control and ws_registry keep all state private behind a small mutating/query API (deny/allow/classify; try_register/unregister/find/empty). hook_bus (state block at lines 228-278), route_table (state block at lines 184-193), and daemon_lifecycle (state block at lines 97-117) instead expose all state as public members, justified per-class as needed for white-box tests and the coordinator/dispatch trampolines. Each choice is individually documented and defensible, but the inconsistency means two of five new collaborators encode a real invariant boundary in the type system while three rely entirely on comment-documented caller discipline (e.g. 'Caller must hold route_table_mutex_' appears on multiple route_table methods with no compile-time enforcement).
   *Recommendation:* No change required given the documented test-access constraints, but consider whether a narrower friend-based or accessor-based mechanism could let hook_bus/route_table/daemon_lifecycle keep private state while still granting the specific white-box tests access, closing the gap with ip_access_control/ws_registry.

8. [ ] **code-quality-reviewer** | `src/httpserver/detail/ip_access_control.hpp:47` | test-coverage
   None of the five newly-extracted collaborators (ip_access_control, ws_registry, hook_bus, route_table, daemon_lifecycle) has a dedicated unit test that instantiates the class directly and exercises its public API/invariants in isolation. Coverage today is indirect, via the pre-existing webserver-level integ/unit tests (test/integ/ip_access_control.cpp, test/unit/route_table_test.cpp exercises segment_trie/route_cache but not route_table itself, hooks_*.cpp integ tests, ws_start_stop.cpp) that were mechanically updated to keep passing through the refactor. That's adequate to prove behavior preservation, but it means invariants documented only in comments -- e.g. insert_wildcard_aware's erase-then-insert precedence rule (ip_access_control.cpp:36-43), ws_registry::try_register's collision-return contract, hook_bus's any_hooks_ gate staying correct across add/remove/alias-wire/alias-unwire sequences -- have no fast, isolated regression test pinning them now that the class is decoupled from webserver_impl.
   *Recommendation:* Add small standalone unit test files (e.g. test/unit/ip_access_control_test.cpp, test/unit/ws_registry_test.cpp, test/unit/hook_bus_test.cpp) that construct the collaborator directly and assert its documented invariants, now that doing so no longer requires spinning up a full webserver.

9. [ ] **code-quality-reviewer** | `test/integ/test_utils.hpp:36` | readability
   The under_valgrind() doc comment is spliced into the middle of what was previously one continuous file-level comment block ("Shared helpers..." -> "Historical note..."). The lone `//` on line 36 is an orphaned separator between the function's doc comment and the unrelated Historical note paragraph about as_shared removal, which makes the comment structure slightly harder to scan.
   *Recommendation:* Use a blank line (not a bare `//`) to separate the under_valgrind() doc comment from the unrelated Historical note section, or move the Historical note above the function so the two topics don't interleave inside one comment run.

10. [ ] **code-quality-reviewer** | `test/unit/ws_registry_test.cpp:122` | readability
   Test name `find_result_survives_concurrent_unregister` implies multi-threaded execution, but the test is entirely sequential (single thread, deterministic ordering) -- it simulates the race outcome by calling find() then unregister() in program order, not by actually racing two threads. Real concurrent coverage for ws_registry lives separately in test/integ/ws_registry_concurrency.cpp. A reader skimming test names could mistake this for a thread-safety test, when it is actually pinning the 'find() returns an owning copy' lifetime contract.
   *Recommendation:* Rename to something like `find_result_outlives_registry_unregister` (or `find_returns_owning_copy_independent_of_registry_slot`) to remove the 'concurrent' implication, since the accompanying comment already explains the scenario being defended against without needing it in the test name.

11. [ ] **code-simplifier** | `src/httpserver/detail/route_table.hpp:102` | naming
   `tier_hit::radix` and `route_tier_kind::radix` retain the word "radix" even though the underlying data structure was renamed from radix_tree to segment_trie as part of this same change. This is likely intentional (the identifiers name a routing-tier concept, not the container type, and renaming would ripple through ~10 test call sites doing mechanical churn out of scope here), but it is a small naming inconsistency worth a deliberate note if the rename was meant to fully propagate.
   *Recommendation:* No action needed unless the segment_trie rename is intended to fully replace "radix" terminology project-wide; if so, a follow-up rename of tier_hit::radix / route_tier_kind::radix (plus the ~10 test call sites) would finish the job. Leaving as-is is a reasonable Chesterton's-Fence call for this PR.

12. [ ] **housekeeper** | `test/REGRESSION.md:null` | documentation-stale
   Commit de0c334's readability sweep removed dangling `specs/tasks/M5-routing-lifecycle/TASK-0NN.md` links from the 'References' section of test/REGRESSION.md, which is good, but the same commit also stripped the task-ID provenance from most prose in this file down to generic phrasing ('the dispatch cutover', 'later deleted') without adding replacement dates/commit hashes, so a reader can no longer tell which commit performed the v1-map removal it refers to. This is a readability trade-off the commit message explicitly signs up for, not a correctness gap, but it's worth a spot-check that no reader-relevant provenance was lost.
   *Recommendation:* No action required unless future maintainers report the doc is too vague; the sweep's intent (stripping churny TASK-/DR- breadcrumbs while keeping behavioral rationale) was applied consistently.

13. [ ] **security-reviewer** | `src/httpserver/detail/modded_request.hpp:116` | logging-observability
   matched_path_template is documented (new comment) to actually hold the canonicalized request URL rather than the registered route pattern, so route_resolved / before_handler hook consumers reading route_descriptor::path_template observe the concrete URL, not the pattern. This is a correctness/observability gap (any hook-based access-control or audit logic keyed on the route pattern rather than the literal URL would silently see the wrong value) rather than a newly introduced vulnerability -- the comment indicates it predates this refactor and was merely documented more precisely here.
   *Recommendation:* If any hook consumer (in this codebase or downstream) uses route_descriptor::path_template for authorization decisions or audit-log grouping, verify it tolerates receiving the concrete request URL instead of the pattern; otherwise track as a follow-up bug independent of this refactor.

14. [ ] **security-reviewer** | `src/httpserver/detail/webserver_impl_dispatch.hpp:236` | insecure-design
   The 9af15a1 perf change threads a raw `http_resource*` (borrowed from finalize_answer's local `hrm` shared_ptr) into fire_after_handler_gated / fire_response_sent_gated / materialize_and_queue_response instead of re-locking mr->resource_weak_. This is currently safe because all three calls happen synchronously within finalize_answer's lexical scope while `hrm` is alive (verified in src/detail/webserver_request.cpp and webserver_response_queue.cpp), but the safety invariant is enforced only by comments, not by the type system -- a future change that defers or asynchronously re-enters materialize_and_queue_response (e.g. for a streaming/suspend-resume path) could pass a dangling pointer without the compiler catching it.
   *Recommendation:* Consider a lightweight non-owning wrapper (or a debug-only assert that the borrowed pointer's control block use_count matches expectations) at the fire_after_handler_gated / fire_response_sent_gated entry points, or at minimum a static_assert-style comment block co-located with materialize_and_queue_response's signature warning against introducing an async boundary between hrm's construction and these calls without re-adding the weak_ptr lock().

15. [ ] **test-quality-reviewer** | `src/httpserver/detail/ip_access_control.hpp:47` | missing-test
   ip_access_control contains real domain logic (insert_wildcard_aware's weight-based precedence comparator, ip_access_control.hpp lines 71-87) but is tested only through test/integ/ip_access_control.cpp, which drives it end-to-end via a running webserver + libcurl round-trips. This is slow, indirect integration coverage for what the checklist's code taxonomy would classify as domain-model logic (high complexity, few collaborators) best served by direct, fast unit tests. This gap predates the current diff (the integ file's changes in this range are two mechanical comment-only lines) but the class extraction was an opportunity to add a fast unit-level companion that was not taken.
   *Recommendation:* Add a unit test that constructs httpserver::detail::ip_access_control directly and exercises deny/allow/remove/classify and the wildcard-precedence erase-then-insert branches without needing a running daemon or curl.

16. [ ] **test-quality-reviewer** | `test/unit/daemon_lifecycle_test.cpp:1` | missing-test
   build_mhd_option_array's TLS/GnuTLS/HTTPS-extra option branches (add_tls_mhd_options, add_gnutls_mhd_options, add_https_extra_options) are not exercised directly at the daemon_lifecycle class boundary -- only add_base_mhd_options (connection_timeout, max_threads, zero-valued omission) and the flag composers are covered. This is likely acceptable since TLS/HTTPS paths are presumably still covered indirectly by other integ-level TLS tests (webserver_dauth_available/unavailable etc.), but it means the newly-extracted collaborator's TLS option composition still has no dedicated unit-level pin.
   *Recommendation:* If TLS config is easy to set on the create_webserver() builder (use_ssl/https_mem_cert etc.) without starting a real daemon, add one or two build_mhd_option_array tests that assert MHD_OPTION_HTTPS_MEM_CERT/MHD_OPTION_HTTPS_MEM_KEY (or the GnuTLS-priority option) appear when TLS is configured -- otherwise leave as-is and note the indirect coverage in the file's header comment for future readers.

17. [ ] **test-quality-reviewer** | `test/unit/hook_bus_test.cpp:143` | missing-test
   The throw-containment/on_error-routing behavior of fire_* is only pinned for one phase (fire_connection_opened, an observation-only void-returning phase). The short-circuit-capable phases (fire_before_handler, fire_handler_exception, fire_after_handler, fire_request_received, fire_body_chunk) share the same try/catch pattern per the class header comment, but a hook throwing inside one of those optional<http_response>-returning firing paths is never directly asserted to be contained and routed to on_error rather than propagating or corrupting the returned optional.
   *Recommendation:* Optional: add one exception-containment test on a short-circuit-capable phase (e.g. before_handler hook throws) asserting fire_before_handler returns nullopt (or a safe fallback) and on_error still receives the message, since that firing shape differs enough from the void-returning connection_opened case to be worth pinning independently.
