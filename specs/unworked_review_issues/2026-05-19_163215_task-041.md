# Unworked Review Issues

**Run:** 2026-05-19 16:32:15
**Task:** TASK-041
**Total:** 35 (0 critical, 4 major, 31 minor)

## Major

1. [x] **code-quality-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-041/Makefile.am:285` | test-coverage
   check-readme.sh is added to EXTRA_DIST (line 42) and runs correctly when invoked directly, but there is no `check-readme` Make target and no invocation in the `check-local` target. By contrast, check-examples.sh (the analog from TASK-040) has a dedicated `check-examples:` target that is listed in `check-local: check-headers check-examples`. The CI gate mentioned in README.md (`scripts/check-readme.sh enforces the byte-for-byte equality`) is therefore never exercised by `make check`.
   *Recommendation:* Add a `check-readme` target that invokes `$(top_srcdir)/scripts/check-readme.sh`, append it to the `check-local` prerequisite list alongside `check-headers check-examples`, and add `check-readme` to the `.PHONY` declaration. This mirrors the exact pattern used for check-examples in TASK-040.
   *Status (fix/task-041-review-cleanup):* Already fixed in feature/v2.0 merge — Makefile.am line 330 has `check-local: check-headers check-examples check-readme ...`, line 355 has the `check-readme:` target, and line 426 includes `check-readme` in `.PHONY`. No further action needed.

2. [x] **security-reviewer** | `scripts/check-readme.sh:26` | insecure-design
   The script uses `set -u` but omits `set -e` and `set -o pipefail`. Without `set -e`, a failed command in a pipeline or subshell (e.g., `grep`, `awk`, `diff`) silently succeeds, allowing the gate to pass when it should fail. Without `set -o pipefail`, the exit status of a pipeline reflects only the last command, so an error in `extract_first_cpp_block "$README" > "$tmp_block"` or in the A3/A4 grep loops could be swallowed.
   *Recommendation:* Change line 26 to `set -euo pipefail`. Every command sequence in the script already uses explicit `fail()` calls, so adding `-e` will not change intentional error paths but will catch unexpected failures.
   *Status (fix/task-041-review-cleanup):* Already fixed — `set -euo pipefail` is at line 26 of check-readme.sh. No further action needed.

3. [x] **spec-alignment-checker** | `Makefile.am:285` | acceptance-criteria
   check-readme.sh is added to EXTRA_DIST but is NOT wired into any make target. The `check-local` target (line 285) only calls `check-headers` and `check-examples`. The README itself states 'a CI gate (scripts/check-readme.sh) enforces the byte-for-byte equality', but `make check` will never invoke it. By contrast, check-examples.sh has a dedicated `check-examples:` target driven from `check-local`. The task's acceptance criterion 'Typecheck passes' is unenforceable by the build system as delivered.
   *Recommendation:* Add a `check-readme:` target that invokes `$(top_srcdir)/scripts/check-readme.sh` and add it as a dependency in `check-local`, mirroring the pattern used for `check-examples`.
   *Status (fix/task-041-review-cleanup):* Same as finding 1 — already fixed in the merged commit. No further action needed.

4. [x] **test-quality-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-041/scripts/check-readme.sh:43` | missing-test
   The A1 byte-for-byte comparison uses `awk … print` which appends a newline after every line it prints (POSIX awk behaviour). If README.md is ever saved with CRLF line endings (e.g., edited on Windows and committed without git's CRLF-to-LF normalisation), the extracted block contains bare `\r` bytes that the real `examples/hello_world.cpp` does not, causing a spurious diff failure. Conversely, if `hello_world.cpp` picks up a trailing CRLF the comparison is also broken. The script does not normalise or document this assumption.
   *Recommendation:* Add a brief prose comment documenting that both files must have LF-only line endings (which the repo's `.gitattributes` or `.editorconfig` should enforce). Optionally add a pre-check: `file $README | grep -q CRLF && fail 'A1: README.md has CRLF line endings'`. This makes the assumption explicit and catchable rather than silent.
   *Status (fix/task-041-review-cleanup):* Fixed — added `gsub(/\r/, "")` to the `extract_first_cpp_block` awk function and added an S4 CRLF check (`grep -qP '\r$'`) that fails loudly if README.md has CRLF line endings.

## Minor

5. [x] **architecture-alignment-checker** | `README.md:274` | adr-violation
   Threading contract bullet 1 describes stop_and_wait as '(the v2 spelling for the v1 kill routine)'. However, the architecture and the actual webserver.hpp define both stop() and stop_and_wait() as distinct v2 methods. stop() returns bool and signals a non-blocking stop; stop_and_wait() additionally waits for in-flight handlers (per the header comment). Describing stop_and_wait as merely the v2 rename of v1's kill routine elides the existence of stop() entirely and misrepresents the API surface.
   *Recommendation:* Clarify that v2 provides both stop() (signal-and-return) and stop_and_wait() (signal-and-drain), and that the deadlock contract applies to stop() (and by extension stop_and_wait() since it delegates to stop()). The parenthetical about v1 'kill' can remain but should not imply stop_and_wait is the only stop method.
   *Status (fix/task-041-review-cleanup):* Already fixed in merged README.md — Threading contract (line 1619-1626) correctly says "v2 provides both `stop()` (signal and return) and `stop_and_wait()` (signal and drain until all in-flight handlers finish)". No further action needed.

6. [x] **architecture-alignment-checker** | `README.md:293` | interface-contract
   The error propagation section (line 293) refers to 'the configured error_logger' when describing the logging step for a caught exception. The actual public API method on create_webserver is log_error() (create_webserver.hpp line 96), not error_logger(). The architecture §5.2 uses 'error_logger' as an informal name, but a user reading the README and looking for the corresponding builder setter will not find 'error_logger' — they will find 'log_error'. This may cause confusion when configuring custom error logging.
   *Recommendation:* Replace 'the configured error_logger' with 'the configured log_error callback' (or similar) to match the actual create_webserver builder method name visible in the public API.
   *Status (fix/task-041-review-cleanup):* Already fixed in merged README.md — Error propagation section (line 1654) uses "configured `log_error` callback". No further action needed.

7. [x] **architecture-alignment-checker** | `specs/architecture/05-cross-cutting.md:20` | interface-contract
   §5.2 of the architecture doc still uses error_logger while the actual public API (create_webserver.hpp log_error(), webserver.hpp log_error field) uses log_error. The README correctly uses log_error now, so the arch doc contains a stale term. This inconsistency is in a file not changed by this task but is worth noting as technical debt.
   *Recommendation:* Update 05-cross-cutting.md §5.2 to replace error_logger with log_error to match the public API; this is a documentation-only fix in the architecture spec.
   *Status (fix/task-041-review-cleanup):* Fixed — updated `specs/architecture/05-cross-cutting.md` §5.2 to replace `error_logger` with `log_error` callback in all three occurrences.

8. [x] **code-quality-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-041/README.md:100` | code-elegance
   The Hello-world section walk-through (lines 100-117) re-explains the snippet that appears verbatim at the top of the README. The prose is clear and useful, but the section header is '## Hello, world — lambda form' while the earlier block already has the comment `// libhttpserver hello-world example`. The duplication is acceptable for a README, but the section could open by noting 'the snippet above is reproduced here for reference' rather than the current framing, which implies readers jump straight to this section.
   *Recommendation:* Minor phrasing: the walk-through section already cross-references the snippet at the top ('The snippet at the top of this README is the entire program.') which is good. No change required; flagged for awareness only.
   *Status (fix/task-041-review-cleanup):* Deferred — finding itself says "No change required; flagged for awareness only". No action taken.

9. [x] **code-quality-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-041/README.md:100` | code-readability
   The Hello-world walk-through section opens 'The snippet at the top of this README is the entire program.' which is clear, but readers jumping to this section via the table-of-contents anchor do not see the snippet inline. This is a documentation UX nit, not a correctness concern.
   *Recommendation:* Optional: repeat the snippet (or a short excerpt) at the top of the walk-through section so the section is self-contained. No change required.
   *Status (fix/task-041-review-cleanup):* Deferred — optional per recommendation; no change required.

10. [x] **code-quality-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-041/README.md:100` | code-readability
   The Hello-world walk-through section opens 'The snippet at the top of this README is the entire program.' — readers jumping to the section via the table-of-contents anchor do not see the snippet inline. This is a documentation UX nit, not a correctness concern. Carried from iter 2.
   *Recommendation:* Optional: repeat the snippet (or a short excerpt) at the top of the walk-through section so the section is self-contained. No change required.
   *Status (fix/task-041-review-cleanup):* Deferred — optional per recommendation; no change required.

11. [x] **code-quality-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-041/scripts/check-readme.sh:135` | code-readability
   `extract_section_body` is an inline awk program defined inside a shell function. It has no corresponding test that exercises it in isolation; if the awk `tolower` comparison drifts from the H2 heading text in README, the section-body checks silently produce an empty string instead of failing loudly. The script currently checks `[ -z "$thread_body" ]` but not `[ -z "$err_body" ]` before testing tokens inside it — `err_body` only has the arch-citation check, not the zero-length guard on its own line (line 163 guards for empty correctly, but only after the DR-009 check would silently pass on empty string for grep -qE).
   *Recommendation:* Add an explicit `[ -z "$err_body" ]` guard with a clear fail message immediately after `err_body=...` (mirroring the `thread_body` guard at line 150), before the grep tests, to ensure a missing Error-propagation section fails loudly rather than the grep calls happening to match vacuously.
   *Status (fix/task-041-review-cleanup):* Already fixed in merged check-readme.sh — `if [ -z "$err_body" ]; then fail "A4: README.md is missing a '## Error propagation' section"; fi` appears at line 172, before the grep tests. No further action needed.

12. [x] **code-quality-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-041/scripts/check-readme.sh:72` | code-readability
   The V1_TOKENS variable is assembled across six concatenated assignments (`V1_TOKENS="$V1_TOKENS"'|...'`). While functional, the pattern makes it hard to see the full token set at a glance. A heredoc fed to `paste` or a single multi-line variable using POSIX line continuation would be cleaner.
   *Recommendation:* Consider using a `REQUIRED_V2_TOKENS`-style array (as used from line 91 onward) for V1_TOKENS, iterating with a loop. This would match the already-clean style used for the positive-token checks and make the list easier to audit.
   *Status (fix/task-041-review-cleanup):* Fixed — V1_TOKENS refactored to a `V1_TOKENS_ARR` bash array (one entry per line) joined with `|` in a loop, matching the REQUIRED_V2_TOKENS style.

13. [x] **code-quality-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-041/scripts/check-readme.sh:72` | code-readability
   V1_TOKENS is assembled via six concatenated string-append assignments, making it harder to audit the full list at a glance. The positive-token equivalent (REQUIRED_V2_TOKENS at line 91) uses a cleaner array form.
   *Recommendation:* Convert V1_TOKENS to a bash array and join with '|' in a loop, mirroring the REQUIRED_V2_TOKENS style. No blocking concern — the current approach is correct and functional.
   *Status (fix/task-041-review-cleanup):* Fixed alongside finding 12 — same refactor.

14. [x] **code-quality-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-041/scripts/check-readme.sh:72` | code-readability
   V1_TOKENS is assembled via six concatenated string-append assignments, making it harder to audit the full list at a glance. The positive-token equivalent (REQUIRED_V2_TOKENS at line 91) uses a cleaner array form. Carried from iter 2.
   *Recommendation:* Convert V1_TOKENS to a bash array and join with '|' in a loop, mirroring the REQUIRED_V2_TOKENS style. No blocking concern — the current approach is correct and functional.
   *Status (fix/task-041-review-cleanup):* Fixed alongside findings 12 and 13.

15. [x] **code-simplifier** | `README.md:90` | code-structure
   The ABI/packaging note appears twice. Lines 90-93 (Build / install section) state: 'SOVERSION bumps from 1 to 2 … Distributions package `libhttpserver2` parallel-installable with `libhttpserver1`. There is no inline namespace … v1.x is end-of-life on the day v2.0 ships'. Lines 389-390 (Migrating from v1 section) repeat the same facts: 'v1.x is end-of-life on the day v2.0 ships, and v2 is packaged as `libhttpserver2` — parallel-installable with `libhttpserver1`'.
   *Recommendation:* Remove the packaging sentence from one location. The Build section is the natural home for SOVERSION/packaging detail; the Migration section should simply reference it: 'v1.x is end-of-life on the day v2.0 ships. See [Build / install](#build--install) for the packaging and SOVERSION details.' Alternatively, keep the Migration note and remove the ABI paragraph from Build, since it reads as migration context more than installation context.
   *Status (fix/task-041-review-cleanup):* Deferred — the two sections (Build/install §ABI/packaging and Migrating from v1) are phrased differently and serve different audiences. The duplication is acceptable; removing either would lose context for one audience. No change made.

16. [x] **code-simplifier** | `scripts/check-readme.sh:193` | code-structure
   The A5 section loop uses two different grep branches controlled by a `^`-prefix sentinel on the pattern string (lines 193-205). Three patterns (`'^##[ \t]+request'`, `'^##[ \t]+response'`, `'^##[ \t]+routing'`) bypass the wrapping logic and are passed verbatim to grep, while the other eight patterns are wrapped as `"^##[ \t]+.*${sec}"`. The comment `# Patterns starting with ^ are absolute` explains it, but the dual-branch logic adds invisible complexity: a maintainer adding a new section must know the convention. The three absolute patterns could simply be written without the `^##[ \t]+` prefix (just `'request'`, `'response'`, `'routing'`) and routed through the else branch, which already anchors them to H2 lines.
   *Recommendation:* Replace the three absolute patterns with bare keywords and remove the if/else branch entirely:

REQUIRED_SECTIONS=(
    'build.*install'
    'hello.*world'
    'class-form'
    'request'
    'response'
    'routing'
    'threading'
    'error[ \t-]+propag'
    'feature[ \t-]+avail'
    'websocket'
    'migrat'
)

missing_sections=()
for sec in "${REQUIRED_SECTIONS[@]}"; do
    if ! grep -qiE "^##[ \t]+.*${sec}" "$README"; then
        missing_sections+=("$sec")
    fi
done

Note: for 'request'/'response'/'routing' the `.*` wildcard is harmless — no spurious H2 headings in the README would accidentally match.
   *Status (fix/task-041-review-cleanup):* Fixed — converted `'^##[ \t]+request'`, `'^##[ \t]+response'`, `'^##[ \t]+routing'` to bare keywords `'request'`, `'response'`, `'routing'` and removed the dual-branch if/else. Single uniform loop now used.

17. [x] **code-simplifier** | `scripts/check-readme.sh:244` | code-structure
   The fence-count test uses `$(grep -cE '^```' ... || true)` to suppress a non-zero exit when there are no matches, but `grep -c` exits 1 only when the pattern is absent — if the file has an odd count it still exits 0. The `|| true` is harmless but adds noise that may mislead a reader into thinking the grep might legitimately fail.
   *Recommendation:* Remove the trailing `|| true` from the fence_count line; grep -c does not fail on zero-match files in most implementations, and the `|| true` is already unconditionally needed only if set -e is active (which it is not — the script uses `set -u`, not `set -eu`). No behavior change on the target platform.
   *Status (fix/task-041-review-cleanup):* Deferred — `set -euo pipefail` is now active (finding 2 fix), making `grep -c` exit 1 (no matches) fatal without `|| true`. Removing `|| true` would break the script on a README with no fences. The `${fence_count:-0}` guard was added for safety, but `|| true` must stay.

18. [x] **code-simplifier** | `scripts/check-readme.sh:63` | code-structure
   A1 failure path uses a direct `echo` + `exit 1` instead of the `fail()` helper, unlike every other failure path in the script.
   *Recommendation:* For consistency, extract the diff-printing into a local variable or use a two-argument variant of fail(). The current approach is functional but breaks the uniform pattern established by fail() elsewhere.
   *Status (fix/task-041-review-cleanup):* Fixed — A1 diff failure path now prints the diff first then calls `fail()` for the error message, consistent with all other failure paths.

19. [x] **code-simplifier** | `scripts/check-readme.sh:8` | naming
   The comment header lists the script's invariants as A1–A6 but the body also has an un-labelled 'A6b' sub-check (relative link resolution). Callers reading the header comment to understand what the script validates will not see A6b mentioned.
   *Recommendation:* Add a one-line entry for A6b in the opening comment block, e.g.: '  A6b. Every relative Markdown link resolves to a file on disk.' This keeps the comment and the code in sync with no behavior change.
   *Status (fix/task-041-review-cleanup):* Fixed — added `A6b. Every relative Markdown link resolves to a file on disk.` to the opening comment block.

20. [x] **performance-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-041/scripts/check-readme.sh:121` | missing-batching
   The A3 required-token loop (lines 121-130) calls `grep -qE` 25 times — once per token — each opening and scanning README.md from scratch. All 25 patterns could be ORed into a single grep invocation and the missing ones identified with a second targeted pass.
   *Recommendation:* Combine the 25 patterns into a single grep with alternation: `grep -oE '<p1>|<p2>|...' README.md | sort -u` gives the set of found tokens in one pass. Subtract from the full set to find missing ones. This halves the I/O from 25 file opens to 1 for the happy path and 2 for the failure path. Impact is negligible for a CI script over a 3 000-line file, but the pattern is worth noting for consistency with A2's single-pass design.
   *Status (fix/task-041-review-cleanup):* Deferred — impact is explicitly "negligible" per the finding. The loop is clear and easy to maintain. Performance optimization not warranted for a developer CI script over a small file.

21. [x] **performance-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-041/scripts/check-readme.sh:121` | missing-batching
   The A3 required-token loop (lines 121-130) still calls `grep -qE` once per token — now 26 invocations — each opening and scanning README.md independently. This is the same finding from iter 1 (previously 25 tokens); one token was added (`\biovec_entry\b`) without consolidating the loop.
   *Recommendation:* Consolidate into a single grep pass: `grep -oE '<p1>|<p2>|...' README.md | sort -u` produces the set of matched tokens in one file read. Subtract from the full set to identify missing ones. Impact is negligible for a CI script over a ~330-line file, but the single-pass design already used in A2 is the idiomatic and consistent approach.
   *Status (fix/task-041-review-cleanup):* Deferred — same rationale as finding 20. Now 30 invocations. Still negligible for a CI script.

22. [x] **performance-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-041/scripts/check-readme.sh:193` | missing-batching
   The A5 section-existence loop (lines 193-208) calls `grep -qiE` once per required section (11 calls), each scanning README.md independently. A single awk pass collecting all H2 headings into memory, followed by in-memory pattern matching, would touch the file exactly once.
   *Recommendation:* Extract all H2 lines with one `grep -iE '^##[ \t]+' README.md` into a shell variable, then test each pattern against that variable with `echo "$h2_lines" | grep -qiE "$sec"`. One file read instead of 11. Not a blocking issue — the file is small and the target is a developer CI script.
   *Status (fix/task-041-review-cleanup):* Deferred — explicitly "Not a blocking issue" per the finding. No change made.

23. [x] **performance-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-041/scripts/check-readme.sh:193` | missing-batching
   The A5 section-existence loop (lines 193-208) still calls `grep -qiE` once per required section pattern (11 calls), each scanning README.md from scratch. Unchanged from iter 1.
   *Recommendation:* Extract all H2 lines with one `grep -iE '^##[ \t]+' README.md` into a variable, then test each pattern against that variable in memory. One file read instead of 11. Not a blocking concern given the small file size.
   *Status (fix/task-041-review-cleanup):* Deferred — same rationale as finding 22. "Not a blocking concern" per the finding.

24. [x] **security-reviewer** | `README.md:173` | sensitive-data
   The request accessor table lists `get_pass()` returning `std::string_view` with the description 'Basic-auth password'. The README does not warn readers that the password is transmitted in cleartext unless TLS is enabled, and the example server on line 30 listens on plain HTTP port 8080 with no TLS qualifier. Users who follow the README and add basic-auth on top of the hello-world pattern will transmit credentials in cleartext.
   *Recommendation:* Add a note in the Request section and/or the Feature availability section that `get_user`/`get_pass` (basic-auth) and `get_digested_user` (digest-auth) are only safe over a TLS-enabled server (`HAVE_GNUTLS`), and link to the relevant example.
   *Status (fix/task-041-review-cleanup):* Fixed — added a security blockquote after the Authentication accessors table warning that basic auth and digest auth are only safe over TLS.

25. [x] **security-reviewer** | `README.md:320` | sensitive-data
   The `internal_error_handler` example echoes the raw `what()` string from an exception back to the HTTP client: `http_response::string(std::string{what}).with_status(500)`. In production this can leak internal exception messages, stack details, or file paths to clients — a common source of information disclosure (CWE-209).
   *Recommendation:* Add a comment in the example warning that `what` should be logged server-side and a generic message returned to the client, e.g., `// In production, log 'what' internally and return a generic message to the client.`
   *Status (fix/task-041-review-cleanup):* Fixed — added `// In production, log 'what' internally and return a generic message to the client.` comment to both internal_error_handler examples.

26. [x] **security-reviewer** | `scripts/check-readme.sh:223` | input-validation
   `$fence_count` is used in an arithmetic expression `$(( fence_count % 2 ))` without quotes. While `fence_count` is the output of `grep -cE` (always a non-negative integer or 0), an unexpected empty value (e.g., if `grep -c` returns nothing on a broken locale) would cause an arithmetic syntax error that propagates as a non-zero exit, making the fence check unexpectedly fatal for reasons unrelated to the README content.
   *Recommendation:* Use `$(( ${fence_count:-0} % 2 ))` to guard against an empty value, or quote the variable: `if [ "$(( fence_count % 2 ))" -ne 0 ]`.
   *Status (fix/task-041-review-cleanup):* Fixed — changed to `$(( ${fence_count:-0} % 2 ))` to guard against empty value.

27. [x] **spec-alignment-checker** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-041/README.md:208` | specification-gap
   README says with_header/with_footer/with_cookie/with_status return '*this' by value, but the actual signatures (http_response& for lvalue, http_response&& for rvalue) return references. PRD-RSP-REQ-004 specifies 'return a reference to *this'. The prose is technically imprecise but the accompanying code example demonstrates correct chaining and no user would be misled.
   *Recommendation:* Change 'returning `*this` by value so calls can chain' to 'returning `*this` by reference so calls can chain'.
   *Status (fix/task-041-review-cleanup):* Already fixed in merged README.md — "Fluent mutation" section (line 1001) correctly says "by reference on lvalues, by rvalue-reference on rvalues". No further action needed.

28. [x] **spec-alignment-checker** | `README.md:208` | specification-gap
   The README states 'returning `*this` by value' for `with_header`, `with_footer`, `with_cookie`, and `with_status`. The actual public API (src/httpserver/http_response.hpp, lines 312-322) declares both `http_response& with_header(...) &` and `http_response&& with_header(...) &&` — returning by reference (lvalue or rvalue reference depending on the call context), not by value. PRD-RSP-REQ-004 also says 'return a reference to *this'. The description is technically inaccurate.
   *Recommendation:* Change 'returning `*this` by value' to 'returning `*this` by reference (lvalue `&` or rvalue `&&` depending on context)' or simply 'returning `*this`' to remain accurate without over-specifying.
   *Status (fix/task-041-review-cleanup):* Same as finding 27 — already fixed in merged README.md. No further action needed.

29. [x] **spec-alignment-checker** | `README.md:241` | ears-requirement
   The Routing section leads the `route()` description with the multi-method bitwise-OR usage (`route(http_method::GET | http_method::HEAD, ...)`) before noting the single-method form. PRD-HDL-REQ-006 defines `route()` specifically as 'When a user wants to register a handler with the HTTP method known only at runtime' — the escape hatch for config-driven / table-driven registration where the method is a runtime value. The 'atomic multi-method' framing is not in the PRD specification of `route()` and may mislead users about its primary purpose.
   *Recommendation:* Lead the `route()` paragraph with the runtime-method use-case ('the method is known only at runtime or loaded from config') as specified in PRD-HDL-REQ-006, and treat the multi-method bitset form as a secondary capability.
   *Status (fix/task-041-review-cleanup):* Fixed — rewrote the `route()` section opening to lead with the runtime-method use-case (PRD-HDL-REQ-006), treating multi-method as secondary.

30. [x] **test-quality-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-041/scripts/check-readme.sh:192` | naming-convention
   The A5 section-matching loop mixes two different pattern shapes in the same array: bare keywords like `'threading'` (matched as `^##[ \t]+.*threading`) and anchored patterns like `'^##[ \t]+request'` (matched with plain `grep -qiE`). The distinction is controlled by a `[[ "$sec" == ^* ]]` guard, but this is not documented and is easy to misread. A future contributor adding a pattern starting with `^` for an unrelated reason could inadvertently change the matching mode.
   *Recommendation:* Split into two separate arrays (e.g. `KEYWORD_SECTIONS` and `ANCHORED_SECTIONS`) or add a comment block above the array explaining the convention and the dual-mode dispatch logic.
   *Status (fix/task-041-review-cleanup):* Fixed as part of finding 16 — removed the dual-mode dispatch entirely by converting all patterns to bare keywords. The `^`-prefix sentinel convention no longer exists.

31. [x] **test-quality-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-041/scripts/check-readme.sh:222` | missing-test
   The S1 balanced-fences check counts lines matching `^\`\`\`` with `grep -cE`. This pattern matches both opening fences (e.g. \`\`\`cpp, \`\`\`sh) and closing fences (bare \`\`\`). A document with two consecutive opening fences and two closing fences still gives an even count yet has logically unbalanced blocks. The check provides a useful basic sanity gate but would not catch a doubled opening fence.
   *Recommendation:* Document the known limitation in a comment: the even-count test catches the common unbalanced-fence mistake but not every structural imbalance. As a stronger check, run the same toggle-based awk used by S3 and verify `in_block == 0` at END. This is already trivially achievable with the existing awk idiom in the script.
   *Status (fix/task-041-review-cleanup):* Fixed — added a comment documenting the known limitation and noting that the S3 awk toggle-based pass could be extended for stricter checking.

32. [x] **test-quality-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-041/scripts/check-readme.sh:224` | missing-test
   iter1-3 (CRLF/BOM) not explicitly addressed: the A6b broken-link extraction loop and the A1 awk block-extractor do not strip \r from line endings. If README.md gains CRLF line endings (e.g., via git autocrlf or a Windows editor), the extract_first_cpp_block awk function would include a trailing \r on each extracted line, causing the A1 diff to fail with a confusing mismatch rather than a clear 'CRLF detected' message. check-examples.sh already strips \r in its awk processing (line 61); check-readme.sh does not. Practical risk is currently low because README.md has no CRLF/BOM and the grep-based checks (A2, A3, A5) function correctly with CRLF.
   *Recommendation:* Add a Markdown sanity check (e.g., S4) that asserts zero CRLF lines: `if grep -qP '\r$' "$README"; then fail "S4: README.md contains CRLF line endings"; fi`. Also add \r stripping (`gsub(/\r/, "")`) to the extract_first_cpp_block awk function, mirroring check-examples.sh line 61.
   *Status (fix/task-041-review-cleanup):* Fixed — added `gsub(/\r/, "")` to `extract_first_cpp_block` awk function and added S4 CRLF check.

33. [x] **test-quality-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-041/scripts/check-readme.sh:234` | missing-test
   A6b broken-link extraction does not handle fragment identifiers. The grep/sed pipeline extracts the full target including any #fragment suffix (e.g., 'examples/foo.cpp#section'), then tests [ -e "$REPO_ROOT/examples/foo.cpp#section" ] which always fails even when the file exists. No README links currently have fragments, so this does not cause a false failure today, but it is a latent defect that would fire the moment any fragment link is added.
   *Recommendation:* Strip fragments before the existence check: `target="${target%%#*}"` after extracting the target from the link, and before the `[ ! -e ... ]` test.
   *Status (fix/task-041-review-cleanup):* Fixed — added `target="${target%%#*}"` to strip fragment identifiers before the existence check in the A6b loop.

34. [x] **test-quality-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-041/scripts/check-readme.sh:246` | missing-test
   The optional markdownlint advisory block (lines 246-250) silently suppresses all markdownlint exit codes with `2>/dev/null` and only prints an advisory note — it never fails the script. If markdownlint is installed in CI but the README has genuine lint errors (e.g. duplicate H2 slugs that break GitHub's anchor links), the gate passes and the errors are invisible unless someone manually reads the advisory line. Given that the acceptance criterion explicitly requires 'README renders cleanly on GitHub', a lint failure should at minimum be prominently flagged rather than silently swallowed.
   *Recommendation:* Redirect stderr to stdout so lint warnings appear in the check output (`markdownlint -q "$README" 2>&1`), and consider promoting the advisory to a warning-level failure on a configurable `MARKDOWNLINT_STRICT` flag, analogous to the existing `HEADER_HYGIENE_STRICT` pattern used in Makefile.am.
   *Status (fix/task-041-review-cleanup):* Fixed — changed to `2>&1` so lint output is visible in CI logs, added `MARKDOWNLINT_STRICT=yes` support to promote to a hard gate (analogous to `HEADER_HYGIENE_STRICT`).

35. [x] **test-quality-reviewer** | `scripts/check-readme.sh:234` | implementation-coupling
   The process substitution `< <(grep ... | sed ...)` relies on bash-specific behavior: under set -euo pipefail, the exit status of a process substitution used as input to a while loop is silently discarded by bash. This is correct and safe today (verified by running the zero-links edge case), but it is a non-obvious bash subtlety. A future porter assuming POSIX sh or a stricter shell could trigger unexpected behavior.
   *Recommendation:* This is low-priority since the script already requires bash (#!/usr/bin/env bash). Consider adding a brief inline comment such as `# grep exit-1 (no matches) is not propagated from process substitution under bash set -e` to document the intentional reliance on this behavior.
   *Status (fix/task-041-review-cleanup):* Fixed — added inline comment before the while loop documenting the bash-specific behavior of process substitution exit-status handling.
