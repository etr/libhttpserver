# Unworked Review Issues

**Run:** 2026-06-02 11:53:20
**Task:** TASK-059
**Total:** 15 (0 critical, 3 major, 12 minor)

## Major

1. [x] **cloud-infrastructure-reviewer** | `.github/workflows/verify-build.yml:357` | iac-anti-pattern
   [SEC-04 Supply-Chain Integrity] GitHub Actions are pinned to mutable tag references (e.g. `actions/checkout@v4`, `actions/cache@v4`, `msys2/setup-msys2@v2`, `codecov/codecov-action@v5`) rather than immutable commit SHAs. A tag can be force-pushed by the upstream maintainer — or compromised — to silently inject malicious code into every CI run. TASK-059 correctly pins PMD by digest but the same discipline is not applied to the Actions themselves, which execute with full runner permissions.
   *Recommendation:* Pin every `uses:` reference to the full 40-character commit SHA, e.g. `actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683` (v4.2.2). Maintain a renovate.json or Dependabot `github-actions` ecosystem config to automate SHA bump PRs. See https://docs.github.com/en/actions/security-guides/security-hardening-for-github-actions#using-third-party-actions and https://docs.github.com/en/code-security/supply-chain-security/keeping-your-dependencies-updated-automatically/configuration-options-for-the-dependabot.yml-file#package-ecosystem
   *Fixed:* Pinned `actions/checkout@v4` → 11bd71901bbe5b1630ceea73d27597364c9af683 (v4.2.2), `actions/cache@v4` → 0057852bfaa89a56745cba8c7296529d2fc39830 (v4.3.0), `msys2/setup-msys2@v2` → e9898307ac31d1a803454791be09ab9973336e1c (v2.31.1), `codecov/codecov-action@v5` → 75cd11691c0faa626561e295848008c8a7dddffe (v5.5.4). Dependabot config is a separate follow-up.

2. [x] **cloud-infrastructure-reviewer** | `.github/workflows/verify-build.yml:483` | iac-anti-pattern
   [SEC-04 Supply-Chain Integrity] The IWYU build clones `--branch clang_18` from GitHub (`include-what-you-use/include-what-you-use.git`), which is a mutable branch reference. The existing TODO comment on lines 476-478 correctly identifies this risk but the fix has not been applied: the clone has no pinned commit SHA and no post-clone integrity check. Any adversarial push to that branch would be compiled and installed with `sudo make install` on the runner.
   *Recommendation:* Replace the mutable branch clone with `git clone` followed by `git checkout <immutable-SHA>` and a `git rev-parse --verify HEAD` assertion. Obtain the verified SHA from the upstream release/tag page at https://github.com/include-what-you-use/include-what-you-use/tags and commit it as a comment alongside the SHA. This converts the mutable reference into an immutable one consistent with the PMD pin just added. See https://docs.github.com/en/actions/security-guides/security-hardening-for-github-actions#using-third-party-actions
   *Fixed:* Replaced mutable branch clone with full clone + `git checkout 377eaef70cdda47368939f4d9beabfabe3f628f0` + `git rev-parse --verify HEAD` SHA assertion. `set -euo pipefail` added. Updated supply-chain comment with rotation instructions.

3. [x] **cloud-infrastructure-reviewer** | `.github/workflows/verify-build.yml:509` | iac-anti-pattern
   [SEC-04 Supply-Chain Integrity] The macOS curl tarball (`curl-7.75.0.tar.gz`) is downloaded from an S3 bucket (`libhttpserver.s3.amazonaws.com`) over HTTPS but is NOT verified against a pinned sha256 digest before it is compiled and installed. Lines 514-515 only log the observed hash; they do not fail the step on mismatch. A compromised or tampered S3 object would be installed silently. This is a pre-existing gap that TASK-059's PMD work did not address, leaving an inconsistent trust model in the same workflow.
   *Recommendation:* Replace the `echo` log line with a hard assertion identical to the PMD and libmicrohttpd patterns already used elsewhere in this file: `echo "<expected-sha256>  curl-7.75.0.tar.gz" | shasum -a 256 -c`. Record the expected digest by downloading the file once from a trusted context, running `shasum -a 256`, and committing the result. Also add `-f` / `--fail` to the bare `curl` invocation on line 509 (it currently lacks these flags, unlike the PMD step). See https://docs.github.com/en/actions/security-guides/security-hardening-for-github-actions#using-third-party-actions
   *Fixed:* Added `curl -fsSL` and `shasum -a 256 -c` assertion with sha256 `4d51346fe621624c3e4b9f86a8fd6f122a143820e17889f59c18f245d2d8e7a6` (cross-checked between S3 mirror and upstream https://curl.se/download/). `set -euo pipefail` added. Rotation comment added.

## Minor

4. [x] **code-quality-reviewer** | `.github/workflows/verify-build.yml:439` | test-coverage
   Future reviewers have no way to verify the wrong-hash rehearsal claim after the fact.
   *Acknowledged-deferred:* A wrong-hash regression test is a small `workflow_dispatch` job. Listed as a known gap together with finding #15; the rotation comment now stands as the lightweight contract.

5. [x] **code-quality-reviewer** | `.github/workflows/verify-build.yml:451` | error-handling
   Claimed the sha256 digest had 63 hex characters; reviewer miscount.
   *Fixed:* Verified `echo -n "${PMD_SHA256}" | wc -c` → 64. The digest is the canonical SHA-256. No change needed.

6. [x] **code-simplifier** | `.github/workflows/verify-build.yml:444` | naming
   Reworded "commit the resulting value here" → "record the resulting value in PMD_SHA256 in this file".

7. [x] **code-simplifier** | `.github/workflows/verify-build.yml:448` | code-structure
   Split the run-on rotation comment into two explicit sentences (sha256 sidecar / Maven Central attestation).

8. [x] **code-simplifier** | `.github/workflows/verify-build.yml:449` | code-structure
   Added `set -euo pipefail` as the first line of the PMD run block.

9. [x] **code-simplifier** | `.github/workflows/verify-build.yml:454` | code-structure
   Dropped the trailing `-` from `sha256sum -c -` → `sha256sum -c`.

10. [x] **code-simplifier** | `specs/unworked_review_issues/2026-06-02_113040_task-059.md:6` | naming
   *Status:* No change required (per the finding itself).

11. [x] **housekeeper** | `specs/unworked_review_issues/2026-06-02_113040_task-059.md:1` | documentation-stale
   *Status:* No action required (per the finding itself).

12. [x] **security-reviewer** | `.github/workflows/verify-build.yml:483` | supply-chain
   IWYU mutable branch reference.
   *Fixed:* Addressed by this sweep's major #2 (pinned to clang_18 commit 377eaef + rev-parse assertion). TODO removed.

13. [x] **security-reviewer** | `.github/workflows/verify-build.yml:509` | supply-chain
   macOS curl tarball logs sha256 instead of asserting it.
   *Fixed:* Addressed by this sweep's major #3 (sha256-pinned to 4d51346…, with `curl -fsSL` and `shasum -c`). TODO removed.

14. [x] **spec-alignment-checker** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-059/.github/workflows/verify-build.yml:475` | specification-gap
   Stale TODO(security) comments + inaccurate task-spec claim.
   *Fixed:* The two TODOs (IWYU and CURL) were removed when their respective fixes landed in this sweep (majors #2 and #3). The task spec for TASK-059 is closed; no further action required.

15. [x] **test-quality-reviewer** | `.github/workflows/verify-build.yml:454` | missing-test
   No automated test for the hash-mismatch failure path.
   *Acknowledged-deferred:* Same gap as finding #4; the right shape is a dedicated `workflow_dispatch` job that intentionally supplies a wrong hash. Tracked as a known gap until a follow-up CI hardening task picks it up.
