# Unworked Review Issues

**Run:** 2026-06-07 23:25:48
**Task:** TASK-071
**Total:** 32 (0 critical, 0 major, 32 minor)

## Minor

1. [ ] **architecture-alignment-checker** | `specs/architecture/04-components/route-table.md:13` | interface-contract
   The architecture document still describes route_entry::handler as `std::variant<lambda_handler, std::shared_ptr<http_resource>>` (line 13 of route-table.md). TASK-071 collapsed this to a bare `std::shared_ptr<http_resource>`, which is correctly reflected in the implementation (route_entry.hpp, webserver_dispatch.cpp, webserver_routes.cpp) and in the implementation-status paragraph at the bottom of the same section, but the field-description bullet at line 13 was not updated. The static_assert in webserver_on_methods_test.cpp and the comment history in route_entry.hpp record the change, but the normative architecture section remains stale.
   *Recommendation:* Update the `A route_entry carries:` bullet in specs/architecture/04-components/route-table.md line 13 from `std::variant<lambda_handler, std::shared_ptr<http_resource>>` to `std::shared_ptr<http_resource>` (a class-derived resource or a lambda_resource shim wrapping user lambdas), and add a note that lambda_handler now lives in detail/lambda_resource.hpp as the per-method slot type rather than as a variant arm of route_entry.

2. [ ] **architecture-alignment-checker** | `specs/architecture/11-decisions/DR-004.md:20` | interface-contract
   DR-004 Consequences says 'First realised in TASK-025 via `detail::lambda_handler` ... in `src/httpserver/detail/route_entry.hpp`'. TASK-071 relocated the `lambda_handler` typedef from route_entry.hpp to lambda_resource.hpp (confirmed: `using lambda_handler` is absent from route_entry.hpp and present in lambda_resource.hpp). The file path in DR-004 is now stale.
   *Recommendation:* Update the Consequences bullet in DR-004 to reference `src/httpserver/detail/lambda_resource.hpp` instead of `src/httpserver/detail/route_entry.hpp`, and note that the typedef moved in TASK-071 (the variant arm it formerly anchored was removed; the typedef itself survives as the per-method slot signature inside lambda_resource).

3. [ ] **code-quality-reviewer** | `src/detail/webserver_aliases.cpp:312` | code-readability
   The install_not_found_alias_ body registers a real hook whose body is a documented no-op (it only calls `(void) ctx`). The extensive block comment inside the lambda correctly explains why the body is intentionally empty, but the no-op lambda itself could mislead a future reader into thinking the hook has work to do. The comment is good, but the code and comment carry the entire explanatory burden for a non-obvious design choice (route_resolved is observation-only).
   *Recommendation:* This is primarily a documentation concern. Consider extracting a named free function (e.g. `observation_noop`) so the intent is visible at the registration call site without requiring the reader to dive into the lambda body. This is a style preference — the current approach is acceptable.

4. [ ] **code-quality-reviewer** | `src/detail/webserver_dispatch.cpp:278` | code-readability
   The comment block at lines 274-278 in resolve_resource_for_request describes the TASK-071 variant collapse in past tense but is phrased as if the variant still needs explaining to the reader. Since route_entry.hpp already carries the canonical history block, this comment in the dispatch file is redundant and makes the function longer without adding runtime clarity.
   *Recommendation:* Trim the TASK-071 history note to a single line cross-reference (`// TASK-071: handler is now shared_ptr directly; see route_entry.hpp.`) and focus the local comment on the defensive null check rationale, which is the actionable information for a reader of this function.

5. [ ] **code-quality-reviewer** | `src/httpserver/detail/route_entry.hpp:54` | code-elegance
   The TASK-071 history comment block in route_entry.hpp (lines 62-72) is thorough but duplicated almost verbatim in lambda_resource.hpp (lines 72-78) and webserver_dispatch.cpp (lines 274-277) and webserver_routes.cpp (lines 191-195) and webserver_on_methods_test.cpp (lines 179-181). This four-way duplication of the migration rationale increases maintenance surface: if the reasoning evolves, all four copies must be updated.
   *Recommendation:* Consolidate the migration rationale into the primary definition site (route_entry.hpp) with a single canonical block comment, and replace the other sites with a brief cross-reference: `// TASK-071: see route_entry.hpp for migration history.` This reduces comment redundancy without losing the paper trail.

6. [ ] **code-quality-reviewer** | `test/integ/hooks_not_found_alias_test.cpp:141` | test-coverage
   The third test (not_found_alias_does_not_suppress_user_route_resolved_hook) verifies the user hook fires on a miss, but does not verify that the hook also fires (or explicitly does not fire) on a hit path. The acceptance criterion mentions only the miss path, so this is not a gap relative to the stated AC, but the test name's 'suppress' wording implies broader coverage than is actually asserted.
   *Recommendation:* Either add a hit-path assertion (register a route, make a matching request, assert user_observer_calls increments) or narrow the test name to `not_found_alias_does_not_suppress_user_route_resolved_hook_on_miss` to accurately scope what is pinned.

7. [ ] **code-quality-reviewer** | `test/integ/hooks_not_found_alias_test.cpp:77` | test-coverage
   The test suite uses three distinct hard-coded ports (8211, 8212, 8213). If any other test binary in the suite happens to bind one of these ports concurrently, the tests will fail non-deterministically. No effort is made to ensure the chosen ports are unused or to document why they are safe.
   *Recommendation:* Prefer ephemeral port 0 (letting the OS assign a free port) or document explicitly that these ports are reserved for this test file in a central port registry, consistent with the pattern used by webserver_on_methods_test.cpp which at least uses a named PORT constant.

8. [ ] **code-quality-reviewer** | `test/integ/hooks_not_found_alias_test.cpp:83` | test-coverage
   wait_for_server() silently exits the retry loop (returns without polling) when curl_easy_init() returns nullptr. In that case the calling test proceeds immediately and may fail with CURLE_COULDNT_CONNECT on the real request. The nullptr branch was presumably an allocation guard, but it leaves the caller with no signal that the poll was skipped.
   *Recommendation:* Either add a fallback sleep or assert/log when curl_easy_init() returns nullptr inside the polling loop so failures remain diagnosable. A simple `std::this_thread::sleep_for(std::chrono::milliseconds(200)); return;` before `break` would prevent an instant false-negative from a transient allocation failure.

9. [ ] **code-quality-reviewer** | `test/integ/hooks_not_found_alias_test.cpp:88` | test-coverage
   Each integration test uses a fixed 50ms sleep to wait for the webserver to start. In a slow CI environment this is fragile, and across three tests (three servers) it adds 150ms of mandatory latency. The sleep is a test smell and can produce spurious failures on overloaded machines.
   *Recommendation:* Replace the fixed sleep with a polling loop that probes the port (e.g. curl with CURLOPT_CONNECTTIMEOUT_MS in a retry loop up to ~2 s) or use the webserver's built-in readiness signal if one is available, consistent with how other integration tests in this suite are structured.

10. [ ] **code-simplifier** | `src/detail/webserver_aliases.cpp:312` | naming
   The block comment above install_not_found_alias_ (lines 285-329) is substantially longer than the equivalent comments for install_auth_alias_ and install_method_not_allowed_alias_. The tail of the comment (lines 302-329) narrates historical debt context ('TASK-071: previously labelled structurally deferred') that is more appropriate in a git commit message than in a source file, adding noise without improving the reader's understanding of the current design.
   *Recommendation:* Trim the trailing historical-debt paragraph (starting 'TASK-071: previously labelled...') from the block comment. Keep the structural-pin rationale and the DR-012 §4.10 reference, which are genuinely explanatory. Move the historical context to the commit message where it belongs.

11. [ ] **code-simplifier** | `src/detail/webserver_aliases.cpp:315` | code-structure
   The observation hook body in install_not_found_alias_ explicitly suppresses the unused-variable warning with `(void) ctx;` on a separate line, but the lambda parameter could simply be unnamed to express the same intent more directly and consistently with C++ idiom.
   *Recommendation:* Replace `[](const route_resolved_ctx& ctx) { (void) ctx; }` with `[](const route_resolved_ctx&) {}`. This makes the intent (parameter intentionally unused) self-documenting at the declaration site rather than via a workaround statement inside the body, and matches the idiom already used throughout the codebase for suppressing unused-parameter warnings in lambdas.

12. [ ] **code-simplifier** | `src/detail/webserver_dispatch.cpp:248` | code-structure
   The TASK-067 comment block (lines 263-277) inside resolve_resource_for_request is long and repeats information already captured in the block-level TASK-053 comment directly above it. The TASK-067 sub-comment ('single_resource mode used to short-circuit...') accurately describes a removed fast path, but its location inside the function body after the preceding comment block makes the overall comment chain difficult to skim.
   *Recommendation:* Consolidate the TASK-053 and TASK-067 explanatory text into a single pre-function comment block. Remove the inline TASK-067 sub-comment from inside the function body. The TASK-071 note at the top already summarises the variant collapse; a shorter function body with a single explanatory header is easier to read.

13. [ ] **code-simplifier** | `src/httpserver/detail/lambda_resource.hpp:78` | code-structure
   The `lambda_handler` typedef line has a `// NOLINT(whitespace/line_length)` suppression, indicating the line is too long. The typedef is a single alias and can be split across two lines to stay within the project line-length limit without the suppression.
   *Recommendation:* Split the typedef to avoid the NOLINT suppression:
```cpp
using lambda_handler =
    std::function<::httpserver::http_response(const ::httpserver::http_request&)>;
```
This makes the line-length policy self-enforcing rather than suppressed.

14. [ ] **code-simplifier** | `test/integ/hooks_not_found_alias_test.cpp:83` | code-structure
   wait_for_server() builds the same URL string on every polling iteration. The string concatenation ("http://127.0.0.1:" + std::to_string(port) + "/") is evaluated inside the while-loop body, creating a new std::string allocation each pass even though the URL never changes across iterations.
   *Recommendation:* Hoist the URL string construction before the loop: `std::string url = "http://127.0.0.1:" + std::to_string(port) + "/"; while (clock::now() < deadline) { ... curl_easy_setopt(curl, CURLOPT_URL, url.c_str()); ... }`. This eliminates the repeated heap allocation on the polling path.

15. [ ] **code-simplifier** | `test/integ/hooks_not_found_alias_test.cpp:84` | naming
   The type alias `using clock = std::chrono::steady_clock;` is declared inside wait_for_server() but is a one-shot alias only used to write `clock::now()` twice. The abbreviated name `clock` shadows the standard `std::clock` name and buys little readability over spelling `std::chrono::steady_clock` directly or extracting it at file scope.
   *Recommendation:* Either remove the alias and spell out `std::chrono::steady_clock::now()` at the two call sites (clear and self-contained), or rename to `steady_clock` to avoid shadowing `std::clock`. Both are behavior-neutral.

16. [ ] **code-simplifier** | `test/integ/hooks_not_found_alias_test.cpp:89` | code-structure
   The test `not_found_alias_default_body_when_no_user_handler` uses `std::this_thread::sleep_for(std::chrono::milliseconds(50))` to wait for the server to become ready, matching the pattern used throughout the test suite. This is a pre-existing pattern, not introduced by this task, but the third test `not_found_alias_does_not_suppress_user_route_resolved_hook` (line 141) omits the sleep after `ws.start(false)`, creating an inconsistency: the user-added hook test may race the server startup on slow machines.
   *Recommendation:* Add `std::this_thread::sleep_for(std::chrono::milliseconds(50));` after `ws.start(false)` in the third test (around line 161), consistent with the other two tests in the same suite. This makes the startup-wait pattern uniform across all three tests.

17. [ ] **code-simplifier** | `test/integ/hooks_not_found_alias_test.cpp:96` | code-structure
   The comment on the CURLE_OK / CURLE_COULDNT_CONNECT branch says "anything else except CURLE_COULDNT_CONNECT is a different error (TLS, etc.) — still counts as the TCP stack being up." This is subtly misleading: CURLE_OK means the TCP handshake completed successfully; CURLE_OPERATION_TIMEDOUT or similar still means the server is not yet up. The condition `rc != CURLE_COULDNT_CONNECT` therefore returns too eagerly on transient poll timeouts.
   *Recommendation:* Change the early-return condition to `rc == CURLE_OK` to precisely express "TCP is up", and remove the misleading comment. Any non-OK result (including CURLOPT_CONNECTTIMEOUT_MS expiry) should continue polling. This is a behavior-correctness improvement but is limited to the test helper and cannot affect production code.

18. [ ] **performance-reviewer** | `src/detail/webserver_aliases.cpp:128` | memory-allocation
   append_sanitized iterates character by character appending to a std::string with operator+=. For long paths this causes repeated capacity checks on every character. The reservation of 64 bytes is a fixed guess and does not account for the actual path + method length, so paths longer than ~50 characters will trigger a reallocation mid-append.
   *Recommendation:* Reserve line.capacity() = path.size() + method.size() + sizeof(" METHOD: ") - 1 before the first append_sanitized call. This single reserve() eliminates all reallocations regardless of path length. The current 64-byte guess is fine for short paths but degrades for long URLs.

19. [ ] **performance-reviewer** | `src/detail/webserver_aliases.cpp:216` | memory-allocation
   In the auth hook registered by install_auth_alias_(), a std::string is heap-allocated from ctx.request->get_path() on every request that has a non-empty auth_skip_paths_normalized list, even when the path trivially does not match any skip prefix. The guard `if (!ws_ptr->auth_skip_paths_normalized.empty())` avoids the allocation when the list is empty (the common case), but for servers that do configure skip-paths every authenticated request pays an unnecessary heap allocation before the prefix check.
   *Recommendation:* Pass the string_view returned by get_path() directly to should_skip_auth and let that function do a view-based prefix comparison, avoiding the std::string construction entirely. If should_skip_auth already accepts string_view internally, change its public signature; otherwise add a string_view overload.

20. [ ] **security-reviewer** | `src/detail/webserver_aliases.cpp:128` | A09: Logging Failures
   The append_sanitized() function in the log_access alias (CWE-117) replaces ASCII control characters (< 0x20 or == 0x7F) with '-'. This correctly prevents newline/CR injection in log lines from the path and method fields. However, the sanitization range misses the DEL character (0x7F) being correctly handled already, and also misses non-ASCII byte sequences (>= 0x80) that could render as control characters in terminals or log viewers using certain encodings. This is a minor hardening gap — not a practical injection risk in UTF-8 environments.
   *Recommendation:* The current sanitization is sufficient for preventing log injection (the critical vector is newlines/CRs which are covered). As a hardening measure, consider also replacing bytes >= 0x80 with '-' if the log consumer is known to use a non-UTF-8 encoding. In UTF-8 environments the current approach is safe.

21. [ ] **security-reviewer** | `src/detail/webserver_aliases.cpp:195` | A01: Broken Access Control
   The auth_handler alias (install_auth_alias_) fires only on route-hit paths (found==true). Requests for unregistered routes receive 404 without auth being checked, while registered routes receive 401 on auth failure. This 401-vs-404 distinction acts as a route-enumeration oracle (CWE-200): an unauthenticated client can distinguish registered from unregistered paths by comparing response codes. This is a known design constraint that is explicitly documented in a comment block added in this PR.
   *Recommendation:* This is already documented in the code as a design note with guidance: callers needing uniform auth on all requests should add a catch-all fallback route or register a not_found_handler with equivalent auth logic. No code change required, but consider surfacing this in the public API documentation so integrators are aware of the oracle behavior before deploying.

22. [ ] **security-reviewer** | `src/detail/webserver_aliases.cpp:312` | A04: Insecure Design
   The install_not_found_alias_() hook body is a pure observation marker (void lambda that immediately discards ctx via (void)ctx). The comment correctly documents that the on-wire 404 body is produced by not_found_page() separately. However, there is no assertion or static_assert that the route_resolved phase truly cannot return a hook_action that shapes the response — the safety relies entirely on the route_resolved_ctx type not exposing a response slot, which is a type-level enforcement rather than a runtime check. This is a correct design but the absence of any runtime invariant check means a future refactor that mistakenly adds a response slot to route_resolved_ctx would not be caught at the alias hook level.
   *Recommendation:* This is well-mitigated by the type system (route_resolved_ctx has no response slot by construction) and is verified by the new integ tests. No change is strictly required. A static_assert or comment at the route_resolved_ctx definition reinforcing that it must never carry a mutable response slot would harden the design contract against future drift.

23. [ ] **security-reviewer** | `src/detail/webserver_routes.cpp:423` | Input Validation
   The validate_on_methods_inputs_() function correctly rejects paths containing embedded null bytes (CWE-20) and empty method sets and null handlers. However, there is no length limit enforced on the path string. An extremely long path (e.g., megabyte-scale) will be accepted at registration time and stored in the route table data structures (exact_routes_ unordered_map key, radix tree nodes). This is a hardening gap, not an exploitable vulnerability, because registration is a privileged server-setup operation not directly accessible to HTTP clients.
   *Recommendation:* Consider adding a reasonable upper bound on path length (e.g., 4096 or 8192 bytes) in validate_on_methods_inputs_() to guard against accidental misconfiguration. Since registration is a server-side API call and not reachable from the network, this is low priority.

24. [ ] **spec-alignment-checker** | `src/detail/webserver_aliases.cpp:312` | acceptance-criteria
   The first acceptance criterion requires install_not_found_alias_ to produce 'the same 404 response shape as the v1 not_found_handler setter'. The alias hook body is intentionally an observation-only no-op that discards `ctx`; the actual 404 body comes from webserver_impl::not_found_page which is the v1 path. The design is internally documented (DR-012 §4.10) and validated by the integ test, but the criterion as written could be read as requiring the alias itself to shape the response. The implementation is architecturally correct per DR-012 — this is a minor spec-interpretation variance, not a defect.
   *Recommendation:* No code change required. Consider amending the acceptance criterion in TASK-071.md to clarify that 'produces the same 404 shape' means the end-to-end wire contract is preserved via not_found_page, not that the alias hook body synthesises the response directly.

25. [ ] **spec-alignment-checker** | `src/httpserver/webserver_routes.hpp:190` | action-item
   The action item states: 'Update Doxygen on webserver::route(...) if the variant shape changed.' The variant was removed (route_entry::handler is now a bare shared_ptr<http_resource>), but the Doxygen comment on webserver::route() in webserver_routes.hpp does not mention the simplified handler storage. The comment is technically accurate (it describes the public API contract, not the internal storage representation), but the action item asked for an update. The implementation comment in webserver_dispatch.cpp (lines 271-277) and in route_entry.hpp (lines 65-71) documents the collapse thoroughly.
   *Recommendation:* The implementation-level documentation is thorough and in the right places. If the action item is interpreted strictly, add a sentence to the route() Doxygen noting that lambda handlers are stored via a lambda_resource shim (shared_ptr<http_resource>) rather than a bare std::function.

26. [ ] **test-quality-reviewer** | `test/integ/hooks_not_found_alias_test.cpp:109` | naming-convention
   The test name `not_found_alias_invokes_user_handler_on_miss_with_v1_body` includes `_with_v1_body` which describes an implementation detail (the handler being invoked at the v1 call site) rather than the observable behaviour being verified. The `_v1_body` suffix will become stale if the body-synthesis path is ever moved, making the name misleading.
   *Recommendation:* Rename to `not_found_alias_custom_handler_returns_user_body_on_miss` to focus on the observable outcome (custom body on a 404 response) without encoding the internal path name.

27. [ ] **test-quality-reviewer** | `test/integ/hooks_not_found_alias_test.cpp:77` | missing-test
   The new integration test covers the default-body 404, the custom-handler body, and the hook-ordering case, but does not exercise a hit path (a request to a registered route). The observation hook in the third test case (`not_found_alias_does_not_suppress_user_route_resolved_hook`) only fires on misses. A hit-path assertion would verify that `ctx.matched.has_value()` is true and that `user_observer_misses` stays at 0 when a route is found, providing a cross-check that the miss detection in `ctx` is not accidentally always true.
   *Recommendation:* In the third test, add a second curl request to a registered route (register one with `ws.register_path` or `ws.on_get`), then assert that `user_observer_calls` incremented again but `user_observer_misses` did not. This validates the `ctx.matched.has_value()` branch in both directions with a single additional request.

28. [ ] **test-quality-reviewer** | `test/integ/hooks_not_found_alias_test.cpp:86` | test-isolation
   wait_for_server() breaks out of the polling loop with no return when curl_easy_init() returns nullptr (line 88: 'if (curl == nullptr) break'). The function then returns without waiting, so the test proceeds with the server potentially not yet ready. On a resource-exhausted host this silently removes the reliability guarantee the helper was added to provide.
   *Recommendation:* Replace 'break' with 'return' so the semantics are 'give up and proceed' consistently with the timeout-expiry path, or add a small fixed sleep before returning to give the server minimal startup time in this degenerate case.

29. [ ] **test-quality-reviewer** | `test/integ/hooks_not_found_alias_test.cpp:94` | aaa-violation
   In `not_found_alias_default_body_when_no_user_handler`, `ws.stop()` is called between the curl request (Act) and the HTTP-code/body assertions (Assert) at lines 98–100. The stop is logically part of teardown, but placing it between the last action and the assertions means a test harness reporting the stop as part of the act phase could obscure the boundary. More practically, if either assertion were to be added before `ws.stop()` in the future, the stop could be forgotten.
   *Recommendation:* Move `ws.stop()` to after all assertions, or use RAII (a guard object calling `stop()` in its destructor) to make teardown automatic and keep Act and Assert contiguous.

30. [ ] **test-quality-reviewer** | `test/unit/route_table_test.cpp:177` | naming-convention
   Test name 'cache_duplicate_insert_replaces_in_place_and_keeps_size' is accurate but the comment block immediately above it (lines 259-260) refers to 'Minor #30: multiple parameterised segments coverage' and then immediately notes the test already exists elsewhere. The stale comment block above the cache test implies a phantom test that was never written, adding reader confusion.
   *Recommendation:* Remove or relocate the stale 'Minor #30:' comment block (lines 259-261) so the cache test stands alone without misleading cross-references.

31. [ ] **test-quality-reviewer** | `test/unit/routing_regression_test.cpp:551` | redundant-test
   The updated comment at line 568–569 (`// TASK-071: route_entry::handler is now a bare shared_ptr<http_resource>`) is correct, but the test `overlapping_two_regex_routes_deterministic_first_wins` asserts `r.entry.handler == first` (pointer identity). This asserts on the route_entry internals (the shared_ptr value) rather than on observable dispatch behavior (e.g., the response body). It is tightly coupled to the internal storage representation and would break if the handler field were ever wrapped or adapted, even if routing behavior was preserved.
   *Recommendation:* Consider replacing the pointer-identity assertion with a wire-level assertion (start the server, issue a real request, check the response body). If keeping the unit-level assertion for performance reasons, add a comment explaining why pointer-identity is the correct proxy for 'first-registered wins' in this context, so a future refactorer does not break this test while preserving routing behavior.

32. [ ] **test-quality-reviewer** | `test/unit/webserver_on_methods_test.cpp:264` | multiple-concerns
   all_seven_on_methods_serve_their_method tests seven HTTP methods in a single test body. A regression in any one method fails the test without clearly identifying which method broke. The per-method tests (on_get_dispatches_get, on_post_dispatches_post, etc.) added later do cover this, making all_seven_on_methods_serve_their_method a partially redundant aggregate.
   *Recommendation:* Consider removing all_seven_on_methods_serve_their_method now that individual per-method tests exist. If keeping it for the 'compose all methods on one path' coverage angle, narrow the assertion to the composition fact (all methods coexist on the route) rather than re-testing dispatch correctness.
