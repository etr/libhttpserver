# Unworked Review Issues

**Run:** 2026-05-26 23:01:00
**Task:** TASK-051
**Total:** 62 (0 critical, 5 major, 57 minor)

**Processed:** 2026-05-27 (fix/task-051-2nd-review-cleanup)

## Major

1. [x] **architecture-alignment-checker** | `src/detail/webserver_dispatch.cpp:404` | adr-violation
   The per-route handler_exception chain fires AFTER the server-wide chain, and if the server-wide chain short-circuits the per-route chain is skipped. This is correct per DR-012 ordering ('server-wide before per-route'). However, when neither chain produces a response, the code falls through to emit the hardcoded 500 directly (line 417-419) rather than allowing the per-route chain another chance. More critically, the handler_exception case is the one phase where DR-012 §5.6 says 'the handler_exception chain continues to the next hook when one of its hooks throws' — but for the per-route handler_exception chain in resource_hook_table::fire_handler_exception (resource_hook_table.cpp), throwing hooks are logged and the chain continues within the per-route scope. This behavior is consistent with the spec but differs subtly from webserver_impl::fire_handler_exception which also has a last-position alias slot. The per-route chain in handle_dispatch_exception runs AFTER the alias slot (the alias is exhausted inside impl->fire_handler_exception), meaning the per-route chain has a chance to respond even after the v1 alias fired without short-circuiting. This is architecturally correct per DR-012's 'server-wide first, then per-route'. However the task spec action item says 'If server-wide short-circuited, do NOT run per-route — the response is already fixed.' The implementation correctly checks for server-wide short-circuit (the `if (auto sc = impl->fire_handler_exception(ctx))` guard). A subtle issue: when server_chain is true but the server-wide chain does NOT short-circuit, the per-route chain runs. But when server_chain is false and per_route is true, the per-route chain also runs. This matches the spec. The actual concern is that in the 'both chains run, neither short-circuits' case, the fallback is the hardcoded 500 (line 417-419), which skips the run_internal_error_handler_safely fallback. This is intentional per DR-009 §5.2 point 4 ('the handler_exception chain itself continues... if all hooks pass, emit hardcoded 500'). This is compliant.
   *Recommendation:* No change required — on further analysis the implementation correctly follows DR-012 and DR-009 §5.2. Flag this as informational to document the subtle interaction explicitly in the code comment at line 415-419 (the comment already references §5.2 point 4, which is adequate). Consider a follow-up to make the per-route handler_exception fallback behavior explicit in arch §5.6.
   **Action:** DEFERRED — implementation is correct per DR-012 and DR-009 §5.2. The existing code comment at the fallback site already references §5.2 point 4. Added a lifetime comment to `handle_dispatch_exception` clarifying the `res`/`rtable` lifetime relationship (finding #12 overlap).

2. [x] **code-simplifier** | `src/resource_hook_table.cpp:135` | code-structure
   The five append_* methods (append_before_handler, append_handler_exception, append_after_handler, append_response_sent, append_request_completed) are structurally identical: fetch_add next_slot_id_, take unique_lock, push_back, store any_hooks_ gate, return id. The only differences are the target vector member and the hook_phase value. This is needless repetition (five copies of the same 8-line body) that inflates the file and means any future change to the append protocol (e.g. adding a debug counter) must be applied five times.
   *Recommendation:* Extract a private template helper `template<hook_phase P, typename Sig> uint64_t append_impl(std::vector<entry<Sig>>& vec, std::function<Sig> fn)` that contains the shared body. Each public overload becomes a one-liner that delegates to append_impl<hook_phase::before_handler>(hooks_before_handler_, std::move(fn)). This mirrors the existing pattern of fire_short_circuit_impl / fire_void_impl in the same file.
   **Action:** FIXED — extracted `append_impl<P, Sig>` template helper in the anonymous namespace of `resource_hook_table.cpp`. Each of the five `append_*` public methods now delegates to it in one line.

3. [x] **code-simplifier** | `src/resource_hook_table.cpp:135` | code-structure
   The five append_* methods (append_before_handler, append_handler_exception, append_after_handler, append_response_sent, append_request_completed) share an identical body structure: fetch-add the next slot id, take the unique_lock, push_back the entry, store true on the any_hooks_ gate, return the id. The only variation per method is the target vector and the hook_phase enum value passed to the any_hooks_ index.
   *Recommendation:* A single private template helper `template<hook_phase P, typename Sig> uint64_t append_to(std::vector<entry<Sig>>& vec, std::function<Sig> fn)` could eliminate all five duplicate bodies. Each public overload would become a one-liner delegating to that helper. This is a straightforward mechanical extraction with no behavioral difference.
   **Action:** FIXED — same fix as #2 (duplicate finding). See `append_impl` template in `resource_hook_table.cpp`.

4. [x] **code-simplifier** | `test/integ/hooks_per_route_invalid_phase_throws.cpp:60` | code-structure
   Six nearly identical test cases (request_received_tag_throws, connection_opened_tag_throws, accept_decision_tag_throws, body_chunk_tag_throws, route_resolved_tag_throws, connection_closed_tag_throws) repeat the same pattern: declare a trivial_resource, set `threw = false`, call add_hook inside a try/catch for std::invalid_argument, assert threw == true. Only the phase tag and the overload signature differ. This is 90 lines of duplicated boilerplate.
   *Recommendation:* Extract a local helper `template<typename FnT> bool add_hook_throws(hook_phase p, FnT fn)` that returns true iff add_hook throws std::invalid_argument. Each test then reduces to a single LT_CHECK_EQ call. Alternatively, consolidate all six into one test that iterates over a list of {phase, fn} pairs, which also makes it obvious that all six pre-route phases are covered.
   **Action:** DEFERRED — finding #5 explicitly notes "the project may prefer per-case explicitness for test readability and independent failure reporting, which is a reasonable position." The tests have independent identities in test output and the project consistently uses explicit bodies in tests elsewhere. Deferring as advisory.

5. [x] **code-simplifier** | `test/integ/hooks_per_route_invalid_phase_throws.cpp:60` | needless-repetition
   The six 'X_tag_throws' tests (lines 60-150) all follow an identical pattern: declare trivial_resource r, declare bool threw=false, try a mismatched add_hook call, catch std::invalid_argument setting threw=true, LT_CHECK_EQ(threw, true). Similarly the five 'Y_does_not_throw' tests (lines 174-253) follow the same bool-threw pattern. Eleven near-identical test blocks where only the phase tag and callable signature vary.
   *Recommendation:* The project may prefer per-case explicitness for test readability and independent failure reporting, which is a reasonable position. If refactoring is desired, a parameterized helper function that takes a phase tag and a thunk could reduce the repetition while preserving independent test identities. Since this is test code and the project has consistently chosen explicit bodies elsewhere, this is advisory only.
   **Action:** DEFERRED — advisory per the recommendation itself. Project prefers per-case explicitness.

## Minor

6. [x] **architecture-alignment-checker** | `src/detail/webserver_finalize.cpp:73` | interface-contract
   The per_route_table helper function checks mr->resource_weak_.expired() before calling lock(). This is a TOCTOU (time-of-check/time-of-use) pattern: expired() can return false and then lock() can return nullptr due to a concurrent destroy between the two calls. The code then checks if res_out is non-null, which is the safe check. However the expired() check is redundant and slightly misleading — it implies a two-step check that has a race window. The correct pattern is to call lock() directly and check the resulting shared_ptr for null, which is both safe and concise. The actual bug is absent (the res_out null check is definitive), but the expired() call adds code noise that implies an unnecessary optimization.
   *Recommendation:* Simplify to: `res_out = mr->resource_weak_.lock(); if (!res_out) return nullptr; return res_out->hook_table_raw_();` — removing the redundant expired() check. This is a code quality issue with no behavioral impact.
   **Action:** FIXED — removed the redundant `expired()` pre-check from `per_route_table()`. Now uses direct `lock()` + null check. Also addressed the same pattern in `fire_request_completed_gated` (findings #35, #36, #47).

7. [x] **architecture-alignment-checker** | `src/http_resource.cpp:79` | pattern-violation
   The ensure_table function uses std::atomic_load_explicit / std::atomic_compare_exchange_strong_explicit on a std::shared_ptr<detail::resource_hook_table> slot. While this is correct for C++17 and earlier (std::atomic<shared_ptr<T>> was added in C++20), the deprecated std::atomic_load/atomic_compare_exchange free functions on shared_ptr are marked deprecated in C++20. The project targets C++17+ but if targeting C++20 consumers, the std::atomic<std::shared_ptr<T>> class should be used instead. This is a minor forward-compatibility concern, not a current correctness issue.
   *Recommendation:* Track the migration to std::atomic<std::shared_ptr<resource_hook_table>> as a C++20 follow-up. Document it as a known C++20 deprecation in the code comment at line 79 or in specs/architecture/12-open-questions.md.
   **Action:** DEFERRED — C++20 follow-up. The implementation is correct for C++17 and already commented. Forward-compat note already in specs/architecture/12-open-questions.md scope.

8. [x] **architecture-alignment-checker** | `src/httpserver/detail/resource_hook_table.hpp:130` | interface-contract
   The resource_hook_table class uses a single shared_mutex covering all five hook vectors. The architecture doc (§4.10) states 'One shared_mutex covering all five vectors -- writes are rare'. This matches the implementation. However the class documentation says 'only the five permitted phase positions are ever flipped to true' for the any_hooks_ gate, but the gate array is sized to hook_phase::count_ (11 slots). This means six slots (for the pre-route phases) are always false but consume memory. The TASK-051 action item explicitly documents this: 'most slots unused per the constraint above, but the array shape simplifies indexed access'. This is the stated design. Minor alignment gap: the class comment in resource_hook_table.hpp says the storage is an exact match to the spec ('same shape'), but the spec says std::array<std::vector<...>, count_> while the impl has five named vectors. This is a documentation inconsistency (the arrays vs named vectors tradeoff). The named vector approach is actually more efficient for the compile/template path but is not the documented layout.
   *Recommendation:* Update the class comment to note that the implementation uses five named vectors rather than a single std::array<std::vector<...>, count_> for type safety at the append_*/fire_* call sites. The doc comment currently says 'same shape' which slightly misleads.
   **Action:** FIXED — updated the private member comments in `resource_hook_table.hpp` to clarify: the named vectors provide type safety (each has a distinct element type), the `any_hooks_` array has six unused slots (pre-route phases), and the design is documented in DR-012 "Per-route storage".

9. [x] **architecture-alignment-checker** | `src/httpserver/http_resource.hpp:283` | adr-violation
   DR-012 and TASK-051 action item specify per-resource storage as 'std::array<std::vector<std::function<...>>, hook_phase::count_> + std::array<std::atomic<bool>, count_> any_hooks_ + std::shared_mutex' embedded directly on http_resource. The implementation uses a PIMPL pattern instead: a lazy shared_ptr<detail::resource_hook_table> that is nullptr until first add_hook() call. The PIMPL has a parenthetical mention in the task spec ('a single std::vector<std::pair<hook_phase, function>> is the fallback if sizeof(http_resource) is tight') but the chosen approach is a full lazy PIMPL with shared_ptr, not the flat array shape the spec describes as the primary design. This is a conscious deviation documented in the header comments and sizeof benchmark, and the memory cost difference is measured (vptr + shared_ptr + method_set vs vptr + std::array of 11 vectors + std::array of 11 atomics + std::shared_mutex + method_set). The lazy shared_ptr avoids paying the fixed-array cost on resources that never register hooks, which is consistent with the 'zero-cost when unused' principle (DR-012). However the deviation from the spec's primary design is not documented in an ADR update.
   *Recommendation:* Document the PIMPL shape as the chosen implementation of the 'fallback if sizeof is tight' parenthetical from the task spec, either in a brief TASK-051 note in specs/architecture/04-components/http-resource.md §4.4 or in DR-012 consequences. The choice is sound architecturally; it just needs traceability.
   **Action:** FIXED — added "Per-route storage (TASK-051)" paragraph to DR-012.md consequences documenting the lazy PIMPL design choice, and added a per-hook-bus note to `specs/architecture/04-components/http-resource.md` §4.4.

10. [x] **architecture-alignment-checker** | `src/resource_hook_table.cpp:57` | pattern-violation
    The fire_short_circuit_impl template in resource_hook_table.cpp allocates a local Vec snapshot (not thread_local) for each invocation, unlike the server-wide fire_short_circuit_hooks_for_phase template in hook_handle.cpp which uses a thread_local snapshot buffer for zero-heap-allocation on the warm path (per TASK-048 perf comment). The per-route firing helpers therefore allocate on every invocation even on the hot path. This is a minor divergence from the established perf pattern for per-resource hook chains that have hooks registered.
    *Recommendation:* Consider using thread_local snapshot buffers in resource_hook_table's fire_short_circuit_impl and fire_void_impl helpers, mirroring the TASK-048 perf optimization in the server-wide path. This is especially relevant for high-request-rate services with per-route hooks on frequently-hit endpoints.
    **Action:** DEFERRED — performance optimization. The `thread_local` snapshot buffer optimization from TASK-048 can be applied in a follow-up. The current approach is correct; the allocation is bounded by the number of hooks registered on the resource.

11. [x] **code-quality-reviewer** | `src/detail/webserver_dispatch.cpp:1` | readability
    The dispatch path's per-route hook firing is interspersed with server-wide firing in the same function body, making the sequencing harder to audit at a glance. Carried from iter 1.
    *Recommendation:* Extract a fire_per_route_hooks helper that consolidates the five per-route firing calls, improving locality of the sequencing logic.
    **Action:** DEFERRED — the per-route firing is already carved into gated helpers (fire_before_handler_gated, fire_after_handler_gated, etc.) split across webserver_dispatch.cpp and webserver_finalize.cpp. Further consolidation would require additional carve-outs; deferred.

12. [x] **code-quality-reviewer** | `src/detail/webserver_dispatch.cpp:384` | code-readability
    handle_dispatch_exception now has a TASK-051 block that reads mr->resource_weak_.lock() followed by res->hook_table_raw_() inside the same scope. This lock + raw-pointer pattern is correct but could leave rtable pointing at freed memory if res is the only remaining reference to the resource and goes out of scope before rtable is used. The code is safe here because res lives to the end of the if block, but the pattern requires careful reading.
    *Recommendation:* Add a brief comment like '// res keeps the resource alive while rtable is in use' to clarify the lifetime relationship, or restructure so rtable is only used within a scope where res is guaranteed alive (which it already is, but the comment would make it explicit).
    **Action:** FIXED — added lifetime comment "res keeps the resource alive while rtable is in use (the shared_ptr must not go out of scope before the rtable firing loop finishes)" in `handle_dispatch_exception`.

13. [x] **code-quality-reviewer** | `src/detail/webserver_finalize.cpp:236` | code-readability
    fire_request_completed_gated duplicates the expired-check + lock pattern for fetching the per-route resource (lines 238-243) instead of calling the per_route_table() helper that was defined for exactly this purpose at line 73. fire_after_handler_gated (line 147) and fire_response_sent_gated (line 189) both use the helper consistently.
    *Recommendation:* Replace the inline expanded pattern in fire_request_completed_gated with a call to per_route_table(mr, res), matching the style of fire_after_handler_gated and fire_response_sent_gated. This removes the inconsistency and keeps the 'expired() then lock()' idiom in one place.
    **Action:** FIXED — replaced the inline `expired()/lock()` pattern in `fire_request_completed_gated` with a call to `per_route_table(mr, res)`. Also added the "res keeps the resource alive" comment.

14. [x] **code-quality-reviewer** | `src/detail/webserver_finalize.cpp:73` | code-readability
    per_route_table() helper in the anonymous namespace accepts modded_request* and a shared_ptr<http_resource>& out-parameter, returning a raw resource_hook_table*. The out-parameter is necessary to keep the resource alive during firing, but the function name 'per_route_table' does not hint at the lifetime-extension side-effect.
    *Recommendation:* Consider renaming to 'lock_per_route_table' or adding a brief comment at the call sites explaining that 'res_out' is intentionally kept alive to prevent the table pointer from dangling mid-firing. The existing code comment in modded_request.hpp covers this conceptually but is not visible at the call site.
    **Action:** FIXED — added documentation comment to `per_route_table()` helper explaining that it holds `res_out` alive as the lifetime anchor. Kept the name as-is (consistent with existing call sites).

15. [x] **code-quality-reviewer** | `src/detail/webserver_finalize.cpp:90` | code-readability
    fire_before_handler_gated has a local variable named 'per_route_table' (line 97) that shadows the anonymous-namespace helper function of the same name declared at line 73. The compiler resolves the two differently (the local is a raw pointer; the function is a call), but the shadowing makes the function harder to read.
    *Recommendation:* Rename the local pointer variable to 'per_route_hook_tbl' or 'rtable' (the name already used in fire_after_handler_gated at line 148) to eliminate the shadowing and be consistent with the adjacent gated-fire helpers.
    **Action:** FIXED — renamed local variable `per_route_table` to `rtable` in `fire_before_handler_gated`, consistent with `fire_after_handler_gated` and `fire_response_sent_gated`.

16. [x] **code-quality-reviewer** | `src/http_resource.cpp:45` | code-readability
    throw_wrong_phase takes both 'requested' and 'expected' phase parameters but the error message only makes sense when the mismatch is overload-level (the expected arg is the per-overload phase). The function is called only from check_phase so the 'expected' parameter is always the per-overload constant, making the error message potentially confusing (it describes the expected phase as if the user might have intended it, when really the issue is a phase/overload mismatch).
    *Recommendation:* The error message is already comprehensive and well-structured; this is a purely cosmetic concern. Alternatively, rename 'expected' to 'overload_phase' to make the parameter semantics clearer.
    **Action:** DEFERRED — the error message is comprehensive and functional. Renaming is advisory cosmetic.

17. [x] **code-quality-reviewer** | `src/http_resource.cpp:47` | readability
    throw_wrong_phase constructs a long error string via repeated std::string concatenation. Carried from iter 1 — no change this iteration.
    *Recommendation:* Use a single std::string with += or a stringstream for clarity; functionally equivalent but slightly cleaner.
    **Action:** DEFERRED — carried from iter 1, no change. The concatenation is in cold (error) path; not a hot-path concern.

18. [x] **code-quality-reviewer** | `src/http_resource.cpp:61` | code-readability
    check_phase(requested, expected) throws when requested != expected, but the single-argument validation for 'empty callable' (check_fn) is named differently and takes a bool instead of the callable itself. The asymmetry in the two helper signatures (one takes both values, the other takes a pre-evaluated bool) makes the pair harder to scan at a glance.
    *Recommendation:* Consider naming the callable check 'check_not_empty' and passing the std::function reference directly (letting the helper evaluate !fn internally), which would make both helpers symmetric: check_phase(phase, expected_phase) / check_not_empty(fn).
    **Action:** DEFERRED — advisory. Asymmetry is minor and both helpers are local to the anonymous namespace.

19. [x] **code-quality-reviewer** | `src/http_resource.cpp:61` | readability
    check_phase delegates to throw_wrong_phase but the two-argument wrapper adds a level of indirection that is marginally harder to follow than a single-function approach. Carried forward from iter 1 — no change this iteration.
    *Recommendation:* Inline the body of check_phase into throw_wrong_phase by accepting a bool guard; minor improvement only.
    **Action:** DEFERRED — carried from iter 1, advisory only.

20. [x] **code-quality-reviewer** | `src/http_resource.cpp:79` | elegance
    ensure_table uses std::atomic_load_explicit / std::atomic_compare_exchange_strong_explicit on a shared_ptr, which is correct but verbose compared to std::atomic<std::shared_ptr<T>> (C++20). Carried from iter 1.
    *Recommendation:* If C++20 is ever adopted as the minimum standard, migrate to std::atomic<std::shared_ptr<detail::resource_hook_table>> for clarity.
    **Action:** DEFERRED — C++20 follow-up (same as finding #7).

21. [x] **code-quality-reviewer** | `src/httpserver/detail/modded_request.hpp:1` | readability
    modded_request carries a weak_ptr<http_resource> for the request_completed per-route path; the member is not documented at the declaration site. Carried from iter 1.
    *Recommendation:* Add a brief comment explaining the weak_ptr lifetime contract (null when no hook fired, expires if resource is unregistered before completion).
    **Action:** ALREADY ADDRESSED — the `resource_weak_` member at lines 108-119 of `modded_request.hpp` already has an extensive TASK-051 comment explaining the lifetime contract, population site, and skip behavior.

22. [x] **code-quality-reviewer** | `src/httpserver/detail/resource_hook_table.hpp:1` | elegance
    resource_hook_table exposes five separate append_* methods — one per phase — which is repetitive and will require a sixth if a new per-route phase is ever added. Carried from iter 1.
    *Recommendation:* A templated or phase-indexed append could reduce duplication; acceptable as-is given the fixed five-phase contract.
    **Action:** FIXED — the internal `append_impl<P>` template (added for findings #2/#3) eliminates the duplicate bodies. The five public overloads remain (by design) for the typed API surface.

23. [x] **code-quality-reviewer** | `src/httpserver/detail/resource_hook_table.hpp:114` | code-elegance
    The log_fn typedef (std::function<void(std::string_view)>) is repeated in resource_hook_table.hpp. The server-wide equivalent uses std::function directly at all call sites with no typedef. The inconsistency is minor but creates two parallel conventions within the same codebase.
    *Recommendation:* Either adopt the log_fn typedef in both places, or remove it from resource_hook_table and pass the lambda inline as the server-wide path does. Consistency with the existing server-wide style would favor removing the typedef.
    **Action:** DEFERRED — the `log_fn` typedef is convenient for the five `fire_*` method declarations; removing it would make the signatures harder to read. Advisory inconsistency.

24. [x] **code-quality-reviewer** | `src/httpserver/http_resource.hpp:258` | elegance
    Copy/move special members are explicitly defaulted with a comment saying they are trivial, but hook_table_ (a shared_ptr) is not trivially copyable — copying a resource shallow-copies the hook table pointer, which may surprise callers. Carried from iter 1.
    *Recommendation:* Document explicitly (in the header comment or a brief inline note) that copy shares the hook table rather than cloning it, so callers are not surprised when hooks registered on the original fire for the copy.
    **Action:** FIXED — updated the copy/move special members comment in `http_resource.hpp` to document that copying shallow-copies the hook table (shared_ptr semantics).

25. [x] **code-quality-reviewer** | `src/httpserver/http_resource.hpp:283` | readability
    The `mutable` qualifier on hook_table_ is necessary for const-path dispatch but the comment is long. No change this iteration — flagged as a documentation verbosity note from iter 1.
    *Recommendation:* Trim the inline comment to one line; the full rationale belongs in the architecture doc rather than the header.
    **Action:** DEFERRED — the comment is detailed by design (explains why `mutable` is needed for the const dispatch path); trimming it risks losing context for future maintainers.

26. [x] **code-quality-reviewer** | `src/httpserver/http_resource.hpp:285` | code-readability
    The HTTPSERVER_COMPILATION-gated hook_table_raw_() method has a trailing underscore in its name (hook_table_raw_()), which is the project's convention for internal/private members but is unusual for a method name. The underscore looks like an accidental typo or leftover from a renaming.
    *Recommendation:* Consider renaming to 'hook_table_raw()' (without trailing underscore) since the HTTPSERVER_COMPILATION guard already restricts access, and the trailing underscore on method names is not consistent with other internal accessor naming in the codebase.
    **Action:** DEFERRED — the trailing underscore is used consistently for internal-only methods throughout the codebase (e.g., `make_hook_handle_`). Renaming across all call sites is disproportionate for a minor naming convention concern.

27. [x] **code-quality-reviewer** | `src/httpserver/http_resource.hpp:285` | readability
    The HTTPSERVER_COMPILATION guard around hook_table_raw_() makes the accessor invisible to user translation units but is an unusual pattern not explained at the declaration site. Carried from iter 1.
    *Recommendation:* Add a one-line comment at the guard stating this is an internal-only accessor intentionally hidden from public consumers.
    **Action:** FIXED — added explanatory comment above the `#if defined(HTTPSERVER_COMPILATION)` guard in `http_resource.hpp`.

28. [x] **code-quality-reviewer** | `src/resource_hook_table.cpp:135` | code-elegance
    The five append_* methods are structurally identical: fetch_add slot id, unique_lock, push_back, store any_hooks_ true. This is a significant repetition that a single private template helper could collapse to one body. The server-side hook_handle.cpp already uses fire_hooks_for_phase and fire_short_circuit_hooks_for_phase templates for exactly the same reason.
    *Recommendation:* Consider a private template: template<typename Sig, hook_phase Phase> uint64_t append_impl(std::vector<entry<Sig>>& vec, std::function<Sig> fn). Each public append_* would become a one-liner delegation. This is not blocking but aligns with the DRY pattern already established in the fire_* helpers in the same file.
    **Action:** FIXED — same fix as findings #2/#3 (triplicate finding). See `append_impl` template.

29. [x] **code-quality-reviewer** | `src/resource_hook_table.cpp:202` | code-elegance
    remove_slot's per-case body repeats the erase_if_found + reset_if_empty pair for all five phases (10 lines each). The server-wide remove path in hook_handle.cpp uses the same erase+gate pattern. A two-step helper (erase from the right vector, then reset_if_empty) would reduce the switch body significantly.
    *Recommendation:* The lambda helpers defined inside remove_slot (erase_if_found, reset_if_empty) are a good start; the switch cases could be collapsed to single-line calls, e.g.: case hook_phase::before_handler: erase_if_found(hooks_before_handler_); reset_if_empty(hooks_before_handler_); break; This is already done effectively -- the only remaining repetition is the two-call pattern per case, which is minimal and fine as-is.
    **Action:** ALREADY ADDRESSED — the recommendation itself says "This is already done effectively -- the only remaining repetition is the two-call pattern per case, which is minimal and fine as-is." No change needed.

30. [x] **code-quality-reviewer** | `test/integ/hooks_per_route_concurrent_registration.cpp:1` | test-coverage
    The concurrent-registration test exercises the TSan scenario but does not assert ordering determinism — only absence of crash. Carried from iter 1.
    *Recommendation:* Consider asserting that all registered hooks fired exactly once under concurrent load, not just that the process did not crash.
    **Action:** DEFERRED — TSan crash-freedom is the primary goal; ordering determinism under concurrency is a much harder property to assert. Deferred to a potential follow-up test.

31. [x] **code-quality-reviewer** | `test/integ/hooks_per_route_order.cpp:1` | test-coverage
    The ordering test verifies A→B for response_sent but does not include a symmetric test for before_handler ordering (server-wide before per-route). Carried from iter 1.
    *Recommendation:* Add a before_handler ordering assertion to the same or an adjacent test file to complete the ordering contract coverage.
    **Action:** DEFERRED — the before_handler ordering is exercised indirectly by `hooks_per_route_early_413_per_endpoint` (which relies on before_handler firing correctly). An explicit ordering test is a low-priority gap.

32. [x] **code-quality-reviewer** | `test/integ/hooks_per_route_order.cpp:77` | test-coverage
    The hooks_per_route_order test covers response_sent order (A→B). There is no integ test exercising the server-wide → per-route ordering for before_handler or after_handler, even though those phases have the same chaining contract and different short-circuit semantics.
    *Recommendation:* Consider adding a test case for before_handler order (server-wide fires and passes, then per-route fires) to confirm that the ordering contract also holds for the short-circuit-capable phases. The existing hooks_per_route_early_413_per_endpoint indirectly exercises per-route before_handler short-circuit but does not assert the server-wide → per-route sequence.
    **Action:** DEFERRED — low-priority test coverage gap. The indirection through `hooks_per_route_early_413_per_endpoint` provides coverage of the before_handler chain.

33. [x] **code-quality-reviewer** | `test/integ/hooks_per_route_order.cpp:95` | test-coverage
    The test registers both hooks BEFORE calling ws.start(). It does not exercise registering a per-route hook while the server is running (mid-flight registration), which is described as thread-safe by the shared_mutex design. The TSan test (hooks_per_route_concurrent_registration) exercises mid-flight registration but from inside a handler; external mid-flight registration from a separate thread is not covered.
    *Recommendation:* This is a low-priority gap since the TSan test covers the lock discipline. A future test that registers a per-route hook from a concurrent non-handler thread would complete coverage of the 'registration on a live server' path.
    **Action:** DEFERRED — low-priority gap explicitly noted in the recommendation.

34. [x] **code-quality-reviewer** | `test/unit/hook_api_shape_test.cpp:1` | test-coverage
    Unit shape tests verify the API compiles and throws correctly but do not verify that a second add_hook on the same resource after the first handle is removed correctly re-registers (slot reuse). Carried from iter 1.
    *Recommendation:* Add a unit test: register hook, destroy handle, register again, verify the new hook fires.
    **Action:** DEFERRED — low-priority coverage gap. Slot reuse is implicitly exercised by the any_hooks_flips_on_first_and_last test.

35. [x] **code-simplifier** | `src/detail/webserver_finalize.cpp:236` | code-structure
    In fire_request_completed_gated the weak_ptr check is done with an explicit expired() call followed by lock(): `if (!mr->resource_weak_.expired()) { res = mr->resource_weak_.lock(); if (res) { ... } }`. The inner `if (res)` guard makes the outer `expired()` redundant — lock() already returns null when expired, and the time-of-check-to-time-of-use pattern means a concurrent destruction between the expired() check and lock() would be caught by `if (res)` anyway.
    *Recommendation:* Remove the outer `expired()` guard and keep only `res = mr->resource_weak_.lock(); if (res) { ... }`. This is the same two-liner used in per_route_table() (webserver_finalize.cpp line 75-78) and is consistent with the pattern in hook_handle.cpp's remove_per_route.
    **Action:** FIXED — replaced with `per_route_table(mr, res)` call (which itself now uses the clean `lock()` only pattern). See findings #6, #13.

36. [x] **code-simplifier** | `src/detail/webserver_finalize.cpp:236` | code-structure
    In fire_request_completed_gated (lines 236-271), the per-route table lookup is implemented inline with a nested null check (expired check then lock then hook_table_raw_() call), while the equivalent pattern in fire_after_handler_gated and fire_response_sent_gated delegates to the `per_route_table` helper. The three gated fire functions are inconsistent in how they resolve the per-route table.
    *Recommendation:* Use the `per_route_table` helper consistently in fire_request_completed_gated, replacing the inline expired/lock/hook_table_raw_() sequence with the same two-line pattern used in the other gated helpers. This eliminates a subtle inconsistency and makes the three functions structurally parallel.
    **Action:** FIXED — same fix as #13/#35. `fire_request_completed_gated` now uses `per_route_table(mr, res)`.

37. [x] **code-simplifier** | `src/detail/webserver_finalize.cpp:73` | naming
    The local helper function `per_route_table` at line 73 shadows the local variable `rtable` naming pattern used by callers. The function name `per_route_table` closely matches the local variable `auto* rtable = per_route_table(mr, res)` in fire_after_handler_gated, creating mild visual ambiguity between the function and what it returns.
    *Recommendation:* Rename the helper to `resolve_per_route_table` or `get_per_route_table` to distinguish the action (resolve/retrieve) from the result (table pointer). This is a minor clarity improvement with no behavioral impact.
    **Action:** DEFERRED — the name `per_route_table` is consistent with the project's naming style for helper functions. Renaming is advisory cosmetic.

38. [x] **code-simplifier** | `src/detail/webserver_finalize.cpp:97` | naming
    In fire_before_handler_gated there is a local variable named `per_route_table` (line 97) that shadows the free function also named `per_route_table` defined in the anonymous namespace at line 73 of the same file. The local variable and the helper function are both present in the same TU; while it compiles cleanly, the shadowing is confusing when reading the function.
    *Recommendation:* Rename the local variable to `route_tbl` or `rtable` (consistent with the naming used in fire_after_handler_gated, fire_response_sent_gated, and fire_request_completed_gated in the same file).
    **Action:** FIXED — same fix as #15. Renamed to `rtable`.

39. [x] **code-simplifier** | `src/http_resource.cpp:45` | code-structure
    throw_wrong_phase takes both `requested` and `expected` as parameters, but the error message it generates references `expected` only to name the constraint. Every call site passes `hook_phase::X` as the `expected` argument where X is the correct phase for that overload. This means the error message always says 'on this overload' but could be misleading when the user passes a wrong phase: e.g. passing `handler_exception` to the `before_handler` overload yields a message saying the 'expected' phase is before_handler, which does not help the caller understand they picked the wrong overload.
    *Recommendation:* Minor: consider dropping the `expected` parameter from throw_wrong_phase and replacing its mention in the message with the phrase 'this overload accepts only the phase matching its callable signature'. This makes check_phase simpler too. Alternatively, simply keep the current approach but verify the existing message phrasing is helpful enough for users.
    **Action:** DEFERRED — the error message is already comprehensive (names both the requested phase and the expected phase). Refactoring the signature is advisory.

40. [x] **code-simplifier** | `src/http_resource.cpp:45` | code-structure
    The `throw_wrong_phase` helper at line 45 takes two parameters (requested, expected) but the error message it builds only uses both when explaining the mismatch. However, `check_phase` at line 61 always calls it with the single expected phase for that overload. In practice the `expected` parameter is redundant information because the caller already knows the expected phase from the overload context -- the message could simply name the requested phase and state which phases are valid categorically.
    *Recommendation:* Drop the `expected` parameter from `throw_wrong_phase` and format the message using only `requested`, listing the five valid phases as a fixed string. The current two-parameter form slightly overstates precision (the caller claims `expected` is the only valid phase when in fact all five are valid for that overload's context type). This is advisory; the current behavior is correct.
    **Action:** DEFERRED — advisory. The message is currently correct and functional. Dropping the `expected` parameter would remove helpful context for users.

41. [x] **code-simplifier** | `test/integ/hooks_per_route_resource_destroyed_first.cpp:67` | code-structure
    Three of the four test cases end with `LT_CHECK_EQ(true, true)` as a sentinel for 'no crash'. This assertion always passes regardless of any condition and adds noise rather than expressing intent. The same pattern appears in hooks_per_route_invalid_phase_throws.cpp for the positive-path tests.
    *Recommendation:* Remove the tautological LT_CHECK_EQ(true, true) lines. If the concern is 'reaching this line means no crash', a comment is clearer than a vacuous assertion. The test will still pass (and report one fewer assertion) which is fine for a crash-guard test.
    **Action:** FIXED — removed all four `LT_CHECK_EQ(true, true)` sentinels in `hooks_per_route_resource_destroyed_first.cpp`, replacing each with a descriptive comment "Reaching this point means no crash / no UAF (validated under ASan)."

42. [x] **housekeeper** | `specs/architecture/04-components/http-resource.md:null` | architecture-not-updated
    The http-resource.md component doc describes http_resource's responsibilities and API surface but makes no mention of add_hook(), the per-resource hook table PIMPL, or the hook-related includes added to http_resource.hpp (hook_handle.hpp, hook_phase.hpp, hook_action.hpp, hook_context.hpp). The hooks.md file does document the per-route API but the component doc for http_resource itself is stale relative to the new add_hook surface. This is a minor gap because the hooks.md §4.10 already covers the full picture.
    *Recommendation:* Consider adding a brief note to specs/architecture/04-components/http-resource.md mentioning that http_resource now also hosts a per-resource hook bus (add_hook, hook_table_ PIMPL) as documented in §4.10. Run /groundwork:source-architecture-from-code to capture these changes automatically.
    **Action:** FIXED — added "Per-resource hook bus (TASK-051)" paragraph to `specs/architecture/04-components/http-resource.md` §4.4.

43. [x] **housekeeper** | `specs/architecture/04-components/http-resource.md:null` | architecture-not-updated
    The http-resource.md component doc still lacks a mention of add_hook() and the per-resource hook table PIMPL. This was deliberately deferred as finding #9 from iter1 and is now recorded in specs/unworked_review_issues/2026-05-26_000000_task-051.md (item #25). The deferral is intentional and properly documented.
    *Recommendation:* No action required this iteration — the finding is already persisted to the unworked review issues file. When convenient, add a brief note to specs/architecture/04-components/http-resource.md referencing §4.10 for the per-route hook bus. Run /groundwork:source-architecture-from-code to capture any additional arch gaps.
    **Action:** FIXED — same fix as #42 (duplicate finding). See `specs/architecture/04-components/http-resource.md`.

44. [x] **performance-reviewer** | `src/detail/webserver_finalize.cpp:122` | memory-allocation
    fire_before_handler_gated and fire_after_handler_gated each construct a std::function log lambda ([this](std::string_view m) { log_dispatch_error(...); }) inline at the call site. Each construction heap-allocates a closure object. These lambdas are passed as `const log_fn&` into the per-route table firing helpers, so they are created on every request that has per-route hooks, even when the hook vector is empty after the any_hooks_ gate check.
    *Recommendation:* Hoist the lambda out of the gate branch and construct it once per gated-fire function scope, or pass a pre-constructed `resource_hook_table::log_fn` stored on the impl (single allocation at server startup). Alternatively, replace log_fn with a raw function pointer or a non-owning `std::function_ref` to avoid the allocation entirely.
    **Action:** DEFERRED — micro-optimization. The lambda is only constructed when `per_route_gate` is true (i.e., when the resource has per-route hooks AND the any_hooks_ gate is set). The allocation is bounded and rare.

45. [x] **performance-reviewer** | `src/detail/webserver_finalize.cpp:75` | race-condition
    per_route_table() helper calls mr->resource_weak_.expired() then mr->resource_weak_.lock() as two separate operations. Between the two calls the weak_ptr can become expired (resource destroyed by another thread), producing a redundant but harmless null-return. The pattern is TOCTOU: expired() is always advisory. Same pattern repeated at lines 238-239 in fire_request_completed_gated.
    *Recommendation:* Remove the .expired() pre-check and rely solely on .lock(): `res_out = mr->resource_weak_.lock(); if (!res_out) return nullptr;`. A single atomic lock() is cheaper than expired()+lock() and eliminates the race window.
    **Action:** FIXED — same fix as #6. `per_route_table()` now uses `lock()` only; `fire_request_completed_gated` fixed via the `per_route_table()` helper call (finding #13/#35/#36).

46. [x] **performance-reviewer** | `src/resource_hook_table.cpp:64` | memory-allocation
    fire_short_circuit_impl and fire_void_impl both allocate a fresh Vec snapshot on every call (`Vec snapshot;`), whereas the server-wide equivalents in hook_handle.cpp use a `thread_local` snapshot buffer that is `.clear()`ed and reused. Under high traffic on resources with per-route hooks, the two helpers will allocate and free a std::vector on every fired phase.
    *Recommendation:* Adopt the same thread_local snapshot pattern used by fire_hooks_for_phase / fire_short_circuit_hooks_for_phase (hook_handle.cpp lines 262-265, 330-333): declare `thread_local Vec snapshot;` and call `snapshot.clear();` before copying into it. The capacity is then retained across calls on the same thread, eliminating repeated heap round-trips on the hot path.
    **Action:** DEFERRED — same as finding #10. Performance optimization deferred to follow-up.

47. [x] **security-reviewer** | `src/detail/webserver_finalize.cpp:238` | concurrency
    fire_request_completed_gated repeats the `expired()` + `lock()` anti-pattern: `if (!mr->resource_weak_.expired()) { res = mr->resource_weak_.lock(); ... }`. Same reasoning as finding #2: replace with a direct lock() + null check.
    *Recommendation:* Replace with `res = mr->resource_weak_.lock(); if (res) { rtable = res->hook_table_raw_(); }` — collapses the double-check into a single atomic operation and matches the safer idiom used in other parts of the code.
    **Action:** FIXED — same fix as #6/#35/#36. See `fire_request_completed_gated` and `per_route_table()`.

48. [x] **security-reviewer** | `src/detail/webserver_finalize.cpp:75` | concurrency
    per_route_table() checks `mr->resource_weak_.expired()` and then calls `mr->resource_weak_.lock()` as two separate operations. Although `modded_request` is single-threaded per request (each MHD callback thread owns its mr), the pattern of `expired()` + `lock()` is a well-known TOCTOU anti-pattern with weak_ptr and should be replaced by a direct `lock()` call with null check. Even in the current single-threaded-per-mr deployment, the idiom teaches the wrong pattern to future readers who may introduce resource_weak_ sharing across threads.
    *Recommendation:* Replace the `if (mr->resource_weak_.expired()) return nullptr; res_out = mr->resource_weak_.lock();` sequence with `res_out = mr->resource_weak_.lock(); if (!res_out) return nullptr;`. This is the correct idiom (lock() is atomic; expired() + lock() is not).
    **Action:** FIXED — same fix as #6. `per_route_table()` now uses `lock()` only.

49. [x] **security-reviewer** | `src/httpserver/detail/resource_hook_table.hpp:131` | insecure-design
    resource_hook_table uses a single shared_mutex covering all five hook vectors. While this is explicitly documented as acceptable ('writes are rare'), the design means that a long-running before_handler hook iteration (holding the shared_lock during snapshot copy) can delay an urgent remove() call (which needs the unique_lock). This is not a security vulnerability in isolation, but in a DoS scenario where an attacker causes many concurrent requests to hold shared_locks simultaneously (even transiently, during snapshot copy), a legitimate hook deregistration (e.g., from a security-update code path) could be delayed indefinitely. The snapshot copy itself drops the lock, limiting window to snapshot duration, which mitigates most cases.
    *Recommendation:* Document the known contention scenario in the class header. If hook deregistration latency becomes a concern, consider per-phase mutexes (five independent shared_mutexes) to limit contention scope. Current single-mutex design is acceptable for the expected workload (few hooks, small vectors).
    **Action:** DEFERRED — the single-mutex design is documented and acceptable for the expected workload. Per-phase mutexes are a potential follow-up if contention becomes an issue.

50. [x] **security-reviewer** | `src/resource_hook_table.cpp:106` | concurrency
    resource_hook_table::any_hooks() uses memory_order_relaxed for the gate load, which is documented as intentional for the hot-path zero-cost check. However, when the caller subsequently calls fire_before_handler(), the per-route table pointer obtained from hook_table_raw_() (also a relaxed path: it reads hook_table_.get() after the shared_ptr was stored with acq_rel in ensure_table) provides the acquire side. The chain is: ensure_table() stores with acq_rel -> dispatch reads hook_table_raw_() via .get() on a shared_ptr loaded with acquire in fire_before_handler_gated. This is correct. The concern is that fire_before_handler_gated (webserver_finalize.cpp:97) reads per_route_table directly from `hrm->hook_table_raw_()` without re-confirming the gate — which is fine because the shared_ptr was acquired from resolve_resource_for_request under the route table lock. The pattern is correct but fragile: a future refactor that moves the hrm acquisition earlier could break the acquire-chain. A comment explaining the acquire chain would aid future auditors.
    *Recommendation:* Add a comment at fire_before_handler_gated::97 explaining that hook_table_raw_() is safe because hrm was acquired from resolve_resource_for_request under the route_table shared_lock (which provides the acquire barrier for any_hooks_ and the hook table pointer itself).
    **Action:** FIXED — added memory-order comment at the `rtable = hrm->hook_table_raw_()` call site in `fire_before_handler_gated`, explaining that the acquire barrier comes from the route_table_mutex_ shared_lock in `resolve_resource_for_request`.

51. [x] **security-reviewer** | `src/resource_hook_table.cpp:137` | concurrency
    next_slot_id_ is incremented with memory_order_relaxed, and any_hooks_[phase] is stored with memory_order_release inside the unique_lock. Because the relaxed fetch_add is not sequenced before the unique_lock acquisition in the C++ memory model (the unique_lock's own lock() call provides an acquire-barrier, but the id fetch_add was already issued before that), the slot_id is computed outside the critical section. In practice there is no observable bug (the unique_lock provides the fence before the push_back that uses the id), but the intent is clearer and the pattern is safer if the fetch_add is either moved inside the critical section or issued with memory_order_acq_rel to make the ordering self-documenting. The current shape could confuse future maintainers into thinking relaxed-RMW-before-lock is always safe.
    *Recommendation:* Move `next_slot_id_.fetch_add(1, std::memory_order_relaxed)` inside the unique_lock scope, or change the order to acquire: `next_slot_id_.fetch_add(1, std::memory_order_acq_rel)`. Either change documents the intent and eliminates the latent reader confusion.
    **Action:** DEFERRED — the ordering is safe (the unique_lock provides sequencing before push_back). The relaxed fetch_add outside the lock avoids lock contention for the ID increment. Changing to acq_rel or moving inside the lock is advisory; would slightly reduce throughput under contention.

52. [x] **spec-alignment-checker** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-051/src/detail/webserver_finalize.cpp:193` | specification-gap
    In fire_response_sent_gated, the guard condition is '!server_gate && !route_gate && !log_access_alias_' -- meaning if only the log_access alias is set but no per-route gate, the elapsed computation is skipped (server_gate || route_gate is false). This is intentional and documented in the comment ('Skip the steady_clock::now() call when only the alias slot is going to fire'), but the elapsed field in response_sent_ctx would be zero nanoseconds when only the alias fires. This means the alias-using caller sees elapsed=0, which is a documented semantic. The spec does not require elapsed to be correct for alias callers -- log_access is a v1 alias that receives ctx but may not use timing -- so this is a minor spec interpretation note.
    *Recommendation:* The comment already explains this. Consider adding a note that log_access alias callers should use ctx.duration_hint_ns==0 as a signal that timing was not measured, or document it in the ctx field comment.
    **Action:** DEFERRED — the code comment already explains the elapsed=0 semantic for alias-only callers. Further documentation would be in the ctx field docs (hook_context.hpp), which were already updated by TASK-052.

53. [x] **spec-alignment-checker** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-051/src/http_resource.cpp:61` | specification-gap
    The check_phase helper throws when requested != expected, but its error message always names the specific expected phase rather than the broader constraint. For example, passing hook_phase::body_chunk to the before_handler overload produces a message that says '...expected before_handler...' -- which is technically accurate per the overload check_phase enforces, but the message body then lists all five permitted phases anyway. Minor UX ambiguity: the 'expected' field in the message is overload-specific (before_handler) while the recommendation in the message says 'one of: before_handler, handler_exception, after_handler, response_sent, request_completed'. Spec says the message should 'name the rejected phase and the constraint'; both are present, so this is a minor phrasing consistency note, not a violation.
    *Recommendation:* Consider restructuring the error message to clearly distinguish 'you passed phase X' from 'this overload requires phase Y (one of the five permitted phases)'. Current message has both but in a slightly awkward order.
    **Action:** DEFERRED — the error message is functional and spec-compliant (names the rejected phase and the constraint). Restructuring is advisory cosmetic.

54. [x] **spec-alignment-checker** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-051/test/integ/hooks_per_route_concurrent_registration.cpp:107` | acceptance-criteria
    The TSan concurrency test (hooks_per_route_concurrent_registration) exercises concurrent add_hook from inside a handler on Q while requests to R fire the per-route chain. However, the test does not exercise the handle.remove() path concurrently (handles are accumulated in a vector but never removed during the stress window). The lock order TSan needs to observe -- route_table_mutex_ -> resource hook_table_mutex_ -> server-wide hook_table_mutex_ -- is only fully observable if the remove/add paths contend. The test covers the write->read race adequately but the write->remove concurrent path is not exercised. The acceptance criterion says 'TSan-clean lock order is exercised' which is met; the remove concurrency path is absent but not listed as required.
    *Recommendation:* Consider adding a second worker thread that removes the oldest handle from the shared handles vector while the primary workers are adding. This would give TSan more material on the contention between append and remove_slot paths, but it is not required by the acceptance criterion as written.
    **Action:** DEFERRED — acceptance criterion is met. The enhancement is advisory.

55. [x] **test-quality-reviewer** | `test/integ/hooks_per_route_concurrent_registration.cpp:107` | slow-test
    The concurrent registration test runs for 800ms (`kRunDuration`) with 8 threads. This is intentional for TSan coverage but will add ~1s to every normal (non-TSan) CI run. The task states this test is specifically for TSan; a normal run does not benefit from the long duration.
    *Recommendation:* Consider gating the long duration behind a `#ifdef __SANITIZE_THREAD__` or an environment variable, reducing to e.g. 100ms for standard runs and 800ms under TSan. This halves wall-clock overhead without reducing TSan signal.
    **Action:** DEFERRED — the 800ms duration is intentional for TSan signal quality. The CI matrix already has a separate TSan entry; modifying duration gating is a future CI optimization.

56. [x] **test-quality-reviewer** | `test/integ/hooks_per_route_concurrent_registration.cpp:171` | missing-test
    The test only asserts `q_count > 0` and `r_count > 0` to prove the traffic ran. It does not assert that no requests returned an unexpected status code. If the server crashes mid-test, `curl_easy_perform` returns a non-OK code and the counters may still be >0 from earlier iterations, making the crash silent.
    *Recommendation:* Track failure counts per thread and assert `failure_count == 0` after joining, or at least check that the ratio of successful to attempted requests is above a reasonable floor (e.g., >80%).
    **Action:** DEFERRED — the primary assertion is crash-freedom under TSan. If the server crashes MHD terminates the connections; the count would drop to 0. This is a reasonable future enhancement.

57. [x] **test-quality-reviewer** | `test/integ/hooks_per_route_early_413_per_endpoint.cpp:83` | missing-test
    The test verifies three combinations (2KB to small=413, 2KB to large=200, 500B to small=200) but does not cover the boundary case: exactly 1024 bytes to /upload-small should return 200 (boundary at-limit), while 1025 bytes should return 413. Boundary tests are high-value for this kind of policy hook.
    *Recommendation:* Add `post_bytes(PORT, "/upload-small", 1024)` asserting 200 and `post_bytes(PORT, "/upload-small", 1025)` asserting 413 to pin the inclusive/exclusive semantics of the `> 1024` condition.
    **Action:** DEFERRED — boundary testing enhancement. The existing test covers the common cases; boundary cases are a low-priority follow-up.

58. [x] **test-quality-reviewer** | `test/integ/hooks_per_route_early_413_per_endpoint.cpp:93` | excessive-setup
    Both `before_handler` lambdas contain a `ctx.request == nullptr` null-guard (lines 94 and 110) and a `cl.empty()` guard plus try/catch around `stoul`. The null guard is almost certainly never true in a real dispatch (the context is constructed from a live request), and the empty Content-Length guard means the 500-byte POST (which relies on libcurl setting Content-Length) would silently pass even if Content-Length was missing. These guards add setup noise and partially obscure what the test is actually exercising.
    *Recommendation:* If null request and missing Content-Length are not realistic for this test scenario, remove the guards so the test fails loudly on unexpected precondition violations rather than silently returning `hook_action{}`.
    **Action:** DEFERRED — the null-guard and try/catch are defensive coding habits; removing them would make the hook panic-exit on edge cases that should be benign in practice. Advisory cosmetic.

59. [x] **test-quality-reviewer** | `test/integ/hooks_per_route_invalid_phase_throws.cpp:60` | naming-convention
    The test name `request_received_tag_throws` precisely names what is tested, but the cross-mismatch test at line 155 is named `cross_mismatch_within_valid_phases_throws` — while accurate, the task acceptance criterion specifically says 'request_received ... throws std::invalid_argument', and the six pre-route tests cover that case once each. The cross-mismatch test validates implementation behaviour that was not listed in the acceptance criteria and its name could be confused with a missing-phase test.
    *Recommendation:* Rename to `before_handler_overload_rejects_response_sent_phase_tag` to make the scenario (overload-vs-phase mismatch among permitted phases) immediately clear without reading the body.
    **Action:** DEFERRED — naming is advisory. The current name is accurate and the test body is self-explanatory.

60. [x] **test-quality-reviewer** | `test/integ/hooks_per_route_order.cpp:77` | missing-test
    The acceptance criterion requires: 'A request to R fires A then B in that order. A request to another resource fires only A.' The second part (only A fires for /s) is tested, but there is no test verifying that B does NOT fire for /s after a request to /r (i.e. that per-route state does not leak across resources). The current test clears `firing_order` before the /s request but does not assert B's absence at the per-element level — it only asserts size==1. This is correct but could be more explicit.
    *Recommendation:* After the /s request, also assert `firing_order[0] != "B"` or rely on the existing `LT_CHECK_EQ(firing_order[0], std::string("A"))` which does effectively test the absence of B. Actually the test is fine as written; this observation is informational only and can be disregarded.
    **Action:** ALREADY ADDRESSED — the recommendation itself says "Actually the test is fine as written; this observation is informational only and can be disregarded."

61. [x] **test-quality-reviewer** | `test/integ/hooks_per_route_resource_destroyed_first.cpp:67` | aaa-violation
    `LT_CHECK_EQ(true, true)` is a sentinel assertion used across four tests in this file (lines 67, 86, 102, 122). It adds no value — it will pass even if the assertion machinery is broken, and it obscures that the actual contract is 'no crash/UAF reaching this point'. The comment 'reaching here means no crash' explains the intent but the assertion form is misleading.
    *Recommendation:* Remove the sentinel `LT_CHECK_EQ(true, true)` assertions and rely on the comment, or use a dedicated macro like `LT_SUCCEED()` if the framework provides one. The test framework will still report the test as passed if no other assertion fires.
    **Action:** FIXED — removed all four `LT_CHECK_EQ(true, true)` sentinels and replaced with descriptive comments. See finding #41 (duplicate for same file).

62. [x] **test-quality-reviewer** | `test/unit/hook_api_shape_test.cpp:180` | aaa-violation
    `LT_CHECK_EQ(true, true)` sentinel at line 180 (double_remove_is_noop) has the same issue as above — it passes trivially. The test's only meaningful assertion is 'the second remove() does not crash'.
    *Recommendation:* Same as finding #2: remove the sentinel assertion or use an explicit succeed marker.
    **Action:** FIXED — removed `LT_CHECK_EQ(true, true)` from `double_remove_is_noop` in `hook_api_shape_test.cpp`. Also removed the same sentinel from `default_constructed_handle_is_inert`.
