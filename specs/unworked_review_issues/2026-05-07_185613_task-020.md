# Unworked Review Issues

**Run:** 2026-05-07 18:56:13
**Task:** TASK-020
**Total:** 35 (0 critical, 5 major, 30 minor)

## Major

1. [x] **code-quality-reviewer** | `src/httpserver/webserver.hpp:157` | code-elegance
   get_fdset's public signature was changed from fd_set* to void* to avoid including <sys/select.h>. While this achieves the header-hygiene goal, it silently drops all type safety at the call site: a caller can pass any pointer type and the compiler will not complain. An incorrect argument (e.g. a plain int* or a mismatched struct*) produces undefined behaviour at the internal cast with no diagnostic. The comment acknowledges this trade-off but does not offer the alternative of a thin opaque wrapper type or a pair of overloads.
   *Recommendation:* Consider introducing a thin opaque tag type (e.g. `struct fd_set_ptr { void* p; fd_set_ptr(fd_set* q): p(q) {} }`) in the public header that preserves single-implicit-conversion safety without requiring <sys/select.h>. This maintains call-site compatibility while restoring type checking. Alternatively, document explicitly in the API contract that only fd_set* is a valid argument, and add an assert or comment in the implementation that makes the expectation clear.
   *Status:* Resolved — webserver.hpp now forward-declares `struct fd_set;` at file scope (line 58) and `get_fdset` is typed as `bool get_fdset(struct fd_set* read_fd_set, struct fd_set* write_fd_set, struct fd_set* except_fd_set, int* max_fd)` (line 307). Type safety is restored at the call site without including `<sys/select.h>`.

2. [x] **code-quality-reviewer** | `src/httpserver/webserver.hpp:454` | code-elegance
   get_fdset now takes `void*` for fd_set parameters, erasing type safety at the call site. Callers who pass a non-fd_set pointer (e.g., a char* buffer) get no diagnostic. The original fd_set* signature gave the compiler a chance to catch misuse; the void* substitution removes that protection entirely. A forward declaration of `fd_set` (which is a struct on POSIX and can be forward-declared as `struct fd_set;`) or an overloaded/templated approach would preserve hygiene without sacrificing type safety.
   *Recommendation:* Consider forward-declaring `struct fd_set` in the public header (it is a plain struct on all POSIX platforms and on Windows via <winsock2.h>) rather than erasing the type to void*. This preserves the header-hygiene goal because the struct tag alone requires no additional system header while still giving callers a typed pointer.
   *Status:* Resolved — exactly the recommended forward-declaration approach was adopted: `struct fd_set;` at webserver.hpp:58 with typed `get_fdset(struct fd_set*, …)` at line 307.

3. [x] **code-simplifier** | `src/httpserver/http_utils.hpp:62` | code-structure
   `struct sockaddr` is forward-declared in both `src/httpserver/http_utils.hpp` (line 67) and `src/httpserver/webserver.hpp` (line 52). Because `webserver.hpp` includes `http_utils.hpp`, the declaration in `webserver.hpp` is redundant — the compiler already sees it from the transitive include. Redundant forward declarations add noise and can mislead readers into thinking the two headers have independent socket dependencies.
   *Recommendation:* Remove the `struct sockaddr;` and `struct sockaddr_storage;` forward declarations from `src/httpserver/webserver.hpp` (lines 52-53). The declarations in `http_utils.hpp` propagate transitively. If `webserver.hpp` ever stops including `http_utils.hpp`, a follow-up can restore them.
   *Status:* Resolved — webserver.hpp no longer redeclares `struct sockaddr;` / `struct sockaddr_storage;`. The header comment at lines 54-57 explicitly notes they are reached transitively via `http_utils.hpp`. The only `struct sockaddr` mentions in webserver.hpp are typed member fields and parameter types (lines 329, 402-403).

4. [ ] **code-simplifier** | `src/httpserver/webserver.hpp:40` | code-structure
   The block comment introducing `get_fdset` and `add_connection` (lines 40-52) and the per-method Doxygen comments (lines 157-197) both explain the `void*`/`unsigned int` ABI workaround, producing duplicate prose. The top-level block comment is long enough to be a distraction at the class level; the per-method comments immediately below are the natural place for callers to look.
   *Recommendation:* Shorten the top-level block comment to a single sentence (e.g. `// Socket-layer types are forward-declared; see individual method comments for the void*/unsigned-int ABI rationale.`) and keep the full explanation only in the Doxygen blocks for `get_fdset` and `add_connection`.
   *Status:* deferred — the top-level comment at webserver.hpp:49-57 is still ~8 lines and overlaps with the per-method Doxygen blocks; a future cleanup pass should collapse it to one sentence with a "see individual method comments" pointer.

5. [x] **security-reviewer** | `src/httpserver/webserver.hpp:163` | insecure-design
   get_fdset() now accepts three void* parameters in place of fd_set*. Callers that accidentally pass an incompatible pointer type (e.g. a pipe-fd array, a raw int buffer) will compile silently because any data pointer converts to void*. The original fd_set* signature produced a compiler error at misuse sites; the new signature moves the error to runtime, potentially corrupting file-descriptor set state and allowing an attacker to influence which sockets are monitored. CWE-704 (Incorrect Type Conversion).
   *Recommendation:* Prefer keeping fd_set* in the public signature and instead forward-declare 'struct fd_set' at file scope (the standard permits this for POSIX types). If the void* approach is retained for ABI reasons, add a C++ template wrapper that enforces the type at compile time: template<typename T, typename = std::enable_if_t<std::is_same_v<T, fd_set>>> bool get_fdset(T* r, T* w, T* e, int* max_fd).
   *Status:* Resolved — the recommended fix landed: `struct fd_set;` is forward-declared at webserver.hpp:58 and the public `get_fdset` signature takes `struct fd_set*` (line 307). CWE-704 risk eliminated; the doxygen at line 297-300 explicitly notes "compile-time type safety: the compiler rejects non-fd_set* arguments that the previous void* signature silently accepted".

## Minor

6. [x] **architecture-alignment-checker** | `src/httpserver/webserver.hpp:172` | interface-contract
   get_fdset() signature changed from (fd_set*, fd_set*, fd_set*, int*) to (void*, void*, void*, int*) to avoid pulling in <sys/select.h>. While existing callers compile without modification (fd_set* converts implicitly to void*), the public ABI is now type-unsafe: the compiler no longer enforces that callers pass fd_set* — any pointer silently satisfies void*. This is a minor deviation from the principle that public API contracts should be maximally type-safe, and it is not required by §2.2 or §5.5 (which mandate no forbidden includes, not erasure of type information). An alternative is to forward-declare fd_set via 'struct fd_set;' or use a library-defined FdSet alias, which would restore type safety without reintroducing the forbidden header.
   *Recommendation:* Consider documenting this type-erasure choice explicitly in §5.5 or the component doc (§4.1) as a deliberate trade-off, or replace void* with a library-defined opaque wrapper/forward-decl for fd_set so callers retain compiler-enforced type checking. At minimum, add a static_assert in webserver.cpp that sizeof(void*) == sizeof(fd_set*) to make the cast sound.
   *Status:* Resolved — the reviewer's stronger alternative was adopted: `struct fd_set;` is forward-declared at webserver.hpp:58 and `get_fdset` uses typed `struct fd_set*` parameters, restoring compile-time type-checking with no `<sys/select.h>` dependency.

7. [ ] **architecture-alignment-checker** | `src/httpserver/websocket_handler.hpp:45` | adr-violation
   The public header forward-declares 'struct MHD_UpgradeResponseHandle' and 'struct MHD_WebSocketStream' at global file scope. These are libmicrohttpd-internal struct names that now appear in the installed header as forward declarations. While this avoids #include <microhttpd.h> (satisfying the letter of §2.2 and §5.5), it still leaks MHD struct names into the public namespace. If libmicrohttpd ever renames these structs (they are not part of the stable ABI contract), the forward declarations silently diverge. §6.1 states 'MHD_Daemon*, MHD_Connection*, MHD_Response* types appear only in detail/ headers and .cpp files'; MHD_UpgradeResponseHandle and MHD_WebSocketStream fall in the same category and are stored as private member fields in websocket_session, making their presence in the public header a residual coupling. A cleaner approach would be to PIMPL websocket_session's internals into a detail/ struct.
   *Recommendation:* Consider wrapping websocket_session's MHD_UpgradeResponseHandle* and MHD_WebSocketStream* fields in a detail/websocket_session_impl PIMPL (consistent with §4.1 and §4.2's PIMPL approach for webserver and http_request) so MHD struct names do not appear in installed headers at all. If the PIMPL is deferred, document the forward-declaration exception in §6.1 explicitly.
   *Status:* deferred — MHD_UpgradeResponseHandle / MHD_WebSocketStream are still forward-declared at websocket_handler.hpp:48-49 and stored as direct member pointers (lines 131-132). A websocket_session PIMPL refactor is the recommended cleanup; not done in this sweep.

8. [x] **architecture-alignment-checker** | `src/httpserver/websocket_handler.hpp:89` | interface-contract
   websocket_handler declares on_binary() and on_ping() twice (lines 89-90 and 91-92 in the post-task file). This is pre-existing (present in HEAD~2, not introduced by TASK-020), but TASK-020 did not remove it during the sweep. The architecture doc (§4.5) states the public abstract base is 'unchanged from v1 in shape'; if the duplicate is a v1 artifact it should have been cleaned up as part of any header sweep. A duplicate virtual declaration is syntactically valid in C++ but misleading and potentially confusing to implementors inheriting from websocket_handler.
   *Recommendation:* Remove the duplicate declarations of on_binary() and on_ping() from websocket_handler. Since TASK-020 already touched this file, removing them now avoids carrying the inconsistency into the v2.0 public ABI.
   *Status:* Resolved — duplicates removed in TASK-034 incidental cleanup. Each of `on_binary` (line 184) and `on_ping` (line 195) is declared exactly once; comment at 170-174 records the cleanup.

9. [x] **code-quality-reviewer** | `Makefile.am:219` | code-readability
   pthread.h was removed from HEADER_HYGIENE_FORBIDDEN in Makefile.am's grep-based check-hygiene target, but the runtime header_hygiene_test.cpp still checks for _PTHREAD_H / _PTHREAD_H_ on non-libc++ platforms. This asymmetry means the make check-hygiene grep scan gives a clean result on all platforms regardless of pthread leakage, while the stronger enforcement is only in the runtime test. A future contributor reading Makefile.am's HEADER_HYGIENE_FORBIDDEN list may not realise pthread is still being policed elsewhere.
   *Recommendation:* Add a comment directly adjacent to the HEADER_HYGIENE_FORBIDDEN assignment (or alongside the grep command) noting that pthread.h is omitted from the grep check because libc++ pulls it in unconditionally, but that the runtime test enforces the pthread invariant on libstdc++ and other STLs. This makes the enforcement split discoverable without reading the runtime test source.
   *Status:* Resolved — Makefile.am:242-264 carries an extensive "TASK-020 caveat" block explaining why pthread.h is omitted from HEADER_HYGIENE_FORBIDDEN and explicitly cross-referencing the `#ifndef _LIBCPP_VERSION && !_GLIBCXX_HAS_GTHREADS` guard ladder in test/unit/header_hygiene_test.cpp.

10. [x] **code-quality-reviewer** | `src/http_utils.cpp:83` | code-readability
   The block comment (lines 83-90) explaining why `<microhttpd.h>` is included directly is placed between the last standard project header and the `#if defined(_WIN32)` guard, but the actual `#include <microhttpd.h>` it introduces appears after the comment block. Placing the comment immediately above the include it describes would improve locality.
   *Recommendation:* Move the `#include <microhttpd.h>` line immediately after its explaining comment block rather than leaving the Windows-platform `#if` guard in between comment and include.
   *Status:* Resolved — http_utils.cpp now places the TASK-020 explanatory comment (lines 23-25) directly above `#include <microhttpd.h>` (line 26) and adds an inline `// TASK-020: needed directly; no longer reachable transitively` marker on the include line itself. The socket-header includes (lines 32, 42) carry the same inline marker.

11. [x] **code-quality-reviewer** | `src/httpserver/http_request.hpp:214` | code-readability
   The comment block explaining the global-scope `struct MHD_Connection;` forward declaration is lengthy (10 lines) and partially repeats itself between the declaration site and the constructor declaration 12 lines later. The duplication adds reading overhead without adding clarity.
   *Recommendation:* Consolidate to a single comment at the forward declaration and replace the second block with a short back-reference, e.g. `// See forward decl at global scope above.`
   *Status:* Resolved — the in-class constructor comment (http_request.hpp:396-399) is now a two-line back-reference ("See the MHD_Connection forward-declaration comment at global scope above for the namespace-injection rationale.") while the full explanation lives once at the forward declaration (lines 56-64).

12. [x] **code-quality-reviewer** | `src/httpserver/http_request.hpp:53` | code-readability
   The block comment explaining the global-scope `struct MHD_Connection;` forward declaration (lines 53-67) is duplicated verbatim inside the class body at lines 473-483, where the constructor is declared. The same rationale is therefore explained twice within the same file.
   *Recommendation:* Consolidate to a single explanation. Keep the comment at the forward declaration site (line 53) and replace the in-class comment block with a short reference such as `// see forward declaration comment above`.
   *Status:* Resolved — duplicate verbatim block removed; the constructor at line 400 carries a short cross-reference comment (lines 396-399) pointing to the single canonical explanation at the global forward declaration (lines 56-64).

13. [x] **code-quality-reviewer** | `src/httpserver/http_utils.hpp:67` | code-readability
   There is a blank line between `struct sockaddr;` and the following blank line before `namespace httpserver`. The double blank line is inconsistent with the single blank line used elsewhere in the file and creates minor visual noise.
   *Recommendation:* Remove the extra blank line so a single blank line separates the forward declaration from the namespace opening.
   *Status:* Resolved — http_utils.hpp:67-69 now has exactly one blank line between `struct sockaddr;` and `namespace httpserver {`.

14. [x] **code-quality-reviewer** | `src/httpserver/http_utils.hpp:68` | code-readability
   There is a double blank line between the `struct sockaddr;` forward declaration and the `namespace httpserver {` opening brace. The rest of the file uses single blank line separators consistently.
   *Recommendation:* Remove one of the two blank lines to match the established spacing convention in the file.
   *Status:* Resolved — duplicate of item 13; collapsed to a single blank line at http_utils.hpp:68.

15. [x] **code-quality-reviewer** | `src/webserver.cpp:569` | code-elegance
   The `static_assert` checking `sizeof(unsigned int) >= sizeof(socklen_t)` is placed inside the function body of `add_connection`. Static assertions with no dependency on template parameters or function-local types are cleaner at namespace or file scope so they are evaluated once at translation-unit start rather than being associated with a specific function.
   *Recommendation:* Move the `static_assert` to file scope (outside the function), near the other TASK-020 assertions in the same translation unit.
   *Status:* Resolved — assertion moved to file scope in src/detail/webserver_setup.cpp:80-85 (where `add_connection` now lives), tagged `// TASK-020-review: ... evaluated once at TU start rather than being associated with the specific function body.`

16. [x] **code-quality-reviewer** | `test/unit/header_hygiene_test.cpp:94` | test-coverage
   The libc++ pthread bypass (`#ifndef _LIBCPP_VERSION`) silently skips the pthread leak check on macOS builds. There is no test or assertion that confirms the bypass is correctly classified as an STL side-effect rather than a libhttpserver regression. A comment citing a concrete version or upstream issue tracker reference would help future maintainers distinguish a deliberate exception from an overlooked real leak.
   *Recommendation:* Add a one-line comment with a URL or MHD/libc++ issue reference documenting why _LIBCPP_VERSION is the right detection macro and that the bypass is intentional, so future reviewers can verify the assumption hasn't changed.
   *Status:* Resolved — header_hygiene_test.cpp:101-106 now documents both `_LIBCPP_VERSION` (libc++) and `_GLIBCXX_HAS_GTHREADS` (libstdc++) as the canonical detection macros, explains why each is correct, and asks reviewers to "re-verify on each major libstdc++/libc++ upgrade", cross-referencing the Makefile.am rationale comment.

17. [ ] **code-quality-reviewer** | `test/unit/http_utils_test.cpp:1` | test-coverage
   The http_utils_test file adds only a missing <unistd.h> include and contains no new test cases exercising the newly hard-coded enum integer values for start_method_T, digest_algorithm, or digest_auth_result. The static_assert blocks in src/http_utils.cpp pin these at compile time, so there is no runtime gap, but a reviewer cannot tell at a glance that the enum values are tested at all.
   *Recommendation:* This is adequately covered by the compile-time static_asserts; no runtime test addition is strictly required. Consider adding a brief comment to http_utils_test.cpp pointing to the static_assert block in src/http_utils.cpp so the coverage intent is clear.
   *Status:* deferred — purely documentation nudge (reviewer explicitly said "no runtime test addition is strictly required"); no cross-reference comment was added to test/unit/http_utils_test.cpp.

18. [x] **code-simplifier** | `src/http_utils.cpp:24` | code-structure
   The TASK-020 comment block (lines 24-29) documents that both `<microhttpd.h>` and `<sys/socket.h>` must now be included directly. However, the text only mentions `<sys/socket.h>` in the rationale; the `#include <microhttpd.h>` directive on line 30 is what actually appears. The comment says "likewise must be requested explicitly" but the actual `#include <sys/socket.h>` for the non-Windows path does not appear until line 43 inside a platform block — making it look like it was forgotten.
   *Recommendation:* Amend the comment to reference the platform-conditional block that follows, or restructure so the comment is adjacent to both includes. A cleaner form: split into two inline `// TASK-020: needed directly; no longer reachable transitively` comments, one on each `#include` line.
   *Status:* Resolved — http_utils.cpp now uses exactly the recommended split-and-inline pattern: `// TASK-020: needed directly; no longer reachable transitively` appears as a trailing comment on each of `#include <microhttpd.h>` (line 26), `#include <winsock2.h>` (line 32), and `#include <sys/socket.h>` (line 42).

19. [x] **code-simplifier** | `src/http_utils.cpp:51` | code-structure
   The TASK-020 comment block before `#include <microhttpd.h>` (lines 54-60) explains why the include was added but also covers `<sys/socket.h>`, which is not actually included in the diff for this file. The comment references `<sys/socket.h>` as being required for `get_ip_str`/`get_port`/`ip_representation`, yet the include for `<sys/socket.h>` does not appear in the diff. If `<sys/socket.h>` is pulled in transitively via `<microhttpd.h>`, the comment is technically correct but potentially misleading since it implies a direct include.
   *Recommendation:* If `<sys/socket.h>` is only transitively available here, add an explicit `#include <sys/socket.h>` and keep the comment, or reword the comment to say 'reachable transitively via <microhttpd.h>' to avoid implying a direct include that is not present.
   *Status:* Resolved — http_utils.cpp now has an explicit `#include <sys/socket.h>` in the non-Windows branch (line 42) with the same inline `TASK-020: needed directly` marker; no transitive-include ambiguity remains.

20. [x] **code-simplifier** | `src/http_utils.cpp:600` | code-structure
   The `static_assert` block at the end of `src/http_utils.cpp` (lines 600-672) checks `start_method_T`, `digest_algorithm`, and `digest_auth_result` values. The comment header (lines 600-606) describes all three groups together, but the code itself is split across a conditional `#ifdef HAVE_DAUTH`. A reader must scan ahead to realize the first three asserts are unconditional and the rest are gated. The groups are also not separated by a blank line, reducing scannability.
   *Recommendation:* Add a blank line and a sub-comment label between the unconditional `start_method_T` asserts and the `#ifdef HAVE_DAUTH` block to make the grouping immediately visible without reading every assert.
   *Status:* Resolved — http_utils.cpp:454-458 now has a blank line plus a sub-comment label ("digest_algorithm and digest_auth_result: pinned only when HAVE_DAUTH is set...") between the unconditional `start_method_T` asserts (lines 443-453) and the `#ifdef HAVE_DAUTH` block (line 458).

21. [x] **code-simplifier** | `src/httpserver/http_request.hpp:473` | code-structure
   The updated Doxygen block for `http_request(MHD_Connection* ...)` (lines 473-484) repeats in detail the forward-declaration rationale that is already fully explained in the comment block above `struct MHD_Connection;` (lines 53-62). The duplication means two places must be updated if the approach ever changes.
   *Recommendation:* Shorten the constructor Doxygen to a single cross-reference line such as `// See the MHD_Connection forward-declaration above for the namespace-injection rationale.` and keep the full explanation only near the forward declaration.
   *Status:* Resolved — the constructor comment (http_request.hpp:396-399) is now exactly the recommended cross-reference: "See the MHD_Connection forward-declaration comment at global scope above for the namespace-injection rationale."

22. [x] **code-simplifier** | `src/httpserver/http_utils.hpp:67` | code-structure
   Double blank line between the `struct sockaddr;` forward-declaration and the start of the `namespace httpserver` block (lines 67-70). The extra blank line adds no separation value; the surrounding single-blank-line rhythm is broken.
   *Recommendation:* Remove one of the two blank lines so the forward-declaration and namespace open are separated by a single blank line, consistent with every other section boundary in the file.
   *Status:* Resolved — third duplicate of items 13/14; http_utils.hpp:67-69 now has a single blank line separator.

23. [x] **code-simplifier** | `src/httpserver/webserver.hpp:161` | naming
   The `get_fdset` Doxygen comment says callers "MUST pass valid `fd_set*` pointers" in bold uppercase. Using ALL-CAPS MUST in a comment is informal; the stronger signal is a `[[nodiscard]]` attribute or a typed wrapper. As-is, the `void*` signature already makes misuse likely — the comment tries to compensate but cannot enforce the constraint.
   *Recommendation:* Consider wrapping the three `void*` parameters in a thin struct (e.g. `struct fd_set_group { void* read; void* write; void* except; }`) so the call site is self-documenting. If that is out of scope for this task, at least lower the comment to normal prose (remove ALL-CAPS MUST) to avoid false formality.
   *Status:* Resolved — `void*` signature replaced with typed `struct fd_set*` parameters (webserver.hpp:307); the ALL-CAPS MUST prose is gone, and the doxygen at lines 297-300 now plainly states "the compiler rejects non-fd_set* arguments".

24. [x] **code-simplifier** | `src/httpserver/webserver.hpp:424` | naming
   `get_fdset` now accepts `void*` parameters for what callers must pass as `fd_set*`. The docstring comment says callers MUST pass valid `fd_set*` pointers, but the parameter names (`read_fd_set`, `write_fd_set`, `except_fd_set`) do not reflect the new type — they still read as if the type is `fd_set`. In isolation this is readable, but anyone reading only the declaration (not the lengthy comment block) will not know the expected concrete type.
   *Recommendation:* Either keep the existing parameter names and rely on the existing comment (acceptable), or add a brief inline type hint in the parameter names, e.g. `read_fd_set_as_void`, though that is verbose. The existing approach with the comment is the cleaner choice — no code change needed — but the comment should be kept close to the declaration. This is a documentation nudge rather than a code change.
   *Status:* Resolved — the naming/type mismatch no longer exists: parameters are now typed `struct fd_set*` (webserver.hpp:307), so the name `read_fd_set` matches the declared type.

25. [x] **performance-reviewer** | `src/httpserver/websocket_handler.hpp:89` | resource-leak
   on_binary and on_ping are each declared twice in websocket_handler (lines 89-90 and 91-92). While this is a compile-time structural defect rather than a runtime issue, duplicate virtual declarations can confuse vtable layout on non-conformant compilers and will certainly cause a hard build error under -Werror. This appears to be a merge artifact in the TASK-020 commit rather than an intentional change.
   *Recommendation:* Remove the second copies of 'virtual void on_binary(...)' and 'virtual void on_ping(...)' from the websocket_handler class definition. Only one declaration of each virtual method should appear.
   *Status:* Resolved — duplicate of item 8; fixed in TASK-034. Each of `on_binary` (line 184) and `on_ping` (line 195) appears exactly once in websocket_handler.hpp.

26. [ ] **performance-reviewer** | `src/websocket_handler.cpp:138` | memory-allocation
   websocket_handler::on_ping default implementation constructs a temporary std::string from the string_view payload on every ping message, solely to satisfy the send_pong(const std::string&) signature. Each ping triggers a heap allocation even when the payload is small and the base class default is used.
   *Recommendation:* Add a send_pong overload that accepts std::string_view directly, or use SSO-safe small-string path: 'session.send_pong(std::string(payload))' is already the code path, but a string_view overload for send_pong would eliminate the allocation entirely for the common case. Low priority — pings are rare in normal traffic — but worth fixing for completeness.
   *Status:* deferred — `send_pong` is still declared only with `const std::string&` (websocket_handler.hpp:100); `on_ping` default body at websocket_handler.cpp:160 still does `session.send_pong(std::string(payload))`. Reviewer marked it low priority; a `std::string_view` overload is a small follow-up.

27. [x] **security-reviewer** | `src/httpserver/http_utils.hpp:130` | insecure-design
   digest_auth_result and digest_algorithm enum values are now hard-coded integers in the public header with no compile-time guard when HAVE_DAUTH is undefined. The static_assert blocks that pin the values to the upstream MHD macros in src/http_utils.cpp are conditional on #ifdef HAVE_DAUTH. If the library is built without digest-auth support (HAVE_DAUTH not set), the pinning asserts are silently skipped and a future libmicrohttpd that renumbers MHD_DigestAuthAlgo3 would produce mismatched enum values with no build-time failure. CWE-697.
   *Recommendation:* Either unconditionally include the static_assert blocks (they can be guarded with a note that MHD_DIGEST_AUTH_ALGO3_* macros exist regardless of HAVE_DAUTH in modern MHD) or add a compile-time comment explicitly calling out that the values are not pinned when HAVE_DAUTH is absent.
   *Status:* Resolved (via second-listed option) — http_utils.cpp:455-457 carries an explicit compile-time comment calling out the gating: "digest_algorithm and digest_auth_result: pinned only when HAVE_DAUTH is set because the MHD_DIGEST_AUTH_ALGO3_* and MHD_DAUTH_* macros are only defined by <microhttpd.h> in digest-auth-enabled builds."

28. [x] **security-reviewer** | `src/httpserver/webserver.hpp:186` | insecure-design
   add_connection() replaces socklen_t with unsigned int. The static_assert in webserver.cpp guards against sizeof(unsigned int) < sizeof(socklen_t), but socklen_t is a signed type on some legacy POSIX implementations (Austin Group defect report). A caller that passes a large address length could trigger a silent sign-extension or truncation when the value is cast back to socklen_t at the call site in MHD_add_connection. In practice on all modern targets this is benign, but the documented guarantee ('unsigned integer of at least 32 bits') is not universal. CWE-681.
   *Recommendation:* Also static_assert that socklen_t is unsigned (or widen to size_t and add a range check) in webserver.cpp alongside the existing size assertion.
   *Status:* Resolved — src/detail/webserver_setup.cpp:86-92 adds `static_assert(std::is_unsigned<socklen_t>::value, "socklen_t is signed on this platform; the static_cast<socklen_t>(addrlen) in add_connection may produce sign-extension or truncation for large address lengths.")` directly under the existing size assertion, explicitly citing CWE-681.

29. [x] **security-reviewer** | `src/httpserver/websocket_handler.hpp:63` | insecure-design
   The websocket_session private member 'sock' is stored as std::intptr_t (a signed integer) rather than the unsigned MHD_socket (int on POSIX, SOCKET on Windows). On Windows SOCKET is UINT_PTR; a valid socket descriptor with the high bit set (possible with many open sockets) would be stored with a different bit pattern when round-tripped through intptr_t -> static_cast<MHD_socket>. The static_assert checks sizeof, not signedness, so this remains a latent risk on 32-bit Windows builds where SOCKET can be 0x80000000+. CWE-681.
   *Recommendation:* Store the handle as uintptr_t (unsigned) rather than intptr_t to preserve all bit patterns of SOCKET/MHD_socket through the round-trip. Update the static_assert to compare sizeof(MHD_socket) <= sizeof(std::uintptr_t) and cast via static_cast<MHD_socket>(static_cast<uintptr_t>(sock)) where needed.
   *Status:* Resolved — `sock` is now declared `std::uintptr_t` in websocket_handler.hpp:130 (constructor at line 123), and the comment block at lines 117-122 explicitly cites CWE-681 and the Windows SOCKET-bit-pattern fidelity rationale.

30. [ ] **spec-alignment-checker** | `src/httpserver/webserver.hpp:157` | ears-requirement
   PRD-HDR-REQ-002 requires that the system not transitively include `<pthread.h>`. The implementation removes `<pthread.h>` from all libhttpserver public headers, but Makefile.am acknowledges (comments at lines 199-209) that libc++ (Apple's default STL) unconditionally includes `<pthread.h>` transitively through STL container headers. The pthread guard check is conditionally skipped in test/unit/header_hygiene_test.cpp only when `_LIBCPP_VERSION` is defined (lines 94-104), and pthread.h is omitted from HEADER_HYGIENE_FORBIDDEN in the preprocessor-grep check. This is a documented platform-specific STL implementation detail rather than a libhttpserver deficiency, but the requirement PRD-HDR-REQ-002 as written ("shall not transitively include <pthread.h>") is not fully enforceable on Apple/libc++ platforms.
   *Recommendation:* Add a note to PRD-HDR-REQ-002 or §2 Non-functional requirements clarifying that the pthread-hygiene invariant is enforced for libstdc++ and other non-libc++ STLs; libc++ is exempted because it unconditionally includes pthread.h via its STL container backend independently of any libhttpserver code path.
   *Status:* deferred — specs/product_specs.md:64 still states PRD-HDR-REQ-002 unconditionally ("shall not transitively include `<pthread.h>` or `<sys/socket.h>`") with no libc++/libstdc++-gthread carve-out. The build-system caveat is documented only in Makefile.am and the runtime test; the PRD requirement should mirror it.

31. [ ] **spec-alignment-checker** | `src/httpserver/webserver.hpp:162` | acceptance-criteria
   The acceptance criterion states `grep -lE 'microhttpd\.h|pthread\.h|gnutls\.h|sys/socket\.h' src/httpserver/*.hpp` returns no results. The grep reports five public headers because the pattern matches text inside Doxygen and C++ block comments (e.g., `<sys/socket.h>` appears in comment lines 162 and 184 of webserver.hpp). No actual `#include` directives for forbidden headers exist; running `grep -E '^\s*#include.*(microhttpd|pthread|gnutls|sys/socket)' src/httpserver/*.hpp` returns nothing. The criterion as literally written is not met, although the underlying invariant it is testing is fully satisfied.
   *Recommendation:* Either rewrite the comment text in webserver.hpp, websocket_handler.hpp, http_utils.hpp, http_request.hpp, and http_response.hpp to use paraphrases instead of the literal header file names, or update the acceptance criterion in TASK-020.md and specs/product_specs.md §3.1 to use `grep -lE '^\s*#include.*(microhttpd|pthread|gnutls|sys/socket)' src/httpserver/*.hpp` so the check is not defeated by comment text.
   *Status:* deferred — verified the criterion is still defeated by comment text: `grep -lE 'microhttpd\.h|pthread\.h|gnutls\.h|sys/socket\.h' src/httpserver/*.hpp` returns 7 files today, while `grep -E '^\s*#include.*(microhttpd|pthread|gnutls|sys/socket)' src/httpserver/*.hpp` returns nothing. specs/product_specs.md:70 still uses the loose pattern and should be tightened to anchor on `^\s*#include`.

32. [ ] **spec-alignment-checker** | `test/Makefile.am:63` | action-item
   Lines 63-64 still read "Currently in XFAIL_TESTS (see below); flips to PASS when M5 lands and the umbrella is free of backend-header leakage." This comment was not updated when the XFAIL_TESTS line was removed, leaving stale documentation that incorrectly implies the test is still expected to fail. The functional change (removal of XFAIL_TESTS = header_hygiene) is correct; only the accompanying comment block describing the header_hygiene target was not updated.
   *Recommendation:* Update the comment block at lines 56-64 of test/Makefile.am to reflect that header_hygiene is now an unconditional PASS, matching the intent of the removal comment added at lines 161-164.
   *Status:* deferred — verified stale: test/Makefile.am:73-74 still reads "Currently in XFAIL_TESTS (see below); flips to PASS when M5 lands and the umbrella is free of backend-header leakage." A one-line refresh of that comment block is still owed.

33. [x] **test-quality-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-020/Makefile.am:199` | missing-test
   The check-hygiene grep target's HEADER_HYGIENE_FORBIDDEN pattern dropped pthread.h (the comment explains the libc++ STL transitive drag), but there is no cross-reference back to the runtime test's `#ifndef _LIBCPP_VERSION` block. A future contributor editing check-hygiene might add pthread.h back to the grep list without realizing the libc++ caveat applies there too.
   *Recommendation:* Add a one-line note in the Makefile.am comment near HEADER_HYGIENE_FORBIDDEN explicitly cross-referencing the `#ifndef _LIBCPP_VERSION` block in test/unit/header_hygiene_test.cpp, explaining that pthread.h is intentionally absent from the grep list for the same libc++ reason the runtime test skips it on libc++.
   *Status:* Resolved — duplicate of item 9; Makefile.am:259-264 names test/unit/header_hygiene_test.cpp explicitly and references the `#ifndef _LIBCPP_VERSION && !_GLIBCXX_HAS_GTHREADS` guard ladder, with rationale for the deliberate omission from HEADER_HYGIENE_FORBIDDEN.

34. [ ] **test-quality-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-020/test/Makefile.am:63` | naming-convention
   The comment on line 63 still says 'Currently in XFAIL_TESTS (see below); flips to PASS when M5 lands and the umbrella is free of backend-header leakage.' This comment was not updated when XFAIL_TESTS was removed and refers to a future state that has already occurred, which is misleading for anyone reading the file.
   *Recommendation:* Update the comment block at lines 56-64 to reflect the post-TASK-020 state: e.g., 'TASK-020: no longer in XFAIL_TESTS; the umbrella is clean and this test is an unconditional PASS.' This mirrors the language already used in the XFAIL removal comment at lines 161-164.
   *Status:* wontfix — cosmetic/style preference, no functional impact; duplicate of #32; stale comment block, no functional impact

35. [x] **test-quality-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-020/test/unit/header_hygiene_test.cpp:84` | implementation-coupling
   The detection mechanism depends on stable include-guard macro names (MHD_VERSION, GNUTLS_GNUTLS_H, _SYS_SOCKET_H, _SYS_SOCKET_H_, _SYS_UIO_H, _SYS_UIO_H_). These are implementation details of the respective libraries and are not guaranteed by any standard. If a library changes its guard name, the test silently passes even though the leak is still present.
   *Recommendation:* This is an inherent trade-off of runtime guard-based detection (vs. a standalone compiler invocation). The approach is sound and the comment block documents the verified-on list. No immediate action needed, but consider adding a CI note that the guard names should be re-verified on each major libmicrohttpd/GnuTLS upgrade.
   *Status:* Resolved — reviewer explicitly said "no immediate action needed". The "re-verify on each major libstdc++/libc++ upgrade" guidance recommended is now present at header_hygiene_test.cpp:104-106; the verified-on guard list is at lines 49-54.
