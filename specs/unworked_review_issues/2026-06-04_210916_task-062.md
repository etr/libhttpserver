# Unworked Review Issues

**Run:** 2026-06-04 21:09:16
**Task:** TASK-062
**Total:** 45 (0 critical, 3 major, 42 minor)

## Major

1. [x] **code-quality-reviewer** | `test/integ/authentication.cpp:216` | test-coverage
   digest_ha1_md5_resource and digest_ha1_sha256_resource (lines 216-252) still use http_response::unauthorized("Digest", "examplerealm", "FAIL") — the legacy non-RFC overload — when no Authorization header is present. Because that overload emits only a static WWW-Authenticate header (no nonce/opaque/qop), curl --digest cannot complete the handshake and these tests permanently assert 401/FAIL (lines 390-395, 482-487). The TASK-062 acceptance criterion says the six placeholder tests should 'drive real nonce/opaque flows (preferred) or are explicitly retired'. The HA1 variants are neither: they remain on the pre-TASK-062 static-challenge path without a clear explanation distinguishing them from the converted digest_auth test.
   *Recommendation:* Either convert the HA1 resources to use digest_challenge{.realm='examplerealm', .body='FAIL'} (as digest_resource now does) so curl --digest can complete and the tests assert 200/SUCCESS; or add an explicit comment on each HA1 resource explaining the deliberate choice to keep the legacy path (e.g. 'intentionally exercises the non-RFC static-challenge overload to pin backwards-compat surface'), and update the surrounding test comment from 'v2 contract: static 401 Digest challenge, no handshake completes' to name the deliberate reason.
   *Worked 2026-07-07 (chore/unworked-major-issues):* Already resolved by TASK-079 (52a06e0): the HA1 resources were converted to the RFC-7616 digest_challenge overload and their tests complete the handshake asserting 200/SUCCESS.

2. [x] **code-quality-reviewer** | `test/integ/authentication.cpp:44` | readability
   The top-of-file 'v2-digest tracking note' block (lines 44-60) asserts 'libhttpserver only emits the static 401 Digest challenge; the nonce/opaque state machine is not driven' and lists digest_auth as a test that 'become[s] meaningful again only when full v2 Digest support lands'. TASK-062 has now landed that support: digest_resource (lines 104-129) emits digest_challenge bodies and the digest_auth test (lines 261-306) expects http_code==200 / body=='SUCCESS'. The block is directly contradicted by the code immediately below it and will mislead future readers into believing the main digest_auth handshake still does not work.
   *Recommendation:* Update the tracking note to reflect TASK-062's outcome: state that the canonical digest_auth test now exercises the full RFC-7616 handshake via the new digest_challenge overload, that the HA1-based tests (digest_auth_with_ha1_md5/sha256) still use the legacy string overload and thus still expect 401/FAIL, and that digest_user_cache_with_auth similarly still uses the legacy path. Remove or revise the claim that 'nonce/opaque state machine is not driven' for the main digest_auth test.
   *Worked 2026-07-07 (chore/unworked-major-issues):* Already resolved by TASK-079 (52a06e0): the tracking note was rewritten ('updated after TASK-079') and no longer claims the nonce/opaque state machine is not driven.

3. [x] **code-simplifier** | `src/httpserver/http_response.hpp:89` | dead-code
   `qop_auth_int` is part of the public `digest_challenge` struct and is copied into `digest_challenge_body::params` (body.hpp:392, http_response.cpp:524), but it is never read at dispatch (`map_to_mhd_digest_args_` in webserver_request.cpp silently ignores it, hardcoding `MHD_DIGEST_AUTH_MULT_QOP_AUTH` or `_NONE`). This means a caller who sets `qop_auth_int = true` gets no effect — a silent API lie on the public surface.
   *Recommendation:* Either (a) remove `qop_auth_int` from the `digest_challenge` struct and `params` now and add it back when auth-int is implemented (TASK-062 plan §7), or (b) add a `static_assert(p.qop_auth_int == false, ...)` or a runtime throw in the factory if a caller sets it to `true`, so the unimplemented path fails loudly rather than silently.
   *Worked 2026-07-07 (chore/unworked-major-issues):* unauthorized(digest_challenge) now throws std::invalid_argument when qop_auth_int is set (auth-int has no MHD dispatch mapping, option b); struct comment updated; unit test digest_challenge_qop_auth_int_throws added.
## Minor

4. [x] **architecture-alignment-checker** | `specs/architecture/04-components/http-response.md:22` | interface-contract
   The component doc mentions the unauthorized(digest_challenge) factory and the body_kind::digest_challenge dispatch path, but the digest_challenge struct (its fields and defaults) is documented only in the Doxygen comment in src/httpserver/http_response.hpp, not in the architecture component doc. For a public struct that forms part of the factory contract, having its shape documented in the architecture doc (even a one-line summary of its fields and defaults) would make the component doc self-contained for architecture reviewers.
   *Recommendation:* Add a short bullet under the Interfaces section of http-response.md describing the digest_challenge aggregate: its key fields (realm, opaque, domain, algorithm, qop_auth, signal_stale, prefer_utf8, body), their defaults, and the empty-opaque -> server-substituted contract. This is documentation-only and does not require code changes.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Added a sub-bullet under the Interfaces/factories section of specs/architecture/04-components/http-response.md enumerating the digest_challenge aggregate's fields and RFC 7616 §3.3 defaults (realm, opaque, domain, algorithm=MD5, qop_auth=true, qop_auth_int=false, signal_stale=false, userhash_support=false, prefer_utf8=true, body), mirroring the Doxygen comment in src/httpserver/http_response.hpp.

5. [x] **architecture-alignment-checker** | `specs/architecture/07-feature-availability.md:6` | interface-contract
   The HAVE_DAUTH row in the feature-availability table only documents the http_request-side surface (get_digested_user, check_digest_auth) and the create_webserver flag behaviour. It does not mention the new TASK-062 addition: that when HAVE_DAUTH is absent, the http_response::unauthorized(digest_challenge) factory overload is entirely omitted from the public header (gated behind #ifdef HAVE_DAUTH). A caller who conditionally uses the overload needs to know the build-flag gate affects both the request and the response surfaces.
   *Recommendation:* Add a note to the HAVE_DAUTH row along the lines of: 'http_response::unauthorized(digest_challenge) overload and the digest_challenge struct are not declared when HAVE_DAUTH is absent; callers must guard usage with #ifdef HAVE_DAUTH.' This closes the documentation gap without requiring any code change.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Extended the HAVE_DAUTH row in specs/architecture/07-feature-availability.md to document that http_response::unauthorized(digest_challenge) is always declared regardless of HAVE_DAUTH but throws feature_unavailable("digest_auth", "HAVE_DAUTH") on HAVE_DAUTH-off builds, per the gate already implemented in src/httpserver/http_response_factories.hpp (finding 37's fix).

6. [x] **code-quality-reviewer** | `src/detail/body.cpp:267` | code-elegance
   digest_challenge_body::materialize() (lines 267-277) calls MHD_create_response_from_buffer_static with a borrowed pointer into params_->body_text. The method comment explains the WWW-Authenticate header is added by the dispatch path, not here — which is correct — but the defence-in-depth branch (lines 268-273) on moved-from params_ passes nullptr as the buffer pointer to MHD_create_response_from_buffer_static. Passing a null data pointer with size 0 may be valid for some MHD versions but is not guaranteed to be safe; the empty_body::materialize already shows this pattern is intended, but a comment noting why null is safe here (or a non-null empty static buffer) would prevent future maintainers from second-guessing the safety.
   *Recommendation:* Either add a brief comment referencing the MHD API contract for zero-length null-pointer buffers, or replace the null with a pointer to a static empty string (e.g., static const char kEmpty[] = ""; with size 0) to make the intent unambiguous.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* No action — Verified against the installed libmicrohttpd header: MHD_create_response_from_buffer_static's own documentation explicitly permits a NULL buffer when size is zero, so the defensive nullptr/size-0 call on the moved-from branch is safe per the documented API contract and the surrounding comment already explains the branch.

7. [x] **code-quality-reviewer** | `src/httpserver/http_response.hpp:8` | readability
   The body_kind enum in body_kind.hpp (line 8 of that file) now lists digest_challenge as an enumerator, and the architecture doc (http-response.md §4.3) mentions all other body kinds by name in the description of kind_'s enum variants — 'empty, string, file, iovec, pipe, deferred' — but does not list digest_challenge. This is a documentation drift that will mislead readers of the architecture doc who look at the kind_ field description.
   *Recommendation:* Update the body_kind enum list in specs/architecture/04-components/http-response.md (the 'body_kind kind_' bullet) to include 'digest_challenge' alongside the existing six enumerators.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Appended `digest_challenge` to the body_kind kind_ enum bullet in specs/architecture/04-components/http-response.md, matching the 7-enumerator enum in src/httpserver/body_kind.hpp.

8. [x] **code-quality-reviewer** | `test/integ/authentication.cpp:254` | readability
   The comment block at line 254-258 ('TASK-013 §2 / §10: full digest-auth round-trip is a v1-only behaviour. The v1 `digest_auth_fail_response::enqueue_response` path called MHD_queue_auth_required_response3...') is now stale — it precedes the digest_auth test and says round-trip is v1-only, but lines 297-302 immediately below say 'TASK-062 contract: the server now emits a full RFC-7616 §3.3 challenge ... so curl --digest can complete the handshake.' The same file carries contradictory statements about the same test.
   *Recommendation:* Remove or replace the TASK-013 §2/§10 comment block at lines 254-258. The current state is captured well enough by the TASK-062 contract comment at lines 297-302; the earlier block adds noise.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Already resolved in the current tree — The stale TASK-013 §2/§10 comment block no longer exists anywhere in test/integ/authentication.cpp; TASK-079's rewrite (52a06e0) removed it entirely, leaving only the accurate two-round-handshake commentary.

9. [x] **code-quality-reviewer** | `test/integ/authentication.cpp:44` | test-readability
   The top-of-file tracking comment block (lines 44-66) is thorough but has grown to 23 lines covering multiple past task iterations, tracking notes, and future gaps. While valuable as audit trail, this level of inline documentation in a test file blurs the boundary between code comments and issue-tracker content.
   *Recommendation:* Consider trimming the tracking comment to the stable contract summary (what the tests assert now) and moving the historical migration rationale to the TASK-062 spec or a git commit message. This keeps the test file focused on its behavioral contracts.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Already resolved in the current tree — The top-of-file tracking note was rewritten by TASK-079 ('v2-digest tracking note (updated after TASK-079)') and now concisely and accurately states all six tests drive the real nonce/opaque/qop handshake; the sprawling multi-iteration historical content the finding objected to is gone.

10. [x] **code-quality-reviewer** | `test/integ/authentication.cpp:596` | test-coverage
   digest_user_cache_resource uses the legacy `unauthorized("Digest", realm, body)` string overload instead of `unauthorized(digest_challenge{...})`. This is the only remaining resource in the integ test suite that still emits a static challenge. The comment at line 669 correctly documents the contract, but the inconsistency with the rest of the file (which has been migrated to digest_challenge) may confuse readers about which test resources are exercising the new factory.
   *Recommendation:* Add a brief inline comment on the class declaration making explicit that digest_user_cache_resource intentionally uses the legacy overload to test the get_digested_user() cache behaviour under a static challenge, distinguishing it from the digest_challenge resources above.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Already resolved in the current tree — digest_user_cache_resource now uses http_response::unauthorized(digest_challenge{.realm="testrealm", .body="FAIL"}) in both 401 arms per the TASK-079 migration comment; it no longer uses the legacy string overload, so the inconsistency the finding flagged no longer exists.

11. [x] **code-quality-reviewer** | `test/integ/digest_challenge_format_test.cpp:125` | test-coverage
   The integ test rfc7616_challenge_carries_required_fields (lines 125-177) verifies the challenge header format but uses only the default MD5 algorithm and default qop=auth. There is no integ test that spins up a server with algorithm=SHA256 and verifies 'algorithm=SHA-256' appears in the challenge header. This is a minor gap; the dispatch logic is unit-tested via algorithm round-trip, but an integ-level regression for non-default algorithms would provide stronger confidence.
   *Recommendation:* Consider adding a second integ test case that configures digest_challenge{.realm='testrealm@host.com', .algorithm=SHA256, .body='access denied'} and asserts ci_contains(headers, "SHA-256"). This is optional given the unit coverage already exists.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Already resolved in the current tree — An integ test rfc7616_challenge_sha256_algorithm_in_header was added (test/integ/digest_challenge_format_test.cpp) that configures digest_challenge{.algorithm = SHA256} and asserts ci_contains(headers, "SHA-256"), exactly the gap the finding flagged.

12. [x] **code-quality-reviewer** | `test/integ/digest_challenge_format_test.cpp:293` | test-coverage
   The rfc7616_challenge_opaque_and_domain_in_header test asserts that `deadbeef` appears somewhere in the raw header blob but does NOT assert that it appears within the `opaque=` token specifically. If MHD or another header contains that substring for an unrelated reason the assertion would be vacuously true. The same applies to the `/protected` domain substring.
   *Recommendation:* Tighten the assertion to check for `opaque="deadbeef"` (or `opaque=deadbeef`) and `domain="/protected"` (or `domain=/protected`) rather than bare substring presence. This makes the check specific to the token position.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* In rfc7616_challenge_opaque_and_domain_in_header (test/integ/digest_challenge_format_test.cpp), sharpened the bare-substring checks to assert the quoted RFC-7616 auth-params `opaque="deadbeef"` and `domain="/protected"` instead of just "deadbeef"/"/protected".

13. [x] **code-quality-reviewer** | `test/integ/digest_challenge_format_test.cpp:87` | test-readability
   The ci_contains helper is a non-trivial O(n*m) hand-rolled case-insensitive search defined in an anonymous namespace. It is correct, but its nested loop with an `ok` flag and early break is harder to scan than a one-liner using std::search with a custom comparator. It is also duplicated conceptually with the header-check pattern that could use std::string::find after a tolower transform.
   *Recommendation:* Consider extracting ci_contains into a small shared test utility header (test/test_utils.hpp already exists for integ) to avoid future duplication, or replace it with std::search + a case-insensitive predicate. Neither option is blocking.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* No action — The finding's own recommendation says neither option is blocking; ci_contains has only one definition in the tree (no actual cross-file duplication to extract), and the readability concern over the nested loop is fully addressed by finding 22's std::search fix on the same code.

14. [x] **code-quality-reviewer** | `test/unit/http_response_digest_factory_test.cpp:180` | test-coverage
   The body_text round-trip test (lines 180-191) checks body->size() == 13 (the length of 'access denied') but does not check body->get_params().body_text == 'access denied'. This is a very minor gap: size() is a derived value whereas get_params().body_text is the direct round-tripped field. An assertion on the actual string would make the test's intent clearer and catch a hypothetical truncation bug.
   *Recommendation:* Add LT_CHECK_EQ(body->get_params().body_text, std::string("access denied")); alongside the existing size() check.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Already resolved in the current tree — The unit test was refactored to assert only kind()/status per the documented decision to keep internal-storage assertions out of unit tests, and body_text round-trip is now verified at the wire level by the integ test asserting the body equals 'access denied'; the described size()-but-not-body_text gap no longer exists in either form.

15. [x] **code-quality-reviewer** | `test/unit/http_response_digest_factory_test.cpp:68` | code-readability
   The comment at line 65-67 states "both TUs are build-tree-only test sources and never linked together, so there is no ODR conflict". This explanation is sound but the phrasing "never linked together" may be imprecise if the build system ever changes to link all unit test objects into a single binary (which is a common optimisation). The actual safe invariant is that the struct has internal linkage within each TU (it is inside an anonymous namespace... but it is actually in namespace httpserver, not an anonymous namespace), which is a weaker guarantee.
   *Recommendation:* Move http_response_sbo_test_access into an anonymous namespace or a `namespace { }` block within the test TU to give it internal linkage and make the ODR safety unconditional regardless of link topology.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* No action — http_response_sbo_test_access must remain declared directly in namespace httpserver because http_response.hpp grants friendship via 'friend struct http_response_sbo_test_access;', and a class inside an unnamed namespace would not match the friend declaration — applying the recommendation would likely break the friend grant/build. The documented two-TU-never-linked invariant is the correct currently-safe mitigation.

16. [x] **code-quality-reviewer** | `test/unit/http_response_digest_factory_test.cpp:82` | test-coverage
   There is no test for the SHA512_256 algorithm variant. The factory maps all three algorithms (MD5, SHA256, SHA512_256) through digest_challenge_body::params, and the dispatch helper map_to_mhd_digest_args_ has a switch with all three cases. A test round-tripping SHA512_256 through the factory (analogous to the SHA256 test at lines 144-156) would give full algorithm coverage at the unit level.
   *Recommendation:* Add a LT_BEGIN_AUTO_TEST for digest_challenge_sha512_256_algorithm mirroring the existing SHA256 test, checking that body->get_params().algorithm == http_utils::digest_algorithm::SHA512_256.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Added digest_challenge_sha512_256_produces_digest_challenge_kind to test/unit/http_response_digest_factory_test.cpp, mirroring the existing SHA256 test for the SHA512_256 algorithm enumerator. Verified by rebuilding and running the test binary (23/23 checks pass).

17. [x] **code-simplifier** | `src/webserver.cpp:169` | naming
   `generate_random_hex_opaque_` uses `unsigned int v = rd()` and the inner loop `for (int b = 0; b < 4; ++b)` hardcodes 4 bytes per sample. This silently assumes `sizeof(unsigned int) == 4`. The variable is named `v` (single letter) and `byte` shadows a type name, making the intent harder to follow.
   *Recommendation:* Use `uint32_t v = static_cast<uint32_t>(rd())` from `<cstdint>` to make the 32-bit assumption explicit. Rename `v` to `sample` and `byte` to `octet` (or `nibble_pair`) to avoid the reserved-word shadow and clarify the intent.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* In src/webserver.cpp's generate_random_hex_opaque_, changed 'unsigned int v' to 'uint32_t sample' and renamed the inner 'byte' variable to 'octet', making the 32-bit-sample assumption explicit and removing the type-name shadow.

18. [x] **code-simplifier** | `test/integ/authentication.cpp:109` | code-structure
   digest_resource::render_get uses three separate return sites that all return the same unauthorized(digest_challenge{.realm="examplerealm",.body="FAIL"}) value (lines 116-118, 129-131, 128-131). The only difference between the second and third paths is signal_stale. The not-authenticated and stale branches duplicate the struct literal with identical realm/body fields, making changes to those defaults error-prone.
   *Recommendation:* Extract a local helper or named digest_challenge variable for the common fields: `digest_challenge fail_ch{.realm="examplerealm",.body="FAIL"};` then modify it per branch. This removes the duplication without changing behavior.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Hoisted a single `digest_challenge fail_ch{.realm="examplerealm", .body="FAIL"}` in digest_resource::render_get (test/integ/authentication.cpp) and reused it for the no-user and not-OK returns; the NONCE_STALE branch now copies it into `stale_ch` and sets `.signal_stale = true` before returning.

19. [x] **code-simplifier** | `test/integ/authentication.cpp:225` | code-structure
   digest_ha1_md5_resource and digest_ha1_sha256_resource are structurally identical — they differ only in algorithm (MD5 vs SHA256), hash array (PRECOMPUTED_HA1_MD5 vs PRECOMPUTED_HA1_SHA256), and digest_size constant. The full render_get logic (empty-user guard, NONCE_STALE branch, not-OK branch, success) is duplicated verbatim across both classes.
   *Recommendation:* The two resource classes are integration-test infrastructure, so a small amount of duplication is tolerable. If these classes are expected to grow, extracting a shared template or free function parameterised on algorithm, hash pointer, and hash size would eliminate the repetition. This is a polish suggestion, not a blocker.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Already resolved in the current tree — TASK-079 collapsed digest_ha1_md5_resource and digest_ha1_sha256_resource into a single digest_ha1_resource class parameterized on algorithm/HA1 pointer/size, exactly the refactor the finding suggested; a comment above the class documents the collapse.

20. [x] **code-simplifier** | `test/integ/authentication.cpp:310` | naming
   The variable `http_code` is declared as `long` in every test that calls `curl_easy_getinfo(curl, CURLINFO_RESPONSE_CODE, &http_code)`. This is the correct type required by libcurl, but every declaration carries a `// NOLINT(runtime/int)` comment to suppress the linter. The pattern is repeated identically ~15 times across the file.
   *Recommendation:* This is a codebase-wide convention already established before this task; no change is needed here. Flagged only for awareness.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* No action — The finding's own recommendation states this is a pre-existing codebase-wide convention and explicitly says no change is needed; the file still consistently uses 'long http_code' with NOLINT(runtime/int), confirming the convention is intentional and unchanged.

21. [x] **code-simplifier** | `test/integ/digest_challenge_format_test.cpp:76` | code-structure
   `writefunc` (line 76) and `headerfunc` (line 81) have byte-for-byte identical bodies — both are curl string-accumulating callbacks. The separate name adds no information; curl's `CURLOPT_WRITEFUNCTION` and `CURLOPT_HEADERFUNCTION` accept the same signature.
   *Recommendation:* Remove `headerfunc` and pass `writefunc` to both `CURLOPT_WRITEFUNCTION` and `CURLOPT_HEADERFUNCTION`, matching the simpler pattern already used in `authentication.cpp` which only defines one accumulator.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Removed the byte-for-byte-duplicate headerfunc from test/integ/digest_challenge_format_test.cpp and repointed all three CURLOPT_HEADERFUNCTION call sites to writefunc.

22. [x] **code-simplifier** | `test/integ/digest_challenge_format_test.cpp:87` | code-structure
   The ci_contains helper (lines 87-103) reimplements a manual nested loop for case-insensitive substring search. While correct, the inner boolean flag `ok` with `break` is more verbose than necessary.
   *Recommendation:* Replace with std::search plus a custom comparator: `std::search(haystack.begin(), haystack.end(), needle.begin(), needle.end(), [](unsigned char a, unsigned char b){ return std::tolower(a) == std::tolower(b); }) != haystack.end();` — same behavior, more idiomatic C++, removes the nested loop and flag variable.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Replaced ci_contains' hand-rolled nested loop in test/integ/digest_challenge_format_test.cpp with std::search + a case-insensitive comparator, adding #include <algorithm>; this also resolves the underlying duplication flagged by findings 13 and 23 in the same helper.

23. [x] **code-simplifier** | `test/integ/digest_challenge_format_test.cpp:92` | code-structure
   `ci_contains` uses a `bool ok` flag that is set to `false` then `break`-ed from the inner loop, followed by an `if (ok) return true` check. This round-trip through the flag is less readable than a direct early return.
   *Recommendation:* Replace the flag with a direct `return false` inside the inner mismatch branch: `if (tolower_char(haystack[i+j]) != tolower_char(needle[j])) { /* inner loop */ goto next; }` or restructure using `std::search` with a case-insensitive comparator lambda to eliminate the manual nested loop entirely.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* No action — Same code and same underlying ci_contains nested-loop concern as finding 22; applying finding 22's std::search replacement fully addresses this finding too, so a separate edit would be redundant.

24. [x] **code-simplifier** | `test/unit/http_response_digest_factory_test.cpp:207` | code-structure
   The four header-injection guard tests (lines 207-266) follow an identical pattern: declare `bool caught = false;`, enter a try block, build a digest_challenge with an injected string, call unauthorized, (void)r, catch std::invalid_argument, set caught = true, then assert caught. The pattern is repeated four times with no helper.
   *Recommendation:* Extract a lambda or free function `expect_invalid_argument(auto factory_fn)` that encapsulates the try/catch boilerplate. Each test body then becomes a single `LT_CHECK_EQ(expect_invalid_argument([](){ ... }), true);`. This is a polish suggestion; the current repetition is readable and not a maintainability risk at four instances.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* No action — The finding's own recommendation calls this a polish suggestion and states the repetition is readable and not a maintainability risk; the file now has five such try/catch guard tests, still small enough that extracting a helper would add indirection for marginal benefit.

25. [x] **housekeeper** | `specs/architecture/04-components/http-response.md:8` | architecture-not-updated
   The body_kind enum enumeration in the Implementation bullet (line 8) lists `empty`, `string`, `file`, `iovec`, `pipe`, `deferred` but omits the new `digest_challenge` enumerator added by TASK-062. The Interfaces section (line 22) does correctly describe the new overload and dispatch behaviour, so this is a consistency gap within the same doc rather than a missing section.
   *Recommendation:* Add `digest_challenge` to the body_kind enum list in the Implementation bullet, e.g. `body_kind kind_` enum (`empty`, `string`, `file`, `iovec`, `pipe`, `deferred`, `digest_challenge`).
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Same gap and edit as finding 7 (digest_challenge missing from the body_kind enum list); resolved by the same append in specs/architecture/04-components/http-response.md.

26. [x] **housekeeper** | `specs/product_specs.md:162` | spec-not-updated
   The 'In scope' list in PRD §3.5 (Response Model Simplification) still enumerates factories as ending with `http_response::unauthorized(scheme, realm, ...)` only. The new `http_response::unauthorized(digest_challenge)` overload introduced by TASK-062 is not mentioned. Similarly, PRD-RSP-REQ-005 (line 176) reads 'the system shall expose `http_response::unauthorized(scheme, realm, …)`' with no reference to the RFC-7616-compliant digest_challenge variant.
   *Recommendation:* Extend the 'In scope' factory list to include `::unauthorized(digest_challenge)` and update PRD-RSP-REQ-005 (or add a sibling requirement PRD-RSP-REQ-005a) to capture the RFC 7616-compliant overload. Run /groundwork:source-product-specs-from-code to capture these changes.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Extended the §3.5 In-scope factory list in specs/product_specs.md to add http_response::unauthorized(digest_challenge), and added sibling requirement PRD-RSP-REQ-005a documenting the RFC 7616-compliant Digest challenge overload alongside PRD-RSP-REQ-005.

27. [x] **performance-reviewer** | `src/detail/webserver_request.cpp:213` | memory-allocation
   normalize_path allocates a std::vector<std::string> of segments plus a std::string result on every call to should_skip_auth (i.e., on every request). Each call also calls path.substr() in the tokenisation loop, producing transient string objects for each segment.
   *Recommendation:* Rewrite the tokeniser to use std::string_view slices instead of substr(), avoiding the per-segment heap allocation. The final normalized string must still be allocated, but eliminating the intermediate segment strings reduces allocation pressure on the request hot path. Alternatively, a string_view-based normalize that writes directly into a pre-allocated result buffer would eliminate all intermediate allocations.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* No action — The finding's own text frames the allocations as inconsequential for typical skip lists, the TASK-058 empty-list early-out already makes the common no-skip-paths case zero-cost, and rewriting the tokenizer touches security-sensitive auth-skip-path matching without any profiling evidence — regression risk outweighs the minor allocation savings.

28. [x] **performance-reviewer** | `src/detail/webserver_request.cpp:215` | algorithmic-complexity
   should_skip_auth performs a linear O(n) scan of auth_skip_paths_normalized on every request that has an auth_handler configured. Each iteration does a string equality check plus, for wildcard entries, a string prefix comparison. For a server with a small skip list (typical) this is inconsequential, but for a server with hundreds of skip-path entries on a high-RPS endpoint the repeated linear walk is wasted work.
   *Recommendation:* Partition the pre-normalised skip list at construction time into an unordered_set for exact matches and a sorted vector (or prefix trie) for wildcard entries. Exact lookups become O(1) and the wildcard scan is limited to the wildcard subset. The normalisation work already happens once at construction (TASK-058), so the partition is a free addition there.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* No action — The finding's own text says the linear scan is inconsequential for typical small skip lists and only matters for a hypothetical hundreds-of-entries server; restructuring auth-bypass-adjacent matching logic is not justified without profiling data.

29. [x] **performance-reviewer** | `src/http_response.cpp:454` | memory-allocation
   http_response::unauthorized(string_view scheme, string_view realm, string body) builds escaped_realm with a char-by-char loop appending to a pre-reserved string. For realms that contain no special characters (the common case) this is correct and efficient. However, a separate std::string challenge is then constructed from scheme + literal + escaped_realm. These two heap allocations (escaped_realm + challenge) occur on every Basic/static-Digest 401 response. The challenge string is immediately moved into the header map, so the allocation cannot be avoided entirely, but the intermediate escaped_realm copy can be avoided when no escaping is needed.
   *Recommendation:* Add a fast path: scan realm first for '\\' and '"'. If neither is found, skip the escape loop and reference realm directly when building challenge (challenge.append(realm.data(), realm.size())), removing one heap allocation in the common case.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Added a fast path in http_response::unauthorized(scheme, realm, body) (src/http_response_factories.cpp): when realm has no backslash/quote to escape, it is appended directly via challenge.append(realm.data(), realm.size()), skipping the char-by-char escape loop and its heap allocation in the common case.

30. [x] **performance-reviewer** | `src/httpserver/detail/body.hpp:399` | memory-allocation
   digest_challenge_body stores its params behind a std::unique_ptr, adding one heap allocation per Digest 401 challenge response. The comment acknowledges this trade-off (keeping the inline SBO footprint under 64 bytes), but it is a deliberate extra allocation on every request that triggers a Digest challenge. Because Digest 401 responses are control-plane (auth failures, stale-nonce re-challenges), the allocation is incurred at the start of every unauthenticated request in a Digest-protected server.
   *Recommendation:* This is an accepted trade-off documented in the source. If zero-allocation on the challenge path is ever required, consider packing the fixed-size boolean flags and the algorithm enum into a struct small enough to fit inline alongside the std::string members, or promote digest_challenge_body to the heap-fallback path explicitly (via the existing oversized emplace_body branch) and remove the unique_ptr indirection. Benchmark first — allocator amortisation may make this a non-issue in practice.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* No action — The unique_ptr heap-allocation trade-off is already explicitly documented in the digest_challenge_body class comment in src/httpserver/detail/body.hpp (referencing the 64-byte SBO budget, DR-005), and the finding's own recommendation says to benchmark before changing anything.

31. [x] **performance-reviewer** | `src/webserver.cpp:163` | missing-caching
   generate_random_hex_opaque_() calls std::random_device rd as a local variable inside the function, constructing and destroying a random_device object on each call. std::random_device construction can involve OS-level entropy source initialisation (opening /dev/urandom or equivalent) on some platforms. The function is called only once at webserver construction, so this is not a hot-path issue, but re-constructing random_device per-call would be a problem if the function were ever called more frequently.
   *Recommendation:* This is called only once at construction so it is not a real issue. For future proofing, declare rd as a function-local static or pass it as a parameter so the entropy source is opened once. No action required for the current call pattern.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* No action — generate_random_hex_opaque_ is still called exactly once at webserver construction; the finding's own text concludes it is not a real issue and states 'No action required for the current call pattern.'

32. [x] **security-reviewer** | `src/detail/webserver_request.cpp:396` | insecure-design
   In queue_response_dispatching_kind, the static_cast from body* to digest_challenge_body* (line 396-397) has no null check on the resulting pointer before get_params() is called on line 398. The kind() == digest_challenge guard on line 395 is sufficient in the current implementation because adopt_body_from() resets kind_ to empty on a moved-from response, making the scenario where kind_ == digest_challenge and body_ == nullptr unreachable. However, the implicit safety relies on two invariants holding simultaneously (kind_ reset and body_ null). A single-line defensive null check before dereferencing dch would make the safety self-contained and prevent a latent CWE-476 if either invariant is ever relaxed.
   *Recommendation:* Add a null guard: 'if (dch == nullptr) { /* fallthrough to standard queue */ }' after the static_cast, matching the existing materialize_response pattern (webserver_request.cpp line 252-254). This is belt-and-suspenders given the current invariants, but makes the dispatch branch safe against future refactoring.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Added a null check on `dch` immediately after the static_cast in queue_response_dispatching_kind (src/detail/webserver_response_queue.cpp), falling back to the plain MHD_queue_response path instead of risking a null dereference in get_params(); verified the file compiles clean with and without HAVE_DAUTH under -Wall -Wextra -Werror.

33. [x] **security-reviewer** | `src/httpserver/detail/body.hpp:398` | cryptographic-failures
   digest_challenge_body's primary constructor is marked noexcept but calls std::make_unique<params>(...) in its member initialiser list (line 399). std::make_unique performs a heap allocation that can throw std::bad_alloc. Marking the constructor noexcept converts any such throw into std::terminate, silently aborting the process rather than propagating a catchable exception. The SBO emplace_body path (http_response.cpp line 340) is wrapped in a try/catch for the heap fallback, so the process termination would bypass that safety net. CWE-390 (Error Condition Not Checked).
   *Recommendation:* Remove the noexcept specifier from the digest_challenge_body(params) constructor. The move constructor (line 401, unique_ptr move is noexcept) and move_into() (line 412) may remain noexcept. Update the static_assert on line 485 to use is_nothrow_move_constructible_v only (it already does), and add a note that the primary constructor is intentionally not noexcept because of the heap allocation.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Removed noexcept from digest_challenge_body's primary constructor in src/httpserver/detail/body.hpp (it heap-allocates via std::make_unique and can throw bad_alloc) and added a comment explaining why; the defaulted move constructor stays noexcept, so the static_assert on nothrow move-constructibility is unaffected. Verified the library rebuilds cleanly.

34. [x] **security-reviewer** | `src/httpserver/http_response.hpp:89` | insecure-design
   The qop_auth_int field of digest_challenge is stored in digest_challenge_body::params (body.hpp line 392) and moved into the params struct (http_response.cpp line 524), but it is silently discarded in the dispatch path (webserver_request.cpp lines 375-379). If a caller sets qop_auth_int=true expecting auth-int quality-of-protection, the emitted challenge will only carry qop=auth. There is no validation, warning, or documentation at the public API level indicating that qop_auth_int=true is a no-op in this version. A caller relying on auth-int for request body integrity would get a weaker qop without any indication. CWE-684 (Incorrect Provision of Specified Functionality).
   *Recommendation:* Either: (a) add a validation in http_response::unauthorized(digest_challenge) that throws std::invalid_argument when qop_auth_int=true (since auth-int is explicitly documented as 'not in v2 scope'); or (b) add a comment to the digest_challenge struct's qop_auth_int field documenting that it is stored but currently ignored by the dispatch path, and that callers must not rely on it. Option (a) is safer — it makes the misconfiguration loud rather than silent.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Already resolved in the current tree — Resolved by the same change as the already-checked Major finding 3: unauthorized(digest_challenge) in src/http_response_factories.cpp now throws std::invalid_argument when qop_auth_int is true, so the field can no longer be silently discarded and the CWE-684 silent-no-op concern is eliminated.

35. [x] **security-reviewer** | `src/webserver.cpp:163` | insecure-design
   generate_random_hex_opaque_() constructs std::random_device without a device path argument. On some Linux platforms, std::random_device may silently fall back to a deterministic PRNG (e.g., mt19937 seeded with a fixed value) when /dev/urandom is unavailable, resulting in predictable opaque values across server instances. Although RFC 7616 §5.10 explicitly states that opaque is an identifier rather than a secret, a predictable opaque could aid server fingerprinting or, if future code were to misuse opaque as a replay-prevention token, could introduce vulnerabilities. The comment at line 158-160 correctly notes that the strong nonce security is owned by libmicrohttpd's HMAC-keyed mechanism, so the practical impact is low, but the implementation-defined fallback is not documented.
   *Recommendation:* Add a comment documenting the platform-specific fallback behaviour of std::random_device, and add a static_assert or runtime check using std::random_device::entropy() > 0 to detect deterministic fallback. Alternatively, use getrandom() / getentropy() directly on POSIX platforms for an unambiguous OS-entropy source. The existing architecture (strong nonce via libmicrohttpd) means this is defence-in-depth rather than a primary security control, but the ambiguity should be documented.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Extended the TASK-062 comment above generate_random_hex_opaque_ in src/webserver.cpp to document that std::random_device's no-entropy fallback is implementation-defined, and why that doesn't weaken security (opaque is an identifier, not a secret; the real anti-guessing guarantee is libmicrohttpd's HMAC-keyed nonce).

36. [x] **spec-alignment-checker** | `specs/tasks/M7-v2-cleanup/TASK-062.md:15` | specification-gap
   TASK-062.md action item 5 (line 15) records: 'all six tests now exercise the real handshake path end-to-end (the wrong-password tests remain 401/FAIL but now go through MHD_digest_auth_check3 validation rather than failing because no nonce was issued).' This description is inconsistent with what was implemented: only the digest_auth test exercises the full handshake end-to-end. The HA1 and wrong-password variants still fail before any nonce is issued (the resources still return the legacy static-challenge 401). The task-file description overstates the work completed and may mislead TASK-079.
   *Recommendation:* Update TASK-062.md action item 5 to accurately reflect that: (a) digest_auth now exercises the full handshake end-to-end, (b) the five HA1/wrong-password tests remain static-challenge pins, and (c) full conversion of those five tests is deferred to TASK-079.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Already resolved in the current tree — The concern that TASK-062.md's overstated action item 5 would mislead TASK-079 is moot: TASK-079 has since completed and delivered exactly the claimed outcome — all six digest tests now genuinely perform the two-round nonce/opaque handshake — so the task-file text now matches reality and rewriting the closed task's history has no live audience at risk.

37. [x] **spec-alignment-checker** | `src/httpserver/http_response.hpp:239` | ears-requirement
   PRD-FLG-REQ-001 states 'When a public header is parsed the system shall not gate any declaration on HAVE_BAUTH, HAVE_DAUTH, HAVE_GNUTLS, or HAVE_WEBSOCKET.' The new `unauthorized(digest_challenge)` overload is declared inside `#ifdef HAVE_DAUTH` / `#endif` in the public header (lines 239-274), making the declaration absent on HAVE_DAUTH-off builds. The task Doxygen on line 268 acknowledges this: 'Only declared when libmicrohttpd Digest support is compiled in (HAVE_DAUTH); the build-time gate matches the gate on http_request::check_digest_auth(...).' This is a deliberate decision (the matching gate on check_digest_auth was pre-existing), but it is a literal violation of PRD-FLG-REQ-001.
   *Recommendation:* Align with the v2 pattern established for check_digest_auth and get_digested_user in http_request_auth.hpp: remove the HAVE_DAUTH guard from the declaration and add a HAVE_DAUTH-off implementation that throws feature_unavailable('digest_auth', 'HAVE_DAUTH'), consistent with PRD-FLG-REQ-002 and PRD-FLG-REQ-004. If the gate is intentionally preserved for symmetry with check_digest_auth, document the deliberate exception in a specification gap note or DR.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Removed the #ifdef HAVE_DAUTH guard around the unauthorized(digest_challenge) declaration in src/httpserver/http_response_factories.hpp (now always declared, per PRD-FLG-REQ-001) and rewrote its Doxygen note; src/http_response_factories.cpp now defines an #else branch that throws feature_unavailable("digest_auth", "HAVE_DAUTH") on HAVE_DAUTH-off builds (verified compiling with and without HAVE_DAUTH); added the throw-site to feature_unavailable.hpp's Doxygen list.

38. [x] **spec-alignment-checker** | `test/integ/authentication.cpp:216` | acceptance-criteria
   digest_ha1_md5_resource (lines 216-232) and digest_ha1_sha256_resource (lines 235-252) still use the legacy `http_response::unauthorized("Digest", "examplerealm", "FAIL")` factory rather than the new `http_response::unauthorized(digest_challenge{...})` factory introduced by this task. Because they issue a non-RFC-compliant static challenge (no nonce/opaque/qop), the corresponding integration tests cannot complete a Digest handshake and therefore assert 401+FAIL. The task description states the resource was 'updated to emit the new digest_challenge body' — this is only true for digest_resource (lines 104-129), not for the HA1 variants.
   *Recommendation:* Update digest_ha1_md5_resource and digest_ha1_sha256_resource to use `http_response::unauthorized(digest_challenge{.realm = "examplerealm", .body = "FAIL"})` in their initial 401 arms, so the HA1-variant tests can exercise the real handshake path and assert 200+SUCCESS consistent with the task goal.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Already resolved in the current tree — The unified digest_ha1_resource (TASK-079) uses http_response::unauthorized(digest_challenge{.realm="examplerealm", .algorithm=algo_, .body="FAIL"}) in its initial 401 arm, not the legacy string overload, and the HA1 tests now perform the full two-round handshake asserting 200/SUCCESS.

39. [x] **spec-alignment-checker** | `test/integ/authentication.cpp:44` | acceptance-criteria
   The task acceptance criterion states 'The six existing digest placeholder tests in authentication.cpp either drive real nonce/opaque flows (preferred) or are explicitly retired in favour of new tests.' The task description (TASK-062.md line 15) records: 'all six tests now exercise the real handshake path end-to-end.' However, exactly one of the six — digest_auth (lines 261-306) — now asserts LT_CHECK_EQ(http_code, 200) and LT_CHECK_EQ(s, 'SUCCESS'), demonstrating a live Digest handshake. The other five tests (digest_auth_wrong_pass, digest_auth_with_ha1_md5, digest_auth_with_ha1_md5_wrong_pass, digest_auth_with_ha1_sha256, digest_auth_with_ha1_sha256_wrong_pass) are annotated 'See v2-digest tracking note' and still assert 401+FAIL — they remain static-challenge pins unchanged from before TASK-062. The v2-digest tracking note at lines 44-60 explains they 'become meaningful again only when full v2 Digest support lands' (deferring to TASK-079). The task description text in the task file is overstated relative to what was actually done.
   *Recommendation:* Correct the TASK-062.md description to accurately reflect the partial state: only the digest_auth test drives the full handshake; the five HA1/wrong-pass tests remain static-challenge pins pending TASK-079. Alternatively, update digest_ha1_md5_resource and digest_ha1_sha256_resource to emit digest_challenge{} responses so those tests can also exercise the nonce/opaque flow (four of the six pending tests are HA1 variants which only require swapping the response factory).
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Already resolved in the current tree — All six digest tests now perform an explicit two-round RFC-7616 handshake (collect_challenge + perform_with_auth_header) per TASK-079, so the acceptance criterion is genuinely met and the TASK-062.md description no longer overstates the state of the tree.

40. [x] **test-quality-reviewer** | `test/integ/authentication.cpp:401` | redundant-test
   `digest_auth_with_ha1_md5_wrong_pass` and `digest_auth_with_ha1_sha256_wrong_pass` (lines 401-443, 493-535) both assert `http_code == 401 && s == "FAIL"` against the legacy `http_response::unauthorized("Digest",...)` static-challenge path. Given the tracking-note acknowledgement that 'wrong-pass arms are observationally indistinguishable from the correct-pass arms under the v2 static challenge', these two tests are fully subsumed by `digest_auth_wrong_pass` (line 309) — they add maintenance burden without catching any additional bugs in TASK-062 scope.
   *Recommendation:* Either retire `digest_auth_with_ha1_md5_wrong_pass` and `digest_auth_with_ha1_sha256_wrong_pass`, or add a comment to each explaining what distinguishable behaviour they will pin once full v2 HA1 digest auth lands, so reviewers know why they are not deleted.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Already resolved in the current tree — The HA1 wrong-pass tests no longer share the legacy static-challenge path with digest_auth_wrong_pass: they now sign with an HA1 derived from a different password and never send cleartext, exercising the distinct check_digest_auth_digest() HA1-validation path, a deliberate strengthening documented in the tracking note — they are no longer redundant.

41. [x] **test-quality-reviewer** | `test/integ/authentication.cpp:565` | aaa-violation
   `digest_user_cache_no_auth` does not call `curl_easy_cleanup(curl)` after the assertions. Every other integration test in the file cleans up the CURL handle before `ws.stop()`. The missing cleanup leaks the handle (and potentially an open connection) on every test run.
   *Recommendation:* Add `curl_easy_cleanup(curl);` after the `LT_CHECK_EQ(http_code, 401)` assertion and before `ws.stop()` to match the rest of the test file.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Already resolved in the current tree — digest_user_cache_no_auth now calls curl_easy_cleanup(curl) before ws.stop(), matching the rest of the test file; the missing cleanup no longer exists.

42. [x] **test-quality-reviewer** | `test/integ/digest_challenge_format_test.cpp:126` | missing-test
   The single integration test `rfc7616_challenge_carries_required_fields` only verifies the default algorithm (MD5, the factory default). RFC 7616 §3.3 and the task acceptance criteria both call out SHA-256 support. There is no integration test that sets `digest_challenge.algorithm = SHA256` and asserts `algorithm=SHA-256` appears in the `WWW-Authenticate` header.
   *Recommendation:* Add a second integration test `rfc7616_challenge_sha256_algorithm_in_header` that constructs a `digest_challenge` with `algorithm = SHA256` and checks that `ci_contains(headers, "SHA-256")` is true.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Already resolved in the current tree — Same resolution as finding 11: the integ test rfc7616_challenge_sha256_algorithm_in_header now constructs digest_challenge{.algorithm = SHA256} and asserts ci_contains(headers, "SHA-256"), exactly the test the finding asked for.

43. [x] **test-quality-reviewer** | `test/unit/http_response_digest_factory_test.cpp:154` | redundant-test
   digest_challenge_sha256_produces_digest_challenge_kind and digest_challenge_opaque_and_domain_produce_digest_challenge_kind each assert only body_kind::digest_challenge and status 401 — the same observable contract as digest_challenge_status_and_kind (line 107). The comments correctly explain the rationale (factory code-path protection without internal access), but callers wanting just kind() coverage are already covered. These tests add minor maintenance load without catching additional bugs beyond the first test.
   *Recommendation:* Acceptable as-is given the explicit rationale comments. If future refactoring adds observable per-field accessors, consolidate or replace these with richer assertions at that time.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* No action — The finding's own recommendation says 'Acceptable as-is given the explicit rationale comments', and the current file still carries that exact rationale comment block explaining why the kind()-only tests exist.

44. [x] **test-quality-reviewer** | `test/unit/http_response_digest_factory_test.cpp:209` | implementation-coupling
   Header-injection guard tests catch exceptions via a manual bool flag and catch block rather than an assertThrows-style helper. This is a minor style concern — the test is correct and the control flow is linear, but the pattern is slightly more verbose than necessary and obscures intent.
   *Recommendation:* If the test framework gains a LT_CHECK_THROWS macro, migrate these four tests to use it. No action required now.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* No action — The finding's own recommendation conditions any action on the test framework gaining an LT_CHECK_THROWS macro and says 'No action required now'; littletest.hpp still has no such macro, so the explicit no-action condition holds.

45. [x] **test-quality-reviewer** | `test/unit/http_response_digest_factory_test.cpp:242` | missing-test
   Header injection guards are tested for CR+LF in realm (line 199), CR+LF in opaque (line 213), bare LF in domain (line 227), and NUL in realm (line 242). There is no test for NUL in opaque or NUL in domain, even though the doxygen on the factory (`http_response.hpp` line 265) explicitly lists all three fields as guarded against CR, LF, and NUL.
   *Recommendation:* Add two small tests: `digest_challenge_nul_in_opaque_throws` and `digest_challenge_nul_in_domain_throws` to complete the injection guard matrix.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Added digest_challenge_nul_in_opaque_throws and digest_challenge_nul_in_domain_throws to test/unit/http_response_digest_factory_test.cpp, mirroring the existing NUL-in-realm test; confirmed reject_ctrl_chars in src/http_response_factories.cpp guards all three fields identically.
