# Unworked Review Issues

**Run:** 2026-05-29 21:53:58
**Task:** TASK-056
**Total:** 48 (0 critical, 8 major, 40 minor)

> **Minor sweep status (2026-06-02):** all 8 major findings closed
> in the original iter1. Minor backlog triaged below. Items not listed
> in §Deferred have been addressed or are moot due to subsequent commits.

## Major (closed — see original iter1 audit trail in git history)

All eight major findings (#1–#8) were closed by commit 24d1b49
("TASK-056: simplify reject_terminus_collision") and the TASK-053
follow-on that added `test/bench_route_lookup.cpp` (c60ae60).

## Minor — Closed / Moot

| # | File | Category | Disposition |
|---|------|----------|-------------|
| 9  | route-table.md | pattern-violation | **Fixed:** dropped the parenthetical task-ID coupling; benchmark reference is now declarative ("enforced by `test/bench_route_lookup.cpp`"). |
| 12 | radix_tree.hpp | pattern-violation | **Acknowledged-no-action:** reviewer confirmed "architecturally neutral", "No action required". |
| 13–16 | webserver_routes.cpp | naming / code-readability / code-elegance | **Moot:** `opposite_is_prefix` was eliminated in commit 24d1b49. Verified absent from current source. |
| 17 | radix_tree.hpp:228 | code-readability | **Fixed:** corrected the `has_terminus_at` comment — it does descend the wildcard child when the pattern segment is wildcard-shaped (same shape rule as `remove()`). |
| 18 | radix_tree.hpp:289 | readability | **Fixed:** comment "tokenize_url takes std::string by value" replaced with the accurate "takes a const std::string&; pass the already-owned string to bind directly without the std::string{view} temporary the previous overload required." |
| 19 | basic.cpp:526 | test-coverage | **Fixed:** inline comment added at the `/EXA/` request site explaining it resolves via trailing-slash path normalisation, not a prefix match. |
| 20 | threadsafety_stress.cpp:237 | test-coverage | **Acknowledged-no-action:** the `insert()` code path covered by sub-test A is the same path sub-test C exercises; reviewer notes the gap is "not a gap because `insert()` is shared". Inline comment would be churn. |
| 21 | threadsafety_stress.cpp:727 | code-elegance | **Fixed:** trimmed the multi-paragraph zero-floor guard comment to a single sentence; the full rationale lives in the test-level block comment above. |
| 23 | routing_regression_test.cpp:359 | test-coverage | **Acknowledged-no-action:** reviewer confirms "No change strictly required; the port value is irrelevant when `start()` is not called." Pre-existing convention across the suite. |
| 24 | webserver_register_path_prefix_test.cpp:558 | test-coverage | **Acknowledged-no-action:** the cross-kind isolation invariant is now enforced at registration time by the collision guard; the test's reduced role is intentional and the comment in the test file already explains it. |
| 25–26 | webserver_routes.cpp:244–245 | code-structure | **Fixed:** dropped the `std::string(...)` wrappers in the error-message concatenation — `operator+` already accepts `const char*` directly on the right of a `std::string`. |
| 27 | webserver_routes.cpp:287 | naming | **Acknowledged-deferred:** double-guard observation is correct; per-call-site comments would add ~3 lines per site. Lower-friction follow-up is consolidation (#32) — tracked there. |
| 28 | radix_tree.hpp:228 | code-structure | **Acknowledged-no-action:** reviewer confirms "No action required now" — `tokenize()` on cold registration path is acceptable. |
| 30 | v2-deferred-backlog-plan.md:238 | documentation-stale | **Moot:** filename already says `route-table.md` in current backlog plan (line 263); the rename happened in an earlier sweep. |
| 31 | webserver_register.cpp:205 | memory-allocation | **Acknowledged-no-action:** reviewer verified "No action needed" — all call sites already pass lvalue strings. |
| 33 | radix_tree.hpp:288 | memory-allocation | **Acknowledged-no-action:** reviewer confirms "cold-path only and acceptable". |
| 35 | radix_tree.hpp:289 | code-clarity | **Fixed:** same comment as #18 — corrected in the same edit. |
| 37 | threadsafety_stress.cpp:758 | insecure-design | **Moot:** the `std::max(warmup_median, 1000)` zero-floor guard is already in place (line 765); the gate degenerates to `p99 < 10 µs` on sub-µs hosts as the reviewer recommended. |
| 38 | route-table.md:24 | specification-gap | **Fixed:** see #9 — `test/bench_route_lookup.cpp` now exists (TASK-053), so the architecture doc's enforcement claim is no longer prospective. |
| 39 | v2-deferred-backlog-plan.md:239 | specification-gap | **Moot:** same as #30. |
| 40 | v2-deferred-backlog-plan.md:254 | specification-gap | **Acknowledged-no-action:** reviewer marked "Low priority"; the cross-reference text is already in the action item, just not duplicated as a separate acceptance bullet. |
| 41 | webserver_routes.cpp:260 | action-item | **Fixed:** TASK-056 action item 2 in v2-deferred-backlog-plan.md rewritten to reference `reject_terminus_collision()` at the actual call sites (`register_v2_route` and `upsert_v2_radix_route`). |
| 42–43 | threadsafety_stress.cpp:628 | acceptance-criteria | **Moot:** acceptance criterion text already says "registration latency spikes > 10× baseline" (v2-deferred-backlog-plan.md line 283). The "dispatch latency" wording was corrected in an earlier sweep. |
| 45 | routing_regression_test.cpp:241 | implementation-coupling | **Acknowledged-deferred:** reviewer marked "No immediate action needed". The pre-existing internal-field assertions in the non-collision tests are pinning AR-003; the right time to replace them is when `lookup_v2` becomes a true implementation detail (later milestone). |
| 48 | webserver_register_path_prefix_test.cpp:557 | redundant-test | **Acknowledged-no-action:** same shape as #24 — the in-file comment already acknowledges the deliberate scope shift to the collision guard. |

## Minor — Deferred (substantive work; outside the bounds of a polishing sweep)

10. **architecture-alignment-checker** | `specs/architecture/11-decisions/:null` | adr-violation
    Add DR-014 capturing the per-segment child-container decision (`std::unordered_map` → `std::map`) with context, options considered (incl. hash-randomized + flat_map), decision, rationale, and consequences. The change is documented inline in route-table.md but lacks the standing of a Decision Record. Cross-reference from DR-007.

11. **architecture-alignment-checker** | `src/detail/webserver_routes.cpp:260` | pattern-violation
    Either thread `is_prefix` through `upsert_v2_radix_route` to make the polarity constraint explicit/extensible, or add a defensive `assert(!is_prefix_registration)` at the call site plus a function-contract comment. The hardcoded `want_is_prefix=false` is correct today but is not self-evident.

22/29/34. **code-quality / code-simplifier / performance-reviewer** | `test/integ/threadsafety_stress.cpp:736` | code-structure / race-condition
    Refactor the latency-gate baseline so `samples_ns` is snapshotted once under `samples_mtx` and both `sorted` and `warmup` are derived from the same copy. Today the double-lock is benign (writers have joined) but `warmup` is interleaved across all four writer threads — it does not give a true low-cardinality baseline. Right shape: per-thread sample collection with a gate on the first quarter of route-space, or sample only from `tid==0`'s first pass.

32. **performance-reviewer** | `src/detail/webserver_routes.cpp:260` | missing-caching
    Consolidate `reject_terminus_collision` to a single outermost call inside `register_v2_route` / `on_methods_`. Today it runs twice under the write lock (once at the outer call, once inside `upsert_v2_radix_route` / `insert_fresh_v2_entry`). Both calls are correct and the atomicity invariant is satisfied by the outer call alone; the inner calls are redundant cost on the cold registration path at 15 000-route scale.

36. **security-reviewer** | `src/httpserver/detail/webserver_impl.hpp:209` | algorithmic-complexity
    `exact_routes_` is still `std::unordered_map<std::string, route_entry>` and is queried per-request with the full attacker-controlled path. The radix-tree DoS surface is closed (this task) but the exact-tier remains vulnerable in principle. Practical risk is lower (attacker would have to enumerate registered route names to construct a same-bucket corpus). Right follow-on: swap to `std::map` here too, or document the accepted residual risk in the architecture doc.

44. **test-quality-reviewer** | `test/integ/threadsafety_stress.cpp:630` | missing-test
    Add a reader thread that calls `lookup_v2` concurrently with the four writers, so the test exercises read-path latency under contention against a growing radix tree. Today only registration latency is measured.

46. **test-quality-reviewer** | `test/unit/routing_regression_test.cpp:340` | missing-test
    Rollback-atomicity from the opposite side: register exact `/admin`, attempt `register_prefix /admin` (must throw), then attempt `register_exact /admin` again (must ALSO throw — duplicate exact). If the second exact succeeds, the rollback of the first exact entry was incomplete.

47. **test-quality-reviewer** | `test/unit/routing_regression_test.cpp:359` | missing-test
    Stress-test variant that races `register_path` and `register_prefix` on the same path from multiple threads. Asserts at most one succeeds and the loser throws, leaving the winner intact. Closes a potential TOCTOU gap in the collision guard.
