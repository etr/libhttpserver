# Unworked Review Issues

**Run:** 2026-05-07 23:20:16
**Task:** TASK-021
**Total:** 31 (0 critical, 1 major, 30 minor)

## Major

1. [x] **test-quality-reviewer** | `test/unit/http_resource_test.cpp:134` | logic-in-test
   default_render_returns_empty uses a for loop over http_method::count_ to assert each method is allowed. Control flow in tests makes failure diagnosis harder (which iteration failed?) and couples the test to the numeric range of the enum sentinel.
   *Recommendation:* Replace the loop with nine explicit LT_CHECK_EQ calls mirroring the pattern used in allow_all_methods and render_only_resource_methods_allowed. Parameterized tests are also acceptable if littletest supports them, but the loop form is not.
   *Resolution (2026-05-27):* Already resolved — current feature/v2.0 code uses `LT_CHECK_EQ(allowed.size(), 9)` (no loop); the TASK-021 worktree form is gone. TASK-027/036/048 refactors superseded it.

## Minor

2. [-] **architecture-alignment-checker** | `src/httpserver/http_resource.hpp:65` | pattern-violation
   Virtual dispatch method names (render_GET, render_POST, render_PUT, render_DELETE, render_PATCH, render_OPTIONS, render_HEAD, render_TRACE, render_CONNECT) use SCREAMING_CAPS suffixes, contrary to the architecture doc §4.4 which states they should be renamed to snake_case (render_get / render_post / etc.) per PRD-NAM-REQ-001. The TASK-021 scope only covers the method_set bitmask, so this is a carry-forward from prior tasks, not introduced by this change.
   *Recommendation:* Track the rename to render_get / render_post / etc. as a separate task consistent with PRD-NAM-REQ-001 and §4.4. This is not a TASK-021 regression.
   *Deferral (2026-05-27):* Explicitly noted as not a TASK-021 regression; requires a separate rename task covering all call sites.

3. [x] **code-quality-reviewer** | `src/httpserver/detail/modded_request.hpp:49` | code-readability
   Comment block on method_enum is prefixed 'TASK-021:' — task-tracker references are appropriate for PR descriptions but become stale noise in permanent source. Same pattern appears in webserver_impl.hpp and webserver.cpp. (Carry-over minor from iter 1, noted in unworked_review_issues.)
   *Recommendation:* Strip the 'TASK-021:' prefix from all four comments, retaining only the explanatory prose.
   *Resolution (2026-05-27):* Already resolved — TASK-027/036/048 refactors replaced the method_enum/TASK-021 code entirely; no such comments exist in current feature/v2.0.

4. [x] **code-quality-reviewer** | `src/httpserver/http_resource.hpp:214` | code-readability
   The static_assert comment anchors to TASK-021, PRD-REQ-REQ-002/REQ-003 but lives in the public header where end-users see it. The task reference is internal context that has no meaning to library consumers.
   *Recommendation:* Replace the TASK-021 reference in the comment with a plain rationale: 'Ensures http_resource stays small: vptr plus one 32-bit method_set.' The assertion itself is valuable; only the comment needs tidying.
   *Resolution (2026-05-27):* Already resolved — current http_resource.hpp uses std::map-based implementation with no static_assert; TASK-021's bitmask form was not merged into feature/v2.0 in that form.

5. [x] **code-quality-reviewer** | `src/httpserver/http_resource.hpp:219` | code-readability
   The static_assert comment references 'PRD-REQ-REQ-002 / PRD-REQ-REQ-003' without a path or summary. Readers cannot verify the constraint without external context. (Carry-over minor from iter 1, noted in unworked_review_issues.)
   *Recommendation:* Either remove the requirement references or add a brief inline note stating what the constraint is (e.g. 'resource footprint must not exceed vptr + one bitmask word plus headroom').
   *Resolution (2026-05-27):* Already resolved — no static_assert with PRD-REQ references exists in current feature/v2.0 http_resource.hpp.

6. [x] **code-quality-reviewer** | `src/webserver.cpp:1519` | code-readability
   Allow-header serialization iterates from 0 to count_-1 via a raw integer cast back to http_method. If a developer adds a new enumerator and forgets to update to_string(), the loop silently emits an empty token in the Allow header. A constexpr method array would make the divergence a compile error. (Carry-over minor from iter 1, noted in unworked_review_issues.)
   *Recommendation:* Replace the raw loop with a constexpr std::array<http_method, N> kAllMethods = {...} and iterate over it so adding an enumerator without updating the array is a compile-time error.
   *Resolution (2026-05-27):* Already resolved — current webserver.cpp calls hrm->get_allowed_methods() which returns a std::vector<std::string>; no count_-based raw loop or enum cast exists.

7. [x] **code-quality-reviewer** | `src/webserver.cpp:1524` | code-elegance
   header_value += std::string(to_string(m)) constructs a temporary std::string from a constexpr std::string_view. Since operator+= on std::string already accepts std::string_view directly in C++17, the explicit std::string(...) cast is unnecessary and allocates a transient object.
   *Recommendation:* Change to `header_value += to_string(m);` — std::string's operator+= accepts std::string_view since C++17, eliminating the redundant copy construction.
   *Resolution (2026-05-27):* Already resolved — current Allow-header code iterates over a std::vector<std::string> with simple `", " + (*it)` string concatenation; no to_string(m) or string_view cast exists.

8. [x] **code-quality-reviewer** | `src/webserver.cpp:1626` | code-readability
   The comment 'Pre-existing latent bug: mr->callback may also be left un-set here; see TASK-027' describes an observable risk — an unrecognised HTTP method reaching finalize_answer with a null/garbage callback pointer — that is deferred to a future task. This is acceptable as a known-deferred item but the comment reads ambiguously: it does not clarify whether the callback is zero-initialised (safe) or indeterminate.
   *Recommendation:* Verify (and document in the comment) that modded_request's callback member is value-initialised to nullptr by its default constructor, so the latent bug is a logic gap (405 not reached via callback) rather than a crash risk. A quick grep confirms the member is uninitialised by default; adding `std::shared_ptr<http_response> (http_resource::*callback)(const http_request&) = nullptr;` to modded_request would close the crash window even before TASK-027.
   *Resolution (2026-05-27):* Fixed — added `= nullptr` default member initializer to `callback` in `src/httpserver/details/modded_request.hpp`. The TASK-027 TASK-021 else-branch comment doesn't exist in current code (TASK-027 reworked the dispatch path). The fix closes the UB window for the uninitialized member pointer.

9. [x] **code-quality-reviewer** | `test/unit/http_resource_test.cpp:88` | test-coverage
   The allow_some_methods test hard-codes the bit positions of get (bit 0) and post (bit 2) from http_method enum ordinals. This couples the test to the enum declaration order. If a new method is inserted before post in the enum, the LT_CHECK_EQ on the raw bits pattern would silently fail the semantic intent.
   *Recommendation:* Replace the raw-bit assertion with `LT_CHECK_EQ(allowed.bits, (method_set{}.set(http_method::get).set(http_method::post)).bits)` to stay resilient to future enum reordering, while keeping the spirit of the bit-level test.
   *Resolution (2026-05-27):* Already resolved — current allow_some_methods uses LT_CHECK_COLLECTIONS_EQ with sorted string vectors; no bit-pattern assertions exist in current feature/v2.0 test.

10. [x] **code-simplifier** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-021/src/httpserver/detail/modded_request.hpp:49` | comments
   The '// TASK-021:' prefix on the method_enum field comment is a ticket annotation in permanent source. The description of the sentinel role of count_ is genuinely useful but should not carry a task-ticket prefix.
   *Recommendation:* Remove the 'TASK-021:' prefix; the rest of the comment ('enum form of the wire method, decoded once...') is clear and should be retained.
   *Resolution (2026-05-27):* Already resolved — the TASK-021 worktree path no longer applies; current modded_request.hpp has no method_enum field or TASK-021 comment.

11. [x] **code-simplifier** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-021/src/httpserver/detail/webserver_impl.hpp:238` | comments
   The '// TASK-021:' comment before finalize_answer explains why the method parameter was dropped. The explanation is useful but the task-ticket prefix is vestigial noise in a permanent public header.
   *Recommendation:* Drop the 'TASK-021:' prefix; keep the rest of the explanation as-is, e.g. '// The wire-string method parameter was dropped because finalize_answer now consults mr->method_enum (set once by answer_to_connection) for the is_allowed check.'
   *Resolution (2026-05-27):* Already resolved — webserver_impl.hpp does not exist in current feature/v2.0; TASK-027/048 rearchitected the dispatch.

12. [x] **code-simplifier** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-021/src/httpserver/http_resource.hpp:215` | comments
   The static_assert comment opens with '// TASK-021 acceptance:' and references internal ticket IDs (PRD-REQ-REQ-002/003) that are meaningless to external maintainers. The static_assert message already captures the intent; the block comment above is redundant noise.
   *Recommendation:* Replace the block comment with a plain description of intent, e.g. '// Ensure sizeof(http_resource) stays small after the method_set refactor:'. Remove the TASK-021 and PRD-REQ references.
   *Resolution (2026-05-27):* Already resolved — current http_resource.hpp has no static_assert; the TASK-021 worktree form was superseded.

13. [x] **code-simplifier** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-021/src/webserver.cpp:1512` | comments
   The '// TASK-021:' comment on the Allow-header serialization loop carries a ticket reference and a note about v1 std::map order that is only relevant during migration. Once the migration is complete this context becomes historical noise.
   *Recommendation:* Shorten to a pure intent comment, e.g. '// Emit allowed methods in http_method enum-declaration order (GET, HEAD, POST, PUT, DELETE, CONNECT, OPTIONS, TRACE, PATCH).'
   *Resolution (2026-05-27):* Already resolved — current webserver.cpp uses get_allowed_methods() vector iteration; no TASK-021 comment or enum-order loop exists.

14. [x] **code-simplifier** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-021/src/webserver.cpp:1592` | comments
   '// TASK-021: also record the enum form once so finalize_answer can...' is a task-ticket annotation in permanent dispatch code. The content is useful but the prefix is noise.
   *Recommendation:* Remove the 'TASK-021:' prefix; keep the explanatory text about recording the enum form once.
   *Resolution (2026-05-27):* Already resolved — no TASK-021 prefix comments in current webserver.cpp; method_enum dispatch was reworked by TASK-027.

15. [x] **code-simplifier** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-021/src/webserver.cpp:1626` | comments
   The else-branch trailing comment references 'TASK-027 for the dispatch redesign'. Forward references to future task tickets are transient noise in permanent source — when TASK-027 lands the comment becomes misleading, and before it lands it adds no actionable information.
   *Recommendation:* Replace 'see TASK-027 for the dispatch redesign' with a TODO that describes the actual issue: '// TODO: unrecognized verb leaves mr->callback unset; caller must guard before invoking it.'
   *Resolution (2026-05-27):* Already resolved — TASK-027 landed and reworked the dispatch; this comment does not exist in current webserver.cpp.

16. [x] **code-simplifier** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-021/test/integ/basic.cpp:1664` | comments
   '// TASK-021: Allow-header tokens are emitted in http_method enum-declaration order...' The ordering rationale is worth keeping but the ticket prefix is noise in a permanent integration test.
   *Recommendation:* Remove the 'TASK-021:' prefix; keep the explanation about enum-declaration order and the coincidental match with alphabetical order.
   *Resolution (2026-05-27):* Already resolved — current test/integ/basic.cpp has no TASK-021 comments.

17. [x] **code-simplifier** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-021/test/unit/http_resource_test.cpp:43` | comments
   The '// TASK-021 acceptance:' block comment references internal ticket numbers and migration-phase context. In test code this is less harmful than in production headers, but it is inconsistent with the rest of the test file's comment style.
   *Recommendation:* Replace with a plain assertion rationale, e.g. '// http_resource should be smaller than a map-based v1 resource (vptr + uint32_t + padding):'
   *Resolution (2026-05-27):* Already resolved — current http_resource_test.cpp has no TASK-021 acceptance comments or sizeof assertions.

18. [x] **code-simplifier** | `src/httpserver/detail/modded_request.hpp:49` | naming
   The comment block on `method_enum` is tagged `// TASK-021:` which is a task-tracker reference appropriate for a PR description but not for permanent source. Committed source comments should explain *why* the field exists, not *which task* introduced it. The same pattern appears in `src/httpserver/detail/webserver_impl.hpp` (line 99) and `src/webserver.cpp` (line 303, 369).
   *Recommendation:* Strip the `TASK-021:` prefix from all four comments, leaving only the explanatory prose. Example for modded_request.hpp: `// Enum form of the wire method, decoded once at the dispatch boundary.`
   *Resolution (2026-05-27):* Already resolved — method_enum field and TASK-021 comments not present in current modded_request.hpp or webserver.cpp.

19. [x] **code-simplifier** | `src/httpserver/http_resource.hpp:214` | code-structure
   The `static_assert` at the bottom of the header references `PRD-REQ-REQ-002 / PRD-REQ-REQ-003` in its comment but provides no path or context for where those requirement IDs live. A reader cannot verify the requirement without external context. The assert itself is valuable; the comment should be self-contained.
   *Recommendation:* Either remove the requirement references from the comment (the assert message already captures intent) or add a brief inline note explaining the constraint, e.g. `// keep http_resource small enough for stack allocation in hot paths`.
   *Resolution (2026-05-27):* Already resolved — no static_assert in current http_resource.hpp; TASK-021 bitmask form was not preserved in feature/v2.0.

20. [-] **performance-reviewer** | `src/webserver.cpp:1518` | missing-caching
   The Allow header string is reconstructed on every 405 response for a given resource, even though the resource's allowed method set does not change between requests. For resources with a fixed method mask (the common case) this string could be computed once at registration time and stored on the resource or route entry.
   *Recommendation:* Consider caching the serialized Allow header string inside http_resource (computed lazily or at set_allowing time). The 405 path is cold and the current code is functionally correct, so this is only worth pursuing if profiling shows many 405 responses in production (e.g. a public-facing API receiving probing traffic).
   *Deferral (2026-05-27):* Review itself says "only worth pursuing if profiling shows..."; 405 path is cold. Deferred as a future optimization.

21. [x] **performance-reviewer** | `src/webserver.cpp:1519` | algorithmic-complexity
   The loop `for (std::uint8_t i = 0; i < static_cast<std::uint8_t>(http_method::count_); ++i)` iterates all 9 method slots unconditionally on every 405 response. This is O(count_) rather than O(popcount(bits)), though with count_=9 the difference is negligible in practice.
   *Recommendation:* No action needed given count_=9 and the cold path. If count_ grows substantially in the future, a __builtin_ctz-based loop over set bits would be marginally faster. Document the O(count_) bound in a comment if count_ expansion is anticipated.
   *Resolution (2026-05-27):* Already resolved — review says "No action needed"; current code uses vector iteration (no count_/enum loop).

22. [x] **performance-reviewer** | `src/webserver.cpp:1524` | memory-allocation
   std::string(to_string(m)) constructs a heap-allocated std::string from a std::string_view on every iteration of the Allow-header build loop (up to 9 iterations on the 405 path). Each call sites header_value += std::string(...) which may trigger additional allocations during concatenation.
   *Recommendation:* Append the string_view data directly: `header_value.append(sv.data(), sv.size())` or use `header_value += to_string(m)` if std::string operator+=(std::string_view) is available (C++17). This avoids constructing a temporary std::string per method token. The 405 path is cold relative to the allowed path, so impact is minor, but the fix is a one-liner.
   *Resolution (2026-05-27):* Already resolved — current code iterates std::vector<std::string> using `", " + (*it)` (no to_string or string_view cast); TASK-021 form was superseded.

23. [x] **security-reviewer** | `src/httpserver/http_resource.hpp:147` | input-validation
   set_allowing now guards against http_method::count_ with an early return. The guard is correct: without it, set(count_) would set bit 9 in the uint32_t bitmask, making is_allowed(count_) return true — observable via any caller that queries is_allowed directly. The fix closes iter-1 finding security-reviewer-iter1-1.
   *Recommendation:* No change required. The guard is well-placed and the unit test set_allowing_count_sentinel_has_no_effect (test/unit/http_resource_test.cpp:213-220) covers both the no-effect semantic and the all-methods-intact postcondition. Consider also adding a static_assert that (method_set{}.set_all().bits & detail::method_bit(http_method::count_)) == 0 in http_method.hpp to pin the invariant at compile time if the enum ever grows.
   *Resolution (2026-05-27):* Already resolved — review says "No change required". Current set_allowing uses map.count() which naturally rejects unknown strings including any future count_ sentinel.

24. [x] **security-reviewer** | `src/webserver.cpp:1519` | insecure-design
   The Allow-header serialization loop iterates from 0 to static_cast<uint8_t>(http_method::count_) - 1 inclusive and casts each index back to http_method via static_cast. This is correct today because count_ == 9, but the upper bound is derived at runtime from the enum value rather than being a compile-time constant. If a developer adds a new enumerator between patch and count_ and forgets to update this loop, the cast produces values that to_string() handles via its default branch (returning an empty string_view), which would silently emit an empty token ('GET, , POST') in the Allow header — a malformed header value. The loop bound should be a constexpr or the serialization should use a compile-time table to eliminate the risk of divergence. CWE-131 (Incorrect Calculation of Buffer Size — analogous maintainability/safety gap).
   *Recommendation:* Replace the raw loop with a compile-time array of the valid method enumerators (e.g. constexpr std::array<http_method, 9> kAllMethods = {...}) and iterate over it. This makes adding a new method a single-point change that the compiler will flag if to_string() is not updated, and eliminates the need for the cast-and-check pattern.
   *Resolution (2026-05-27):* Already resolved — current webserver.cpp uses get_allowed_methods() vector; no enum cast loop. The risk described is moot in the current map-based implementation.

25. [x] **security-reviewer** | `src/webserver.cpp:1626` | insecure-design
   The else-branch of the wire-string decode in answer_to_connection leaves mr->method_enum at count_ and mr->callback unset. The comment acknowledges this as a pre-existing latent bug (TASK-027). For the current task scope, count_ as default causes is_allowed(count_) to return false, which routes to the 405 path — a safe fail-closed outcome. No UB or incorrect access control results from TASK-021 changes.
   *Recommendation:* No action required within TASK-021 scope. TASK-027 should initialise callback to a safe sentinel (nullptr or a stub that returns 400 Bad Request) and validate it before dereferencing. A nullptr deref on the unrecognised-verb path would be a crash, not merely a 405.
   *Resolution (2026-05-27):* Review says "No action required within TASK-021 scope." Additionally fixed by item 8: callback = nullptr default-initialized in modded_request.hpp. TASK-027 has already landed.

26. [x] **spec-alignment-checker** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-021/src/httpserver/http_resource.hpp:218` | specification-gap
   The static_assert bound `sizeof(void*) + sizeof(method_set) * 2` (= 16 on 64-bit) is looser than the 32-byte ceiling in the unit test (http_resource_test.cpp:50). Both constraints are satisfied by the current layout (vptr 8 B + uint32_t 4 B + 4 B padding = 16 B on typical 64-bit), but the two independently-stated bounds could diverge on unusual platforms or after future edits. This is a documentation / consistency nit, not a defect.
   *Recommendation:* Consider expressing both bounds from the same constant or adding a comment cross-referencing the test assertion so the two constraints stay in sync.
   *Resolution (2026-05-27):* Already resolved — no static_assert in current http_resource.hpp and no sizeof assertion in current http_resource_test.cpp; TASK-021 worktree form superseded.

27. [x] **test-quality-reviewer** | `test/unit/http_resource_test.cpp:135` | logic-in-test
   The `default_render_returns_empty` test uses a `for` loop iterating over `http_method::count_` to assert every method is allowed. A for-loop in test code obscures which specific method failed when an assertion fires — the LT framework will report a failed line number but not which iteration produced the failure. This is a minor readability/debuggability concern rather than a logical correctness issue.
   *Recommendation:* Replace the loop with explicit individual `contains()` checks for each of the nine methods, matching the style already used in `render_only_resource_methods_allowed` (lines 142-152). Alternatively, assert that `allowed.bits == method_set{}.set_all().bits` (a single expression, no loop), which is already done in `default_state_all_methods_set` for `simple_resource`. The loop approach risks hiding which specific method fails.
   *Resolution (2026-05-27):* Already resolved — same as item 1; current test uses `LT_CHECK_EQ(allowed.size(), 9)` (no loop).

28. [x] **test-quality-reviewer** | `test/unit/http_resource_test.cpp:140` | redundant-test
   render_only_resource_methods_allowed and is_allowed_known_methods (line 177) both assert that every method is allowed on a freshly constructed resource and differ only in the concrete resource subtype. Since the default allow-mask is set in the base class constructor, both tests exercise the same code path. One of them adds no regression protection the other does not already provide.
   *Recommendation:* Remove render_only_resource_methods_allowed. The existing default_state_all_methods_set and is_allowed_known_methods already cover the default-all-set invariant. If subtype isolation is a concern, a single representative method check (e.g., is_allowed(http_method::get)) on render_only_resource suffices.
   *Resolution (2026-05-27):* Fixed — removed `render_only_resource_methods_allowed` test from http_resource_test.cpp; `is_allowed_known_methods` provides equivalent coverage.

29. [x] **test-quality-reviewer** | `test/unit/http_resource_test.cpp:141` | redundant-test
   `render_only_resource_methods_allowed` (lines 141-153) and `is_allowed_known_methods` (lines 178-189) test the same code path — all nine methods allowed by default — on two different resource subclasses (render_only_resource and simple_resource). Since neither subclass overrides any allow-mask logic, both tests exercise the same http_resource default constructor path and produce the same observable results. The additional coverage benefit of the second test is near-zero.
   *Recommendation:* Keep `is_allowed_known_methods` (it uses `simple_resource`, the primary test class) and remove `render_only_resource_methods_allowed`, or repurpose it to verify something distinct about `render_only_resource` (e.g. that a call to `render()` returns the expected response). If the intent is to confirm that subclass choice does not affect the default mask, add a single comment explaining that instead of duplicating nine assertions.
   *Resolution (2026-05-27):* Fixed — same fix as item 28; `render_only_resource_methods_allowed` removed.

30. [-] **test-quality-reviewer** | `test/unit/http_resource_test.cpp:71` | redundant-test
   disallow_all_methods asserts bits==0 after disallow_all(). allow_all_after_disallow_all (line 190) performs the same disallow_all() + bits==0 check as its first half before continuing. The first test is a strict subset of the second.
   *Recommendation:* Keep allow_all_after_disallow_all as it tests the full round-trip. Consolidate disallow_all_methods into it, or at minimum leave it only if its isolated failure message adds meaningful diagnostic value.
   *Deferral (2026-05-27):* Kept `disallow_all_methods` for isolated diagnostic value — a failure in `disallow_all_methods` pinpoints `disallow_all()` specifically without the round-trip context of `allow_all_after_disallow_all`. Minor benefit vs churn.

31. [x] **test-quality-reviewer** | `test/unit/http_resource_test.cpp:88` | implementation-coupling
   The test asserts `allowed.bits == (1<<0)|(1<<2)` to verify that GET and POST are allowed after disallowing everything else. This pins the test to the internal enum ordinals (get=0, post=2) rather than to the observable behavior (GET and POST are set, everything else is clear). If the enum order ever changes to satisfy a new requirement, this assertion will fail even though the behavior is correct — a classic refactoring fragility.
   *Recommendation:* Remove the bit-pattern assertion on line 88-89 and rely solely on the `contains()` checks already present on lines 84-86. The contains() calls already verify the correct observable state without coupling to ordinals. If you want to verify exactly two bits are set you can do `LT_CHECK_EQ(allowed.bits != 0u && (allowed.bits & (allowed.bits - 1)) != 0u, false)` — but given the contains() checks already cover the needed behavior, simply removing the raw-bits line is the clearest fix.
   *Resolution (2026-05-27):* Already resolved — current allow_some_methods test uses LT_CHECK_COLLECTIONS_EQ with sorted string vectors; no bit-pattern assertion exists.
