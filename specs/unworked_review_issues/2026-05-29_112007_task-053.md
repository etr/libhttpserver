# Unworked Review Issues

**Run:** 2026-05-29 11:20:07
**Task:** TASK-053
**Total:** 50 (0 critical, 4 major, 46 minor)

## Major

1. [x] **architecture-alignment-checker** | `src/httpserver/detail/webserver_impl.hpp:248` | interface-contract
   The NOTE comment on lookup_v2's declaration states 'lookup_v2 is a SHADOW TABLE only…' — factually wrong post-TASK-053. CWE-284 security audit hazard.
   *Fixed:* Already corrected in `feature/v2.0` prior to this sweep — the current comment now reads "Walk the v2 route table for (method, path) per the lookup pipeline…" and explicitly notes "lookup_v2 is the live dispatch path. As of TASK-053 step 3, resolve_resource_for_request() calls lookup_v2() exclusively; all v1 helper functions were deleted." Finding pre-dates the fix.

2. [x] **code-simplifier** | `src/httpserver/detail/webserver_impl.hpp:246` | comments
   The NOTE doc-comment is factually wrong post-cutover.
   *Fixed:* See item 1 — comment already reflects post-cutover reality.

3. [x] **security-reviewer** | `src/httpserver/detail/webserver_impl.hpp:201` | A04-insecure-design
   `exact_routes_` is std::unordered_map<std::string, route_entry> with default std::hash<std::string>; not seeded per-process on libstdc++/libc++. Hash-DoS surface on the dispatch hot path (CWE-400 / CWE-407).
   *Fixed:* Swapped `exact_routes_` to `std::map<std::string, route_entry, std::less<>>` for hash-free O(log n) lookup with transparent string_view comparators. Same posture TASK-056 applied to the radix-tree per-segment child container. Comment block extended with the rationale so future maintainers don't regress to unordered_map.

4. [x] **test-quality-reviewer** | `test/unit/v2_dispatch_contract_test.cpp:88` | test-isolation
   Port 8231 is shared with `test/integ/hooks_handler_exception_user_handler_throws_continues_chain.cpp`; race under `make check -j`.
   *Fixed:* Renumbered v2_dispatch_contract_test PORT from 8231 to 8260 (verified unused elsewhere in test/ after also checking the recently-added auth_handler tests at 8290–8295). Comment added explaining why the move was made.

## Minor

5. [ ] **architecture-alignment-checker** | `specs/architecture/05-cross-cutting.md:12` | pattern-violation
   §5.1 'Internal locks' table still lists `route_table_mutex` and `route_cache_mutex` without trailing underscore, inconsistent with the actual member names `route_table_mutex_` (shared_mutex on webserver_impl) and the internal `mutex_` inside route_cache. The same doc uses `route_table_mutex_` (with underscore) at line 88 in the lock-order sentence, creating an intra-document inconsistency. The v1 `route_cache_mutex_` field referenced in the table was removed in TASK-053 step 3 and no longer exists.
   *Recommendation:* Update the §5.1 table: rename `route_table_mutex` → `route_table_mutex_`; replace the stale `route_cache_mutex` entry with a note that the LRU cache's internal mutex is owned by `route_lru_cache` (a `route_cache` instance) and is not a named top-level field on webserver_impl. This was a carry-over minor from iter-1 and was not addressed in the iter-1 fix commit.

6. [ ] **architecture-alignment-checker** | `specs/architecture/05-cross-cutting.md:13` | adr-violation
   The cross-cutting threading section (§5.1) lists 'route_cache_mutex (std::mutex) — LRU cache promotion' as a named member of webserver_impl. After TASK-053 (and TASK-027 before it) the LRU cache is encapsulated inside the route_cache class and its mutex is a private member of route_cache, not a named field on webserver_impl. The field route_lru_cache holds the cache object. A reader of the architecture doc will look for a field named route_cache_mutex on webserver_impl and not find it.
   *Recommendation:* Update specs/architecture/05-cross-cutting.md §5.1 'Internal locks' to replace 'route_cache_mutex (std::mutex) — LRU cache promotion' with 'route_lru_cache's internal std::mutex (owned by detail::route_cache) — LRU promotion; not a named field on webserver_impl' to match the implemented encapsulation.

7. [ ] **architecture-alignment-checker** | `src/detail/webserver_dispatch.cpp:305` | pattern-violation
   On a v2 cache hit the resolve_resource_for_request function falls through to the 'if (need_path_template)' block and sets mr->matched_path_template = mr->standardized_url (the concrete request URL, e.g. /users/42) rather than the registered route template (e.g. /users/{id}). Only non-cache-hit radix matches populate result.captured_params but the template is not stored in cache_value, so there is no way to recover it on a cache hit. The architecture doc (§4.7) calls the cache an LRU front-end that should be transparent to callers; the route_resolved hook ctx contract in hook_context.hpp describes path_template as 'the registered route pattern', not the resolved URL. This means hook subscribers observing route_resolved on a cache-warm request will see the concrete URL where they expect the route template.
   *Recommendation:* Add a path_template field to cache_value (a std::string storing the registered pattern) populated on cache insert alongside captured_params. On cache hit, assign mr->matched_path_template from cached.path_template rather than falling through to mr->standardized_url. Alternatively document explicitly in hook_context.hpp that path_template may equal the concrete URL on cache hits when the template cannot be recovered cheaply.

8. [ ] **code-quality-reviewer** | `src/detail/webserver_dispatch.cpp:221` | code-readability
   The file contains two separate `namespace detail { ... }` blocks (lines 89-219 and 221-454) within the same `namespace httpserver` scope. Both blocks define `webserver_impl` member functions. This split namespace form is not used in the sibling files (`webserver_register.cpp`, `webserver_routes.cpp`) and creates unnecessary visual noise — a reader scanning the file must mentally track that the second block is a continuation, not a separate module.
   *Recommendation:* Merge the two `namespace detail` blocks into one continuous block. If a section comment is needed to separate `lookup_v2` from the resolver/dispatch helpers, use a blank-line comment block rather than re-opening the namespace.

9. [ ] **code-quality-reviewer** | `src/detail/webserver_dispatch.cpp:303` | code-readability
   The comment at line 303-306 says 'v2 cache_value does not store the matched URL template; fall back to standardized_url'. This means `mr->matched_path_template` is set to `standardized_url` (the request URL, not the route pattern) on v2 cache misses. The comment is honest, but the consequence is that `route_resolved_ctx::matched->path_template` carries a request URL rather than a registration pattern for cache-miss calls, which differs from the single_resource fast-path (line 266) that sets `get_url_complete()`. This is a deferred correctness gap that is documented but not marked with a TASK reference for follow-up.
   *Recommendation:* Add a TODO comment referencing a specific follow-up task (e.g., TASK-058 or a new entry in the deferred backlog) so the gap is tracked. Storing `url_complete` in `cache_value` at insertion time and reading it back on hit/miss would close the gap without significant overhead.

10. [ ] **code-quality-reviewer** | `src/detail/webserver_dispatch.cpp:304` | code-elegance
   In `resolve_resource_for_request`, `mr->matched_path_template` is set to `mr->standardized_url` (the concrete request URL) rather than the registered route pattern. The comment acknowledges this as a known limitation ('v2 cache_value does not store the matched URL template; fall back to standardized_url'). This means `route_resolved_ctx` consumers receive `/users/42` instead of `/users/{id}`, losing the template information the hook contract implies.
   *Recommendation:* File a follow-up task (or add a TODO with a task reference) to store the registered `url_complete` pattern in `cache_value` so the hook context always carries the route template. For now the behavior is an acknowledged limitation but should be tracked explicitly rather than buried in a dispatch comment.

11. [ ] **code-quality-reviewer** | `src/detail/webserver_dispatch.cpp:97` | readability
   The `invalidate_route_cache()` comment references the removed v1 dispatch cache by name ('route_cache_list / route_cache_map / route_cache_mutex_ — was removed in TASK-053 step 3'). The comment is about code that no longer exists; it adds historical noise that future maintainers will question. The function body is a single `route_lru_cache.clear()` call; a brief accurate comment is sufficient.
   *Recommendation:* Trim the comment to: '// Clear the LRU cache shared by the v2 dispatch path.' The historical v1 reference adds no actionable context now that those artifacts are gone.

12. [ ] **code-quality-reviewer** | `src/detail/webserver_routes.cpp:228` | readability
   `insert_fresh_v2_entry` has a `case route_tier_kind::radix:` arm that contains `__builtin_unreachable()`. The comment explains the arm is unreachable given current callers, but `__builtin_unreachable()` is UB if ever reached; the comment relies on callers respecting the precondition without any assert. Using `assert(false)` before the builtin would preserve UB-elision in release builds while crashing with a diagnostic in debug builds.
   *Recommendation:* Replace `__builtin_unreachable()` with `assert(!"unreachable: radix paths are handled by upsert_v2_radix_route"); __builtin_unreachable();` so a future caller violation produces a clear crash in debug builds rather than silent undefined behavior.

13. [ ] **code-quality-reviewer** | `src/httpserver/detail/webserver_impl.hpp:267` | code-readability
   The class-level comment on `any_hooks_` (lines 267-280) explicitly states 'Do NOT use `memory_order_relaxed` here' and prescribes `memory_order_acquire` for hot-path loads. The `has_hooks_for` accessor at line 303 uses `memory_order_relaxed` and its inline comment says 'the same semantics used at every firing site'. The two comments are contradictory. In practice `memory_order_relaxed` is safe because the firing sites acquire `hook_table_mutex_` (shared_lock) before reading the vector, providing the necessary happens-before — but a reader seeing both comments is left confused about the intended invariant.
   *Recommendation:* Reconcile the two comments. Either (a) update the class-level comment to acknowledge that the mutex acquire inside the firing template renders a relaxed gate load safe, or (b) change `has_hooks_for` to use `memory_order_acquire` to match the explicit prescription. Option (a) is cheaper and avoids the unnecessary acquire on the cold-path false branch. This is a pre-existing inconsistency not introduced by TASK-053 but it is surfaced in the changed file.

14. [ ] **code-quality-reviewer** | `test/integ/basic.cpp:1203` | readability
   The `regex_matching_arg_custom` test now documents and asserts a known v2 behavioral regression (per-segment `{name|regex}` constraint not enforced by the radix tier). The test name no longer describes what is asserted — it tests the broken behavior, not the expected correct behavior. This means `make check` output shows passing tests named after features that are regressed.
   *Recommendation:* Consider adding a brief TODO comment before the `LT_BEGIN_AUTO_TEST` macro noting this is a known-regression pin, so `make check` output immediately signals the intent. No structural code change is required if the team prefers naming stability.

15. [ ] **code-quality-reviewer** | `test/unit/v2_dispatch_contract_test.cpp:119` | test-coverage
   The `do_get` and `do_post_status` helpers do not call `curl_global_init` / `curl_global_cleanup`. In isolation most libcurl builds default-initialize on the first `curl_easy_init` call, but this is not guaranteed (POSIX threading, Windows WinSock). The omission is benign in most CI environments but technically violates the libcurl API contract.
   *Recommendation:* Add `curl_global_init(CURL_GLOBAL_DEFAULT)` in `set_up()` (or at the top of `main()`) and `curl_global_cleanup()` in `tear_down()` / at program exit. Other integ tests in the repo (e.g., `test/integ/basic.cpp`) follow this pattern — a quick grep confirms `basic.cpp` does not either, so this is a systemic minor gap rather than a TASK-053 regression.

16. [ ] **code-simplifier** | `src/detail/webserver_dispatch.cpp:161` | naming
   The variable `key` that takes ownership of `lookup_path` via move is declared at the top of a long scope. The comment explaining the move semantics is accurate but the name `key` alone (without `cache_`) is slightly less descriptive than the type `cache_key` suggests it could be.
   *Recommendation:* Rename `key` to `cache_key_val` or `ck` is not an improvement; the current name is acceptable. This is borderline — leave as-is if the team prefers minimalism.

17. [ ] **code-simplifier** | `src/detail/webserver_dispatch.cpp:202` | comments
   The block comment at lines 202-208 explaining why result.entry is copied rather than moved is thorough but references 'the v2 resolver' and 'shared_ptr arm is null after a move' — the reasoning is sound, but the comment is longer than necessary given that the code itself expresses the behavior. A one-line summary comment would suffice.
   *Recommendation:* Condense to: `// Copy into cache_value; result.entry must remain valid for the caller after insert.` The full rationale is already preserved in git history and can be trimmed here.

18. [ ] **code-simplifier** | `src/detail/webserver_dispatch.cpp:209` | code-structure
   cache_value is populated via two separate field assignments before being passed to insert(). A braced aggregate initializer would make the construction intent clearer and more concise.
   *Recommendation:* Replace the three-line v/v.entry/v.captured_params block with: `route_lru_cache.insert(key, cache_value{result.entry, result.captured_params});` — the struct is an aggregate so brace-initialization works directly.

19. [ ] **code-simplifier** | `src/detail/webserver_dispatch.cpp:97` | naming
   invalidate_route_cache contains the comment '// Clear the v2 LRU cache. (The v1 dispatch cache — route_cache_list / route_cache_map / route_cache_mutex_ — was removed in TASK-053 step 3 along with the v1 resolver.)' The parenthetical is a historical note about removed fields that no longer exist anywhere in the codebase. It adds length without aiding comprehension of what the function does today.
   *Recommendation:* Trim to: '// Clear the LRU cache. Both the cache list and map are reset; // registration callers must invalidate after any table mutation.' The historical reference to v1 field names is only useful during the transition window; now that the cutover is complete it is noise.

20. [ ] **code-simplifier** | `src/detail/webserver_routes.cpp:222` | code-structure
   insert_fresh_v2_entry has a case route_tier_kind::radix with a __builtin_unreachable() and a comment 'Unreachable: upsert_v2_table_entry routes url_pars-non-empty paths through upsert_v2_radix_route before calling insert_fresh_v2_entry.' The comment is accurate but the defensive break after __builtin_unreachable() (line 229) is dead code that follows a noreturn intrinsic. On GCC/Clang the break is silently elided but it adds visual noise.
   *Recommendation:* Remove the break after __builtin_unreachable() to make the intent clearer — the case body terminates unconditionally. Alternatively, keep it with a comment '// break unreachable' to satisfy static analysis tools, but the current style elsewhere in the file omits trailing breaks after early returns.

21. [ ] **code-simplifier** | `src/httpserver/detail/route_cache.hpp:150` | code-structure
   In find_by_view, after the empty-map early-out (bucket_count()==0), the code calls map_.find(it->first) to get a mutable iterator for the splice (line 155-156). This is a second hash probe on the hit path — the bucket walk already found the entry. The comment says 'Promote: splice requires a mutable iterator from the main map' but the double-probe is avoidable.
   *Recommendation:* This is a genuine performance concern on the warm-cache hit path. Consider storing a mutable iterator in the bucket-walk loop (change the loop's iterator type from cbegin/cend to begin/end on the non-const overload) to avoid the second hash probe. The behavior is identical; only the number of hash probes differs. This may warrant a separate TASK-058 entry rather than an inline fix.

22. [ ] **code-simplifier** | `src/httpserver/detail/route_tier.hpp:107` | code-structure
   The function has two early-return paths for route_tier_kind::exact — one inside the regex block (line 99) and one at the bottom (line 107-108). The bottom return is the implicit fallthrough for the non-regex case, but structurally the function would read more clearly as: classify, return early on radix, compile regex if needed, otherwise exact.
   *Recommendation:* No code change needed — the iter-1 fix already consolidated the regex flag correctly and the two exact returns are in different branches. The current structure is correct and readable.

23. [ ] **code-simplifier** | `test/unit/v2_dispatch_contract_test.cpp:163` | code-structure
   The hook lambda in prefix_route_marks_is_prefix_true (lines 166-178) and the nearly identical lambda in exact_route_marks_is_prefix_false (lines 201-213) and method_mismatch_still_resolves_route (lines 239-250) are copy-pasted with minor structural differences. Each test also repeats the same webserver setup pattern (create_webserver, add_hook, register_*, start, sleep, request, stop).
   *Recommendation:* Extract a shared helper — e.g., a local lambda `make_probe_hook(hook_probe& probe)` that returns the std::function — to eliminate the three identical hook bodies. The webserver setup is intentionally per-test for fixture isolation, so that part is acceptable as-is, but the hook lambda body is pure duplication. This is a minor test-quality issue; behavior is unaffected.

24. [ ] **housekeeper** | `specs/tasks/v2-deferred-backlog-plan.md:26` | task-not-marked-complete
   The summary table in v2-deferred-backlog-plan.md (lines 24-32) has no Status column, so the row '| TASK-053 | Wire `lookup_v2()` into dispatch hot path | ...' does not visibly show Done status. A reader scanning only the summary table cannot see the task is complete. The Done status is correctly recorded in the task body (**Status:** Done at line 45), but the summary table is the first thing a reader sees.
   *Recommendation:* Add a Status column to the summary table in v2-deferred-backlog-plan.md and mark TASK-053 as Done there, to keep the table consistent with the task body and make completion visible at a glance.

25. [ ] **performance-reviewer** | `src/detail/webserver_dispatch.cpp:126` | memory-allocation
   canonicalize_lookup_path always returns std::string by value, which allocates even for paths that need no normalization (already start with '/', no trailing '/'). For the common case (well-formed paths from MHD), this is an unconditional allocation per request. The TASK-058 prep comment in the task description notes this return type should eventually become string_view; until that refactor lands, the current design always allocates.
   *Recommendation:* Short-term: add a fast-path check — if the input path is already canonical (non-empty, starts with '/', does not end with '/'), return it directly (still by value, but a copy of a std::string the caller already has, which may be elided). Long-term (TASK-058): return string_view pointing into the caller's buffer when no normalization is needed, allocating only on the rare trailing-slash strip case.

26. [ ] **performance-reviewer** | `src/detail/webserver_dispatch.cpp:144` | missing-caching
   canonicalize_lookup_path always allocates a new std::string before the cache probe, even for paths that are already canonical (leading slash, no trailing slash). For the common case — paths that need no transformation — this is an unconditional heap copy per request on the cache-hit path.
   *Recommendation:* Add a fast-path check before constructing `lookup_path`: if `path` is non-empty, starts with '/', and (size==1 || path.back()!='/'), pass `path` as-is via string_view to `find_by_view` and only allocate when normalization is actually needed. The `cache_key` construction on the miss path can then use `std::string(lookup_path_sv)` or move the already-normalized string.

27. [ ] **performance-reviewer** | `src/detail/webserver_dispatch.cpp:208` | memory-allocation
   At cache-insert time (step 3, line 208-212), result.captured_params is copied into cache_value::captured_params via 'v.captured_params = result.captured_params' rather than moved. The comment on lines 202-207 explains that result.entry cannot be moved because the caller reads it after the insert, but result.captured_params is only read again in the caller via the same 'result' struct — which is returned by value. Once the insert is done the caller owns result and returns it, so captured_params can safely be moved into cache_value, saving a vector allocation on every radix-tier miss that captures parameters.
   *Recommendation:* Change line 211 to: 'v.captured_params = result.captured_params;' is correct for entry (shared_ptr bump), but 'v.captured_params = std::move(result.captured_params);' is safe for captured_params because result is returned by value after the insert call and the caller (resolve_resource_for_request) uses result.captured_params in its own for-loop after lookup_v2 returns — so the move happens inside lookup_v2 before return. Verify: resolve_resource_for_request at line 295 iterates result.captured_params after lookup_v2 returns, so the move must NOT happen inside lookup_v2. The current copy is therefore correct for safety, but a structural refactor (return before insert, or copy only what's needed) could avoid the redundancy.

28. [ ] **performance-reviewer** | `src/detail/webserver_dispatch.cpp:304` | missing-caching
   When need_path_template is true, matched_path_template is set to mr->standardized_url (the raw request URL) rather than the registered route template (e.g. '/users/{id}'). This is documented as a known limitation ('v2 cache_value does not store the matched URL template; fall back to standardized_url'). The consequence is that hooks reading route_resolved_ctx.matched->path_template receive the concrete URL, not the pattern — which defeats a common hook use-case (rate-limiting or logging by route pattern, not per-URL). This is not a performance issue per se but is a behavioral regression vs. the v1 path if hooks were relying on the template.
   *Recommendation:* Store the url_complete (registration-time template string) in cache_value alongside captured_params, and populate mr->matched_path_template from it. The extra std::string in cache_value adds ~24-32 bytes per cache entry (256 entries = ~6-8 KB), which is negligible. This also eliminates the 'fall back to standardized_url' code path.

29. [ ] **performance-reviewer** | `src/httpserver/detail/route_cache.hpp:157` | race-condition
   find_by_view copies the full cache_value (shared_ptr ref-count bump + captured_params vector copy) while holding the exclusive cache mutex. For routes with non-empty captured_params (parameterized radix hits), this means every concurrent cache hit serializes a vector allocation under a plain mutex, not a shared_mutex. Under high concurrency this becomes a contention bottleneck proportional to the size of captured_params.
   *Recommendation:* Consider copying only the list iterator under the lock and deferring the cache_value copy to after the lock is released — requires storing the value in a separately-heap-allocated node (e.g., shared_ptr<cache_value>) so the node lifetime is independent of list membership. Alternatively, accept the current behavior as sufficient given the 200 ns ceiling holds with empty captured_params (the bench-measured case), and document the contention risk explicitly.

30. [ ] **performance-reviewer** | `src/httpserver/detail/webserver_impl.hpp:302` | race-condition
   has_hooks_for() uses memory_order_relaxed to load the any_hooks_ atomic gate. The long comment above it (lines 269-283) correctly documents that acquire/release ordering is required for the gate to provide happens-before guarantees on the hook vector contents — yet the implementation uses relaxed. This is noted as intentional ('correct hot-path pattern') in the comment but the implementation does NOT follow the documented pattern: it says 'Do NOT use memory_order_relaxed here' in the documentation block but the code does exactly that. Under TSan on ARM this is a latent race: a thread can observe any_hooks_[phase]==true while the hook vector is still being written.
   *Recommendation:* Change the load in has_hooks_for() to memory_order_acquire to match the documented contract. The release store must already be memory_order_release (verify in register_hook_impl / hook_handle::remove). This is a correctness issue that will manifest as non-deterministic behavior under TSan on weakly-ordered architectures (ARM64, POWER), not a pure performance issue — but it is listed here because the acquire load is also the architecturally correct approach for the zero-cost-when-unused gate.

31. [ ] **performance-reviewer** | `test/bench_route_lookup.cpp:222` | missing-caching
   The radix-tier bench comment claims 16 paths 'blow out' the 256-entry LRU cache repeatedly, forcing a radix walk on every lookup. This is incorrect: 16 entries fit comfortably in a 256-entry LRU and will stay warm after the first 16 misses per outer round, meaning most inner iterations of the radix bench are served by the cache rather than the radix tier. The bench therefore measures primarily cache-hit latency with 16-entry warm-up overhead, not radix-tier latency.
   *Recommendation:* Expand kPaths to at least 257 distinct URLs to guarantee LRU eviction on every rotation and ensure the bench actually exercises the radix tier. Alternatively document explicitly that the bench measures 'mixed cache+radix' and adjust the ceiling comment accordingly. The 5 µs ceiling remains conservative enough that the current bench still passes even when measuring cache hits, so this is a bench-accuracy issue only and does not affect the production performance verdict.

32. [ ] **security-reviewer** | `src/detail/webserver_dispatch.cpp:209` | insecure-design
   The copy-instead-of-move fix for cache insertion is correct. Moving result.entry into the cache_value would leave the shared_ptr variant arm null, causing the subsequent get_if check in resolve_resource_for_request to return nullptr and emit a false-negative 404. The copy (one shared_ptr ref-count bump + one vector copy on the miss path only) is the correct fix.
   *Recommendation:* No action needed. The fix is correct and the comment documents the moved-from hazard clearly.

33. [ ] **security-reviewer** | `src/detail/webserver_dispatch.cpp:295` | A09-logging-failures
   captured_params (URL parameter names and values extracted from the client URL by the radix tier) are replayed into mr->dhr via set_arg(name, value) without any sanitization of the parameter name or value beyond the content_size_limit truncation applied by http_request::set_arg. The parameter value is already URL-segment content and is bounded by content_size_limit (correctly), but the parameter name comes from the route registration string ({id}, {user_id}, etc.) which is server-controlled and therefore safe. This is noted because the v1 apply_extracted_params had equivalent behavior; no new injection surface is introduced, but the v2 path should be confirmed to not expose captured_params to logging in raw form.
   *Recommendation:* No code change required. Confirm in a future logging review that captured_params values are not logged at debug or info level in a way that could write attacker-controlled URL path segments to logs without sanitization. The content_size_limit truncation is correctly applied through the set_arg chain.

34. [ ] **security-reviewer** | `src/detail/webserver_dispatch.cpp:305` | A04-insecure-design
   On a regex-tier or radix-tier cache miss, matched_path_template is set to mr->standardized_url (the client-supplied URL after standardization) rather than to the registered route pattern (e.g. /api/users/[0-9]+). The v1 cache-hit path had the same behavior; this is a behavior preservation choice. However, hook consumers (route_resolved_ctx, before_handler_ctx) receive a value that embeds unvalidated user input (the raw URL path) in a field named 'matched_path_template', which implies a registered pattern, not a user value. A hook that logs matched_path_template assuming it is a safe, server-controlled string may inadvertently log or forward user-controlled content. This is a minor information leakage / confused-deputy issue rather than a direct vulnerability.
   *Recommendation:* Document in the hook_context headers (route_resolved_ctx, before_handler_ctx) that matched_path_template falls back to the standardized request URL on cache hit and for the regex/radix tiers, and may therefore contain user-controlled path content. Alternatively, store the registered pattern string in the route_entry (alongside the compiled regex) and populate matched_path_template from it instead of from standardized_url. CWE-116.

35. [ ] **security-reviewer** | `src/httpserver/detail/route_cache.hpp:148` | insecure-design
   The bucket_count==0 early-out correctly prevents UB on libc++ when find_by_view is called on a freshly-constructed empty unordered_map. The previous ternary (bucket_count() ? hash % bucket_count() : 0) would have passed bucket index 0 to cbegin(0)/cend(0) on an empty map, which dereferences a null internal pointer on libc++ (undefined behavior exploitable as a crash vector on the first request to a freshly-started server). The fix is sound.
   *Recommendation:* No further action needed. The fix is correct and the comment explains the libc++ specific behavior.

36. [ ] **security-reviewer** | `src/httpserver/detail/route_cache.hpp:151` | A04-insecure-design
   find_by_view manually computes the bucket index as bucket_hash % map_.bucket_count() and then walks the bucket using cbegin(b)/cend(b). This is a valid technique but it relies on the invariant that std::hash<std::string_view> and std::hash<std::string> produce identical hashes for the same character sequence (which is a C++17 guarantee). However, the combined hash (h1 ^ (h2 + kHashMix + (h1 << 6) + (h1 >> 2))) is computed manually in find_by_view rather than using the same cache_key_hash::operator() on a temporary cache_key. If the cache_key_hash formula is ever updated, find_by_view must be updated in lockstep or it will silently probe the wrong bucket and return false negatives (treating a cached route as a miss), bypassing the cache and forcing tier walks. This is a latent maintenance hazard, not an exploitable vulnerability today.
   *Recommendation:* Extract the hash combining formula from cache_key_hash::operator() into a private static helper and call it from both operator() and find_by_view, ensuring both are always consistent. Add a static_assert or unit test that verifies find() and find_by_view agree for a known key.

37. [ ] **security-reviewer** | `src/httpserver/detail/route_tier.hpp:92` | authentication
   Re-verification of iter-1 nosubs fix: std::regex::nosubs is confirmed present at construction. The self-match check at line 98 (regex_match on url_complete using the nosubs regex) correctly returns a boolean without requiring capture groups. The stored compiled_re (with nosubs) in regex_route is used only for boolean dispatch matching in webserver_dispatch.cpp:192, which is also correct. The fix does not suppress match results — it only suppresses sub-expression capture allocation. No regression introduced.
   *Recommendation:* No action needed. The fix is correct. Consider adding a comment noting that nosubs is intentional and that dispatch does not rely on sub-captures (all parameter capture is done by the radix tier, not the regex tier).

38. [ ] **spec-alignment-checker** | `src/httpserver/detail/webserver_impl.hpp:248` | specification-gap
   The comment block on `lookup_v2` (lines 248-253) still reads 'NOTE (CWE-284 guard): lookup_v2 is a SHADOW TABLE only. The live HTTP dispatch path (finalize_answer -> resolve_resource_for_request) still uses the v1 registered_resources* maps. lookup_v2 is used by tests to pin the v2 tier pipeline and will be wired into finalize_answer at Cycle K dispatch cutover. Until then, any access control registered via the v2 table has NO effect on live traffic.' This comment was accurate before TASK-053 but is now factually wrong: `finalize_answer` calls `resolve_resource_for_request`, which calls `lookup_v2` exclusively (v1 fallback removed in step 3). A reader of this header gets the opposite of the true state.
   *Recommendation:* Replace the stale CWE-284 comment with a post-cutover note, e.g. 'TASK-053: lookup_v2 is the sole route resolver for the dispatch hot path. resolve_resource_for_request calls it directly; the v1 registered_resources* maps survive only as registration-time bookkeeping (see comment above those maps).'

39. [ ] **spec-alignment-checker** | `src/httpserver/detail/webserver_impl_dispatch.hpp:null` | action-item
   Action item 2 specifies 'gated by the use_lookup_v2_ flag (default true)'. The implementation completed the dispatch cutover (step 2 comment is accurate in the task plan), but the final implementation in TASK-053 step 3 removed the v1 fallback entirely rather than leaving a runtime flag. The `use_lookup_v2_` member field does not exist anywhere in the codebase. This is a divergence from the action item text, though the outcome (v2 dispatch is unconditional) is arguably stricter and more correct per the task's stated goal. The task plan notes step 2 as done and step 3 (remove v1 fallback) as also done, so this is an implementation-path divergence that the plan itself authorized.
   *Recommendation:* No code change required — removing the flag entirely rather than keeping a defaulted-true flag is the cleaner outcome. Consider updating the task description in v2-deferred-backlog-plan.md to note that the flag was never needed in the final implementation since step 2 and step 3 landed together.

40. [ ] **spec-alignment-checker** | `test/REGRESSION.md:19` | specification-gap
   REGRESSION.md §'Why two surfaces' (lines 19-36) still states 'dispatch in `webserver_impl::finalize_answer` still walks the v1 registration maps' and 'The v2 3-tier table (TASK-027) is shadow-populated ... but does not yet drive requests — that cutover is TASK-036.' TASK-053 has already performed this cutover; the section describes a state that no longer exists. The same document's §3 ('Custom-regex parameter constraints', line 115) compounds this: 'end-to-end dispatch is still served by v1 maps (TASK-036 has not landed), so user-visible 404 behavior is unchanged' — again describing the old pre-cutover state.
   *Recommendation:* Update REGRESSION.md §'Why two surfaces' to reflect the post-TASK-053 reality: the v2 3-tier table now drives all HTTP dispatch, the v1 maps survive only as registration-time bookkeeping, and TASK-036 is now superseded by TASK-053. Update §3 note similarly.

41. [ ] **test-quality-reviewer** | `test/bench_route_lookup.cpp:218` | non-deterministic
   The comment at line 218 states 'The cache is sized so that 16 entries blow it out repeatedly, forcing the radix walk on every lookup.' This is factually incorrect: the bench webserver uses the default route_lru_cache size of 256 entries (ROUTE_CACHE_MAX_SIZE), and the 16 rotating kPaths paths will all fit in the cache simultaneously. After the first 16-entry warmup round the radix bench measures cache-warm hits (LRU lookup), not cold radix-tier walks as the comment claims. The benchmark ceiling of 5 µs/lookup is generous enough to pass either way, so no false fail occurs — but the measurement is not what the comment describes.
   *Recommendation:* Either: (a) correct the comment to reflect that the 16-path rotation yields mostly cache-warm measurements and the ceiling is set conservatively enough to accommodate that, or (b) pre-clear the cache before each bench round (if route_cache::clear() is accessible via webserver_test_access) to force genuine radix-tier walks. Option (a) is the minimal fix; option (b) would give a more informative cold-tier measurement.

42. [ ] **test-quality-reviewer** | `test/bench_route_lookup.cpp:223` | missing-test
   The radix-tier bench uses 16 distinct paths to defeat the LRU cache. The comment says 'the cache is sized so that 16 entries blow it out repeatedly', but the actual cache capacity is not validated here. If the LRU cache were resized to 64 entries in a future task, the bench would silently measure warm-cache latency instead of radix-tier latency, making ceiling (b) pass trivially without catching a genuine radix regression.
   *Recommendation:* Add a brief comment stating the minimum cache size that must hold for the bust-out strategy to work (e.g., 'Assumes cache capacity < 16; see detail::route_cache default capacity'). Alternatively assert or static_assert the cache capacity from the bench setup. This is a bench-only concern and does not affect the correctness of `make check`.

43. [ ] **test-quality-reviewer** | `test/integ/basic.cpp:1239` | aaa-violation
   In `regex_matching_arg_custom`, the `http_code` variable is declared inside the Act block (after `curl_easy_perform`) and then used in an Assert (`LT_ASSERT_EQ`), but `s` is both used in the Arrange (`curl_easy_setopt`) and then checked in Assert (`LT_CHECK_EQ(s, "")`). The `http_code` declaration between the curl call and the assertions slightly obscures the Act/Assert boundary. This is a pre-existing style and not introduced by this task; the task only added comments around this block.
   *Recommendation:* Declare `http_code` before `curl_easy_perform` alongside `s` to keep Arrange / Act / Assert phases visually separated. Minor style only; does not affect test correctness.

44. [ ] **test-quality-reviewer** | `test/unit/v2_dispatch_contract_test.cpp:146` | naming-convention
   The test name `parameterized_route_extracts_capture` is good, but `prefix_route_marks_is_prefix_true` and `exact_route_marks_is_prefix_false` describe internal struct fields rather than the observable user contract. For tests that drive real HTTP round-trips (not internal state probes), names closer to user-facing semantics are preferred.
   *Recommendation:* Consider renaming to `prefix_route_sub_path_resolves_and_is_flagged_prefix` and `exact_route_only_matches_exact_path_and_is_flagged_non_prefix`. This is stylistic; the current names are not harmful.

45. [ ] **test-quality-reviewer** | `test/unit/v2_dispatch_contract_test.cpp:151` | slow-test
   Each of the four contract tests calls `std::this_thread::sleep_for(std::chrono::milliseconds(50))` to wait for the server to start. On a loaded CI host this may be insufficient (flaky) or unnecessarily delays 4 × 50 ms = 200 ms of wall time per run.
   *Recommendation:* Replace the fixed sleep with a lightweight poll loop that attempts `curl_easy_perform` and retries until the response code is non-zero (or a short deadline elapses). This both removes the flake risk and avoids over-sleeping on fast hosts. The existing `do_get` helper can be wrapped in a small `wait_for_server(port)` utility.

46. [ ] **test-quality-reviewer** | `test/unit/v2_dispatch_contract_test.cpp:162` | implementation-coupling
   Tests 2, 3, and 4 use a `hook_probe` struct populated via a `route_resolved` hook to verify internal `is_prefix` and `resource != nullptr` flags. This is appropriate here because the hook contract itself is part of the published public surface (TASK-048/050 defined it). However, the probe's `last_matched_engaged` / `last_is_prefix` / `last_resource_non_null` fields use `std::atomic<bool>` stores without an acquire fence on the reading side (the test reads them after `ws.stop()` without a `std::atomic_thread_fence`). In practice `ws.stop()` acts as a synchronization barrier, but this relies on an implicit ordering guarantee that is not documented.
   *Recommendation:* Add a comment explaining that `ws.stop()` provides the happens-before edge that makes the relaxed atomic reads after it safe, or switch reads to `std::memory_order_acquire` to make the intent explicit.

47. [ ] **test-quality-reviewer** | `test/unit/v2_dispatch_contract_test.cpp:259` | naming-convention
   The iter-1 fix adds probe.last_is_prefix.store() inside the hook lambda of method_mismatch_still_resolves_route (line 270) and asserts LT_CHECK_EQ(probe.last_is_prefix.load(), false) at line 298. This is correct and closes the coverage gap (the 405 code path was previously not asserting is_prefix). The hook_probe struct already had last_is_prefix declared (line 156), so the field was always present but never stored in this test's hook. The fix is structurally complete and no further changes are needed; this finding is informational to note the iter-1 change is verified correct.
   *Recommendation:* No action required. Finding is informational: the is_prefix assertion in the 405 path is correct and the hook_probe field was already declared. The fix is complete.

48. [ ] **test-quality-reviewer** | `test/unit/v2_dispatch_contract_test.cpp:272` | missing-test
   The four pinned invariants cover parameterized capture, prefix hit, exact hit, and method mismatch. The task explicitly flips three `basic.cpp` tests to accept that v2 no longer enforces per-segment regex constraints — i.e., `/items/text` now resolves (200) instead of 404. There is no contract test in `v2_dispatch_contract_test.cpp` pinning the 404 / not-found path for genuinely unregistered routes, which is the other side of the coin: if the dispatch path broke the miss path (always returning 200), none of the four tests would catch it.
   *Recommendation:* Add a fifth test `unregistered_path_returns_404` that requests a path with no registered handler and asserts `status == 404`. This closes the miss-path gap in the safety net and costs one curl round-trip.

49. [ ] **test-quality-reviewer** | `test/unit/v2_dispatch_contract_test.cpp:61` | non-deterministic
   wait_for_server_ready polls with a 50 ms CURLOPT_TIMEOUT_MS per attempt and sleeps 5 ms between attempts up to a 3 000 ms deadline. Each of the four tests binds and unbinds the same port 8231 sequentially within the same binary. After ws.stop() the OS may hold the port in TIME_WAIT for a brief interval; the next test's ws.start(false) could fail to bind if the socket lingers. The polling helper only probes readiness but does not set SO_REUSEADDR/SO_REUSEPORT on the server side — that depends on libmicrohttpd's defaults. If the MHD daemon silently fails to bind, ws.start() returns without error but do_get() gets CURLE_COULDNT_CONNECT throughout the polling loop and the test falls through to the first assertion with status 0.
   *Recommendation:* Insert a short wait (10–20 ms via std::this_thread::sleep_for) after ws.stop() before the next test's ws.start() to let the OS reclaim the port, or use distinct port offsets per test (PORT, PORT+1, PORT+2, PORT+3). The wait_for_server_ready pattern is otherwise the right fix for the original sleep(50 ms) fragility; this is a refinement of the inter-test port reuse concern, not the intra-test readiness concern.

50. [ ] **test-quality-reviewer** | `test/unit/v2_dispatch_contract_test.cpp:64` | test-isolation
   All four tests share the compile-time constant `PORT 8231`. Each test starts and stops its own `webserver` instance, but they run sequentially in a single process, so port re-use after `ws.stop()` can occasionally fail if the OS has not yet released the port (TIME_WAIT). The suite is registered as part of `make check` alongside other integration tests that also use fixed ports; a port collision from a sibling test is possible.
   *Recommendation:* Either use `SO_REUSEPORT` / `SO_REUSEADDR` (already configured by libmicrohttpd in most builds) and accept the risk as known, or, if the test framework allows, pick a port that is not used by any other suite. Document the chosen port in a central registry comment (the way other suites reference their ports), or use `create_webserver(0)` with a post-bind port query if the API supports it.
