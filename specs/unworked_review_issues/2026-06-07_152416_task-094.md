# Unworked Review Issues

**Run:** 2026-06-07 15:24:16
**Task:** TASK-094
**Total:** 41 (0 critical, 3 major, 38 minor)

## Major

1. [x] **code-simplifier** | `test/integ/threadsafety_stress.cpp:151` | code-structure
   cas_witness_resource is a separate class whose body is byte-for-byte identical to noop_resource (same base class, same render_get override returning "ok"). The only reason cited in the comment is "so a single stack frame can re-arm the lazy hook_table_ CAS", but that property follows from constructing any http_resource subclass on the stack — noop_resource already satisfies it. Having two identical classes introduces needless repetition (clean-code smell #5) and forces readers to wonder whether the distinction is meaningful.
   *Recommendation:* Remove cas_witness_resource and replace every use with noop_resource. In Sub-test D: `noop_resource r;`. In the switch cases 6/7 in driver_body: `noop_resource transient;`. The comment about "dedicated subclass" can be collapsed into one sentence explaining the stack-local allocation pattern. This reduces the class count by one without any behavioral change.
   *Worked 2026-07-07 (chore/unworked-major-issues):* cas_witness_resource removed; noop_resource used at all three sites; rationale collapsed to one sentence.

2. [x] **test-quality-reviewer** | `test/integ/threadsafety_stress.cpp:355` | implementation-coupling
   Op 6 in driver_body creates a fresh stack-local `cas_witness_resource transient` on every invocation. Because each call to case 6 allocates its own isolated resource with hook_table_==nullptr, no two concurrent case-6 invocations can race on the *same* null slot. The contended-null CAS window in ensure_table() requires at least two threads to call add_hook on the **same** resource instance before the winner's compare_exchange publishes. The test accumulates cas_resource_hook_ok but this counter only shows that ensure_table() completed, not that the CAS was contended. The actual null-CAS contention for op 6 is exercised only in the unlikely event that the MHD thread pool concurrently dispatches two op-6 requests that happen to share the same handler invocation — which is impossible because each request gets its own transient local. Sub-test D correctly targets a shared resource per-iteration, but the comment at line 346 claiming op 6 exercises 'the full three-tier order' via the contended-null CAS is overclaimed for the handler path.
   *Recommendation:* For Sub-test A, change op 6 to call add_hook on `shared_cas_resource` (same resource used by op 7) rather than a transient stack-local. The distinction between op 6 and op 7 in Sub-test A then becomes the null-vs-installed race driven by relative timing across 16 concurrent clients, not a structural per-invocation isolation. Alternatively, keep a small array of shared cas_witness_resource instances (e.g. 4) and rotate among them so multiple threads can race on the same slot. Update the comment at lines 346-354 to accurately reflect that the contended-null guarantee in Sub-test A comes from Sub-test D, not from handler-dispatch timing.
   *Worked 2026-07-07 (chore/unworked-major-issues):* Op 6 now rotates across a shared std::array<noop_resource, 4> pool (outliving all requests) so concurrent clients genuinely contend on the same null slot; comments updated to credit Sub-test D for the guaranteed contended-null coverage.

3. [x] **test-quality-reviewer** | `test/integ/threadsafety_stress.cpp:989` | non-deterministic
   The `contended_window_iters` counter (line 989-991) uses `entered_after_latch >= 2` as a proxy for CAS contention. This checks that at least 2 workers returned from `arrive_and_wait()` in the same iteration — which is trivially true for every iteration with kThreads=8 since all 8 threads are released simultaneously by the latch. The counter does NOT prove that two threads simultaneously called ensure_table() before the CAS winner published. The latch release is a synchronization barrier: all workers atomically unblock, but OS scheduling may serialize them entirely. On a single-core CI host or under heavy TSan slowdown, the first thread may complete the entire add_hook call (including the CAS) before the second thread even enters ensure_table(). Consequently, contended_window_iters will reach kRepeats on nearly any run regardless of whether genuine CAS contention occurred. The LT_CHECK_GT at line 1013 is essentially always satisfied and provides weak falsifiability.
   *Recommendation:* To produce a genuine CAS-loser count, gate it on the actual CAS outcome inside ensure_table(). The simplest approach is to add an optional `std::atomic<int>* cas_loser_count` output parameter to ensure_table() (behind #ifdef HTTPSERVER_TEST_INSTRUMENTATION) that is incremented on the compare_exchange_strong failure branch (line 107 of http_resource.cpp). The test then asserts `total_cas_losers > 0` across all iterations. Alternatively, document explicitly that the latch-and-8-threads approach is a scheduling-rationale argument (not a structural counter) and add a comment explaining why: with kThreads=8 and a TSan-serialized scheduler, at least some iterations will overlap at the CAS site because TSan's instruction-level interleavings are exhaustive enough over 64 iterations.
   *Worked 2026-07-07 (chore/unworked-major-issues):* Option (b): counter and gate kept, but re-documented with an explicit HONESTY NOTE as a scheduling proxy (no HTTPSERVER_TEST_INSTRUMENTATION hooks exist in src/http_resource.cpp, so option (a) was not trivially available).
## Minor

4. [ ] **architecture-alignment-checker** | `src/httpserver/http_resource.hpp:397` | pattern-violation
   The CONTRACT comment on hook_table_raw_() correctly documents the synchronisation precondition and is in the right file. However, the comment says 'ensure_table() itself uses the deprecated free-function std::atomic_*_explicit overloads' — the word 'deprecated' is a C++26 characterisation; under the project's C++20 floor (DR-001) these overloads are not yet deprecated and calling them 'deprecated' may confuse future readers who build only with -std=c++20. The cross-reference to TASK-070 for the planned migration to std::atomic<std::shared_ptr<T>> is fine, but using 'deprecated' conflates the C++26 status with the current build target.
   *Recommendation:* Replace 'deprecated free-function std::atomic_*_explicit overloads' with 'non-member std::atomic_*_explicit overloads (deprecated as of C++26)' or similar phrasing that makes the C++26 scope explicit. This keeps the comment accurate for both C++20 and future C++26 builds.

5. [ ] **architecture-alignment-checker** | `test/integ/threadsafety_stress.cpp:343` | pattern-violation
   Case 6 comment claims 'this case exercises the full three-tier lock order: route_table_mutex_ (shared, this thread is a reader inside dispatch) -> resource hook_table_ (this thread's CAS in ensure_table()) -> server-wide hook_table (other threads' webserver::add_hook in cases 4/5)'. The third tier of the documented lock order (resource hook_table_ -> server-wide hook_table_mutex_) requires both the resource hook_table_ AND the server-wide hook_table_mutex_ to be held on the SAME thread. In case 6, the server-wide hook_table operations happen on different threads (cases 4/5), not on the same thread as the resource hook_table CAS. This is a concurrent interleaving, not a same-thread lock ordering. The comment overstates the claim: it exercises concurrent interleaving of all three tiers across threads, which is valuable for TSan, but it is not the same as single-thread acquisition of the three-tier lock order documented in §5.6. Sub-test D's isolation of the per-resource CAS is the cleaner proof of the middle tier.
   *Recommendation:* Adjust the case 6 comment to clarify that the three-tier lock-order interleaving is cross-thread concurrent (TSan-observable) rather than single-thread sequential acquisition. This avoids overstating the architectural claim and aligns with the actual concurrency shape being tested.

6. [ ] **architecture-alignment-checker** | `test/integ/threadsafety_stress.cpp:907` | component-boundary
   The test uses hook_table_raw_() — a method gated on HTTPSERVER_COMPILATION — to observe the internal hook_table_ pointer directly. This works because test/Makefile.am defines HTTPSERVER_COMPILATION via AM_CPPFLAGS for all test TUs. However, this means the integration test is compiled with the same internal-symbol access as library implementation files, which slightly blurs the boundary between the library's private implementation surface and test code. The architecture doc (§5.5) specifies that HTTPSERVER_COMPILATION gating is for library translation units only, not for consumer/test code. The TASK-094 task description explicitly permits an '#ifdef HTTPSERVER_TEST_INSTRUMENTATION counter' as the allowed instrumentation pattern, suggesting a narrower, test-specific gate would be more architecturally precise. That said, the existing test corpus already uses this pattern (noted in Makefile.am line 291 and elsewhere), so this is consistent with established convention rather than a novel violation.
   *Recommendation:* Consider whether hook_table_raw_() should be gated on a dedicated HTTPSERVER_TEST_INSTRUMENTATION macro (as suggested in TASK-094's constraint) rather than HTTPSERVER_COMPILATION, to preserve the conceptual separation between 'library compilation unit' access and 'test instrumentation' access. This is a minor convention alignment point, not a correctness issue.

7. [ ] **code-quality-reviewer** | `test/integ/threadsafety_stress.cpp:151` | code-elegance
   cas_witness_resource is structurally identical to noop_resource — both subclass http_resource and provide the same trivial render_get implementation. The only stated difference is intent (stack-frame re-arm in Sub-test D vs MHD handler role), which could be expressed via a comment on noop_resource rather than a dedicated class.
   *Recommendation:* Consider removing cas_witness_resource and aliasing it as noop_resource (or adding a using-alias), with a comment explaining the two usage contexts. This eliminates the duplicate render_get body and makes the DRY violation explicit. If the subclass will acquire distinct state in a future task, keep it but note the planned divergence.

8. [ ] **code-quality-reviewer** | `test/integ/threadsafety_stress.cpp:151` | code-readability
   cas_witness_resource is structurally identical to noop_resource (same render_get body, no added state). The subclass exists solely to allow stack-local destruction in Sub-test D, but the comment at line 149 accurately explains that rationale. The duplication is intentional and documented, though it violates DRY at the surface level.
   *Recommendation:* If a future cleanup pass touches either class, consider making noop_resource the single concrete class and renaming it or adding a using-alias for the Sub-test D role. Not required now — the comment justifies the duplication.

9. [ ] **code-quality-reviewer** | `test/integ/threadsafety_stress.cpp:355` | readability
   In case 6 of driver_body, the hook_handle `h` is immediately silenced with `(void)h;` and then falls out of scope, dropping the registration. The intent (hook_handle dtor triggers remove) is correct but the named-then-suppressed pattern is slightly opaque — a reader must reason that the dtor is the point, not the handle value.
   *Recommendation:* Either add a brief inline comment above `(void)h;` explicitly stating "handle dropped here; dtor calls remove()" — or restructure as `{ auto _ = transient.add_hook(...); }` with a comment on the braced block, which is the idiomatic C++ RAII-scope pattern and signals intent without a separate suppression statement.

10. [ ] **code-quality-reviewer** | `test/integ/threadsafety_stress.cpp:855` | code-readability
   The comment block for Sub-test D states 'Total CAS races per run = kRepeats * kThreads = 64 * 8 = 512'. This conflates the number of add_hook calls with the number of CAS attempts; each call always attempts the CAS once, but only the first call on a fresh resource actually races. The statement is not wrong but slightly imprecise — readers familiar with CAS loops may expect it to mean successful CAS wins, not call count.
   *Recommendation:* Clarify the comment to 'Total add_hook calls racing the null CAS slot per run = kRepeats * kThreads = 64 * 8 = 512; at most 1 per iteration wins the CAS, the rest either lose or short-circuit.' Minor — the existing wording is unlikely to mislead a careful reader.

11. [ ] **code-quality-reviewer** | `test/integ/threadsafety_stress.cpp:887` | test-coverage
   The falsifiable gate `LT_CHECK_GT(total_post_race_nonnull.load(), 0)` checks that at least one iteration installed a table, but the comment says 'every iteration must drive at least one successful add_hook installing the table'. The check does not verify kRepeats installations — it would pass even if only one out of 64 iterations succeeded.
   *Recommendation:* Strengthen the gate to `LT_CHECK_EQ(total_post_race_nonnull.load(), kRepeats)` (or `LT_CHECK_GT(..., kRepeats - 1)` if LT_CHECK_EQ does not exist) to match the documented acceptance requirement, or align the comment to accurately reflect that the gate is a lower-bound sentinel rather than a per-iteration invariant.

12. [ ] **code-quality-reviewer** | `test/integ/threadsafety_stress.cpp:903` | readability
   The `iter` loop variable is declared but never used inside the loop body — all iteration state is through `r`, `start`, `entered_after_latch`, `handles_mtx`, `handles`, and `workers`. The unused variable is harmless but noise.
   *Recommendation:* Either use `iter` for the pre-race witness print or structured logging (e.g. tagging TSan output with iteration number for easier diagnosis), or replace `for (int iter = 0; iter < kRepeats; ++iter)` with a canonical `for (int i = 0; i < kRepeats; ++i)` and suppress via `(void)i;` with a note — or simply leave as-is if the compiler does not warn (most modern compilers will not warn on a used loop variable).

13. [ ] **code-quality-reviewer** | `test/integ/threadsafety_stress.cpp:965` | code-elegance
   The post-race burst phase in Sub-test D spawns a second wave of threads (kBurstThreads=4, kBurstOpsPerThread=8) but kBurstThreads and kBurstOpsPerThread are defined inside the loop body as constexpr locals. These are logically test parameters and belong alongside kRepeats/kThreads at the top of the test body for consistency and discoverability.
   *Recommendation:* Move `constexpr int kBurstThreads = 4` and `constexpr int kBurstOpsPerThread = 8` to the top of the test body alongside `constexpr int kRepeats = 64` and `constexpr int kThreads = 8`. This groups all tunable test parameters in one place — matching the pattern used in Sub-test C.

14. [ ] **code-quality-reviewer** | `test/integ/threadsafety_stress.cpp:989` | readability
   The contended_window_iters counter is incremented after workers join, based on `entered_after_latch.load() >= 2`. However entered_after_latch counts workers that returned from arrive_and_wait() — not workers that entered add_hook simultaneously. On a uniprocessor or under TSan serialisation, all 8 workers may increment entered_after_latch sequentially (each context-switches after the other finishes), making contended_window_iters potentially unreliable as 'structural proof of the contended-null window'. The comment in the header (lines 898-901) acknowledges this is a proxy metric, but the check `LT_CHECK_GT(contended_window_iters.load(), 0)` could be fragile on slow/single-core hosts.
   *Recommendation:* Add a `// Non-deterministic on single-core / heavily serialised builds` note above the LT_CHECK_GT gate and document that it is a best-effort witness rather than a hard invariant — or gate the check on `std::thread::hardware_concurrency() >= 2` to avoid flaking on single-core CI workers.

15. [ ] **code-quality-reviewer** | `test/integ/threadsafety_stress.cpp:989` | test-coverage
   contended_window_iters gate (LT_CHECK_GT at line 1022) asserts that at least one iteration observed >= 2 workers entering add_hook concurrently. On a heavily loaded single-core CI host all workers could in theory serialize and no iteration would qualify, causing the gate to fire as a false negative. In practice this is extremely unlikely across 64 iterations with 8 threads, but it is a theoretically flaky gate.
   *Recommendation:* Document in the comment that this gate is probabilistic and may occasionally miss on single-core hosts (similar to the existing TSan comment pattern). Alternatively lower the threshold to >= 1 (checking that at least one worker entered — always true), and rely on the TSan clean run as the structural proof. Current threshold is fine for multi-core CI.

16. [ ] **code-simplifier** | `test/integ/threadsafety_stress.cpp:151` | code-structure
   cas_witness_resource::render_get is identical to noop_resource::render_get. The subclass comment correctly notes render_get is unreachable in Sub-test D, so the override carries no functional distinction from noop_resource. The subclass adds only a named type for readability, which a type alias achieves without the duplicated body.
   *Recommendation:* Replace `class cas_witness_resource : public ht::http_resource { ... render_get ... }` with `using cas_witness_resource = noop_resource;`. The alias keeps the distinct name visible at every use site in Sub-test D while eliminating the duplicate render_get implementation.

17. [ ] **code-simplifier** | `test/integ/threadsafety_stress.cpp:355` | code-structure
   In switch case 6, the hook_handle `h` is immediately suppressed with `(void)h;` and then falls out of scope. The variable name and the suppression cast add noise. Compare with case 7, which calls `h.remove()` explicitly — the asymmetry makes readers ask whether the difference is intentional.
   *Recommendation:* Either (a) call `h.remove()` in case 6 as well, mirroring case 7 and removing the `(void)h` cast, or (b) replace the two lines with a single statement: `transient.add_hook(...).remove();`. Option (b) makes both cases structurally uniform. Note: the task prompt says do not reduce contention — this change does not affect the CAS race because the race occurs inside `add_hook`; the handle disposition afterward is irrelevant to the window.

18. [ ] **code-simplifier** | `test/integ/threadsafety_stress.cpp:887` | naming
   The constants `kRepeats = 64` and `kThreads = 8` are defined inside the test body and appear in the summary comment above the test (`kRepeats * kThreads = 64 * 8 = 512`). The comment hard-codes the product 512 rather than deriving it from the named constants, so if either constant changes the comment becomes stale.
   *Recommendation:* Remove the product from the comment and instead express it as a formula: `Total CAS races per run = kRepeats * kThreads`. Alternatively, define a named constant `constexpr int kTotalCasRaces = kRepeats * kThreads;` and reference it in both the comment and the diagnostic `std::cout` at the bottom so there is a single source of truth.

19. [ ] **code-simplifier** | `test/integ/threadsafety_stress.cpp:965` | code-structure
   The burst phase (lines 965-981) spawns `kBurstThreads = 4` threads each doing `kBurstOpsPerThread = 8` ops, totalling 32 add/remove pairs. Both constants are defined at the point of use inside the loop body, but `kBurstThreads` and `kBurstOpsPerThread` are used only once and their values have no relationship to any other named constant in the test. Declaring two `constexpr` names for values that appear exactly once inside a tight block adds more lines than they save in comprehension.
   *Recommendation:* Inline the literals as `4` and `8` directly in the loop bounds with a brief comment, or lift `kBurstThreads` and `kBurstOpsPerThread` to the top of the test body alongside `kRepeats` and `kThreads` so all tuning knobs for Sub-test D are visible together at a glance. The current placement (inside the loop, after workers are already defined) makes the knobs harder to find when adjusting the test.

20. [ ] **housekeeper** | `/Users/etr/progs/libhttpserver/specs/architecture/05-cross-cutting.md:92` | architecture-not-updated
   Section 5.6 documents the lock order claim ('route_table_mutex_ → resource's hook_table_mutex_ → server-wide hook_table_mutex_') and references 'verified by bench_hook_overhead' for zero-cost-when-unused. The TSan stress coverage of this three-tier lock order (now proven by TASK-094 Sub-test D) is not cited, leaving the claim without a test reference. This is low priority since the claim was already made; the new test just upgrades it from asserted to verified.
   *Recommendation:* Add a parenthetical to the lock-order sentence in §5.6 noting that Sub-test D of threadsafety_stress (TASK-094) provides TSan validation of the full three-tier order.

21. [ ] **housekeeper** | `/Users/etr/progs/libhttpserver/specs/architecture/09-testing.md:10` | architecture-not-updated
   Section 9, item 6 describes the thread-safety stress test as exercising 'register_path / unregister_path / block_ip / unblock_ip from within handlers'. It does not mention the hook-bus tiers (added by TASK-052) or the new per-resource CAS phase (added by TASK-094). The description now understates what the test actually covers.
   *Recommendation:* Extend item 6 in /Users/etr/progs/libhttpserver/specs/architecture/09-testing.md to mention that the stress test also exercises the server-wide hook bus (TASK-052) and the per-resource hook bus CAS path (TASK-094 Sub-test D) under TSan.

22. [ ] **housekeeper** | `specs/tasks/M7-v2-cleanup/_index.md:37` | documentation-stale
   TASK-070 row still shows 'Backlog' rather than 'Blocked' in the task table. The TASK-070.md file itself correctly records Status: Blocked (with the libc++ P0718R2 blocker section), but the index row does not reflect that.
   *Recommendation:* Change the TASK-070 row status in _index.md from 'Backlog' to 'Blocked' to match the TASK-070.md file and project memory note about the libc++ gap.

23. [ ] **performance-reviewer** | `test/integ/threadsafety_stress.cpp:315` | algorithmic-complexity
   The HookBag recycling path in Sub-test A's driver (case 4) does erase(begin()) on a std::vector, which is O(kCap) = O(256) element-shifting each time the cap is hit. Under the 16-client 60-second load this is called frequently: if the net rate of op-4 calls exceeds op-5 removals, the cap is hit repeatedly. At kCap=256 this is a 256-element memmove per recycle event — negligible in wall-clock terms, but the comment in the code itself flags the concern and suggests std::deque. This is a pre-existing pattern (TASK-052) and the diff does not touch it, but the new ops-6/7 added by TASK-094 reduce the probability that op-4 wins out, which actually makes this less of a concern than before.
   *Recommendation:* No action required for TASK-094; the existing comment already documents the deque suggestion. The TASK-094 changes do not worsen this path.

24. [ ] **performance-reviewer** | `test/integ/threadsafety_stress.cpp:903` | memory-allocation
   Inside the kRepeats=64 iteration loop, each iteration allocates a std::vector<ht::hook_handle> (handles), a std::vector<std::thread> (workers, kThreads=8), and a second std::vector<std::thread> (burst, kBurstThreads=4). That is three heap allocations per iteration, 192 total, all inside a tight loop that also creates and destroys a cas_witness_resource on the stack. Under TSan, every allocation and deallocation incurs shadow-memory tracking overhead. The total object count is modest (64*20 = 1280 objects) and bounded, so this does not materially affect CI wall-clock, but pre-sizing workers and burst outside the loop and reusing them (or at least moving the reserve() calls to before the loop) would reduce per-iteration allocator pressure by about 30%.
   *Recommendation:* Consider declaring workers and burst with workers.reserve(kThreads) / burst.reserve(kBurstThreads) before the loop body and using resize(0) + emplace_back inside the loop, or alternatively using a fixed-size std::array<std::thread, kThreads> on the stack to eliminate heap allocations entirely for the worker arrays.

25. [ ] **performance-reviewer** | `test/integ/threadsafety_stress.cpp:950` | memory-allocation
   In the Sub-test D worker lambda (line 950-955), each of the kThreads=8 workers constructs a std::function<void(const ht::request_completed_ctx&)> holding a stateless captureless lambda. Stateless lambdas converted to std::function can still trigger a small heap allocation for the type-erased callable on some standard library implementations (libc++ does apply the small-buffer optimisation for trivially-copyable callables, but this is implementation-defined). Multiplied by kRepeats=64 iterations * kThreads=8 workers * (1 CAS phase + kBurstThreads=4 * kBurstOpsPerThread=8 = 32 burst ops), the total std::function constructions are approximately 64*(8+32*4) = 64*136 = 8704. Under TSan shadow tracking this is acceptable but the burst inner loop at line 972-978 reconstructs a fresh std::function on every one of the kBurstOpsPerThread=8 iterations per burst thread. Hoisting the lambda construction outside the inner op loop and passing it by const-ref to add_hook would eliminate 64*4*8 = 2048 redundant type-erasure operations.
   *Recommendation:* In the burst lambda body, hoist the std::function construction above the for loop: define it once as a local variable before `for (int op = 0; op < kBurstOpsPerThread; ++op)` and reuse the same callable for each add_hook call.

26. [ ] **security-reviewer** | `src/httpserver/http_resource.hpp:410` | data-integrity
   The CONTRACT comment correctly documents that hook_table_raw_() is safe only after all mutating threads have been joined (a happens-before edge over ensure_table()). The comment also notes the deprecated free-function std::atomic_*_explicit path and defers a proper acquire-load to TASK-070. This is an acceptable interim posture given the Apple Clang / libc++ gap blocking TASK-070, but the non-atomic read path will remain a latent TSan annotation burden until TASK-070 lands.
   *Recommendation:* No action required now. When TASK-070 migrates hook_table_ to std::atomic<std::shared_ptr<T>>, replace the .get() call in hook_table_raw_() with an acquire load (hook_table_.load(std::memory_order_acquire).get()) and drop the CONTRACT caveat.

27. [ ] **security-reviewer** | `test/integ/threadsafety_stress.cpp:241` | input-validation
   Query parameters 'op' and 'i' are parsed with std::stoi inside a catch(...) that silently swallows all exceptions and falls through to compute slot/ip with the default-initialized values (op=0, i=0). While the test context means this is not a security risk in production, the silent swallow means a malformed URL biases all operations toward slot 0 and ip 198.51.100.0, potentially reducing stress coverage without any diagnostic output.
   *Recommendation:* Consider logging the parse failure to std::cerr before continuing (mirrors the pattern used for unexpected unregister_path exceptions at line 288) so test anomalies are visible in CI logs.

28. [ ] **security-reviewer** | `test/integ/threadsafety_stress.cpp:391` | resource-exhaustion
   `stress_seconds()` caps the env-var value at 3600 s (1 hour), which is correctly bounded. However, in Sub-test A the `HTTPSERVER_STRESS_SECONDS` value also implicitly controls the watchdog in Sub-test C (line 775, same `stress_seconds()` call). If the env-var is set to 3600 for Sub-test A, Sub-test C will also run its wall-clock watchdog for up to 3600 s, potentially consuming far more CI time than intended. The two sub-tests have different scale characteristics (Sub-test C is bounded primarily by `kMaxRoutesPerParent`, not wall-clock), but an operator setting `HTTPSERVER_STRESS_SECONDS=3600` for Sub-test A may not expect Sub-test C to also run for an hour. There is no separate cap or env-var for Sub-test C. CWE-400 (uncontrolled resource consumption in CI).
   *Recommendation:* Introduce a second env-var (e.g. `HTTPSERVER_SEGMENT_STRESS_SECONDS`) for Sub-test C's watchdog, or document clearly in the file-level comment that `HTTPSERVER_STRESS_SECONDS` applies to both A and C and the combined CI budget should account for both. The current 100-cap on samples (line 800) provides a partial safety valve, but a CI run with a very slow host and a high env-var value could still exceed budget.

29. [ ] **security-reviewer** | `test/integ/threadsafety_stress.cpp:591` | resource-management
   In Sub-test B (child process branch), `curl_easy_init()` at line 590 is called but its return value is not checked for NULL before being passed to `curl_easy_setopt` at line 591. If `curl_easy_init()` returns NULL (resource exhaustion in the child process after the fork), the subsequent `curl_easy_setopt` calls will be passed a NULL handle, which is undefined behaviour per libcurl's documentation. The child process is isolated and any crash is contained by the parent's waitpid logic, but the crash would be attributed to a curl null-deref rather than the documented DR-008 contract, potentially causing the parent's `WIFSIGNALED` / `WIFEXITED` checks to pass for the wrong reason. CWE-476 (NULL pointer dereference).
   *Recommendation:* Add a NULL check after `curl_easy_init()` in the child process branch (mirror the pattern used in the Sub-test A client threads at line 457: `if (!curl) return;`). In the child's case, `std::_Exit(kExitCodeCurlCompleted)` or a distinct sentinel would be the appropriate bail-out so the parent can distinguish a curl-init failure from the DR-008 observation.

30. [ ] **spec-alignment-checker** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-094/src/httpserver/http_resource.hpp:398` | specification-gap
   The iter1 fixer added a CONTRACT documentation comment to src/httpserver/http_resource.hpp (the header), not src/http_resource.cpp (the implementation). The task's action item 6 says 'Do not modify src/http_resource.cpp semantics' and the component field names both files. The header edit is a documentation-only addition (no code, no behavior change) and the constraint letter-for-letter names the .cpp file, not the .hpp. The spirit of the constraint — keeping semantics of ensure_table() and the hook_table_ field unchanged — is fully respected. However, the component field listing 'src/http_resource.cpp' alongside the test file could be read as intent to scope changes narrowly, and the header edit was not explicitly called for by any action item. This is a minor scope-creep observation, not a violation.
   *Recommendation:* No change required. The CONTRACT comment correctly documents a real synchronization hazard on hook_table_raw_() and aids safe use of the accessor in Sub-test D. The TASK-094.md task file could optionally note the header addition in a post-implementation note, but this is cosmetic.

31. [ ] **spec-alignment-checker** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-094/test/integ/threadsafety_stress.cpp:989` | specification-gap
   Acceptance criterion 2 requires 'an instrumented counter shows >=1 CAS-loser' or 'a documented design rationale explains why the schedule provably hits the contended-null window.' The implementation counts iterations where >=2 threads entered add_hook concurrently (contended_window_iters) rather than counting actual CAS losers. This is the structural-witness branch the task permits ('inspecting hook_table_raw_() from a witness thread'), and the design rationale is documented inline at lines 940-953, but the relationship between 'entered_after_latch >= 2' and 'a CAS loser occurred' is implicit (concurrent entry is a necessary but not sufficient condition for a CAS loss on any given iteration). The task text explicitly allows design-rationale documentation as the alternative, so this is met, but a note acknowledging that contended_window_iters is a proxy for CAS-loser presence rather than a direct count would tighten the evidence.
   *Recommendation:* No code change required. Consider adding a one-line comment near the contended_window_iters gate explaining that concurrent entry into add_hook on a null slot is the required precondition for a CAS loser to occur — this makes the structural-witness reasoning explicit for future readers.

32. [ ] **spec-alignment-checker** | `test/integ/threadsafety_stress.cpp:1010` | acceptance-criteria
   The task acceptance criterion states 'New phase observably drives the CAS in ensure_table(): either an instrumented counter shows >= 1 CAS-loser across a representative run, or a documented design rationale explains why the schedule provably hits the contended-null window.' The implementation takes the second branch: no HTTPSERVER_TEST_INSTRUMENTATION counter was added to src/http_resource.cpp (which is correct per the 'do not modify semantics' constraint), and instead the design rationale is documented in comments. The observable evidence is hook_table_raw_() post-race non-null (total_post_race_nonnull counter) plus contended_window_iters (>= 2 workers entered add_hook concurrently). The contended_window_iters gate is a structural proxy for the CAS contention window: it proves >= 2 threads entered add_hook concurrently on the same null resource, which means at least one of them must have raced the CAS. This satisfies the 'documented design rationale' branch of the criterion. Note however that contended_window_iters counts iterations where entered_after_latch >= 2, not CAS losers specifically — the rationale should be accepted as it is a reasonable structural substitute.
   *Recommendation:* The approach is sound. No code change required. The plan rationale documented in comments is clear enough for future maintainers.

33. [ ] **spec-alignment-checker** | `test/integ/threadsafety_stress.cpp:346` | ears-requirement
   Op 6 in Sub-test A constructs a stack-local cas_witness_resource per handler invocation and calls add_hook on it from a single handler thread, then drops it. This does exercise the contended-null branch of ensure_table() but with zero concurrent callers racing on the same resource object (since each invocation creates a fresh transient resource). The genuine CAS contention (multiple threads competing on a null slot) is reserved for Sub-test D. The comment at line 341-355 describes this as exercising 'the full three-tier order' by relying on *other* concurrent handler threads holding route_table_mutex_ and calling webserver::add_hook — the lock-order interleaving is across threads and objects, not within one ensure_table() call. This is a valid but indirect exercise of the middle tier. The spec says 'sequence the new phase alongside the existing webserver-side hook ops so a single run exercises both lock-order tiers'; this is satisfied at the run level even if op 6 alone does not produce a race within ensure_table(). Sub-test D is the dedicated falsifiable gate for CAS contention.
   *Recommendation:* Consider adding a brief note to the op 6 comment clarifying that the CAS-contention guarantee within ensure_table() is provided by Sub-test D, not by op 6 alone, to avoid future reader confusion.

34. [ ] **spec-alignment-checker** | `test/integ/threadsafety_stress.cpp:null` | acceptance-criteria
   The task acceptance criterion states 'grep -n "resource hook_table" test/integ/threadsafety_stress.cpp returns at least one comment line that accurately describes the new coverage'. The grep does return hits (lines 35, 348, 875), so the literal criterion is met. However, the criterion text says the comment should replace the 'previous overclaim'. The original line 34 text 'The hook ops exercise the documented route_table_mutex_ -> resource hook_table -> server-wide hook_table lock order under TSan' was indeed an overclaim because only the server-side tier was actually exercised; the new comment correctly distinguishes Sub-test A (exercises server-wide tier AND resource middle tier concurrently via ops 6/7) from Sub-test D (standalone CAS race). The replacement is accurate and the criterion is fully met.
   *Recommendation:* No action needed. Noting only for traceability.

35. [ ] **test-quality-reviewer** | `test/integ/threadsafety_stress.cpp:1002` | implementation-coupling
   hook_table_raw_() post-race read (iter1 major: 'bypasses the atomic load'). Iter2 adds the CONTRACT comment to http_resource.hpp lines 399-409 documenting the happens-before requirement and explicitly notes this is a non-atomic .get() safe only after join(). The test code at line 1002 is correctly placed after both workers.join() (line 959) and burst.join() (line 981) and handles.clear() (line 987), which together establish the required happens-before edge. The concern is resolved; downgrading to minor for the note that once TASK-070 lands the accessor itself should use an acquire load.
   *Recommendation:* When TASK-070 migrates hook_table_ to std::atomic<shared_ptr<T>>, update hook_table_raw_() to use an acquire load (as the CONTRACT comment already calls out) and remove the happens-before precondition from callers.

36. [ ] **test-quality-reviewer** | `test/integ/threadsafety_stress.cpp:151` | naming-convention
   `cas_witness_resource` is a subclass of `noop_resource`'s sibling that adds no new state or behavior beyond the inherited render_get. The subclass exists solely to document intent (per the comment at line 149), which is a valid pattern, but the class declaration at lines 151-157 is identical in body to `noop_resource` (lines 138-144). Having two identical concrete classes derived from http_resource with different names but same body can mislead maintainers into thinking there is a behavioral distinction.
   *Recommendation:* Either collapse `cas_witness_resource` into a type alias (`using cas_witness_resource = noop_resource;`) or add a comment in the class body explicitly stating it is structurally identical to noop_resource and the separate type exists only for semantic disambiguation in test assertions.

37. [ ] **test-quality-reviewer** | `test/integ/threadsafety_stress.cpp:885` | missing-test
   Sub-test D exercises only the `hook_phase::request_completed` overload of add_hook in every worker thread (lines 950-953 and 972-976). The ensure_table() CAS is phase-agnostic — it runs identically regardless of which phase is requested — but the resource_hook_table's per-phase append functions are distinct code paths. The post-race burst (lines 969-981) uses the same phase. Coverage of the 'mixed phases racing on the same resource' scenario (e.g. before_handler + after_handler + request_completed all in-flight simultaneously) is absent from Sub-test D. This is a low-risk gap since ensure_table() completes before any phase-specific append runs, but concurrent phase-specific appends on the same table are a real code path.
   *Recommendation:* In the burst phase (lines 965-981), vary the hook_phase across burst workers (e.g. cycle through before_handler, after_handler, request_completed across the kBurstThreads) to exercise concurrent multi-phase appends on the installed resource_hook_table. This is a minor enhancement; the existing burst already exercises the shared_mutex write path.

38. [ ] **test-quality-reviewer** | `test/integ/threadsafety_stress.cpp:885` | naming-convention
   Sub-test D is named per_resource_add_hook_first_call_cas_no_data_race. The name accurately describes the primary concern (first-call CAS, no data race) but omits the mixed add/remove burst phase that exercises the middle tier after table installation. Minor — the comment block above the test adequately fills the gap.
   *Recommendation:* Optionally rename to per_resource_add_hook_cas_and_mixed_load_no_data_race or extend the inline comment to mention the burst phase.

39. [ ] **test-quality-reviewer** | `test/integ/threadsafety_stress.cpp:965` | excessive-setup
   The post-race burst phase (lines 965-981) spawns kBurstThreads=4 threads each performing kBurstOpsPerThread=8 sequential add_hook+remove cycles. Total burst ops = 32 per iteration, across kRepeats=64 iterations = 2048 burst ops total. Each burst op creates and destroys a std::thread, adding OS scheduling overhead to an already 64×8=512 CAS-race loop. The burst contributes to coverage of the middle-tier lock contention but the per-thread overhead may slow Sub-test D under TSan more than necessary.
   *Recommendation:* Consider reusing a fixed thread pool (e.g. std::barrier-based) across all burst iterations rather than spawning+joining 4 threads per iteration. With kRepeats=64 this is 256 thread-lifecycle events for the burst alone. Alternatively, reduce kBurstThreads to 2 and increase kBurstOpsPerThread to 16 to keep total ops constant while halving thread-spawn overhead. This is a performance concern, not a correctness issue.

40. [ ] **test-quality-reviewer** | `test/integ/threadsafety_stress.cpp:989` | implementation-coupling
   contended_window_iters counts iterations where >=2 threads incremented entered_after_latch after the latch released, not direct CAS-loser observations. On a heavily loaded host TSan run, all 8 workers may atomically increment entered_after_latch before any one of them actually enters ensure_table(), so the counter proves scheduler proximity but not that the CAS contended path was exercised. This was flagged as major in iter1; the iter2 CONTRACT comment and the total_post_race_nonnull gate together make this acceptable: the nonnull gate is the authoritative falsifiable gate, and contended_window_iters is documented as a structural proxy. Downgrading to minor.
   *Recommendation:* No code change required. If stronger proof is ever needed, an HTTPSERVER_TEST_INSTRUMENTATION counter inside the CAS-loser branch of ensure_table() would replace the proxy entirely — noted in the task spec as acceptable.

41. [ ] **test-quality-reviewer** | `test/integ/threadsafety_stress.cpp:993` | implementation-coupling
   The post-race witness `r.hook_table_raw_()` (line 993) calls `.get()` on the `shared_ptr<resource_hook_table>` field without going through an atomic load. This is safe here because all worker threads have been joined before this read (join establishes happens-before), so no concurrent atomic writes to `hook_table_` can be in flight. However, the pre-race check at line 910 also calls `hook_table_raw_()` on the main thread before any workers launch — also safe. The concern is that the accessor is documented as an 'internal accessor for the dispatch path' (http_resource.hpp:391) and its use in a test TU relies on the HTTPSERVER_COMPILATION gate being set globally for the entire test translation unit (via AM_CPPFLAGS). This means the test TU is compiled with HTTPSERVER_COMPILATION, which also enables other internal-only symbols — a broader gate than intended for a test file. The situation is pre-existing and not introduced by TASK-094, but worth noting.
   *Recommendation:* If a narrower gate is feasible in future, consider a separate HTTPSERVER_TEST_INSTRUMENTATION macro that enables only test-facing accessors, independent of HTTPSERVER_COMPILATION which has semantic meaning for the library's own .cpp files. This is a low-priority cleanup; the current approach works correctly.
