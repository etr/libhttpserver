# Unworked Review Issues

**Run:** 2026-06-08 18:11:59
**Task:** TASK-074
**Total:** 28 (0 critical, 6 major, 22 minor)

## Major

1. [x] **code-simplifier** | `src/detail/webserver_body_pipeline.cpp:154` | code-structure
   debug_dump_request_body_opted_in() is a one-line wrapper that does nothing but call the anon-ns helper debug_dump_request_body_enabled(). The two functions have effectively identical semantics (both return the same bool), but the split forces two named concepts where one suffices. The named-at-namespace-detail boundary exists so webserver_setup.cpp can call something declared in the header without exposing the static-local internals, but that could be satisfied by just declaring debug_dump_request_body_enabled() at namespace detail instead.
   *Recommendation:* Collapse the thin passthrough: rename debug_dump_request_body_enabled() to debug_dump_request_body_opted_in() and move it out of the anon namespace directly into namespace detail, then declare it in webserver_impl.hpp. Remove the now-redundant wrapper at line 154-156. Every call site (lines 173 and 288) already uses one of these two names, so the rename is mechanical and the binary behaviour is unchanged.
   *Worked 2026-07-07 (chore/unworked-major-issues):* Wrapper collapsed: the getenv-caching body now lives directly in detail::debug_dump_request_body_opted_in(); the anon-ns duplicate is deleted and call sites updated.

2. [x] **code-simplifier** | `src/detail/webserver_body_pipeline.cpp:154` | code-structure
   debug_dump_request_body_opted_in() is a thin public wrapper that does nothing except forward to the private debug_dump_request_body_enabled() static. The two functions share identical semantics and the wrapper exists solely to bridge the anonymous-namespace boundary, adding an indirection layer with no encapsulation benefit since both live in the same TU.
   *Recommendation:* Remove debug_dump_request_body_opted_in() and promote debug_dump_request_body_enabled() out of the anonymous namespace (making it a named-namespace function declared in webserver_impl.hpp). Call sites (maybe_warn_debug_dump_request_body and webserver_setup.cpp) can then call it directly, eliminating the wrapper.
   *Worked 2026-07-07 (chore/unworked-major-issues):* Same closure as finding 1.

3. [x] **code-simplifier** | `test/integ/debug_dump_request_body_set_test.cpp:63` | code-structure
   The stream_capture struct and the begin_capture / end_capture helpers are copy-pasted verbatim between debug_dump_request_body_set_test.cpp and debug_dump_request_body_unset_test.cpp. Any fix or change to the capture logic must be applied in both files.
   *Recommendation:* Extract the stream_capture struct and begin_capture / end_capture helpers into a shared test-only header (e.g., test/integ/stream_capture_helpers.hpp) and include it from both test files. This follows the DRY principle and matches how other shared test utilities are handled in the project.
   *Worked 2026-07-07 (chore/unworked-major-issues):* stream_capture/begin_capture/end_capture extracted to test/integ/stream_capture_helpers.hpp (writefunc reuses the existing httpserver_test::writefunc from curl_helpers.hpp); registered in noinst_HEADERS.

4. [x] **code-simplifier** | `test/integ/debug_dump_request_body_set_test.cpp:64` | patterns
   The stream_capture struct and the begin_capture / end_capture / writefunc helpers are copy-pasted verbatim between debug_dump_request_body_set_test.cpp and debug_dump_request_body_unset_test.cpp. This is needless repetition (clean-code smell §5) that will require a dual update if the capture logic ever changes.
   *Recommendation:* Extract the four helpers (stream_capture, begin_capture, end_capture, writefunc) into a shared test-only header (e.g., test/integ/stream_capture_helpers.hpp) and include it from both test files. Because this is test code with no runtime behaviour change, the extraction is straightforward and directly shrinks each test file by ~40 lines.
   *Worked 2026-07-07 (chore/unworked-major-issues):* Same extraction as finding 3.

5. [x] **test-quality-reviewer** | `test/integ/debug_dump_request_body_set_test.cpp:196` | non-deterministic
   The warning-idempotence assertion (count_substring(captured_stderr, 'SECURITY WARNING') == 1) is logically correct but fragile under concurrent MHD worker threads. Both ws1 and ws2 spin up with max_threads(2). The g_debug_dump_warning_emitted atomic in production code serialises the stderr write correctly, but the test buffers the entire stderr from both servers' lifetimes in one capture window. If ws2's start() call on line 149 triggers a second attempt to emit the warning while ws1 is still dispatching requests on the MHD thread pool (possible if stop() returns before all MHD callbacks complete), there is a window where two emissions could race into the capture buffer. The test would flake if the warning fires more than once. The test does not explicitly wait for ws1's request to complete before starting ws2.
   *Recommendation:* Insert a brief synchronization point after the run_one_post/ws1->stop() block to ensure ws1's MHD threads are fully quiesced before ws2 starts. Alternatively, restructure so ws1 is fully torn down (ws1.reset()) before ws2->start() is called — the reset() already appears on line 146, so the issue is whether MHD's internal threads drain before stop() returns. Verify whether the INTERNAL_SELECT start method guarantees drain on stop(); if not, add a short MHD_run_wait() poll after stop().
   *Worked 2026-07-07 (chore/unworked-major-issues):* Verified no gap: ws1->stop() + ws1.reset() complete before ws2 is constructed, and MHD_stop_daemon blocks until connections drain and workers join; a clarifying comment at the reset() site records the quiesce point.

6. [x] **test-quality-reviewer** | `test/integ/debug_dump_request_body_unset_test.cpp:63` | redundant-test
   The stream_capture struct and begin_capture/end_capture/writefunc helpers are copy-pasted verbatim (lines 63-107) from debug_dump_request_body_set_test.cpp (lines 64-108). Both files are compiled into separate binaries so sharing via a header is straightforward. The duplication creates a maintenance burden: any bug fix or enhancement to the capture helper must be applied in two places.
   *Recommendation:* Extract stream_capture, begin_capture, end_capture, and writefunc into a shared test utility header (e.g., test/integ/debug_dump_helpers.hpp or extend the existing test/integ/curl_helpers.hpp). Both test TUs then include the single header. The Makefile.am already has a noinst_HEADERS block that could absorb it.
   *Worked 2026-07-07 (chore/unworked-major-issues):* Same extraction as finding 3.
## Minor

7. [x] **architecture-alignment-checker** | `src/detail/webserver_body_pipeline.cpp:289` | pattern-violation
   The raw-body dump writes to std::cout (C++ stream) while the startup warning uses std::fprintf(stderr) matching the established fprintf-to-stderr pattern in the codebase. The mixing of C and C++ I/O APIs is a minor inconsistency, though the comment explicitly notes stdout for the dump path and stderr for the warning. The pre-existing code used std::cout and this is preserved intentionally.
   *Recommendation:* This is acceptable as-is since the comment in webserver_body_pipeline.cpp explicitly calls out the pre-existing pattern and it does not affect correctness. If a future cleanup task standardizes I/O style, the dump line could be changed to fprintf(stdout, ...) for consistency with the warning path.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* No action — The mixed std::cout / fprintf(stderr) I/O style is unchanged, but the finding's own recommendation concluded it is acceptable as-is since it is a deliberate, preserved pre-existing pattern with no correctness impact; no action was taken.

8. [x] **code-quality-reviewer** | `src/detail/webserver_body_pipeline.cpp:154` | code-elegance
   debug_dump_request_body_opted_in() is a one-liner forwarding wrapper around the anon-ns debug_dump_request_body_enabled(). The only reason it exists is to give webserver_setup.cpp a linkage-visible symbol; the thin wrapper adds an extra call frame with no observable benefit. A comment or namespace alias would communicate the intent more clearly than a forwarding function.
   *Recommendation:* Either make debug_dump_request_body_enabled() itself a detail:: free function declared in webserver_impl.hpp (removing the indirection entirely), or at minimum add a brief comment at the declaration site in the header explaining that the wrapper exists solely for cross-TU linkage visibility.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Already resolved in the current tree — The thin forwarding wrapper this finding describes no longer exists: the 2026-07-07 major-findings sweep (findings 1/2) collapsed debug_dump_request_body_enabled() and debug_dump_request_body_opted_in() into a single detail:: function that contains the getenv-caching logic directly and is declared in webserver_impl.hpp; there is no remaining indirection to remove.

9. [x] **code-quality-reviewer** | `src/detail/webserver_body_pipeline.cpp:289` | code-readability
   std::cout is used for the dump output while std::fprintf(stderr, ...) is used for the warning. The inconsistency in I/O style (C++ streams vs C stdio) is noted by the existing comment, but the comment says 'Stderr is used (not std::cerr)' without addressing why stdout is used via std::cout rather than printf/fprintf for consistency.
   *Recommendation:* Either switch the body dump to std::fprintf(stdout, ...) to match the stdio convention used in the warning path, or add a brief comment acknowledging the mixed I/O style deliberately (e.g. citing that std::cout was the existing convention at that site).
   *Worked 2026-07-12 (chore/unworked-minor-issues):* No action — Duplicate of finding 7's stdout/stderr I/O-style observation on the same line; the reviewer consensus (finding 7) already treats this as an intentional preserved convention, and adding a further comment here would conflict with the comment-trim recommended by findings 10/17.

10. [x] **code-quality-reviewer** | `src/detail/webserver_body_pipeline.cpp:70` | code-readability
   The large block comment (lines 70-89) preceding debug_dump_request_body_enabled() describes implementation rationale that could be more concisely conveyed as a Doxygen doc-comment on the function itself. The current narrative style mixes design decisions (DR-009 cross-reference, C++11 guarantee) in a free-form block that is harder to scan than structured doc comments.
   *Recommendation:* Trim the narrative block to the key facts and convert it to a Doxygen-style comment matching the style used for the two declarations in webserver_impl.hpp. The DR-009 reference can stay as an inline note.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Trimmed the ~18-line block comment above debug_dump_request_body_opted_in() in src/detail/webserver_body_pipeline.cpp down to the thread-safety (C++11 magic-static) rationale and the ignore-later-setenv semantic, deferring the env-var name/accepted-value/default-silent contract to the Doxygen comment in webserver_impl.hpp:103-113.

11. [x] **code-quality-reviewer** | `src/detail/webserver_body_pipeline.cpp:86` | code-readability
   The env-var truthiness logic (non-null, non-empty, not "0") is expressed inline inside the lambda with no named helper or comment explaining why "0" is treated as falsy. This is a non-obvious policy choice that differs from the UNIX convention of 'any value truthy'.
   *Recommendation:* Extract the three-part truthiness check into a named constexpr/inline helper (e.g. env_var_is_truthy(v)) or add a short inline comment naming the convention chosen ("mirrors shell: empty or '0' == disabled").
   *Worked 2026-07-12 (chore/unworked-minor-issues):* No action — The '0'-is-falsy rule is already documented immediately above the check, both in the .cpp block comment ('Accepts any non-empty, non-"0" value') and in the webserver_impl.hpp Doxygen doc-comment; extracting a single-use 3-part boolean check into a named helper would add indirection without benefit and would conflict with findings 10/17's request to trim (not grow) the surrounding comment.

12. [x] **code-quality-reviewer** | `src/detail/webserver_body_pipeline.cpp:97` | code-readability
   g_debug_dump_warning_emitted is a file-scope atomic in an anonymous namespace inside a named namespace. Its name uses a g_ prefix which is an encoding convention (type/scope information in the name), contrary to the clean-code Names Rule 6 ('Avoid encodings'). Within anonymous namespace scope the prefix adds no disambiguation value.
   *Recommendation:* Rename to debug_dump_warning_emitted (drop the g_ prefix) to match the no-prefix style of other file-scope atomics in the codebase (e.g. g_arena_fallback_count referenced in the comment is the counterexample the comment itself cites, not a local pattern to emulate).
   *Worked 2026-07-12 (chore/unworked-minor-issues):* No action — The g_ prefix matches an existing, real codebase convention: src/http_request.cpp:98 defines `std::atomic<std::uint64_t> g_arena_fallback_count` with the same prefix. The finding's premise that this is a 'counterexample not to emulate' is incorrect -- it is an actual precedent that the local comment correctly cites.

13. [x] **code-quality-reviewer** | `test/integ/debug_dump_request_body_set_test.cpp:1` | test-coverage
   The set_test does not verify the value="0" falsy case (i.e. setting the env var to "0" should behave identically to unset). The implementation explicitly treats "0" as disabled, but no test pins this boundary. A future refactor removing the "0" special-case would go undetected.
   *Recommendation:* Add a test case (or a separate binary) that sets LIBHTTPSERVER_DEBUG_DUMP_REQUEST_BODY=0 and asserts no stdout output and no SECURITY WARNING, mirroring the unset_test assertions.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Added test/integ/debug_dump_request_body_zero_test.cpp (setenv LIBHTTPSERVER_DEBUG_DUMP_REQUEST_BODY="0", asserts silent stdout/stderr), registered it in test/Makefile.am, and verified it builds and passes (1 test, 6/6 checks).

14. [x] **code-quality-reviewer** | `test/integ/debug_dump_request_body_set_test.cpp:132` | test-coverage
   The set-test exercises two webservers sequentially but both send non-empty, non-'0' values ('1'). The env-var acceptance rule also covers any non-empty non-'0' value, but there is no test verifying that value '0' (the explicit-off case) is treated as unset. If the parsing logic in debug_dump_request_body_enabled() were accidentally broken for the '0' case, no test would catch it.
   *Recommendation:* Add a minimal test case (can be a unit test rather than a full integ test) that sets the env var to '0' and verifies the function returns false / no output appears.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Duplicate of idx 13; resolved by the same new debug_dump_request_body_zero_test.cpp binary and Makefile.am wiring.

15. [x] **code-quality-reviewer** | `test/integ/debug_dump_request_body_set_test.cpp:64` | code-readability
   The stream_capture struct and begin_capture/end_capture helpers are duplicated verbatim between debug_dump_request_body_set_test.cpp and debug_dump_request_body_unset_test.cpp. While test files are often kept self-contained, an identical ~40-line block in two test files in the same directory is a maintenance liability.
   *Recommendation:* Extract stream_capture, begin_capture, and end_capture into a shared integ test utility header (e.g. test/integ/stream_capture.hpp), or at minimum add a TODO comment acknowledging the duplication so a future consolidation pass picks it up.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Already resolved in the current tree — stream_capture, begin_capture, and end_capture (writefunc reuses httpserver_test::writefunc from curl_helpers.hpp) were already extracted into test/integ/stream_capture_helpers.hpp during the 2026-07-07 major-findings sweep (findings 3/4/6); both debug_dump_request_body_set_test.cpp and debug_dump_request_body_unset_test.cpp already #include it and use httpserver_test::begin_capture/end_capture, and it is registered in test/Makefile.am's noinst_HEADERS.

16. [x] **code-quality-reviewer** | `test/integ/debug_dump_request_body_set_test.cpp:70` | code-readability
   The begin_capture / end_capture helpers are duplicated verbatim between debug_dump_request_body_set_test.cpp and debug_dump_request_body_unset_test.cpp (stream_capture struct, begin_capture, end_capture, writefunc). This is a DRY violation that will require a double-maintenance fix if the capture approach changes.
   *Recommendation:* Extract the stream_capture helpers into integ/test_utils.hpp (already listed in noinst_HEADERS in test/Makefile.am) or a dedicated integ/stream_capture.hpp so both test binaries share one definition.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Already resolved in the current tree — Same extraction as finding 15 -- stream_capture helpers already live in the shared test/integ/stream_capture_helpers.hpp header, consumed by both test binaries; no duplication remains.

17. [x] **code-simplifier** | `src/detail/webserver_body_pipeline.cpp:70` | code-structure
   The block comment above debug_dump_request_body_enabled() (lines 70-88) is 13 lines long — nearly as long as the function plus the global atomic it introduces. Much of this repeats information already in the doc-comment in webserver_impl.hpp (lines 103-113) and in docs/debug-env-vars.md. In-function comments are most valuable when explaining non-obvious decisions, not restating the documented contract.
   *Recommendation:* Trim the block comment to the one or two sentences that explain the thread-safety rationale (the C++11 magic-static guarantee) and the deliberate mid-process-setenv-ignored semantic. The architecture doc and the header declaration are the right places for the full rationale.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Same trim as finding 10 (duplicate finding): resolved by the single comment edit above debug_dump_request_body_opted_in().

18. [x] **code-simplifier** | `src/detail/webserver_body_pipeline.cpp:83` | naming
   The anon-ns function is named debug_dump_request_body_enabled() while the public namespace-detail function at line 154 is named debug_dump_request_body_opted_in(). Using two different suffixes (_enabled vs _opted_in) for the same concept inside the same file is mildly inconsistent and causes a brief double-take when reading.
   *Recommendation:* If the wrapper is kept, align the names so the internal function also uses the _opted_in suffix (or vice versa). This is moot if finding #1 is acted on.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Already resolved in the current tree — The _enabled vs _opted_in naming inconsistency this finding describes is moot, per the finding's own text ('moot if finding #1 is acted on'): finding #1 was worked 2026-07-07, collapsing the two functions into the single debug_dump_request_body_opted_in(); there is no longer a second, differently-named internal function to be inconsistent with.

19. [x] **performance-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-074/src/detail/webserver_body_pipeline.cpp:289` | memory-allocation
   When the dump is active, `std::string(upload_data, *upload_data_size)` constructs a heap-allocated copy of every body chunk solely for printing. For large or streaming bodies this incurs an avoidable allocation and copy on every call to requests_answer_second_step while the env var is set. This path is intentionally opt-in so it is minor, but a simple `std::cout.write(upload_data, *upload_data_size)` would avoid the heap round-trip entirely.
   *Recommendation:* Replace `std::cout << std::string(upload_data, *upload_data_size)` with `std::cout.write(upload_data, static_cast<std::streamsize>(*upload_data_size))` to eliminate the per-chunk heap allocation. The string_view overload of operator<< would also work: `std::cout << std::string_view(upload_data, *upload_data_size)`.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* requests_answer_second_step (src/detail/webserver_body_pipeline.cpp) now writes the raw body chunk via std::cout.write(upload_data, ...) instead of constructing a std::string(upload_data, *upload_data_size) per chunk, eliminating the heap allocation while preserving identical stdout bytes.

20. [x] **security-reviewer** | `docs/debug-env-vars.md:25` | sensitive-data-handling
   The documentation states that bytes are written 'verbatim to stdout, prefixed by Writing content:'. It correctly references CWE-532. However, it does not warn operators that the env var value is cached at first call, meaning a runtime unsetenv() (e.g., via an orchestration layer's secret scrubbing) has no effect once the process has handled its first request. This could mislead an operator who believes they can dynamically disable the dump without a restart.
   *Recommendation:* Add an explicit note to the 'How to disable' section: 'Note: if a request has already been handled since the variable was set, calling unsetenv() in the same process has no effect because the value is cached at first request. A full process restart is required to disable the dump.'
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Already resolved in the current tree — docs/debug-env-vars.md's 'How to disable' section already states: 'The value is cached in a function-local static on first request, so a mid-process unsetenv() has no effect' (current lines 66-68), which is exactly the warning this finding asked to add.

21. [x] **security-reviewer** | `src/detail/webserver_body_pipeline.cpp:289` | sensitive-data-handling
   Raw body bytes are written to std::cout (stdout) rather than to a dedicated log sink or the configured log_error callback. This means a production operator who accidentally sets the env var will see credentials on stdout, which may be captured by process supervisors, container log aggregators, or systemd journal without the 'SECURITY WARNING' label that goes to stderr. The two streams could be separated in log aggregation, causing the credential dump to appear in logs without the surrounding warning context.
   *Recommendation:* Consider also forwarding the body dump line through the parent->get_error_logger() sink (when wired) so it flows into the same audit trail as the startup warning. Alternatively document clearly in docs/debug-env-vars.md that stdout and stderr will be written to independently and operators must capture both.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Added a paragraph to docs/debug-env-vars.md, after the Startup signal section, noting that the stdout body dump and stderr SECURITY WARNING are independent streams that log aggregators/supervisors may capture separately, and operators must ensure both are captured together to correlate them.

22. [x] **security-reviewer** | `src/detail/webserver_body_pipeline.cpp:83` | insecure-design
   The function-local static in debug_dump_request_body_enabled() locks in the env-var value permanently on first call. The documentation correctly states this behavior, but the startup warning (maybe_warn_debug_dump_request_body) fires from webserver::start() which may be called after the static is already initialized if the pipeline is touched first. In practice the body pipeline is only called after start(), so the ordering is safe -- but this is an implicit temporal dependency that is not enforced by the type system. If in a future refactor the pipeline is accessed before start(), the warning would silently fire after the static is already set.
   *Recommendation:* Add a comment in webserver::start() explicitly noting the ordering invariant: maybe_warn_debug_dump_request_body must be called before any request is dispatched (and hence before debug_dump_request_body_enabled() is first invoked). This is currently true by construction but should be documented as a maintenance invariant.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Extended the comment above detail::maybe_warn_debug_dump_request_body(this) in webserver::start() (src/detail/webserver_setup.cpp) to state the maintenance invariant that this call must precede the body pipeline's magic-static initialization.

23. [x] **security-reviewer** | `test/integ/debug_dump_request_body_set_test.cpp:203` | logging-failures
   The test sets the env var via setenv() at LT_BEGIN_AUTO_TEST_ENV (main() body), which is correct. However the test binary for the 'set' case will also initialize the process-wide g_debug_dump_warning_emitted atomic. If this binary is ever linked into a test harness that runs other test programs in the same process (not the current setup, but a risk if the test framework evolves), the idempotence sentinel would suppress warnings that should appear. This is a test-design note, not a production code issue.
   *Recommendation:* No immediate action needed given the current two-binary split design (which is explicitly correct). Add a comment in the test explaining that the two-binary split is load-bearing for exactly this reason -- each binary gets a fresh process-scope static.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* No action — The finding's own recommendation opens with 'No immediate action needed given the current two-binary split design (which is explicitly correct)'; the load-bearing rationale for the two-binary split is already documented in test/Makefile.am (lines 645-650, TASK-074 comment block) and in both test files' header comments.

24. [x] **spec-alignment-checker** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-074/src/detail/webserver_body_pipeline.cpp:97` | specification-gap
   g_debug_dump_warning_emitted is defined in the anonymous namespace of webserver_body_pipeline.cpp, while maybe_warn_debug_dump_request_body() is a non-anonymous-namespace detail:: function that uses it. This is correct (the function is in the same TU so it can access the anon-ns variable), but placing the atomic flag in the anon namespace and the consuming function in the named namespace creates a subtle coupling that is not self-documenting. The architecture note in TASK-074 describes the flag as 'process-wide' but its process-wide scope is enforced only by TU-level linkage, which is not immediately obvious.
   *Recommendation:* Move g_debug_dump_warning_emitted to namespace httpserver::detail (or add a comment beside the anon-ns definition explaining that maybe_warn_debug_dump_request_body in the same TU is its only consumer), to make the coupling explicit.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Extended the comment above g_debug_dump_warning_emitted in src/detail/webserver_body_pipeline.cpp to state that its only consumer, detail::maybe_warn_debug_dump_request_body(), is defined later in the same translation unit, making the TU-local (anonymous-namespace) visibility explicit.

25. [x] **spec-alignment-checker** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-074/test/integ/debug_dump_request_body_set_test.cpp:203` | acceptance-criteria
   The env var is set via setenv() inside LT_BEGIN_AUTO_TEST_ENV() which runs before test bodies but after the translation unit is loaded. The magic-static cache in debug_dump_request_body_enabled() is initialised on first call, which happens during the first request dispatch — not at static-init time — so the ordering is correct. However, the comment in the test file says 'before any libhttpserver code runs', which is slightly misleading: the webserver constructor and registration calls run before setenv(). Only the cache-filling first call to debug_dump_request_body_enabled() is deferred until dispatch. This is not a functional problem but the comment could mislead a future reader into thinking a different ordering is required.
   *Recommendation:* Tighten the comment to 'before the first request dispatch (and therefore before the magic-static is initialised)' to avoid implying the env var must be set before the webserver constructor.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Reworded the top-of-file banner comment in test/integ/debug_dump_request_body_set_test.cpp to match the more precise wording already used at the setenv() call site ('opted in ... before the test bodies run ... before the magic-static cache ... is initialised on first dispatch').

26. [x] **test-quality-reviewer** | `test/integ/debug_dump_request_body_set_test.cpp:132` | naming-convention
   The test name 'dump_and_warn_then_warn_only_once' conflates two distinct concerns: (a) body dump to stdout, and (b) startup warning idempotence. The 'and' in the name is the canonical signal that these should be two separate tests. As a single test, a failure in the idempotence assertion does not localise cleanly, because the developer must also verify whether the dump assertion passed.
   *Recommendation:* Split into two tests: 'dump_body_to_stdout_when_env_set' covering the stdout Writing-content assertion, and 'startup_warning_emitted_exactly_once_across_multiple_webservers' covering the stderr assertion and the idempotence count check. Each can use its own pair of webservers if needed. This also naturally resolves finding #2: the second test would start ws2 independently after ws1 is fully torn down.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Split dump_and_warn_then_warn_only_once in test/integ/debug_dump_request_body_set_test.cpp into startup_warning_emitted_exactly_once_across_multiple_webservers (stderr/idempotence, using ws1/ws2) and dump_body_to_stdout_when_env_set (stdout-only, using a new ws3). Ordered the warning test first (documented with a NOTE ON TEST ORDER comment) since littletest registers/runs tests in definition order and the SECURITY WARNING is a one-shot, process-lifetime event gated by whichever test starts a webserver first — verified this ordering is correct by building and running the binary (2 tests, 9/9 checks passing).

27. [x] **test-quality-reviewer** | `test/integ/debug_dump_request_body_unset_test.cpp:116` | missing-test
   The unset test sends only one POST and verifies the stdout/stderr are silent. The TASK-074 acceptance criterion specifies the default is silent on 'both RELEASE and DEBUG builds', but the test binary is compiled with the same flags as all other integ tests. There is no coverage for a second POST (verifying the silence is not just an artifact of skipping a first-call initialization path) or for the env var being explicitly set to '0' (which the production code accepts as opt-out, per the string_view != '0' check). The var=0 case is behaviorally distinct from unset and goes completely untested.
   *Recommendation:* Add a second POST in the unset test to confirm continued silence, or at minimum add a comment explaining why one POST is sufficient. Add a sibling test (or a parameterized case) for LIBHTTPSERVER_DEBUG_DUMP_REQUEST_BODY=0, confirming it is treated as disabled. Since each binary's magic-static is locked to its setenv call, a separate test binary debug_dump_request_body_zero_test.cpp using setenv('LIBHTTPSERVER_DEBUG_DUMP_REQUEST_BODY', '0', 1) would be the cleanest approach consistent with the existing split strategy.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Primary fix shared with idx 13/14 (new debug_dump_request_body_zero_test.cpp). Also applied the optional secondary suggestion: added a second curl POST in debug_dump_request_body_unset_test.cpp's no_stdout_no_warning test to confirm silence holds past the first dispatch.

28. [x] **test-quality-reviewer** | `test/integ/debug_dump_request_body_unset_test.cpp:128` | aaa-violation
   curl_global_init(CURL_GLOBAL_ALL) is called inline in the test body (line 128) after the webserver is started (line 120-126), mixing arrangement steps. The set test avoids this by calling curl_global_init inside run_one_post (line 112), but the unset test inlines the curl setup directly and skips curl_global_cleanup. curl_global_init is a process-wide call and leaving it without a matching cleanup is technically a resource leak, though benign for a test binary.
   *Recommendation:* Either call curl_global_init once in set_up() / a fixture and curl_global_cleanup in tear_down(), or use the run_one_post helper that already calls curl_global_init internally (and add curl_global_cleanup for correctness). Consistency with the set test's approach would also eliminate the inline Arrange/Act interleaving.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* In debug_dump_request_body_unset_test.cpp, moved curl_global_init() before ws->start() (Arrange no longer interleaves with the started webserver) and added the previously-missing curl_global_cleanup() after curl_easy_cleanup(). Verified the binary still builds and passes (8/8 checks).
