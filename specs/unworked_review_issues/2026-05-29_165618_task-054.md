# Unworked Review Issues

**Run:** 2026-05-29 16:56:18
**Task:** TASK-054
**Total:** 35 (0 critical, 4 major, 31 minor)

## Major

1. [x] **test-quality-reviewer** | `test/unit/auth_handler_legacy_shim_test.cpp:160` | redundant-test
   legacy_setter_registers_one_before_handler (line 160) asserts that the deprecated setter installs one hook via the same webserver_test_access bridge. hooks_alias_count_test.cpp does not cover the legacy setter path, so this assertion is not a strict duplicate — but the intent of the hooks_alias_count suite is to be the single location for hook-count assertions. The legacy shim TU should focus on compile-type pinning and wire behavior, not hook counts.
   *Recommendation:* Consider moving the hook-count assertion for the legacy setter into hooks_alias_count_test.cpp alongside the canonical-setter count test, so all hook-count contracts live in one file. This is a minor structural concern; the test itself is useful.
   *Fixed:* Moved legacy hook-count assertion to hooks_alias_count_test.cpp as `legacy_auth_handler_registers_one_before_handler` (with a TU-scoped `#pragma` to suppress the deprecation warning, same pattern as the legacy shim TU). Removed the assertion and now-unused helper/include from the shim TU.

2. [x] **test-quality-reviewer** | `test/unit/auth_handler_optional_signature_test.cpp:1` | missing-test
   The acceptance criteria call for a negative case where the auth handler throws an exception. Neither new unit file contains a test where the handler callable throws — there is no coverage of what the dispatch path does when auth_handler itself throws (does it propagate, swallow, or invoke internal_error_handler?).
   *Recommendation:* Add a test in auth_handler_optional_signature_test.cpp where the handler throws std::runtime_error and assert the observable outcome (e.g., 500 returned, or exception propagated). This is the only way to pin the contract for error paths through the hook-invocation machinery.
   *Fixed:* Added `throwing_handler_is_swallowed_and_request_passes` which pins the observed contract: exceptions thrown from the alias hook are swallowed and the request passes (response 200 SUCCESS). A docstring explains the design rationale (the hook framework's handler-exception slot is the named extension point; the alias hook stays non-fatal so a buggy callable cannot DoS the server). Any future change to short-circuit must update the pin.

3. [x] **test-quality-reviewer** | `test/unit/auth_handler_optional_signature_test.cpp:1` | missing-test
   The acceptance criteria include a negative case: 'handler returning moved-from optional, large response payload not corrupted in transit'. There is no test verifying that a large response body (e.g., 64 KB) set on the auth rejection optional arrives on the wire uncorrupted. The SBO-path claim in the production code comments ('small responses ride the http_response SBO') makes the large-payload case a meaningful regression target.
   *Recommendation:* Add a test where the auth handler returns an engaged optional whose body is a large string (e.g., 64 KB). Assert the response_code and that the received body length and content match exactly, confirming that the MOVE from optional into hook_action::respond_with does not truncate or corrupt it.
   *Fixed:* Added `large_engaged_optional_payload_arrives_intact` (64 KB body, status 403); asserts body.size() and full equality against the constructed string.

4. [x] **test-quality-reviewer** | `test/unit/auth_handler_optional_signature_test.cpp:122` | redundant-test
   accepting_optional_lambda_registers_one_before_handler (line 122) checks that the optional-signature setter installs exactly one before_handler hook. The same assertion — that auth_handler(fn) installs one hook — is already pinned in hooks_alias_count_test.cpp:auth_handler_registers_one_before_handler (line 65). Both tests use the same webserver_test_access::impl bridge, exercise the identical code path, and assert the identical value. One of them is redundant.
   *Recommendation:* Remove accepting_optional_lambda_registers_one_before_handler from auth_handler_optional_signature_test.cpp and let hooks_alias_count_test.cpp own the hook-count contract. The optional-signature TU can then focus exclusively on compile-time type pinning and end-to-end wire behavior.
   *Fixed:* Removed the redundant test and the now-unused `before_handler_count` helper + webserver_impl include + `hook_phase` using-declaration from the optional-signature TU. Hook-count contract now lives only in `hooks_alias_count_test.cpp`. Also paired per-test PORT_N/PORT_N_STRING macros (item 9 fix) so URL strings cannot silently drift from the constructor port.

## Minor

5. [ ] **architecture-alignment-checker** | `specs/architecture/04-components/create-webserver.md:21` | adr-violation
   The architecture doc says the compat alias and setter overload 'will be removed after the next release' but does not name which release (e.g. v2.1). The deprecation policy cited in the task spec ('keep transitional alias for one build with [[deprecated]]') is satisfied at the code level, but the architectural prose does not pin the specific version number. Without a named version the policy is open-ended and could be silently deferred.
   *Recommendation:* Add the concrete target version to the architecture doc sentence, e.g. 'removed in v2.1'. Mirror the same version in RELEASE_NOTES.md's 'What changed semantically' section, which currently says only 'for one transitional build' without naming v2.1.

6. [ ] **architecture-alignment-checker** | `src/httpserver/create_webserver.hpp:143` | pattern-violation
   The `compat::adapt_legacy_auth` function and the `auth_handler_v1_ptr` typedef are `[[deprecated]]` inline definitions in the public header. The deprecation attribute on `adapt_legacy_auth` will fire at the call site inside the `[[deprecated]]` setter overload itself (line 466), which may generate a spurious or confusing nested deprecation warning depending on compiler. MSVC and GCC/Clang handle this differently: calling a deprecated function from a deprecated function can still emit diagnostics in some configurations.
   *Recommendation:* Suppress the inner call-site diagnostic by wrapping the `adapt_legacy_auth` invocation inside the deprecated setter with a `HTTPSERVER_NO_DEPRECATED` pragma or by restructuring so the setter body calls the lambda directly instead of going through `adapt_legacy_auth`. Alternatively add a note in the code comment explaining that the compiler is expected to suppress the inner warning when the caller is itself deprecated.

7. [ ] **code-quality-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-054/src/detail/webserver_aliases.cpp:224` | code-readability
   The local variable is named `auth_rejection_response` which is accurate, but it is only referenced immediately after assignment, making the intermediate name add some noise. The pattern `if (auto r = ws_ptr->auth_handler(...); r.has_value())` would be equally clear and remove the extra binding. Minor style preference only.
   *Recommendation:* Consider using `if`-with-initializer (`if (auto r = ws_ptr->auth_handler(*ctx.request); r.has_value()) { return hook_action::respond_with(std::move(*r)); }`) to scope the optional tightly to its use. Not a required change.

8. [ ] **code-quality-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-054/src/httpserver/create_webserver.hpp:149` | code-readability
   The `adapt_legacy_auth` lambda captures `legacy` by move inside `[legacy = std::move(legacy)](...) -> std::optional<http_response>`. The return expression `std::optional<http_response>(std::move(*ptr))` constructs the optional explicitly. The shorter idiomatic form `return std::move(*ptr);` (implicit conversion to optional) or `return {std::move(*ptr)};` is a style improvement but has no functional consequence.
   *Recommendation:* Use `return {std::move(*ptr)};` for brevity, consistent with how modern C++ optional idioms are typically written.

9. [ ] **code-quality-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-054/test/unit/auth_handler_optional_signature_test.cpp:141` | test-coverage
   The test `nullopt_allows_request_through` constructs the webserver on port PORT+1 (8291) and hardcodes the curl URL as `localhost:8291/protected`. If PORT ever changes the URL string will silently go stale (the PORT constant is only used for the create_webserver call, not the curl string). The legacy-shim test has the same pattern at line 185 (`localhost:8296`).
   *Recommendation:* Build the curl URL from the PORT macro: e.g. `"localhost:" PORT_STRING "/protected"` — the file already defines `PORT_STRING`. Avoids silent drift if the base port is ever changed.

10. [ ] **code-quality-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-054/test/unit/auth_handler_optional_signature_test.cpp:63` | test-coverage
   The compile-time `static_assert` on `auth_handler_ptr` is excellent as a regression pin. However there is no corresponding `static_assert` that `auth_handler_ptr` is NOT `std::function<std::shared_ptr<http_response>(const http_request&)>` (i.e., a negative pin). The positive pin is the important one, so this is purely minor.
   *Recommendation:* Optionally add `static_assert(!std::is_same_v<auth_handler_ptr, std::function<std::shared_ptr<http_response>(const http_request&)>>)` as an explicit negative guard. Low value given the positive already covers it.

11. [ ] **code-quality-reviewer** | `examples/centralized_authentication.cpp:59` | code-elegance
   In the auth_handler function, both rejection branches construct the optional explicitly: `return std::optional<httpserver::http_response>(http_response::string(...).with_status(500))` and `return std::optional<httpserver::http_response>(http_response::unauthorized(...))`. In the same file's test counterpart (test/integ/authentication.cpp line 634) the same verbose explicit construction is used. Since the function return type is already `std::optional<httpserver::http_response>`, the factory result can be returned directly via implicit construction (`return http_response::unauthorized(...)`) which is shorter and idiomatic C++17. The test in auth_handler_optional_signature_test.cpp line 158 also uses the explicit form when it is not necessary.
   *Recommendation:* In all three sites, replace `return std::optional<httpserver::http_response>(http_response::...)` with `return http_response::...` — the compiler will wrap it in the optional implicitly. This is consistent with the README example at line 1234 which omits the explicit wrapper.

12. [ ] **code-quality-reviewer** | `src/detail/webserver_aliases.cpp:224` | code-readability
   The variable name `auth_rejection_response` at line 224 is verbose; more importantly it is declared with an explicit type annotation (`std::optional<http_response>`) when `auto` would be sufficient and would make refactoring the return type of `auth_handler` transparent. This is a minor style note — the current code is clear but slightly over-typed.
   *Recommendation:* Consider `auto auth_result = ws_ptr->auth_handler(*ctx.request);` and rename to convey the dual meaning (nullopt = pass, engaged = reject), e.g. `auto maybe_rejection = ...;`. This is purely stylistic and low priority.

13. [ ] **code-quality-reviewer** | `src/httpserver/create_webserver.hpp:139` | code-readability
   The file-scope free function `install_log_access_alias_` (in webserver_aliases.cpp) has a trailing underscore in its name while `install_internal_error_alias` does not. The comment at line 89–91 of webserver_aliases.cpp acknowledges this inconsistency and justifies it by reference to a review finding, but the trailing underscore on one and not the other violates the codebase's own convention ('no trailing underscore for file-scope free functions in anonymous namespace'). The inconsistency is in webserver_aliases.cpp lines 97 and 139.
   *Recommendation:* Rename `install_log_access_alias_` to `install_log_access_alias` to match the naming of `install_internal_error_alias`. The comment at lines 89–91 of webserver_aliases.cpp explains the correct rule; the function name should follow it.

14. [ ] **code-quality-reviewer** | `test/unit/auth_handler_legacy_shim_test.cpp:183` | test-coverage
   The test `legacy_nullptr_allows_request_through` uses a hardcoded URL string `"localhost:8296/protected"` (PORT+1) rather than the PORT_STRING macro. The macro is defined at line 85 but the test uses a raw literal. If the port value is ever changed, this hardcoded URL would silently diverge from the PORT constant. The same test uses `PORT + 2` for the webserver in `legacy_response_state_is_forwarded_verbatim` but then fetches from `"localhost:8297/protected"` — again a hardcoded address.
   *Recommendation:* Define PORT_STRING macros for PORT+1 and PORT+2 (e.g. `#define PORT_STRING_1 "8296"` and `"8297"`) and use them in the curl URL strings, or use a helper that formats the URL from the integer port number.

15. [ ] **code-quality-reviewer** | `test/unit/auth_handler_legacy_shim_test.cpp:64` | test-coverage
   The TU-scope `#pragma GCC diagnostic ignored "-Wdeprecated-declarations"` suppresses the deprecation warning for the entire file rather than wrapping only the specific sites where the deprecated API is exercised. This means any *accidental* use of other deprecated symbols in this file would also be silently swallowed. The comment above correctly notes that the intent is to prove 'warning fires, build does not break' — but the pragma is broader than necessary.
   *Recommendation:* Wrap only the three test bodies (and the static_assert) in a `#pragma GCC diagnostic push / ... pop` pair, or accept the current approach as pragmatic since the file exists solely to exercise deprecated API.

16. [ ] **code-simplifier** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-054/examples/centralized_authentication.cpp:59` | code-structure
   The auth_handler function constructs `std::optional<httpserver::http_response>(...)` explicitly in two places (lines 59 and 64) instead of using the simpler brace-init or direct return forms. For example, `return std::optional<httpserver::http_response>(httpserver::http_response::string(...).with_status(500))` can be written as `return httpserver::http_response::string(...).with_status(500)` since the return type is already `std::optional<http_response>` and http_response is implicitly convertible into the optional. This is the same pattern used in the test files (e.g., auth_handler_optional_signature_test.cpp line 158-160).
   *Recommendation:* Simplify the explicit `std::optional<httpserver::http_response>(...)` constructions to direct returns, letting the implicit conversion from `http_response` to `std::optional<http_response>` handle the wrapping. This is consistent with how the new signature is documented and used in tests.

17. [ ] **code-simplifier** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-054/src/detail/webserver_aliases.cpp:226` | code-structure
   The auth hook uses `if (!auth_rejection_response.has_value())` to test the optional, but the idiomatic and more concise C++ idiom is `if (!auth_rejection_response)`. Both forms are semantically identical. The has_value() call adds visual noise without adding clarity, and is inconsistent with how the pattern is used in examples/centralized_authentication.cpp and other call sites that simply return `std::nullopt` and check the value directly.
   *Recommendation:* Replace `if (!auth_rejection_response.has_value())` with `if (!auth_rejection_response)` for consistency with the idiomatic optional check pattern used throughout the codebase.

18. [ ] **code-simplifier** | `examples/centralized_authentication.cpp:58` | code-structure
   Both return sites in auth_handler construct `std::optional<httpserver::http_response>(...)` explicitly. The factory result already converts implicitly to `std::optional<http_response>`, so the explicit wrapper is noise.
   *Recommendation:* Replace `return std::optional<httpserver::http_response>(http_response::string(...).with_status(500));` with `return http_response::string(...).with_status(500);` and similarly on line 64-66. The README example (lines 1231-1237) already uses this implicit-conversion style — matching it would keep the example consistent with the canonical pattern in the docs.

19. [ ] **code-simplifier** | `src/detail/webserver_aliases.cpp:224` | naming
   The local variable `auth_rejection_response` on line 224 is accurate but long; it is used on the very next line (225) and never stored. The verbosity adds no extra clarity over a shorter name at this scope.
   *Recommendation:* Consider `auth_result` or simply `rejection` to reduce line length at the check site. This is purely stylistic and does not affect clarity of intent significantly, so it is optional.

20. [ ] **code-simplifier** | `test/unit/auth_handler_optional_signature_test.cpp:77` | patterns
   Both new test TUs define identical `writefunc`, `simple_resource`, and `before_handler_count` helpers in their anonymous namespaces. The duplication is benign (each TU compiles independently and the project has no shared unit-test utility header for integ-style helpers), but it is the exact repetition pattern the clean-code principles flag.
   *Recommendation:* This is a minor note only — the project does not appear to have a shared unit test utility header that these helpers could move into without creating a build dependency. If such a header is introduced in a future task, these helpers are candidates. No action needed for this task.

21. [ ] **performance-reviewer** | `src/detail/webserver_aliases.cpp:211` | memory-allocation
   The auth hook lambda constructs a `std::string path(ctx.request->get_path())` unconditionally on every authenticated request, including when `auth_skip_paths` is empty (the common case when no skip paths are configured). The string is heap-allocated from a string_view-like value before the short-circuit `should_skip_auth` guard is even needed. When the skip list is empty, this allocation is pure waste.
   *Recommendation:* Gate the string construction behind a fast empty-check on the skip paths list: if auth_skip_paths is empty, skip the heap allocation entirely and fall straight through to calling `ws_ptr->auth_handler(*ctx.request)`. This saves one heap alloc per authenticated request when skip paths are not configured.

22. [ ] **performance-reviewer** | `src/httpserver/create_webserver.hpp:151` | memory-allocation
   In `compat::adapt_legacy_auth`, the legacy callable is captured by value inside a new `auth_handler_ptr` std::function wrapper, resulting in double std::function wrapping. If the closure size exceeds the SBO threshold of std::function on the target platform, both the outer and inner std::function objects will heap-allocate. This is inherent to the adapter pattern and affects only transitional-build users who have not migrated their callables.
   *Recommendation:* No code change required for the shim; the deprecation warnings and documentation already direct migration to the direct `optional<http_response>` signature. This is informational: users who remain on the shim pay a one-time extra std::function heap allocation per webserver construction plus an extra indirection per auth call.

23. [ ] **security-reviewer** | `examples/centralized_authentication.cpp:54` | sensitive-data-handling
   The example calls std::getenv("AUTH_USER") and std::getenv("AUTH_PASS") on every request invocation of auth_handler. While environment variables are the correct place for credentials (CWE-798 correctly avoided), calling getenv on every request is slightly inefficient and — more importantly — could be affected by concurrent setenv/putenv calls from other threads in the same process (POSIX: setenv/getenv are not thread-safe with respect to each other in all implementations). The example shows good practice with the CWE-798 security note, but the per-request getenv pattern is not the safest usage in a multi-threaded server.
   *Recommendation:* In production code, capture the environment variables once at startup (before the server starts accepting requests) and store them in thread-safe read-only variables. For the example, a code comment noting that AUTH_USER and AUTH_PASS should be read once at startup and stored rather than re-read per request would guide developers appropriately. The current code is acceptable for an illustrative example given the security note already present.

24. [ ] **security-reviewer** | `src/detail/webserver_aliases.cpp:197` | broken-access-control
   Auth oracle: the before_handler hook only fires for route-matched requests (found==true). Unauthenticated clients can distinguish registered from unregistered paths because registered paths return 401 while unregistered paths return 404 without any auth check. This allows route enumeration without credentials (CWE-204: Observable Response Discrepancy). The code comment at line 189-195 documents this but does not classify it as a security property.
   *Recommendation:* The existing inline comment (security-reviewer-iter1-7 / CWE-200) acknowledges the behavior and suggests catch-all fallback routes as a mitigation. Consider promoting this to a @security note in the auth_handler Doxygen block in create_webserver.hpp so callers are informed at the API level rather than only in implementation comments. No code change is strictly required, but API-level documentation of this property would reduce the risk of callers deploying the auth handler under the false assumption that all requests are uniformly gated.

25. [ ] **security-reviewer** | `src/httpserver/create_webserver.hpp:146` | insecure-design
   compat::adapt_legacy_auth dereferences `*ptr` on line 153 (`std::move(*ptr)`) after the null check on line 152. The null check is correct and safe, but the dereference happens on the value behind the shared_ptr without any validation that the pointed-to http_response is in a valid state. If a buggy v1 caller returns a non-null shared_ptr wrapping a default-constructed or partially-built http_response (e.g. status 0, empty body), that response is moved verbatim into the optional and sent on the wire with no sanitization. This is a quality issue rather than a security issue in a well-written v1 caller, but in a security-sensitive shim adapter it is worth noting.
   *Recommendation:* This is within acceptable risk given the shim is deprecated and transitional. Consider adding a runtime assert (or at minimum a debug-mode assert) that `ptr->get_status() != 0` before moving, so callers are alerted to malformed responses during development. Example: `assert(ptr->get_status() != 0 && "adapt_legacy_auth: v1 callable returned non-null shared_ptr with zero status");`

26. [ ] **security-reviewer** | `test/integ/authentication.cpp:632` | authentication-failures
   The integration test's centralized_auth_handler (line 632-638) compares credentials using `!=` operator on string_view / const char* return values. If get_user() or get_pass() return a type where `!=` does pointer comparison rather than value comparison, there could be a silent auth bypass in tests. This is a test-code concern rather than production code, but integration tests that incorrectly validate security properties provide false assurance.
   *Recommendation:* Verify that get_user() and get_pass() return std::string_view or std::string where `!=` performs value (not pointer) comparison. If they return const char*, the comparison is safe only if both sides are null-terminated C-strings compared by value, which `!=` on const char* does NOT do (it compares pointers). Review the return types of these methods and, if they return std::string_view or std::string, the existing code is safe.

27. [ ] **spec-alignment-checker** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-054/specs/product_specs.md:178` | specification-gap
   PRD-RSP-REQ-007 states 'Neither std::unique_ptr<http_response> nor std::shared_ptr<http_response> shall be required' for returning a response from a handler. This requirement is now fully met for the auth handler path. However, the product_specs.md does not have a dedicated requirement for the auth handler callback signature specifically; it only covers the handler-return-type dimension. The architecture doc (specs/architecture/04-components/create-webserver.md) has been updated to describe the new shape, but product_specs.md section 3.5 does not mention the auth_handler_ptr shape at all — callers reading only the PRD cannot discover the expected auth callback signature from that document.
   *Recommendation:* Consider adding a note to PRD section 3.5 or 3.8 explicitly stating the auth_handler callback returns std::optional<http_response> (not shared_ptr). The architecture doc already covers this; aligning the PRD would make the requirement chain complete from PRD through architecture to implementation.

28. [ ] **spec-alignment-checker** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-054/specs/tasks/v2-deferred-backlog-plan.md:138` | acceptance-criteria
   Acceptance criterion 'One heap allocation removed per authenticated request (verifiable via `bench_auth.cpp` or by inspection)' — `bench_auth.cpp` does not exist; neither `test/Makefile.am` nor any test directory contains it. The criterion allows 'by inspection' as an alternative, and the code comment in `webserver_aliases.cpp` line 221 explicitly states '(one heap alloc removed per request that runs through this hook)'. The inspection path is present but the benchmark path is absent.
   *Recommendation:* Either add `test/bench_auth.cpp` to close the benchmark path of the acceptance criterion, or explicitly document in the task completion notes that the criterion is met by inspection (citing webserver_aliases.cpp:221 and the compat::adapt_legacy_auth body). The 'by inspection' option is arguably sufficient for this type of performance claim.

29. [ ] **spec-alignment-checker** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-054/src/httpserver/create_webserver.hpp:128` | acceptance-criteria
   Acceptance criterion states '`grep -rE 'shared_ptr<.*http_response' src/httpserver/` returns no results in public headers.' The grep does return results (9 lines) in `create_webserver.hpp` — all within the `compat::` namespace shim (`auth_handler_v1_ptr` typedef, `adapt_legacy_auth` body, and Doxygen comments). These hits are mandated by Action Item 1 ('Keep the old typedef alias for one transitional build with a [[deprecated]] attribute') and Action Item 3 ('Update the documented v1 alias surface'), so the criterion and the action items are in tension. The implementation correctly follows the action items; the acceptance criterion was written as if the compat shim wouldn't exist in the same header, but the spec simultaneously requires it there. This is a specification gap / ambiguity, not an implementation error.
   *Recommendation:* Update the acceptance criterion in TASK-054 to exclude the compat namespace: `grep -rE 'shared_ptr<.*http_response' src/httpserver/ | grep -v compat`. The compat shim is intentional per Action Item 1/3 and is correctly marked [[deprecated]]. Alternatively, document in the spec that the criterion applies only to non-compat public surface.

30. [ ] **spec-alignment-checker** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-054/src/httpserver/create_webserver.hpp:128` | acceptance-criteria
   Acceptance criterion 1 states '`grep -rE 'shared_ptr<.*http_response' src/httpserver/` returns no results in public headers.' The implementation satisfies action item 1 by preserving `httpserver::compat::auth_handler_v1_ptr` (a `[[deprecated]]` typedef) and `compat::adapt_legacy_auth` in `create_webserver.hpp`. Both definitions contain `std::shared_ptr<http_response>` and are in a public header, so the literal grep check would return results. This is a specification-internal conflict: action item 1 and AC 1 are mutually exclusive when the deprecated shim lives in the public header. The implementation chose correctly to follow the action item (one-transitional-build shim), but the AC as written remains technically unmet.
   *Recommendation:* Clarify the acceptance criterion to read: '`grep -rE 'shared_ptr<.*http_response' src/httpserver/` returns no results OUTSIDE the `httpserver::compat` namespace.' This makes it consistent with the action item that explicitly requires the deprecated compat typedef to exist for one transitional build.

31. [ ] **spec-alignment-checker** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-054/src/httpserver/create_webserver.hpp:null` | acceptance-criteria
   Acceptance criterion 4 states 'One heap allocation removed per authenticated request (verifiable via `bench_auth.cpp` or by inspection).' No `bench_auth.cpp` file was created. The AC explicitly allows verification by inspection, so this is not a blocking gap, but the absence of the benchmark means the allocation improvement cannot be quantified by a repeatable automated test.
   *Recommendation:* Either accept the by-inspection justification as sufficient (the `std::optional<http_response>` return eliminates the `std::shared_ptr` control-block allocation visibly in the code), or create a `test/bench_auth.cpp` micro-benchmark and add it to `test/Makefile.am` to provide a reproducible measurement. The latter is preferable for a GA-blocker item.

32. [ ] **test-quality-reviewer** | `test/integ/authentication.cpp:632` | missing-test
   centralized_auth_handler (line 632) is a file-scope function shared across multiple integration tests. It uses req.get_user() / req.get_pass() which are basic-auth accessors (hence the #ifdef HAVE_BAUTH guard). There is no integration test that exercises the new optional-signature auth handler when HAVE_BAUTH is disabled — meaning the only runtime coverage of the optional path on non-basic-auth builds is the unit tests. This is acceptable by project convention (unit tests cover the non-basic-auth case via nullopt/engaged paths), but worth noting.
   *Recommendation:* Consider extracting a thin integration test (not guarded by HAVE_BAUTH) that exercises the optional auth path purely by inspecting a request header or URL token, independent of basic-auth parsing. This is a nice-to-have; the existing unit coverage is sufficient for the TASK-054 scope.

33. [ ] **test-quality-reviewer** | `test/unit/auth_handler_legacy_shim_test.cpp:172` | naming-convention
   legacy_nullptr_allows_request_through (line 172) and legacy_response_state_is_forwarded_verbatim (line 197) test on-the-wire behavior but do not follow the full MethodName_Scenario_ExpectedBehavior pattern. The suite name is auth_handler_legacy_shim_suite but individual test names drop the 'legacy shim' context prefix already conveyed by the suite. Names are clear enough to read, but the asymmetry with the canonical TU (which uses more explicit scenario names) may cause confusion.
   *Recommendation:* Low priority. Names are readable and the file-scope comment provides full context. No action required unless the project standardizes test naming across TUs.

34. [ ] **test-quality-reviewer** | `test/unit/auth_handler_optional_signature_test.cpp:108` | implementation-coupling
   before_handler_count() reaches into webserver_impl::hooks_before_handler_.size() via a test-access bridge. This is acceptable for a hooks_alias_count test (which explicitly exists to count internal hook registrations), but coupling the optional-signature TU to this internal field means the test will break if the internal hook vector is ever renamed or if the alias is moved to a dedicated slot (like handler_exception_alias_). The wire-behavior tests (nullopt_allows_request_through, engaged_optional_rejects_request) are the load-bearing assertions; the hook-count assertion adds maintenance coupling without catching additional regressions that the wire tests would miss.
   *Recommendation:* Drop the before_handler_count helper from auth_handler_optional_signature_test.cpp and delete the test that uses it (accepting_optional_lambda_registers_one_before_handler — already flagged as redundant in finding #3). Keep the helper and tests only in hooks_alias_count_test.cpp where the coupling is intentional.

35. [ ] **test-quality-reviewer** | `test/unit/auth_handler_optional_signature_test.cpp:134` | test-isolation
   Port numbers are defined via #define PORT 8290 and each test increments by hand (PORT, PORT+1, PORT+2). The legacy shim TU uses PORT 8295 (PORT, PORT+1, PORT+2 = 8295-8297). These ranges are currently non-overlapping, but there is no enforced contract; a future test added to either TU could silently collide with a port in the other TU or with the integration test suite ports.
   *Recommendation:* Add a brief comment to each TU documenting the port range it owns (e.g., '// Ports 8290-8292 reserved by this TU'). Alternatively, use ephemeral ports (port 0) where the test does not need to embed the port in a URL string — though the curl-based fetch helpers hard-code the URL, so a range comment is the minimal-friction fix.
