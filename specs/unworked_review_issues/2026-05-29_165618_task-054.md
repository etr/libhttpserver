# Unworked Review Issues

**Run:** 2026-05-29 16:56:18
**Task:** TASK-054
**Total:** 35 (0 critical, 4 major, 31 minor)

> **Minor sweep status (2026-06-02):** all 4 major findings closed
> in iter1 (see git history). 31 minor items triaged below.

## Major (closed in iter1 — see original audit trail in git history)

All four majors (#1–#4) were closed in the iter1 sweep: hook-count
assertions consolidated in `hooks_alias_count_test.cpp`, a
throwing-handler test added, a 64 KB payload-integrity test added,
and the redundant hook-count test removed from the optional-signature TU.

## Minor — Closed / Moot

| # | File | Category | Disposition |
|---|------|----------|-------------|
| 5 | create-webserver.md / RELEASE_NOTES.md | adr-violation | **Fixed:** both docs now name **v2.1** as the concrete removal target for the compat alias and setter overload. |
| 6 | create_webserver.hpp:143 | pattern-violation | **Acknowledged-deferred:** reviewer notes the inner deprecation diagnostic from `[[deprecated]]` on `adapt_legacy_auth` called from a `[[deprecated]]` overload is a compiler-quirk concern. The setter TU already wraps its tests in `#pragma GCC diagnostic ignored "-Wdeprecated-declarations"` (intentional, see #15); no compiler in our matrix emits a nested warning today. Tracked for the v2.1 removal pass. |
| 7 | webserver_aliases.cpp:224 | code-readability | **Fixed:** rewrote the auth hook lambda — `auth_rejection_response` renamed to `rejection`, idiomatic `if (!rejection)` check, no more explicit type annotation. The intermediate is still kept for clarity inside a `std::move` chain. |
| 8 | create_webserver.hpp:149 | code-readability | **Fixed:** `adapt_legacy_auth` lambda now returns `std::move(*ptr)` directly; the implicit conversion to `std::optional` does the wrapping. |
| 9 | auth_handler_optional_signature_test.cpp:141 | test-coverage | **Moot:** paired `PORT_N` / `PORT_N_STRING` macros already in place since the iter1 Major #4 fix (`PORT_1`..`PORT_4` + matching strings); curl URLs now use `"localhost:" PORT_N_STRING "/protected"`. |
| 10 | auth_handler_optional_signature_test.cpp:63 | test-coverage | **Acknowledged-no-action:** reviewer marks it "Low value given the positive already covers it". Skipped. |
| 11 / 16 / 18 | examples/centralized_authentication.cpp:59 etc. | code-elegance | **Fixed:** dropped the explicit `std::optional<httpserver::http_response>(...)` wrapping in the example and in both test sites (`auth_handler_optional_signature_test.cpp:155, 205`, `test/integ/authentication.cpp:634`). Implicit conversion from `http_response` handles the wrap. |
| 12 / 19 | webserver_aliases.cpp:224 | code-readability / naming | **Fixed:** same edit as #7 — local renamed to `rejection`, type annotation dropped in favour of `auto`. |
| 13 | webserver_aliases.cpp:139 | code-readability | **Fixed:** renamed `install_log_access_alias_` → `install_log_access_alias` to match `install_internal_error_alias` (no trailing underscore on file-scope free functions in anonymous namespace). Updated all call sites and the comment that pointed at the old name as the convention exemplar. |
| 14 | auth_handler_legacy_shim_test.cpp:183 | test-coverage | **Fixed:** added paired `PORT_1` / `PORT_1_STRING` and `PORT_2` / `PORT_2_STRING` macros in the legacy shim TU and rewrote the two curl URLs to use them. |
| 15 | auth_handler_legacy_shim_test.cpp:64 | test-coverage | **Acknowledged-no-action:** reviewer offered to "accept the current approach as pragmatic since the file exists solely to exercise deprecated API". The TU is deprecation-test-only; the pragma scope matches its intent. |
| 17 | webserver_aliases.cpp:226 | code-structure | **Fixed:** see #7 — now `if (!rejection)`. |
| 20 | auth_handler_optional_signature_test.cpp:77 | patterns | **Acknowledged-no-action:** reviewer notes the duplication is benign given no shared unit-test utility header exists; "No action needed for this task". |
| 21 | webserver_aliases.cpp:211 | memory-allocation | **Fixed:** gated the `std::string path(ctx.request->get_path())` construction behind a fast `ws_ptr->auth_skip_paths_normalized.empty()` check at the alias site. Servers with no skip paths now skip both the allocation and the `should_skip_auth` call entirely on the production-typical path. (`should_skip_auth` retains its own TASK-058 empty-list early-out as defence in depth.) |
| 22 | create_webserver.hpp:151 | memory-allocation | **Acknowledged-no-action:** reviewer marks the double `std::function` wrapping inherent to the adapter pattern and "informational only". |
| 23 | examples/centralized_authentication.cpp:54 | sensitive-data-handling | **Fixed:** added an explicit comment to the example noting that production code should read `AUTH_USER` / `AUTH_PASS` once at startup and stash them immutably — per-request `getenv` is not thread-safe wrt. concurrent `setenv`/`putenv` from other threads. |
| 25 | create_webserver.hpp:146 | insecure-design | **Acknowledged-deferred:** reviewer marks "within acceptable risk given the shim is deprecated and transitional". A debug assert on `ptr->get_status() != 0` is a reasonable v2.1-cleanup item. |
| 26 | authentication.cpp:632 | authentication-failures | **Moot:** `http_request::get_user()` returns `const std::string&` and `get_pass()` returns `const std::string&` (BAUTH variants) or `std::string_view` (non-BAUTH); both `operator!=` performs value comparison. Verified by inspection. |
| 28 / 31 | tasks/v2-deferred-backlog-plan.md:138 | acceptance-criteria | **Fixed:** rewrote the "one heap allocation removed" acceptance criterion to make the by-inspection verification explicit (citing `src/detail/webserver_aliases.cpp:221`) — `bench_auth.cpp` is no longer pending. |
| 29 / 30 | tasks/v2-deferred-backlog-plan.md:138 | acceptance-criteria | **Fixed:** rewrote the `grep` acceptance criterion to read `grep -rE 'shared_ptr<.*http_response' src/httpserver/ \| grep -v compat`, explicitly excluding the intentional v1 compat shim per Action Item 1. |
| 32 | authentication.cpp:632 | missing-test | **Acknowledged-deferred:** "nice-to-have", existing unit coverage sufficient for TASK-054 scope. The auth-skip-paths edit (#21) above adds incidental coverage of the empty-list early-out. |
| 33 | auth_handler_legacy_shim_test.cpp:172 | naming-convention | **Acknowledged-no-action:** reviewer marks "Low priority. Names are readable... No action required unless the project standardizes test naming across TUs." |
| 34 | auth_handler_optional_signature_test.cpp:108 | implementation-coupling | **Moot:** addressed by iter1 Major #4 — the `before_handler_count` helper and the test that used it (`accepting_optional_lambda_registers_one_before_handler`) were removed; the hook-count contract now lives only in `hooks_alias_count_test.cpp`. |
| 35 | auth_handler_optional_signature_test.cpp:134 | test-isolation | **Acknowledged-no-action:** reviewer suggests adding port-range comments. The TU now has paired `PORT` / `PORT_N` macros (see #9) which serve the same documentation purpose; the legacy shim TU has its own equivalent (#14). |

## Minor — Deferred

24. **security-reviewer** | `src/detail/webserver_aliases.cpp:197` | broken-access-control
    Auth-oracle / route-enumeration (CWE-204): the before_handler hook only fires for route-matched requests, so unauthenticated clients can distinguish registered from unregistered paths by the 401 vs 404 discrepancy. Reviewer suggests promoting this from an implementation comment to an `@security` block in the auth_handler Doxygen in `create_webserver.hpp`, so callers learn about the property at the API level. Worth doing in a follow-up doc pass when other auth-handler API docs are touched.

27. **spec-alignment-checker** | `specs/product_specs.md:178` | specification-gap
    Add an explicit PRD note in §3.5 or §3.8 stating that the auth_handler callback returns `std::optional<http_response>`, so the requirement chain (PRD → architecture → implementation) is complete on the auth signature dimension. The architecture doc already covers this; the PRD only covers the handler-return-type dimension generally.
