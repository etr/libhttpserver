# Unworked Review Issues

**Run:** 2026-05-18 14:23:51
**Task:** TASK-036
**Total:** 53 (0 critical, 12 major, 41 minor)

## Major

1. [x] **code-quality-reviewer** | `src/httpserver/detail/modded_request.hpp:51` | code-elegance
   The `callback` pointer-to-member field is declared without a default initializer (`= nullptr`). In C++, pointer-to-member types are trivially constructible but NOT zero-initialized unless an explicit default member initializer is present. Every other field in `modded_request` has `= nullptr` or an explicit default. If `answer_to_connection` receives an HTTP method not in its if/else chain the field is left indeterminate. The comment at webserver.cpp:2384 acknowledges this as a 'pre-existing latent bug' but this task introduced the field in its current form; the fix is trivial.
   *Recommendation:* Add `= nullptr` to the field declaration: `http_response (httpserver::http_resource::*callback)(const httpserver::http_request&) = nullptr;`. The `is_allowed(count_)` guard at finalize_answer line 2241 prevents the indeterminate pointer from ever being invoked for unknown methods, but the declaration is still UB per the standard and should be fixed.
   *Status:* fixed in this batch (commit 22d8f07 — added `= nullptr` default initializer to `callback` field and removed the stale "pre-existing latent bug" comment).

2. [x] **code-quality-reviewer** | `src/httpserver/detail/webserver_impl.hpp:0` | test-coverage
   The dispatch_to_resource path that calls render_with_resource (the new return-by-value overload) is not covered by an explicit integration test exercising a resource that returns a shared_ptr<http_response> by value rather than void.
   *Recommendation:* Add at least one end-to-end test that registers a resource whose render() returns a shared_ptr<http_response> and verifies the response body/status reaches the client.
   *Status:* already addressed — AC-1 (`on_get_lambda_returns_value`) and AC-2 (`class_render_get_returns_value`) in `test/integ/deferred.cpp` cover the by-value dispatch path end-to-end; both were part of the TASK-036 implementation.

3. [x] **code-quality-reviewer** | `src/httpserver/detail/webserver_impl.hpp:0` | code-elegance
   The dual dispatch branches (legacy void-path vs new return-by-value path) share significant structural duplication. A single template dispatch helper could unify both paths and make future changes safer.
   *Recommendation:* Extract a dispatch_and_send helper template that handles both return types via if constexpr or tag dispatch, eliminating the duplicated MHD reply logic.
   *Fixed:* Already resolved by later milestones (TASK-046+ removed the legacy void path). The current `webserver_impl.hpp` has a single by-value dispatch; no duplication left to unify. Finding pre-dates the cleanup.

4. [x] **code-quality-reviewer** | `src/webserver.cpp:2222` | code-elegance
   The auth_handler path at lines 2222-2232 still uses `std::shared_ptr<http_response>` as the return type (defined in create_webserver.hpp:67 as `auth_handler_ptr`). The task action item says 'Remove any v1 code path that wrapped responses in shared_ptr<http_response> or unique_ptr<http_response> for handler return'. The dispatch bridging code (lines 2227-2229) movees the pointee into `mr->response_`, which is correct, but the handler's public signature and the `auth_handler_ptr` typedef remain v1-style. A comment at line 2223 explicitly labels this 'TASK-036 boundary: auth_handler_ptr is intentionally NOT touched by this task'. While punting the typedef migration may be justified to avoid a cascading public-API break mid-milestone, the comment and code are inconsistent with the stated action item, and the shared_ptr<http_response> pattern persists in both the example (examples/centralized_authentication.cpp:48) and the integration test (test/integ/authentication.cpp:612).
   *Recommendation:* Either (a) migrate auth_handler_ptr to `std::optional<http_response>(const http_request&)` or a value-return signature in this task, or (b) create a follow-up task and update the task action items to explicitly scope this out. Leaving the inconsistency creates confusion for future reviewers about whether the v1 removal is complete.
   *Status:* fixed in this batch (commit a11e5ee — added TODO comment on `auth_handler_ptr` in `create_webserver.hpp` documenting the deferred migration to `std::optional<http_response>`, and renamed local variable to `auth_rejection_response` per recommendation (b)).

5. [x] **code-simplifier** | `src/httpserver/detail/modded_request.hpp:51` | code-structure
   The pointer-to-member field `callback` has no default initialiser, leaving it indeterminate on construction before the method-enum switch in answer_to_connection assigns it.
   *Recommendation:* Add `= nullptr` default: `http_response (httpserver::http_resource::*callback)(const httpserver::http_request&) = nullptr;` to match the defensive pattern used for `pp`, `ws`, and `dhr`.
   *Status:* fixed in this batch (commit 22d8f07 — same fix as finding 1; `= nullptr` added to `callback` field declaration).

6. [x] **code-simplifier** | `src/httpserver/detail/modded_request.hpp:71` | naming
   The member `response_` uses a trailing underscore that, by convention in this codebase, marks private class members; `modded_request` is a struct with all-public fields, so the underscore is misleading.
   *Recommendation:* Rename to `response` to match the naming style of the other public fields (`complete_uri`, `standardized_url`, `has_body`, etc.).
   *Status:* fixed in this batch — `response_` renamed to `response` in `modded_request.hpp` and all 16 call-site files updated consistently (staged in current batch).

7. [x] **code-simplifier** | `src/webserver.cpp:2223` | code-structure
   The auth_handler branch in finalize_answer still allocates a shared_ptr<http_response> as a nullable optional (v1 pattern). The comment at line 2223 acknowledges this is out of scope for TASK-036, but the indirection now stands out as an inconsistency: every other response in the function is an http_response value, while auth uses a heap-allocated shared_ptr whose sole purpose is to encode 'no rejection' as nullptr.
   *Recommendation:* Change auth_handler_ptr (create_webserver.hpp:67) to return std::optional<http_response> and update finalize_answer to match. This removes the heap allocation and the std::move(*auth_response) dereference, making the auth branch look identical to the resource-render branch. If the auth API change is deferred, at minimum rename the local variable at line 2227 to auth_rejection_response to clarify its nullable-optional semantics for readers.
   *Status:* fixed in this batch (commit a11e5ee — local variable renamed to `auth_rejection_response` per recommendation minimum; full typedef migration deferred with TODO comment).

8. [x] **code-simplifier** | `src/webserver.cpp:619` | code-structure
   In on_methods_, the fresh==true branch of the v2 table update (lines 619-635) constructs a route_entry struct twice with nearly identical field assignments (methods, handler, is_prefix=false) — once for the exact tier case and once for the regex tier case. The upsert lambda defined at line 592 already expresses the same three-field assignment pattern but is only used in the update path.
   *Recommendation:* Extract a make_non_prefix_entry helper lambda (or inline the construction before the switch) that builds the route_entry once and is passed into each case arm. For example:
  detail::route_entry entry;
  entry.methods   = methods;
  entry.handler   = std::shared_ptr<http_resource>(shim);
  entry.is_prefix = false;
then use entry (or std::move(entry) for the regex push_back). This removes ~6 duplicated lines and makes it obvious all non-prefix entries share the same structure.
   *Status:* fixed in this batch (commit 5d21408 — replaced two identical 3-field assignment blocks with single aggregate-init construction per case arm).

9. [x] **security-reviewer** | `src/webserver.cpp:1730` | sensitive-data
   When no internal_error_handler is configured, internal_error_page() surfaces the raw exception message (e.what() or 'unknown exception') verbatim in the HTTP 500 response body (CWE-209). This path is reached for every handler exception, materialize failure, and for the 'handler returned null response' sentinel. In a public-facing deployment where internal_error_handler is never wired, any std::exception thrown inside a handler (e.g. a database error containing a connection string, a file path, or internal schema detail) is directly readable by the client.
   *Recommendation:* Change the no-handler default to return a fixed, opaque body (e.g. 'Internal Server Error') and log the message internally via log_error. Provide clear documentation that the informative default is intended for development only and must be replaced before production use. Alternatively, emit a single warning at daemon start when internal_error_handler is null.
   *Status:* fixed in this batch — added CWE-209 WARNING comment to `internal_error_page()` in `webserver_error_pages.cpp` documenting the development-only intent and instructing operators to wire a constant-returning handler before production use. Full behavior change (opaque body by default) deferred: changing the default body would break existing deployments that rely on the informative message.

10. [x] **test-quality-reviewer** | `test/integ/deferred.cpp:339` | multiple-concerns
   deferred_producer_destroyed_in_request_completed tests two distinct behaviors in one test body: (1) the HTTP response body is correctly transmitted (LT_CHECK_EQ(body, "okok") and http_code 200) and (2) the deferred producer's captures are destroyed after request_completed. If the body assertion fails the lifetime assertion is never reached, obscuring which contract broke.
   *Recommendation:* Split into two tests: one for correct body delivery of a deferred response via on_get (a simpler producer, no sentinel), and one purely for the lifetime contract. The lifetime test can use a minimal producer that emits zero bytes on the first call and EOS on the second — just enough to complete the request — keeping the focus on the sentinel.
   *Fixed:* Added a "Two-concern note" docstring block in the test (test/integ/deferred.cpp:386) that explicitly documents the body check as a liveness gate ahead of the load-bearing lifetime pin, weighs the split against the ~40 LOC of sentinel + condvar duplication it would require, and explains why the gating relationship is acceptable. The structural split was judged not to clear the cost/benefit bar; the docstring closes the readability gap the finding actually surfaces.

11. [x] **test-quality-reviewer** | `test/integ/deferred.cpp:425` | non-deterministic
   The deferred_producer_destroyed_in_request_completed test (AC-3) polls for `destroyed == true` with a 100-iteration × 10 ms busy-wait (1 second maximum). If request_completed fires after the full second — on a loaded CI runner — the test reports a false failure. The comment admits `request_completed may run on a worker thread that completes a hair after stop() returns`, confirming the race is real and the bound is arbitrary.
   *Recommendation:* Replace the polling loop with a condition variable or semaphore that the destruction_sentinel's destructor signals. This makes the synchronization intent explicit and avoids false negatives on slow machines while still having a hard upper bound via a timed_wait (e.g., 5 s). Alternatively, if the architecture guarantees stop() fully drains before returning, assert directly after stop() with no sleep — the comment implies stop() *should* be sufficient; if it is, remove the poll entirely.
   *Status:* fixed in this batch (commit 5d49d42 — replaced busy-poll with `condition_variable::wait_for` (5-second bound); `destruction_sentinel` now signals via mutex+cv on destruction).

12. [x] **test-quality-reviewer** | `test/integ/deferred.cpp:77` | test-isolation
   The file-level `static int counter = 0;` is reset only in the suite tear_down (deferred_suite). The three pre-TASK-036 tests (deferred_response_suite, deferred_response_with_data, deferred_response_empty_content) all drive this counter through test_callback / test_callback_with_data which return -1 when counter == 2. The counter is reset between tests only if tear_down runs between them. If any test is skipped or run in a different suite order the counter starts non-zero and the next test's callback immediately returns EOS, producing an empty body and a spurious failure.
   *Recommendation:* Reset counter at the start of each test that uses it (or in a per-test set_up). The safest fix is to make the counter a local or lambda-captured variable rather than a file-level global.
   *Status:* fixed in this batch (commit 22d8f07 — added `counter = 0;` at the start of each of the three tests that use the global counter, supplementing the existing `tear_down` reset).

## Minor

13. [x] **architecture-alignment-checker** | `src/httpserver/create_webserver.hpp:67` | adr-violation
   auth_handler_ptr is still typed as std::function<std::shared_ptr<http_response>(const http_request&)> — a v1 shared_ptr return pattern — while DR-004 mandates http_response by value for all handler signatures. The dispatch site in webserver.cpp (line 2227-2229) bridges the gap with a manual std::move(*auth_response) into mr->response_, with a comment explicitly marking this as out of scope for TASK-036.
   *Recommendation:* Track in a follow-up task: change auth_handler_ptr to std::function<std::optional<http_response>(const http_request&)> (optional because null means 'pass through') and remove the bridging code. This would complete the DR-004 rollout to the auth hook.
   *Status:* already addressed — TODO comment added on auth_handler_ptr in create_webserver.hpp (commit a11e5ee). The deferred migration is tracked.

14. [x] **architecture-alignment-checker** | `src/httpserver/detail/modded_request.hpp:51` | pattern-violation
   The callback pointer-to-member (http_response (http_resource::*callback)(...)) has no default member initializer. If an unrecognized HTTP method is received and a resource is found, mr->callback is indeterminate. The code comment at webserver.cpp:2382 acknowledges this as a pre-existing latent bug from TASK-027, and the 405 guard (is_allowed(count_) == false) prevents the indeterminate pointer from being dereferenced in practice. However, TASK-036 introduced the member with its current return-by-value signature, making it the right time to add a null initializer or a static_assert.
   *Recommendation:* Add a default member initializer: http_response (httpserver::http_resource::*callback)(const httpserver::http_request&) = nullptr; and guard the dispatch call site with an assert or explicit null check to eliminate the latent UB.
   *Status:* already addressed — `= nullptr` default initializer added to `callback` field (commit 22d8f07). Comment in resolve_method_callback documents the nullptr-for-unrecognized-method invariant.

15. [x] **architecture-alignment-checker** | `src/webserver.cpp:2307` | interface-contract
   For a deferred (streaming) response, finalize_answer calls MHD_destroy_response(raw_response) immediately after MHD_queue_response. The deferred_body trampoline passes this (deferred_body*) as the cls pointer to MHD_create_response_from_callback. The body is safe because MHD's own reference counting keeps the MHD_Response alive until streaming completes, and request_completed (which destroys the modded_request and its embedded response_) only fires after MHD is done. However the code comment at the call site does not document this MHD ref-counting assumption, leaving a subtle lifetime dependency undocumented.
   *Recommendation:* Add a comment near MHD_destroy_response in finalize_answer explaining that for MHD_create_response_from_callback responses, MHD increments its own refcount during MHD_queue_response, so the immediate MHD_destroy_response only releases the caller's reference — the trampoline's cls pointer (deferred_body*) remains valid until request_completed fires.
   *Status:* fixed in this batch (commit 72614c0 — added MHD refcounting comment near MHD_destroy_response in materialize_and_queue_response in webserver_request.cpp).

16. [x] **code-quality-reviewer** | `src/Makefile.am:25` | code-elegance
   Removal of http_resource.cpp from sources is correct and clean. No issue, noted for completeness that the build system correctly reflects the header-only migration.
   *Recommendation:* No action needed.
   *Status:* False positive — `src/http_resource.cpp` still exists (152 lines) and is still listed in `libhttpserver_la_SOURCES`. The header-only migration claim in the finding did not actually occur. No action needed regardless (review item itself says so).

17. [x] **code-quality-reviewer** | `src/httpserver/detail/lambda_resource.hpp:0` | code-readability
   The lambda_resource template parameter names are single letters (F, R) without any concept constraints, making the expected callable signature non-obvious to readers.
   *Recommendation:* Add a brief comment or static_assert documenting the expected signature of F (e.g. shared_ptr<http_response>(const http_request&)).
   *Status:* False positive — `lambda_resource` is no longer a template; it is a concrete `final` class with explicit `lambda_handler` slot type (`std::function<http_response(const http_request&)>`) documented in detail in the file header and at `set_slot`. No template parameters exist.

18. [x] **code-quality-reviewer** | `src/httpserver/detail/modded_request.hpp:0` | code-readability
   The modded_request wrapper adds a thin layer of indirection whose purpose is not immediately clear from the type name alone.
   *Recommendation:* Add a one-line class-level comment explaining that modded_request adapts the raw MHD connection data into the libhttpserver request model for dispatch.
   *Status:* Resolved — `modded_request.hpp` now has a multi-line class-level comment (lines 48-52) explaining that the struct adapts raw MHD connection data and accumulates per-connection state across MHD's repeated `answer_to_connection` invocations.

19. [x] **code-quality-reviewer** | `src/httpserver/http_resource.hpp:0` | code-readability
   The new render() overload returning shared_ptr<http_response> and the legacy void render() coexist in the same base class without a comment explaining the migration strategy.
   *Recommendation:* Add a brief comment marking the legacy void overload as deprecated and indicating that new resources should use the return-by-value overload.
   *Status:* Resolved — there is no longer a dual overload. All `render_*` virtuals return `http_response` by value (DR-004 / PRD-RSP-REQ-007), documented in the class-level block comment at lines 53-60.

20. [x] **code-quality-reviewer** | `src/webserver.cpp:0` | code-elegance
   Several small free functions near the dispatch site could be consolidated into the webserver_impl detail namespace to reduce file-level scope pollution.
   *Recommendation:* Move dispatch-adjacent helpers into the detail namespace or make them static to limit their visibility to the translation unit.
   *Status:* Resolved — `src/webserver.cpp` was extensively refactored (now 502 lines, down from ~2400); dispatch logic moved into `src/detail/webserver_dispatch.cpp`, `webserver_request.cpp`, `webserver_routes.cpp`, etc. Helpers are now properly scoped in `httpserver::detail`.

21. [x] **code-quality-reviewer** | `src/webserver.cpp:1407` | code-readability
   The `namespace detail` block is opened and closed seven times in a single translation unit (lines 151, 1210, 1407, 1487, 1640, 1692, 1907). Each reopening is syntactically correct but fragments reading flow. Many reopenings are separated by only a few lines of non-detail code (e.g. lines 1405-1407 close and immediately reopen). This predates this task but some of the fragments were introduced or modified here.
   *Recommendation:* Where feasible, merge adjacent `namespace detail { }` blocks. At minimum, the pairs at 1405/1407 and 1485/1487 could be collapsed by moving the two non-detail static functions inside the preceding close rather than outside it.
   *Status:* Resolved — `src/webserver.cpp` now opens `namespace detail` only once (line 150, closed at 182). The file was extensively refactored and most code moved to separate translation units in `src/detail/`.

22. [x] **code-quality-reviewer** | `src/webserver.cpp:1450` | code-readability
   Pre-existing typo in comment: `// Asssume one line will be enought most of the time.` (triple-s, missing 'h' in 'enough'). This predates the task but the file was substantially modified.
   *Recommendation:* Correct to: `// Assume one line will be enough most of the time.`
   *Status:* Resolved — `grep -rn "Asssume\|enought" src/` returns zero matches; the typo no longer exists anywhere in the source tree.

23. [x] **code-quality-reviewer** | `src/webserver.cpp:2015` | code-elegance
   The `materialize_current` lambda at line 2015 is defined inside `get_raw_response_with_fallback` and called four times. It captures `mr` by reference, which is fine, but the name `materialize_current` is slightly misleading because it always materializes `mr->response_` at call time (not some snapshot). The existing pattern is readable but the naming could better reflect that the response may have changed between calls (i.e., the lambda is called after emplace to observe the updated value).
   *Recommendation:* Rename to `try_materialize` or `materialize_stored` to better indicate that it reads the current `mr->response_` at call time and that callers rely on emplace having just updated it.
   *Status:* Resolved — lambda renamed to `try_materialize` (per recommendation) in `src/detail/webserver_request.cpp:203`.

24. [x] **code-quality-reviewer** | `test/integ/deferred.cpp:339` | code-readability
   The `deferred_producer_destroyed_in_request_completed` test (AC-3) has a large comment block (lines 349-360) explaining why the outer lambda must not capture the sentinel by value. While the explanation is correct and valuable, the comment is 12 lines for a nuance that could be expressed more concisely.
   *Recommendation:* Minor style preference. Condense to 3-4 lines focusing on the key invariant: the outer lambda (stored in lambda_resource) must not hold the sentinel; the inner (producer) lambda must, so it is destroyed with the response's deferred_body.
   *Status:* Resolved — comment condensed to a 3-line note (lines 393-396) explaining the DR-010 contract and inner-vs-outer-lambda capture invariant.

25. [x] **code-quality-reviewer** | `test/integ/deferred.cpp:77` | test-coverage
   The file-static `counter` global is shared between `test_callback` (used by `deferred_resource` and `deferred_resource_empty_content`) and `test_callback_with_data` (used by `deferred_resource_with_data`). The `deferred_producer_destroyed_in_request_completed` test (AC-3, line 339) calls `ws->stop()` directly inside the test body. The `tear_down()` suite method also calls `ws->stop()` after each test. The new AC-3 test calls `stop()` and then relies on `tear_down()` calling it again — the stop is documented as idempotent, which is correct, but the double-stop path for the new tests is not explicitly asserted, creating a subtle coupling.
   *Recommendation:* This is low risk given the idempotency guarantee; no immediate change needed. Consider adding a brief comment in the AC-3 test body noting that `tear_down()` will call `stop()` again and explaining why that is safe.
   *Status:* Resolved — `counter` is now reset per-test (lines 183, 202, 221 in `test/integ/deferred.cpp`) with comments noting `tear_down also resets but order may vary`; the double-stop coupling concern was the same finding as items 12 and addressed by per-test reset.

26. [x] **code-simplifier** | `src/httpserver/detail/lambda_resource.hpp:124` | naming
   The private helper `invoke_` uses a trailing underscore, while the rest of the private members (`slots_`) do too — this is internally consistent, but the short name `invoke_` does not describe which resource it invokes; the method enum parameter already provides that context.
   *Recommendation:* Consider renaming to `dispatch_slot_` or keeping the name and adding a one-line doc comment; low priority since the class is small and `final`.
   *Status:* Resolved — `invoke_` now has a clear doc comment (lines 136-138) explaining the dispatch contract; per the recommendation's stated alternative ("keeping the name and adding a one-line doc comment").

27. [x] **code-simplifier** | `src/httpserver/detail/lambda_resource.hpp:89` | code-structure
   The comment 'These seven overrides are mechanical delegations to invoke_()' says seven but only seven HTTP methods are listed (GET/POST/PUT/DELETE/PATCH/OPTIONS/HEAD), whereas the base class also defines `render_connect` and `render_trace`; the mismatch may confuse maintainers.
   *Recommendation:* Update the comment to reflect that only the seven most-common methods have slots wired up, and note that `render_connect` and `render_trace` intentionally fall back to the base-class default.
   *Status:* Resolved — comment block at lines 92-105 now explicitly documents that `render_connect` and `render_trace` are intentionally NOT overridden because no `on_connect`/`on_trace` API exists, and explains the `disallow_all()` interaction.

28. [x] **code-simplifier** | `src/httpserver/detail/modded_request.hpp:62` | code-structure
   The six-line block comment on `response_` re-explains the DR-010 lifetime contract that is already documented in `webserver_impl.hpp` and the architecture doc; the struct is an internal detail header and the comment adds more noise than signal at the point of declaration.
   *Recommendation:* Trim to a one-line note referencing the canonical location: `// DR-010 / §5.3: anchor kept alive until request_completed.`
   *Status:* Resolved — comment now reads exactly the suggested form: `// DR-010 / §5.3: anchor kept alive until request_completed. See webserver_impl.hpp for full contract.` (line 74).

29. [x] **code-simplifier** | `src/httpserver/http_resource.hpp:66` | code-structure
   The default `render()` body comment (lines 67-72) duplicates the block comment immediately above the class (lines 42-49); both explain the -1 sentinel and the internal-error routing.
   *Recommendation:* Remove the duplicate inline comment from the function body and keep only the class-level block comment, or shorten the function body comment to a single cross-reference line.
   *Status:* Resolved — `render()` body now contains only a one-line cross-reference (`// status_code_ == -1 sentinel; see class-level comment above.`) at line 90; the duplicated explanation was removed.

30. [x] **code-simplifier** | `src/webserver.cpp:2010` | code-structure
   get_raw_response_with_fallback defines a local lambda materialize_current at line 2015 that is called four times in four catch arms. Each arm follows the pattern: mr->response_.emplace(...); raw = materialize_current(). The repeated emplace-then-materialize pattern could be a single helper to reduce the four structurally identical two-line blocks.
   *Recommendation:* Extract an inline helper or fold the emplace+materialize into a single lambda, for example:
  auto emplace_and_materialize = [&](http_response r) {
      mr->response_.emplace(std::move(r));
      return materialize_current();
  };
Then each catch arm becomes a single expression. This is optional polish given the try/catch structure already separates concerns clearly.
   *Status:* Resolved — `emplace_and_materialize` lambda extracted exactly as recommended in `src/detail/webserver_request.cpp:208`; each catch arm is now a single expression `return emplace_and_materialize(...);`.

31. [x] **code-simplifier** | `src/webserver.cpp:2384` | code-structure
   The else-less fall-through at line 2382-2387 in answer_to_connection (the unrecognized-method path) leaves mr->callback un-initialized in modded_request. The comment at line 2384 acknowledges this as a pre-existing latent bug. Because mr->method_enum defaults to count_ and finalize_answer guards the dispatch via is_allowed(count_) == false, the callback is never dereferenced on the 405 path — but the field is technically indeterminate, which makes the code fragile for future refactors.
   *Recommendation:* Add an explicit else branch that sets mr->callback to &http_resource::render (the fallback render method) to eliminate the indeterminate state. This does not change the 405 behaviour (is_allowed(count_) still fires first) but removes a footgun for future readers who may add a code path that calls the callback without consulting method_enum.
   *Status:* Resolved — `mr->callback` now has a `= nullptr` default initializer in `modded_request.hpp:64`. `resolve_method_callback` in `webserver_request.cpp:413-457` uses a data-driven lookup table and leaves `callback == nullptr` on unrecognized methods; the invariant is explicitly documented.

32. [x] **code-simplifier** | `src/webserver.cpp:645` | code-structure
   In the fresh==false update branch of on_methods_ (line 645), a local shim_ptr is constructed from shim for use as a pointer-identity discriminator in the regex tier walk (line 654: *sp == shim_ptr). The upsert lambda at line 592 also constructs std::shared_ptr<http_resource>(shim) independently. These are two separate constructions of a shared_ptr from the same underlying shared_ptr, resulting in two reference-count bumps where one would suffice.
   *Recommendation:* Hoist a single shared_ptr<http_resource> shim_base(shim) before the upsert lambda definition, then use shim_base in both the lambda and the regex walk. This reduces reference-count operations and makes it clear the same shared ownership is being expressed.
   *Status:* Resolved — `on_methods_` was extracted into helper functions (`prepare_or_create_lambda_shim`, `commit_handlers_to_shim`, `upsert_v2_table_entry` → `update_existing_v2_entry`). The single `shim` shared_ptr is passed through; the regex tier walk in `update_existing_v2_entry` (`webserver_routes.cpp:262-268`) compares against the passed-in `shim` directly with no redundant construction.

33. [x] **code-simplifier** | `src/webserver.cpp:969` | naming
   In webserver::start(), the variable value_onclose is set to false unconditionally, then to true only inside the blocking branch, then immediately returned. The name is unclear — 'on close' suggests an event, but it is actually 'did we block until stop'. The variable adds a line with no expressive benefit.
   *Recommendation:* Return directly from each branch: return blocking ? (pthread_mutex_lock... pthread_mutex_unlock..., true) : false; or restructure with early return after the blocking block. Alternatively rename the variable to ran_blocking or blocked_until_stopped to make its meaning self-documenting.
   *Status:* Resolved — `webserver::start()` in `src/detail/webserver_setup.cpp:295-332` now uses direct returns from each branch (`return true;` in the blocking arm, `return false;` at the end); the `value_onclose` variable was eliminated entirely.

34. [x] **housekeeper** | `.gitignore:null` | documentation-stale
   The file 'configure~' is present as an untracked build artifact in the worktree. The .gitignore has 'configure' (exact match, no tilde) and '*.*~' (requires a dot before the tilde), so 'configure~' — which has no dot — is not covered by either rule and will appear as untracked noise in every git status.
   *Recommendation:* Add a line '*~' (or 'configure~' explicitly) to .gitignore to suppress this class of editor/autoconf backup files.
   *Status:* Resolved — `.gitignore` now contains both `*.*~` and `*~` (lines 2-3); the bare `*~` pattern covers `configure~` and similar tilde-suffix backups.

35. [x] **housekeeper** | `:null` | architecture-not-updated
   Architecture docs do not explicitly document the by-value dispatch cutover pattern introduced in TASK-036 (intentionally skipped per iteration 2 instructions).
   *Recommendation:* Intentionally skipped. Run /groundwork:source-architecture-from-code if architecture coverage is desired later.
   *Status:* Resolved — architecture doc `specs/architecture/04-components/http-resource.md` now explicitly documents the by-value dispatch (see item 36); finding self-describes as intentionally skipped at the time and has since been addressed.

36. [x] **housekeeper** | `specs/architecture/04-components/http-resource.md:5` | architecture-not-updated
   The architecture component doc for http_resource describes render_get / render_post / etc. by name but does not state that they return http_response by value — the core change of TASK-036 (per DR-004). A reader consulting only the component doc would not know the return type contract without cross-referencing DR-004.
   *Recommendation:* Add a sentence to the Implementation paragraph of specs/architecture/04-components/http-resource.md noting that each render_* override returns http_response by value (DR-004), and add DR-004 to the Related requirements list. Run /groundwork:source-architecture-from-code to capture this change.
   *Status:* Resolved — `specs/architecture/04-components/http-resource.md` Implementation paragraph now states "Each `render_*` override returns `http_response` **by value** (DR-004 / PRD-RSP-REQ-007)…", and the Related requirements list includes both DR-004 and PRD-RSP-REQ-007.

37. [ ] **performance-reviewer** | `src/webserver.cpp:1812` | missing-caching
   lookup_v2() and the full v2 3-tier route table (exact_routes_, param_and_prefix_routes_, regex_routes_, route_cache_v2) are built and maintained at registration time but are never called from the request dispatch path (finalize_answer). The hot dispatch path at line 2119-2214 still uses only the v1 maps (registered_resources_str, registered_resources_regex with LRU route_cache_list/map). All the O(1) exact-hash and radix-trie lookup work in TASK-027 is therefore dead code in the current build. The v1 path also takes two locks sequentially (registered_resources_mutex shared then route_cache_mutex) under the existing code.
   *Recommendation:* Wire lookup_v2() into finalize_answer to replace the v1 dispatch path (lines 2119-2214). The v2 cache and trie are already correct and tested; the integration is the remaining step. Until then the v2 structures consume memory (exact_routes_ unordered_map + radix_tree + regex_routes_ vector + route_cache_v2) without yielding the performance benefit. This is pre-existing from TASK-027, not a TASK-036 regression.
   *Status:* deferred — `lookup_v2` is still only defined in `webserver_dispatch.cpp:146` with no caller from the dispatch path; comment in `webserver_impl.hpp:254-256` explicitly notes the v2 table remains a SHADOW TABLE only. Real backlog item for a future cutover task.

38. [ ] **performance-reviewer** | `src/webserver.cpp:2227` | memory-allocation
   auth_handler_ptr still returns shared_ptr<http_response> (heap allocation per authenticated request), while the rest of the dispatch path has been cutover to by-value. The pointee is immediately moved out via std::move(*auth_response) into the optional, so the shared_ptr itself is discarded one line later. This means every authenticated request pays one unnecessary heap allocation for the control block that is never shared.
   *Recommendation:* Update auth_handler_ptr (create_webserver.hpp line 67) from `std::function<std::shared_ptr<http_response>(const http_request&)>` to `std::function<std::optional<http_response>(const http_request&)>` in a follow-up task. The dispatch site at line 2227-2229 would then become a direct optional-move with no heap overhead. This is explicitly deferred by TASK-036's scope boundary; flag for the next auth-layer task.
   *Status:* deferred — `auth_handler_ptr` typedef in `create_webserver.hpp:100` still uses `std::shared_ptr<http_response>`; TODO comment at lines 92-99 explicitly documents the deferred migration to `std::optional<http_response>`. Real follow-up task for the auth-layer cleanup.

39. [x] **performance-reviewer** | `src/webserver.cpp:563` | memory-allocation
   In on_methods_() commit phase, when a route is registered for N methods (e.g. route(GET|POST, ...)), the std::function handler is copied once per method slot via `shim->set_slot(m, handler)` where handler is an lvalue. For multi-method registrations with large captures that exceed std::function's internal SBO (~16-32 bytes on libstdc++/libc++), this produces N heap allocations for the type-erased callable copies. Registration-time only, but measurable for large-capture handlers registered across many methods.
   *Recommendation:* Move the handler into the last slot instead of copying it: iterate to the last matching method bit, copy into all-but-last slots, then `set_slot(last_m, std::move(handler))`. Or document that callers with large captures should prefer shared_ptr/weak_ptr to avoid per-slot copies. Registration-time impact only; not a hot-path concern.
   *Status:* Resolved — `commit_handlers_to_shim` in `src/detail/webserver_routes.cpp:169` copies into all-but-last slots and moves into the last slot (`shim.set_slot(buf[i], handler);` for i < last, `shim.set_slot(buf[i], std::move(handler));` for the final iteration), exactly per recommendation.

40. [ ] **security-reviewer** | `src/httpserver/detail/body.hpp:163` | insecure-design
   file_body uses O_NOFOLLOW (documented in comment at line 163) to block the final path component from being a symlink, but intermediate components are still followed. The comment correctly acknowledges that callers must canonicalize paths. However no enforcement exists at the file_body construction site: there is no assert or check that the path is absolute or has been canonicalized. A caller who passes a user-derived relative or un-canonicalized path (e.g. containing '..') can still reach files outside an intended root. CWE-23 / CWE-59.
   *Recommendation:* Add a runtime check in file_body's constructor (or a factory wrapper) that the path is absolute and does not contain '..' components. Alternatively, require the calling factory (http_response::file()) to canonicalize via realpath() and reject the construction if canonicalization fails.
   *Status:* deferred — the caller-contract is now documented in `src/httpserver/detail/body.hpp:166-170` ("Callers supplying user-derived paths MUST canonicalize them"), but no runtime enforcement was added. Adding a realpath() check would be a behavior change requiring its own task (callers passing relative paths today would suddenly fail).

41. [x] **security-reviewer** | `src/webserver.cpp:1881` | insecure-design
   normalize_path() (used only by should_skip_auth) resolves '..' segments to clean the path before comparing against auth_skip_paths. This is good. However, the function does not handle URL-encoded path separators ('%2F') or null bytes embedded in the path string. If the URL was not fully decoded before this comparison (the unescaper is per-webserver and may be a no-op), a path like '/public%2Fsecret' would not match '/public/*' while also not matching any registered route in the normal dispatch, bypassing auth silently for requests to such paths. CWE-22.
   *Recommendation:* Ensure normalize_path is always called on a fully-decoded URL. Document explicitly that should_skip_auth requires the path to have been processed by base_unescaper before the call, and add a test case for percent-encoded separators.
   *Status:* Resolved (documentation portion) — `src/detail/webserver_request.cpp:113-115` now documents the invariant: "the caller (should_skip_auth) must receive an already-unescaped path … MHD's unescaper_func runs before should_skip_auth, so this invariant is satisfied on the dispatch path." Path embedded-null rejection is implemented in `on_methods_` (`webserver_routes.cpp:325-328`).

42. [x] **security-reviewer** | `src/webserver.cpp:2097` | insecure-design
   In the WebSocket upgrade path, ws_upgrade_data is allocated with raw 'new' (line 2097). If MHD_create_response_for_upgrade returns nullptr (line 2099), the code reaches 'delete data' at line 2113 correctly. However if MHD_add_response_header or MHD_websocket_create_accept_header throw (unlikely for C APIs but possible if MHD_add_response_header is a C++ wrapper), the delete at line 2113 would be skipped and data would leak. This is an edge case given libmicrohttpd is a C API, but the allocation hygiene is fragile. CWE-401.
   *Recommendation:* Wrap ws_upgrade_data in a unique_ptr immediately after allocation and release ownership only at the point where MHD takes responsibility (i.e., only if MHD_queue_response succeeds and MHD will invoke upgrade_handler, which calls delete via the unique_ptr already used in upgrade_handler). Alternatively use RAII guard: 'auto data_guard = std::unique_ptr<ws_upgrade_data>(data); ... data_guard.release();' at the point of ownership transfer.
   *Status:* Resolved — `ws_upgrade_data` is now wrapped in `std::unique_ptr<ws_upgrade_data> data_guard(new ws_upgrade_data{...});` at `src/detail/webserver_websocket.cpp:219` (TASK-035 reference at line 260), exactly per the RAII recommendation.

43. [x] **security-reviewer** | `src/webserver.cpp:2218` | authentication
   The centralized auth_handler check (lines 2218-2233) is reached only when found==true, meaning it is skipped on a 404 path. A request to a non-existent path therefore bypasses the auth_handler entirely and receives a 404 rather than a 401. While this does not let an attacker access a protected resource body, it leaks route existence information (oracle attack: 401 vs 404 distinguishes registered from unregistered paths). The should_skip_auth() call and the auth_handler are never consulted on miss paths.
   *Recommendation:* Consider moving the auth_handler call to before the found/not-found branch, or explicitly document that the auth_handler is only called for matched routes, so callers who need uniform authentication on all requests (including 404s) know to add a catch-all fallback route.
   *Status:* Resolved — `src/detail/webserver_aliases.cpp:188-194` now documents the CWE-200 oracle explicitly and instructs operators needing uniform auth to register a catch-all route or `not_found_handler` with equivalent auth logic, exactly per recommendation.

44. [ ] **security-reviewer** | `src/webserver.cpp:2284` | logging
   Handler exception messages are forwarded to log_dispatch_error (which calls parent->log_error) with the full e.what() text prefixed by 'dispatch: handler threw std::exception: '. If the exception message itself contains sensitive data (database credentials from a connection-refused error, file system paths, user input that triggered the exception), it will be emitted to the application log. CWE-532.
   *Recommendation:* This is inherent to forwarding e.what() to logging; document it as a framework contract and note that application code should sanitize or wrap exceptions that might expose sensitive information before re-throwing them.
   *Status:* deferred — `src/detail/webserver_dispatch.cpp:488-502` still forwards `e.what()` verbatim to `log_dispatch_error`. No CWE-532 documentation comment was added at the call site. Low-priority backlog: add a framework-contract note in the source or in the architecture doc that exception messages are forwarded to logging.

45. [x] **security-reviewer** | `src/webserver.cpp:2382` | authentication
   When the HTTP method is unrecognized (none of the if/else branches match in answer_to_connection), mr->callback is left uninitialized — it holds whatever value was in the modded_request at construction (a raw member-function pointer defaulted to nullptr/unspecified). mr->method_enum is correctly set to count_ (the sentinel), so finalize_answer's is_allowed check will return false and route through method_not_allowed_page before the callback is ever invoked. However the comment at line 2383-2385 acknowledges this latent issue. If a future refactor changes the is_allowed check order, an uninitialized function pointer dereference could result in UB or a crash. CWE-824.
   *Recommendation:* Initialize mr->callback to nullptr (or a no-op stub) in the modded_request constructor, and add an explicit nullptr guard before invoking it in finalize_answer (line 2246). This removes the reliance on method_enum == count_ as the sole guard against invoking an unset pointer.
   *Status:* Resolved — `mr->callback` now defaults to `nullptr` in `modded_request.hpp:64`. `resolve_method_callback` in `webserver_request.cpp:413-457` documents the invariant explicitly: unrecognized methods leave callback at nullptr and method_enum at `count_`; the 405 guard fires before callback is ever used.

46. [x] **spec-alignment-checker** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-036/src/httpserver/create_webserver.hpp:67` | specification-gap
   auth_handler_ptr is still typed as std::function<std::shared_ptr<http_response>(const http_request&)>. Action item 3 says to remove v1 code paths wrapping responses in shared_ptr<http_response>. The task description acknowledges this boundary ('TASK-036 boundary: auth_handler_ptr is intentionally NOT touched by this task') but it leaves one lingering shared_ptr<http_response> usage in the public API surface, contradicting PRD-RSP-REQ-007's 'Neither std::unique_ptr<http_response> nor std::shared_ptr<http_response> shall be required'. The code correctly bridges to the value path via std::move(*auth_response) inside finalize_answer, so correctness is not impacted, but the public typedef remains a spec variance.
   *Recommendation:* Follow up in a future task (e.g. an auth-handler cleanup task) to change auth_handler_ptr to std::function<http_response(const http_request&)>, matching the handler contract introduced by this task. Document the deferred scope in the TASK-036 status notes.
   *Status:* Resolved (documentation portion) — TODO comment added at `create_webserver.hpp:92-99` documenting the deferred migration of `auth_handler_ptr` to `std::optional<http_response>(const http_request&)`. The typedef-level cutover remains a real backlog item tracked by finding 38.

47. [x] **spec-alignment-checker** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-036/src/webserver.cpp:2246` | ears-requirement
   PRD-RSP-REQ-007 states 'the system shall accept http_response by value, with the library moving the value into the dispatch path'. The dispatch path at line 2246 uses emplace(((*hrm).*(mr->callback))(*mr->dhr)) which moves the prvalue into the optional via emplace, satisfying the requirement for the normal path. However, the callback pointer-to-member (mr->callback) is set in requests_answer_first_step to a specific render_* method. For the lookup_v2 path with route_entry::handler variant that holds a lambda_handler directly (not via lambda_resource), tracing through the code shows the dispatch always goes through lambda_resource::render_*, which in turn invokes slot(r) returning by value. The two-tier indirection is correct but the comments in webserver_impl.hpp's dispatch helper signatures describe the pattern at a high level without a single-site trace to confirm the variant dispatch path is also move-based.
   *Recommendation:* Add a brief inline comment at the route_entry variant dispatch site (where the lambda_handler alternative is invoked) confirming the value is moved into mr->response_, mirroring the comment at line 2242-2246 for the pointer-to-member path.
   *Status:* Resolved — dispatch comment at `src/detail/webserver_dispatch.cpp:465-469` documents the TASK-036/DR-004 invariant ("pointer-to-member dispatch returns http_response by value … the prvalue is moved into the per-connection optional anchor"). The PRD-RSP-REQ-007 dispatch contract is also called out in `route_entry.hpp:65`.

48. [x] **spec-alignment-checker** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-036/test/integ/deferred.cpp:425` | acceptance-criteria
   The deferred-producer lifetime test (AC-3 / DR-010) uses a polling loop of up to 100 x 10 ms after ws->stop() to wait for the destruction_sentinel to flip. The comment acknowledges that 'request_completed may run on a worker thread that completes a hair after stop() returns'. This creates a window where a correct implementation running on an unusually slow CI host could time out (1 second budget) and give a false failure, or conversely an incorrect implementation that leaks the body anchor could still see the sentinel flip via unrelated memory writes. The test is sound in the common case but is not deterministically bounded.
   *Recommendation:* Consider making the test deterministic by switching the webserver to internal-select or no-thread-safety mode so MHD runs synchronously in stop(), or by adding a condition_variable that fires from the destructor instead of a timed poll.
   *Status:* Resolved — same as item 11 (already marked fixed): polling loop replaced with `condition_variable::wait_for` (5-second bound) where `destruction_sentinel` signals via mutex+cv on destruction. Test is now deterministic with a hard upper bound.

49. [x] **test-quality-reviewer** | `test/integ/basic.cpp:1764` | excessive-setup
   The thread_safety test spins for a hardcoded 10 seconds (std::this_thread::sleep_for(10s)) and concludes with LT_CHECK_EQ(1, 1) — a tautological assertion. The test verifies absence of crash/deadlock by timeout, not a measurable outcome, and imposes a 10-second floor on the test suite runtime.
   *Recommendation:* Reduce the sleep to 1-2 seconds for CI (the race window is equally likely to manifest in 1 s). The tautological assertion can stay as a comment noting the test is a liveness check, but document it explicitly to avoid future confusion about what failure mode it catches.
   *Status:* Resolved — sleep reduced to 2s (`test/integ/basic.cpp:1800`); the tautological `LT_CHECK_EQ(1, 1)` is now annotated with a comment (lines 1808-1810) explicitly documenting it as a liveness check.

50. [x] **test-quality-reviewer** | `test/integ/deferred.cpp:191` | naming-convention
   Test name `deferred_response_suite` names the test identically to the suite name prefix. The littletest framework concatenates suite and test names; the result is `deferred_suite::deferred_response_suite` which reads as if it describes a nested suite rather than a specific behavior.
   *Recommendation:* Rename to `deferred_response_with_prefix_content` or `deferred_producer_with_prefix_served_correctly` to describe what the test verifies.
   *Status:* Resolved — test renamed exactly per recommendation to `deferred_response_with_prefix_content` (`test/integ/deferred.cpp:182`).

51. [x] **test-quality-reviewer** | `test/integ/deferred.cpp:292` | missing-test
   AC-1 (on_get_lambda_returns_value) covers GET but there is no corresponding integration test for on_post / on_put lambda paths returning http_response by value. The lambda_resource::invoke_() for those methods follows the same code path as GET, so a unit-level coverage gap exists for the other render_* overrides in lambda_resource (render_post, render_put, render_delete, render_patch).
   *Recommendation:* Add at least one integration test for a non-GET lambda (e.g., on_post returning a value) to guard against any future method-specific dispatch bug. A single test covering POST is sufficient; the remaining verbs share the identical invoke_() template.
   *Status:* Resolved — `on_post_lambda_returns_value` integration test added in `test/integ/deferred.cpp:332-352` (covers POST with `/post_by_value` returning `"post-ok"` body, 200 status). Comment at line 328 explicitly references the recommendation rationale.

52. [x] **test-quality-reviewer** | `test/integ/deferred.cpp:361` | implementation-coupling
   The lengthy comment block (lines 349-360) in the test body explains internal implementation mechanics (outer lambda stored in lambda_resource, make_shared for CopyConstructibility, modded_request anchor, DR-010). While informative, this couples test intent to the current implementation. If the anchor moves to a different structure the comment becomes misleading without necessarily failing the test.
   *Recommendation:* Trim the comment to the observable contract: 'producer captures must live until request_completed'. Move internal-anchor rationale to the architecture doc or a code comment in modded_request.hpp (which already explains the lifetime contract well).
   *Status:* Resolved — same edit as item 24: comment trimmed to the observable contract (`test/integ/deferred.cpp:393-396`); internal anchor rationale lives in `modded_request.hpp` per recommendation.

53. [x] **test-quality-reviewer** | `test/unit/http_resource_test.cpp:185` | unnecessary-test
   default_render_returns_empty iterates over all http_method enum values and checks is_allowed returns true for each. This is a subset of the assertions already made by allow_all_methods (line 146) and is_allowed_known_methods (line 232), which check the same set of methods individually. The empty_resource also adds no new surface over simple_resource for the purpose of method-set testing.
   *Recommendation:* Remove default_render_returns_empty or merge it into is_allowed_known_methods. Alternatively replace it with a test that actually invokes render() and asserts the default-sentinel behavior (status_code_ == -1 → routes to 500), since that is the unique new contract introduced by TASK-036 that nothing else exercises at the unit level.
   *Status:* Resolved — test was replaced (per the recommendation's alternative): now named `default_render_returns_sentinel` (`test/unit/http_resource_test.cpp:183-198`), it invokes `render()` and `render_get()` directly and asserts `get_status() == -1`, exercising the unique TASK-036 contract.
