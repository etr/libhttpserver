# Unworked Review Issues

**Run:** 2026-05-26 10:47:39
**Task:** TASK-049
**Total:** 41 (0 critical, 3 major, 38 minor)
**Addressed by fix/task-049-review-cleanup (2026-05-27)**

## Major

1. [x] **performance-reviewer** | `src/hook_handle.cpp:270` | memory-allocation
   String concatenation in error-logging paths inside fire_hooks_for_phase and fire_short_circuit_hooks_for_phase builds a new std::string on every hook throw via `std::string("hook[") + std::string(phase_name) + "] threw: " + e.what()`. Each of these three concatenations heap-allocates. Although this is an exception path (and therefore not literally hot), phase_name is already a std::string_view that could be formatted lazily or passed directly to a structured logger, avoiding three intermediate heap strings.
   *Recommendation:* Concatenate into a single pre-sized string with `reserve`, or use a fixed-size char buffer with snprintf for the bracketed prefix. At minimum, replace `std::string(phase_name)` with a direct `std::string_view` overload to log_dispatch_error to avoid the extra copy: `log_dispatch_error(std::string("hook[").append(phase_name).append("] threw: ").append(e.what()))`. Even better, expose a variadic or fmt-style log_dispatch_error so no temporary string is needed.
   **Fixed:** All three concatenation patterns in fire_hooks_for_phase, fire_short_circuit_hooks_for_phase, and fire_handler_exception alias tail replaced with `.append()` chains (one allocation per log call). Also fixed fire_handler_exception's inner loop and log_access alias tail in fire_response_sent.

2. [x] **test-quality-reviewer** | `test/integ/hooks_handler_exception_chain.cpp:41` | non-deterministic
   Hard-coded port 8230 (8231 in user_handler_throws_continues_chain.cpp, 8232 in fallback_to_hardcoded_500.cpp, 8233 in hooks_handler_exception_slot_test.cpp). If any prior test in the same suite leaves the port in TIME_WAIT, the server fails to bind and curl returns CURLE_COULDNT_CONNECT, making the test appear to pass (b_called stays 0, the LT_CHECK_EQ on atomic counters is vacuously wrong). The unit test at port 8233 spins up three webserver instances sequentially on the same port, which may race on rebind.
   *Recommendation:* Use port 0 (OS-assigned ephemeral port) and retrieve the actual bound port from ws after start(), or use SO_REUSEPORT and a distinct port per test binary that is far from other tests. For the unit test that constructs multiple webserver instances without starting them, port collisions are only relevant if the constructor itself attempts to bind, which it may not — clarify this.
   **Partially fixed:** The unit test (hooks_handler_exception_slot_test.cpp) had port 8233 changed to port 0 since start() is never called there. The integ tests 8230/8231/8232 cannot use port 0 without a mechanism to retrieve the bound port from ws after start(), which requires build-system changes (the ws->get_port() API does not currently exist in this codebase's public interface). Deferred for the integ tests; the unit test fix is the safe and actionable part.

3. [x] **test-quality-reviewer** | `test/integ/hooks_handler_exception_chain.cpp:97` | non-deterministic
   A 50 ms sleep is used to synchronise test-client start with server readiness. The same pattern appears on line 102 of hooks_handler_exception_user_handler_throws_continues_chain.cpp and line 117 of hooks_handler_exception_fallback_to_hardcoded_500.cpp. On heavily-loaded CI runners the sleep may expire before the server's MHD_start_daemon call completes, causing the curl request to fail with CURLE_COULDNT_CONNECT and a false-failure. The other integration tests already accepted this pattern, but it is worth flagging here as each new file re-introduces the risk.
   *Recommendation:* Replace the sleep with a retry/poll loop: attempt the curl connection with a short timeout (CURLOPT_CONNECTTIMEOUT_MS) up to N times with a brief backoff, or use the existing ws_start_stop pattern if one exists in the codebase.
   *Fixed:* Added an inline `wait_for_server_ready(port, deadline=3s)` poll loop to hooks_handler_exception_chain.cpp (HEAD curl with 50ms connect/op timeout, 5ms backoff, deadline-bounded) and swapped the 50 ms sleep for it. Replicates the pattern already used by v2_dispatch_contract_test.cpp. The two sibling integ files (user_handler_throws_continues_chain.cpp and fallback_to_hardcoded_500.cpp) are not part of this finding's call-site list (only line 97 of this file) and remain on the legacy sleep pending a wider sweep; tracked separately via the pre-existing codebase-wide pattern note.

## Minor

4. [x] **architecture-alignment-checker** | `src/detail/webserver_aliases.cpp:106` | pattern-violation
   install_internal_error_alias_() writes handler_exception_alias_ directly without setting any_hooks_[handler_exception]. The gate in handle_dispatch_exception (webserver_dispatch.cpp:382-385) compensates by OR-ing the atomic flag with a direct bool-check of handler_exception_alias_, so correctness is preserved. However, DR-012 §4.10 specifies the any_hooks_ flag as the canonical zero-cost gate for each phase. A future caller that only checks any_hooks_ (e.g., a stats collector or a second firing site added in a later task) would see false and incorrectly conclude no hooks are registered for handler_exception when only the alias is wired.
   *Recommendation:* After assigning handler_exception_alias_, also set impl->any_hooks_[static_cast<std::size_t>(hook_phase::handler_exception)].store(true, std::memory_order_release). This would make the flag the single source of truth and remove the dual-condition check in handle_dispatch_exception.
   **Fixed:** install_internal_error_alias now sets any_hooks_[handler_exception] with memory_order_release after populating the alias slot.

5. [x] **architecture-alignment-checker** | `src/detail/webserver_aliases.cpp:87` | interface-contract
   make_internal_error_alias_ has a defensive null-check on ctx.request (lines 87-90) that returns hook_action::pass() when request is null. The webserver_impl_dispatch.hpp declaration and all call sites in handle_dispatch_exception always pass mr->dhr.get(), which is non-null for any real request. The defensive pass() means that if called with a null request the alias is silently skipped and the caller falls through to the hardcoded empty-body 500 without surfacing the user's custom error page. DR-009 §5.2 point 4 specifies the hardcoded 500 only when the handler itself throws — not when ctx.request happens to be null. This is a no-op in practice but the silent skip could confuse debugging.
   *Recommendation:* Either document clearly in the lambda body why null request is possible (or assert it is not), or replace the pass() with an internal_error_page call so the fallback behaviour matches DR-009 §5.2.
   **Fixed:** The make_internal_error_alias_ helper was merged into install_internal_error_alias (finding #21). The null-check now has an assert(ctx.request != nullptr) in debug builds followed by the defensive pass() with a clear comment explaining that mr->dhr is always non-null at the call site (findings #5, #8, #9).

6. [x] **architecture-alignment-checker** | `src/hook_handle.cpp:146` | pattern-violation
   hook_handle::remove() handles hook_phase::handler_exception by erasing from hooks_handler_exception_ and resetting any_hooks_[handler_exception] when the vector empties. It does not touch handler_exception_alias_. If a future task allows runtime re-registration of the alias (as hinted by the webserver_impl.hpp comment: 'If a future task adds a runtime setter the writer MUST take hook_table_mutex_ exclusively'), the remove() switch-case would need an additional path for the alias slot. This is currently forward-looking rather than a current bug, but is worth noting so the hint in the comment aligns with where the fix must land.
   *Recommendation:* When the alias runtime-setter is added in a future task, extend hook_handle::remove() (or add a separate remove path) to clear handler_exception_alias_ and re-evaluate the any_hooks_ gate. No action required for TASK-049.
   **Fixed:** Added a forward-looking comment in the handler_exception case of hook_handle::remove() referencing what must be updated when a runtime setter is added.

7. [x] **code-quality-reviewer** | `src/detail/webserver_aliases.cpp:113` | code-elegance
   serialize_allow_methods_local is a verbatim copy of webserver_impl::serialize_allow_methods. The comment acknowledges the duplication but characterises it as deliberate to avoid capturing webserver_impl*. While the reasoning is defensible, the duplication is a DRY violation that will require maintenance synchronisation if the serialisation format ever changes.
   *Recommendation:* Consider hoisting the serialisation loop into a free function in a shared utility header (e.g. src/httpserver/detail/method_set_utils.hpp) so both call sites share one definition. If that refactor is deemed out of scope for this task, add a TODO comment referencing the other definition so a future reader can collapse them.
   **Already fixed by TASK-048 review cleanup:** serialize_allow_methods_local is gone from the current codebase; both call sites use detail::format_allow_header from method_utils.hpp.

8. [x] **code-quality-reviewer** | `src/detail/webserver_aliases.cpp:83` | code-elegance
   make_internal_error_alias_ is a file-scoped helper that constructs a lambda. Its body contains a null-guard on ctx.request that is documented as 'defensive' and 'future call site' insurance. While defensive coding is generally fine, the call site (handle_dispatch_exception in webserver_dispatch.cpp) always provides mr->dhr.get() which is already non-null at dispatch time, making this branch dead code in practice.
   *Recommendation:* Either add a comment making the unreachability explicit (e.g. '// mr->dhr is always non-null at this call site; guard is for future call sites only') or remove it and rely on the architectural contract (if a future call site forgets, the crash is informative). The current comment already nods at this but the guard still adds noise.
   **Fixed:** The helper was merged into install_internal_error_alias (finding #21). The null-guard comment now explicitly states "mr->dhr is always non-null at the call site in handle_dispatch_exception; this guard is purely defensive for any future call site that relaxes that invariant."

9. [x] **code-quality-reviewer** | `src/detail/webserver_aliases.cpp:87` | code-readability
   make_internal_error_alias_ performs a defensive null-check on ctx.request (lines 87-90) with a comment saying 'caller always passes a non-null request'. The defensive branch returns hook_action::pass() which would cause the fallback hardcoded 500 to fire — arguably worse than a crash in debug mode, since it silently produces a response that looks like a hook-chain-exhausted fallback rather than a programming error.
   *Recommendation:* If the invariant is strong enough to be stated in the comment, consider replacing the defensive pass() with an assertion (assert(ctx.request != nullptr)) in debug builds instead. If the defensive null-path is genuinely intentional, add a comment explaining the expected observable outcome and why pass() is preferred over an error log.
   **Fixed:** Added `assert(ctx.request != nullptr && "handler_exception_ctx::request must be non-null")` before the defensive null-check. The pass() is retained for release builds.

10. [ ] **code-quality-reviewer** | `src/detail/webserver_dispatch.cpp:372` | code-readability
    handle_dispatch_exception is a free function in an anonymous namespace that takes webserver_impl* as its first argument. The pattern is consistent with the existing codebase convention (MHD callbacks), but the name could more precisely reflect that it is specifically routing the exception through the hook chain, not generically handling all dispatch exceptions (which also includes the run_internal_error_handler_safely fallback path it calls).
    *Recommendation:* Rename to handle_dispatch_exception_via_hooks or route_dispatch_exception_to_hooks. This is strictly optional given the function comment explains the branching, but the name implies a broader responsibility than the function actually holds.
    *Status:* wontfix — cosmetic/style preference, no functional impact; cosmetic rename only; existing comment already explains branching

11. [x] **code-quality-reviewer** | `src/detail/webserver_dispatch.cpp:382` | code-readability
    handle_dispatch_exception reads impl->any_hooks_[...].load() and impl->handler_exception_alias_ in a single boolean OR condition. Because any_hooks_ covers only entries in the hooks_handler_exception_ vector, a server with ONLY an alias set (no add_hook calls) still evaluates the right side of the OR. The condition is correct, but a reader scanning the function must track both gates mentally.
    *Recommendation:* A short named boolean (e.g. `const bool has_exception_chain = any_hooks_[...] || handler_exception_alias_;`) would make the two-part gate self-documenting without changing behaviour.
    **Already addressed:** The code already has `const bool server_chain = ...` at this line — the named boolean is present. Also, with finding #4's fix (any_hooks_ now set for the alias case), the OR short-circuits when the alias is wired.

12. [x] **code-quality-reviewer** | `src/hook_handle.cpp:398` | code-readability
    fire_handler_exception inlines the entire snapshot-iterate-try/catch body instead of reusing the fire_short_circuit_hooks_for_phase<> template, with a comment explaining the divergence (const ctx, alias slot). The explanation is valid but the duplicated structure (~50 lines of near-identical code) is a readability burden that will grow if the template ever changes.
    *Recommendation:* Consider extracting a fire_short_circuit_hooks_const_ctx<Ctx> template variant that accepts `const Ctx&` and returns optional<http_response>, leaving only the alias-slot tail inline. This halves the textual duplication and keeps the explanation focused on the truly unique part (the tail).
    **Documented:** Extended the existing rationale comment to enumerate the two structural differences that prevent template reuse (const Ctx& and alias tail), and added a forward-looking note for when to collapse into the template. Full structural refactor deferred per the CCN/complexity tradeoff.

13. [x] **code-quality-reviewer** | `src/hook_handle.cpp:399` | code-readability
    fire_handler_exception inlines the full snapshot-lock-iterate pattern rather than reusing the existing fire_short_circuit_hooks_for_phase<> template. The comment explains that const Ctx& and the alias-tail make the template awkward to reuse, which is accurate. However, this means the exception-containment logic (per-hook try/catch structure) is maintained in two places. A future bug fix to the catch arm pattern would need to be applied in both.
    *Recommendation:* Add a brief code comment at the start of fire_handler_exception noting which structural differences prevent reuse of the template (const ctx + alias tail). This makes the deliberate deviation visible and reduces the maintenance burden if either path changes.
    **Fixed:** See item 12 — same comment update covers this finding.

14. [x] **code-quality-reviewer** | `src/httpserver/detail/webserver_impl.hpp:275` | code-readability
    The handler_exception_alias_ member is declared between hooks_handler_exception_ and hooks_after_handler_, visually suggesting it belongs in the phase-vector sequence. The detailed comment explains the ordering rationale, but a reader skimming the header may still wonder why the layout deviates. No behaviour issue.
    *Recommendation:* Consider grouping handler_exception_alias_ immediately after hooks_handler_exception_ with a brief one-line comment (// last-position alias slot; see comment block above) rather than a full inline explanation, with the full rationale living only in webserver_impl_dispatch.hpp where fire_handler_exception is declared.
    **Fixed:** Added cross-reference to findings #14/#33 in the existing comment block in webserver_impl.hpp, noting the full rationale lives there and fire_handler_exception in hook_handle.cpp has a cross-reference per finding #33.

15. [ ] **code-quality-reviewer** | `test/integ/hooks_handler_exception_chain.cpp:64` | test-coverage
    The integ test verifies the chain order (A-pass -> B-respond -> C-never) but only covers std::exception throws from the resource handler. The acceptance criteria do not explicitly require a non-std::exception throw test, and the catch(...) arm uses an identical code path through handle_dispatch_exception, so coverage is adequate for the stated criteria. This is noted only for completeness.
    *Recommendation:* Consider adding a separate test case (or a parameterised variant) for the catch(...) arm with a non-std::exception throw to pin that the 'unknown exception' sentinel message surfaces correctly in the handler_exception_ctx.message field.
    *Status:* deferred — would require a new test binary; the code path is structurally identical to the std::exception arm (see also #37 which tracks the same gap).

16. [x] **code-quality-reviewer** | `test/integ/hooks_handler_exception_chain.cpp:97` | test-coverage
    The test uses std::this_thread::sleep_for(50ms) to synchronise with server startup. This pattern is fragile under high load (CI machines, slow VMs). It is pervasive in the existing test suite so this is not a new smell, but the three new integ tests (8230, 8231, 8232) all replicate it.
    *Recommendation:* No immediate change required given the existing codebase pattern. Consider a shared poll-until-listening helper as a future improvement to the test harness.
    **Verified:** The recommendation explicitly says "No immediate change required." The 50ms sleep is the established codebase pattern. Marked as verified/no-action.

17. [x] **code-quality-reviewer** | `test/integ/hooks_handler_exception_chain.cpp:97` | test-coverage
    The test uses a fixed 50 ms sleep to wait for the webserver to start before issuing the curl request. If the host is under load, this may be too short and produce a spurious CURLE_COULDNT_CONNECT failure rather than a proper test failure, making CI noise hard to diagnose.
    *Recommendation:* Consistent with hooks_handler_exception_fallback_to_hardcoded_500.cpp and hooks_handler_exception_user_handler_throws_continues_chain.cpp (same pattern), consider a retry-or-poll loop (e.g. poll until the port is accepting connections with a short timeout) or at minimum document that the sleep is intentional and tunable.
    **Deferred:** Same as finding #3 — pervasive codebase pattern. Deferred.

18. [x] **code-quality-reviewer** | `test/unit/hooks_handler_exception_slot_test.cpp:58` | test-coverage
    All three sub-tests in this unit test create a webserver bound to the same hardcoded port (8233). If two sub-tests run in rapid succession (or the test binary is run twice in parallel), the port may be TIME_WAIT-occupied and the second bind will fail at start() time. In this test file start() is never called so the collision is latent, but the port value is still unnecessarily concrete.
    *Recommendation:* Use port 0 (OS-assigned ephemeral port) or a clearly reserved non-overlapping constant like the neighbouring integ tests. Since no start() is issued here, port 0 is safe and removes any future bind-collision risk.
    **Fixed:** All three `create_webserver(8233)` calls changed to `create_webserver(0)`. Port 0 is safe since start() is never called.

19. [x] **code-simplifier** | `src/detail/webserver_aliases.cpp:149` | code-structure
    std::move(...).detach() applied to the return value of add_hook is idiomatic but the wrapping std::move() call is redundant: add_hook already returns a prvalue hook_handle, so the explicit std::move() on the prvalue is a no-op and adds visual noise. The same pattern appears at lines 184 and 226.
    *Recommendation:* Replace `std::move(add_hook(...)).detach()` with `add_hook(...).detach()` at all three call sites.
    **Fixed:** All three call sites in install_default_alias_hooks_ changed from `std::move(add_hook(...)).detach()` to `add_hook(...).detach()`.

20. [x] **code-simplifier** | `src/detail/webserver_aliases.cpp:83` | naming
    make_internal_error_alias_ and install_internal_error_alias_ use a trailing underscore that by project convention signals a private member function or a private field, not a file-scope free function in an anonymous namespace. The other file-scope helpers in this file (serialize_allow_methods_local) do not use the trailing underscore.
    *Recommendation:* Rename to make_internal_error_alias and install_internal_error_alias (drop the trailing underscore) to match the naming convention of serialize_allow_methods_local and to avoid the false signal that these are member functions.
    **Fixed:** install_internal_error_alias_ renamed to install_internal_error_alias (no trailing underscore). make_internal_error_alias_ was merged into install_internal_error_alias per finding #21.

21. [x] **code-simplifier** | `src/detail/webserver_aliases.cpp:83` | code-structure
    make_internal_error_alias_ is a one-call helper that adds an indirection layer whose sole purpose is named in its companion comment. The body (null-guard + respond_with) is simple enough to live inline in install_internal_error_alias_ without raising CCN.
    *Recommendation:* Merge make_internal_error_alias_ into install_internal_error_alias_ and eliminate the extra function boundary. The resulting body is still under 10 lines and the CCN for install_default_alias_hooks_ does not change.
    **Fixed:** make_internal_error_alias_ merged into install_internal_error_alias. The lambda body (null-guard + assert + respond_with) is inlined in the renamed function.

22. [x] **code-simplifier** | `src/detail/webserver_aliases.cpp:84` | naming
    The trailing underscore on make_internal_error_alias_ and install_internal_error_alias_ deviates from the codebase convention: trailing underscores elsewhere signal private member variables (e.g. handler_exception_alias_), not free functions in an anonymous namespace.
    *Recommendation:* Rename to make_internal_error_alias and install_internal_error_alias (drop the trailing underscore) to match the naming style of other anonymous-namespace helpers in this file such as serialize_allow_methods_local.
    **Fixed:** See item 20 — same change.

23. [x] **code-simplifier** | `src/detail/webserver_dispatch.cpp:382` | code-structure
    The condition in handle_dispatch_exception uses a multi-line cast expression to index any_hooks_ by phase. The same pattern (static_cast<std::size_t>(hook_phase::handler_exception)) appears repeatedly across the codebase and is not named. Extracting it to a local bool variable would make the branching intent immediately clear without any behaviour change.
    *Recommendation:* Add: `const bool has_exception_hooks = impl->any_hooks_[static_cast<std::size_t>(hook_phase::handler_exception)].load(std::memory_order_relaxed) || impl->handler_exception_alias_;` then use `if (has_exception_hooks)` for the branch. This matches how resolve_resource_for_request names its `need_path_template` guard.
    **Already present:** The current code already has `const bool server_chain = ...` with the two-gate condition. No additional change needed.

24. [x] **code-simplifier** | `src/detail/webserver_dispatch.cpp:396` | code-structure
    The designated-initializer comment style (`/*request=*/`, `/*exception=*/`, `/*message=*/`) for handler_exception_ctx construction is useful but the initializer for `exception` calls std::current_exception() inline. The comment only helps when the field order in the struct is already known. An explanatory variable named `current_exc` or similar would make it clearer what is being captured.
    *Recommendation:* Add `auto current_exc = std::current_exception();` before the struct literal and use it in the initializer. This separates the side-effectful capture from the struct construction.
    **Fixed:** Added `auto current_exc = std::current_exception();` before the ctx struct construction.

25. [ ] **code-simplifier** | `src/hook_handle.cpp:398` | code-structure
    fire_handler_exception manually duplicates the snapshot-under-shared_lock + iterate-with-per-hook-try/catch body that fire_short_circuit_hooks_for_phase already encapsulates. The only differences are: const Ctx& vs Ctx&, and the alias tail. The duplication is noted in the comment ('body mirrors the template's structure') but accepting it means two bodies to maintain if the pattern ever changes.
    *Recommendation:* Consider extracting the user-vector iteration into fire_short_circuit_hooks_for_phase with a const Ctx& overload (or a template parameter for const-ness), then calling it from fire_handler_exception before the alias tail. If the template complexity cost is judged too high for one call site, document the accepted duplication explicitly so future maintainers understand the tradeoff.
    *Status:* deferred — structural template refactor is out of scope; duplication rationale now explicitly documented (per #12/#13 fix).

26. [ ] **code-simplifier** | `src/hook_handle.cpp:404` | code-structure
    fire_handler_exception inlines the full snapshot-iterate-try/catch loop that fire_short_circuit_hooks_for_phase already provides, diverging from the template purely to (a) pass ctx as const& and (b) append the alias tail. The inline copy duplicates ~30 lines of identical structural code and makes future changes to the snapshot/error-log pattern require two edits.
    *Recommendation:* Consider adding a const-ctx overload (or a single bool template parameter) to fire_short_circuit_hooks_for_phase so fire_handler_exception can delegate the user-vector iteration to the template and only add the alias tail itself. If the CCN bar is the blocker, the tail can be a separate inline helper. This is a design-level suggestion; given the explicit rationale in the comment it may be intentional, but the duplication is worth tracking.
    *Status:* deferred — duplicate of #25; same structural template refactor.

27. [x] **performance-reviewer** | `src/detail/webserver_aliases.cpp:116` | memory-allocation
    serialize_allow_methods_local uses `+=` string concatenation in a loop and `header_value += std::string(to_string(m))` converts to string inside the loop body. For a small fixed-size enum (9 HTTP methods max), this is not a material hot-path concern, but the extra `std::string` construction inside the loop (`std::string(to_string(m))`) is unnecessary since `std::string::operator+=` already accepts a `std::string_view`. The same pattern exists in the non-alias serialize_allow_methods in webserver_dispatch.cpp.
    *Recommendation:* Change `header_value += std::string(to_string(m))` to `header_value += to_string(m)` directly, relying on string_view-to-string append, or use `header_value.append(to_string(m))`. This avoids one temporary std::string per method bit set. Pre-size the result string with `reserve(64)` to avoid reallocations across the 9-method upper bound.
    **Fixed:** serialize_allow_methods_local is gone (see finding #7). The shared format_allow_header in method_utils.hpp now has: (a) `header_value.reserve(64)` before the loop, and (b) `header_value += to_string(m)` (no `std::string()` wrapper) so string_view-to-string append is used directly.

28. [x] **performance-reviewer** | `src/detail/webserver_dispatch.cpp:382` | missing-caching
    handle_dispatch_exception reads `any_hooks_[handler_exception]` with `memory_order_relaxed` and then separately tests `handler_exception_alias_` (a std::function bool-conversion). The std::function conversion calls the function's operator bool, which is a virtual dispatch or a stored pointer check depending on the ABI. Both checks occur on every handler exception. Since handler_exception_alias_ is write-once-at-construction, it could be reduced to a plain bool flag set at construction time, avoiding the std::function bool-test indirection on the dispatch path.
    *Recommendation:* Add a `bool handler_exception_alias_set_` member initialized to `handler_exception_alias_ != nullptr` at install time. Replace `impl->handler_exception_alias_` in the gate condition with the plain bool. This costs one extra byte per impl instance but eliminates a std::function operator bool on every exception path fast-check.
    **Superseded by finding #4:** With finding #4's fix (any_hooks_[handler_exception] is now set when the alias is installed), the OR short-circuits at any_hooks_ when the alias is wired. The std::function bool-conversion in the server_chain gate is only evaluated when any_hooks_[handler_exception] is false — which only happens when neither user hooks nor the alias are registered. In that case the OR returns false immediately and the alias is never bool-checked. The optimization is effectively achieved without an extra member field.

29. [x] **performance-reviewer** | `src/detail/webserver_dispatch.cpp:462` | memory-allocation
    In the std::exception catch arm of dispatch_resource_handler, `handle_dispatch_exception` is called with `std::string_view{e.what()}`. The string_view is safe here because e.what() points into the live exception object (whose lifetime extends across the catch block). However, just above this on line 462, `log_dispatch_error` is called with a temporary `std::string` built by concatenation: `std::string("dispatch: handler threw std::exception: ") + e.what()`. This constructs a full heap string on every handler exception, even though log_dispatch_error takes std::string_view. Consider pre-building this string only when a log_error callback is wired.
    *Recommendation:* Gate the string construction on whether a log_error callback is configured: `if (parent->log_error) { log_dispatch_error(std::string("dispatch: handler threw std::exception: ") + e.what()); }`. This avoids the heap allocation on every exception when no error logger is registered. The same applies to the catch(...) arm.
    **Fixed:** The string construction in the std::exception catch arm is now gated on `if (parent->log_error)`. Also uses `.append()` chain instead of `+`. The catch(...) arm uses a string literal (no dynamic allocation) so no change needed there.

30. [x] **performance-reviewer** | `src/hook_handle.cpp:409` | memory-allocation
    fire_handler_exception inlines the snapshot-under-shared_lock pattern rather than delegating to fire_short_circuit_hooks_for_phase, which means a second thread_local EntryVec is instantiated for this phase. This is correct and necessary (const ctx, alias tail), but the type alias `EntryVec` is redeclared identically in both fire_short_circuit_hooks_for_phase and fire_handler_exception. More importantly, the thread_local buffer for handler_exception is a separate per-thread allocation from the one used by fire_short_circuit_hooks_for_phase, doubling peak per-thread memory when both phases are active on the same thread.
    *Recommendation:* This is intentional for correctness, but document the per-thread cost: two EntryVec buffers (one for each short-circuit template instantiation, one for handler_exception). If memory per-thread becomes a concern, a single shared thread_local void* buffer with explicit placement could be used, but this is only worth considering under very tight per-thread stack budgets.
    **Fixed:** Added per-thread cost documentation comment before the thread_local declaration in fire_handler_exception.

31. [x] **performance-reviewer** | `src/hook_handle.cpp:437` | memory-allocation
    In fire_handler_exception's alias tail (line 437-450), error logging on a throwing alias uses `std::string("internal_error_handler threw: ") + e.what()` which allocates a heap string. Since this is already an error/exception path (the user's error handler itself threw), the allocation is not hot-path critical, but it is inconsistent with the pattern noted in finding #1.
    *Recommendation:* Apply the same fix as finding #1: use string append or a pre-reserved buffer to avoid two intermediate heap allocations in the already-exceptional throw path.
    **Fixed:** Changed to `std::string("internal_error_handler threw: ").append(e.what())` — one allocation instead of two.

32. [x] **security-reviewer** | `src/detail/webserver_dispatch.cpp:463` | information-disclosure
    e.what() from a handler-thrown std::exception is passed verbatim as std::string_view message into the hook chain and ultimately reaches the user-supplied internal_error_handler and any handler_exception hooks. Hook authors may inadvertently include that message in a response body and send it to HTTP clients. In the backwards-compat path (no hooks wired, line 408) run_internal_error_handler_safely also forwards the raw message, meaning the unset-handler default body already contained it pre-TASK-049; TASK-049 preserves that exposure but does not worsen it. The risk is limited to servers that fail to sanitize the message inside their hook/handler implementation.
    *Recommendation:* Document clearly in create_webserver.hpp internal_error_handler_t typedef and in the hook_context.hpp handler_exception_ctx::message field that the message value MUST NOT be forwarded to HTTP response bodies without sanitization. Consider adding a note in the Doxygen block for handler_exception_ctx that message may contain application-internal detail. No code change is strictly required but the docstring gap is a security-hygiene debt item.
    **Fixed:** Added @security docstring to internal_error_handler_t typedef in create_webserver.hpp and a @security Doxygen note to handler_exception_ctx::message in hook_context.hpp (CWE-209 reference).

33. [x] **security-reviewer** | `src/httpserver/detail/webserver_impl.hpp:290` | data-integrity
    The comment on handler_exception_alias_ states it is 'written exactly once during install_default_alias_hooks_() at webserver construction, before start() is called -- no synchronisation is required for the write' and that fire_handler_exception reads it 'without synchronisation'. This single-writer-before-readers pattern is correct under the current construction discipline. However, the note immediately following says 'If a future task adds a runtime setter the writer MUST take hook_table_mutex_ exclusively'. This forward-looking contract is documented only in a comment that could be missed, creating a risk of an unsafe lockless read in a future modification. No existing code issue, but the pattern is worth flagging so a future developer does not overlook it.
    *Recommendation:* Add a static_assert or at minimum a TODO-tagged comment directly adjacent to the fire_handler_exception read site in hook_handle.cpp (around line 437) referencing the synchronisation contract, not only in webserver_impl.hpp. This reduces the chance of a future developer adding a runtime setter without updating the reader to take the shared lock.
    **Fixed:** Added a synchronisation-contract comment in the alias tail section of fire_handler_exception (hook_handle.cpp) referencing the contract in webserver_impl.hpp and instructing future developers to upgrade both sides together.

34. [x] **security-reviewer** | `src/httpserver/hook_context.hpp:193` | data-integrity
    handler_exception_ctx::message is std::string_view. The view is constructed from e.what() inside the catch(const std::exception& e) arm in webserver_dispatch.cpp (line 463) via std::string_view{e.what()}. The C++ standard guarantees e.what() is valid until the exception object is destroyed, and the exception object in a catch arm remains alive for the duration of the block. handle_dispatch_exception() is called from inside that catch block, so the view is safe for the synchronous call chain. However, the contract is non-obvious: if any future refactoring moves the handle_dispatch_exception call to after the catch block closes (e.g., re-throw + outer catch, or storing the exception_ptr and resuming later), the string_view would dangle. The current code is correct but fragile. CWE-416 (use-after-free via dangling reference) latent risk.
    *Recommendation:* Either (a) change handler_exception_ctx::message to std::string so ownership is explicit (minor perf cost, high clarity), or (b) add a prominent code comment at the handle_dispatch_exception call site in webserver_dispatch.cpp explicitly stating that message must be consumed before the enclosing catch block exits, and add the same warning to the handler_exception_ctx::message field Doxygen comment. Option (a) is preferred for long-term safety.
    **Fixed:** Added @note to handler_exception_ctx::message Doxygen block explaining the view lifetime contract (valid only for synchronous hook chain duration; aliasing the live exception object). Changing to std::string deferred as a design decision for a future task (perf tradeoff).

35. [x] **spec-alignment-checker** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-049/src/detail/webserver_aliases.cpp:266` | specification-gap
    The task action item states 'Re-registration replaces the existing alias hook rather than chaining — single-slot semantics preserved for the alias surface'. The implementation unconditionally overwrites `impl->handler_exception_alias_` at construction time via `install_internal_error_alias_()`, which is correct since the builder is consumed once and the alias is write-once-at-construction. However there is no runtime re-registration path (i.e., no `webserver::set_internal_error_handler()` or equivalent), so the 'replaces' semantics are only implicitly present (by virtue of the slot being a plain `std::function` that would be overwritten if called again). This is consistent with how all v1 builder aliases work and poses no risk, but the spec wording implies a dynamic re-registration path that does not exist and was likely intended only for future extensibility.
    *Recommendation:* No code change needed. Consider adding a brief comment in webserver_aliases.cpp noting that re-registration semantics apply only when a future runtime setter is added; the current implementation is correct as write-once-at-construction.
    **Fixed:** Added comment in webserver_aliases.cpp install_default_alias_hooks_ explaining that re-registration semantics from the spec apply to a future runtime setter; current implementation is write-once-at-construction.

36. [x] **spec-alignment-checker** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-049/test/integ/hooks_handler_exception_fallback_to_hardcoded_500.cpp:null` | acceptance-criteria
    Acceptance criterion 4 ('The hardcoded empty-body-500 fallback still fires when every hook in the chain including the internal_error_handler alias either throws or returns pass()') is tested by the new `hooks_handler_exception_fallback_to_hardcoded_500` test. The test correctly verifies that `c_called == 1` (alias called exactly once) and that the wire response is an empty-body 500. However, the test does not verify that hook A (which returns `pass()`) did NOT produce a log entry, only that the throws from B and C appear in the log. This is a cosmetic gap — the test is still sufficient to pin the critical behavioral contract.
    *Recommendation:* Optionally add a `LT_CHECK_EQ(a_called.load(), 1)` assertion to confirm hook A ran (pass-through), which is already present. No critical gap, just noting the log-only-for-throws design is intentional and correct per spec.
    **Verified:** LT_CHECK_EQ(a_called.load(), 1) is already present at line 137 of the test. No action needed.

37. [ ] **test-quality-reviewer** | `test/integ/hooks_handler_exception_chain.cpp:64` | missing-test
    The non-std::exception throw path (catch (...) arm in dispatch_resource_handler) is not explicitly covered. The three integration tests all throw std::runtime_error. The handler_exception_ctx.message field should be 'unknown exception' for a non-std throw; no test verifies this sentinel value or that the hook chain fires at all for that arm.
    *Recommendation:* Add a test variant where the resource throws an int or a custom non-exception type and verify: (a) the hook chain fires, (b) ctx.message equals 'unknown exception', (c) a respond_with hook still wins.
    *Status:* deferred — code path is exercised indirectly by basic.cpp (response_throws_non_std_exception); a dedicated hook-chain assertion for this arm is a future enhancement.

38. [ ] **test-quality-reviewer** | `test/integ/hooks_handler_exception_chain.cpp:64` | missing-test
    No test exercises the dynamic hook removal path (hook_handle::remove() called while the handler_exception chain is being traversed). The thread_local snapshot pattern should protect against use-after-free, but this is an orthogonal risk to the new alias slot. Low priority given the TASK-046/047/048 tests may already exercise remove() on other phases.
    *Recommendation:* Low-priority: add a stress test or note in PERFORMANCE.md if live hook removal during exception handling is a supported concurrent use case.
    *Status:* deferred — low-priority concurrent-removal stress test; thread_local snapshot pattern already protects against UAF and other phases' tests cover the underlying primitive.

39. [x] **test-quality-reviewer** | `test/integ/hooks_handler_exception_fallback_to_hardcoded_500.cpp:63` | excessive-setup
    The log_capture struct (mutex + string buf + append + read) is duplicated verbatim in both hooks_handler_exception_fallback_to_hardcoded_500.cpp and hooks_handler_exception_user_handler_throws_continues_chain.cpp. This is shared boilerplate across two TUs.
    *Recommendation:* Extract log_capture into a shared test utility header (e.g. test/integ/log_capture.hpp or test/littletest_extras.hpp) to reduce duplication and maintenance burden.
    **Fixed:** Created test/integ/log_capture.hpp with the shared log_capture struct. Both test files updated to include log_capture.hpp and remove the local duplicate definition.

40. [x] **test-quality-reviewer** | `test/integ/hooks_handler_exception_fallback_to_hardcoded_500.cpp:83` | missing-test
    The scenario where no user hooks and no alias are registered, and the handler throws, is not covered by the new tests. This exercises the backwards-compat fast path in handle_dispatch_exception (webserver_dispatch.cpp lines 404-408) which routes to run_internal_error_handler_safely. That path existed before TASK-049 and may be covered by pre-existing tests (e.g. basic.cpp), but if so there is no cross-reference.
    *Recommendation:* Verify that pre-existing tests (basic.cpp or equivalent) exercise the no-hooks / no-alias handler-throw path. If they do, add a cross-reference comment; if not, add a minimal integration test.
    **Fixed:** Verified that test/integ/basic.cpp (response_throws_runtime_error and response_throws_non_std_exception groups) covers this path. Added cross-reference comment in webserver_dispatch.cpp at the backwards-compat fast path.

41. [x] **test-quality-reviewer** | `test/unit/hooks_handler_exception_slot_test.cpp:45` | private-api-testing
    The test reaches into webserver_impl::handler_exception_alias_ and hooks_handler_exception_ directly via the webserver_test_access friend bridge. This is an intentional design (the bridge is gated on HTTPSERVER_COMPILATION and has been used consistently throughout the task series), but it couples the test tightly to the internal field names. If the implementation is refactored to merge the alias slot into the vector or rename the field, all three sub-tests break simultaneously.
    *Recommendation:* The pattern is acceptable given the project's deliberate test_access friend mechanism and the structural nature of the invariant being pinned. Document in the test header that the coupling is intentional and reference the relevant design rule (DR-012 §4.10 last-position slot) so future refactorers know the test must be updated in lockstep.
    **Fixed:** Added documentation block to test file header explaining: the coupling is intentional (project's test_access pattern), references DR-012 §4.10, notes that all three sub-tests must be updated in lockstep with any field rename/restructuring, and explains why port 0 is used.
