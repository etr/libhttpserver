# Unworked Review Issues

**Run:** 2026-06-29 19:59:34
**Task:** TASK-086
**Total:** 12 (0 critical, 0 major, 12 minor)

## Minor

1. [ ] **architecture-alignment-checker** | `src/httpserver/http_utils_inet.hpp:1` | pattern-violation
   The task specification groups http_utils_inet.hpp with the four class-body fragment headers and states it must follow the 'included inside the class body' pattern. However, http_utils_inet.hpp declares namespace-scope free functions (get_ip_str, get_port, dump_header_map, dump_arg_map, http_unescape, load_file, base_unescaper) in httpserver::http and is included at file scope in http_utils.hpp (line 439, after the namespace closing braces), not inside any class body. This is the correct architectural decision for namespace-level content, but differs from the stated requirement. Unlike the other four fragments, it also lacks an inner namespace-scope gate analogous to the SRC_HTTPSERVER_*_INSIDE_CLASS_ guards, meaning a file that defines HTTPSERVER_COMPILATION could include it standalone without the expected namespace context.
   *Recommendation:* No code change required — the namespace-scope inclusion is architecturally correct for free-function declarations. Update the task specification to classify http_utils_inet.hpp as a 'namespace-scope fragment header' rather than a 'class-body fragment header'. Consider adding a lightweight namespace-scope gate (e.g. #ifndef SRC_HTTPSERVER_HTTP_UTILS_INET_HPP_INSIDE_NS_ / #error) analogous to the class-body guards, for consistency.

2. [ ] **code-quality-reviewer** | `src/detail/http_request_impl_args.cpp:49` | code-readability
   The file-level block comment ('http_request_impl_args.cpp -- GET-argument and querystring population. Carved out of src/detail/http_request_impl.cpp in TASK-086...') appears at line 49, inside the opened namespace httpserver { namespace detail { block, rather than at file scope. This makes it semantically a namespace-body comment rather than a file-level description. Compare hook_phase_dispatch.cpp where the equivalent carve-out comment (lines 22-29) is correctly placed at file scope before the namespace.
   *Recommendation:* Move the file-level block comment to before the first 'namespace httpserver {' to be consistent with hook_phase_dispatch.cpp and the project convention that file-level Doxygen blocks are at global scope.

3. [ ] **code-quality-reviewer** | `src/detail/webserver_response_queue.cpp:33` | code-readability
   http_utils.hpp is included unconditionally but is only needed inside the #ifdef HAVE_DAUTH block (for http::http_utils::digest_algorithm). In non-HAVE_DAUTH builds the include is dead. Strict IWYU would nest it inside the conditional.
   *Recommendation:* Move `#include "httpserver/http_utils.hpp"` inside the `#ifdef HAVE_DAUTH` block to be consistent with the IWYU pruning already applied to the rest of the file. This is optional — the current form is correct and not harmful.

4. [ ] **code-quality-reviewer** | `src/http_response_factories.cpp:61` | code-elegance
   kForbiddenFieldChars is defined in an anonymous namespace in both src/http_response.cpp (line 187) and src/http_response_factories.cpp (line 61). The factories file acknowledges this with the comment 'Duplicated from http_response.cpp (anonymous-namespace, internal linkage)'. Two identical anonymous-namespace constants that must stay in sync is a DRY violation and creates a silent-drift risk: if the forbidden character set changes in one file (e.g. adding '\t'), the other must be updated manually.
   *Recommendation:* Move kForbiddenFieldChars to a private internal header (e.g. src/detail/http_response_internals.hpp, not installed) included by both http_response.cpp and http_response_factories.cpp. This eliminates the duplication while keeping the constant out of the public ABI.

5. [ ] **code-quality-reviewer** | `src/httpserver/http_utils_inet.hpp:35` | code-elegance
   http_utils_inet.hpp has its own include guard and is installed as a public header (nobase_include_HEADERS in Makefile.am), yet the file comment states 'not intended to be included on its own' and references types (header_view_map, header_map, arg_view_map, unescaper_ptr) that are only defined in http_utils.hpp. Including this header directly would fail to compile because those types are missing. The dual-nature (installable but not standalone) creates a contradiction: installed headers in a C++ library are conventionally expected to be either self-contained or explicitly a detail-only header guarded against direct use.
   *Recommendation:* Either add the necessary forward-declarations/includes to make http_utils_inet.hpp self-contained, or add a compile-time guard (similar to the _HTTPSERVER_HPP_INSIDE_ pattern used by the other public headers) that produces a meaningful error when included directly. The existing _HTTPSERVER_HPP_INSIDE_ / HTTPSERVER_COMPILATION gate at the top already provides this protection.

6. [ ] **code-simplifier** | `src/detail/http_request_impl_args.cpp:21` | naming
   The file-level banner comment ('detail::http_request_impl method bodies (non-TLS section). The TLS / client-cert section lives in...') was copied from the parent http_request_impl.cpp and is no longer accurate: this TU holds only the argument/querystring enumeration callbacks, not the full 'non-TLS method bodies'. A correct module-level description does appear later, inside namespace detail at line 49, but a reader scanning the top of the file gets a misleading overview.
   *Recommendation:* Replace the four-line banner at line 21-24 with a description specific to this file, e.g.: '// detail::http_request_impl argument-population bodies. See http_request_impl.cpp for the // rest of the non-TLS surface. Carved out in TASK-086 to keep both TUs under FILE_LOC_MAX.' The accurate module comment at line 49 can then be shortened or merged.

7. [ ] **code-simplifier** | `src/detail/webserver_response_queue.cpp:25` | dependencies
   <optional> is transitively available through modded_request.hpp, which must include it to define std::optional<http_response> as a data member. The standalone include is therefore redundant.
   *Recommendation:* Either remove <optional> (relying on modded_request.hpp's transitive pull) or keep it as explicit IWYU documentation of the dependency. Both choices are defensible; if the project follows IWYU discipline, retaining it is correct. No action required to unblock.

8. [ ] **code-simplifier** | `src/detail/webserver_routes_upsert.cpp:57` | code-structure
   The floating comment block at lines 57-73 begins with '2. Looks up any existing entry...' — it is steps 2-5 of a numbered list whose step 1 remained in webserver_routes.cpp. The block sits at namespace scope between the file-level comment and 'namespace {', with no cross-reference to the sibling file, making it unreadable without consulting webserver_routes.cpp.
   *Recommendation:* Either move the full numbered list into a shared design comment at the top of webserver_routes_upsert.cpp (repeating or summarising step 1), or replace the orphan steps with a self-contained comment such as '// prepare_or_create_lambda_shim implements steps 2-3 of the upsert sequence described in webserver_routes.cpp::on_methods_.'.

9. [ ] **code-simplifier** | `src/http_response_factories.cpp:58` | code-structure
   kForbiddenFieldChars is declared in an anonymous namespace on line 61, with a comment explicitly noting it is 'Duplicated from http_response.cpp (anonymous-namespace, internal linkage)'. The duplication is acknowledged but the comment stops short of explaining why it could not be unified. Two copies of a security-critical constant (the header-injection character set) living in separate TUs with no enforcement that they stay in sync is a quiet maintenance risk.
   *Recommendation:* Move kForbiddenFieldChars into a shared internal header, e.g. httpserver/detail/http_response_internal.hpp, with internal linkage via inline constexpr or a constexpr array, and include it from both http_response.cpp and http_response_factories.cpp. This eliminates the duplication and makes a future change to the forbidden set update a single location.

10. [ ] **housekeeper** | `specs/tasks/M7-v2-cleanup/_index.md:53` | task-not-marked-complete
   The task table in _index.md still lists TASK-086 as 'Backlog'. This will remain stale if only the TASK-086.md Status field is updated by the orchestrator.
   *Recommendation:* Update the Status column for TASK-086 in specs/tasks/M7-v2-cleanup/_index.md from 'Backlog' to 'Done' when the task is merged.

11. [ ] **spec-alignment-checker** | `:null` | acceptance-criteria
   Acceptance criteria 'Typecheck passes; tests pass' cannot be independently confirmed without a full autoconf/automake build. Static analysis shows all new TUs are registered in src/Makefile.am, all new headers are included in their parent files or listed in nobase_include_HEADERS, and configure.ac is untouched. The build graph is structurally sound.
   *Recommendation:* Run 'make check' and 'make check-soversion' in the worktree to confirm dynamically. The static evidence strongly suggests both will pass.

12. [ ] **spec-alignment-checker** | `src/detail/hook_phase_dispatch.cpp:null` | specification-gap
   The task action item suggested naming the hook_handle.cpp split-out file 'detail/hook_handle_dispatch.cpp', but the executor chose 'detail/hook_phase_dispatch.cpp'. The chosen name is semantically accurate (per-phase dispatch) and the resulting LOC reduction is correct, but it diverges from the stated naming suggestion.
   *Recommendation:* No action needed — the suggestion in the task was prefixed with 'Likely split:' indicating it was a hint, not a requirement. The final name is semantically clearer.
