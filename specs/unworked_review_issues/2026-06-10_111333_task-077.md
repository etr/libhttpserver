# Unworked Review Issues

**Run:** 2026-06-10 11:13:33
**Task:** TASK-077
**Total:** 27 (0 critical, 0 major, 27 minor)

## Minor

1. [x] **architecture-alignment-checker** | `test/PORTABILITY.md:39` | pattern-violation
   PORTABILITY.md §threaded.cpp states the MinGW64 libmicrohttpd package is configured with --enable-poll=no, but the workflow actually builds libmicrohttpd from source on Windows (line 702-711 of verify-build.yml) rather than installing a pre-built MinGW64 package via pacman. The root cause is accurate but the phrasing 'the mingw-w64-x86_64-libmicrohttpd package' implies a pacman-installed package, which could mislead a future contributor investigating the build.
   *Recommendation:* Update the PORTABILITY.md §threaded.cpp root-cause sentence to say 'libmicrohttpd is built from source on the Windows CI lanes with ./configure --enable-poll=no (see the Build and install libmicrohttpd (Windows) step in .github/workflows/verify-build.yml)' rather than referencing a package name.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Reworded the threaded.cpp root-cause paragraph in test/PORTABILITY.md to describe the from-source libmicrohttpd build step (./configure --enable-poll=no, the "Build and install libmicrohttpd (Windows)" step) instead of the nonexistent mingw-w64-x86_64-libmicrohttpd package.

2. [x] **code-quality-reviewer** | `.github/workflows/verify-build.yml:1041` | code-readability
   The 'Verify Windows smoke test ran' step uses `grep -E "windows_smoke"` which would match any string containing 'windows_smoke' in the log — including the test registration line (not just a PASS/RUN marker). This means the assertion passes even if the test was registered but skipped or did not run its body.
   *Recommendation:* Tighten the grep to match the PASS or RUN marker format that littletest emits, e.g. `grep -E '(PASS|RUN).*windows_smoke'`, so accidental false positives from test-name registration lines are excluded.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* No action — Traced littletest.hpp: test registration (LT_BEGIN_TEST constructor) is silent — the string 'windows_smoke' is only ever written to the log by test_runner::run() immediately before the test body executes ('Running test (N): windows_smoke'), and littletest emits no separate PASS/RUN registration marker at all. The false-positive scenario the finding describes (a registration-only log line) does not exist, so the current bare grep already only matches on actual execution; applying the literal suggested pattern '(PASS|RUN)' would in fact break the check since littletest never prints those tokens.

3. [x] **code-quality-reviewer** | `scripts/check-skip-rationales.sh:49` | code-readability
   The early-exit 'no files found' branch exits 0 with a PASS message, but in a healthy repo this path should never be reached (test/integ/ always exists). Silently succeeding when the watched directory is empty could mask a misconfigured run (e.g. wrong REPO_ROOT). The analogous scripts in this codebase (check-warning-suppressions.sh) exit 1 on an empty watched set.
   *Recommendation:* Consider changing the empty-file-set branch to print a warning and exit 1 so a misconfigured invocation (wrong working directory, missing test/integ/) is surfaced rather than silently passing.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* No action — Verified the finding's own comparison is inaccurate: scripts/check-warning-suppressions.sh has no explicit empty-watched-set branch at all — an empty WATCHED_FILES array simply skips the for-loop body and the script falls through to its unconditional PASS echo and exits 0, the same silent-pass behavior check-skip-rationales.sh's explicit early-exit already produces. Changing check-skip-rationales.sh to exit 1 on empty would make it inconsistent with, not aligned with, its sibling script.

4. [x] **code-quality-reviewer** | `scripts/check-skip-rationales.sh:63` | code-elegance
   The SKIP_REGEX ends at the closing `\)` and does not anchor the `#if !defined(...)` branch to end-of-pattern (no `([[:space:]]|$)` guard). While in practice the grep is run on whole lines, the asymmetry with the `#ifndef` branch (which does have `([[:space:]]|$)`) is a minor inconsistency that could allow a hypothetical false-positive match on `#if !defined(_WINDOWS_NT)` etc.
   *Recommendation:* Append `([[:space:]]|[[:space:]]*&&|$)` to the `#if !defined(...)` branch of SKIP_REGEX for consistency with the `#ifndef` branch, and to match only the exact macro names.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* No action — Empirically verified (grep -E test) that '#if !defined(_WINDOWS_NT)' does NOT match the current !defined(...) branch of SKIP_REGEX, because the branch already requires a literal closing paren immediately (mod whitespace) after the identifier — the closing '\\)' already serves the same anchoring role the '#ifndef' branch's explicit '([[:space:]]|$)' guard provides. The asymmetry is cosmetic; the false-positive risk described does not exist in the current regex.

5. [x] **code-quality-reviewer** | `scripts/check-skip-rationales.sh:87` | code-elegance
   The bash parameter expansion `"${directive#"${directive%%[![:space:]]*}"}"` for trimming leading whitespace is correct but non-obvious — it requires two nested expansions that most readers will need to decode mentally. The project already uses `grep -E` and `awk` elsewhere; a simpler `trimmed_directive=$(printf '%s' "$directive" | sed 's/^[[:space:]]*//')` or an awk-side trim would be immediately readable.
   *Recommendation:* Cosmetic only. Consider replacing with `trimmed_directive=$(printf '%s' "${directive}" | sed 's/^[[:space:]]*//')` or an inline awk expression to reduce cognitive load for future maintainers, consistent with the readability standard used elsewhere in the script.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Simplified the whitespace trim in scripts/check-skip-rationales.sh from the nested parameter-expansion idiom to 'printf '%s' "${directive}" | sed "s/^[[:space:]]*//"'.

6. [x] **code-quality-reviewer** | `scripts/test_check_skip_rationales.sh:1` | test-coverage
   There is no test case covering the `#if !defined(DARWIN)` variant (only `#ifndef DARWIN` and `#if !defined(_WINDOWS)` are covered). The regex SKIP_REGEX supports both but no test fixture exercises the `!defined(DARWIN)` path.
   *Recommendation:* Add a test case analogous to test 8 but using `#if !defined(DARWIN)` to ensure the regex covers this form as well.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Added test 9b to scripts/test_check_skip_rationales.sh: a bare `#if !defined(DARWIN)` fixture (no reason comment) asserting exit 1, mirroring test 8 for DARWIN. Verified all tests pass.

7. [x] **code-quality-reviewer** | `scripts/test_check_skip_rationales.sh:173` | code-readability
   Test 10 (live tree regression guard) is described as 'Skipped if the live tree does not exist or is in the middle of the implementation', but the code does not emit a SKIP notice — it simply runs nothing. The PASS/FAIL counters remain unchanged, so a missing live tree is invisible in the summary line.
   *Recommendation:* Add an `else` branch with a clear `echo "  SKIP: test 10 — live test/integ/ not found"` so developers can see the guard did not run and understand why.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Added an `else` branch to the live-tree regression guard (test 10) in scripts/test_check_skip_rationales.sh that echoes a SKIP message when test/integ/ is not found, so the guard's non-execution is visible in run output.

8. [x] **code-quality-reviewer** | `scripts/test_check_skip_rationales.sh:41` | test-coverage
   The self-test suppresses both stdout and stderr with `2>/dev/null` in every case. When a test unexpectedly fails (wrong exit code), the suppressed stderr output from check-skip-rationales.sh is unavailable, making it harder to diagnose a regression in CI.
   *Recommendation:* Capture stderr to a variable or temp file and print it only on failure, or redirect only stdout and leave stderr visible. This matches how test harnesses in this codebase typically handle diagnostic output.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* No action — Checked sibling test_check_*.sh scripts in scripts/: they follow the same or a stricter convention (several use `>/dev/null 2>&1`, suppressing stdout too, e.g. test_check_msan_lane.sh, test_check_workflow_pinning.sh). The claim that capturing-stderr-on-failure 'matches how test harnesses in this codebase typically handle diagnostic output' does not hold — no script in this repo does that. The real CI gate step (verify-build.yml:899) invokes check-skip-rationales.sh without any redirection, so actual CI failures already show full stderr; only this local self-test suppresses it, consistent with repo convention.

9. [x] **code-quality-reviewer** | `test/integ/threaded.cpp:61` | code-readability
   The suite-level `#ifndef _WINDOWS` guard wraps only the `ws` member variable declaration (line 65–67), but the `set_up()`, `tear_down()`, and `base` test body all have separate `#ifndef _WINDOWS` guards with repeated reason comments. The repeated comments are individually correct but add verbosity; a single outer `#ifndef _WINDOWS` wrapping the whole suite body would be equivalent and less repetitive.
   *Recommendation:* Minor style note — the current approach is defensively correct (each guard is independently readable), so no change is required. The alternative (single outer guard) might be cleaner but is a separate refactor decision.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* No action — The finding's own recommendation explicitly states no change is required ('the current approach is defensively correct... so no change is required'). Left as-is per the finding's own conclusion.

10. [x] **code-quality-reviewer** | `test/integ/threaded.cpp:61` | code-readability
   The `// reason:` comment on line 61 (wrapping the `ws` member declaration inside the suite body) is structurally awkward: a suite-level field declaration guarded by `#ifndef _WINDOWS` with a comment inside the `LT_BEGIN_SUITE` / `LT_END_SUITE` block reads differently from the guard pattern used everywhere else, where the comment sits immediately above the directive. The intent is clear but the placement is unusual.
   *Recommendation:* Move the `// reason:` comment to the line immediately before the `#ifndef _WINDOWS` at line 65 so it follows the same pattern used in all other skip sites in this file, improving scan-pattern consistency.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* No action — Checked test/integ/threaded.cpp both at the TASK-077 commit (317e71d) that the review ran against and in the current tree: the // reason: comment block already sits on the lines immediately preceding the #ifndef _WINDOWS member-declaration guard (no blank line or other statement between them), matching the placement pattern used at the other three skip sites in the file. The described awkward placement was not present at review time or now.

11. [x] **code-quality-reviewer** | `test/integ/ws_start_stop.cpp:1437` | test-coverage
   The `windows_smoke` test uses `PORT` (the shared base port) rather than an offset port. All other tests in the wide-skip block that were suppressed on Windows also used `PORT`. If `windows_smoke` races with another test that claims the same port in the same test binary run, the curl request fails non-deterministically. This is the same port-collision risk already present in the non-Windows `start_stop` test, so it is not a regression introduced by TASK-077, but the new Windows-only test is the only test that runs on those lanes and therefore its isolation matters more.
   *Recommendation:* Low priority given only one test runs on the Windows lanes, so collisions are unlikely. Document or add a comment noting the shared-port assumption, or use a dedicated port offset consistent with the pattern used by `pedantic_mode` (PORT+26) and `digest_auth_random` (PORT+27).
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Already resolved in the current tree — Verified test/integ/ws_start_stop.cpp's windows_smoke test now binds `create_webserver(0)` and reads the real port via `ws.get_bound_port()` (line ~1311-1315) instead of the old shared PORT macro — this was changed by commit 6f20d31 ('test: bind integration tests to ephemeral ports (fix -j port-collision flakiness)'), which moved every port-binding integration test, including windows_smoke, off hardcoded/shared ports. The described collision risk no longer exists.

12. [x] **code-simplifier** | `.github/workflows/verify-build.yml:27` | naming
   The env variable `CXX` is assigned from `matrix.cc-compiler` (note the double-c), while the field is named `cc-compiler` (C-C, not C-X-X). The asymmetry between the env var name (CXX) and the matrix field name (cc-compiler) is a minor inconsistency that could confuse someone adding a new compiler entry.
   *Recommendation:* Rename the matrix field from `cc-compiler` to `cxx-compiler` throughout the matrix so the mapping `CXX: ${{ matrix.cxx-compiler }}` is self-explanatory. This is a low-priority cosmetic rename since the existing mapping is functionally correct.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Skipped — Applied the mechanical cc-compiler -> cxx-compiler rename across verify-build.yml, then discovered it breaks scripts/check-msan-lane.sh (which asserts the literal 'cc-compiler' matrix key is present) and scripts/test_check_msan_lane.sh's fixture -- both out of scope for this batch's target list. Reverted the rename; left cc-compiler unchanged pending a coordinated rename across all three files.

13. [x] **code-simplifier** | `scripts/check-skip-rationales.sh:63` | code-structure
   SKIP_REGEX is defined once at module level but is only used inside the for-loop body. Placing a definition far from its single use slightly slows orientation for a reader scanning the file.
   *Recommendation:* No change strictly necessary — the script is short enough that SKIP_REGEX at the top is easily found. If the script grows, consider inlining the regex into the grep call or adding a brief comment at the definition site pointing forward to its use.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* No action — The finding's own recommendation states 'No change strictly necessary — the script is short enough that SKIP_REGEX at the top is easily found.' Left as-is per the finding's own conclusion.

14. [x] **code-simplifier** | `scripts/test_check_skip_rationales.sh:22` | naming
   The variable `TMPDIR_BASE` shadows the well-known POSIX environment variable `TMPDIR`. While this is a local variable and the script works correctly, it conflicts with the conventional meaning of `TMPDIR` in the shell environment and could confuse readers familiar with the POSIX convention.
   *Recommendation:* Rename the variable to something that does not shadow a POSIX environment variable, e.g. `WORK_DIR` or `TEST_TMPDIR`, to avoid any ambiguity: `WORK_DIR="$(mktemp -d)"` with a corresponding `trap 'rm -rf "$WORK_DIR"' EXIT`.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* No action — Checked: `TMPDIR_BASE` and `TMPDIR` are different identifiers in bash (shadowing requires the exact same name), so the claimed conflict with the POSIX `TMPDIR` environment variable does not actually occur — `TMPDIR_BASE` is a distinct local variable name with no aliasing effect. The finding's premise does not hold.

15. [x] **code-simplifier** | `test/integ/threaded.cpp:70` | code-structure
   The `set_up()` block carries a `// reason:` comment that is nearly identical to the one on the `#ifndef _WINDOWS` guard at line 62 (the suite-body member declaration). The detection window for the lint script only requires the reason comment within 5 lines of the directive; duplicating essentially the same rationale sentence on every `#ifndef _WINDOWS` inside the same function adds noise without adding information.
   *Recommendation:* Keep the `// reason:` comment immediately above the `#ifndef _WINDOWS` at line 72 (the set_up directive) as required by the lint, but shorten the repetition: a single `// reason: see test/PORTABILITY.md §threaded.cpp` on each directive is sufficient. The longer explanatory prose is already captured in the block comment above line 62 and in PORTABILITY.md itself.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* No action — Re-read all four // reason: comments in test/integ/threaded.cpp: they are not near-duplicates as claimed — the member-decl comment gives the root cause, while the set_up/tear_down/base comments each append a distinct site-specific clause (why that particular function is affected). Shortening them to one generic sentence would remove that local context for a marginal reduction in line count, so the existing comments were left as-is.

16. [x] **performance-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-077/scripts/check-skip-rationales.sh:77` | resource-leak
   One awk subprocess is spawned per matched directive inside the per-file loop. For the current test/integ/ corpus (fewer than ~20 files, each with at most a handful of directives) this is completely acceptable, but the pattern does not scale gracefully if the watched tree grows large or if the script is reused against a much larger directory.
   *Recommendation:* For the current usage envelope this is fine and no change is required. If the watched tree ever grows significantly, the awk call could be hoisted into a single-pass awk script that processes the entire file at once, eliminating per-directive subprocess overhead.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* No action — The finding's own recommendation states 'For the current usage envelope this is fine and no change is required.' Left as-is per the finding's own conclusion.

17. [x] **security-reviewer** | `.github/workflows/verify-build.yml:619` | insecure-design
   The libmicrohttpd tarball is fetched over plain HTTP from an S3 URL (https://s3.amazonaws.com/...) with a sha256sum integrity check immediately after. The sha256sum check is sound and mitigates MITM-substitution risk, but the S3 bucket owner and bucket ACL are not auditable from outside the repository. If the S3 bucket were taken over or mis-configured to allow public writes, an attacker could swap the tarball before the sha256 pin is updated. This is a minor supply-chain concern, not an active exploitable issue. CWE-494 (Download of Code Without Integrity Check) is mitigated by the pin, but the trust anchor is a single S3 bucket.
   *Recommendation:* Cross-document the pinned sha256 digest against the official GNU ftp mirror or a transparency log entry so that rotation of the S3 mirror can be audited. No immediate code change is required; this is an operational hardening note.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* No action — The finding's own recommendation states 'No immediate code change is required; this is an operational hardening note' about cross-documenting the sha256 pin against an external mirror — not a repository edit. Left as-is per the finding's own conclusion.

18. [x] **security-reviewer** | `scripts/check-skip-rationales.sh:73` | injection
   IFS=: splitting on the grep -n output (lineno:directive) is not injection-exploitable in this context (the input is file content piped from grep on a trusted repo path), but if a source file ever contained a colon in a preprocessor directive (e.g. '#ifndef _WINDOWS // URL: http://...') the IFS=: split would assign the URL fragment to 'directive'. The variable is only used for display (line 88), not for further shell evaluation, so there is no code execution risk. However the trimmed_directive value is printed inside single-quoted '$trimmed_directive' on stderr, which is safe. CWE-116 (improper encoding) is tangential but worth noting for robustness.
   *Recommendation:* Use a delimiter that cannot appear in a preprocessor directive, for example NUL or a control character, or switch to awk -F: NR==1 to split only on the first colon so a colon inside the directive text does not corrupt the output. Example: while IFS= read -r raw_line; do lineno=${raw_line%%:*}; directive=${raw_line#*:}; ...
   *Worked 2026-07-12 (chore/unworked-minor-issues):* No action — Empirically verified bash `read` semantics: `IFS=: read -r lineno directive` with exactly two named variables assigns only the first colon-delimited field to `lineno` and folds every remaining field (colons included) into `directive` unmodified — confirmed with `printf '5:foo:bar:baz\n' | { IFS=: read -r a b; }` yielding a=5, b='foo:bar:baz'. The claimed mis-split on an embedded colon in a directive does not occur; the code already behaves like the recommended 'split on first colon only' approach.

19. [x] **security-reviewer** | `scripts/check-skip-rationales.sh:91` | injection
   The SKIP_REGEX is passed as a shell variable to 'grep -nE'. If SKIP_REGEX were ever modified to interpolate attacker-controlled content this could become a ReDoS vector. In the current implementation SKIP_REGEX is a hardcoded literal and therefore not exploitable, but the pattern uses nested alternation and back-to-back quantifier groups that could become expensive on pathological input if the regex is extended without care. CWE-1333 (ReDoS).
   *Recommendation:* The current literal pattern is safe. When extending SKIP_REGEX in the future, prefer anchored alternatives (each sub-pattern anchored at ^) and avoid unbounded repetition inside alternation. Document the regex intent with a comment.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* No action — The finding's own recommendation states 'The current literal pattern is safe' and only offers forward-looking guidance for if SKIP_REGEX is extended later. Left as-is per the finding's own conclusion.

20. [x] **security-reviewer** | `test/integ/authentication.cpp:204` | authentication
   The digest-auth block is now permanently skipped on Windows (#if !defined(_WINDOWS) && defined(HAVE_DAUTH)). This is correctly documented in test/PORTABILITY.md and carries a // reason: comment, so the lint gate passes. However, the skip means that the digest challenge/response round-trip and the check_digest_auth_digest() path are not exercised on the Windows CI lanes. This does not introduce a vulnerability but it is a test-coverage gap for an authentication code path. This matches the task's stated intent (document the gap rather than fix it), so it is noted here for awareness only.
   *Recommendation:* Ensure the existing Linux/Darwin CI lanes continue to execute the full digest-auth suite on every PR. Consider adding a comment in the test file noting which CI lane is the authoritative coverage owner for the digest code path.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* No action — The finding itself concludes 'This matches the task's stated intent (document the gap rather than fix it), so it is noted here for awareness only' — it is an informational observation about CI coverage ownership, not an action item, and the gap is already documented in test/PORTABILITY.md §authentication.cpp with a // reason: comment.

21. [x] **spec-alignment-checker** | `.github/workflows/verify-build.yml:773` | specification-gap
   The skip-rationale gate (check-skip-rationales.sh) is wired only to the 'lint' build-type lane on ubuntu-latest. The task's acceptance criterion says 'Windows lane in verify-build.yml exercises new variants where added'. The Windows smoke variant is indeed in ws_start_stop.cpp and the 'Verify Windows smoke test ran' step at line 1027 exercises it correctly. However, the lint gate itself only fires on Linux; Windows CI never runs check-skip-rationales.sh. This means a Windows-only skip without a reason: comment would evade the gate on the Windows lanes. This is consistent with how other lint gates (cpplint, check-warning-suppressions.sh, etc.) are already scoped, so this is an existing pattern rather than a new gap introduced by TASK-077.
   *Recommendation:* Consider running check-skip-rationales.sh as a gate-only step on both Ubuntu and Windows, or document that lint gates are Linux-only in the CI notes. Low priority given this is consistent with the pre-existing lint gate scope.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* No action — Confirmed check-skip-rationales.sh (verify-build.yml line ~899) is gated identically to every other lint-only check in the file (cpplint, check-complexity.sh, check-duplication.sh, check-file-size.sh, check-warning-suppressions.sh — all `if: matrix.build-type == 'lint' && matrix.os-type == 'ubuntu'`). The finding itself notes this 'is consistent with how other lint gates ... are already scoped, so this is an existing pattern rather than a new gap', and its recommendation calls it low priority. Re-litigates the existing, deliberate lint-gate scoping convention.

22. [x] **spec-alignment-checker** | `test/PORTABILITY.md:54` | acceptance-criteria
   The PORTABILITY.md §ws_start_stop.cpp section refers to 'lines 113–1413' for the wide skip, but the actual #ifndef _WINDOWS directive in the current file is at line 121 (after the 8-line block of explanatory comments was prepended). This is a documentation/cross-reference drift between the portability doc and the live file, not a code defect, but a reader following the documented line reference will land on the opening comment block rather than the directive itself.
   *Recommendation:* Update PORTABILITY.md §ws_start_stop.cpp to reference 'line 121 (directive)' or use a range like '113–121 (rationale comment + directive)' to match the current file layout.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Updated the ws_start_stop.cpp heading in test/PORTABILITY.md to the current line numbers (rationale comment 130-137, #ifndef _WINDOWS directive at 138, closes at line 1300), superseding both the doc's stale numbers and the finding's own suggested correction.

23. [x] **spec-alignment-checker** | `test/PORTABILITY.md:71` | acceptance-criteria
   The PORTABILITY.md §authentication.cpp section references 'line 201' for the digest-auth block, but the actual #if !defined(_WINDOWS) && defined(HAVE_DAUTH) directive in authentication.cpp is at line 204 (after the 3-line rationale comment block was prepended). Same cross-reference drift as finding 1.
   *Recommendation:* Update PORTABILITY.md §authentication.cpp to reference line 204 (or a comment+directive range) to match the current file layout.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Updated the authentication.cpp digest-auth block heading in test/PORTABILITY.md to the current line 265 (the #if !defined(_WINDOWS) && defined(HAVE_DAUTH) directive), superseding both the doc's stale number and the finding's suggested correction.

24. [x] **test-quality-reviewer** | `scripts/test_check_skip_rationales.sh:113` | missing-test
   There is no test exercising a file that has multiple '#ifndef _WINDOWS' directives — one with a '// reason:' comment and one without. The current tests only place a single directive per file, so the multi-directive path through the inner 'while' loop in check-skip-rationales.sh is never exercised. A future bug that processes only the first directive per file would not be caught.
   *Recommendation:* Add a test fixture with two '#ifndef _WINDOWS' blocks: the first guarded by a '// reason:' comment, the second without. The script should exit 1 and the report should name the second (non-compliant) directive. This directly exercises the 'while IFS=: read -r lineno directive' loop iterating more than once per file.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Added test 9e to scripts/test_check_skip_rationales.sh: a fixture with two `#ifndef _WINDOWS` blocks (first compliant, second not) asserting exit 1 and that stderr names the second directive's line number, exercising the multi-directive path through the inner while-read loop.

25. [x] **test-quality-reviewer** | `scripts/test_check_skip_rationales.sh:127` | missing-test
   Test 7 verifies that '#ifdef _WINDOWS' (additive polarity) does not require a '// reason:' comment and exits 0. There is no equivalent test for '#ifdef DARWIN'. The SKIP_REGEX explicitly excludes both '#ifdef _WINDOWS' and '#ifdef DARWIN', so the DARWIN additive case should be verified separately to catch any future regex change that accidentally includes it.
   *Recommendation:* Add a test fixture using '#ifdef DARWIN' (no '// reason:' comment) and assert exit 0, mirroring test 7 for the DARWIN identifier.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Added test 7b to scripts/test_check_skip_rationales.sh: an `#ifdef DARWIN` (additive, no reason comment) fixture asserting exit 0, mirroring test 7 for DARWIN.

26. [x] **test-quality-reviewer** | `scripts/test_check_skip_rationales.sh:156` | missing-test
   Tests 8 and 9 cover '#if !defined(_WINDOWS)' with an additional '&& defined(HAVE_DAUTH)' conjunction, but no test exercises the bare '#if !defined(_WINDOWS)' form without a conjunction — i.e., '#if !defined(_WINDOWS)' alone on the line. The SKIP_REGEX in check-skip-rationales.sh is designed to match both forms, but only the compound form is exercised by the self-test.
   *Recommendation:* Add a fixture using '#if !defined(_WINDOWS)' (no '&& ...' clause) without a '// reason:' comment and verify exit 1. Add the matching positive case (with '// reason:') and verify exit 0. This rules out a regex anchoring issue where the bare form is accidentally excluded.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Added tests 9c and 9d to scripts/test_check_skip_rationales.sh: a bare `#if !defined(_WINDOWS)` fixture with no `&&` conjunction and no reason (exit 1), and the same form with an adjacent reason comment (exit 0), closing the conjunction-free coverage gap left by tests 8/9.

27. [x] **test-quality-reviewer** | `scripts/test_check_skip_rationales.sh:80` | missing-test
   There is no test case covering the boundary condition of exactly 5 lines between the '// reason:' comment and the directive (the maximum allowed distance). Test 3 exercises 3 lines above and test 4 exercises 6 lines above, so the window edge at exactly 5 is untested. An off-by-one in check-skip-rationales.sh's awk window (`NR >= target - 5`) would not be caught.
   *Recommendation:* Add a test case where the '// reason:' comment is placed exactly 5 lines above the directive (target - 5 == NR exactly). The fixture should contain 5 lines of filler between the reason comment and the '#ifndef _WINDOWS' line and the script must exit 0 for this case.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Added test 3b to scripts/test_check_skip_rationales.sh: a fixture with the reason comment exactly 5 lines above the directive (the awk window's inclusive `NR >= target - 5` boundary), asserting exit 0, closing the gap between test 3 (3 lines) and test 4 (6 lines).
