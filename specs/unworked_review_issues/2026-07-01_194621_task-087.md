# Unworked Review Issues

**Run:** 2026-07-01 19:46:21
**Task:** TASK-087
**Total:** 30 (0 critical, 0 major, 30 minor)

## Minor

1. [ ] **architecture-alignment-checker** | `specs/architecture/09-testing.md:7` | pattern-violation
   Section 09-testing.md states 'sanitizer-clean tests' are required (DR-5 reference) without distinguishing which sanitizers cover which test surfaces. The msan lane deliberately scopes to unit tests only (curl is uninstrumented), but this intentional narrowing is not reflected in the architecture doc. Other readers may infer full make-check coverage is required for all sanitizers.
   *Recommendation:* Add a note to 09-testing.md clarifying that msan coverage is scoped to backend-free unit tests because curl cannot be practically instrumented, mirroring the inline justification already present in the workflow step. This is a documentation gap, not a code defect.

2. [ ] **architecture-alignment-checker** | `specs/architecture/09-testing.md:7` | pattern-violation
   The msan scoping rationale is appended inline to item 3 (DR-5 move-semantics bullet), making that bullet very dense. msan coverage spans all unit/ targets, not just move-semantics tests, so the rationale is architecturally broader than its parent bullet's subject.
   *Recommendation:* Consider extracting the sanitizer-matrix paragraph into a standalone item 3a or a sub-bullet so the DR-5 coverage sentence stays focused on move-semantics and the msan scoping explanation stands on its own. This is a readability/navigation concern, not a correctness issue; the content itself is accurate.

3. [ ] **cloud-infrastructure-reviewer** | `.github/workflows/verify-build.yml:1059` | iac-anti-pattern
   [OE-01 Operational Excellence — Observability] The grep pattern in MSAN_TESTS derivation (`'^[A-Za-z0-9_]+_SOURCES[[:space:]]*=[[:space:]]*unit/'`) requires each unit test's first-listed source to reside under `unit/`. A future test whose Makefile.am entry lists a non-unit/ source first (or uses a multi-line assignment) will be silently excluded from the msan run, narrowing coverage without a build error. The empty-guard only fires if ALL unit tests disappear, not if a subset is silently dropped.
   *Recommendation:* Consider a broader extraction strategy, e.g. extracting test binary names from `check_PROGRAMS` or a dedicated Makefile variable, to ensure new unit tests are never silently excluded. See https://www.gnu.org/software/automake/manual/html_node/Tests.html

4. [ ] **cloud-infrastructure-reviewer** | `.github/workflows/verify-build.yml:571` | insecure-default
   [Security — MemorySanitizer False-Positive Risk] The 'Install CURL (for testing on linux)' step installs libcurl4-openssl-dev on every ubuntu lane, including msan, without a `matrix.build-type != 'msan'` guard. If the autoconf ./configure detects curl and the test Makefile links libcurl into unit test binaries via a global LDADD (rather than per-target LDADD scoped to integration tests), MSAN will observe buffer copies from the uninstrumented libcurl and report use-of-uninitialized-value false positives. This risk does not affect asan/tsan/lsan/ubsan lanes (they do not track uninitialized memory). The MSAN_TESTS scoping prevents curl-using integration tests from executing, but it does not prevent the uninstrumented library from being linked into unit test binaries. The current implementation is safe only if unit test binaries do not link libcurl.
   *Recommendation:* Verify that the test/Makefile.am unit test targets use per-target LDADD that excludes libcurl (i.e., only integration test targets carry `-lcurl`). If any unit test binary links libcurl, add `matrix.build-type != 'msan'` to the curl install step's `if:` condition and omit `--with-curl` (or equivalent) from the msan configure invocation. Document the assumption in the step comment. See MSAN documentation on uninstrumented origins: https://clang.llvm.org/docs/MemorySanitizer.html#handling-external-code

5. [ ] **cloud-infrastructure-reviewer** | `.github/workflows/verify-build.yml:633` | iac-anti-pattern
   [Security — Supply-Chain Integrity] The Build instrumented libc++ step clones the LLVM project via `git clone --depth 1 --branch llvmorg-18.1.8` without a post-clone commit-SHA verification step. If the llvmorg-18.1.8 tag were ever force-pushed (or a DNS/MITM attack substituted a different repository), the build would silently use a different LLVM tree. This is inconsistent with the project's own established supply-chain policy: the IWYU build (lines 515–525) pins to a commit SHA and verifies it with `git rev-parse --verify HEAD`; curl and MHD tarballs are sha256-pinned; PMD is sha256-pinned. LLVM is the only external source without a post-clone integrity check.
   *Recommendation:* After the git clone, record the expected commit SHA for llvmorg-18.1.8 in the workflow (obtainable via `git ls-remote https://github.com/llvm/llvm-project refs/tags/llvmorg-18.1.8`) and add a verification step mirroring the IWYU pattern: `HEAD_SHA="$(git -C llvm-project rev-parse --verify HEAD)"; if [ "${HEAD_SHA}" != "${LLVM_SHA}" ]; then echo "LLVM SHA mismatch" >&2; exit 1; fi`. Document the rotation procedure (update LLVM_SHA in the same PR as any llvmorg tag bump). See SLSA provenance guidance: https://slsa.dev/spec/v1.0/levels#build-l2

6. [ ] **cloud-infrastructure-reviewer** | `.github/workflows/verify-build.yml:645` | iac-anti-pattern
   [OE-02 Operational Excellence — Reproducibility] The instrumented libc++ build clones `--depth 1 --branch llvmorg-18.1.8` from GitHub each time the cache misses. The cache key `${{ matrix.os }}-${{ matrix.c-compiler }}-libcxx-msan-origins-v1` does not embed the LLVM tag, so a future cache bust (e.g. runner image rotation that changes `matrix.os`) rebuilds at whatever HEAD `llvmorg-18.1.8` points to at that moment. If the tag is later moved or the clone resolves to a different commit, the cached artifact and the rebuilt artifact will differ silently.
   *Recommendation:* Include the LLVM tag in the cache key (e.g. `libcxx-msan-llvmorg-18.1.8-origins-v1`) and pin the clone with `--branch llvmorg-18.1.8` plus a post-clone `git rev-parse HEAD` echo for traceability. See https://docs.github.com/en/actions/using-workflows/caching-dependencies-to-speed-up-workflows#using-the-cache-action

7. [ ] **code-quality-reviewer** | `.github/workflows/verify-build.yml:633` | code-elegance
   The instrumented libc++ build clones `--branch llvmorg-18.1.8` (a mutable tag reference) without a subsequent SHA assertion. Every other pinned external in this workflow uses an immutable reference — IWYU is pinned to a commit SHA with a mismatch-fail assertion, PMD is SHA256-verified, libmicrohttpd is SHA256-verified. The llvmorg tag is extremely unlikely to be moved in practice, but the inconsistency with the project's stated supply-chain posture is notable.
   *Recommendation:* After `git clone --depth 1 --branch llvmorg-18.1.8`, add `LLVM_SHA=$(git -C llvm-project rev-parse HEAD) ; echo "LLVM pinned at: $LLVM_SHA"` and optionally a hard-coded expected-SHA assertion (mirroring the IWYU pattern). Alternatively, add `--depth 1` already present and document the accepted SHA in a comment so rotation is visible in diff.

8. [ ] **code-quality-reviewer** | `scripts/check-msan-lane.sh:113` | error-handling
   When PyYAML is not importable the embedded Python subprocess exits with code 10, but the outer shell's `if ! python3 - "$WF" <<'PY'; then ... fail=1; fi` path treats any non-zero exit the same — it sets fail=1 and prints `(a)/(b) FAILED`. A developer without PyYAML installed would see a confusing "msan lane is not fully wired" failure rather than a clear "install PyYAML" message. The Python script does print to stderr, but stderr is not surfaced in the outer shell's error path.
   *Recommendation:* Capture the Python exit code explicitly and short-circuit with a distinct message when it is 10: `python3 - "$WF" <<'PY'; py_rc=$?; if [ "$py_rc" -eq 10 ]; then echo 'check-msan-lane: SKIP — PyYAML unavailable (pip install pyyaml)' >&2; exit 0; fi; if [ "$py_rc" -ne 0 ]; then fail=1; fi`. This distinguishes a missing-tool skip from a real structural failure.

9. [ ] **code-quality-reviewer** | `scripts/check-msan-lane.sh:121` | code-readability
   Assertion (c) is brittle to future historical comments. The grep patterns `'^[[:space:]]*#.*ubuntu-18\.04'` and `'^[[:space:]]*#.*clang-6\.0'` would fire on any commented line mentioning those strings — including, for example, a future commit message or changelog reference in the YAML that says 'previously used ubuntu-18.04'. This could produce a false failure that is confusing to diagnose.
   *Recommendation:* Narrow the pattern to the specific context: check for a commented-out `build-type: msan` co-occurring with `ubuntu-18.04` or `clang-6.0`. Alternatively, document the limitation in a comment above the grep lines so future maintainers understand the heuristic is intentionally coarse.

10. [ ] **code-quality-reviewer** | `scripts/check-msan-lane.sh:29` | code-readability
   The header comment documents only exit codes 0 and 1, but a missing PyYAML installation causes the embedded Python script to call sys.exit(10), which propagates through the gate with exit code 10. Callers that check specifically for exit code 1 (rather than any non-zero) would not match.
   *Recommendation:* Either map sys.exit(10) to sys.exit(1) in the ImportError handler, or document exit code 10 in the header comment. Since CI only tests non-zero, a simple sys.exit(1) in the ImportError branch is the cleaner fix.

11. [ ] **code-quality-reviewer** | `scripts/test_check_msan_lane.sh:72` | test-coverage
   The five test cases do not exercise the missing-workflow-file path in check-msan-lane.sh (lines 41-44). That branch unconditionally exits 1, so a regression that accidentally skipped the guard would go undetected by the self-test.
   *Recommendation:* Add Test 6: pass a path that does not exist (e.g. $TMPDIR_BASE/does_not_exist.yml) and assert exit 1.

12. [ ] **code-quality-reviewer** | `scripts/test_check_msan_lane.sh:72` | test-coverage
   The self-test has no case for the 'duplicate msan entries' branch (Python sys.exit(5), check-msan-lane.sh line 84). A workflow that accidentally duplicates the msan include entry would pass the test suite unreported.
   *Recommendation:* Add a fixture that has two msan include entries and assert exit 1.

13. [ ] **code-simplifier** | `.github/workflows/verify-build.yml:1050` | patterns
   The `-o` flag in `grep -oE '^[A-Za-z0-9_]+_SOURCES[[:space:]]*=[[:space:]]*unit/'` outputs only the matched prefix of each line (e.g., `foo_SOURCES = unit/`). The subsequent `sed -E 's/_SOURCES.*//'` then strips `_SOURCES...` to leave just the target name. Without `-o`, grep outputs the full matching line and the same `sed` expression still yields the correct target name (since `_SOURCES.*` anchors to the variable name regardless of what follows). The `-o` flag adds precision without changing the output.
   *Recommendation:* Drop the `-o` flag: `grep -E '^[A-Za-z0-9_]+_SOURCES[[:space:]]*=[[:space:]]*unit/' ../test/Makefile.am | sed -E 's/_SOURCES.*//' | tr '\n' ' '`

14. [ ] **code-simplifier** | `scripts/check-msan-lane.sh:121` | patterns
   Assertion (c) opens the workflow file twice with two separate `grep -qE` calls joined by `||`. A single grep with alternation is equivalent and avoids the double file read.
   *Recommendation:* Replace the two-call form with: `if grep -qE '^[[:space:]]*#.*(ubuntu-18\.04|clang-6\.0)' "$WF"; then`

15. [ ] **code-simplifier** | `scripts/check-msan-lane.sh:61` | naming
   `# noqa: BLE001` inside a Python heredoc embedded in bash will never be processed by any linter (ruff, flake8, or otherwise). The annotation is noise that could mislead a reader into thinking this code is linted.
   *Recommendation:* Replace `# noqa: BLE001 - surface any parse failure` with a plain comment: `# broad catch: surface any YAML parse error to stderr`

16. [ ] **code-simplifier** | `specs/architecture/09-testing.md:7` | code-structure
   Bullet 3 now carries two distinct ideas run together into one long paragraph: the original move-semantics claim, then a second block explaining msan scope rationale (why curl is excluded, why libc++ and libmicrohttpd are instrumented). The combined line exceeds 450 characters and makes the bullet noticeably harder to scan than the others.
   *Recommendation:* Split the msan scope rationale into a dedicated sub-bullet or a note immediately after bullet 3, e.g. '   - Note: msan coverage is scoped to `unit/` targets because curl (uninstrumented) is linked into every integration round-trip; libc++ and libmicrohttpd are built instrumented in-CI to keep the unit subset clean without wildcard suppressions.' This keeps bullet 3 focused on the move-semantics claim and makes the scope decision easy to find on its own.

17. [ ] **housekeeper** | `specs/tasks/M7-v2-cleanup/TASK-087.md:15` | action-item-not-marked-complete
   Action item 3 ('Triage any new findings') remains unchecked. The deferral note is well-formed and the rationale is sound — triage requires an actual CI green run, and the conditional `msan.supp` wiring is already in place — but the item is technically still open at merge time.
   *Recommendation:* No action required before merge. The deferral note ('deferred to PR — CI-side') accurately describes the situation. The item should be revisited and either checked off or converted to a follow-up task once the first msan CI run completes.

18. [ ] **performance-reviewer** | `.github/workflows/verify-build.yml:626` | missing-caching
   The instrumented-libc++ cache key (`${{ matrix.os }}-${{ matrix.c-compiler }}-libcxx-msan-origins-v1`) does not encode the LLVM tag (`llvmorg-18.1.8`) that is hardcoded in the build step on line 633. If the tag is bumped in the build command without a corresponding version-suffix increment, the runner restores stale cached libc++ binaries that were built from a different LLVM commit, silently running msan tests against the wrong runtime.
   *Recommendation:* Embed the LLVM tag in the cache key suffix, e.g. `libcxx-msan-origins-llvmorg-18.1.8-v1`, or define a workflow-level env var `LLVM_TAG: llvmorg-18.1.8` and reference it in both the key and the clone command so a single edit updates both atomically.

19. [ ] **performance-reviewer** | `.github/workflows/verify-build.yml:626` | missing-caching
   libcxx-msan cache key omits the LLVM source tag. The build command hardcodes llvmorg-18.1.8 (line 632), but the cache key is keyed only on os + c-compiler + static suffix libcxx-msan-origins-v1. If the tag is bumped in the build command the key does not change and runners reuse stale instrumented libc++ artifacts until v1 is manually renamed.
   *Recommendation:* Encode the LLVM tag in the key, e.g. ${{ matrix.os }}-${{ matrix.c-compiler }}-libcxx-msan-llvmorg-18.1.8-v1, or add a comment immediately above the key line explicitly stating that any tag change requires a key-version bump.

20. [ ] **performance-reviewer** | `.github/workflows/verify-build.yml:708` | missing-caching
   libmicrohttpd-msan cache key omits the MSAN CFLAGS. The build step at line 721 uses -fsanitize=memory -fsanitize-memory-track-origins -fno-omit-frame-pointer -g -O1, but the cache key is keyed only on os + c-compiler + version + static suffix msan-v1. Changing any flag (e.g. removing -fsanitize-memory-track-origins for a leaner build) will silently reuse a stale artifact built with the old flags until v1 is manually bumped.
   *Recommendation:* Either hash the CFLAGS into the key (a short hash of the flag string appended to the key) or document the manual v1-bump dependency in a comment directly adjacent to the key field so the linkage is explicit.

21. [ ] **performance-reviewer** | `.github/workflows/verify-build.yml:724` | missing-parallelization
   The instrumented libmicrohttpd build on line 724 uses bare `make` with no `-j` flag. On a cache miss this runs the libmicrohttpd compile serially. GitHub Actions ubuntu-latest runners have 4 vCPUs; adding `-j$(nproc)` would cut this step's wall-clock time by roughly 60-70% on a miss. (The stock libmicrohttpd build on line 667 has the same omission, but that is pre-existing; the msan build is the one introduced by this task.)
   *Recommendation:* Change `make ;` to `make -j$(nproc) ;` on line 724 to exploit all available cores, consistent with how other heavy C builds in the repo (e.g. the IWYU cmake/ninja steps) are handled.

22. [ ] **security-reviewer** | `.github/workflows/verify-build.yml:632` | supply-chain
   The instrumented libc++ build step clones llvm-project with `--branch llvmorg-18.1.8` (a mutable git tag) rather than a pinned commit SHA with a post-clone verification assertion. Git tags can be moved or force-pushed on GitHub. This is inconsistent with the existing IWYU build step (same file) which pins to IWYU_SHA and explicitly verifies `git rev-parse HEAD` matches before building. CWE-829 (inclusion from untrusted control sphere).
   *Recommendation:* After the clone, record the resolved SHA and assert it matches a known-good value, mirroring the IWYU pattern: `HEAD_SHA=$(git -C llvm-project rev-parse HEAD); [ "$HEAD_SHA" = "$LLVM_EXPECTED_SHA" ] || { echo "SHA mismatch"; exit 1; }`. Obtain the expected SHA for llvmorg-18.1.8 from the LLVM GitHub releases page and embed it as LLVM_EXPECTED_SHA.

23. [ ] **spec-alignment-checker** | `specs/tasks/M7-v2-cleanup/TASK-087.md:15` | action-item
   Action item 'Triage any new findings' is left unchecked ([ ]) because actual msan findings cannot be observed until the instrumented CI lane runs on a real PR. The task text documents the deferral explicitly and the conditional suppression wiring is already in place. This is an intentional open item, not an implementation gap.
   *Recommendation:* No code change required. After the first green CI run, mark the action item done or add a narrow test/msan.supp entry if a real finding surfaces. This minor is carried from iter-1.

24. [ ] **spec-alignment-checker** | `specs/tasks/M7-v2-cleanup/TASK-087.md:31` | specification-gap
   TASK-087.md references 'PRD §3.6 sanitizer-clean' but specs/product_specs.md §3.6 is 'Request Type Ergonomics (API-REQ)' — it contains no sanitizer-clean requirements. Similarly 'PRD §2 security NFR' is cited, but §2 ('Non-functional & cross-cutting requirements') lists build stability, header hygiene, const correctness, performance, naming, documentation, and concurrency; no explicit sanitizer-clean NFR exists. The msan lane is justified by the general quality intent of §2 NFRs, but the specific cross-references in the task definition are incorrect.
   *Recommendation:* Update TASK-087.md to reference the correct rationale: the msan lane supports PRD §2's implicit correctness and memory-safety quality bar. No code change needed.

25. [ ] **test-quality-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-087/scripts/test_check_msan_lane.sh:106` | missing-test
   Test 4 breaks both assertion (d1) (MSAN_OPTIONS) and (d2) (scoped 'make check TESTS=') simultaneously by stripping the entire msan branch. A developer who removed just the 'grep -q MSAN_OPTIONS' check from the gate while keeping the 'make check TESTS=' check would not be caught: Test 4 still exits 1 via the surviving (d2) check.
   *Recommendation:* Add a second wiring-fixture that retains MSAN_OPTIONS but omits 'make check TESTS=' (or vice versa) so each sub-assertion has an independent failing fixture.

26. [ ] **test-quality-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-087/scripts/test_check_msan_lane.sh:120` | missing-test
   No test exercises the 'more than one msan include entry' branch (Python sys.exit(5) path in check-msan-lane.sh). A workflow with two build-type:msan entries is a realistic misconfiguration but the test suite does not verify the gate catches it.
   *Recommendation:* Add a fixture that duplicates the msan include block and assert the gate exits 1 for it.

27. [ ] **test-quality-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-087/scripts/test_check_msan_lane.sh:17` | naming-convention
   Test descriptions passed to ok()/fail() encode the expected exit code ('exits 0', 'exits 1') rather than the behavioral property under test (e.g., 'assertion_b_detects_missing_msan_entry'). Exit codes are implementation details; a behavioral label makes failures self-describing without reading the gate source.
   *Recommendation:* Rename descriptions to state the assertion letter and property, e.g., 'assertion_b: missing msan include entry detected' — consistent with the gate's own (a)/(b)/(c)/(d) labeling.

28. [ ] **test-quality-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-087/scripts/test_check_msan_lane.sh:81` | missing-test
   Assertion (b) has several uncovered sub-cases: (b2) an msan entry that exists but carries a wrong fixed-key value (e.g., compiler-family: gcc, exit 6), (b3) an msan entry missing a presence-only key such as 'os' (exit 6), and (b4) two msan entries (exit 5). Test 2 only covers the case where no msan entry exists at all (exit 4). A future accidental key rename or duplication would not be caught.
   *Recommendation:* Add at least one fixture where the msan entry is present but has a wrong value for a fixed key (e.g., replace 'debug: debug' with 'debug: nodebug') and one where a presence-only key such as 'os' is absent, covering the exit-6 path.

29. [ ] **test-quality-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-087/scripts/test_check_msan_lane.sh:81` | missing-test
   No test covers a msan include entry that is present but has wrong fixed-key values (e.g., compiler-family: gcc or debug: release), which would trigger sys.exit(6) in the gate. Only the complete-absence case for assertion (b) is tested.
   *Recommendation:* Add a fixture with a msan entry that has an incorrect fixed-key value and assert the gate exits 1.

30. [ ] **test-quality-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-087/scripts/test_check_msan_lane.sh:83` | redundant-test
   write_good "$NO_MSAN" on line 83 is immediately overwritten on line 85 by the sed substitution that writes from $GOOD into $NO_MSAN. The write_good call is dead code: the file it creates is replaced before it is ever read.
   *Recommendation:* Drop the write_good call on line 83; the fixture is fully produced by the sed pipeline on line 85.
