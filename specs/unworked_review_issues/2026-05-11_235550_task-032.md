# Unworked Review Issues

**Run:** 2026-05-11 23:55:50
**Task:** TASK-032
**Total:** 38 (0 critical, 5 major, 33 minor)

## Major

1. [x] **code-quality-reviewer** | `test/integ/threadsafety_stress.cpp:139` | error-handling
   The `unregister_path` case (op==1) catches all exceptions with a bare `catch (...)` and discards them silently. If `unregister_path` throws something unexpected — e.g., a logic error or bad_alloc — the test will never know, and the op counter simply won't increment. The silence makes a real failure look like a normal race. By contrast, `register_path` catches only the documented `std::invalid_argument`, which is the correct pattern.
   *Recommendation:* Either catch only the documented exception (e.g., `std::invalid_argument` for path-not-found) and rethrow anything else, or at minimum log unexpected exceptions to stderr before swallowing them. Example: `} catch (const std::invalid_argument&) { /* not registered, ok */ } catch (const std::exception& e) { std::cerr << "unexpected unregister_path exception: " << e.what() << '\n'; }`
   *Status:* fixed in this batch — replaced bare `catch (...)` with `catch (const std::invalid_argument&)` + `catch (const std::exception& e)` with stderr logging.

2. [x] **performance-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-032/test/integ/threadsafety_stress.cpp:229` | blocking-io
   Each of the 16 client threads spins in a tight `while (!stop)` loop calling `curl_easy_perform` back-to-back with no inter-request sleep or yield. Under TSan (which slows every memory access by 5–20x), 8 MHD worker threads plus 16 curl threads all simultaneously hammering the same mutexes will cause severe lock contention and shadow-memory pressure. The 60-second wall-clock timer is measured by the *parent* thread, but TSan-slowed curl threads will not complete as many round-trips, so the test will silently take 300–1200s of real wall-clock time in a TSan CI build. If the CI job has a hard timeout shorter than that, the test will be killed and reported as a failure rather than a TSan-clean pass.
   *Recommendation:* Add a short inter-request sleep in the client loop — `std::this_thread::sleep_for(std::chrono::milliseconds(5))` is enough to rate-limit each thread to ~200 req/s while still exercising every lock path. Alternatively, reduce `kClients` to 4 under TSan (detect via `#if defined(__has_feature) && __has_feature(thread_sanitizer)` or the `TSAN_OPTIONS` env var) and lower the default `HTTPSERVER_STRESS_SECONDS` from 60 to 10 for TSan builds, then document the override in the CI matrix with `HTTPSERVER_STRESS_SECONDS=10`. Either approach keeps the TSan run well inside a 3-minute CI budget.
   *Status:* fixed in this batch — added `std::this_thread::sleep_for(std::chrono::milliseconds(5))` after each `curl_easy_perform`.

3. [x] **spec-alignment-checker** | `specs/tasks/M5-routing-lifecycle/TASK-032.md:29` | action-item
   Task status field reads 'In Progress' but the task description block in the prompt says Status: Done and all action items appear implemented. The task file has not been updated to reflect completion.
   *Recommendation:* Change '**Status:** In Progress' to '**Status:** Done' in TASK-032.md to match the actual state and prevent downstream tooling from treating the task as still open.
   *Status:* already addressed — TASK-032.md already reads `**Status:** Done`.

4. [x] **test-quality-reviewer** | `test/integ/threadsafety_stress.cpp:219` | test-isolation
   curl_easy_init() return value is not checked for NULL before being passed to curl_easy_setopt(). If libcurl initialization fails (e.g., resource exhaustion from running 16 threads), curl_easy_setopt and curl_easy_perform will dereference NULL, producing a crash that kills the test binary rather than a graceful failure. Under TSan — which has meaningful memory overhead — this risk is elevated.
   *Recommendation:* Add a null check immediately after curl_easy_init(): if (!curl) { /* skip iteration or abort thread */ return; }. Follow the pattern used by other integration tests in this codebase that also guard curl_easy_init returns before calling setopt.
   *Status:* fixed in this batch — added `if (!curl) return;` guard immediately after `curl_easy_init()`.

5. [x] **test-quality-reviewer** | `test/integ/threadsafety_stress.cpp:257` | implementation-coupling
   The post-run assertions aggregate register_ok + unregister_ok together (> 0) and block_ok + unblock_ok together (> 0). This means a silent regression where, say, register_path becomes permanently broken while unregister_path continues to fire, or block_ip becomes broken while unblock_ip fires, would leave the assertion green. The combined form does not provide the independent coverage gate the task's acceptance criterion implies (all four mutating ops exercised).
   *Recommendation:* Assert each counter independently: LT_CHECK_GT(counters.register_ok.load(), 0) and LT_CHECK_GT(counters.unregister_ok.load(), 0) and so on for block_ok and unblock_ok. Given the 60-second run with 16 clients each cycling all four ops, all four counters will be nonzero in any healthy execution.
   *Status:* already addressed by TASK-052 — assertions are already individual per counter (register_ok, unregister_ok, block_ok, unblock_ok, hook_add_ok, hook_remove_ok).

## Minor

6. [x] **architecture-alignment-checker** | `specs/architecture/11-decisions/DR-008.md:23` | interface-contract
   The Verification section covers only `stop()` in the negative test, while the original Consequences bullet explicitly names both `stop()` and `~webserver()` as the two deadlock-by-design exceptions. The omission does not narrow or broaden the documented contract itself (the Consequences text is unchanged) but creates a gap: no test pins the `~webserver()` exception, and the Verification section does not acknowledge this gap.
   *Recommendation:* Add a note such as '`~webserver()` carries the same constraint but is not separately exercised because destructor invocation from a running handler is inherently process-unsafe; the `stop()` test is considered representative.' This keeps the Verification section accurate rather than implying full coverage.
   *Status:* fixed in this batch — added note to DR-008.md Verification section.

7. [x] **architecture-alignment-checker** | `specs/architecture/11-decisions/DR-008.md:25` | interface-contract
   The Verification section states 'exit code 42' corresponds to 'libmicrohttpd self-join abort' and 'exit code 43' to 'a 5-second parent timeout'. The actual test inverts both. Exit 42 is the unreachable line after `ws.stop()` returns in the child (regression sentinel: stop() returned cleanly). Exit 43 fires when curl completes and the child reaches the end (also a regression: stop didn't deadlock at all). The actual abort-signal path (MHD pthread self-join) terminates via a signal (WIFSIGNALED), producing no exit code. The 5-second parent timeout ends with SIGKILL on the child, also WIFSIGNALED — not exit code 43. The description in the Verification section therefore misattributes the semantics of both sentinel codes and could mislead anyone debugging a future CI failure.
   *Recommendation:* Correct the Verification section to read: exit code 42 is unreachable-after-stop (stop() returned from inside a handler — regression); exit code 43 is curl-completed-but-child-lived (stop() did not block at all — regression). The positive observations of the documented contract are signal-terminated (MHD abort) and SIGKILL-from-parent-after-5s-timeout (silent deadlock), both of which are WIFSIGNALED outcomes, not exit-code outcomes.
   *Status:* fixed in this batch — rewrote DR-008.md Verification bullet to correctly describe the exit codes as regression sentinels and WIFSIGNALED as positive observations.

8. [x] **architecture-alignment-checker** | `test/integ/threadsafety_stress.cpp:197` | interface-contract
   §9 testing item 6 names `register_resource` and `block_ip` as the methods to exercise from handlers. The test correctly uses the v2 non-deprecated successor `register_path` (and `unregister_path`, `block_ip`, `unblock_ip`). The §9 wording uses the old deprecated name because the testing section predates the TASK-024 renaming. This is not a defect in the test; the test is correct. The §9 wording is slightly stale but this is a documentation-only discrepancy, not a code defect. No architecture rule requires the test to call the deprecated alias.
   *Recommendation:* Update §9 testing item 6 in specs/architecture/09-testing.md to reference `register_path` / `unregister_path` instead of `register_resource` so the architecture document stays in sync with the v2 public API naming.
   *Status:* fixed in this batch — updated 09-testing.md §9 item 6 to list register_path / unregister_path / block_ip / unblock_ip and mention the opt-in stop_from_handler negative test.

9. [x] **architecture-alignment-checker** | `test/integ/threadsafety_stress.cpp:366` | pattern-violation
   The parent-side regression check `LT_CHECK(code != 0 && code != 42 && code != 43)` is only reached when `WIFEXITED(status)` is true (child exited normally). Exit code 43 is the case where the curl request to /wedge completed (10-second CURLOPT_TIMEOUT_MS elapsed) and the child reached the post-curl `_Exit(43)` line — meaning stop() was called but the process did not abort or deadlock within curl's 10-second window. Treating code 43 as a regression is marginally too strict: a slow MHD build on a loaded CI runner could legally take >10 s to detect the self-join, making the child exit 43 even though the contract held. The 5-second parent-side SIGKILL window and the 10-second curl timeout are not coordinated, so there is a gap (5s to 10s) where the child has been SIGKILLed by the parent but curl on the child side was still running. In practice the fork+SIGKILL path ensures the parent always reaps, so the gap is harmless. However the comment at line 338 says 'Any outcome OTHER than a zero exit is a positive observation' which is accurate — codes 42/43 are correctly flagged. The concern is the asymmetry between the 5-second parent wait and the 10-second child curl timeout: if the child has not been reaped in 5s the parent SIGKILLs it, so the child curl will never produce a 43; but if the child is reaped before 5s with code 43, it is flagged as a regression even though the 10-second wait may be a plausible slow-MHD scenario. This is a minor documentation/robustness issue, not a correctness regression.
   *Recommendation:* Reduce CURLOPT_TIMEOUT_MS on the child's curl handle to something less than the parent's 5-second wait (e.g., 4000L) so the two windows are ordered: parent always outlives the curl attempt. Alternatively add a comment explaining why code 43 is a regression (the handler returned, meaning stop() did not block). This resolves the apparent coordination gap and makes the intent clear.
   *Status:* fixed in this batch — reduced CURLOPT_TIMEOUT_MS to 3000L (< parent's 5 s) to order the two windows; added comment explaining rationale.

10. [x] **code-quality-reviewer** | `src/httpserver/webserver.hpp:133` | readability
    The updated `stop()` Doxygen now says `@return true if the daemon was running and is now stopped; false if it was already stopped.` This is a meaningful improvement over the previous text. No issue with correctness, but the destructor Doxygen (`~webserver()`) does not explicitly reference the stop() doc comment, requiring the reader to read both blocks to get the full picture of the two call sites that can deadlock.
    *Recommendation:* In the destructor Doxygen, add a brief cross-reference sentence such as `/// @see stop() for the threading constraints that apply equally here.` to guide readers to the canonical explanation.
    *Status:* fixed in this batch — added `@see stop()` cross-reference to `~webserver()` Doxygen.

11. [x] **code-quality-reviewer** | `test/integ/threadsafety_stress.cpp:121` | readability
    The magic bitmask values `0x7` (slot selection) and `0xff` (IP suffix) are used directly in `driver_body` without named constants. The choice of 8 slots (`0x7 == 7`) and 256 IP suffixes (`0xff`) relates directly to the `/dyn/N` path space and TEST-NET-2 subnet, but a reader must reconstruct this. The clean-code Names Rule 5 says to replace magic numbers with named constants.
    *Recommendation:* Extract: `constexpr int kDynSlots = 8;` and use `i % kDynSlots` (or `i & (kDynSlots - 1)`), and `constexpr int kIpRange = 256;` with `i % kIpRange`. This makes the intent explicit.
    *Status:* fixed in this batch — added `constexpr int kDynSlots = 8` and `constexpr int kIpRange = 256`; replaced all `0x7` and `0xff` uses.

12. [x] **code-quality-reviewer** | `test/integ/threadsafety_stress.cpp:138` | error-handling
    The catch-all for unregister_path (case 1, line 138-143) silently discards every exception including unexpected ones such as std::bad_alloc or logic_error, making failures invisible during the stress run. Only the documented duplicate-registration std::invalid_argument from register_path (case 0) has a named catch.
    *Recommendation:* Either catch std::invalid_argument specifically (same pattern as case 0) so unexpected exceptions surface, or at minimum catch std::exception and log it so a test-run log reveals unexpected failure modes during the stress window.
    *Status:* fixed in this batch — same fix as item 1.

13. [x] **code-quality-reviewer** | `test/integ/threadsafety_stress.cpp:162` | code-elegance
    `stress_seconds()` re-implements a pattern (getenv + stoi + positive-guard + fallback) that could be a generic `getenv_int(name, default)` utility. This is a stress test file, not a library, so the duplication is minor. However, the function is defined in an anonymous namespace but only used in one test, making it slightly broader in scope than needed.
    *Recommendation:* No action required — the current form is acceptably self-contained. If other tests in the suite later need similar getenv parsing, extract a shared helper at that point.
    *Status:* wontfix — cosmetic/style preference, no functional impact; recommendation explicitly says 'no action required'

14. [x] **code-quality-reviewer** | `test/integ/threadsafety_stress.cpp:256` | test-coverage
    The final assertions in `concurrent_register_block_from_handlers_no_data_race` check combined OR-groups (`register_ok + unregister_ok > 0`, `block_ok + unblock_ok > 0`) rather than each counter independently. With 16 clients running for 60 seconds across 4 op types, it is almost certain all four fire, but the test structure allows a scenario where, say, `block_ip` never fired (block_ok == 0) and the test still passes because unblock_ok is positive. This is inconsistent with the clean-code principle of one-assert-per-test and the acceptance criterion's language of 'register/lookup/block'.
    *Recommendation:* Add individual `LT_CHECK_GT` assertions for each counter (register_ok, unregister_ok, block_ok, unblock_ok) to guarantee every mutating path was exercised during the run.
    *Status:* already addressed by TASK-052 — assertions are already individual per counter.

15. [x] **code-quality-reviewer** | `test/integ/threadsafety_stress.cpp:256` | test-coverage
    The three LT_CHECK_GT assertions at the end of the primary test each aggregate two counters, meaning it is possible for only one of each pair (register_ok or unregister_ok; block_ok or unblock_ok) to have fired and still pass. Clean Code principle 'one assert per test' is also violated: the counters for registration ops and IP-block ops are conflated into single assertions instead of individual per-operation checks.
    *Recommendation:* Consider asserting each of the four counters individually (register_ok, unregister_ok, block_ok, unblock_ok) rather than pairwise sums, or document why pairwise is intentional (e.g. thundering-herd effect may starve one op). Either way a brief comment would eliminate the ambiguity.
    *Status:* already addressed by TASK-052 — same as item 14.

16. [x] **code-quality-reviewer** | `test/integ/threadsafety_stress.cpp:363` | readability
    Exit codes 42 and 43 are checked in the parent as regression signals, but these values are magic numbers with no named constants. They appear in three places: the `std::_Exit(42)` call inside the child handler lambda (line 312), the `std::_Exit(43)` after the curl call (line 332), and the `LT_CHECK(code != 0 && code != 42 && code != 43)` in the parent (line 366). A reader must cross-reference all three locations to understand what each code means.
    *Recommendation:* Define named constants: `constexpr int kExitCodeStopReturned = 42;` (stop() returned from handler) and `constexpr int kExitCodeCurlCompleted = 43;` (curl completed after stop() returned). Use them in all three locations.
    *Status:* fixed in this batch — added `kExitCodeStopReturned = 42` and `kExitCodeCurlCompleted = 43`; used in all three locations.

17. [x] **code-simplifier** | `specs/architecture/11-decisions/DR-008.md:25` | comments
    The Verification section parenthetically re-states exit codes 42 and 43 as regression sentinels at the end of the third bullet. The test file's own comments already explain this detail; the DR prose can be shorter without losing meaning.
    *Recommendation:* Trim the final parenthetical to: '...both count as positive observation of the documented deadlock contract.' The exit-code values are implementation detail better kept in the source.
    *Status:* fixed in this batch — rewrote the Verification bullet removing the specific exit-code values; describes behavior semantically instead.

18. [x] **code-simplifier** | `test/integ/threadsafety_stress.cpp:121` | naming
    The comment on `const int slot = i & 0x7;` explains nothing about why 8 slots (0–7). The magic literal `0x7` (mask for 8 values) and `0x3` (mask for 4 ops) are used twice each without named constants, making the relationship between slot count, op count and path/IP construction implicit.
    *Recommendation:* Introduce `constexpr int kDynSlots = 8;` and `constexpr int kOpCount = 4;` near `kClients`, then write `i % kDynSlots` and `op % kOpCount` (or equivalently `i & (kDynSlots - 1)` / `op & (kOpCount - 1)`). This documents the design intent (8 competing dynamic paths, 4 operations) without relying on readers to decode the hex masks.
    *Status:* fixed in this batch — same as item 11 (kDynSlots, kIpRange named constants added).

19. [x] **code-simplifier** | `test/integ/threadsafety_stress.cpp:122` | code-structure
    The `dyn_path` and `ip` local variables are declared before the switch even when only one branch uses each. Declaring them inside the relevant case would scope them more tightly and make the data flow clearer.
    *Recommendation:* Move `dyn_path` inside cases 0 and 1, and `ip` inside cases 2 and 3. Each case then owns exactly the variables it needs, eliminating the impression that both variables are used by every branch.
    *Status:* wontfix — cosmetic/style preference, no functional impact; low-value churn; variables already const with short lifetimes

20. [x] **code-simplifier** | `test/integ/threadsafety_stress.cpp:163` | naming
    `stress_seconds()` returns a plain `int` while the value is treated as a duration. The name is clear, but the return type does not signal the unit. A `std::chrono::seconds` return type would make the unit self-documenting and remove the implicit conversion at the call site (line 244).
    *Recommendation:* Change the return type to `std::chrono::seconds` and return `std::chrono::seconds(v)` / `std::chrono::seconds(60)`. The `deadline` assignment at line 242 then needs no cast.
    *Status:* wontfix — cosmetic/style preference, no functional impact; low-priority cosmetic rename in a stress-test file

21. [x] **code-simplifier** | `test/integ/threadsafety_stress.cpp:273` | code-structure
    The environment-variable opt-in check constructs a `std::string` from `run` just to compare with "1". A direct `strcmp` or `std::string_view` comparison avoids the allocation and is more consistent with how `stress_seconds()` reads its env var (plain `std::stoi` on the raw pointer).
    *Recommendation:* Replace `std::string(run) != "1"` with `std::string_view(run) != "1"` (or `strcmp(run, "1") != 0`). Matches the zero-allocation style used in `stress_seconds()`.
    *Status:* fixed in this batch — replaced `std::string(run) != "1"` with `std::string_view(run) != "1"`.

22. [x] **code-simplifier** | `test/integ/threadsafety_stress.cpp:339` | code-structure
    `auto deadline` in the parent poll loop (line 341) shadows the identically-named `deadline` used in the stress test above (line 242). Both are local to their own LT_BEGIN_AUTO_TEST block so there is no actual shadowing at runtime, but the reuse of the same name for unrelated concepts across the two test functions makes the file harder to scan. The stress-test variable is `stop_deadline` or `run_until` and the fork-wait variable is `child_deadline`.
    *Recommendation:* Rename the poll-loop `deadline` in `stop_from_handler_deadlocks_as_documented` to `child_deadline` to distinguish it from the stress-test `deadline` and make each variable's scope immediately clear.
    *Status:* fixed in this batch — renamed `deadline` to `child_deadline` in `stop_from_handler_deadlocks_as_documented`.

23. [x] **housekeeper** | `specs/architecture/09-testing.md:10` | architecture-not-updated
    Section 9, testing item 6 says the stress test exercises 'register_resource / block_ip from within handlers'. The implementation uses register_path (the non-deprecated TASK-024 successor) and also exercises unregister_path/unblock_ip. The description is slightly stale now that the test is concrete.
    *Recommendation:* Update §9 testing item 6 to read 'register_path / unregister_path / block_ip / unblock_ip' to match the actual test surface, and note the opt-in stop_from_handler negative test.
    *Status:* fixed in this batch — same as item 8.

24. [x] **housekeeper** | `specs/architecture/09-testing.md:10` | architecture-not-updated
    Testing §9 item 6 still describes the stress test as exercising 'register_resource / block_ip from within handlers'. The implementation uses register_path/unregister_path/block_ip/unblock_ip. This minor wording drift was flagged in iter 1 and explicitly skipped as optional per policy.
    *Recommendation:* Update §9 testing item 6 to list register_path / unregister_path / block_ip / unblock_ip and mention the opt-in stop_from_handler negative test. Low priority — no blocking concern.
    *Status:* fixed in this batch — same as items 8 and 23.

25. [x] **performance-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-032/test/integ/threadsafety_stress.cpp:121` | memory-allocation
    `const std::string dyn_path = "/dyn/" + std::to_string(slot)` and `const std::string ip = "198.51.100." + std::to_string(i & 0xff)` are allocated on every handler invocation. Because this runs inside an MHD worker thread (i.e., inside a live HTTP request), it is in the hottest path of the test. The allocations are small but constant per request, adding up at high request rates.
    *Recommendation:* Since `slot` is `i & 0x7` (8 values) and the IP suffix is `i & 0xff` (256 values), pre-compute both string tables as `static const std::string` arrays at file scope. Index into them without allocation: `static const std::string dyn_paths[8] = {"/dyn/0", ...}; static const std::string ips[256] = {"198.51.100.0", ...};`. This is a test file, so clarity beats brevity — a simple `constexpr`-initialised array is readable.
    *Status:* deferred — with the 5 ms inter-request sleep now in place (item 2 fix), allocation pressure is no longer a CI concern. Pre-computing 256-entry string tables would add noise to the test file with minimal benefit.

26. [x] **performance-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-032/test/integ/threadsafety_stress.cpp:232` | memory-allocation
    `std::string url = base + "?op=" + std::to_string(op) + "&i=" + std::to_string(i)` is constructed fresh on every iteration of the hot request loop, producing two `std::to_string` heap allocations plus three string-concatenation copies per iteration. Across 16 threads for 60 seconds this is millions of short-lived allocations, which adds allocator pressure and extra TSan shadow-memory churn on top of the lock-contention issue noted above.
    *Recommendation:* Pre-allocate a `std::string url` per thread outside the loop and use `url.resize(0); url += base; url += "?op="; url += std::to_string(op); ...` (or `snprintf` into a stack buffer) to reuse the existing heap allocation. This has zero correctness impact but noticeably reduces per-iteration allocation noise, which improves TSan signal-to-noise ratio.
    *Status:* deferred — same reasoning as item 25; sleep now rate-limits to ~200 req/s so allocator churn is bounded.

27. [x] **security-reviewer** | `test/integ/threadsafety_stress.cpp:163` | input-validation
    HTTPSERVER_STRESS_SECONDS is read with std::getenv and converted with std::stoi inside a catch-all, which correctly suppresses invalid-value exceptions. However, there is no upper-bound check: an extremely large value (e.g., INT_MAX) is accepted and passed directly to std::chrono::seconds, potentially producing a test that runs indefinitely in a CI environment. CWE-1284 (Improper Validation of Specified Quantity in Input).
    *Recommendation:* Add a reasonable upper bound, e.g., cap at 3600 seconds: `if (v > 0 && v <= 3600) return v;`
    *Status:* fixed in this batch — added `&& v <= 3600` to the guard in `stress_seconds()`.

28. [x] **security-reviewer** | `test/integ/threadsafety_stress.cpp:273` | sensitive-data
    HTTPSERVER_RUN_STOP_FROM_HANDLER is compared by constructing a std::string from the raw char* returned by getenv before any length check. For a pathologically long env var value this causes unnecessary heap allocation rather than a security issue, but the comparison to the string literal "1" is correct and the env var cannot inject code. Low-risk, informational only. CWE-20.
    *Recommendation:* Use `std::string_view(run) == "1"` to avoid heap allocation; the functional behaviour is unchanged.
    *Status:* fixed in this batch — same as item 21.

29. [x] **security-reviewer** | `test/integ/threadsafety_stress.cpp:291` | process-handling
    After fork(), the child process calls ws.start(false) and then curl_easy_perform(). If fork() returns a child but start() throws or the kernel port-assignment fails, the child path falls through to std::_Exit(43) which is correct. However, the parent does not validate that child > 0 before entering the wait loop — the LT_CHECK(child >= 0) assertion at line 292 fires a test failure but does not prevent the parent from entering the wait loop with child == -1 (fork failure), causing waitpid(-1, ..., WNOHANG) to reap an arbitrary child process. CWE-362 (race) / incorrect resource cleanup.
    *Recommendation:* After LT_CHECK(child >= 0), add an early return if child < 0: `if (child < 0) return;` — this mirrors standard fork-failure hygiene and prevents waitpid(-1, ...) from accidentally reaping unrelated processes.
    *Status:* fixed in this batch — added `if (child < 0) return;` immediately after `LT_CHECK(child >= 0)`.

30. [x] **spec-alignment-checker** | `specs/product_specs.md:33` | specification-gap
    PRD §2 NFR lists no explicit EARS-formatted concurrency requirement with a 'When/While/If' stem. The thread-safety contract is captured entirely in DR-008.md and referenced by TASK-032 as 'PRD §2 NFR (concurrency)', but there is no matching PRD-CON-REQ-* identifier in product_specs.md. The test exercises DR-008 faithfully, but the PRD itself has a gap: the concurrency contract is only in the architecture decision layer, not in a formal EARS requirement.
    *Recommendation:* Add a concurrency EARS requirement to PRD §2 (e.g. 'When a caller invokes any webserver public method from a handler thread except stop() or ~webserver(), the system shall complete the call without deadlock or data race') so the contract is traceable from PRD to DR to test.
    *Status:* fixed in this batch — added PRD-CON-REQ-001 and PRD-CON-REQ-002 to product_specs.md §2.

31. [x] **spec-alignment-checker** | `specs/product_specs.md:33` | specification-gap
    PRD §2 NFR contains no explicit EARS-formatted concurrency requirement with a When/While/If stem. The thread-safety contract is captured entirely in DR-008.md, but there is no matching PRD-CON-REQ-* identifier in product_specs.md. The test exercises DR-008 faithfully but the PRD itself has a traceability gap.
    *Recommendation:* Add a concurrency EARS requirement to PRD §2 (e.g. 'When a caller invokes any webserver public method from a handler thread except stop() or ~webserver(), the system shall complete the call without deadlock or data race') so the contract is traceable from PRD to DR to test.
    *Status:* fixed in this batch — same as item 30.

32. [x] **spec-alignment-checker** | `test/integ/threadsafety_stress.cpp:311` | action-item
    The stop-from-handler child process uses 'ws.stop()' inside the handler, which is correct for testing the DR-008 negative path. However, the curl timeout inside the child (CURLOPT_TIMEOUT_MS, 10000L) is longer than the parent's 5-second wait window. If the MHD abort is slow, the curl call on the child side will not have timed out before the parent SIGKILLs the child. This is cosmetic (parent SIGKILL is the fallback branch DR-008 documents), but curl's 10 s timeout is misleading since it can never elapse under the 5-second parent deadline.
    *Recommendation:* Set CURLOPT_TIMEOUT_MS on the child's curl handle to a value less than the parent's 5-second deadline (e.g. 3000L) so the timeout annotation is internally consistent.
    *Status:* fixed in this batch — changed CURLOPT_TIMEOUT_MS to 3000L with explanatory comment.

33. [x] **spec-alignment-checker** | `test/integ/threadsafety_stress.cpp:311` | action-item
    The stop-from-handler child's curl timeout (CURLOPT_TIMEOUT_MS 10000L) exceeds the parent's 5-second deadline. curl's 10s timeout can never elapse before the parent SIGKILLs the child, making the annotation internally inconsistent. Cosmetic only — parent SIGKILL is the documented fallback branch.
    *Recommendation:* Set CURLOPT_TIMEOUT_MS to a value less than the parent's 5-second deadline (e.g. 3000L) so the timeout annotation is internally consistent.
    *Status:* fixed in this batch — same as item 32.

34. [x] **spec-alignment-checker** | `test/integ/threadsafety_stress.cpp:92` | action-item
    The noop_resource helper class in the stress test still uses the v1 idiom: 'render_get' returns 'std::shared_ptr<ht::http_response>' wrapping a 'shared_ptr<http_response>(new http_response(http_response::string("ok")))'. Under the v2 contract (PRD-RSP-REQ-007, TASK-036), handler render methods are expected to return 'http_response' by value. While TASK-036 (dispatch cutover) is not yet done, this helper class is new code written under the v2 task set and should model the post-v2 style rather than the old shared_ptr return.
    *Recommendation:* Change noop_resource::render_get to return 'ht::http_response' by value using 'return ht::http_response::string("ok");' consistent with the v2 handler contract.
    *Status:* already addressed — noop_resource::render_get already returns `ht::http_response` by value (`return ht::http_response::string("ok");`).

35. [x] **spec-alignment-checker** | `test/integ/threadsafety_stress.cpp:92` | action-item
    The noop_resource helper class still uses the v1 shared_ptr return idiom for render_get. This is new code written under the v2 task set and should model the post-v2 value-type return style. Does not block TASK-032 acceptance since TASK-036 (dispatch cutover) is not yet done.
    *Recommendation:* Change noop_resource::render_get to return ht::http_response by value consistent with the v2 handler contract.
    *Status:* already addressed — same as item 34.

36. [x] **test-quality-reviewer** | `test/integ/threadsafety_stress.cpp:184` | missing-test
    The stress test exercises register_path, unregister_path, block_ip, and unblock_ip from handler threads, but does not exercise register_prefix / unregister_prefix, which share distinct lock paths through register_impl_(family=true) vs register_impl_(family=false). The task acceptance criterion mentions only register_path by name, so this is a coverage gap rather than a strict failure, but the DR-008 contract is about the full public mutating surface.
    *Recommendation:* Consider adding op cases 4 and 5 in driver_body to exercise register_prefix and unregister_prefix on prefix-style paths (e.g., '/pfx/' + slot), or at minimum document in the test comment that prefix registration is intentionally excluded.
    *Status:* fixed in this batch — added comment in driver_body explaining that register_prefix / unregister_prefix are intentionally not exercised because they share the same lock path as register_path / unregister_path.

37. [x] **test-quality-reviewer** | `test/integ/threadsafety_stress.cpp:227` | non-deterministic
    The per-client seed `static_cast<uint32_t>(c) * 0x9e3779b9u` is deterministic across runs — good. However, only 8 dynamic path slots (slot = i & 0x7) are used by 16 clients all sharing the same slot space, which means all 16 clients contend heavily on the same 8 paths. The contention is intentional for stress purposes, but the coverage of the register_path / unregister_path conflict-handling path is asymmetric: clients with even-numbered op values will nearly always find a slot already registered. The existing catch(const std::invalid_argument&) handles this correctly, but the comment at line 104 says 'Catches the documented std::invalid_argument on duplicate-registration races' while the unregister catch block (line 141) catches all exceptions (...) with no comment. Silently swallowing unexpected exception types from unregister_path could hide regressions.
    *Recommendation:* Narrow the unregister_path catch to a specific type or add a logging/counter increment on unexpected catch so that genuinely unexpected exceptions from unregister_path do not silently vanish. At minimum add a comment explaining why the broad catch is intentional.
    *Status:* fixed in this batch — same as items 1 and 12; replaced bare `catch (...)` with specific catches.

38. [x] **test-quality-reviewer** | `test/integ/threadsafety_stress.cpp:291` | aaa-violation
    In stop_from_handler_deadlocks_as_documented, LT_CHECK(child >= 0) is placed inside the parent path of the fork() branch but evaluates a value already set before the if (child == 0) split (line 293). If fork() returns -1, the code falls through to the parent polling loop with child == -1, then calls ::kill(-1, SIGKILL) which sends SIGKILL to every process in the process group — a potentially catastrophic side effect. The assertion fires after the damage.
    *Recommendation:* Assert child >= 0 immediately after the fork() call and before the if (child == 0) branch. If the assertion fails, the test should return or abort before executing any code that treats `child` as a valid PID.
    *Status:* fixed in this batch — same as item 29; added early return after LT_CHECK.
