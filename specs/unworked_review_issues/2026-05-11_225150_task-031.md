# Unworked Review Issues

**Run:** 2026-05-11 22:51:50
**Task:** TASK-031
**Total:** 40 (0 critical, 7 major, 33 minor)

## Major

1. [x] **code-simplifier** | `test/integ/basic.cpp:3402` | code-structure
   Every one of the 8 new DR-009 integration tests repeats the same ~14-line curl setup block verbatim: curl_global_init, curl_easy_init, setopt URL, setopt HTTPGET, setopt WRITEFUNCTION, setopt WRITEDATA, curl_easy_perform, assert res==0. The only variation is the URL string and the response variable `s`. This is textbook Needless Repetition (code smell #5) and violates the DRY principle, making each test harder to scan for its actual assertion.
   *Recommendation:* Extract a small helper — e.g. `static std::string http_get(const std::string& url, int* out_code = nullptr)` — that handles curl setup/teardown and returns the response body. Each test then becomes ~5 lines of setup + assertion. The existing pre-TASK-031 tests in the file already demonstrate this pattern could be applied uniformly. Example:

  static std::pair<std::string, int64_t> do_get(const std::string& url) {
      string body;
      CURL* curl = curl_easy_init();
      curl_easy_setopt(curl, CURLOPT_URL, url.c_str());
      curl_easy_setopt(curl, CURLOPT_HTTPGET, 1L);
      curl_easy_setopt(curl, CURLOPT_WRITEFUNCTION, writefunc);
      curl_easy_setopt(curl, CURLOPT_WRITEDATA, &body);
      curl_easy_perform(curl);
      int64_t code = 0;
      curl_easy_getinfo(curl, CURLINFO_RESPONSE_CODE, &code);
      curl_easy_cleanup(curl);
      return {body, code};
  }

Then each test collapses to:
  auto [s, http_code] = do_get(url);
  LT_ASSERT_EQ(http_code, 500);
  LT_CHECK_NEQ(s.find("boom"), std::string::npos);

   *Status:* Fixed — `task031::do_get()` helper added inside `namespace task031` in `test/integ/basic.cpp`; all 8 dr009_* tests refactored to use it.

2. [x] **code-simplifier** | `test/integ/basic.cpp:3402` | code-structure
   Each of the 8 new tests also repeats the same webserver lifecycle: create `webserver ws2{create_webserver(PORT + N)}`, `register_path`, `start(false)`, ... `ws2.stop()`. With 8 tests across PORT+80..+87, port assignments are scattered through the diff and the next author must search for the next free offset. The coupling between test identity and port number is an implicit dependency that invites collisions.
   *Recommendation:* Introduce a thin RAII wrapper or a factory helper that accepts the `create_webserver` builder, a resource, and a path, starts the server, and calls stop() in its destructor. Alternatively, document the port-offset allocation with a single comment block that lists all PORT+N assignments in one place. At minimum, define named constants (e.g. `constexpr int PORT_DR009_BASE = PORT + 80;`) so the range is explicit and conflict-free.

   *Status:* Partially fixed — added `PORT_DR009_BASE` constant and a comment block listing all PORT+N assignments in one place inside `namespace task031`. Full RAII wrapper deferred (low ROI given do_get already reduces per-test boilerplate significantly).

3. [x] **security-reviewer** | `src/webserver.cpp:1635` | information-disclosure
   When no internal_error_handler is configured, e.what() is written verbatim into the HTTP response body (CWE-209). Exception messages routinely contain internal file paths, database connection strings, service hostnames, stack metadata injected by allocators or boost::exception, or user-controlled data that triggered the exception. The comment 'informative for debugging' confirms this is intentional, but it makes the default unsafe for any deployment that has not explicitly wired a custom handler. A remote attacker who can trigger a predictable exception (type-mismatch, DB constraint violation, out-of-range, etc.) can harvest internal system information with no authentication.
   *Recommendation:* Change the default (no-handler) path to emit a fixed, opaque body such as the former 'Internal Error' constant. Move the e.what() surfacing to an opt-in: either (a) require operators to pass a handler to get the message, or (b) gate it on a compile-time or runtime debug flag. The existing DR-009 contract can be preserved by still passing e.what() to a configured handler — just do not echo it in the HTTP body by default. Example: return http_response::string(std::string{constants::GENERIC_ERROR}).with_status(http_utils::http_internal_server_error);
   *Fixed:* TASK-055 / DR-009 Revision 1 implemented exactly this. `src/detail/webserver_error_pages.cpp:95-101` now emits `constants::INTERNAL_SERVER_ERROR` as the default body (opaque) and only surfaces `e.what()` in the response body when the operator opts in via `create_webserver::expose_exception_messages(true)`. Verbatim text still reaches `log_dispatch_error` (operator-controlled). Finding pre-dates the revision.

4. [x] **security-reviewer** | `src/webserver.cpp:1936` | information-disclosure
   In get_raw_response_with_fallback, the catch(const std::exception& e) arm at line 1935 calls log_dispatch_error(std::string("materialize threw: ") + e.what()) and then run_internal_error_handler_safely(mr, e.what()). When no internal_error_handler is set, this routes into the same default path as finding #1 and surfaces the materializer's exception text in the response body. The body materializer can throw with messages that include internal file descriptor details or MHD-internal state strings — details that are not meaningful to clients but are valuable to attackers.
   *Recommendation:* Apply the same fix as finding #1: use a fixed opaque body for the default no-handler case. The log path may keep e.what() since it is operator-controlled.
   *Fixed:* Same fix as finding #3 — TASK-055 / DR-009 Revision 1 routes every default-path internal-error response through `webserver_error_pages.cpp` which now uses the opaque body. The materializer-exception path inherits the new behaviour without needing a separate change site.

5. [x] **test-quality-reviewer** | `test/integ/basic.cpp:2086` | redundant-test
   response_throws_invalid_argument / response_throws_runtime_error / response_throws_non_std_exception (lines 2086-2149) assert only on the HTTP status code (500) without checking body or log message. The new dr009_* suite (lines 3400-3677) covers the same code paths with more specific assertions (body content, message forwarding, log capture). These three tests now add maintenance burden — three separate webserver instances, three endpoint registrations, three curl loops — without catching any additional bug that the dr009_* tests would miss.
   *Recommendation:* Remove response_throws_invalid_argument, response_throws_runtime_error, and response_throws_non_std_exception; the dr009_* family already covers std::exception -> 500 and non-std -> 500 more precisely. If the distinct exception type (std::invalid_argument) matters for the dispatch path, add a targeted assertion inside the existing dr009_runtime_error_message_surfaces_in_default_body test or add a one-liner dr009_invalid_argument variant.

   *Status:* Fixed — all three tests and their resource classes (invalid_arg_resource, runtime_error_resource, non_std_exception_resource) removed; replaced with a consolidation comment referencing the dr009_* superseding tests.

6. [x] **test-quality-reviewer** | `test/integ/basic.cpp:2160` | redundant-test
   internal_error_handler_also_throws (lines 2160-2187) only asserts on HTTP status 500, which is a subset of what dr009_throwing_handler_yields_empty_body_500 (line 3575) and dr009_throwing_handler_logs_generically (line 3609) assert (empty body + generic log message). The older test adds an independent webserver instance on PORT+50 and a file-scope helper function (throwing_internal_error_handler) for no additional coverage.
   *Recommendation:* Delete internal_error_handler_also_throws and the file-scope throwing_internal_error_handler function; the two dr009_throwing_handler_* tests supersede it with stronger assertions.

   *Status:* Fixed — test and its file-scope throwing_internal_error_handler helper removed; included in the same consolidation comment as finding 5.

7. [x] **test-quality-reviewer** | `test/integ/basic.cpp:3183` | redundant-test
   builder_custom_internal_error_handler (line 3183) tests that a custom handler's return value is used for the body — identical observable behaviour to dr009_runtime_error_message_passed_to_handler (line 3430), which verifies the same property AND additionally verifies the message forwarding. PORT+76 and PORT+81 are both consumed for functionally equivalent coverage.
   *Recommendation:* Delete builder_custom_internal_error_handler and its file-scope custom_internal_error_handler/throwing_resource helpers; dr009_runtime_error_message_passed_to_handler provides a strict superset of its assertions.

   *Status:* Fixed — test, custom_internal_error_handler function, and throwing_resource class removed; replaced with consolidation comment. Port 76 is now free.

## Minor

8. [x] **architecture-alignment-checker** | `src/webserver.cpp:2186` | pattern-violation
   The belt-and-suspenders fallback at the end of finalize_answer (after get_raw_response_with_fallback returns null) calls internal_error_page(..., force_our=true) directly without first calling log_dispatch_error. DR-009 §5.2 point 4 says 'library logs' before sending the hardcoded 500. The two try/catch sites inside get_raw_response_with_fallback do call log_dispatch_error, but the outer post-call check at line 2186 silently forces the empty-body 500 without any logging.
   *Recommendation:* Add a log_dispatch_error call immediately before the force_our=true call at line 2186 so operators can diagnose why even the fallback materialization path returned null, matching the 'library logs' contract in §5.2 point 4.

   *Status:* Fixed — `log_dispatch_error(...)` call added before `internal_error_page(mr, "", /*force_our=*/true)` in `materialize_and_queue_response` in `src/detail/webserver_request.cpp`.

9. [x] **code-quality-reviewer** | `src/httpserver/create_webserver.hpp:56` | code-readability
   The typedef comment for internal_error_handler_t says 'callers MUST NOT capture it past the handler's return (it points into either a caught std::exception's internal storage or a fixed string literal)'. This lifetime warning is correct and important, but it is only on the typedef — it is duplicated (with slightly different wording) in the Doxygen for the setter. Having two canonical sources of truth for the same lifetime contract is a minor maintenance hazard.
   *Recommendation:* Keep the warning in one place (the typedef or the setter Doxygen, not both) and use @note or @warning in the other to cross-reference. The typedef comment is the right canonical location since it travels with the type; the setter Doxygen can say 'see internal_error_handler_t for lifetime constraints on message'.

   *Status:* Already addressed — the current code has the lifetime constraint only in the typedef Doxygen (`The view is valid only for the duration of the call; copy if you need to retain it.`); the setter Doxygen does not duplicate it. No action needed.

10. [x] **code-quality-reviewer** | `src/httpserver/detail/webserver_impl.hpp:313` | readability
   The comment on `internal_error_page` mentions "the few non-handler-throw call sites that synthesise a 500 with no exception in flight" but does not enumerate them, making it harder for future maintainers to audit all call sites. The `get_raw_response_with_fallback` path and the belt-and-suspenders path in `finalize_answer` are both silent callers.
   *Recommendation:* Add a brief enumeration or a cross-reference tag, e.g.: "Called from: run_internal_error_handler_safely (double-fault), get_raw_response_with_fallback (null materializer), and the final belt-and-suspenders site in finalize_answer."

   *Status:* Fixed — "Call sites:" block added to the `internal_error_page` declaration comment in `src/httpserver/detail/webserver_impl_dispatch.hpp` enumerating all four callers.

11. [x] **code-quality-reviewer** | `src/webserver.cpp:1928` | code-elegance
   In `get_raw_response_with_fallback`, the catch(const std::invalid_argument&) arm (pre-existing, not introduced by this task) routes to not_found_page without passing through run_internal_error_handler_safely or logging. This arm predates TASK-031 and falls outside the scope of this task, but it is a latent inconsistency: a std::invalid_argument thrown by materialize_response is not reported to the error logger and does not invoke the internal_error_handler.
   *Recommendation:* File as a follow-up: consider merging the std::invalid_argument arm into the std::exception arm in a future task so all materialize exceptions are logged and routed consistently.

   *Status:* Deferred — explicitly noted as predating TASK-031 and out of scope. Tracked for future follow-up task.

12. [x] **code-quality-reviewer** | `src/webserver.cpp:2134` | code-elegance
   The null/invalid-response branch inside the `found` try block calls `run_internal_error_handler_safely` with the hardcoded message `"handler returned null response"`, but this is a handler misbehaviour rather than an exception thrown by the handler. The contract documented in webserver.hpp and the task spec covers only exception-thrown paths; routing a silent null return through `internal_error_handler` slightly overloads that contract. The error message is also an implementation detail and will reach the user-supplied handler, potentially leaking internal diagnostics.
   *Recommendation:* Consider routing the null-response case directly to `internal_error_page(mr, "", /*force_our=*/true)` (the hardcoded empty-body 500) instead of calling `run_internal_error_handler_safely`. Alternatively, document explicitly in the Doxygen that `internal_error_handler` is also invoked for null responses, so the contract is complete.

   *Status:* Deferred — this is a contract design question (should null response route through the user handler?). The current behavior provides more useful diagnostics. Requires a DR update if changed. File for M6.

13. [x] **code-quality-reviewer** | `src/webserver.cpp:2164` | readability
   The comment on the `catch (const std::exception& e)` arm at line 2164 says "run_internal_error_handler_safely contains a possible re-throw from the user handler (point 4)." This is inaccurate: `run_internal_error_handler_safely` does NOT re-throw; it catches any throw from the user handler internally and returns an empty-body 500. The comment misleads readers into thinking an exception can propagate out of it.
   *Recommendation:* Replace the comment with: "run_internal_error_handler_safely absorbs any throw from the user handler (§5.2 point 4) and never propagates."

   *Status:* Already addressed — in the refactored code (`src/detail/webserver_dispatch.cpp`), the catch arm comment accurately describes `handle_dispatch_exception` behavior with no misleading re-throw language. No action needed.

14. [x] **code-quality-reviewer** | `test/integ/basic.cpp:3183` | test-coverage
   The pre-existing `builder_custom_internal_error_handler` test (line 3183) registers the resource with path `"throw"` using the bare string rather than `"/throw"` (the macro's port offset is also different from the new DR-009 tests). The test verifies the custom handler body text but does NOT verify that `e.what()` was passed as the `message` argument; the handler signature ignores the second argument. This is not a new regression, but it leaves a gap since the DR-009 tests added in this task do cover the argument-passing path correctly elsewhere.
   *Recommendation:* Update the `builder_custom_internal_error_handler` test to capture and assert the `std::string_view message` argument value, confirming it equals the exception message. This closes the gap left by the test predating the two-arg signature.

   *Status:* Superseded — `builder_custom_internal_error_handler` was removed (finding 7); the message-argument coverage is in `dr009_runtime_error_message_passed_to_handler`.

15. [x] **code-quality-reviewer** | `test/integ/basic.cpp:3346` | test-coverage
   All DR-009 integration tests exercise the class-based dispatch path (http_resource subclasses). The lambda-route path (on_get/route) goes through the same try/catch in finalize_answer, but there is no test that verifies a throwing lambda handler is caught and propagated correctly. If a future refactor splits the dispatch paths the regression would be invisible.
   *Recommendation:* Add one integration test using ws.on_get("/boom_lambda", [](const http_request&){ throw std::runtime_error("lambda boom"); return ...; }) to confirm the same 500 + message behaviour for lambda-registered handlers.

   *Status:* Deferred — valid coverage gap. Adding a new integration test with a dedicated webserver port is out of scope for this cleanup pass. File as a follow-up test task.

16. [x] **code-quality-reviewer** | `test/integ/basic.cpp:3346` | test-coverage
   The double-fault path (internal_error_handler throws) is tested for the empty-body 500 status (dr009_throwing_handler_yields_empty_body_500) and for the generic log entry (dr009_throwing_handler_logs_generically), but there is no test that verifies the double-fault body is strictly empty when log_error is not configured (i.e. the null-logger branch of log_dispatch_error is exercised silently). This leaves the null-logger branch of log_dispatch_error untested.
   *Recommendation:* Add a test variant of dr009_throwing_handler_yields_empty_body_500 that omits .log_error() from the create_webserver builder and still asserts the body is empty and status is 500, confirming the null-logger branch does not interfere.

   *Status:* Already addressed — `dr009_throwing_handler_yields_empty_body_500` uses `create_webserver(PORT + 85).internal_error_handler(handler)` with no `.log_error()`. It asserts body is empty and status is 500. The null-logger branch is exercised by this test.

17. [x] **code-quality-reviewer** | `test/integ/basic.cpp:3609` | test-coverage
   The test `dr009_throwing_handler_logs_generically` (AC3.b) asserts that the log buffer contains `"internal_error_handler threw"` but does NOT assert the HTTP response status code or body, unlike all other DR-009 tests. While the status is implicitly covered by `dr009_throwing_handler_yields_empty_body_500`, having the status check here too would make the test self-contained and avoid silent regressions if the tests are run in isolation.
   *Recommendation:* Add `LT_ASSERT_EQ(http_code, 500)` and `LT_CHECK_EQ(s, "")` to `dr009_throwing_handler_logs_generically` after the log-buffer assertion.

   *Status:* Fixed — `LT_ASSERT_EQ(http_code, 500)` and `LT_CHECK_EQ(s, "")` added to `dr009_throwing_handler_logs_generically` in `test/integ/basic.cpp`.

18. [x] **code-simplifier** | `src/httpserver/create_webserver.hpp:55` | naming
   The new type alias `internal_error_handler_t` uses a `_t` suffix that is inconsistent with the existing `error_handler`, `validator_ptr`, `log_access_ptr`, and `log_error_ptr` aliases in the same header — none of which carry a `_t` suffix. The inconsistency is minor but violates the project's established naming convention.
   *Recommendation:* Rename to `internal_error_handler` to match the pattern of `error_handler`. If a disambiguation alias is needed in test/unit code, define it there locally (as the test already does with `internal_error_handler_fn`). This keeps the public API consistent.

   *Status:* Deferred — renaming `internal_error_handler_t` is a public API breaking change. The TASK-049 alias mechanism already wired it into the hook bus. Renaming requires updating all downstream callers. File as M6 API cleanup with a deprecation cycle.

19. [x] **code-simplifier** | `src/httpserver/detail/webserver_impl.hpp:306` | code-structure
   The TASK-031 block comment above `internal_error_page` in the header (lines 306-327) is longer than the Doxygen comment block on the same function in create_webserver.hpp. The header comment documents implementation behavior in prose that duplicates the .hpp Doxygen and the in-line source comments. Three sources of truth for the same contract increases maintenance burden.
   *Recommendation:* Condense the header comment to a single brief `// See DR-009 / §5.2 for the dispatch contract.` line referencing the authoritative Doxygen in create_webserver.hpp. Keep implementation notes in webserver.cpp source comments only.

   *Status:* wontfix — cosmetic/style preference, no functional impact; low-priority comment condensation; comments serve different audiences

20. [x] **code-simplifier** | `src/httpserver/detail/webserver_impl.hpp:329` | naming
   The `force_our` boolean parameter of `internal_error_page` is a flag argument that selects between two fundamentally different code paths (invoke user handler vs. return hardcoded 500). The function does two things depending on this flag, which is a code smell per the Functions rules (no flag arguments; split into independent methods).
   *Recommendation:* Consider splitting into two named helpers: `internal_error_page_default(modded_request*, std::string_view)` for the user-handler path and `internal_error_page_hardcoded_500()` for the double-fault fallback. The call sites in `run_internal_error_handler_safely` and the belt-and-suspenders site in `finalize_answer` would then call the appropriate helper directly, making the intent unambiguous without reading the boolean. Note: this is a low-priority rename; the current `force_our` name and the named-argument comment style (`/*force_our=*/true`) already partially mitigate the opacity.

   *Status:* wontfix — cosmetic/style preference, no functional impact; low-priority per review; named-argument comment already mitigates opacity

21. [x] **code-simplifier** | `src/webserver.cpp:1636` | code-structure
   `std::string{msg}` in `internal_error_page` creates a temporary std::string from the string_view just to pass it to `http_response::string()`. If `http_response::string()` has (or could have) a string_view overload, this conversion is unnecessary. Even without that overload, the conversion is implicit and the explicit brace-construction obscures intent.
   *Recommendation:* Use `std::string(msg)` (parentheses) for clarity, or check whether `http_response::string()` accepts `std::string_view` directly (in which case pass `msg` unmodified). The brace form `std::string{msg}` is unusual for a single-argument string construction and may surprise readers.

   *Status:* wontfix — cosmetic/style preference, no functional impact; low-priority style preference; consistent with existing pattern

22. [x] **code-simplifier** | `src/webserver.cpp:1935` | code-structure
   The `catch(std::exception)` and `catch(...)` arms of `get_raw_response_with_fallback` share identical structure (call `run_internal_error_handler_safely`, call `materialize_response`, return nullptr on inner throw) and differ only in the log message and the string passed as `msg`. A small lambda would eliminate the three-line duplication and make the pattern self-documenting.
   *Recommendation:* Extract a local lambda: `auto recover = [&](std::string_view msg) -> MHD_Response* { try { mr->dhrs = run_internal_error_handler_safely(mr, msg); return materialize_response(mr->dhrs.get()); } catch (...) { return nullptr; } };`. Then the two catch arms become single-line calls: `catch (const std::exception& e) { log_dispatch_error(...); return recover(e.what()); }` and `catch (...) { log_dispatch_error(...); return recover("unknown exception"); }`. Behavior is identical; the structural symmetry is made explicit.

   *Status:* Deferred — current code is already readable and the two arms are intentionally distinct per DR-009 contract (two-branch catch is load-bearing). Lambda would obscure the catch semantics for marginal DRY gain.

23. [x] **code-simplifier** | `src/webserver.cpp:1943` | code-structure
   In `get_raw_response_with_fallback`, the `catch(...)` branch at line 1943 repeats the same structure as the `catch(const std::exception& e)` branch at line 1935 — log, call `run_internal_error_handler_safely`, materialize, return nullptr on inner throw. The only difference is the log message and the absence of `e.what()`. This is a minor duplication that could be reduced, but the two-branch catch pattern is mandated by the DR-009 contract so the duplication is partially load-bearing.
   *Recommendation:* Extract the shared tail (call safely, materialize, catch inner) into a lambda or inline helper `auto recover = [&](std::string_view msg) -> struct MHD_Response* { ... };` and call it from both catch arms. This keeps the two-branch structure explicit while eliminating the duplicated body.

   *Status:* Deferred — same as finding 22; the two arms are load-bearing per DR-009. Minor duplication is acceptable.

24. [x] **housekeeper** | `specs/tasks/M5-routing-lifecycle/TASK-031.md:32` | task-not-marked-complete
   Task status is 'In Progress' in both TASK-031.md and _index.md. All acceptance criteria are met by the implementation (all 3 integration test suites in test/integ/basic.cpp cover AC1–AC3; type-check static_asserts in test/unit/create_webserver_explicit_test.cpp cover the signature contract). The task can be advanced to 'Done' once action-item checkboxes are ticked.
   *Recommendation:* After marking all action items complete, update Status to 'Done' in TASK-031.md and update the _index.md row for TASK-031 from 'In Progress' to 'Done'.

   *Status:* Already done — TASK-031.md shows `**Status:** Done` and `_index.md` shows `| TASK-031 | ... | Done |`. No action needed.

25. [x] **performance-reviewer** | `src/webserver.cpp:1635` | memory-allocation
   In the no-handler-configured branch of internal_error_page, std::string{msg} is constructed from the std::string_view to feed into http_response::string(). This is on the error path and unavoidable given the current http_response::string() signature, but it is worth noting as a one-copy cost per error response.
   *Recommendation:* If http_response::string() were overloaded to accept std::string_view directly, this copy could be eliminated. Low priority since this only fires when no internal_error_handler is configured.

   *Status:* Accepted as-is — error path, unavoidable with current API. Low priority per recommendation.

26. [x] **performance-reviewer** | `src/webserver.cpp:1648` | memory-allocation
   log_dispatch_error constructs a std::string from the std::string_view argument on every call (line: parent->log_error(std::string(msg))). This allocation always happens when a logger is configured and an error occurs. Since log_error is typed as std::function<void(const std::string&)>, the copy is unavoidable with the current signature. The allocation is on the error path and not on the happy path, so the impact is limited.
   *Recommendation:* Accept as-is, or consider changing log_error_ptr to accept std::string_view or a const std::string& to allow the caller to pass a pre-existing std::string without an extra copy. Since this is a public API type (log_error_ptr), the change would be a breaking API change and should be deferred.

   *Status:* Accepted as-is — error path only. Breaking API change deferred to M6.

27. [x] **performance-reviewer** | `src/webserver.cpp:1654` | missing-caching
   run_internal_error_handler_safely wraps a call to internal_error_page inside a try/catch, which itself calls into internal_error_page again (force_our=true) in the double-fault arm. This is a nested try/catch (one in finalize_answer, one in run_internal_error_handler_safely, plus one in get_raw_response_with_fallback). On most ABIs, nested try/catch blocks have zero cost unless an exception is thrown (LSDA table traversal), but they do slightly increase binary size and the unwind-table complexity. This is correct and acceptable; it is noted for awareness.
   *Recommendation:* No action needed. The nesting is deliberate and correct per DR-009 §5.2 point 4. The zero-cost exception model ensures no runtime overhead on the happy path.

   *Status:* No action needed per recommendation.

28. [x] **performance-reviewer** | `src/webserver.cpp:1916` | memory-allocation
   get_raw_response_with_fallback now has a catch(const std::exception& e) arm (line ~1935) that constructs a std::string via operator+ to pass to log_dispatch_error. This is a second site (beyond finalize_answer) that does a heap allocation on the materialize exception path. Both sites are error paths only, so there is no happy-path regression.
   *Recommendation:* Same as finding 1: acceptable on the error path. No action required.

   *Status:* Accepted as-is per recommendation.

29. [x] **performance-reviewer** | `src/webserver.cpp:2169` | memory-allocation
   On the std::exception catch arm, a temporary std::string is constructed via operator+ to concatenate the prefix 'dispatch: handler threw std::exception: ' with e.what() before passing to log_dispatch_error. This allocation is on the error path only, so it is not a hot-path regression. However, log_dispatch_error already accepts std::string_view; if a small_buffer or fmt-style helper were used, the heap allocation could be avoided on the error path entirely.
   *Recommendation:* Consider accepting the current approach as acceptable since it is purely on the exception path. If error-path allocation must be zero, use a fixed-size stack buffer with snprintf or pass the two pieces separately to an overload of log_dispatch_error that takes a prefix + suffix.

   *Status:* Accepted as-is — error path only, low priority.

30. [x] **security-reviewer** | `src/httpserver/create_webserver.hpp:58` | unsafe-api-design
   internal_error_handler_t takes std::string_view message, whose backing storage is either e.what() (alive only while the catch block is executing) or a string literal. The typedef comment warns 'callers MUST NOT capture it past the handler's return', but the type system does not enforce this. A user who captures the string_view in an std::function that stores it in a member, posts it to an async executor, or passes it to a coroutine will get a dangling pointer that is not flagged at compile time (CWE-416). The risk is latent: it will not manifest in synchronous handlers but is a trap for async users of the API.
   *Recommendation:* Change the argument type to const std::string& (or std::string by value). The extra heap allocation per error event is negligible compared to the I/O cost of sending the HTTP response, and it eliminates the entire class of lifetime errors. If binary compatibility or performance is a strict constraint, at minimum add a static_assert or [[deprecated]] note pointing to the lifetime restriction, and provide a helper that copies the view before the handler is called.

   *Status:* Deferred — changing to `const std::string&` is a public API change. The current typedef comment clearly warns users. The copy vs view tradeoff is also related to finding 3 (default body surfacing e.what()). File as M6 API cleanup.

31. [x] **security-reviewer** | `src/httpserver/create_webserver.hpp:58` | data-integrity
   The internal_error_handler_t callback receives the exception message as std::string_view (non-owning). The comment warns callers not to capture it past the handler's return, but there is no API-level enforcement. A user who stores the view in a lambda capture or a member variable will have a dangling view after the catch block unwinds (CWE-825). The risk is in user code, not in the library itself, but the interface design invites the mistake.
   *Recommendation:* Consider changing the internal_error_handler_t signature to take std::string (by value) instead of std::string_view, accepting the one extra copy per error dispatch in exchange for eliminating the dangling-view trap. If the string_view form is kept for performance, add a [[nodiscard]] note or a static_assert pattern that prevents storage; at minimum expand the Doxygen comment to explicitly call out the dangling-view risk with a concrete bad-usage example.

   *Status:* Deferred — same as finding 30. Grouped for M6 API cleanup.

32. [x] **security-reviewer** | `src/webserver.cpp:1635` | input-validation
   e.what() is placed into the HTTP response body without sanitization of control characters (NUL bytes, carriage returns, line feeds, non-printable bytes). While the response Content-Type is text/plain (which prevents XSS in modern browsers), null bytes in the body can confuse HTTP/1.1 intermediaries that count on NUL-termination semantics, and CRLF sequences could cause response-splitting in HTTP/1.0 or non-conformant proxies (CWE-113). The existing validate_header_field guard in http_response.cpp correctly rejects CRLF in headers but does not apply to body content.
   *Recommendation:* Before inserting exception text into the response body, strip or replace non-printable and control characters. A minimal sanitizer: replace any byte with value < 0x20 (except 0x09, 0x0A, 0x0D which are normal whitespace in text/plain) and byte 0x00 with a replacement character such as '?' or a Unicode replacement marker. Alternatively, apply the same fix recommended in finding #1 (opaque default body) which avoids the problem entirely.

   *Status:* Deferred — moot if finding 3 (opaque default body) is implemented. If finding 3 stays deferred, this needs its own fix. File together with finding 3 for M6.

33. [x] **security-reviewer** | `src/webserver.cpp:2169` | logging
   log_dispatch_error concatenates e.what() directly into the log string without sanitizing embedded newlines or control characters (CWE-117). An attacker who can influence exception message content — for instance via crafted request data that flows into a thrown std::exception message — could inject log lines when the operator's log_error callback writes to a structured log sink or a log file that a SIEM parses by newlines.
   *Recommendation:* This is advisory: the log_error callback is operator-supplied and the log format is operator-defined. No fix is required in the library, but the Doxygen comment on log_error_ptr in create_webserver.hpp should note that callers should sanitize or quote the msg argument if the sink is newline-delimited. A stricter option is to strip \r and \n in log_dispatch_error before forwarding to the callback.

   *Status:* Deferred — the recommendation says no fix required in library. A Doxygen note on log_error_ptr could be added but is low priority. File for M6 documentation pass.

34. [x] **spec-alignment-checker** | `src/httpserver/create_webserver.hpp:58` | specification-gap
   The typedef for internal_error_handler_t documents that the std::string_view message parameter 'must NOT be captured past the handler's return'. This lifetime constraint is noted inline but is not formally captured in the EARS requirements or the task's acceptance criteria. The note is adequate for current purposes but could be missed by consumers reading only the task definition.
   *Recommendation:* In a future documentation pass (M6 README), add an explicit lifetime note to the public API docs for internal_error_handler_t.

   *Status:* Deferred — M6 documentation pass as recommended.

35. [x] **spec-alignment-checker** | `test/integ/basic.cpp:3402` | acceptance-criteria
   Acceptance criterion 'A handler that throws std::runtime_error("boom") produces a 500 response whose body / log message contains "boom" when default handler is used OR whatever internal_error_handler produced' is split across three tests (dr009_runtime_error_message_surfaces_in_default_body, dr009_runtime_error_message_passed_to_handler, dr009_runtime_error_logged_via_error_logger). All three sub-cases are well covered; no gap exists. Minor note: the task AC states 'body / log message contains "boom" when default handler is used' as a single criterion — the implementation correctly covers both the body case and the logger case, which is strictly more than required.
   *Recommendation:* No action needed; coverage exceeds the stated criterion.

   *Status:* No action needed per recommendation.

36. [x] **test-quality-reviewer** | `test/integ/basic.cpp:1452` | redundant-test
   exception_forces_500 (line 1452) uses exception_resource which throws std::domain_error("invalid") and asserts the body contains "invalid" AND the status is 500. This is a direct subset of dr009_runtime_error_message_surfaces_in_default_body (line 3402). The only difference is the exception type (domain_error vs runtime_error), which exercises the same catch branch and the same internal_error_page default path.
   *Recommendation:* The test has historical value as a regression anchor (domain_error is a std::exception subclass distinct from runtime_error). Keep it, but add a brief comment noting it is intentionally distinct from dr009_runtime_error_message_surfaces_in_default_body to prevent future reviewers from removing it as a duplicate.

   *Status:* Fixed — explanatory comment added before `exception_forces_500` in `test/integ/basic.cpp`.

37. [x] **test-quality-reviewer** | `test/integ/basic.cpp:1477` | redundant-test
   untyped_error_forces_500 (line 1477) uses error_resource which throws a char* literal and checks the body contains "unknown exception" and status is 500. dr009_non_std_exception_yields_unknown_exception_in_default_body (line 3509) covers the same observable contract with throw 42 (an int). Both exercise the `catch(...)` branch; the different non-std type (char* vs int) does not distinguish any distinct code path.
   *Recommendation:* Keep untyped_error_forces_500 as a type-diversity anchor (char* literal vs int) but add a comment clarifying it is deliberately complementary to the dr009 variant, not a duplicate to delete.

   *Status:* Fixed — explanatory comment added before `untyped_error_forces_500` in `test/integ/basic.cpp`.

38. [x] **test-quality-reviewer** | `test/integ/basic.cpp:3471` | missing-test
   dr009_runtime_error_logged_via_error_logger (line 3471) does not assert that the log message contains the context prefix ("dispatch: handler threw"). The test only checks that "boom" appears somewhere in the log buffer. A regression where the log message is "boom" with no context would still pass.
   *Recommendation:* Also assert that the log buffer contains the dispatch-context prefix (e.g., "dispatch: handler threw") so the test pins both the message content and the logging context.

   *Status:* Fixed — `LT_CHECK_NEQ(log_buf.find("dispatch: handler threw"), std::string::npos)` added to `dr009_runtime_error_logged_via_error_logger` in `test/integ/basic.cpp`.

39. [x] **test-quality-reviewer** | `test/integ/basic.cpp:3609` | missing-test
   dr009_throwing_handler_logs_generically (line 3609) does not assert the HTTP status code or body of the response. The log capture is tested, but a regression where the fallback sends 200 or some non-empty body would go undetected. This leaves the empty-body guarantee partially covered: dr009_throwing_handler_yields_empty_body_500 (PORT+85) covers the body/status but not the log; this test (PORT+86) covers the log but not the body/status.
   *Recommendation:* Add LT_ASSERT_EQ(http_code, 500) and LT_CHECK_EQ(s, "") to dr009_throwing_handler_logs_generically so a single test asserts all three observables of the double-fault path, or merge both tests into one.

   *Status:* Fixed — same as finding 17; `LT_ASSERT_EQ(http_code, 500)` and `LT_CHECK_EQ(s, "")` added to `dr009_throwing_handler_logs_generically`. (Findings 17 and 39 both target the same test.)

40. [x] **test-quality-reviewer** | `test/unit/create_webserver_test.cpp:43` | unnecessary-test
   All 40+ tests in create_webserver_test.cpp end with LT_CHECK_EQ(true, true). They verify that the builder chain compiles and does not crash, but they assert nothing about the resulting create_webserver state (no field accessors are called). Tests that cannot possibly fail regardless of implementation correctness provide zero protection against regressions.
   *Recommendation:* For builder methods that store a value (port, max_threads, max_connections, etc.), add a getter call and assert the stored value equals what was passed. At minimum the port constructor test should read back the configured port. If create_webserver intentionally exposes no accessors, note that explicitly in a comment so future developers don't add "check" tests that can never fail.

   *Status:* Already addressed — `LT_CHECK_EQ(true, true)` was replaced with `LT_CHECK_NOTHROW(...)` as part of TASK-038 review cleanup. The file now uses `LT_CHECK_NOTHROW` for compilation/no-crash checks. No further action needed.
