# Unworked Review Issues

**Run:** 2026-06-08 11:20:46
**Task:** TASK-072
**Total:** 35 (0 critical, 3 major, 32 minor)

## Major

1. [x] **code-quality-reviewer** | `src/detail/http_request_impl.cpp:188` | correctness
   The boundary check in http_unescape_raw uses `size > rpos + 2` which is vulnerable to integer overflow when rpos is near SIZE_MAX. Although in practice rpos can never be that large (it is bounded by the arena buffer size), the idiom `rpos + 2 < size` is semantically identical and cannot overflow because rpos is always strictly less than size at the top of the loop.
   *Recommendation:* Replace `if (size > rpos + 2)` with `if (rpos + 2 < size)` — the overflow-safe form. Alternatively use `if (size - rpos > 2)` which is equally safe and more idiomatic for unsigned arithmetic. The latter matches the convention already used in the source function in http_utils.cpp (if it uses subtraction-based bounds).
   *Worked 2026-07-07 (chore/unworked-major-issues):* Bound check rewritten as the overflow-safe `size - rpos > 2` form in src/httpserver/detail/unescape_helpers.hpp (the logic had moved there from http_request_impl.cpp) with an explanatory comment.

2. [x] **test-quality-reviewer** | `test/unit/http_request_unescape_arena_test.cpp:251` | missing-test
   The '+' to space conversion is a behaviour branch in http_unescape_raw (case '+': data[wpos++] = ' ') that is not exercised by any test in this file. The existing correctness tests cover %2F decode and invalid-hex passthrough, but the '+'-decode path is a distinct production branch that has equal regression risk. A crafted value like 'a+b' should produce 'a b' under the default unescaper. Missing this case means that if http_unescape_raw's '+' branch were accidentally deleted, all new tests would still pass.
   *Recommendation:* Add a test case `unescape_plus_decodes_to_space` that inserts a value of 'a+b' and asserts the stored string_view equals 'a b'. Follow the same AAA shape as `unescape_pct2f_decodes_to_slash` (test 3).
   *Worked 2026-07-07 (chore/unworked-major-issues):* Added unescape_plus_decodes_to_space ('a+b' -> 'a b').

3. [x] **test-quality-reviewer** | `test/unit/http_request_unescape_arena_test.cpp:309` | missing-test
   The '%' followed by only one hex digit (e.g. '%2' at end-of-string) is a distinct boundary from '%%' and trailing '%'. The http_unescape_raw implementation checks `size > rpos + 2` before decoding; a value like 'abc%2' (one digit after '%', no second digit) exercises the fall-through differently from 'abc%' (zero digits). Given that both TASK-072 and the original http_unescape had subtle off-by-one risk in this boundary, the missing case is a regression gap.
   *Recommendation:* Add a test `unescape_single_hex_digit_after_percent_passthrough` with value 'abc%2' and assert the stored string equals 'abc%2'. This is the same shape as `unescape_trailing_percent_passthrough` but targets the rpos+2 boundary check rather than end-of-string.
   *Worked 2026-07-07 (chore/unworked-major-issues):* Added unescape_single_hex_digit_after_percent_passthrough ('abc%2' -> 'abc%2').
## Minor

4. [x] **architecture-alignment-checker** | `specs/architecture/04-components/http-request.md:22` | pattern-violation
   The architecture doc note about the per-thread scratch buffer (thread_local std::string user_scratch) is accurate and correctly placed, but the threading model section (05-cross-cutting.md §5.1) does not mention thread_local per-worker state as a permitted intra-library pattern. This is not prohibited, but leaves a gap: if a future contributor adds a thread_local with connection-level semantics (rather than request-level semantics), there is no guidance to distinguish the two patterns.
   *Recommendation:* Consider adding a brief note in §5.1 or §5.3 acknowledging thread_local scratch buffers as an approved amortisation pattern for ABI-locked callbacks, with the constraint that they must hold stateless or capacity-only state (no connection-scoped data).
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Added a paragraph to specs/architecture/05-cross-cutting.md §5.1 documenting thread_local scratch buffers (e.g. unescape_in_arena's thread_value) as an approved amortisation pattern for ABI-locked callbacks, constrained to stateless/capacity-only state with no connection-scoped data.

5. [x] **architecture-alignment-checker** | `specs/architecture/05-cross-cutting.md:70` | interface-contract
   The §5.5 header layout tree lists individual detail/ files but does not include unescape_helpers.hpp (added by TASK-072). The doc currently ends the detail/ subtree at modded_request.hpp. Since the doc explicitly enumerates each detail header, a new one should be added to keep the layout description accurate and to make the 'not installed' boundary visible to reviewers.
   *Recommendation:* Add `unescape_helpers.hpp` to the detail/ block in the §5.5 header layout listing in specs/architecture/05-cross-cutting.md, e.g.: `├── unescape_helpers.hpp  # NEW — shared percent-decode helpers (TASK-072)`.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Added unescape_helpers.hpp to the detail/ header layout tree in specs/architecture/05-cross-cutting.md §5.5.

6. [x] **architecture-alignment-checker** | `src/Makefile.am:32` | interface-contract
   unescape_helpers.hpp is not listed in noinst_HEADERS in src/Makefile.am. The §5.5 architecture doc states 'Makefile.am continues to install httpserver/*.hpp and exclude httpserver/detail/', and all other detail/ headers are enumerated in noinst_HEADERS. Omitting it means the file is neither tracked in the install manifest nor explicitly marked as non-installed, which creates an implicit gap vs. the documented header accounting.
   *Recommendation:* Add `httpserver/detail/unescape_helpers.hpp` to the noinst_HEADERS list in src/Makefile.am alongside the other detail/ headers.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Already resolved in the current tree — src/Makefile.am's noinst_HEADERS already lists `httpserver/detail/unescape_helpers.hpp` alongside the other detail/ headers, so the file is properly tracked as a non-installed header.

7. [x] **architecture-alignment-checker** | `src/detail/http_request_impl.cpp:165` | pattern-violation
   hex_digit_value is duplicated from src/http_utils.cpp (same logic, same name in a different anonymous namespace). The comment explains the rationale (tiny, constexpr-foldable, no header exposure justified), but the architecture doc does not speak to intra-module code duplication policy for trivial helpers and a future reader may not notice this is intentional.
   *Recommendation:* The current approach is acceptable given the comment rationale. Consider promoting to a shared internal-only inline header (e.g. src/detail/http_utils_internal.hpp) if a third copy ever appears, but no action required now.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Already resolved in the current tree — hex_digit_value is no longer duplicated: src/http_utils.cpp's own comment states it was promoted (TASK-072/code-simplifier-iter1-1/2) to the single shared httpserver::detail::hex_digit_value in src/httpserver/detail/unescape_helpers.hpp, and neither src/detail/http_request_impl.cpp nor its successor src/detail/http_request_impl_args.cpp defines a local copy.

8. [x] **architecture-alignment-checker** | `src/httpserver/detail/unescape_helpers.hpp:1` | pattern-violation
   The new detail/ header is missing the `#if !defined(HTTPSERVER_COMPILATION)` guard that §5.5 documents as the access-control convention for all detail/ headers: 'detail/ headers gate on HTTPSERVER_COMPILATION only (consumers cannot reach in)'. Every existing peer (http_request_impl.hpp, webserver_impl.hpp) enforces this guard and raises a hard error when included outside a library build. unescape_helpers.hpp has only a #pragma once.
   *Recommendation:* Add the standard guard block at the top of the file, matching the pattern in src/httpserver/detail/http_request_impl.hpp lines 33-35:
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Added the standard `#if !defined(HTTPSERVER_COMPILATION)` access-control guard to src/httpserver/detail/unescape_helpers.hpp, matching every sibling detail/ header.
#if !defined(HTTPSERVER_COMPILATION)
#error "unescape_helpers.hpp is internal; only reachable when compiling libhttpserver."
#endif

9. [x] **code-quality-reviewer** | `src/detail/http_request_impl.cpp:190` | code-readability
   unescape_in_arena carries a block comment that is 28 lines long (lines 161-189). While each paragraph is individually meaningful, the aggregate length is high relative to the function body (18 lines). The amortisation contract and arena-waste note are important, but the first two paragraphs repeat information that is already present in unescape_helpers.hpp's header comment.
   *Recommendation:* Trim the comment to focus on the two pieces of information unique to this call site: (a) the thread_local amortisation contract, and (b) the arena-waste trade-off. Remove the re-statement of what unescape_buf_raw does, which is already documented at its definition.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Trimmed the unescape_in_arena header comment in src/detail/http_request_impl_args.cpp, removing the 'Default path' paragraph that restated unescape_buf_raw's own documented behaviour (verified unescape_helpers.hpp already documents it in full) and folding a one-line pointer to unescape_helpers.hpp into the surrounding comment; kept the amortisation-contract and arena-waste-note paragraphs.

10. [x] **code-quality-reviewer** | `src/detail/http_request_impl.cpp:198` | readability
   The fall-through comment `// intentional fall through!` uses an informal style. Modern C++ (C++17) and most linters expect `[[fallthrough]];` as a statement or at minimum the magic comment `/* fall through */` in the GCC/Clang-recognised format to suppress -Wimplicit-fallthrough warnings. The current text may generate a warning on stricter warning sets.
   *Recommendation:* Replace the comment with `[[fallthrough]];` inserted as a statement immediately before the `default:` case, or use the GCC/Clang magic comment form `/* fall through */` if the project's C++ standard floor is below C++17. The rest of the codebase should be checked for consistency with whichever form is chosen.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Already resolved in the current tree — The switch/fall-through logic that used to live in src/detail/http_request_impl.cpp around line 198 was carved out (TASK-086) into unescape_buf_raw in src/httpserver/detail/unescape_helpers.hpp; that file no longer contains this code at all, and the underlying fall-through-comment style issue is tracked at its new location by findings #14 and #20 in this same document.

11. [x] **code-quality-reviewer** | `src/detail/http_request_impl.cpp:218` | code-elegance
   unescape_in_arena takes sink by non-const reference and user_fn by value (a function pointer). The function pointer copy is trivially cheap, but the parameter order (sink first, callback second) is the reverse of the call site in build_request_args (line 313: unescape_in_arena(sink, aa->unescaper)). This is fine as written, but the local anonymous-namespace helper find_or_insert_arg defined later (line 477) follows a different argument-first convention. There is no inconsistency that hurts correctness, but a brief comment in the unescape_in_arena signature explaining why sink comes first (it is mutated in-place) would aid readers skimming top-to-bottom.
   *Recommendation:* Add a one-line comment above the parameter list: `// sink: arena-backed destination, mutated in-place; user_fn: optional ABI-locked callback.` This is optional but makes the intent clear without requiring readers to cross-reference the call site.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* No action — unescape_in_arena's own header comment (src/detail/http_request_impl_args.cpp) already documents that `value` is the arena-backed destination mutated in place, and the finding's own recommendation labels the extra one-line parameter comment as optional with no functional benefit; the call site already uses the same (value, user_fn) order as the definition, so there is no real inconsistency.

12. [x] **code-quality-reviewer** | `src/detail/http_request_impl.cpp:277` | readability
   The large block comment inserted at the TASK-072 call site (lines 277-296) duplicates substantial prose from the unescape_in_arena function's header comment (lines 206-218). Both explain the two-path design (in-place vs thread_local scratch) and reference the TASK-018 lifetime contract. This is not harmful but violates the DRY principle for comments: a reader updating one copy may not find or update the other.
   *Recommendation:* Trim the call-site comment to a one or two-line reference (`// Routes unescape into the arena; see unescape_in_arena for the two-path design and TASK-018 lifetime contract.`) and keep the full explanation at the function definition only.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Already resolved in the current tree — The call-site comment above the unescape_in_arena() call in build_request_args (src/detail/http_request_impl_args.cpp, ~line 145) has already been trimmed to the exact one-line cross-reference form recommended: '// Arena-routed unescape: see unescape_in_arena() above for path details.' plus a short lifetime note; the multi-paragraph duplicate prose is gone.

13. [x] **code-quality-reviewer** | `src/httpserver/detail/unescape_helpers.hpp:79` | code-readability
   unescape_buf_raw casts the decoded byte as 'static_cast<char>((hi << 4) | lo)'. The original http_utils.cpp used 'static_cast<unsigned char>(...)', which is unambiguously correct for all byte values 0x80-0xFF; casting to signed char is implementation-defined for values >= 128. The behaviour is identical on all two's-complement targets (C++20 mandates two's complement), but the unsigned cast was explicit about intent.
   *Recommendation:* Consider restoring 'static_cast<unsigned char>((hi << 4) | lo)' for clarity, then relying on the implicit narrowing to char that stores into data[wpos++]. Alternatively add a brief comment noting the two's-complement assumption is now guaranteed by C++20.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Changed `static_cast<char>((hi << 4) | lo)` to `static_cast<unsigned char>((hi << 4) | lo)` in unescape_buf_raw (src/httpserver/detail/unescape_helpers.hpp), matching the historical http_utils.cpp behaviour.

14. [x] **code-quality-reviewer** | `src/httpserver/detail/unescape_helpers.hpp:85` | code-elegance
   The intentional fall-through from the '%' case to 'default' is documented only with a comment '// intentional fall through!' but no [[fallthrough]] attribute. Modern C++ (-Wimplicit-fallthrough enabled by -Wextra) may warn on compilers that do not recognize the comment pattern. The same pattern was carried over from the old http_utils.cpp; the new shared header is the right place to fix it once.
   *Recommendation:* Replace the '// intentional fall through!' comment with '[[fallthrough]];' (C++17) to silence the warning portably and make the intent machine-checkable.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Replaced the `// intentional fall through!` comment with the C++17 `[[fallthrough]];` attribute in unescape_buf_raw (src/httpserver/detail/unescape_helpers.hpp).

15. [x] **code-quality-reviewer** | `test/unit/http_request_unescape_arena_test.cpp:121` | test-coverage
   assert_no_upstream_resource is declared and carefully implemented (counting upstream spills) but is never used in any of the 8 test cases. The upstream-spill detection role is covered exclusively by the global operator new counter (count_new_window). The unused class adds cognitive noise and risks misleading future contributors into thinking spill detection has two independent mechanisms.
   *Recommendation:* Either remove assert_no_upstream_resource if it is truly unused, or add a test that wires it as the arena's upstream resource to verify zero spills independently of the global-new counter.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Removed the unused assert_no_upstream_resource class from test/unit/http_request_unescape_arena_test.cpp; spill detection remains fully covered by the global operator-new counter (count_new_window).

16. [x] **code-quality-reviewer** | `test/unit/http_request_unescape_arena_test.cpp:282` | test-coverage
   The test `unescape_double_percent_passthrough` checks that `%%` passes through unchanged. However, the http_unescape_raw logic only examines `data[rpos+1]` and `data[rpos+2]` when `size > rpos+2`: for `%%b` (3 chars) the second `%` is not a valid hex digit so it falls through correctly, but for a bare `%%` at the end of the string (2 chars: `%` then `%`, no third char) the boundary check `size > rpos + 2` is false, so it falls through to default and copies `%` then re-enters the loop at `%` again. The test input `a%%b` exercises the fall-through path correctly, but a test with a trailing `%%` (i.e. string ending in `%%`) would give different output than a trailing `%`. This edge case is untested.
   *Recommendation:* Add a test case for `a%%` (two percent signs at end-of-string) to verify the output is `a%%` (both percent signs are preserved). This pins the boundary behaviour and guards against future tweaks to the boundary check.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Added test unescape_double_percent_at_end_passthrough asserting decode_via_arena("a%%") == "a%%", pinning the bare-trailing-'%%' boundary distinct from mid-string "a%%b" and single trailing "abc%".

17. [x] **code-quality-reviewer** | `test/unit/http_request_unescape_arena_test.cpp:408` | test-coverage
   Test `unescape_user_callback_can_grow_value` explicitly opts out of an allocation count assertion with a comment (line 409-410). The comment is accurate and acceptable, but it means the test only verifies observable output correctness (the `X-original` prefix). There is no test that verifies the arena-backed sink is actually grown (i.e. that the pmr::string sink.size() equals the expanded string's size rather than the original). The test is still a valid correctness pin; the comment opts out transparently.
   *Recommendation:* Optionally add `LT_CHECK_EQ(it->second[0].size(), std::size_t{10});` (length of "X-original") as an explicit size check to pin that sink was actually updated to reflect the grown value, not just that the returned data matches. This is a trivial addition that makes the test more complete.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Added LT_CHECK_EQ(it->second[0].size(), std::size_t{10}) to unescape_user_callback_can_grow_value, pinning that the arena-backed sink itself was grown to the expanded length, not just that the returned content matches.

18. [x] **code-simplifier** | `src/detail/http_request_impl.cpp:277` | code-structure
   The block comment spanning lines 277-296 (14 lines) above the find_or_insert / emplace_back call site repeats — at length — the same explanation already given in the unescape_in_arena function's header comment (lines 209-238). The call site comment adds 'TASK-072:' prefix, two sub-bullet paths, and an ownership lifetime note, all of which are visible one screenful up in the same function.
   *Recommendation:* Reduce the call-site comment to a single line naming what is happening and a cross-reference: // Arena-routed unescape: see unescape_in_arena() above for path details. The ownership/lifetime note belongs with the type definition of arguments_accumulator or the struct's field documentation, not repeated at each call site.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Already resolved in the current tree — Same call site as finding #12: the multi-paragraph duplicate explanation at the build_request_args call site (src/detail/http_request_impl_args.cpp) has already been reduced to a single cross-referencing comment line, matching this finding's recommended text almost verbatim.

19. [x] **code-simplifier** | `src/httpserver/detail/unescape_helpers.hpp:64` | naming
   The function `unescape_buf_raw` uses the suffix `_raw` to signal 'operates on a raw byte buffer rather than a std::string', but `_raw` is ambiguous — it could mean 'unescaped/raw input', 'raw pointer', or 'raw bytes'. The comment at the top of the file clarifies the intent, but the name alone does not. The companion wrapper in http_utils.cpp is called `http_unescape`; the internal helper name could be more expressive without being longer.
   *Recommendation:* Rename to `unescape_buf_inplace` or `percent_decode_inplace` to make the in-place mutation and the buffer-level operation both explicit in the name. Either name is unambiguous and eliminates the need for the `_raw` suffix explanation. Apply the rename in both the header and the two call sites (http_utils.cpp line 314 and http_request_impl.cpp line 197).
   *Worked 2026-07-12 (chore/unworked-minor-issues):* No action — unescape_buf_raw already carries a thorough header doc comment (src/httpserver/detail/unescape_helpers.hpp, ~lines 51-63) fully disambiguating the '_raw' suffix (in-place buffer mutation, size semantics, null-termination contract); renaming would touch the header plus call sites in src/http_utils.cpp and src/detail/http_request_impl_args.cpp for a purely cosmetic, subjective naming preference already mitigated by the existing documentation.

20. [x] **code-simplifier** | `src/httpserver/detail/unescape_helpers.hpp:85` | code-structure
   The fall-through between the '%' case and the 'default' case uses a plain comment `// intentional fall through!` rather than the C++17 `[[fallthrough]]` attribute. The rest of the codebase has no other switch fall-throughs (grep confirms zero `[[fallthrough]]` uses), but modern compilers warn on implicit fall-through without the attribute under -Wimplicit-fallthrough. Using the attribute is self-documenting and silences the warning without a pragma.
   *Recommendation:* Replace the comment with `[[fallthrough]];` on the line immediately before the `default:` label. The comment can be kept as an extra explanation above: `// invalid or truncated %HH: fall through and emit '%' as-is`. This matches both the C++17 standard and the clean-code principle of explaining yourself in code rather than with comments.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Same [[fallthrough]] edit as finding 14 (single shared change) resolves this duplicate.

21. [x] **code-simplifier** | `test/bench_warm_path.cpp:295` | code-structure
   Bench sections (5) and (6) for build_request_args (lines 295-383) duplicate the arena+impl+accumulator setup and the warmup call verbatim, with the only difference being the kValue string and the label string. The bench comment at lines 344-346 even documents their intended relationship ('The median for (5) should land within noise of the (6) baseline'). This structural duplication means any change to the setup (e.g., the 65536-byte buffer size or the generous DoS limits) must be made in two places.
   *Recommendation:* Extract a run_build_args_bench(const char* label, int number, const char* value) helper that encapsulates the bench section setup (buf, arena, alloc, impl, accumulator, DoS limits, warmup call, measure_median_ns call, delete_object). Each of the two bench sections reduces to a one-line call.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Extracted a run_build_args_bench(label, value) helper in test/bench_warm_path.cpp encapsulating the shared buf/arena/impl/accumulator setup and measure_median_ns call; sections (5) and (6) now each reduce to a one-line call with their own label/value pair.

22. [x] **code-simplifier** | `test/unit/http_request_unescape_arena_test.cpp:300` | code-structure
   Tests (6) unescape_empty_value, (7) unescape_view_outlives_subsequent_inserts, and (8) unescape_user_callback_can_grow_value each open-code the same 5-line arena+impl+accumulator setup (buf, arena, alloc, new_object, aa setup) that decode_via_arena already encapsulates. Tests 3-5 were successfully deduplicated via decode_via_arena; tests 6-8 were not, leaving residual repetition. The inlined setup is not materially different from what decode_via_arena does — the only variation is the unescaper field (nullptr vs. a function pointer) and the multi-step assertions that follow.
   *Recommendation:* Either (a) expand decode_via_arena to accept an optional unescaper_ptr parameter (defaulting to nullptr) and return both the result string and the full impl pointer for tests that need multi-step access, or (b) extract a lower-level make_arena_impl helper that returns the arena+impl pair so tests 6-8 can call it and do their own assertions without duplicating the setup boilerplate. Option (b) keeps the helpers composable and single-purpose. This is consistent with the clean-code principle of not repeating yourself and the 'one thing per function' rule.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Extracted an arena_impl_fixture RAII helper (owning buf/arena/alloc/impl/accumulator) in test/unit/http_request_unescape_arena_test.cpp and refactored tests (6)-(8) to use it instead of open-coding the same setup.

23. [x] **performance-reviewer** | `src/detail/http_request_impl.cpp:178` | memory-allocation
   http_unescape_raw has a redundant early-exit condition: 'if (size == 0 || data[0] == '\0') return 0'. The size == 0 check is correct, but data[0] == '\0' is redundant because the while loop condition 'rpos < size && data[rpos] != '\0'' already handles embedded null terminators correctly. This is called from unescape_in_arena which guards with 'if (sink.empty()) return' before calling http_unescape_raw, so size == 0 is also unreachable from that call site. The double guard adds a branch on every call but has no functional effect; it slightly obscures the loop invariant.
   *Recommendation:* Remove the data[0] == '\0' check from http_unescape_raw. The only caller is unescape_in_arena which already filters the empty case, so the check is dead code. If http_unescape_raw is intended to be called independently by future callers, keep only the 'size == 0' guard and remove the null-byte early exit.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Already resolved in the current tree — unescape_buf_raw (src/httpserver/detail/unescape_helpers.hpp) now only guards with `if (size == 0) return 0;` -- the redundant `data[0] == '\0'` early-exit this finding describes is no longer present.

24. [x] **performance-reviewer** | `test/bench_warm_path.cpp:301` | missing-caching
   Bench variants (5) and (6) allocate and warm their arenas inside a block scope and call alloc.delete_object(p) at the end. The 10,000-iteration warmup inside measure_median_ns accumulates 10,000 entries in the vector before the 11 outer rounds begin. These 10,000 entries already overflow the 65536-byte arena (~819 entries to fill), so by the start of the first measured outer round, every new emplace_back already goes to the upstream heap. The warmup was intended to prime the instruction cache and branch predictor, but it also irreversibly fills the arena. This means the warmup unintentionally ensures that 100% of the measured iterations pay heap-allocation cost.
   *Recommendation:* See finding #1 for the primary fix. As a secondary improvement, move the warmup calls inside a fresh-arena scope that is discarded before the measured outer loop begins, so the measured rounds start with a fresh arena. Alternatively, reset the impl state between warmup and measurement.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Already resolved in the current tree — test/bench_warm_path.cpp's own in-code comment (citing TASK-083) confirms sections (5)/(6) were redesigned to build a fresh monotonic_buffer_resource and a fresh arguments_accumulator inside every measured call, replacing the old persistent-warmup-loop-fills-one-shared-arena design this finding describes.

25. [x] **performance-reviewer** | `test/bench_warm_path.cpp:328` | memory-allocation
   The 65536-byte stack buffer is value-initialised (`buf{}`) on every measured call in sections (5) and (6). On x86-64 this compiles to a full-buffer memset (~16 KB), adding roughly 50-200 ns of work per iteration that is unrelated to the arg-insert warm path being measured. Both sections are identically affected so relative (5)-vs-(6) comparisons remain valid, but the absolute median is inflated versus the real production path where the arena is already allocated and merely reset between requests.
   *Recommendation:* Zero-initialise the buffer once before the `measure_median_ns` call and reuse it across iterations. Because `monotonic_buffer_resource` tracks its own bump pointer independently of the backing memory's contents, the buffer does not need to be re-zeroed between calls -- only the arena object needs to be reconstructed to reset the bump pointer. Move `buf` outside the lambda and construct only `arena`, `alloc`, the impl, and the accumulator inside it. This removes the memset from the hot path and makes the reported ns/call match the production steady-state more faithfully.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Already resolved in the current tree — test/bench_warm_path.cpp sections (5)/(6) already declare buf5/buf6 once outside the measured lambda (zero-initialised a single time) and only reconstruct the lightweight monotonic_buffer_resource per call; the file's own comment (~lines 279-287) documents this was done specifically to avoid the per-iteration memset described by this finding.

26. [x] **security-reviewer** | `src/detail/http_request_impl.cpp:193` | input-validation
   http_unescape_raw casts the decoded byte to static_cast<char> (line 193), diverging from the reference implementation in src/http_utils.cpp (line 332) which uses static_cast<unsigned char>. When char is a signed type (the common case on x86/ARM), values in the range 0x80–0xFF are stored as negative chars. While the bit patterns are identical and no downstream consumer is known to interpret the sign, this divergence from the reference could cause subtle mismatches if the two code paths are ever compared character-by-character (e.g. in a test that round-trips through both) or if a future consumer is sign-sensitive. CWE-704 (incorrect type conversion).
   *Recommendation:* Change `static_cast<char>((hi << 4) | lo)` to `static_cast<unsigned char>((hi << 4) | lo)` to match src/http_utils.cpp line 332 exactly and make the cast well-defined for the full 0x00–0xFF range under both C++17 and C++20.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Same static_cast<unsigned char> edit as finding 13 (single shared change) resolves this duplicate.

27. [x] **security-reviewer** | `src/detail/http_request_impl.cpp:272` | input-validation
   The DoS byte-budget check (`aa->accumulated_bytes + this_pair_bytes > aa->max_args_bytes`, line 272) is applied against the raw, pre-unescape value length. A user-registered unescaper callback (`unescaper_ptr`) that expands its input (e.g. base64-decoding, HTML-entity expansion) can cause the actually stored bytes to exceed `max_args_bytes` by an unbounded factor. Because `user_fn` is operator-registered trusted code rather than attacker-controlled data, this is not directly exploitable, but if the callback is ever sourced from untrusted configuration or plugin code the budget becomes ineffective. CWE-770 (allocation without limits).
   *Recommendation:* Document explicitly in `arguments_accumulator` and the surrounding comment that `max_args_bytes` is a pre-unescape bound and that user callbacks which expand values are not constrained by it. If defense-in-depth is desired, add a post-unescape size cap: after `unescape_in_arena(sink, aa->unescaper)` check `sink.size()` against a per-value ceiling and truncate or reject if exceeded.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Added a comment on arguments_accumulator::max_args_bytes (src/httpserver/detail/http_request_impl.hpp) noting it bounds pre-unescape raw key+value bytes only, and that a value-expanding user unescaper is not constrained by it.

28. [x] **security-reviewer** | `src/httpserver/detail/unescape_helpers.hpp:75` | input-validation
   The truncated-escape guard `if (size > rpos + 2)` uses unsigned arithmetic. Because both operands are std::size_t the expression `rpos + 2` can wrap to 0 when rpos == SIZE_MAX - 1, making the guard trivially true and allowing an out-of-bounds read of data[rpos+1] and data[rpos+2]. In practice rpos is bounded by the caller's `size` parameter (which comes from std::string::size()), so rpos can never approach SIZE_MAX on any real platform or input; the original code had the same pattern with `unsigned int`. The risk is theoretical only, but the idiom `size - rpos > 2` (which avoids the addition entirely) would be unambiguously safe.
   *Recommendation:* Replace `if (size > rpos + 2)` with `if (size - rpos > 2)` (i.e. at least 3 bytes remaining). Both evaluate identically for all reachable inputs, but the subtraction form carries no theoretical wrap risk. CWE-190 (Integer Overflow in Condition).
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Already resolved in the current tree — The unsigned-overflow-prone `size > rpos + 2` guard was already rewritten to the overflow-safe `size - rpos > 2` form (with an explanatory comment) in src/httpserver/detail/unescape_helpers.hpp, per major finding #1's fix worked 2026-07-07 in chore/unworked-major-issues.

29. [x] **spec-alignment-checker** | `test/bench_warm_path.cpp:null` | acceptance-criteria
   The task action item says 'Pin no allocations under heap profiler as in TASK-058' in bench_warm_path.cpp. The bench itself contains no runtime assertion of zero allocations (no operator new counter, no assert). The allocation pin is instead in the separate unit test test/unit/http_request_unescape_arena_test.cpp via a count_new_window RAII guard. The bench only reports timing numbers. The mechanism chosen (programmatic operator new override in the unit test) is arguably stronger and more deterministic than a heap profiler, so the acceptance criterion is met, but the bench itself does not directly pin allocation counts as the action item's wording implies.
   *Recommendation:* The approach is acceptable and arguably superior to a heap-profiler check. Consider updating the bench comment (line 47-55) to note that the allocation pin is in http_request_unescape_arena_test.cpp rather than implying the bench itself validates zero allocs.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* No action — The finding's own text concedes the current approach (allocation pin lives in the unit test's operator-new counter) is acceptable and arguably superior to a heap profiler; test/bench_warm_path.cpp's header comment describes timing measurements only and does not claim to itself validate zero-alloc counts, so there is nothing misleading to correct.

30. [x] **test-quality-reviewer** | `test/bench_warm_path.cpp:295` | missing-test
   The bench measures build_request_args_pct2f and build_request_args_plain side-by-side, but neither bench variant sets `accumulated_bytes = 0` between loop iterations. Over INNER=1M iterations with the same key 'k', the accumulated_bytes field grows monotonically to INNER * (key_len + val_len) bytes. Since max_args_bytes defaults to DEFAULT_MAX_ARGS_BYTES (65536), the bench will hit the byte-budget guard after fewer than 65536/52 ≈ 1256 iterations and return MHD_NO for all remaining ~998,744 iterations per outer round, measuring only the fast-exit path after warmup. The printed median will be artifactually low and not reflect the steady-state unescape cost. The unit tests use a fresh accumulator per call so they do not have this issue.
   *Recommendation:* Reset `aa.accumulated_bytes = 0` at the start of each inner iteration of the bench lambda, or set `aa.max_args_bytes` to a value larger than `INNER * (key_len + val_len)` (the bench already sets `aa.max_args_count = INNER + 1024` but omits the bytes reset). This ensures the bench measures the actual unescape path, not the early-exit path, for all INNER iterations.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Already resolved in the current tree — The redesigned bench (test/bench_warm_path.cpp sections 5/6) constructs a fresh `arguments_accumulator aa` inside every measured call, so `accumulated_bytes` starts at 0 each iteration; the described monotonic-growth-triggers-early-exit-after-~1256-iterations flaw no longer applies.

31. [x] **test-quality-reviewer** | `test/unit/http_request_unescape_arena_test.cpp:216` | test-isolation
   Test (2) `warm_path_zero_global_allocs_user_unescape` depends on a `thread_local std::string user_scratch` that was warmed by a previous test on the same thread. If tests run in a different order, or if test (1) did not run first, the two warmup calls in test (2) may not be sufficient to exhaust all per-thread growth. The test comment acknowledges this ('Two cold calls: warm any one-shot impl-internal caches AND grow the per-thread user-scratch std::string capacity') but the reliance on ordering is implicit. This is a mild non-determinism risk rather than a structural error, because two warmup calls with kLongValueWithPct2F should be sufficient in practice.
   *Recommendation:* Document explicitly above the test that it is safe to run standalone (two warmup calls are sufficient because the thread_local scratch grows to kLongValueWithPct2F's length after the first call). Optionally add a third warmup call for the user-callback path to make the 'worst cold case' obvious.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* No action — Both warmup calls in warm_path_zero_global_allocs_user_unescape pass the identical kLongValueWithPct2F value used for the measured call, so the thread_local buffer reaches sufficient capacity after the first warmup regardless of any earlier test's state on that thread; the test's correctness does not actually depend on run order, only on its own two internal warmup calls.

32. [x] **test-quality-reviewer** | `test/unit/http_request_unescape_arena_test.cpp:252` | excessive-setup
   Tests (3) through (8) repeat the same 8-line arena/allocator setup verbatim (alignas array, monotonic_buffer_resource, impl_alloc_t alloc, alloc.new_object). This pattern is identical across six tests and inflates the Arrange phase while obscuring the test's distinctive behaviour. The existing http_request_arena_test.cpp uses the same repetition style, so this is consistent with codebase convention, but the six new tests together create a significant maintenance surface.
   *Recommendation:* Consider extracting a local `make_arena_impl()` helper or using a suite-level fixture that provides the pre-built arena and impl pointer, matching the pattern used by http_request_arena_test.cpp's `one_request_cycle` lambda in test (5) of that file. This is a minor recommendation; the current style is readable and consistent with the rest of the test file.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* No action — Duplicate of finding #22 in this ledger. The tests-(3)-through-(5c) portion of the duplication this finding describes has already been eliminated via the decode_via_arena helper; the remaining duplication is confined to tests (6)-(8) and is the exact scope finding #22 recommends fixing, so no separate action is needed here.

33. [x] **test-quality-reviewer** | `test/unit/http_request_unescape_arena_test.cpp:271` | aaa-violation
   Test (3) `unescape_pct2f_decodes_to_slash` performs three assertions on the iterator: `it != end`, `it->second.size() == 1`, and then the decoded string content. The second assertion (`size == 1`) is a structural guard that belongs to the arrange/guard phase rather than being a co-equal correctness assertion. It is not wrong, but the expected-size assertion `std::size_t{1}` is not directly stated in the acceptance criteria and couples the test to the internal vector-of-values representation.
   *Recommendation:* Keep the bounds guard `LT_CHECK(it != p->unescaped_args.end())` and the string-content assertion `LT_CHECK_EQ(..., 'a/b')`. The `size == 1` check is defensive and adds value as a guard against a multi-value bug -- consider a brief comment marking it as a guard rather than a content assertion.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Already resolved in the current tree — Test (3) unescape_pct2f_decodes_to_slash is now a single `LT_CHECK_EQ(decode_via_arena("a%2Fb"), std::string("a/b"));` call via the decode_via_arena helper; the three-assertion structure (it != end, size == 1, content) this finding describes no longer exists in the test body.

34. [x] **test-quality-reviewer** | `test/unit/http_request_unescape_arena_test.cpp:278` | missing-test
   The '+' to space decoding branch in unescape_buf_raw (unescape_helpers.hpp:70-73) has no coverage in this test file. The iter-1 finding was not addressed. unescape_buf_raw explicitly handles '+' → ' ' before the '%' branch, yet the correctness tests only exercise '%2F', '%%', trailing '%', and empty input. A value such as 'a+b' should decode to 'a b' and this path is not pinned.
   *Recommendation:* Add a single LT_CHECK_EQ(decode_via_arena("a+b"), std::string("a b")) test case alongside the existing correctness tests (3)-(5). This closes the one remaining gap from iter 1 at zero additional test infrastructure cost.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Already resolved in the current tree — unescape_plus_decodes_to_space (test/unit/http_request_unescape_arena_test.cpp, ~lines 299-302) already pins 'a+b' -> 'a b'; it was added in the major-findings sweep worked 2026-07-07 (chore/unworked-major-issues, see finding #2 in this same document).

35. [x] **test-quality-reviewer** | `test/unit/http_request_unescape_arena_test.cpp:282` | naming-convention
   Test (4) is named `unescape_double_percent_passthrough` but the input is 'a%%b' (double-percent in the middle), while test (5) `unescape_trailing_percent_passthrough` uses 'abc%' (single percent at end). The name 'double_percent' is slightly misleading: the actual invariant being tested is that a non-hex character after '%' falls through to the default case, not that two consecutive '%' are handled specially. The actual '%%' sequence decodes one '%' and leaves the next '%' to the next iteration, but the expected output 'a%%b' (unchanged) depends on both '%' being invalid non-hex pairs, which happens to be true for '%' followed by '%' but the mechanism is different from what the name implies.
   *Recommendation:* Rename to `unescape_invalid_hex_after_percent_passthrough` to make the invariant explicit (non-hex digits after '%' are passed through), and add a comment explaining that '%%' is preserved because the second '%' is also not a valid two-hex-digit sequence.
   *Worked 2026-07-12 (chore/unworked-minor-issues):* Renamed test (4) from unescape_double_percent_passthrough to unescape_invalid_hex_after_percent_passthrough and added a comment clarifying '%%' passes through because the second '%' is also not a valid two-hex-digit sequence, not via special-casing of consecutive percents.
