# Unworked Review Issues

**Run:** 2026-05-10 15:53:13
**Task:** TASK-024
**Total:** 33 (0 critical, 1 major, 32 minor)

## Major

1. [ ] **test-quality-reviewer** | `test/unit/webserver_register_path_prefix_test.cpp:192` | missing-test
   All seven runtime tests in webserver_register_path_prefix_test.cpp pass std::make_shared<ok_resource>() — no runtime test exercises the unique_ptr overloads of register_path or register_prefix. Static_asserts at lines 104-137 verify the overloads compile and return void, but they do not verify that ownership is transferred (resource is actually served and not double-freed). By contrast, webserver_register_smartptr_test.cpp validates unique_ptr ownership at runtime via unique_ptr_overload_compiles_and_serves. The analogous coverage is absent for the new register_path/register_prefix names.
   *Recommendation:* Add at least one runtime test each for register_path and register_prefix that passes std::make_unique<ok_resource>() (using std::move), starts the server, and verifies the resource is served correctly. This mirrors what unique_ptr_overload_compiles_and_serves does for register_resource and eliminates any gap in ownership-transfer coverage for the new named overloads.

## Minor

2. [ ] **architecture-alignment-checker** | `src/httpserver/webserver.hpp:380` | pattern-violation
   The private register_impl_ and unregister_impl_ helpers carry a bool family parameter that exactly mirrors the old public register_resource(path, res, bool family) signature that TASK-024 was chartered to eliminate. While keeping the bool internally is pragmatic (the route-table data structures are keyed on it), the internal API perpetuates the concept that the public API just named away. Architecture §4.7 and DR-007 do not prohibit this, but keeping the internal bool unnamed at call sites (/*family=*/true comments are present) is good practice — it is already done correctly at all call sites in the diff.
   *Recommendation:* No code change required; the named-comment pattern (/*family=*/false, /*family=*/true) is already in place at every call site. Consider replacing the bool with a scoped enum (enum class route_kind { exact, prefix }) in a follow-up for even stronger internal clarity, but this is optional.

3. [ ] **architecture-alignment-checker** | `src/webserver.cpp:646` | pattern-violation
   unregister_impl_ takes route_cache_mutex nested inside registered_resources_mutex. §4.7 documents these as separate mutexes but does not explicitly define a lock-acquisition order. The nesting is consistent with what register_impl_ already does (invalidate_route_cache is also called under registered_resources_mutex), so the order is established by convention. However the convention is implicit rather than documented in the architecture spec.
   *Recommendation:* Add a one-line comment in §4.7 (route-table.md) explicitly recording the lock-acquisition order: registered_resources_mutex (write) must be acquired before route_cache_mutex whenever both are needed. This makes the invariant explicit for future contributors and matches the existing code pattern.

4. [ ] **architecture-alignment-checker** | `src/webserver.cpp:650` | pattern-violation
   The unregister_impl_ private helper still erases from registered_resources_regex using the same http_endpoint key that was built with the supplied family flag. The original unregister_resource always passed family=false when building the key, which worked because 'the family flag does not matter — it just checks the url_normalized anyhow' (old code comment). After the split, unregister_prefix passes family=true. If prefix routes were ever stored under a regex key (which requires http_endpoint to be constructed with family=true), the erase would still be correct; but if prefix routes are never stored in the regex chain, erasing from it is harmless but slightly misleading. This is a low-risk correctness question, not a proven bug, but it deserves a confirming comment.
   *Recommendation:* Add a comment in unregister_impl_ clarifying whether prefix routes can appear in the regex chain and thus whether the registered_resources_regex.erase(he) line is needed for the family=true path, or is a safe no-op. This makes future readers confident the split is complete.

5. [ ] **code-quality-reviewer** | `src/httpserver/webserver.hpp:387` | clean-code
   The private helper register_impl_(path, res, bool family) preserves the 'family' flag naming as a private implementation detail. This is not a public API leak, but 'bool family' as a parameter name is still an opaque flag argument (clean code Functions Rule 6: 'Don't use flag arguments'). The flag is purely internal, so it is a minor issue, but a named enum (e.g., MatchKind::Exact / MatchKind::Prefix) would make the intent self-documenting at call sites like register_impl_(path, res, /*family=*/false).
   *Recommendation:* Consider introducing a private enum: enum class match_kind { exact, prefix }; and replacing the bool parameter in register_impl_ and unregister_impl_ with it. Call sites become register_impl_(path, res, match_kind::exact) which is self-documenting without the /*family=*/false comment crutch.

6. [ ] **code-quality-reviewer** | `src/webserver.cpp:260` | readability
   The single_resource validation guard inside register_impl_ reads: 'if (single_resource && ((resource != "" && resource != "/") || !family))'. The negation and compound condition require careful parsing; a reader must mentally invert '!family' to understand this allows prefix-only. The clean code 'Avoid negative conditionals' tip and 'Encapsulate boundary conditions' apply here.
   *Recommendation:* Extract the guard into a named helper or at minimum a named boolean: const bool is_prefix = family; const bool is_valid_single_resource_call = is_prefix && (resource.empty() || resource == "/"); if (!is_valid_single_resource_call) throw .... This eliminates the double negation.

7. [ ] **code-quality-reviewer** | `src/webserver.cpp:643` | readability
   unregister_impl_ acquires registered_resources_mutex via unique_lock but then performs the cache invalidation inside a nested lock_guard on route_cache_mutex. The outer unique_lock is unlocked only when it goes out of scope (end of function), yet the cache clear is the first operation. A comment explaining the lock ordering (registered_resources_mutex always acquired before route_cache_mutex) would help maintainers avoid introducing deadlocks.
   *Recommendation:* Add a brief comment above the nested lock_guard block: '// Lock ordering: registered_resources_mutex (outer) -> route_cache_mutex (inner). Do not acquire route_cache_mutex without holding registered_resources_mutex.'

8. [ ] **code-quality-reviewer** | `src/webserver.cpp:670` | readability
   unregister_resource builds both http_endpoint keys before acquiring the lock (which is correct), but the CWE-367 comment above the lock acquisition would be more useful if it also noted that cache invalidation is included in the same critical section, since that is the other half of the atomicity guarantee.
   *Recommendation:* Extend the comment to mention that cache invalidation happens under the same lock: '// Hold a single write-lock across both erasures AND cache invalidation so no request thread can observe a partially-unregistered state.'

9. [ ] **code-quality-reviewer** | `test/unit/webserver_register_path_prefix_test.cpp:184` | test-coverage
   The new unit test covers happy-path prefix matching, exact matching, parameterized exact matching, and both unregister variants. However, it does not contain a test for: (a) null pointer to register_path or register_prefix throws std::invalid_argument, (b) duplicate registration via register_path / register_prefix throws, and (c) unregister_path does NOT remove a co-registered prefix at the same path (cross-kind isolation). Error-path tests for register_path and register_prefix are present only in the integ suite (ws_start_stop.cpp), not in the focused unit test for this task.
   *Recommendation:* Add at least one null-pointer test and one duplicate-registration test directly in webserver_register_path_prefix_test.cpp so the dedicated test file is self-contained and documents the contracts of the new API. A cross-kind isolation test (register_path + register_prefix at '/x', unregister_path('/x'), verify prefix still routes) would also strengthen the unregister contract.

10. [ ] **code-quality-reviewer** | `test/unit/webserver_register_path_prefix_test.cpp:294` | test-coverage
   The new unregister_resource_removes_both_path_and_prefix_registrations test verifies the post-unregister state but does not assert the pre-unregister state for the prefix child URL (localhost:8187/admin/panel). Without a before-check for the child URL, a regression that broke prefix matching before unregister would pass the test silently.
   *Recommendation:* Add LT_CHECK_EQ(fetch("localhost:8187/admin/panel").response_code, 200) before the unregister_resource call, mirroring the pattern used in register_prefix_then_unregister_prefix_404s.

11. [ ] **code-simplifier** | `src/httpserver/webserver.hpp:114` | code-structure
   The doc-comment for register_path's unique_ptr overload says 'Templated unique_ptr<T> shim funnels into the shared_ptr overload exactly the way TASK-023's pattern does'. This is implementation detail language (with another task reference) in a public API doc comment. Public callers don't need to know about the funnel strategy or TASK-023.
   *Recommendation:* Replace with caller-facing language, e.g. 'Accepts a derived resource type; ownership is transferred to the webserver.' The analogous sentence in the register_prefix doc (line 147) has the same issue.

12. [ ] **code-simplifier** | `src/httpserver/webserver.hpp:168` | code-structure
   The register_resource deprecated-alias doc comment says 'existing TASK-023-era call sites keep compiling'. The task reference is internal noise for a public API comment. The 'Deprecated alias for register_path' part is clear on its own.
   *Recommendation:* Remove 'TASK-023-era' and rewrite as: 'Deprecated alias for register_path(). Kept for backward compatibility; use register_path() for exact match or register_prefix() for prefix match.'

13. [ ] **code-simplifier** | `src/httpserver/webserver.hpp:383` | naming
   Private helper declarations are annotated with 'TASK-024:' task-reference prefixes. Task identifiers belong in commit messages and changelogs, not in persistent source comments — they rot as the ticket system moves on and add no value for a future reader who doesn't have access to the tracker.
   *Recommendation:* Drop the 'TASK-024:' prefix from both comment blocks (lines 383 and 391). The intent is already clear from the comment body: 'Shared registration helper. Both register_path and register_prefix funnel through here...' etc.

14. [ ] **code-simplifier** | `src/webserver.cpp:249` | naming
   The section-header comment on line 249 also carries a 'TASK-024:' prefix, and the inline comment on line 300 ('Deprecated TASK-023-era forwarder') references a task number inline in the source. Same issue as finding #1 — these are changelog concerns, not code comments.
   *Recommendation:* Rewrite as plain English: '// register_path / register_prefix split. Both public methods funnel into this private helper...' and '// Deprecated forwarder kept for backward compatibility. Users that want prefix matching must call register_prefix().' Remove all TASK-NNN references from .cpp source comments.

15. [ ] **code-simplifier** | `src/webserver.cpp:636` | code-structure
   The block comment above unregister_impl_ (lines 636-641) repeats information already expressed by the code itself (the named booleans /*family=*/false and /*family=*/true at the call sites) and the inline comment on line 654. The comment about cache invalidation is useful, but the first three lines about the family flag are redundant given the parameter name and the comment on 654.
   *Recommendation:* Trim to the essential: remove lines 637-639 ('Each kind keeps a distinct http_endpoint key...') which duplicate the inline comment at line 654, keeping only the cache-invalidation rationale sentence.

16. [ ] **code-simplifier** | `src/webserver.cpp:670` | code-structure
   unregister_resource duplicates the cache-clear block that also lives in unregister_impl_. The two-endpoint case requires a single lock, so calling unregister_impl_ twice cannot be used here, but the inline structure is slightly more verbose than it needs to be. The comment block at lines 636-641 (on unregister_impl_) and the inline comment at line 677 say the same thing from two angles.
   *Recommendation:* Consider extracting the cache-clear inner block into a private helper such as clear_route_cache_locked_() that both unregister_impl_ and unregister_resource can call. This removes the repeated four-line pattern and keeps the locking rationale in one comment. Only worth doing if there is a third call site; otherwise the current duplication is acceptable.

17. [ ] **code-simplifier** | `test/unit/webserver_register_path_prefix_test.cpp:296` | naming
   The test at PORT + 7 (port 8187) skips PORT + 6 (used by the deprecated-forwarder test at line 323). The port assignment is non-sequential: tests appear in order 8180, 8181, 8182, 8183, 8184, 8185, 8187, 8186. A reader scanning the file top-to-bottom encounters 8187 before 8186, which looks like a numbering mistake.
   *Recommendation:* Swap the PORT offsets so that the test defined last in the file uses the highest offset (+7), and the deprecated-forwarder test defined just before it uses +6. No logic changes needed, only the PORT + N literals. This removes the apparent off-by-one that distracts reviewers.

18. [ ] **code-simplifier** | `test/unit/webserver_register_path_prefix_test.cpp:86` | code-structure
   fetch() leaks the CURL handle if curl_easy_init() returns null (although this is an OOM case). More practically, the function has no early-return guard, so curl_easy_setopt calls on a null handle are undefined behaviour.
   *Recommendation:* Add a null check after curl_easy_init(): if (!curl) return fr; This is a one-liner and makes the invariant explicit, consistent with how most other test helpers in the repo are written.

19. [ ] **housekeeper** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-024/configure~:null` | documentation-stale
   An untracked `configure~` file (autoconf backup) is present in the worktree root and has not been committed or gitignored. It is not harmful but leaves the worktree in a dirty state.
   *Recommendation:* Either delete the file (`rm configure~`) or add `configure~` to `.gitignore`. It should not be committed.

20. [ ] **performance-reviewer** | `src/webserver.cpp:284` | memory-allocation
   In register_impl_, the shared_ptr res is inserted into registered_resources at line 267 (copied into the map pair) and then copied again into registered_resources_regex at line 284. The second insert at line 284 is the last use of res, so it could be std::moved to avoid the extra ref-count increment/decrement.
   *Recommendation:* Change the regex insert to: impl_->registered_resources_regex.insert({idx, std::move(res)}); — this is safe because res is not used after that point. This is a startup-path micro-optimisation but reflects correct move semantics.

21. [ ] **performance-reviewer** | `src/webserver.cpp:670` | resource-leak
   unregister_resource acquires registered_resources_mutex and route_cache_mutex twice and clears the route cache twice — once via unregister_path and once via unregister_prefix. The second cache clear is a redundant write under a second lock cycle.
   *Recommendation:* Either inline both erases in a single unregister_impl_ call that accepts a bitmask/enum for which kinds to erase, or clear the route cache only once after both erases complete. Since unregister_resource is an admin-path call this has no request-serving impact, but the pattern is worth cleaning up to avoid spurious double-invalidation on every generic unregister call.

22. [ ] **performance-reviewer** | `src/webserver.cpp:681` | race-condition
   route_cache_mutex is acquired as a nested lock while registered_resources_mutex (unique_lock) is already held. The nesting order must be strictly observed everywhere to avoid deadlock. Currently unregister_impl_ (lines 644-660) and unregister_resource (lines 678-692) both nest cache_lock inside registered_resources_lock, which is consistent. If any future code path acquires these locks in the opposite order, a deadlock will occur. There is no performance issue today, but the lock ordering is not documented and a misordered acquisition in a hot path (e.g., the route-lookup path) would serialize all requests.
   *Recommendation:* Add a comment near both mutex declarations (in the impl struct) stating the required acquisition order: registered_resources_mutex must always be taken before route_cache_mutex. Consider replacing the nested approach with a single shared mutex for both maps, or restructuring the route-cache invalidation to use an atomic generation counter so it does not require a separate lock at all.

23. [ ] **security-reviewer** | `src/webserver.cpp:274` | A09: Logging Failures — registration errors expose internal path strings
   The duplicate-registration error message 'A resource is already registered at this path' does not include the path, which is appropriate. However the null-pointer and single_resource messages also do not include the path string, which is fine. No sensitive data is leaked. This is noted only because the exception messages are part of the library's public API surface and are user-visible; they are adequate as-is.
   *Recommendation:* No action required. Noted for completeness.

24. [ ] **security-reviewer** | `src/webserver.cpp:286` | locking
   register_impl_ releases registered_resources_mutex before calling invalidate_route_cache() (which acquires route_cache_mutex alone). This is correct and avoids nesting, but the unlock-then-invalidate window means a reader can transiently see the new registration without a warm cache entry, causing an extra regex scan. Not a safety issue — the resource is already visible under the shared lock — but worth noting as the asymmetry with unregister_impl_ (which clears cache under the write lock) could confuse future maintainers.
   *Recommendation:* Add a brief comment at line 286-287 explaining that unlocking first is intentional to preserve lock-order discipline (registered_resources -> route_cache) while keeping invalidation outside the write window. No code change required.

25. [ ] **security-reviewer** | `src/webserver.cpp:295` | A04: Insecure Design — empty/root path accepted by register_prefix without restriction
   register_prefix() passes through register_impl_() with family=true and has no check that rejects an empty string or bare '/' path when single_resource mode is NOT active. While this is intentional (register_prefix('/') means 'catch-all'), the API provides no warning and a developer who accidentally calls register_prefix('') (empty string) or register_prefix('/') intending a narrow resource will silently expose every unmatched route to that handler. The existing single_resource guard only fires when single_resource==true. CWE-20 (Improper Input Validation) — low exploitability but a latent misconfiguration risk.
   *Recommendation:* Add an optional developer-facing guard: either a [[nodiscard]]-style compile hint, or at minimum a debug-mode assertion that logs a warning when an empty/root prefix is registered outside of single_resource mode. Consider documenting the catch-all semantics explicitly in the register_prefix() Doxygen block.

26. [ ] **spec-alignment-checker** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-024/src/httpserver/webserver.hpp:387` | specification-gap
   register_impl_ and unregister_impl_ are declared in the public header's private section rather than in a private .cpp-only helper. This is an implementation detail exposed in the header, but it causes no functional issue because they are private members. The task spec does not require them to be hidden from the header.
   *Recommendation:* No action required; private helpers in the header are acceptable. If the team ever wants to reduce header noise, they can be moved into a detail namespace or into the .cpp file via a free function.

27. [ ] **spec-alignment-checker** | `src/httpserver/webserver.hpp:207` | specification-gap
   PRD-NAM-REQ-005 (TASK-027 scope, but observable here) requires `block_ip` / `unblock_ip` as the sole IP access-control verbs. The current header still declares `ban_ip`, `allow_ip`, `unban_ip`, `disallow_ip` (lines 207-210). These are out-of-scope for TASK-024 but represent pending spec alignment that was not disturbed by this task either.
   *Recommendation:* Track under TASK-027 or a dedicated naming task. Not a blocker for TASK-024 approval.

28. [ ] **spec-alignment-checker** | `src/httpserver/webserver.hpp:231` | specification-gap
   PRD-NAM-REQ-002 / §3.7 requires `sweet_kill` to be renamed `stop_and_wait`. The method `sweet_kill` (line 231) still exists unchanged. Out of scope for TASK-024 but untouched by the task.
   *Recommendation:* Track under the naming task milestone. Not a blocker for TASK-024 approval.

29. [ ] **spec-alignment-checker** | `src/httpserver/webserver.hpp:387` | specification-gap
   The private helpers `register_impl_` and `unregister_impl_` use a `bool family` parameter internally (lines 387-393). This is not a public API violation — the acceptance-criterion grep against `register_resource(...)` finds no results, and `bool family` never appears in a public declaration. However, §4.7 specifies a `route_entry` type carrying `std::variant<lambda_handler, std::shared_ptr<http_resource>>` plus `bool is_prefix`, but the route table still stores naked `std::shared_ptr<http_resource>` values in three separate maps (confirmed by the unworked review issue logged at specs/unworked_review_issues/2026-05-10_114120_task-023.md). TASK-024 does not introduce the `route_entry` variant abstraction that §4.7 prescribes.
   *Recommendation:* This is a known deferred gap (the unworked review issue explicitly notes it). The TASK-024 commit need not fix it, but the task file (TASK-024.md) should note that §4.7 route_entry compliance is still outstanding and deferred to TASK-025/TASK-026. No code change required for TASK-024 verdict.

30. [ ] **test-quality-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-024/test/unit/webserver_register_path_prefix_test.cpp:270` | aaa-violation
   unregister_resource_alias_handles_both_kinds interleaves actions and assertions (fetch, unregister, fetch) without AAA phase separation. The two resources are separate concerns bundled together; the test name says 'both kinds' but asserts them in the same body, blurring which kind is under test at each assertion.
   *Recommendation:* Either split into two tests (one for prefix, one for exact) or add inline comments demarcating Arrange/Act/Assert phases to make the flow immediately readable.

31. [ ] **test-quality-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-024/test/unit/webserver_register_smartptr_test.cpp:203` | naming-convention
   null_unique_ptr_throws and null_shared_ptr_throws use a manual bool-flag pattern (threw = false; try/catch; LT_CHECK_EQ(threw, true)) instead of an assertion helper, making the intent one level of indirection away from the reader.
   *Recommendation:* If littletest provides an LT_ASSERT_THROWS or equivalent, prefer it. If not, this is acceptable as-is — just note the pattern for consistency when the framework is extended.

32. [ ] **test-quality-reviewer** | `test/unit/webserver_register_path_prefix_test.cpp:184` | missing-test
   The new test file has no negative/error-path tests for register_path or register_prefix (null pointer throws, duplicate registration throws). These are covered for register_resource in webserver_register_smartptr_test.cpp (null_unique_ptr_throws, null_shared_ptr_throws, duplicate_registration_throws) and for register_path in ws_start_stop.cpp (register_path_nullptr_throws, register_duplicate_resource_throws). Because the new public surface is register_path/register_prefix rather than register_resource, the TASK-024-specific TU is the natural home for these contracts; their absence means a reader of the focused TASK-024 file cannot confirm the new methods enforce the same invariants.
   *Recommendation:* Add two tests to webserver_register_path_prefix_test.cpp: one that calls register_prefix with a null shared_ptr and asserts std::invalid_argument is thrown, and one that calls register_prefix twice with the same path and asserts the second call throws. These are low-cost tests that pin the invariants on the new API surface without duplicating the broader coverage already present in ws_start_stop.

33. [ ] **test-quality-reviewer** | `test/unit/webserver_register_path_prefix_test.cpp:270` | missing-test
   The test unregister_resource_alias_handles_both_kinds (line 270) verifies that unregister_resource removes both a prefix and an exact registration but does NOT verify that unregister_path is selective — i.e., that unregister_path on a path that also has a prefix registration leaves the prefix registration intact (and vice versa). This cross-kind selectivity is a key invariant of the two-registration-kinds design introduced in TASK-024.
   *Recommendation:* Add a test that registers both register_path('/q', r1) and register_prefix('/q', r2) on the same path, calls unregister_path('/q'), and then verifies that GET /q/child still returns 200 (the prefix route survived) while GET /q returns 404 or the prefix response depending on implementation semantics. This pins the isolation between the two registration kinds.
