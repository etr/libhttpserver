# Unworked Review Issues

**Run:** 2026-05-11 12:39:49
**Task:** TASK-029
**Total:** 27 (0 critical, 6 major, 21 minor)

> Cleanup note (2026-05-11): the original auto-generated file contained 10
> entries (3 critical, 7 major) emitted by spec-alignment-checker iter 1,
> which had inspected the wrong base (master) instead of the merged TASK-029
> branch and wrongly reported the rename as not implemented. Those entries
> were removed after independent verification by every other reviewer
> (code-quality, test-quality, security, architecture, code-simplifier,
> performance) and by the acceptance-criteria greps. Absolute worktree
> paths from the deleted `.worktrees/TASK-029/` were also normalized to
> repo-relative paths.

## Major

1. [ ] **code-quality-reviewer** | `src/webserver.cpp:1097` | code-elegance
   block_ip() has a redundant unconditional insert after the conditional branch. The logic at lines 1100-1106 is: `if (it != end && t_ip.weight() < it->weight()) { erase; insert; } else { insert; }`. Both branches call `impl_->bans.insert(t_ip)`, so the insert is always executed. The conditional branch only additionally erases the existing entry when the new IP has lower weight (i.e. is a wildcard replacing a more-specific entry). The 'no entry found' case also falls into the else branch and inserts, which is correct, but inserting into a `std::set` when the element already exists (when `t_ip.weight() >= it->weight()`) silently no-ops. The intent — replace only when new entry is less specific — is correct in outcome but the code reads as if it unconditionally inserts when the weight comparison is false, obscuring that the set's uniqueness property makes the second insert harmless. A reader unfamiliar with std::set semantics will be confused.
   *Recommendation:* Restructure the logic to make intent explicit: handle the three distinct cases separately — (1) no existing entry: insert; (2) existing entry with higher weight (more specific already present, new one less specific): no-op; (3) existing entry with lower weight: erase then insert. This eliminates the always-present `else { insert; }` that currently conflates cases (1) and (2).

2. [ ] **code-quality-reviewer** | `src/webserver.cpp:1101` | code-elegance
   block_ip's conditional is always a superset of the else branch: both paths call impl_->bans.insert(t_ip), so the if-branch only adds an erase before inserting. The else always inserts even when the key already exists (std::set::insert is idempotent on equal keys), meaning a higher-weight (more-specific) existing entry is silently kept when a lower-weight wildcard is added a second time, but the else branch will also no-op correctly. However the comment on the block_specific_then_wildcard test says 'lower weight replaces higher', which is exactly what the if-branch does — yet when the wildcard is added first and a more-specific IP is added second, the code falls to the else branch and insert() on a std::set is a no-op because ip_representation equality is key-based. This means the wildcard blocks are not superseded by specific IPs in the else path. The logic is incomplete: it only replaces when the new entry has lower weight than the existing one, and silently no-ops (keeps the existing higher-weight entry) when the new entry has higher weight.
   *Recommendation:* Invert the condition or add a symmetric branch: if the new entry has higher weight than the existing one, erase the old and insert the new. The current code only handles the wildcard-to-specific upgrade direction, not the specific-to-wildcard downgrade direction (the erase+insert inside the if fires when t_ip.weight() < it->weight(), i.e. the new entry is LESS specific). Verify the intended semantics against the test comments and fix whichever branch is wrong.

3. [ ] **code-quality-reviewer** | `src/webserver.cpp:246` | code-readability
   stop_and_wait() is documented in the header (webserver.hpp line 337) and README (line 547) as stopping the server AND waiting for in-flight handlers to complete before returning, which distinguishes it from stop(). The implementation body is just `stop();` — a single call with no additional waiting or join semantics. If MHD_stop_daemon() already blocks until all connections are drained, the implementation is correct but the doc comment is misleading ('wait for in-flight handlers' implies something beyond what stop() guarantees). If MHD_stop_daemon() does NOT wait, then stop_and_wait() fails to fulfil its contract.
   *Recommendation:* Clarify the implementation: if MHD_stop_daemon() already provides the wait-for-in-flight guarantee, update the doc comment on stop_and_wait() to say 'delegates to stop(), which internally calls MHD_stop_daemon() — a blocking call that waits for all connections to close.' If it does not, add the missing quiesce/join logic. Either way, the current silence makes the function look like an alias with no additional value.

4. [ ] **code-quality-reviewer** | `src/webserver.cpp:246` | code-elegance
   stop_and_wait() is documented in the header (webserver.hpp line 336) as 'Stop the webserver and wait for in-flight handlers to complete before returning' — a semantically stronger guarantee than plain stop(). However, the implementation at src/webserver.cpp:246-248 is literally `void webserver::stop_and_wait() { stop(); }` — it delegates unconditionally to stop(), which calls MHD_stop_daemon and returns. MHD_stop_daemon does drain in-flight handlers before returning, so the behaviour is actually correct, but the doc/code mismatch is misleading: readers will look for explicit waiting logic (a pthread_join, a condition variable, an MHD quiesce step) and find none. The distinction from stop() therefore exists only through MHD_stop_daemon's internal behaviour, which is not captured in any comment.
   *Recommendation:* Add an inline comment inside stop_and_wait() explaining that the wait guarantee is provided by MHD_stop_daemon's blocking behaviour, not by additional synchronisation in this wrapper. Alternatively, if 'wait for in-flight handlers' is meant to be a stronger contract than what stop() supplies today, the implementation must be updated to provide it (e.g., quiesce then MHD_stop_daemon). The current wrapper is not wrong but is undocumented in its intent.

5. [ ] **code-simplifier** | `src/webserver.cpp:1097` | code-structure
   block_ip has a duplicated impl_->bans.insert(t_ip) call in both branches of the if/else. The condition only guards whether to erase first; the insert is unconditional in both paths. The else branch is therefore dead-code equivalent to the then-branch minus the erase, making the structure misleading.
   *Recommendation:* Flatten to: erase only when the weight condition is met, then always insert. E.g.:
  if (it != impl_->bans.end() && t_ip.weight() < it->weight())
      impl_->bans.erase(it);
  impl_->bans.insert(t_ip);
This removes the duplicated insert and makes the intent — 'replace if the new entry is more specific' — immediately visible.

6. [ ] **code-simplifier** | `src/webserver.cpp:1097` | code-structure
   block_ip contains duplicate impl_->bans.insert(t_ip) calls: one inside the erase branch and one in the else branch. The unconditional insert can be hoisted after the conditional erase, eliminating the duplication.
   *Recommendation:* Restructure as: if (it != impl_->bans.end() && t_ip.weight() < it->weight()) { impl_->bans.erase(it); } impl_->bans.insert(t_ip); — one insert, no duplication.

## Minor

7. [ ] **architecture-alignment-checker** | `README.md:943` | pattern-violation
   Section heading '## IP Blacklisting and Whitelisting' and the TOC entry at line 55 still use the v1 'blacklisting/whitelisting' vocabulary. PRD §3.7 and OQ-004 collapse to block_ip/unblock_ip (a blocking model, not a blacklist/whitelist dichotomy), so the section heading is inconsistent with the renamed API surface. The body text at lines 949-950 correctly uses block_ip/unblock_ip.
   *Recommendation:* Rename the section heading and TOC entry to 'IP Blocking' (or 'IP Block List') to match the canonical v2.0 verb pair block_ip/unblock_ip and the intent of PRD §3.7.

8. [ ] **architecture-alignment-checker** | `README.md:985` | pattern-violation
   The block_ip code example at line 985 calls ws.register_resource('/hello', &hwr) using the deprecated raw-pointer v1 form. The architecture doc (04-components/webserver.md) lists register_path/register_prefix as the canonical v2.0 registration interface. This example was not changed by TASK-029 but appears inside the section being modified (IP Blacklisting and Whitelisting), making it a visible inconsistency in the updated documentation.
   *Recommendation:* Update the example to use register_path('/hello', std::make_shared<hello_world_resource>()) to match the v2.0 API surface.

9. [ ] **code-quality-reviewer** | `README.md:55` | code-readability
   The README still uses the terms 'IP Blacklisting and Whitelisting' in the TOC entry (line 55), section heading (line 943), subsection heading (line 509), and example heading (line 964). The public API now exposes block_ip/unblock_ip with no 'whitelist' concept at all (the allow-list half was dropped). Retaining 'Blacklisting/Whitelisting' as a section title is misleading for new users consulting the docs, and the section body (line 944) still says 'blacklist and whitelist IP addresses' in its opening sentence.
   *Recommendation:* Rename the section and all cross-references to 'IP Blocking' or 'IP Block List' to match the renamed API. Update line 944's prose accordingly. The example at line 985 already uses register_resource (deprecated) instead of register_path — consider correcting that as well while touching the section.

10. [ ] **code-quality-reviewer** | `README.md:55` | code-readability
   The table of contents entry at line 55 reads '* [IP Blacklisting and Whitelisting](#ip-blacklisting-and-whitelisting)', the section heading at line 943 reads '## IP Blacklisting and Whitelisting', the inline reference at line 509 reads '### IP Blacklisting/Whitelisting', and the example caption at line 964 reads '#### Example of IP Whitelisting/Blacklisting'. These all use the deprecated terminology. The public API now uses block_ip/unblock_ip exclusively (lines 949-950 correctly document the new names), making the section framing inconsistent with the API surface. The example body at lines 977-989 correctly uses block_ip.
   *Recommendation:* Rename the README section headings and TOC entry to 'IP Blocking' or 'IP Access Control' to align with the block_ip/unblock_ip naming. The example caption should read 'Example of IP Blocking'. This removes the only remaining use of blacklist/whitelist vocabulary from user-facing documentation.

11. [ ] **code-quality-reviewer** | `README.md:985` | code-readability
   The IP Blocking example in README (line 985) calls ws.register_resource("/hello", &hwr) using the deprecated raw-pointer form. The new preferred API is register_path with a smart pointer, and the existing register_resource overload was changed to only accept unique_ptr/shared_ptr in v2.0.
   *Recommendation:* Update the README example to use register_path with make_shared or make_unique to match the actual v2.0 API and avoid confusing readers who try to compile the snippet.

12. [ ] **code-quality-reviewer** | `test/integ/ban_system.cpp:148` | test-coverage
   The test reject_policy_neither_allowed_nor_blocked verifies that a REJECT-policy server denies a request when the IP is in neither list. However, there is no test confirming that unblock_ip on an IP that was never blocked is a true no-op (does not throw or corrupt state). The doc comment on unblock_ip says 'Idempotent: removing an IP that is not currently blocked is a no-op.'
   *Recommendation:* Add a small test case that calls ws.unblock_ip("127.0.0.1") without a preceding block_ip and verifies the server continues to serve normally. This guards against a future regression in the erase-on-empty-set path.

13. [ ] **code-simplifier** | `README.md:55` | naming
   The README TOC entry (line 55) still reads '* [IP Blacklisting and Whitelisting](#ip-blacklisting-and-whitelisting)', and the section header (line 943) is '## IP Blacklisting and Whitelisting'. The public API now uses block_ip / unblock_ip with no whitelisting surface. The terminology 'blacklist / whitelist' is both outdated and inconsistent with the new canonical verbs.
   *Recommendation:* Rename the TOC entry, the section header, and the anchor to 'IP Block List' (matching the doc body already updated at lines 949-950 which correctly says 'block list'). Also rename the subsection at line 509 from '### IP Blacklisting/Whitelisting' and line 510's body text accordingly.

14. [ ] **code-simplifier** | `README.md:55` | naming
   README still uses the old 'Blacklisting and Whitelisting' / 'blacklist/whitelist' terminology in the table of contents (line 55), a startup-options subsection heading (line 509), section prose (lines 510, 943-944), and an example subsection heading (line 964). The public API now uses block_ip/unblock_ip and the section should be renamed to reflect 'IP Block List' or similar neutral language.
   *Recommendation:* Rename the TOC entry, section headings, and prose to use 'IP block list / block and unblock' language consistent with the new public API names block_ip and unblock_ip.

15. [ ] **code-simplifier** | `README.md:964` | naming
   The example sub-section heading is '#### Example of IP Whitelisting/Blacklisting' (line 964). The example itself correctly uses block_ip, but the heading still references the removed whitelist concept.
   *Recommendation:* Rename to '#### Example of IP Blocking' or '#### Example of block_ip / unblock_ip' to match the canonical v2 API names.

16. [ ] **code-simplifier** | `src/webserver.cpp:246` | code-structure
   stop_and_wait() is a one-liner wrapper that only calls stop(). Its doc comment promises 'wait for in-flight handlers to complete before returning', but the implementation delegates unconditionally to stop(), which may or may not provide that guarantee depending on the MHD shutdown mode. The wrapper adds a named concept without any additional behavior beyond stop().
   *Recommendation:* Either add the actual wait-for-completion logic inside stop_and_wait() (if the guarantee is real and stop() alone does not provide it), or consolidate the two by removing the wrapper and having callers use stop() directly — ensuring the doc comment on stop() is updated to describe the full guarantee. Do not keep a named abstraction that implies stronger semantics than what it implements.

17. [ ] **housekeeper** | `specs/tasks/M5-routing-lifecycle/TASK-029.md:null` | documentation-stale
   The commit message for ec82d90 states all acceptance criteria were verified (grep checks, make check 40/40 pass, cpplint clean). This verification evidence is not captured anywhere in the task file, but that is not a mandatory convention in this project — just an observation.
   *Recommendation:* No action required; the commit message provides sufficient audit trail.

18. [ ] **performance-reviewer** | `src/webserver.cpp:1099` | memory-allocation
   block_ip constructs a temporary std::string from std::string_view (std::string{ip}) to satisfy ip_representation's constructor, which is the expected cost. However, the erase+insert branch (lines 1102-1103) performs two separate set mutations when a single erase followed by a hint-insert would suffice. More importantly, when t_ip is NOT already present (the common path), the else branch at line 1105 always attempts insert even if the weight condition is not met — but the condition on line 1101 already gates correctly, so the insert in the else also covers the not-found case. The net result is one redundant pair of set lookups (find + insert) on the common first-insert path; a try_emplace-style insert-or-update idiom would reduce this to a single lookup.
   *Recommendation:* Use insert_or_assign / extract-then-reinsert to replace the find+erase+insert triple with a single set operation: auto [it, inserted] = impl_->bans.insert(t_ip); if (!inserted && t_ip.weight() < it->weight()) { auto node = impl_->bans.extract(it); node.value() = t_ip; impl_->bans.insert(std::move(node)); }. This eliminates a redundant tree traversal on each call.

19. [ ] **performance-reviewer** | `src/webserver.cpp:1111` | memory-allocation
   unblock_ip constructs a temporary std::string from std::string_view (std::string{ip}) only to pass it to ip_representation's constructor. This allocation is unavoidable given ip_representation's interface, but it is worth noting as the only allocation on the hot-path erase. If ip_representation gains a std::string_view constructor in a future task, the copy can be eliminated.
   *Recommendation:* No action needed in this task; document as a future improvement if ip_representation is ever extended with a string_view constructor.

20. [ ] **security-reviewer** | `src/webserver.cpp:1097` | input-validation
   block_ip() and unblock_ip() accept std::string_view and immediately copy it into a std::string before passing to ip_representation (std::string{ip}). This is correct and avoids the lifetime hazard called out in the task definition. However, ip_representation's constructor accepts the resulting std::string by value and the internal bans set stores ip_representation objects; no validation is performed on the IP string before insertion (e.g. malformed strings such as empty string, overly long strings, or non-IP characters). A caller who passes a malformed IP will silently insert an unmatched entry into the bans set, burning memory but having no security effect. CWE-20.
   *Recommendation:* Consider adding input validation in block_ip()/unblock_ip() that rejects obviously malformed IP strings (empty string, length > 43 chars for IPv6, characters outside [0-9a-fA-F.:*]) and throws std::invalid_argument. This hardens the security control against misuse at the call site.

21. [ ] **security-reviewer** | `src/webserver.cpp:1327` | broken-access-control
   The internal allowances set (impl_->allowances) is still read in policy_callback and governs the REJECT-policy allow-list path: if default_policy==REJECT and is_allowed is false, the connection is denied. The public API no longer exposes allow_ip/disallow_ip, so the allowances set is permanently empty for all callers in v2.0. Under REJECT policy this means every connection is denied regardless of intent — callers who relied on REJECT + allow_ip to whitelist specific IPs have their whitelist silently removed with no API replacement. This is not a bug in the sense of a new vulnerability, but it is a semantic regression for the REJECT-policy use-case: the security control (IP allowlist) was removed from the public surface without a documented migration path.
   *Recommendation:* Add a note in the public documentation (and ideally a comment above policy_callback) stating that default_policy(REJECT) in v2.0 unconditionally blocks all connections because the allowlist path is no longer populated from the public API. If selective allow under REJECT semantics is still a desired feature, expose block_ip/unblock_ip under a separate allowlist pair (e.g. allow_ip/unallow_ip) in a subsequent task. Leaving the dead allowances set in the codebase without documentation is a latent confusion vector.

22. [ ] **security-reviewer** | `src/webserver.cpp:246` | insecure-design
   stop_and_wait() is documented as 'wait for in-flight handlers to complete before returning', but its implementation is a direct alias for stop(), which calls MHD_stop_daemon(). MHD_stop_daemon() does block until active connections are closed, so the net behaviour matches the promise — however the difference from stop() is zero: both implementations are identical. The name implies an additional wait guarantee that is not visibly distinct in the implementation, which could mislead future maintainers into weakening one of the two paths and introducing a race where the listener is torn down while handlers are still live (CWE-362).
   *Recommendation:* Either add a code comment at the stop_and_wait() definition that explicitly documents why MHD_stop_daemon() is already a blocking, draining call (so the name-vs-body discrepancy is visibly explained), or differentiate the two entrypoints in a follow-up task (e.g. stop() -> MHD_quiesce_daemon + async close; stop_and_wait() -> full MHD_stop_daemon). The current symmetry is safe but the comment gap is a maintenance hazard.

23. [ ] **spec-alignment-checker** | `src/httpserver/http_utils.hpp:82` | specification-gap
   The struct 'generateFilenameException' (a type name, not a method) matches the [a-z][A-Z] acceptance criterion grep. This is a pre-existing issue that predates TASK-029 and is out of scope for this task. The acceptance criterion text in the PRD specifies 'no public method names matching camelCase other than shoutCAST', and generateFilenameException is a type name, not a method. However the raw grep in the acceptance criteria would include it as a match.
   *Recommendation:* Track renaming of generateFilenameException to generate_filename_exception as a separate task under the API-NAM feature. TASK-029 should not be blocked on this pre-existing issue.

24. [ ] **spec-alignment-checker** | `src/httpserver/webserver.hpp:82` | acceptance-criteria
   The webserver(const create_webserver&) constructor still carries the comment '// Keeping this non explicit on purpose' and the NOLINT annotation at line 82. While TASK-029 notes this is 'covered in TASK-030', the NOLINT remains and the constructor is non-explicit, which conflicts with PRD-NAM-REQ-004. This is noted as out-of-scope for this task but the comment warrants attention.
   *Recommendation:* No action required for TASK-029 specifically; verify TASK-030 addresses PRD-NAM-REQ-004.

25. [ ] **test-quality-reviewer** | `test/integ/ban_system.cpp:148` | missing-test
   The REJECT-policy test (reject_policy_neither_allowed_nor_blocked) only verifies that an unlisted IP is refused, but does not exercise unblock_ip under a REJECT policy. The now-removed allow_ip/disallow_ip tests did drive that internal allow-list path; the comment at line 82 acknowledges it is unreachable from the public API, but leaves no test for the observable REJECT + block_ip combination (e.g. block a specific IP that is not in any list — same observable outcome, but confirms block_ip is a no-op on the deny side when policy is already REJECT).
   *Recommendation:* Add a test that sets default_policy(REJECT), then calls block_ip on an already-refused IP and confirms the connection is still refused, ensuring block_ip is safe to call under REJECT and does not accidentally open access.

26. [ ] **test-quality-reviewer** | `test/integ/ban_system.cpp:239` | missing-test
   There is no test for unblock_ip on an IP that was never blocked (idempotency / no-crash guarantee). The header doc for unblock_ip explicitly states it is a no-op in that case, but that contract is untested.
   *Recommendation:* Add a one-line test (or extend an existing test) that calls ws.unblock_ip("10.0.0.1") without a prior block_ip and asserts the server is still reachable.

27. [ ] **test-quality-reviewer** | `test/integ/ban_system.cpp:90` | multiple-concerns
   accept_default_block_blocks covers three distinct behaviors in one test body: (1) initial request succeeds, (2) after block_ip request is refused, (3) after unblock_ip request succeeds again. Each phase uses its own curl handle and assertion block. This is acceptable given integration-test conventions but each scenario could be named and isolated for faster diagnosis.
   *Recommendation:* Consider splitting into accept_default_allows, block_ip_denies_connection, and unblock_ip_restores_access if debugging time becomes an issue; not a blocker.
