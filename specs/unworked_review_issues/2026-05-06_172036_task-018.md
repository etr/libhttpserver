# Unworked Review Issues

**Run:** 2026-05-06 17:20:36
**Task:** TASK-018
**Total:** 28 (0 critical, 1 major, 27 minor)

## Major

1. [ ] **security-reviewer** | `src/http_request.cpp:812` | data-integrity
   get_arg_flat() returns a string_view that aliases a pmr::string inside impl_->unescaped_args (line 818: 'return it->second[0];'). If unescaped_args is ever mutated after this call returns but before the caller finishes using the view (e.g. a concurrent request handler sharing the same http_request*, or test code that calls set_arg after get_arg_flat), the view dangles. The public header comment lists get_arg_flat() in the affected-getters list (CWE-416) but the comment is the only guard. The same applies to the string_views in get_args_flat() (line 849: 'ret[key] = val[0]') which aliases unescaped_args values directly. No compile-time or runtime enforcement prevents a caller from storing the view past the mutation point.
   *Recommendation:* No code change is strictly required for single-threaded handler use, which is the documented contract. However, add a [[nodiscard]] attribute to get_arg_flat() and get_args_flat() so callers are nudged to consume the result immediately. Also consider a debug-mode assertion that args_populated is true and no set_arg mutation can occur after population (i.e., once args_populated is set, lock the map). For the multi-value get_arg() path the design is safe because it copies into http_arg_value; document that divergence explicitly.

## Minor

2. [ ] **architecture-alignment-checker** | `src/httpserver/http_request.hpp:95` | pattern-violation
   The class-level Doxygen block (TASK-016 era) still says 'All std::string_view values returned by this class are only valid within the handler's call frame', while all TASK-018 per-method @note comments say 'valid for the lifetime of the request object'. The task spec action item explicitly says 'Document lifetime: the view is valid for the lifetime of the request object'. The two statements are not contradictory in practice (the handler invocation is the request lifetime on the live path), but the class-level wording is more restrictive than necessary and is now inconsistent with the per-method documentation added in this task.
   *Recommendation:* Update the class-level string_view lifetime contract block to use the same 'valid for the lifetime of the request object (typically the duration of the handler invocation)' phrasing used in the per-method @note comments, so the two levels of documentation agree.

3. [ ] **code-quality-reviewer** | `src/http_request.cpp:138` | code-readability
   get_connection_value() uses a lambda immediately invoked to produce a map pointer, which is a slightly unusual pattern in this codebase. The lambda result is stored in a raw pointer `const auto* map`, with the actual lookup following immediately after. The indirection adds cognitive overhead for a simple switch on four enum values.
   *Recommendation:* Flatten the immediately-invoked lambda into a plain switch/case that assigns `map` directly, matching the style already used in ensure_headerlike_cache(). This is already done in the ensure_headerlike_cache helper; applying the same pattern here removes the inconsistency.

4. [ ] **code-quality-reviewer** | `src/http_request.cpp:817` | code-elegance
   get_arg_flat() silently returns only the first value (index 0) when the key is found in unescaped_args, but falls back to get_connection_value() on a miss instead of returning EMPTY directly. The fallback path calls MHD_lookup_connection_value after populate_args() has already run, creating a subtle dual-path lookup that could return stale or inconsistent data on the live-MHD path if args were partially populated.
   *Recommendation:* Return an explicit `http_request::EMPTY` (or `std::string_view{}`) on a miss rather than delegating to get_connection_value. The args should already be fully populated at that point via populate_args(); falling through to MHD is a redundant and confusing second layer.

5. [ ] **code-quality-reviewer** | `src/http_request.cpp:862` | code-elegance
   get_querystring() is declared noexcept but its body dereferences impl_->querystring through a std::unique_ptr with a custom deleter. If impl_ is null (default-constructed http_request), this is UB rather than a thrown exception, so noexcept is technically correct but the safety guarantee is implicit. The pattern differs from get_path/get_method/get_content/get_version which return direct std::string members and are trivially noexcept. A brief comment noting why the impl_ deref is safe here would clarify intent.
   *Recommendation:* Add a one-line comment above the return such as '// impl_ is always non-null on live requests; default-ctor is private.' to make the safety contract explicit at the call site.

6. [ ] **code-quality-reviewer** | `src/httpserver/http_request.hpp:269` | code-readability
   get_cookie's doc block is minimal compared to every other per-key getter in this file. It has only a @note but no @param or @return line, while get_header (line 261) and get_footer (line 273) both document their parameter and return value.
   *Recommendation:* Add '@param key the specific cookie to get the value from' and '@return the value of the cookie.' lines to align with the surrounding getter docs.

7. [ ] **code-quality-reviewer** | `src/httpserver/http_request.hpp:434` | code-readability
   The friend declaration `friend std::ostream &operator<< (std::ostream &os, http_request &r);` (line 434) takes the argument by non-const reference, but the free function defined at the bottom of the same header is declared as `std::ostream &operator<< (std::ostream &os, const http_request &r)` (line 584). The signatures differ in const-ness of the second parameter, which means the friend declaration does not actually befriend the free function that is implemented.
   *Recommendation:* Change the friend declaration to `friend std::ostream& operator<<(std::ostream& os, const http_request& r);` to match the implemented free function signature. As written, the non-const friend declaration is dead and the const free function is not a friend.

8. [ ] **code-quality-reviewer** | `test/unit/create_test_request_test.cpp:338` | test-coverage
   The missing_key_does_not_insert test verifies non-insertion via the container-cache size, which is an indirect proxy. It doesn't exercise the case where get_arg_flat falls through to get_connection_value (src/http_request.cpp line 820) on a live-connection path. This fallback path exists only for the MHD case but is untested by any test in the changed set.
   *Recommendation:* The current test is adequate for the test-request path. Consider a comment noting the MHD-path fallback in get_arg_flat is exercised by integration tests, or add a TODO tracking a future unit-test for that branch.

9. [ ] **code-quality-reviewer** | `test/unit/create_test_request_test.cpp:338` | test-coverage
   The missing_key_does_not_insert test verifies no-insertion by comparing container sizes before and after per-key misses. This is indirect; it relies on the container cache being built from the same underlying map. A miss that inserted into a different internal map (e.g., get_connection_value fallback inserting into headers_local) would not be caught by this check.
   *Recommendation:* Consider also asserting that get_header(missing) returns an empty string_view whose data() pointer equals http_request::EMPTY (or at minimum std::string_view{}.data()), providing a direct signal about the return value rather than only the size invariant. This is a belt-and-suspenders addition, not a blocking issue.

10. [ ] **code-quality-reviewer** | `test/unit/http_request_const_getters_test.cpp:46` | code-readability
   The type aliases 'h' and 'cref' are terse but reasonably clear in this sentinel context. However, 'cref' as a name could be confused with std::cref from <functional>. In a type-traits-heavy file, slightly more descriptive aliases would improve legibility.
   *Recommendation:* Consider renaming 'cref' to 'const_req_ref' or 'req_const_ref' to avoid any confusion with std::cref. Minor — acceptable as-is given the file's narrow purpose.

11. [ ] **code-simplifier** | `src/http_request.cpp:481` | code-structure
   populate_all_cert_fields() acquires the TLS session in two separate steps with an intermediate null check, but get_tls_session() already guards against a null connection internally. The split (has_tls_session → get_tls_session) at lines 481-487 adds an extra branch that mirrors logic already encapsulated in the helper.
   *Recommendation:* Call get_tls_session() directly and guard on its return value: `gnutls_session_t session = has_tls_session() ? get_tls_session() : nullptr;` — this collapses the two-step pattern into one and removes the separate `session = nullptr` initializer, making the flow easier to scan.

12. [ ] **code-simplifier** | `src/http_request.cpp:812` | code-structure
   get_arg_flat falls through to get_connection_value(key, MHD_GET_ARGUMENT_KIND) on a miss, but get_connection_value with MHD_GET_ARGUMENT_KIND is not handled in the switch inside get_connection_value (only HEADER/FOOTER/COOKIE kinds are switched). On the test path (connection_ == nullptr) the switch falls to the default nullptr branch and returns EMPTY anyway. On the live path MHD_lookup_connection_value is called with MHD_GET_ARGUMENT_KIND, which could return a value but the args have already been unescaped and stored in unescaped_args — so any value returned here would be the raw (non-unescaped) string. This creates a subtle behavioural inconsistency between the populated-args path and the fallback path that is hard to notice from the function body alone.
   *Recommendation:* Simplify the fallback to just `return EMPTY;` (or `return std::string_view{};`) instead of delegating to get_connection_value with a kind that has no consistent meaning here. The current fallback can return a raw un-unescaped value from MHD on a live connection, which contradicts the unescaping contract of the arg getters.

13. [ ] **code-simplifier** | `src/http_request.cpp:928` | naming
   The helper `to_std_string` defined inside an anonymous namespace (lines 928-931) only converts pmr::string to std::string and is called in four places directly below. Its name is generic enough that a future reader might not immediately know why it exists — the conversion is driven by the API contract, not an arbitrary choice.
   *Recommendation:* Add a brief one-line comment above the function (e.g. `// pmr::string → std::string: required because the public API still spells std::string for TLS cert fields.`) rather than renaming; the existing name is clear, but the 'why' helps maintainers not widen the usage accidentally.

14. [ ] **code-simplifier** | `src/httpserver/http_request.hpp:320` | code-structure
   The `@note (TASK-018)` tag in the get_querystring doc comment encodes a task-tracking reference into a user-facing API doc comment. Task numbers are internal implementation history, not part of the public API contract a downstream consumer needs to know.
   *Recommendation:* Remove the `@note (TASK-018)` prefix so the sentence reads simply: "The querystring is assembled eagerly at construction on the live MHD path, so the public reader is `noexcept`."

15. [ ] **code-simplifier** | `src/httpserver/http_request.hpp:434` | naming
   The free-function `operator<<` is declared twice with different signatures: once as a friend inside the class body (`friend std::ostream &operator<< (std::ostream &os, http_request &r);` — non-const reference) and once at namespace scope after the class (`std::ostream &operator<< (std::ostream &os, const http_request &r);` — const reference). These are two different overloads. The friend declaration does not match the namespace-scope declaration, so the friend injection is injecting a different (non-const) overload that has no definition, while the out-of-class const overload is not a friend. This is a pre-existing issue but is in a changed file and should be corrected: the friend declaration should use `const http_request&`.
   *Recommendation:* Change the friend declaration to `friend std::ostream& operator<<(std::ostream& os, const http_request& r);` to match the namespace-scope declaration and the definition in http_request.cpp, which accepts `const http_request&`.

16. [ ] **performance-reviewer** | `src/http_request.cpp:150` | memory-allocation
   get_connection_value constructs a temporary std::string(key) for map::find on the test-request path (connection_ == nullptr). The owning map is http::header_map keyed by std::string, so heterogeneous lookup is not available without a transparent comparator, forcing the allocation. On the live MHD path there is no allocation (MHD_lookup_connection_value is called directly with key.data()), so this only affects test requests. Repeated per-key misses in tests will each pay one small-string allocation per call.
   *Recommendation:* Switch headers_local / footers_local / cookies_local from http::header_map (std::map<std::string,...>) to a transparent-comparator variant (e.g. std::map<std::string, std::string, http::arg_comparator> if arg_comparator already provides is_transparent, or add a string_view-comparable custom comparator). That makes map::find(std::string_view) heterogeneous and removes the temporary std::string construction on every test-path per-key lookup.

17. [ ] **performance-reviewer** | `src/http_request.cpp:797` | memory-allocation
   get_arg copies the found pmr::vector<pmr::string> values element-by-element into a freshly heap-allocated http_arg_value::values (std::vector<std::string_view>). Each call with a hit allocates a new vector and fills it from the pmr-backed storage. For arguments with many values this is O(n) allocation on a hot read path. The fix is a non-trivial API change (get_arg already returns by value for multi-value semantics), but the copy is avoidable.
   *Recommendation:* If get_arg is called frequently, consider returning const http_arg_value& by reference from a pre-built view in args_view_cached_ (similar to how get_args() works), or at least reserve the vector capacity with arg.values.reserve(it->second.size()) before the loop (already done -- this is acceptable). Mark as low priority since the reserve is already present and the cost is proportional only to the number of values for that specific key.

18. [ ] **performance-reviewer** | `src/http_request.cpp:812` | missing-caching
   get_arg_flat falls through to get_connection_value(key, MHD_GET_ARGUMENT_KIND) when the key is absent from unescaped_args. MHD_GET_ARGUMENT_KIND is not one of the three kinds that ensure_headerlike_cache handles (HEADER / FOOTER / COOKIE), so the call bypasses the one-time-fill cache and issues a raw MHD_lookup_connection_value call on every miss. On the live path this is a libmicrohttpd O(n) linear scan of the connection's argument list on every call with a missing key, where the cache path would have been O(log n) after first fill.
   *Recommendation:* Either extend ensure_headerlike_cache to cover MHD_GET_ARGUMENT_KIND (so get_arg_flat can use the args view-cache on miss), or simply remove the MHD fallback since populate_args() already iterates MHD_GET_ARGUMENT_KIND and stores all arguments in unescaped_args -- if the key is absent after populate_args(), MHD will also not find it. Removing the fallback makes the miss path return http_request::EMPTY directly after the find() and eliminates the redundant MHD scan.

19. [ ] **performance-reviewer** | `src/http_request.cpp:845` | algorithmic-complexity
   get_args_flat reconstructs a std::map<string_view, string_view> from scratch on every call, iterating all of unescaped_args. Unlike get_args() there is no cache for the flat view, so repeated callers each pay O(n log n) allocation + construction. For a request with many arguments and a handler that calls get_args_flat() more than once this is unnecessary repeated work.
   *Recommendation:* Add a mutable flat_args_view_cached_ cache (similar to args_view_cached_) in http_request_impl and populate it once on the first get_args_flat() call. Alternatively, document that callers should capture the result by value once and reuse it. The latter is simpler and avoids adding another cache flag.

20. [ ] **security-reviewer** | `src/http_request.cpp:156` | input-validation
   get_connection_value() passes key.data() directly to MHD_lookup_connection_value() (line 156). std::string_view is not guaranteed to be null-terminated, so if a caller passes a non-terminated view (e.g. a substring view), MHD will read past the intended end of the key, potentially seeing a different (or garbage) key. The public API accepts std::string_view key arguments (new in this task), making this a latent OOB-read risk when non-terminated views are supplied.
   *Recommendation:* Convert key to a null-terminated string before passing to MHD: 'std::string key_str(key); MHD_lookup_connection_value(connection_, kind, key_str.c_str())'. This is a one-time allocation per lookup and preserves correctness. Alternatively, add a static_assert or documentation note that callers must supply null-terminated string_views (e.g. string literals or data() from std::string), though the API signature does not enforce this.

21. [ ] **security-reviewer** | `src/http_request.cpp:205` | data-integrity
   ensure_headerlike_cache() and populate_args() use a plain non-atomic bool guard (*built / args_populated) to implement a lazy-fill pattern on mutable members accessed through a const pointer. If two threads invoke const getters concurrently on the same http_request (which the library does not currently prohibit at the public API level), there is a data race on the bool and on the cache map itself. The bool is not std::atomic and the map write is not synchronized. This is CWE-362 (Race Condition). The existing single-threaded handler model avoids the race in practice, but the public API exposes const methods, which users commonly assume are thread-safe.
   *Recommendation:* Document explicitly in the class-level Doxygen (http_request.hpp, the string_view lifetime contract block) that http_request instances are not thread-safe and must not be accessed from multiple threads simultaneously. Alternatively, protect the lazy-fill flag with std::atomic<bool> and use a std::once_flag / std::call_once pattern for the map fill. Given the performance focus of this library the documentation approach is likely the right trade-off; the comment at line 96-131 of the header is already strong -- extend it with a thread-safety note.

22. [ ] **security-reviewer** | `src/http_request.cpp:845` | data-integrity
   get_args_flat() (line 845-852) returns a by-value std::map<string_view, string_view>. The string_view keys and values alias pmr::string storage inside impl_->unescaped_args. The returned map is documented nowhere in the header comment's 'affected getters' list (line 109), meaning callers may not know those views carry the same dangling risk as the listed getters. If a caller stores the returned map past the request lifetime, all views become dangling (CWE-416).
   *Recommendation:* Add get_args_flat() to the list of affected getters in the class Doxygen block (http_request.hpp lines 109-111). The fix is documentation-only; the allocation pattern is intentional and correct for within-handler use.

23. [ ] **security-reviewer** | `src/httpserver/detail/http_request_impl.hpp:284` | input-validation
   The arguments_accumulator DoS guard defaults (DEFAULT_MAX_ARGS_COUNT=64, DEFAULT_MAX_ARGS_BYTES=65536) are used on the test-request path (populate_args, line 322-329 of http_request.cpp) without an explicit accumulator configuration. The populate_args() call constructs a default arguments_accumulator (line 322) which picks up the compile-time defaults. This is correct for production but the comment at line 276-278 of the impl header says 'the webserver hot path sets these from connection_state or a compile-time constant once the create_webserver API exposes them (TODO(M5))'. Until M5 lands, the limits are the global defaults with no per-webserver configurability, meaning an operator cannot tighten or relax them without a code rebuild.
   *Recommendation:* This is a known deferred item (TODO M5). No change required in this task. Verify the TODO is tracked and that the defaults are conservative enough for the deployed threat model (64 args, 64 KiB total seems reasonable for most APIs).

24. [ ] **spec-alignment-checker** | `src/httpserver/http_request.hpp:241` | specification-gap
   get_args_flat() is a container-level getter that returns 'const std::map<std::string_view, std::string_view, http::arg_comparator>' by value (not by const&). PRD-REQ-REQ-001 requires 'get_args' returns const&, and the existing get_args() correctly does so, but get_args_flat() still returns by value. This pre-dates TASK-018 and is outside the strict task scope, but the TASK-018 scope statement says 'get_arg_flat as const string_view' (the per-key getter) while the container variant get_args_flat() remains a by-value return. This is a minor scope ambiguity: the task spec does not explicitly address the container-level variant.
   *Recommendation:* Confirm that get_args_flat() is intentionally out of scope for TASK-018 and tracked by a separate task (consistent with PRD-REQ-REQ-001 which targets get_args, get_headers, get_footers, get_cookies, get_path_pieces, get_files but does not name get_args_flat). If so, add a TODO comment referencing the follow-up task.

25. [ ] **spec-alignment-checker** | `src/httpserver/http_request.hpp:286` | action-item
   Action item states 'Document lifetime: the view is valid for the lifetime of the request object'. get_arg() returns http_arg_value (a struct with std::vector<std::string_view> values). The class-level doc block lists 'get_arg_flat()' in the lifetime-restricted getters but does not explicitly list 'get_arg()' even though the string_view values inside http_arg_value carry the same arena-lifetime constraint. The action item requires documenting lifetime on all the per-key getters that return string_view-based values.
   *Recommendation:* Add a @note to get_arg()'s doxygen comment or to the class-level contract block making explicit that the string_view values inside the returned http_arg_value alias the request's arena storage and must not outlive the handler invocation.

26. [ ] **test-quality-reviewer** | `test/unit/create_test_request_test.cpp:112` | missing-test
   get_arg_flat is only tested against a single-value arg. The task's acceptance criterion for get_arg_flat is 'if the arg key has more than one value, only one is returned', but no test exercises a multi-value arg through get_arg_flat to confirm first-value semantics are preserved.
   *Recommendation:* Add a test case that builds a request with .arg("k","first").arg("k","second"), then asserts get_arg_flat("k") == "first". This directly covers the documented disambiguation behaviour.

27. [ ] **test-quality-reviewer** | `test/unit/create_test_request_test.cpp:355` | logic-in-test
   The missing_key_does_not_insert test uses a for-loop (i = 0..4) to repeat five identical miss assertions. Loop logic in tests obscures intent and makes failures harder to attribute to a specific iteration.
   *Recommendation:* Either remove the loop and assert once (one miss is sufficient to prove the invariant), or use the littletest parameterisation pattern if available. A single miss plus the container-size post-check is equally rigorous and removes the loop.

28. [ ] **test-quality-reviewer** | `test/unit/http_request_const_getters_test.cpp:130` | missing-test
   The compile-time sentinel does not assert that the per-key getters (get_header / get_footer / get_cookie / get_arg_flat) are declared const (i.e. that calling them on const http_request& does not require a mutable object). std::is_invocable_v only verifies the function can be called with those argument types but does not distinguish const vs non-const overloads when both could theoretically exist. The existing checks are correct but a complementary negative static_assert (confirming the method pointer is const-qualified) would make the contract airtight.
   *Recommendation:* Add static_asserts of the form static_assert(std::is_same_v<decltype(&h::get_header), std::string_view (h::*)(std::string_view) const>, ...) for each per-key getter to lock the const-qualifier into the type signature, not just the invocability.
