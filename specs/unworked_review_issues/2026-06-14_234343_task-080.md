# Unworked Review Issues

**Run:** 2026-06-14 23:43:43
**Task:** TASK-080
**Total:** 39 (0 critical, 2 major, 37 minor)

## Major

1. [x] **spec-alignment-checker** | `test/PERFORMANCE.md:243` | acceptance-criteria
   Acceptance criterion: 'The test has not flaked in the last 50 CI runs across the matrix.' PERFORMANCE.md §Acceptance criterion verification explicitly states this cannot be enforced at PR-time and the proxy is: (1) a local 10-round Apple Silicon sweep showing worst p95 of 13.4× vs 20× gate, and (2) post-merge monitoring. The criterion is therefore unmet at time of merge and deferred to post-merge monitoring. The task definition provides no waiver for this deferral.
   *Recommendation:* Mark this acceptance criterion as deferred with explicit tracking (e.g., a follow-up ticket or milestone note). The current PERFORMANCE.md text is appropriate as documentation but the acceptance criterion itself is technically unmet at merge.
   *Worked 2026-07-07 (chore/unworked-major-issues):* Criterion formally marked DEFERRED with tracking: annotation in TASK-080.md acceptance criteria + expanded PERFORMANCE.md §'Acceptance criterion verification — 50-run stability'.

2. [x] **spec-alignment-checker** | `test/integ/threadsafety_stress.cpp:1056` | task-goal
   The task goal states 'restore the gate to 10× warmup-median'. The implementation sets a 20× p95 gate (LT_CHECK_LT(worst_p95, worst_baseline * 20)). The change in statistic (warmup-median → p95) and the doubled multiplier (10× → 20×) both diverge from the primary goal statement. The escape clause in both the task and acceptance criterion ('or the tightest stable floor') does apply, but the measurement backing it is a single Apple Silicon local run (worst observed 13.4×), not a multi-lane CI characterisation across the full matrix. No Linux/CI runner sweep is documented.
   *Recommendation:* The documentation rationale is solid for the local macOS runner, but the 50-run-across-matrix acceptance criterion cannot be verified at PR-merge time per the PERFORMANCE.md itself. Either document the Linux/GHA runner noise floor with a sweep result (even one CI artifact) to validate 20× is also sufficient on Linux, or explicitly call out that the matrix sweep is post-merge monitoring.
   *Worked 2026-07-07 (chore/unworked-major-issues):* PERFORMANCE.md now documents the missing Linux/GHA sweep as a known characterisation gap and directs that a Linux-lane flake be treated as the missing sweep surfacing, not noise.
## Minor

3. [ ] **architecture-alignment-checker** | `test/integ/threadsafety_stress.cpp:1056` | adr-violation
   The gate threshold is 20x warmup median rather than the 10x the task initially targeted. DR-008's Verification section specifies '16 concurrent curl clients for 60 seconds' but does not specify a latency ratio; the 10x threshold comes from PRD §3.6 and TASK-056, not DR-008 itself. The implementation uses 20x, which deviates from the task's primary goal of restoring a 10x gate. The task's own acceptance criteria allow 'the documented tightest stable floor' as an alternative, and test/PERFORMANCE.md documents the measurement data (worst observed p95/baseline ratio 13.4x on Apple Silicon) supporting that 10x is genuinely infeasible on the shared runner hardware without rewriting the locking strategy. The deviation is thus within the task's acceptance criteria and is fully documented.
   *Recommendation:* No code change required. The deviation from 10x to 20x is within the task's own acceptance criteria ('or the documented tightest stable floor with measurement data') and is backed by empirical data in test/PERFORMANCE.md. This finding is informational only.

4. [ ] **code-quality-reviewer** | `test/PERFORMANCE.md:152` | code-readability
   The PERFORMANCE.md section header reads 'Methodology — threadsafety_stress adversarial_segments latency gate' but the sub-test name in code is `adversarial_segments_registration_no_latency_spike`. The short-form used in the doc is fine for prose but the discrepancy could confuse a reader searching for the section by test name.
   *Recommendation:* Minor: add a parenthetical '(sub-test C: `adversarial_segments_registration_no_latency_spike`)' on the section header or in the first sentence so grep-by-function-name lands in the right doc section.

5. [ ] **code-quality-reviewer** | `test/PERFORMANCE.md:176` | code-readability
   The gate table lists 'Threshold ratio' as '< 20× warmup median' but the 'Statistic' row says 'p95 of overall samples vs median of first quarter'. The 'first quarter' phrasing in the statistic row is inconsistent with the 'warmup median' phrasing in the threshold row — they refer to the same thing but a reader who skims the table may not connect them without reading the prose below.
   *Recommendation:* Align the phrasing: change the Statistic row value to 'p95 of overall samples vs warmup-window median (first quarter of insertion-order samples)' for consistency with the threshold row.

6. [ ] **code-quality-reviewer** | `test/PERFORMANCE.md:209` | task-values
   The sweep table in 'Why 20x, not 10x' records only one measurement row (Apple Silicon, no pinning) and a vague pre-TASK-080 baseline row. The acceptance criterion mentions '50 CI runs'; the documentation acknowledges this cannot be enforced at PR-time but the proxy (10-round local sweep) is only described verbally. No CI-lane breakdown (e.g. Linux x86-64 with and without pinning) is included, making it impossible for a future maintainer to know whether the 20x floor was characterised on the CI lanes that will actually run the gate.
   *Recommendation:* Add at least one measured row for a Linux x86-64 lane (even if estimated from a local VM) to the sweep table, or explicitly note which CI lanes the 20x gate has NOT been characterised on and that re-measurement is required if a flake surfaces there.

7. [ ] **code-quality-reviewer** | `test/integ/threadsafety_stress.cpp:1088` | test-coverage
   The `[WARNING]` log and `LT_CHECK(rounds_ran > 0)` now correctly fail the test when the watchdog fires on a saturated host, but the companion `LT_CHECK_GT(register_ok_first_round, 100)` check at line 1039 would already fail in that scenario too (first round collects < 100 samples). The two checks are slightly redundant; the `rounds_ran` check gives the clearer CI log message, which was the stated intent.
   *Recommendation:* No code change required. If future pruning is desired, the `register_ok_first_round > 100` check could be removed since `rounds_ran > 0` subsumes it for the watchdog-fired path; but keeping both as belt-and-suspenders is also reasonable and costs nothing.

8. [ ] **code-quality-reviewer** | `test/integ/threadsafety_stress.cpp:70` | code-readability
   `#define _GNU_SOURCE` appears after the platform guard `#if defined(__linux__)` but _before_ the POSIX headers it unlocks (`pthread.h`, `sched.h`). However `_GNU_SOURCE` must be defined before _any_ system header is included; if a unity build or PCH has already included system headers the define is silently ignored. The conventional placement is at the very top of the TU, before all includes.
   *Recommendation:* Move `#define _GNU_SOURCE` (inside its `#if defined(__linux__)` guard) to the top of the file, before the `#include <curl/curl.h>` line.

9. [ ] **code-quality-reviewer** | `test/integ/threadsafety_stress.cpp:799` | code-readability
   `round_result` is defined inside the lambda capture scope of `run_one_round` (a local struct inside the test body). While legal in C++14+, defining a struct inside a lambda body is unconventional and reduces searchability. Moving it to the anonymous namespace or to just before the test macro would make it easier to find and re-use.
   *Recommendation:* Hoist `struct round_result` to the file-level anonymous namespace alongside `adversarial_segment` so it is visible without entering the test body.

10. [ ] **code-quality-reviewer** | `test/integ/threadsafety_stress.cpp:914` | code-readability
   The round-robin merge loop (lines 914-925) uses a bare `size_t idx` + `bool any` pattern to iterate across per-thread vectors. The intent (interleave at the same position across all thread buffers) is correct but the idiom is non-obvious; a reader must trace two levels of loop to understand what it does. A comment naming the pattern ('round-robin merge') is absent at the loop itself even though the surrounding block comment mentions it.
   *Recommendation:* Add a single-line comment directly above the `while (any)` loop: `// Round-robin merge: pick element [idx] from each per-thread buffer in turn.`

11. [ ] **code-quality-reviewer** | `test/integ/threadsafety_stress.cpp:929` | code-elegance
   The round-robin merge loop uses a mutable `bool any` sentinel toggled inside the inner loop. A cleaner alternative is iterating `idx` from 0 to `max_size - 1` where `max_size = max(v.size() for v in per_thread_samples)`, eliminating the sentinel and the double-loop scan past exhausted vectors.
   *Recommendation:* Replace the `while (any) { any=false; for (v) { if (idx < v.size()) { push; any=true; } } ++idx; }` pattern with a pre-computed `max_size` loop: `for (size_t idx=0; idx<max_size; ++idx) { for (auto& v : per_thread_samples) { if (idx < v.size()) samples_ns.push_back(v[idx]); } }`. This is more direct and avoids the flag variable entirely.

12. [ ] **code-quality-reviewer** | `test/integ/threadsafety_stress.cpp:944` | code-elegance
   Percentile index computation uses `static_cast<double>(sorted.size()) * 0.95` cast back to `size_t`. Floating-point rounding is unnecessary here; integer arithmetic `sorted.size() * 95 / 100` avoids the double conversion and is exact for any size up to ~10^17.
   *Recommendation:* Replace the three percentile index expressions with pure integer arithmetic: `sorted.size() * 95 / 100`, `sorted.size() * 99 / 100`, `sorted.size() * 999 / 1000`.

13. [ ] **code-quality-reviewer** | `test/integ/threadsafety_stress.cpp:966` | code-readability
   p999 percentile uses a different bias formula (999/1000) than p95/p99 (99/100), making the three index computations inconsistent at a glance even though all three are mathematically correct ceiling-integer arithmetic.
   *Recommendation:* Unify the formula to the same bias pattern: `(n * 999 + 999) / 1000` is already correct but is visually inconsistent with `(n * 95 + 99) / 100`. A brief inline comment noting 'bias = denominator - 1' for all three would remove the apparent inconsistency without changing behaviour.

14. [ ] **code-quality-reviewer** | `test/integ/threadsafety_stress.cpp:982` | code-readability
   The warmup window is computed from `samples_ns` (insertion-ordered) but the comment above it correctly notes the round-robin merge may not give a strict wall-clock first quarter. The variable is named `warmup` yet is sourced from the unordered merged vector. This naming is fine but the local variable `quarter` is only used once; inlining it would make the slice boundary clearer without a separate binding.
   *Recommendation:* Minor: either inline `quarter` into the `std::vector<int64_t> warmup(...)` constructor, or add a brief comment next to the `quarter` declaration stating 'first quarter of insertion-ordered (round-robin) merged samples' to link it to the NOTE comment above.

15. [ ] **code-quality-reviewer** | `test/integ/threadsafety_stress.cpp:989` | test-coverage
   The `worst_baseline` variable is initialised to 0 and only updated when `r.p95 > worst_p95`. If the first round has the highest p95 that update fires; but if all rounds produce the same p95 only the first round's baseline is recorded. More critically, if `rounds_ran > 0` but every round produces `r.p95 == 0` (degenerate fast host) `worst_baseline` stays 0 and `LT_CHECK_LT(0, 0)` would pass vacuously. The more correct update condition is `if (r.p95 >= worst_p95)` or maintaining `worst_baseline` independently.
   *Recommendation:* Change the update guard to `if (r.p95 >= worst_p95)` so `worst_baseline` always corresponds to the round that produced `worst_p95`, even when multiple rounds tie.

16. [ ] **code-simplifier** | `test/integ/threadsafety_stress.cpp:784` | naming
   Type aliases `clock_t` and `ns` are extremely terse and `clock_t` shadows the POSIX type of the same name, which can cause compiler warnings on some platforms (POSIX reserves `clock_t` in the global namespace).
   *Recommendation:* Rename to `using StressClock = std::chrono::steady_clock;` and `using NsDuration = std::chrono::nanoseconds;` to avoid the POSIX name collision and improve readability.

17. [ ] **code-simplifier** | `test/integ/threadsafety_stress.cpp:799` | naming
   `round_result` uses snake_case while the enclosing test file uses camelCase for local lambdas (`run_one_round`, `writer`) and ALL_CAPS for constants. The struct name is fine, but its field `gate_ran` vs the lambda name `run_one_round` both describe "the gate ran" — the boolean is redundant when `samples >= 100` is already tested as the gating condition for populating the other fields.
   *Recommendation:* Consider replacing `gate_ran` with an explicit sentinel value on one of the numeric fields (e.g. `warmup_median == -1` means gate did not run), or leave as-is — the current approach is readable and explicit. This is purely optional polish.

18. [ ] **code-simplifier** | `test/integ/threadsafety_stress.cpp:914` | code-structure
   The round-robin interleaving loop uses a raw `idx` counter advancing through all per-thread vectors in a `while(any)` loop with an inner `for`. This is correct but its intent is not immediately obvious — a reader must trace the `any` flag and `idx` counter together to understand the round-robin merge.
   *Recommendation:* Extract the merge into a small named helper `merge_round_robin(per_thread_samples)` returning `std::vector<int64_t>`. The call site in `run_one_round` then reads: `std::vector<int64_t> samples_ns = merge_round_robin(per_thread_samples);` which names the intent directly. The loop body is unchanged inside the helper.

19. [ ] **code-simplifier** | `test/integ/threadsafety_stress.cpp:925` | code-structure
   The total-sample count accumulation at lines 925-927 (`size_t total = 0; for (auto& v : ...) total += v.size(); samples_ns.reserve(total);`) iterates `per_thread_samples` once just to compute `total` for the reserve call, then the round-robin merge loop immediately below iterates it again. This double-traversal is a minor structural duplication.
   *Recommendation:* Either (a) fold the size accumulation into the round-robin loop by calling `samples_ns.reserve(kMaxRoutesPerParent * kParents.size())` — an upper-bound reserve that avoids the extra pass — or (b) keep the separate pass but note why the exact reserve matters (it avoids reallocation under high sample counts). As written the intent is not immediately clear.

20. [ ] **code-simplifier** | `test/integ/threadsafety_stress.cpp:929` | code-structure
   The round-robin merge loop uses a separate `bool any` flag initialised to `true` before the outer `while (any)` loop, then reset to `false` at the top of each inner iteration. This is an unusual pattern where the flag serves as both a loop-continue signal and an 'any element found' sentinel within the same pass. A simpler expression of the same logic would use a size-based termination condition.
   *Recommendation:* Replace the `any`-flag loop with an index-bounded loop: `for (size_t idx = 0; ; ++idx) { bool pushed = false; for (auto& v : per_thread_samples) { if (idx < v.size()) { samples_ns.push_back(v[idx]); pushed = true; } } if (!pushed) break; }`. This removes the `bool any` variable entirely and makes the termination condition (`no thread had an element at position idx`) immediately visible.

21. [ ] **code-simplifier** | `test/integ/threadsafety_stress.cpp:964` | code-structure
   The ceiling-percentile formula `(n * k + (k_denom-1)) / k_denom` is spelled out three times at lines 964-966 with slightly different denominators (99 for p95/p99 and 999 for p999), and then restated in prose in the comment block starting at line 962. Readers must mentally verify all three instantiations match the same pattern.
   *Recommendation:* Extract a small inline lambda or a named helper `percentile_idx(n, k, denom)` that returns `std::min((n * k + (denom - 1)) / denom, n - 1)`. Then lines 964-966 become three readable one-liners (`r.p95 = sorted[percentile_idx(n, 95, 100)];` etc.) and the comment above need only mention the formula once.

22. [ ] **performance-reviewer** | `test/PERFORMANCE.md:175` | algorithmic-complexity
   The PERFORMANCE.md gate table documents 'p95 of overall samples vs median of first quarter' but the code actually computes the median of the first quarter using a full sort of the quarter slice (O(n/4 * log(n/4))). This is correct but inconsistent with the documented claim that the 'first quarter' corresponds to the 'warmup at low cardinality regime'. Since round-robin merge (see finding 2) may not perfectly preserve insertion order across threads, the 'first quarter' of the merged vector is an approximation of the warmup window, not an exact one. The document should note this approximation.
   *Recommendation:* Add a note to the PERFORMANCE.md methodology section: 'The warmup window (first quarter of merged samples) is an approximation — round-robin merge preserves per-thread ordering but not cross-thread wall-clock ordering. The approximation is conservative: if thread scheduling causes fast early samples to appear later in the merge, the warmup_median is inflated, making the gate stricter, not looser.'

23. [ ] **performance-reviewer** | `test/integ/threadsafety_stress.cpp:319` | algorithmic-complexity
   In `driver_body`, case 4 (op==4), when the HookBag is at capacity, the code calls `hooks->handles.erase(hooks->handles.begin())` on a `std::vector`. The comment at line 319-320 correctly notes this is O(n) for vector. The cap is 256 (`kCap`), so worst case is 256 element shifts per eviction. This is in a hot path exercised by 1/8 of all handler calls during a 60-second stress run. The comment suggests switching to `std::deque` if kCap grows, but does not address the current cost.
   *Recommendation:* Replace `std::vector<ht::hook_handle>` with `std::deque<ht::hook_handle>` in `HookBag` now, so `pop_front` is O(1) instead of O(n). Alternatively, use a circular buffer with a head-index pointer to avoid reallocation entirely. At kCap=256 the current cost is acceptable (256 moves per eviction at ~200 req/s × 1/8 ops = ~25 evictions/s), but the comment sets a false expectation that only growth triggers the problem.

24. [ ] **performance-reviewer** | `test/integ/threadsafety_stress.cpp:855` | missing-caching
   Inside the writer lambda, `adversarial_segment(parent, i)` and the path construction `std::string("/") + parent + "/" + segment` allocate a new std::string on every iteration of the inner `kParents` loop. Since `adversarial_segment` itself does a `reserve` + three `append` calls (already reasonably efficient), the extra allocation is the outer path concatenation. At 15000 routes × 3 parents × 4 threads = 180000 iterations total, this is 180000 heap allocations that could be reduced to one reused buffer per thread.
   *Recommendation:* Pre-allocate a `std::string path` before the outer loop, then use `path.clear(); path += '/'; path += parent; path += '/'; path.append(segment);` to reuse the buffer. Since `adversarial_segment` already returns a string by value, either inline its logic into the path builder or accept the temporary and append in-place.

25. [ ] **performance-reviewer** | `test/integ/threadsafety_stress.cpp:941` | missing-caching
   The `sorted` vector is a full copy of `samples_ns` (line 941: `std::vector<int64_t> sorted = samples_ns;`), then a separate `warmup` vector is extracted from the unsorted `samples_ns` (line 957-959) and sorted independently. This means `samples_ns` is kept in memory simultaneously with both `sorted` and `warmup`, peaking at roughly 3× the sample count in memory. For 15000 samples × 8 bytes = 120 KB, this is negligible, but the extra sort of the warmup slice (quarter of 15000 = 3750 elements) is an avoidable O(n log n) operation on a sub-slice that could be obtained via `std::nth_element` + `std::sort` on just the median index.
   *Recommendation:* Use `std::nth_element` to find the warmup median in O(n) instead of fully sorting the warmup slice: `std::nth_element(warmup.begin(), warmup.begin() + warmup.size()/2, warmup.end()); r.warmup_median = warmup[warmup.size()/2];`. For the overall percentile computation, a single sort of `sorted` is unavoidable, but `samples_ns` can be moved into `sorted` (via `std::move`) to avoid the copy.

26. [ ] **performance-reviewer** | `test/integ/threadsafety_stress.cpp:987` | missing-caching
   warmup median uses floor division (warmup.size()/2) on an even-length array, which returns the lower of the two middle elements rather than a true median. For the warmup window this produces a slightly pessimistic (lower) baseline, making the 20× gate stricter rather than looser — the direction is safe, but it is not a canonical median.
   *Recommendation:* For statistical precision, use (warmup[warmup.size()/2 - 1] + warmup[warmup.size()/2]) / 2 when warmup.size() is even, or document explicitly that a lower-middle floor median is intentionally chosen because it makes the gate conservative. The current behaviour is safe and the gate still functions correctly; this is purely a documentation/precision note.

27. [ ] **security-reviewer** | `test/integ/threadsafety_stress.cpp:399` | input-validation
   stress_seconds(), stress_repeats(), and stress_pin_cpu() use std::stoi() to parse environment variables, which throws std::invalid_argument on non-numeric input and std::out_of_range on values exceeding INT_MAX. Both exceptions are caught by the blanket catch(...) block and cause fallback to defaults, which is correct. However, a value like '99999999999999' silently falls back to the default rather than emitting a diagnostic. This is minor since it is test-only infrastructure, not production code. CWE-390.
   *Recommendation:* Optionally emit a std::cerr warning when std::out_of_range is caught so operators are informed their environment variable was ignored rather than silently accepted as default.

28. [ ] **security-reviewer** | `test/integ/threadsafety_stress.cpp:440` | input-validation
   stress_pin_cpu() accepts CPU ids 0–4095 from the environment variable HTTPSERVER_STRESS_PIN_CPU without validating the value against the actual number of online CPUs on the host. An out-of-range CPU id passed to pthread_setaffinity_np (e.g. CPU 4095 on a 4-core machine) returns EINVAL, which is silently discarded by pin_this_thread_to_cpu. While this causes no security issue (the call is a best-effort optimisation and failure is explicitly documented as acceptable), the upper bound of 4096 is an arbitrary magic constant rather than a validated sysconf(_SC_NPROCESSORS_CONF) check. CWE-20.
   *Recommendation:* Either document that out-of-range values silently no-op (already implied by 'best-effort') or clamp to sysconf(_SC_NPROCESSORS_CONF) on Linux. Since this is test-only code and pin failure is already silently tolerated, documentation is sufficient.

29. [ ] **spec-alignment-checker** | `test/PERFORMANCE.md:209` | action-item
   Action item: 'Profile what causes the CI-runner noise (cold caches, sibling-CPU scheduling, MHD socket-accept jitter). Capture median, p99, and max under the current runner.' The profiling in PERFORMANCE.md only documents local Apple Silicon sweep results ('TASK-080 measurement, Apple Silicon (M-series), -O3 -DNDEBUG, HTTPSERVER_STRESS_REPEATS=10, no pinning: 13.4× worst p95'). There is no captured data from a CI runner, GitHub Actions, or a Linux machine. The dominant noise contributor is identified as route_table_mutex_ contention (not OS scheduling noise as the task hypothesised), which is valid, but the action item specifically asked for CI-runner profiling.
   *Recommendation:* Append at least one CI-runner [STATS] line (from a GHA log artifact) to the sweep table to satisfy the 'capture under the current runner' portion of this action item.

30. [ ] **spec-alignment-checker** | `test/integ/threadsafety_stress.cpp:1056` | ears-requirement
   PRD §3.6 acceptance criterion states 'A microbenchmark of req.get_headers() shows ≥10× reduction in per-call cost vs v1.' This requirement is not directly related to TASK-080, but the task's 'Related Requirements: PRD §3.6 performance acceptance' could be read as the overall §3.6 section. The TASK-080 gate (latency spike ratio for register_path) is not the same metric as the §3.6 get_headers benchmark. The PERFORMANCE.md correctly documents both separately. The PRD §3.6 requirement itself (get_headers ≥10× faster) is satisfied per the bench data (230×), so this is a traceability note rather than a violation.
   *Recommendation:* No code change needed. The task description's 'Related Requirements: PRD §3.6' is slightly misleading since §3.6 is about get_headers ergonomics, not registration latency. Consider noting in TASK-080 that the relevant PRD claim is the DR-008 concurrency requirement (§2 PRD-CON-REQ-001) rather than §3.6.

31. [ ] **spec-alignment-checker** | `test/integ/threadsafety_stress.cpp:927` | specification-gap
   The minimum sample count threshold is 100 (line 927: 'if (samples_ns.size() < 100)'). With 15 000 total ops across 4 writers, this threshold would only trigger if the wall-clock deadline cuts the run to under 0.67% completion — which is unlikely but possible on extremely slow or TSan-instrumented hosts. There is no test or documented expectation for what ratio is acceptable when the gate is skipped (the test passes deadlock-free completion as the gate, which is weaker). This is not specified in the task or PRD.
   *Recommendation:* Document the skip condition explicitly in PERFORMANCE.md or add a minimum threshold check that produces a warning when the gate is unexpectedly skipped, so CI logs are clearly distinguishable from a gate pass.

32. [ ] **spec-alignment-checker** | `test/integ/threadsafety_stress.cpp:null` | action-item
   Action item: 'discard the slowest N%' was considered and explicitly rejected in PERFORMANCE.md as 'unprincipled'. The rejection is reasoned and the alternative (p99→p95 statistic switch) is principled. However, the action item also mentions 'use median-of-medians rather than a single median' — this is not addressed anywhere in the implementation or documentation. The implementation uses median of first quarter as the baseline, which is a single-pass median computation, not median-of-medians.
   *Recommendation:* Add a note in PERFORMANCE.md explicitly addressing why median-of-medians was not needed (e.g., because the dominant noise source is contention rather than OS preemption, the per-thread buffer approach already achieves sufficient baseline stability).

33. [ ] **test-quality-reviewer** | `test/integ/threadsafety_stress.cpp:1013` | aaa-violation
   The acceptance/gate block at line 1013-1057 mixes the 'act' (iterating repeats) and the 'assert' (LT_CHECK_GT / LT_CHECK_LT) in the outer loop body and after it. The pattern is: loop calls run_one_round (act), immediately updates worst_p95 (implicit assertion setup), and then asserts once at the end. This is not a hard violation but the absence of a clear AAA separation in the outer driver makes it hard to see at a glance which lines are setup vs. act vs. assert.
   *Recommendation:* Consider extracting the 'run all repeats and collect worst_p95' logic into a helper or clearly labeled block with a comment, then keep assertions at the end in a clearly delimited section.

34. [ ] **test-quality-reviewer** | `test/integ/threadsafety_stress.cpp:1081` | aaa-violation
   The comment block at line 1081 ('Gate: require that at least one latency round actually ran.') is separated from its corresponding LT_CHECK(rounds_ran > 0) at line 1096 by 14 lines of CHOICE 1 / CHOICE 2 prose. A reader tracing the gate logic must jump over an unrelated rationale block to find the actual assertion. The description of the rounds_ran gate belongs immediately before the LT_CHECK, not embedded in the CHOICE 2 rationale section.
   *Recommendation:* Move the comment block at lines 1081-1087 to immediately precede LT_CHECK(rounds_ran > 0) at line 1096, after the closing of the CHOICE 2 paragraph. This keeps the two LT_CHECK calls and their explanatory comments together in the same visual block.

35. [ ] **test-quality-reviewer** | `test/integ/threadsafety_stress.cpp:1097` | non-deterministic
   When rounds_ran == 0 and the test reaches LT_CHECK_LT(worst_p95, worst_baseline * 20) at line 1097, both worst_p95 and worst_baseline are 0 (never updated in the loop because !r.gate_ran caused every iteration to continue). This evaluates to LT_CHECK_LT(0, 0) which is a false assertion (0 < 0 is false). The intent is for the earlier LT_CHECK(rounds_ran > 0) at line 1096 to short-circuit the test before reaching line 1097, but LT_CHECK in littletest.hpp may not abort the test body — if it records a failure and continues, the second CHECK fires with confusing diagnostics ('expected 0 < 0') that obscure the root cause.
   *Recommendation:* Add an early return after LT_CHECK(rounds_ran > 0) fails, or restructure the gate to guard LT_CHECK_LT inside an explicit 'if (rounds_ran > 0)' block. This ensures the 0 < 0 spurious failure can never appear in logs. Alternatively, confirm that LT_CHECK failure in littletest.hpp always terminates the test body — if so, document that assumption with a brief comment.

36. [ ] **test-quality-reviewer** | `test/integ/threadsafety_stress.cpp:784` | naming-convention
   The typedef `using clock_t = std::chrono::steady_clock` shadows the POSIX C function `clock_t` (a typedef for an arithmetic type from `<time.h>`). While there is no functional conflict here (C++ name lookup resolves the using alias in this scope), it is a surprising alias name that may confuse readers familiar with the C typedef.
   *Recommendation:* Rename to `using StressClock = std::chrono::steady_clock` or `using SteadyClock = std::chrono::steady_clock` to avoid the shadowing of the C99/POSIX `clock_t` typedef.

37. [ ] **test-quality-reviewer** | `test/integ/threadsafety_stress.cpp:799` | naming-convention
   The `round_result` struct is defined inside a lambda body (`run_one_round`) but declared before the lambda, which makes it invisible from the outer scope — this is correct. However the struct fields `gate_ran`, `warmup_median`, `median`, etc. are plain `int64_t`/`bool` with no documentation of units (nanoseconds? microseconds?). The code uses them correctly but a future reader editing the gate must track down that all duration fields are in nanoseconds from context (the `ns` typedef and `clock_t` usage).
   *Recommendation:* Add a short comment on the struct fields indicating units, e.g. `int64_t warmup_median = 0; // ns` or rename them `warmup_median_ns`, etc., consistent with the `samples_ns` naming already used for the vector.

38. [ ] **test-quality-reviewer** | `test/integ/threadsafety_stress.cpp:810` | excessive-setup
   The webserver `ws` is created inside `run_one_round` (line 812-815) but never started (`// No ws.start() — registration does not need a running daemon`). Its destructor calls `ws.stop()` (or the equivalent) regardless. Additionally, line 964 calls `ws.stop()` explicitly before returning `r`, but there is also an early return path at line 937 (`ws.stop(); return r;`) inside the `samples_ns.size() < 100` branch. If a future refactor adds another early return path, this pattern could leak. Using RAII (or a scope guard) would make the cleanup robust.
   *Recommendation:* Wrap the webserver in a scope guard or defer cleanup via RAII rather than two explicit `ws.stop()` calls. This is low priority since the pattern works today, but worth noting for maintainability.

39. [ ] **test-quality-reviewer** | `test/integ/threadsafety_stress.cpp:927` | missing-test
   The minimum-samples guard `if (samples_ns.size() < 100)` causes the latency gate to be silently skipped. There is no separate test or assertion that verifies the gate was actually exercised in the normal CI run (REPEATS=1, no wall-clock cutoff). The `rounds_ran > 0` check at line 1055 is the only proxy, and it is only evaluated after the loop. If CI always produces >= 100 samples, the guard is never triggered, but if it sometimes fires (flaky CI timeout), the test passes silently rather than signaling that something is wrong.
   *Recommendation:* Add a test-level assertion or at least a `[WARNING]` log line indicating that the latency gate was skipped due to insufficient samples, so the CI log is not silently 'green' in a degraded state.
