# Unworked Review Issues

**Run:** 2026-05-27 01:06:19
**Task:** TASK-052
**Total:** 34 (1 critical, 0 major, 33 minor)

## Critical

1. [ ] **code-quality-reviewer** | `examples/:null` | task-completeness
   Three of the four mandated example files are missing from the diff: banned_ip_log.cpp, early_413.cpp, and clf_access_log.cpp. The task acceptance criteria require all four examples to build. README.md, examples/README.md, examples/Makefile.am, scripts/check-examples.sh, and RELEASE_NOTES.md all reference these files as if they exist on disk, but they are not in the changed-files list and the diff stat shows no additions for them. scripts/check-examples.sh would fail at its bidirectional Makefile.am coverage check the moment it runs because the listed programs have no .cpp source files.
   *Recommendation:* Add banned_ip_log.cpp, early_413.cpp, and clf_access_log.cpp under examples/. Each should follow the same structure as per_route_auth.cpp: a block comment describing the issue it closes, a SECURITY NOTE if credentials are involved, and the minimal std::function invocation of the relevant hook phase. Only per_route_auth.cpp was delivered in this diff.

## Minor

2. [ ] **architecture-alignment-checker** | `README.md:1535` | interface-contract
   The README 'Phases' table rows are presented in the same non-canonical order (connection_closed appears at row 3, before request_received). The canonical firing order defined in DR-012/§4.10 and reflected in hook_phase.hpp places connection_closed last (after request_completed).
   *Recommendation:* Reorder the table rows to match canonical firing order: connection_opened, accept_decision, request_received, body_chunk, route_resolved, before_handler, handler_exception, after_handler, response_sent, request_completed, connection_closed.

3. [ ] **architecture-alignment-checker** | `RELEASE_NOTES.md:105` | interface-contract
   The 'What's new' hook-bus bullet lists the eleven phases in a non-canonical order: 'connection_opened, accept_decision, connection_closed, request_received, body_chunk, ...' — placing connection_closed third instead of last. DR-012, §4.10 (the hooks.md phase table), and hook_phase.hpp all define connection_closed as the eleventh and final phase in firing order.
   *Recommendation:* Reorder the parenthetical to match the canonical firing order: connection_opened, accept_decision, request_received, body_chunk, route_resolved, before_handler, handler_exception, after_handler, response_sent, request_completed, connection_closed.

4. [ ] **code-quality-reviewer** | `examples/per_route_auth.cpp:83` | code-readability
   The hook lambda at line 83 guards against ctx.request == nullptr and returns hook_action::pass() in that branch. The hook_context.hpp Doxygen for before_handler_ctx does not say request can be null for normal dispatches; the null guard looks defensive but may mislead readers into thinking null is a plausible runtime case when calling add_hook on a resource that is always dispatched after route resolution.
   *Recommendation:* Add a one-line comment explaining the null check: either 'belt-and-braces; request should never be null at before_handler on a matched route' or remove the guard and add an assertion, depending on whether null is truly possible here.

5. [ ] **code-quality-reviewer** | `src/httpserver/hook_context.hpp:124` | code-readability
   The accept_ctx struct has both a Doxygen /** @brief ... */ block AND a trailing block of inline // comments (lines 124-138) that repeat much of the same information in a different style. The two documentation styles are inconsistent with every other context struct in the file, which uses only the /** */ Doxygen form.
   *Recommendation:* Consolidate the inline // TASK-046 comment block into the Doxygen /** */ block above accept_ctx, matching the style of request_completed_ctx (which also has an extended inline comment block — that could be similarly consolidated if the author is doing a pass). The duplication adds noise without adding information.

6. [ ] **code-quality-reviewer** | `src/httpserver/hook_context.hpp:168` | code-readability
   body_chunk_ctx has the same mixed-documentation pattern: a Doxygen /** */ block followed by a lengthy // comment block that duplicates information. This is repeated for response_sent_ctx (lines 271-293) and request_completed_ctx (lines 308-334). The // blocks contain important detail (lifetime constraints, null conditions, the 'offset' counter semantics) that should be in the @brief / @param sections of the Doxygen block instead.
   *Recommendation:* Migrate the detail from the // comment blocks into the respective Doxygen /** */ blocks as @note, @param, or @attention tags. This makes the information visible to Doxygen-generated docs and eliminates the style inconsistency.

7. [ ] **code-quality-reviewer** | `test/bench_hook_overhead.cpp:119` | code-readability
   median_of_sorted and p99_of_sorted both take the vector by non-const reference even though they only read it (after the sort). The sort in median_of_sorted is a side-effect that destroys the original ordering; p99_of_sorted relies on this side-effect via the comment 'v already sorted'. This implicit ordering dependency between two functions violates the clean-code principle that functions should have no hidden side effects on their arguments.
   *Recommendation:* Either (a) have median_of_sorted take a copy and sort internally while returning both median and p99 in a single pass, or (b) rename the function to sort_and_return_median to make the mutation explicit, and pass a pre-sorted reference to p99_of_sorted with a clearer API contract.

8. [ ] **code-quality-reviewer** | `test/integ/threadsafety_stress.cpp:105` | readability
   The HookBag struct comment says 'shared_ptr to keep the test mutator obvious in handler captures' but HookBag is passed and used everywhere as a raw pointer (HookBag*) to a stack-allocated struct. There is no shared_ptr in the implementation. The comment describes a design that was not adopted.
   *Recommendation:* Replace the comment with accurate wording, e.g. 'Raw pointer (passed to driver_body) to a stack-allocated bag; lifetime is the test body, so no ownership transfer is needed.'

9. [ ] **code-quality-reviewer** | `test/integ/threadsafety_stress.cpp:254` | code-elegance
   When the HookBag is at capacity the recycle path calls hooks->handles.erase(hooks->handles.begin()), which is O(n) on a std::vector. With kCap = 256 the cost is negligible for the stress test's purposes, but the comment says 'Recycle: drop the oldest entry's registration first so the bag stays bounded' without acknowledging the linear erase. A deque or circular-buffer would be more idiomatic here.
   *Recommendation:* Either change std::vector to std::deque (pop_front is O(1)) or add a brief comment explaining why O(n) erase is acceptable given kCap = 256. This is a minor readability issue, not a correctness problem.

10. [ ] **code-quality-reviewer** | `test/integ/threadsafety_stress.cpp:258` | code-readability
   The comment inside the cap-exceeded branch ('its destructor fires on a moved-from (empty) handle') is accurate but longer than necessary given the surrounding context already explains the invariant. Minor verbosity in inline comment.
   *Recommendation:* Trim the three-sentence comment to a single sentence noting that the moved-from handle's dtor is a no-op, deferring the full rationale to the block comment at the top of the file.

11. [ ] **code-simplifier** | `test/bench_hook_overhead.cpp:119` | code-structure
   `median_of_sorted` sorts its argument in place (a side effect) but is named as if it only reads. The name suggests a pure query but the function mutates the vector. The caller relies on this mutation before calling `p99_of_sorted`, creating an invisible ordering dependency.
   *Recommendation:* Rename to `sort_and_median` to make the mutation explicit, or add a `const` ref parameter and sort a local copy. The current shape works because both callers happen to call `median_of_sorted` first, but the contract is not obvious from the names.

12. [ ] **code-simplifier** | `test/bench_hook_overhead.cpp:124` | naming
   `p99_of_sorted` carries a comment `// v already sorted` to explain a hidden precondition (the vector must already be sorted by `median_of_sorted`). This is a code smell: the invariant is communicated only via a comment rather than through the call structure.
   *Recommendation:* Fold both statistics into a single helper that sorts once and returns a struct `{ double median; double p99; double min; double max; }`. This removes the ordering dependency entirely and lets `measure_gate_cost` call one function instead of two. Example: `auto stats = compute_stats(samples_ns);`

13. [ ] **code-simplifier** | `test/bench_hook_overhead.cpp:132` | naming
   The function `median_of_sorted` sorts its argument in-place as a side effect, but the name only advertises the return value. The caller `measure_gate_cost` relies on sorting happening before calling `p99_of_sorted`, making this an implicit sequencing dependency.
   *Recommendation:* Rename to `sort_and_median` or extract the sort into the caller so both stat helpers receive an already-sorted vector without hidden coupling.

14. [ ] **code-simplifier** | `test/integ/threadsafety_stress.cpp:114` | code-structure
   The `noop_resource::render_get` body splits its `return` statement across three lines with an awkward line break after `return` and before `ht::http_response::string("ok")`. The surrounding codebase uses single-line returns for trivial bodies.
   *Recommendation:* Collapse to a single line: `return ht::http_response::string("ok");` — consistent with the pattern used in `per_route_auth.cpp` lines 49 and 56, and in the other resource definitions in the integration tests.

15. [ ] **code-simplifier** | `test/integ/threadsafety_stress.cpp:127` | code-structure
   install_random_hook's switch covers 11 phases (case 0..9 + default) using `phase_idx % 11u`, but the modulo and the case count must be kept in sync manually. If a phase is added later the modulo is the only thing that changes, which is easy to miss.
   *Recommendation:* Add a static_assert or a comment explicitly tying the modulo constant to the number of hook_phase enumerators, so a future phase addition triggers a visible reminder to update both.

16. [ ] **housekeeper** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-052/specs/unworked_review_issues/2026-05-27_000000_task-052.md:1` | documentation-stale
   The unworked review issues file header uses timestamp '2026-05-27 00:00:00' which is a round-zero placeholder rather than an actual wall-clock time. Prior entries (TASK-049: 10:47:39, TASK-050: 12:39:48, TASK-051: 23:01:00) all carry real timestamps. This is cosmetic but breaks the pattern.
   *Recommendation:* If the timestamp is auto-generated, update the tooling to capture actual wall-clock time rather than midnight. The content of the file is otherwise complete and correct.

17. [ ] **performance-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-052/test/bench_hook_overhead.cpp:127` | algorithmic-complexity
   median_of_sorted sorts the vector in-place and p99_of_sorted then relies on it already being sorted, but the call to std::min_element / std::max_element (lines 167-168) happens BEFORE the sort in measure_gate_cost and BEFORE the call to median_of_sorted. This is correct and intentional. However, p99_of_sorted silently assumes the vector is sorted when called — there is no assertion or documentation guarding this contract inside the function itself, making it easy to call out of order in a future refactor.
   *Recommendation:* Add a comment inside p99_of_sorted (e.g. `// precondition: v is sorted ascending`) or an assert(std::is_sorted(v.begin(), v.end())) under a debug flag. This is a correctness / maintainability note rather than a runtime performance issue, but a violated precondition would silently return a wrong p99 rather than a performance regression.

18. [ ] **performance-reviewer** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-052/test/bench_hook_overhead.cpp:151` | memory-allocation
   samples_ns is re-created inside measure_gate_cost on every call (called twice: once for config (a), once for (b)). The vector is reserved to OUTER=51 elements — negligible allocation, but the pattern is worth noting for consistency if OUTER ever grows significantly.
   *Recommendation:* No action needed at current OUTER=51. If OUTER is ever increased to thousands, consider passing the vector in by reference and clearing+reserving inside the function to reuse the backing allocation.

19. [ ] **performance-reviewer** | `test/bench_hook_overhead.cpp:119` | missing-caching
   median_of_sorted sorts the vector in-place and then p99_of_sorted relies on that sorted state. This is a hidden sequential dependency: calling p99_of_sorted before median_of_sorted (or calling median_of_sorted twice) would give wrong results. The function names do not communicate the dependency (p99_of_sorted implies the caller is responsible for pre-sorting, but median_of_sorted implies it will sort — so a caller who calls p99_of_sorted first gets an unsorted vector). The dependency is respected today but is brittle.
   *Recommendation:* Rename to make the contract explicit: sort the vector once at the call site and rename both helpers to median_of_presorted / p99_of_presorted, or fold both into a single summarise(vector) function that sorts once and returns a struct { double median, p99, min, max; }.

20. [ ] **performance-reviewer** | `test/bench_hook_overhead.cpp:130` | missing-caching
   The warmup loop (10 000 iterations, line 137) is performed inside measure_gate_cost before the timed section. On arm64 this is sufficient; on x86_64 with Intel Turbo Boost the CPU may still be ramping from a low-power P-state after only 10 K iterations if the bench was idle before. The Task specification says '1 M warm-path requests' in the task description but the bench runs 11 outer rounds × 1 M inner iterations = 11 M loads; the 10 K warmup is dwarfed by the first outer round which acts as a de-facto warmup. This is acceptable for the current gate but worth documenting: the true warmup is the first outer round, not the explicit 10 K pre-loop.
   *Recommendation:* Discard the first outer-round sample (treat it as warmup) by initialising a `bool first_round = true` guard and skipping the push_back for round 0, or simply document in the comment that the first outer iteration is the effective thermal warmup.

21. [ ] **performance-reviewer** | `test/bench_hook_overhead.cpp:178` | missing-caching
   Scenarios (a) and (b) are measured sequentially in the same process with no cache-flush or CPU affinity between them. The scenario (a) webserver (ws_a) is destroyed before ws_b is constructed, which flushes the any_hooks_ atomic from L1 cache. Scenario (b) then warms L1 on a freshly constructed ws_b. The atomic-load in scenario (b) will therefore benefit from the warm L1 state that scenario (a) primed — the branch predictor has also been trained by 11 M loads of `false`. This makes the (b) median artificially low relative to a cold-start measurement. Since (b) is informational this does not affect the gate, but it does reduce the utility of the (a)/(b) comparison.
   *Recommendation:* To make (a) and (b) comparable, measure them in separate processes (or at minimum interleave them: measure (a), then (b), then (a) again and use the second (a) as the gated value). The simplest approach: run scenario (b) first (informational only) and scenario (a) second (gated), so the branch predictor is trained on `true` before the zero-hooks measurement, which is the more conservative direction.

22. [ ] **performance-reviewer** | `test/integ/threadsafety_stress.cpp:254` | algorithmic-complexity
   When the HookBag reaches capacity (kCap=256), the cap-overflow path calls hooks->handles.front().remove() and then hooks->handles.erase(hooks->handles.begin()) (lines 254-255). erase(begin()) on a std::vector is O(n) because every subsequent element must shift left. With kCap=256 and 16 concurrent client threads this is called at most a few thousand times over 60 seconds, so the absolute cost is negligible in the stress test. However, as a pattern it would be O(n) per overflow event if kCap were raised to a large value.
   *Recommendation:* For the current kCap=256 this is a non-issue. Document the O(n) cost in the comment so future maintainers who raise kCap understand the trade-off, or switch to std::deque<hook_handle> which provides O(1) pop_front.

23. [ ] **security-reviewer** | `examples/per_route_auth.cpp:84` | insecure-design
   The hook callback returns `hook_action::pass()` when `ctx.request == nullptr` (line 84-86). In practice the before_handler context should never have a null request pointer at this phase per the architecture docs, but treating a null pointer as a permission-granted pass-through is a fail-open pattern (CWE-636). If a future internal code path ever supplies a null here, the auth check silently skips.
   *Recommendation:* Fail closed on unexpected null: return `hook_action::respond_with(http_response::unauthorized(...))` when `ctx.request` is null, or assert/log and return unauthorized. The comment should note that null should never occur here in practice.

24. [ ] **security-reviewer** | `examples/per_route_auth.cpp:87` | insecure-design
   HTTP Basic authentication example transmits credentials over plain HTTP (port 8080, no TLS configured). The example does not warn in-code that Basic auth over unencrypted HTTP exposes credentials in plaintext on the wire. The SECURITY NOTE comment (lines 13-19) correctly warns against hardcoding credentials but says nothing about transport security. A developer copying this pattern to production without TLS would expose passwords to network eavesdroppers (CWE-523).
   *Recommendation:* Add a brief in-source caveat near the SECURITY NOTE block, for example: '// TRANSPORT NOTE: HTTP Basic auth sends credentials base64-encoded but NOT encrypted. // In production, serve only over TLS (use create_webserver().use_ssl(...)).'

25. [ ] **security-reviewer** | `examples/per_route_auth.cpp:89` | authentication
   Credential comparison uses `std::string_view::operator==` (line 89: `u == user && p == pass`). This is a short-circuit lexicographic comparison; on most implementations it returns early as soon as the first differing byte is found. This creates a timing side-channel that could, in theory, allow an attacker to enumerate valid credentials one character at a time (CWE-208). For a demonstration example the risk is low, but the pattern may be copied into real auth code without the timing-safe caveat.
   *Recommendation:* Either add a comment noting that constant-time comparison is required in production (e.g., using `CRYPTO_memcmp` or `std::equal` with a fixed-length loop), or replace the comparison with a simple constant-time helper to model the correct pattern directly. Since this is an example meant to be followed, modeling the correct approach is preferable.

26. [ ] **security-reviewer** | `scripts/check-readme.sh:266` | sensitive-data
   Line 266 uses `sed` in a pipeline (`grep -oE ... | sed 's/^](...'`) to extract Markdown link targets. The sed expressions operate on file content (not external user input), so there is no injection risk. However, the script uses `set -euo pipefail` at line 3 but iterates over broken_links with `while IFS= read -r target` fed from a process substitution. If the README contains a link target with embedded shell metacharacters (e.g., a backtick or `$(...)` in a Markdown URL), the `case` statement at lines 259-264 would treat those characters as literal string values — no shell expansion occurs inside `[[ ]]` or `case` without eval, so this is safe as written.
   *Recommendation:* No action required; this is a note for future maintainers that the safe pattern is already in use.

27. [ ] **spec-alignment-checker** | `examples/per_route_auth.cpp:null` | specification-gap
   The diff only contains per_route_auth.cpp as a new example file. The other three hook examples (banned_ip_log.cpp, early_413.cpp, clf_access_log.cpp) are present on disk and listed in examples/Makefile.am, but were introduced in earlier tasks (TASK-050). The TASK-052 action items list all four as deliverables, but three of the four were pre-existing. This is not a problem — the acceptance criterion 'Four examples build under make check' is met — but the task description slightly overstates TASK-052's contribution.
   *Recommendation:* No code change needed. Consider updating the TASK-052 action items to reflect that banned_ip_log.cpp, early_413.cpp, and clf_access_log.cpp were delivered in TASK-050, and TASK-052 adds only per_route_auth.cpp plus the documentation/spotcheck/bench/stress work.

28. [ ] **spec-alignment-checker** | `test/bench_hook_overhead.cpp:117` | acceptance-criteria
   The PRD acceptance criterion (product_specs.md §3.8) and the TASK action item both state the CI gate for (a) is 'within 2x microbench noise of the pre-hook-system baseline'. The implementation instead uses an absolute fixed ceiling of kMaxGateNsPerCall = 50.0 ns justified as '10x what we measure on a clean release build'. There is no stored HOOK_BASELINE_NS constant for comparison; the comment in the bench source (line 48) mentions 'within 2x of HOOK_BASELINE_NS' but that constant does not exist in the implementation.
   *Recommendation:* Either introduce a compile-time HOOK_BASELINE_NS constant capturing the pre-hook atomic-load baseline (e.g. measured at TASK-044 baseline time) and gate at 2x that value, or update the TASK action item wording to match the implemented 'absolute ceiling' approach. The current 50ns ceiling is generous and correct in spirit but differs from the specified 2x-relative gate.

29. [ ] **spec-alignment-checker** | `test/bench_hook_overhead.cpp:57` | acceptance-criteria
   The acceptance criterion says 'bench_hook_overhead passes gate (a)'. The source file itself documents 'NOT part of make check' and is wired under EXTRA_PROGRAMS / make bench only (test/Makefile.am line 514-515). As a result, automated CI running make check will not execute the benchmark gate. The bench passes its gate when run manually via make bench, but CI does not enforce it on every commit.
   *Recommendation:* This is consistent with the established pattern for bench_sizeof_http_resource and bench_get_headers (same EXTRA_PROGRAMS treatment, same rationale about sanitizer distortion). If CI enforcement is desired, add bench_hook_overhead to check_PROGRAMS with the existing sanitizer-skip guard (kSanitizerBuild exit 0). Otherwise update the acceptance criterion wording to say 'passes gate when run under make bench'.

30. [ ] **test-quality-reviewer** | `test/bench_hook_overhead.cpp:119` | naming-convention
   median_of_sorted() and p99_of_sorted() sort only in median_of_sorted; p99_of_sorted carries a comment 'v already sorted' but relies on a call-site invariant that is not enforced. measure_gate_cost calls median_of_sorted (which sorts) and then p99_of_sorted on the same vector, but this ordering is implicit — future refactoring could easily reorder the calls and produce a wrong p99 without any compiler warning.
   *Recommendation:* Either sort once explicitly before calling both functions, or rename to `median_of` and `p99_of` (without the `_sorted` suffix) and sort at the top of each function. The extra sort is O(n log n) on 11 elements and negligible for a bench.

31. [ ] **test-quality-reviewer** | `test/bench_hook_overhead.cpp:127` | naming-convention
   median_of_sorted and p99_of_sorted mutate (sort) their vector argument but the parameter is a non-const reference. The sort-in-place side-effect is why min/max must be captured before calling these functions. The parameter name and signature do not communicate the mutation.
   *Recommendation:* Either rename the parameter to signal mutation (e.g. samples_inout) or add a brief comment in the function signature noting the sort-in-place contract. This is documentation, not a functional defect.

32. [ ] **test-quality-reviewer** | `test/bench_hook_overhead.cpp:172` | excessive-setup
   OUTER=11 rounds × INNER=1,000,000 iterations = 11 million atomic-load iterations per configuration. With the task description calling for '1M-request warm-path microbench', 11M iterations with only 10,000 warmup iterations may be proportionally under-warmed. The warmup is 0.09% of the total run, which on a cold instruction cache (first bench run after a build) could mean the first 1-3 outer rounds include branch-predictor training, biasing the minimum (already sorted-out by the median). This is a minor statistical concern because the median of 11 rounds is robust to 2-3 outlier high values.
   *Recommendation:* Consider increasing warmup to 100,000 iterations (still sub-millisecond on any modern core) or discarding the first 2 outer rounds from the sample. At minimum, add a comment explaining why 10,000 warmup iterations are sufficient relative to the OUTER/INNER parameters.

33. [ ] **test-quality-reviewer** | `test/integ/threadsafety_stress.cpp:268` | implementation-coupling
   Case 5 (hook remove) calls hooks->handles.back().remove() followed by pop_back(). If remove() throws (e.g. impl already torn down), pop_back is skipped and the handle stays in the bag, but the hook is already deregistered — the subsequent destructor call would hit the already-removed guard. This is a latent inconsistency, not a data race, and the guard makes it safe; the concern is purely about maintenance clarity.
   *Recommendation:* Mirror the case-4 recycle pattern: move the back handle into a local, pop_back first, then call remove() on the local. This eliminates any question about the exception-safety and double-remove ordering.

34. [ ] **test-quality-reviewer** | `test/integ/threadsafety_stress.cpp:380` | missing-test
   The final assertions check only the combined sums (register_ok + unregister_ok > 0, block_ok + unblock_ok > 0, hook_add_ok + hook_remove_ok > 0) rather than each counter individually. Because op selection is `rng() % 6`, each op is expected roughly 1/6 of the time across 60 seconds and 16 clients, so all six should fire thousands of times. Using individual checks (`LT_CHECK_GT(counters.hook_add_ok.load(), 0)` and `LT_CHECK_GT(counters.hook_remove_ok.load(), 0)` separately) would catch a scenario where a subtle bug silently suppresses one of the two TASK-052 ops without the other, which the combined-sum check masks.
   *Recommendation:* Split the combined TASK-052 assertion into two separate checks: `LT_CHECK_GT(counters.hook_add_ok.load(), 0)` and `LT_CHECK_GT(counters.hook_remove_ok.load(), 0)`. Apply the same split to the TASK-032 pairs for consistency.
