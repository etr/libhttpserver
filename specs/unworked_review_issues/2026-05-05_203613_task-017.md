# Unworked Review Issues

**Run:** 2026-05-05 20:36:13
**Task:** TASK-017
**Total:** 24 (0 critical, 0 major, 24 minor)

## Minor

1. [x] **architecture-alignment-checker** | `src/http_request.cpp:685` | pattern-violation
   get_path_pieces() maintains two in-lockstep caches: the existing pmr::vector<pmr::string> (path_pieces) and a new default-allocator vector<string> (path_pieces_public_). The architecture doc §4.2 notes that containers alias impl-owned storage and lists path_pieces as a lazy cache. The dual-cache design is a pragmatic trade-off (the pmr version cannot be returned as const vector<string>& without a public ABI change), but the additional heap allocation on first call and the synchronisation logic between the two caches is not discussed anywhere in the architecture docs. The design is sound but slightly deviates from the 'single cache, return by const&' pattern used by the other five getters.
   *Recommendation:* Add a brief inline comment in http_request_impl.hpp (near path_pieces_public_) explicitly acknowledging that this is a deliberate dual-cache and that the pmr version exists as a forward-compatible internal consumer hook. The architecture doc §4.2 already notes the lazy-cache strategy broadly; no doc update is strictly needed, but a TODO referencing any future consolidation task would aid maintainability.
   **Fixed 2026-05-27:** Added detailed inline comment near path_pieces_public_ in http_request_impl.hpp with TODO(post-018) for future consolidation.

2. [x] **architecture-alignment-checker** | `test/integ/file_upload.cpp:265` | interface-contract
   Inside print_file_upload_resource::render_POST and render_PUT, `files = req.get_files()` performs a copy-assignment from the newly returned const& into a resource-member map. The comment correctly notes this is a deliberate copy because req goes out of scope. However, the public header's container-reference lifetime contract (documented under 'Container reference lifetime contract (TASK-017)') does not explicitly call out that file_info objects held in the returned reference contain disk-side state (file paths) and that copying the map does NOT copy the temporary files on disk -- it only copies the metadata. This could mislead future callers into thinking the copy is sufficient for long-lived access to the files themselves.
   *Recommendation:* Add a sentence to the 'Container reference lifetime contract' block in http_request.hpp (or the get_files() Doxygen comment) clarifying that copying the returned map copies metadata only; the on-disk files are still subject to cleanup when the http_request destructor runs unless the file_cleanup_callback suppresses deletion.
   **Fixed 2026-05-27:** Added @note to get_files() Doxygen in http_request.hpp clarifying that copying the map copies metadata only; on-disk files remain subject to cleanup.

3. [x] **code-quality-reviewer** | `src/http_request.cpp:171` | code-readability
   ensure_headerlike_cache() uses a four-branch switch with a default that silently falls back to headers_cached_ for unsupported MHD_ValueKind values. The comment says 'the public API never reaches here', but there is no assertion or [[unlikely]] annotation to communicate this invariant to the compiler or to future readers who might add a new public getter.
   *Recommendation:* Add a debug-mode assert (assert(false) or std::unreachable()) in the default branch with an explanatory comment, so any future caller that mistakenly passes an unsupported kind fails loudly instead of silently returning stale header data.
   **Fixed 2026-05-27:** Added assert(false) in default branch with explanatory comment. Added <cassert> include.

4. [x] **code-quality-reviewer** | `src/http_request.cpp:685` | code-elegance
   get_path_pieces() builds the public mirror cache inline inside the forwarder function body rather than inside http_request_impl where the two sibling caches (path_pieces / path_pieces_public_) already live. This scatters the population logic across two classes (impl owns the data, outer owns the build loop), which is inconsistent with how ensure_headerlike_cache() and populate_args() centralise their logic entirely inside http_request_impl.
   *Recommendation:* Move the path_pieces_public_ build loop into a new http_request_impl::ensure_path_pieces_public_cached() helper (analogous to ensure_path_pieces_cached), and call it from http_request::get_path_pieces(). This keeps all cache-maintenance code inside the impl class and makes the outer forwarder a one-liner.
   **Fixed 2026-05-27:** Extracted ensure_path_pieces_public_cached() into http_request_impl (declared in hpp, defined in impl.cpp). get_path_pieces() is now a two-liner.

5. [x] **code-quality-reviewer** | `src/httpserver/detail/http_request_impl.hpp:209` | code-elegance
   Two separate caches are maintained for path pieces: the PMR-backed path_pieces (pmr::vector<pmr::string>) and the public-typed path_pieces_public_ (vector<string>). The comment says the PMR version 'stays arena-friendly for any future internal consumer', but no current internal consumer actually uses it post-PIMPL. Maintaining two lockstep caches for a hypothetical future use adds ongoing memory overhead (both caches live for the request lifetime) and complexity.
   *Recommendation:* This is an acceptable forward-looking trade-off given the arena allocator design, but it should be annotated with a concrete TASK reference for when the PMR version will be consumed internally, or a note that it can be collapsed to a single cache if no such task emerges by the time TASK-018 lands.
   **Fixed 2026-05-27:** Added TODO(post-018) annotation with consolidation note to http_request_impl.hpp comment block.

6. [x] **code-quality-reviewer** | `test/integ/basic.cpp:2366` | code-readability
   The args_cache_resource render_GET binds args1 and args2 and immediately casts them to void, with a comment explaining the address-equality guarantee. The test is intentionally not asserting address equality (just that caching works end-to-end via the response body), but the (void) suppression of the reference variables makes the intent opaque to future readers: it looks like dead code.
   *Recommendation:* Either remove args1/args2 and the accompanying comment entirely (the caching is already tested by the response body comparison), or assert &args1 == &args2 directly to make the caching contract explicit in the test.
   **Fixed 2026-05-27:** Replaced (void) casts with runtime address-equality check; returns error response if &args1 != &args2.

7. [x] **code-quality-reviewer** | `test/unit/create_test_request_test.cpp:306` | test-coverage
   The getters_return_const_ref_stable test verifies address stability for the test-request (connection_ == nullptr) code path, but there is no equivalent stability test for the live-request (MHD connection_) path. The MHD path goes through ensure_headerlike_cache() which sets *built = true after the first call; if this flag were accidentally not persisted, only the integration test would catch it rather than a focused unit/integ test.
   *Recommendation:* The existing integration test in test/integ/basic.cpp (args_cache_resource with args1/args2) partially covers this, but it does not assert address equality. Consider adding a pointer-equality check to the integration-side cache test to give the same stability guarantee on the live path.
   **Fixed 2026-05-27:** args_cache_resource now asserts &args1 == &args2 with a runtime guard (see finding 6 fix).

8. [x] **code-simplifier** | `src/http_request.cpp:685` | code-structure
   get_path_pieces() first calls ensure_path_pieces_cached() (which populates impl_->path_pieces), then separately checks path_pieces_public_built_ and populates path_pieces_public_. The two-step build is a consequence of the dual-cache design but the outer getter shoulders both responsibilities. The comment explains the intent but the code is slightly harder to follow than the header-cache equivalent.
   *Recommendation:* Consider folding the public-mirror build into ensure_path_pieces_cached() itself (accepting the path argument as it already does), setting path_pieces_public_built_ at the end of that function. get_path_pieces() then becomes: impl_->ensure_path_pieces_cached(path); return impl_->path_pieces_public_; This collapses the two steps into one function and removes the public-cache-built_ check from the getter body.
   **Fixed 2026-05-27:** Implemented via separate ensure_path_pieces_public_cached() helper (see finding 4). get_path_pieces() is now a two-liner.

9. [x] **code-simplifier** | `src/http_request.cpp:809` | code-structure
   get_args() inlines the lazy-cache-build logic directly in the public getter, while get_path_pieces() (line 685) does the same. Both blocks follow an identical pattern (check built_ flag, clear, populate, set built_ = true, return cache). The repetition is minor but slightly increases the maintenance surface if the pattern needs to change.
   *Recommendation:* Consider extracting the cache-build loop for get_args() into a private helper on http_request_impl (e.g. ensure_args_view_cache()) analogous to ensure_headerlike_cache() and ensure_path_pieces_cached(). The public getter then becomes a two-liner: impl_->ensure_args_view_cache(); return impl_->args_view_cached_; This aligns with the pattern already established for the header/footer/cookie caches.
   **Fixed 2026-05-27:** Extracted ensure_args_view_cached() into http_request_impl. get_args() is now a two-liner.

10. [ ] **code-simplifier** | `src/httpserver/detail/http_request_impl.hpp:196` | naming
   The six new cache members use two different naming conventions: the header/footer/cookie group uses a trailing underscore (headers_cached_, footers_cached_, cookies_cached_, headers_cache_built_, ...) while the args group uses args_view_cached_ / args_view_cache_built_ and the path group uses path_pieces_public_ / path_pieces_public_built_. The inconsistency makes it harder to scan the member list and understand which members are TASK-017 caches at a glance.
   *Recommendation:* Align the cache-flag names across all six caches. One consistent scheme: <name>_cache_ for the container and <name>_cache_built_ for the flag. For example: headers_cache_ / headers_cache_built_, args_view_cache_ / args_view_cache_built_, path_pieces_cache_ / path_pieces_cache_built_. This is a rename-only change with no behavioral impact.
   **Deferred 2026-05-27:** Mass rename across all six caches, setters, getters, and tests is high-churn with merge-conflict risk for concurrent branches. Defer to a dedicated cleanup task.

11. [x] **housekeeper** | `specs/tasks/M3-request/TASK-017.md:null` | documentation-stale
   TASK-016's housekeeping commit created per-task unworked_review_issues files (specs/unworked_review_issues/2026-05-04_*.md) as its 'review records'. TASK-017 has no corresponding review-issue files for this iteration, which breaks the pattern established by TASK-016 and is referenced in the commit title convention.
   *Recommendation:* If the validation-loop agents produced review findings files for TASK-017 (e.g. security-reviewer, performance-reviewer), add them to specs/unworked_review_issues/ under a timestamped name matching the pattern 2026-05-05_<time>_task-017.md, consistent with the TASK-016 precedent.
   **Fixed 2026-05-27:** This file (2026-05-05_203613_task-017.md) is the review record for TASK-017.

12. [x] **performance-reviewer** | `src/http_request.cpp:203` | missing-caching
   ensure_headerlike_cache() uses a switch statement and pointer-variable approach to select between three cache slots (headers/footers/cookies). The warm path (built == true) is a branch-and-load-return; the three pointer indirections (cache, built, local_fallback) are resolved even on the warm path. With three separate getters that each incur a function call crossing the PIMPL boundary, the warm path is still marginally more expensive than it needs to be.
   *Recommendation:* This is a low-priority concern. If profiling shows the warm path is measurable, consider inlining the built-check directly inside get_headers/get_footers/get_cookies before delegating to ensure_headerlike_cache, or using an array indexed by kind. The current code is correct and readable -- only worth addressing if benchmarks show the branch overhead.
   **Accepted 2026-05-27:** Low-priority concern with no profiling data showing measurable overhead. Accept current design.

13. [x] **performance-reviewer** | `src/http_request.cpp:685` | memory-allocation
   get_path_pieces() maintains two caches in lockstep: the PMR-backed path_pieces (vector<pmr::string>) and a separate public path_pieces_public_ (vector<std::string>). Every cold call populates both. Since the only callers of the PMR copy are get_path_pieces() and get_path_piece(), and the latter constructs a std::string copy on each call anyway, the PMR mirror provides no observable performance benefit today while doubling the allocation cost and adding an extra boolean flag.
   *Recommendation:* Consider collapsing to a single std::vector<std::string> path_pieces_public_ cache. If the PMR copy is being preserved for a documented future internal consumer, add a code comment naming that consumer; otherwise remove the lockstep pattern to reduce cold-path overhead and struct size.
   **Fixed 2026-05-27:** Added comment with TODO(post-018) for future consolidation. Full collapse deferred until post-TASK-018 evaluation.

14. [x] **performance-reviewer** | `src/http_request.cpp:781` | memory-allocation
   get_arg(string_view key) still constructs a new http_arg_value by-value on every call, copying all associated string values from the PMR vector into a heap-allocated vector<string> inside the returned http_arg_value. This is a hot path (individual argument lookup is far more common than full-map iteration) and pays the same per-call allocation cost that TASK-017 eliminated for the bulk getters.
   *Recommendation:* Return const http_arg_value& by caching a map<string_view, http_arg_value> alongside args_view_cached_, or at minimum add a reserve() so the copy is a single allocation. If the by-value return is intentional for lifetime safety, document it explicitly next to the function.
   **Accepted 2026-05-27:** By-value return is intentional for lifetime safety and documented in http_request.hpp Doxygen. Caching deferred to a future task.

15. [x] **performance-reviewer** | `src/http_request.cpp:829` | memory-allocation
   get_args_flat() is excluded from TASK-017's const-ref treatment and still builds and returns a new std::map<string_view, string_view> by value on every call. While less commonly used than get_args(), the construction iterates unescaped_args and inserts into a freshly heap-allocated map, which is inconsistent with the caching pattern applied to the six other container getters.
   *Recommendation:* Apply the same lazy-cache pattern: add a mutable args_flat_cached_ and args_flat_cache_built_ flag to http_request_impl, populate once, and return const std::map<string_view, string_view, arg_comparator>&. This is low-risk given the existing pattern in get_args().
   **Accepted 2026-05-27:** get_args_flat() caching deferred per existing TODO comment in http_request.hpp. Not in TASK-017 scope.

16. [x] **security-reviewer** | `src/http_request.cpp:193` | insecure design / silent fallback (CWE-704)
   The default branch of the switch in ensure_headerlike_cache() silently falls back to the headers cache when an unsupported MHD_ValueKind is passed. The comment says 'the public API never reaches here', but the function is not private to the translation unit (it is declared in the internal header and callable by any future internal consumer). An unexpected kind would silently populate and mark headers_cache_built_, causing subsequent calls to get_headers() to return the rogue data. This is not exploitable through the current public API, but the silent mutation of unrelated state is a latent correctness hazard.
   *Recommendation:* Add an assertion (assert or DCHECK) in the default branch, or return a reference to a file-scoped empty static const http::header_view_map{} sentinel, so misuse is immediately visible rather than silently corrupting the headers cache.
   **Fixed 2026-05-27:** Same fix as finding 3 — assert(false) added in the default branch.

17. [x] **security-reviewer** | `src/http_request.cpp:808` | lifetime / use-after-free (CWE-416)
   get_args() builds args_view_cached_ by storing std::string_view values that alias the pmr-backed strings in impl_->unescaped_args. If any setter (set_arg, set_arg_flat, grow_last_arg) is called after the cache has been built (args_view_cache_built_ == true), the view cache is not invalidated. A second call to get_args() would then return stale (but still valid-memory) views while the underlying unescaped_args has been mutated. On the live MHD path this is unreachable (setters are only called before the handler executes), but the test-request path via create_test_request allows build() followed by direct arg mutation through the friend interface. The window is narrow but worth hardening.
   *Recommendation:* Reset args_view_cache_built_ to false in every set_arg / set_arg_flat / grow_last_arg / set_args impl-side method so a post-build mutation forces the cache to be rebuilt on the next get_args() call. Alternatively, document with a static_assert or comment that the cache is only valid after construction is complete (i.e., setters must not be called after the first getter call).
   **Fixed 2026-05-27:** Added args_view_cache_built_ = false at the top of all five setter methods (set_arg x2, set_arg_flat, set_args, grow_last_arg) to invalidate the view cache on mutation.

18. [x] **security-reviewer** | `src/httpserver/http_request.hpp:119` | lifetime documentation / CWE-672
   The class-level doc block for the Container reference lifetime contract (lines 114-131) correctly states that the string_view keys/values inside header_view_map and arg_view_map share the handler-frame lifetime restriction. However, the individual get_headers(), get_footers(), get_cookies(), get_args() Doxygen comments do not repeat this string_view sub-element lifetime warning; they only say 'The reference remains valid until the http_request is destroyed'. A caller reading only the per-method doc (the common pattern for IDE tooling) would miss that iterating the returned map and storing a string_view element is unsafe past the handler.
   *Recommendation:* Add a @note line to each of the four container-getter Doxygen comments mirroring the pattern already used on get_header() / get_cookie() / get_footer(): 'The string_view keys and values inside the returned map are only valid within the handler call frame. Copy to std::string if a longer lifetime is required.'
   **Fixed 2026-05-27:** Added @note with string_view lifetime warning to get_headers(), get_footers(), get_cookies(), get_args() Doxygen in http_request.hpp.

19. [x] **spec-alignment-checker** | `src/httpserver/http_request.hpp:250` | scope-creep
   get_files() was changed from `const std::map<...> get_files() const` to `const std::map<...>& get_files() const noexcept`. The `noexcept` specifier is beyond the task's stated scope (which only required changing the return type to const& and marking the getter const). The noexcept addition is harmless and arguably correct given the trivial forwarding body, but it was not called for by any action item or EARS requirement.
   *Recommendation:* Accept as a minor additive improvement or remove noexcept if strict scope adherence is required.
   **Accepted 2026-05-27:** noexcept is correct for a trivial member forwarding. Retained as is.

20. [x] **spec-alignment-checker** | `test/integ/basic.cpp:175` | specification-gap
   The `full_args_resource` handler in test/integ/basic.cpp calls `req.get_args().at("arg")` where `get_args()` now returns `const http::arg_view_map&`. The `.at()` call is correct against const&, but the expression `std::string(req.get_args().at("arg"))` relies on implicit string_view-to-string conversion of the `http_arg_value` at the call site. There is no explicit test that this integration-test handler compiles and behaves identically to before the change. The TASK-017 task specification requires that all callers in test/ be migrated; this caller was touched but only indirectly (return type changed, call site not modified). No regression is expected but explicit migration comment is absent.
   *Recommendation:* Add a brief comment explaining that get_args() now returns const& and the call site is already read-only, confirming it was reviewed for copy-semantic reliance.
   **Fixed 2026-05-27:** Added inline comment to full_args_resource in basic.cpp confirming the call site was reviewed.

21. [x] **spec-alignment-checker** | `test/unit/http_request_pimpl_test.cpp:null` | acceptance-criteria
   The acceptance criterion `static_assert(std::is_lvalue_reference_v<decltype(std::declval<const http_request&>().get_headers())>)` is satisfied. The pimpl test file additionally asserts exact return types for all six getters. However, the `get_files()` static_assert uses `const std::map<std::string, std::map<std::string, httpserver::http::file_info>>&` which correctly matches the implementation. All static_asserts are present and cover the required criterion plus exact-type checks — this is satisfactory.
   *Recommendation:* No action required; finding is informational confirmation.
   **No action required 2026-05-27:** Informational confirmation only.

22. [x] **test-quality-reviewer** | `test/integ/basic.cpp:2374` | naming-convention
   Inside args_cache_resource::render_GET the local variables args1 and args2 are bound as const& but are immediately silenced with (void) casts. The comment explains this is intentional to lock in the call-site contract, but from a test-readability perspective the variables add noise without asserting the meaningful property (address equality). The actual caching check is end-to-end via the response body, so the reference binding buys nothing here that the compile-time static_assert in http_request_pimpl_test.cpp does not already provide.
   *Recommendation:* Either remove the args1/args2 bindings and (void) casts to simplify the render helper, or promote the address-equality check into the integration test body (similar to getters_return_const_ref_stable in the unit test) so the intent is a testable assertion rather than dead code.
   **Fixed 2026-05-27:** Promoted to runtime address-equality check; returns error response if &args1 != &args2 (see findings 6/7).

23. [x] **test-quality-reviewer** | `test/unit/create_test_request_test.cpp:195` | multiple-concerns
   empty_getters_no_crash tests eleven different getter paths in a single test body. While each call is a no-crash smoke check rather than a behavioral assertion, grouping them obscures which specific getter is the root cause of a failure and requires re-reading the whole test to triage a single crash.
   *Recommendation:* Either rename to empty_getters_smoke_check to communicate intent more clearly, or split the container-size assertions (lines 204-210) into a separate test body (e.g. empty_containers_return_size_zero) so naming makes the failure point self-evident. The current arrangement is acceptable but could be clearer.
   **Fixed 2026-05-27:** Renamed empty_getters_no_crash to empty_getters_smoke_check with updated comment.

24. [x] **test-quality-reviewer** | `test/unit/create_test_request_test.cpp:306` | missing-test
   getters_return_const_ref_stable verifies address stability for all six container getters but does not include a check for get_files(). The sanity-count block at lines 325-329 asserts sizes for headers/footers/cookies/args/path_pieces but skips files. The address-equality check at line 323 does cover get_files(), but the size sanity is omitted. This is a minor gap since get_files() returns a non-lazy direct reference to impl_->files_ (no cache flag needed) so it cannot be wrong in the same way as the other five, but consistency in the sanity block would make the test easier to reason about.
   *Recommendation:* Add LT_CHECK_EQ(cref.get_files().size(), static_cast<size_t>(0)); after line 329 to close the symmetry gap and make the sanity block exhaustive over all six getters.
   **Fixed 2026-05-27:** Added LT_CHECK_EQ(cref.get_files().size(), static_cast<size_t>(0)) to sanity block in getters_return_const_ref_stable.
