# Unworked Review Issues

**Run:** 2026-05-18 18:11:39
**Task:** TASK-037
**Total:** 33 (0 critical, 3 major, 30 minor)

## Major

1. [ ] **code-quality-reviewer** | `.github/workflows/verify-build.yml:643` | correctness
   The 'Run libhttpserver configure' step has no special-casing for build-type flag-invariance-on. In the else-branch it falls through to `../configure --disable-fastopen`, which omits --enable-tls --enable-bauth --enable-dauth --enable-websocket. Whether those flags are actually detected depends entirely on the host environment's installed libraries. For the ON lane to be a reliable gate it should either assert those features are all 'yes' in config.log (mirroring the OFF-lane verification step), or the configure command itself should pass the explicit --enable-* flags so the step fails fast if any feature is absent.
   *Recommendation:* Add a 'Verify libhttpserver detected all features on (TASK-037 flag-invariance-on lane)' step analogous to the OFF-lane step (lines 696-714), grepping config.log for each of 'TLS Enabled', 'Basic Auth', 'Digest Auth', 'WebSocket' with value 'yes'. Alternatively, add `--enable-tls --enable-bauth --enable-dauth --enable-websocket` to the configure invocation for the ON lane and let configure itself fail if the dependencies are absent.

2. [ ] **code-quality-reviewer** | `.github/workflows/verify-build.yml:765` | code-readability
   The iteration 2 fixes (make -C test consumer_fixture, SHA-256 on all downloads, permissions block, TLS setter additions) are present only as unstaged working-tree changes and have not been committed. The current HEAD commit (49f18bd) still carries the original `make consumer_fixture` invocation in the workflow and the commit message body explicitly states `make consumer_fixture`, which is incorrect.
   *Recommendation:* Stage and commit all four fix groups together so the branch tip is clean and the CI workflow reflects what is actually on disk.

3. [ ] **performance-reviewer** | `.github/workflows/verify-build.yml:545` | missing-caching
   The flag-invariance-off lane downloads and builds libmicrohttpd from source on every CI run (curl + tar + configure + make) with no actions/cache step, unlike every other lane which uses the pre-built cache keyed on os/compiler/version. Since the configure flags are deterministic (--disable-bauth --disable-dauth --disable-websockets), the output is reproducible and cacheable.
   *Recommendation:* Add an actions/cache step for the flag-invariance-off libmicrohttpd build, keyed on something like `${{ matrix.os }}-${{ matrix.c-compiler }}-libmicrohttpd-1.0.3-flags-off-v1`. Gate the build step on `steps.cache-libmicrohttpd-off.outputs.cache-hit != 'true'` (mirroring the pattern already used for the standard lane at lines 515-534). This saves a full source build (~2-3 min on ubuntu-latest) on every PR push after the first cache population.

## Minor

4. [ ] **architecture-alignment-checker** | `.github/workflows/verify-build.yml:693` | pattern-violation
   The 'Verify libhttpserver detected all features off' step uses a grep pattern against config.log (e.g. `^  TLS Enabled[[:space:]]*:[[:space:]]*no$`) that is fragile: it assumes a specific format and exact label text for the configure summary lines. If the configure.ac summary format ever changes slightly (e.g. spacing, label text), the gate silently passes even if features are not actually off. A more robust approach would grep for `HAVE_GNUTLS` / `HAVE_BAUTH` / `HAVE_DAUTH` / `HAVE_WEBSOCKET` in config.log or in a generated `config.h`, which are the canonical auto-detected values.
   *Recommendation:* Consider cross-checking against generated config.h (e.g. `grep -qE '#define HAVE_GNUTLS 1' src/config.h && echo 'ERROR: HAVE_GNUTLS still on'`) or at minimum verify which exact string configure.ac emits in its summary for robustness.

5. [ ] **code-quality-reviewer** | `.github/workflows/verify-build.yml:274` | code-readability
   The TASK-037 matrix entry comments (lines 274-310) are noticeably longer than comments for comparable entries (e.g., header-hygiene at lines 256-273). The multi-paragraph explanation of HAVE_GNUTLS auto-detection mechanism belongs in an ADR or task spec, not in the YAML matrix.
   *Recommendation:* Reduce each matrix-entry comment to 2-3 lines referencing the task and purpose, e.g., '# TASK-037 / PRD-FLG-REQ-001: flag-invariance-on gate. Compiles consumer_fixture with all features enabled.'.

6. [ ] **code-quality-reviewer** | `.github/workflows/verify-build.yml:289` | test-coverage
   The flag-invariance-on lane does not have a symmetric verification step confirming that all four HAVE_* features were detected as 'yes'. The flag-invariance-off lane has a dedicated verification step (lines 706-724). Without a corresponding check on the ON lane, a misconfigured gnutls or libmicrohttpd install that leaves a feature undetected would pass the ON lane silently -- the binary would compile but would not actually be exercising the all-features-on invariant.
   *Recommendation:* Add a 'Verify libhttpserver detected all features on' step analogous to the off-lane check, asserting TLS Enabled / Basic Auth / Digest Auth / WebSocket are all 'yes' in config.log for the flag-invariance-on build-type.

7. [ ] **code-quality-reviewer** | `.github/workflows/verify-build.yml:545` | test-coverage
   The 'Build libmicrohttpd with all features off' step downloads libmicrohttpd fresh on every run with no caching, while all other MHD build steps use actions/cache. This means every flag-invariance-off run pays a full download + compile cost.
   *Recommendation:* Add a cache step for the flag-invariance-off MHD build, keyed on `${{ matrix.os }}-${{ matrix.c-compiler }}-libmicrohttpd-1.0.3-flag-invariance-off`, analogous to the existing cache-libmicrohttpd step and the ARM cache pattern.

8. [ ] **code-quality-reviewer** | `.github/workflows/verify-build.yml:706` | code-readability
   The `Verify libhttpserver detected all features off` step uses an anchored ERE pattern `^  ${feat}[[:space:]]*:[[:space:]]*no$` against config.log. The pattern is correct (confirmed against actual config.log output which has two leading spaces without a configure: prefix for AC_MSG_NOTICE summary lines), but the correctness depends on a non-obvious autoconf behaviour: AC_MSG_NOTICE writes continuation lines without the 'configure:' prefix. This is not documented anywhere in the workflow.
   *Recommendation:* Add a one-line comment above the grep explaining that AC_MSG_NOTICE summary lines in config.log have two leading spaces rather than a 'configure:' prefix, to prevent future maintainers from 'correcting' the pattern.

9. [ ] **code-quality-reviewer** | `.github/workflows/verify-build.yml:767` | code-readability
   The `make -C test consumer_fixture` command is run after `cd build ;` — so the full invocation is `cd build && make -C test consumer_fixture`, which correctly targets build/test/Makefile. However the step comment block above (lines 759-766) still describes the intent as 'compile + link' without mentioning the subdirectory invocation, which could confuse a reader who only sees the comment and not the fix context.
   *Recommendation:* Update the inline comment to read 'make -C test consumer_fixture (builds in build/test/)' to make the subdirectory routing self-evident.

10. [ ] **code-quality-reviewer** | `test/consumer_fixture.cpp:128` | code-elegance
   touch_create_webserver_setters() constructs a local create_webserver{8080} to call the TLS credential-material setters, but the function now serves two distinct concerns: (1) member-function-pointer address-taking for use_ssl/basic_auth/digest_auth and (2) actual calls on a live instance for the credential setters. The two halves are separated only by a blank line and a comment, which is reasonable given the compile-only nature of this fixture, but a reader might expect a single-concern function.
   *Recommendation:* This is acceptable given the file's compile-only nature. Optionally split into touch_create_webserver_flag_setters() and touch_create_webserver_tls_credentials() if future extensions make the distinction important.

11. [ ] **code-quality-reviewer** | `test/consumer_fixture.cpp:130` | code-elegance
   touch_ws() catches all exceptions with an empty catch block (`catch (...) {}`), which silently swallows any unexpected error during the fixture invocation. While the stated goal is compile-time proof, this pattern can mask link errors at runtime if the binary is ever invoked outside of the pure-compile scenario.
   *Recommendation:* Since main() only takes the address of touch_ws (line 167) and never calls it, the try/catch blocks are unreachable dead code. The try/catch wrappers could be removed entirely; the compile/link assertion is provided by the function existing and being address-taken.

12. [ ] **code-quality-reviewer** | `test/consumer_fixture.cpp:21` | code-readability
   The block comment at the top of the file spans 20 lines and reads more like a changelog / task history than a code-level rationale. It mentions TASK-034, TASK-037, cycle F, and multiple acceptance criteria references. Clean code prefers comments that explain intent rather than replaying issue history.
   *Recommendation:* Trim the block comment to a brief statement of what the fixture is and why it must compile in all HAVE_* configurations. Move task/AC cross-references to a single trailing line (e.g., '// CI gate: PRD-FLG-REQ-001 / .github/workflows/verify-build.yml').

13. [ ] **code-quality-reviewer** | `test/consumer_fixture.cpp:83` | code-readability
   The inline comments on the member-function-pointer blocks are lengthy and repeat information already covered in the file-level block comment. The comment at lines 83-89 ('pin the DAUTH-gated check_digest_auth() declaration via a member-function pointer rather than a call...') is more verbose than necessary for someone reading the code.
   *Recommendation:* Condense to a single-line comment such as '// link-time proof that both check_digest_auth overloads are declared unconditionally'.

14. [ ] **code-simplifier** | `.github/workflows/verify-build.yml:542` | code-structure
   The 'Build libmicrohttpd without digest auth (no-dauth test)' step and the new 'Build libmicrohttpd with all features off (TASK-037 flag-invariance-off lane)' step share identical curl download + sha256sum check + tar extract sequences; only the configure flags differ. This is needless repetition (clean-code smell #5).
   *Recommendation:* Consider extracting the common download/verify/extract sequence into a reusable composite action or a shared shell function called from both steps, passing the configure flags as a parameter. Alternatively, a single 'Download and extract libmicrohttpd tarball' step (gated on the two build-types that need a custom build) followed by separate configure+make steps would eliminate the duplicated curl/sha/tar lines.

15. [ ] **code-simplifier** | `.github/workflows/verify-build.yml:577` | naming
   The symbol list for the bauth/dauth verification loop uses `MHD_queue_auth_fail_response` as the proxy for websocket absence, but that symbol is a digest-auth symbol, not a websocket symbol. The comment loop also never verifies a websocket-specific symbol separately; websocket absence is instead inferred from `libmicrohttpd_ws*` file presence in a separate branch. The two checks are semantically distinct but grouped inside one step without a separating label, making it harder to understand which symbol guards which HAVE_* flag.
   *Recommendation:* Add a brief inline comment next to each symbol in the for-loop naming the HAVE_* flag it probes (e.g. `# HAVE_BAUTH`, `# HAVE_DAUTH`), and similarly label the `libmicrohttpd_ws*` check as `# HAVE_WEBSOCKET`. No logic change needed.

16. [ ] **code-simplifier** | `.github/workflows/verify-build.yml:704` | code-structure
   The `Verify libhttpserver detected all features off` step uses `grep -qE "^  ${feat}[[:space:]]*:[[:space:]]*no$"` against `config.log`. The `config.log` format used here (two leading spaces, colon-separated key/value) is an autoconf summary block format, which is actually written to `config.status` summary output, not the verbose `config.log`. If this pattern silently never matches, the step would always enter the `else` branch and exit 1 even on a correct build.
   *Recommendation:* Verify the exact pattern against actual `config.log` output from a real run; if the summary block lives in a different file (e.g. the configure terminal output), adjust the grep target or add a `cat config.log | grep -i 'tls\|auth\|websocket'` debug line so CI output clearly shows why a mismatch was detected.

17. [ ] **code-simplifier** | `test/consumer_fixture.cpp:104` | code-structure
   The block comment above touch_create_webserver_setters() (lines 104-116) describes the member-function-pointer technique in detail and then repeats the same rationale inside the function body as a second comment block (lines 127-130). The explanation is given twice in the same function's preamble + body.
   *Recommendation:* Keep the above-function comment that explains the overall strategy (the 'why') and remove or shorten the inline comment at line 127 to a single line such as '// TLS credential-material setters -- always unconditional.' The duplicated rationale adds noise without adding clarity.

18. [ ] **code-simplifier** | `test/consumer_fixture.cpp:90` | code-structure
   The `touch_request_accessors` function has grown to touch two conceptually distinct sets of things: request field accessors (user/pass/digested_user) and method-pointer address probes for digest-auth methods. The function name no longer fully describes its content.
   *Recommendation:* Consider extracting the method-pointer probes (lines 90-101) into a separate `touch_request_dauth_symbols()` function to keep each function focused on one concept, matching the pattern used for `touch_create_webserver_setters`. This is a pure rename/extract with no behavioral change.

19. [ ] **performance-reviewer** | `.github/workflows/verify-build.yml:547` | missing-caching
   The flag-invariance-off lane re-downloads libmicrohttpd-1.0.3.tar.gz via curl on every run even though the same tarball is already downloaded (and cached) by the no-dauth lane using the same URL. There is no shared tarball cache between these two lanes.
   *Recommendation:* If caching the full build output (finding #1) is not adopted, at minimum cache the tarball itself with actions/cache keyed on the URL hash or version string to avoid redundant S3 downloads.

20. [ ] **performance-reviewer** | `.github/workflows/verify-build.yml:745` | missing-parallelization
   The flag-invariance-on lane runs the full sequential build pipeline (autoreconf, configure, make, consumer_fixture) but skips make check and cppcheck. The on-lane libmicrohttpd is already covered by the existing cache and shares the standard build path, so this lane duplicates the standard ubuntu/gcc/nodebug/dynamic lane's build work. The only unique assertion is `make consumer_fixture`.
   *Recommendation:* Consider whether the flag-invariance-on lane needs its own full library build or whether the consumer_fixture gate could be appended as an extra step on the existing standard ubuntu/gcc/nodebug/dynamic run. Eliminating the duplicate full build would save ~3-5 min per run. This is a design trade-off (isolation vs. cost) rather than a correctness issue; acceptable to leave as-is if CI isolation is preferred.

21. [ ] **security-reviewer** | `.github/workflows/verify-build.yml:361` | A06: Vulnerable Components / Unpinned action versions
   All GitHub Actions are referenced by mutable major-version tags (actions/checkout@v4, actions/cache@v4, msys2/setup-msys2@v2, codecov/codecov-action@v5). A tag can be force-pushed by the action maintainer or, in the case of a compromised account, replaced with malicious code. The TASK-037 changes do not add new action references but also do not address this existing weakness. CWE-829: Inclusion of Functionality from Untrusted Control Sphere.
   *Recommendation:* Pin each action to an immutable commit SHA alongside the tag as a comment, e.g.: 'uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4'. Use a tool like Dependabot or pinact to automate updates. At minimum, consider pinning the third-party actions (msys2/setup-msys2, codecov/codecov-action) which carry higher supply-chain risk than first-party GitHub actions.

22. [ ] **security-reviewer** | `.github/workflows/verify-build.yml:408` | A04: Insecure Design / matrix variable interpolation in shell
   Matrix variables are interpolated directly into shell run: strings (e.g. 'sudo apt-get install ${{ matrix.c-compiler }}' on line 408, 'sudo apt-get install ${{ matrix.cc-compiler }}' on line 412). These matrix values are defined statically in the workflow YAML so there is no external injection vector today, but the pattern is fragile: if a matrix variable were ever sourced from an external event (e.g. a repository_dispatch payload) it would become a shell injection. The TASK-037 additions do not introduce new interpolations of this type, but the pre-existing ones remain. CWE-78: OS Command Injection (latent).
   *Recommendation:* Assign matrix context values to environment variables at the step level and reference the environment variable in the shell script rather than inline-expanding the context expression. Example: 'env: PKG: ${{ matrix.c-compiler }}' then 'sudo apt-get install "$PKG"'. This eliminates any future injection risk if the source of these values changes.

23. [ ] **security-reviewer** | `.github/workflows/verify-build.yml:482` | insecure-design
   The curl-7.75.0 tarball downloaded for macOS testing (libhttpserver.s3.amazonaws.com/travis_stuff/curl-7.75.0.tar.gz) still has no sha256sum verification step, unlike all five libmicrohttpd download sites that were fixed in this iteration. This is a pre-existing gap not introduced by TASK-037, and the macOS lane is a test-tooling download rather than a dependency compiled into the shipped library, but it represents an inconsistency in the verification posture.
   *Recommendation:* Add an echo '...' | sha256sum -c (or shasum -a 256 -c on macOS) guard immediately after the curl download and before tar -xzf, matching the pattern used for libmicrohttpd. Note: macOS runners have shasum, not sha256sum.

24. [ ] **security-reviewer** | `.github/workflows/verify-build.yml:576` | A09: Logging Failures / information disclosure in verify steps
   The symbol verification steps (lines 576-591 and 701-713) print 'grep ... config.log' output verbatim on failure. config.log can contain compiler invocations, full configure output, and system path details. In a public repository CI log this is visible to all. This is consistent with the pre-existing 'Print config.log' step (line 716-721) but the new verification steps add additional unconditional path echoing (ls /usr/local/lib/libmicrohttpd_ws*) that may expose host filesystem layout. Severity is minor because this is a public open-source project and no secrets are expected in config.log.
   *Recommendation:* Gate the verbose grep/ls output behind 'if: failure()' or ensure it is intentionally informational. Consider redirecting 'ls /usr/local/lib/libmicrohttpd_ws*' output to /dev/null on success, only printing on failure.

25. [ ] **spec-alignment-checker** | `.github/workflows/verify-build.yml:681` | action-item
   Action item 1 states the flag-invariance-on lane should build 'once with --enable-tls --enable-bauth --enable-dauth --enable-websocket'. The CI configure step for this lane falls through to the generic else branch ('../configure --disable-fastopen') without those explicit flags. Features are enabled only through auto-detection of the installed gnutls and the standard libmicrohttpd library. If the standard libmicrohttpd package on the runner lacks a feature, the on-lane silently becomes a weaker test.
   *Recommendation:* Add an explicit branch in the 'Run libhttpserver configure' step for build-type == 'flag-invariance-on' that passes --enable-tls --enable-bauth --enable-dauth --enable-websocket (matching the task wording), and add a post-configure verification step (analogous to the off-lane step at line 696) that asserts all four HAVE_* features are actually on.

26. [ ] **spec-alignment-checker** | `.github/workflows/verify-build.yml:null` | acceptance-criteria
   Acceptance criterion 2 — 'Intentional regression (re-introducing #ifdef HAVE_GNUTLS around a public method) makes matrix red' — is documented as 'verified locally during TASK-037 cycle R' in the Makefile.am comment, but there is no automated regression test (e.g., a compile-fail canary file or a CI step that intentionally re-introduces the guard and expects failure). The acceptance criterion relies on manual verification only.
   *Recommendation:* This is low-risk given the fixture is compile-only and any re-introduction of the guard would break the off-lane at compile time. Document the manual verification artefact in TASK-037.md as evidence, or add a comment in the workflow linking to the cycle-R test run.

27. [ ] **spec-alignment-checker** | `test/consumer_fixture.cpp:117` | specification-gap
   touch_create_webserver_setters() covers raw_https_mem_key/cert/trust, https_priorities, https_mem_dhparams, cred_type, psk_cred_handler but omits the file-loading variants https_mem_key(), https_mem_cert(), https_mem_trust() and https_priorities_append(). These were never #ifdef HAVE_*-gated (confirmed in create_webserver.hpp), so they fall outside the strict scope of PRD-FLG-REQ-001. The gap is noted only as a future regression risk if a maintainer inadvertently adds a guard.
   *Recommendation:* No action required for this task. If desired, add the file-loading variants in a follow-on coverage pass to round out the fixture's surface.

28. [ ] **spec-alignment-checker** | `test/consumer_fixture.cpp:117` | specification-gap
   The task spec says 'cw.use_ssl(true)' should be called; the fixture resolves use_ssl, basic_auth, and digest_auth via member-function pointers rather than direct calls with `true`. build_server() calls use_ssl(false) directly. The pointer approach correctly proves unconditional declaration and is safe for the TLS-off build lane where calling these with `true` would throw at runtime. The compile-time invariant is satisfied.
   *Recommendation:* No change needed. The member-function-pointer approach is a valid and safer way to prove unconditional declaration without causing runtime failures in the off-lane.

29. [ ] **spec-alignment-checker** | `test/consumer_fixture.cpp:122` | specification-gap
   PRD-FLG-REQ-003 requires 'webserver::features()' to return a struct of bool fields for basic_auth, digest_auth, tls, and websocket. The fixture's touch_features() calls all four fields. However the fixture does not exercise the PRD-FLG-REQ-002/PRD-FLG-REQ-004 sentinel/throw behaviour (e.g. calling use_ssl(true) or basic_auth(true) in the off-lane to verify it either returns a sentinel or throws feature_unavailable with the flag name). The member-function-pointer approach in touch_create_webserver_setters() proves the symbols exist but does not validate runtime behaviour. This is acceptable for a compile/link gate, but the PRD-FLG-REQ-004 error-message criterion is not covered here.
   *Recommendation:* PRD-FLG-REQ-004 is already covered by dedicated tests (webserver_dauth_unavailable, webserver_ws_unavailable). No change required in the fixture; document the cross-reference in a code comment or the task.

30. [ ] **test-quality-reviewer** | `.github/workflows/verify-build.yml:700` | missing-test
   The 'Verify libhttpserver detected all features off' step uses the strict regex `^  ${feat}[[:space:]]*:[[:space:]]*no$` against `config.log`. While the pattern has been verified to match the actual `config.log` format (confirmed from the local build artifact), the existing 'no-dauth' verification (line 687) uses a looser pattern `"Digest Auth.*:.*no"`. The inconsistency creates a maintenance trap: if the configure summary indentation ever changes, the strict pattern will silently start returning false negatives (the `grep -q` returns non-zero, the `fail=1` branch runs, the lane goes red) before any source regression is detected.
   *Recommendation:* Align the pattern style with the existing no-dauth check: use `grep -q "${feat}.*:.*no"` (same loose form) for all four features, or add a comment explaining why the strict anchor form is intentional and will remain correct.

31. [ ] **test-quality-reviewer** | `.github/workflows/verify-build.yml:715` | implementation-coupling
   The config.log grep in 'Verify libhttpserver detected all features off' uses a strictly anchored ERE pattern ('^  ${feat}[[:space:]]*:[[:space:]]*no$') that matches the actual config.log format confirmed in the local build log. However, the existing no-dauth verification step (line 697) uses the looser 'Digest Auth.*:.*no' pattern without anchoring, creating an inconsistency in verification style. Both currently work, but the looser form could silently pass if a different line containing the feature name appeared before the real summary line.
   *Recommendation:* The new anchored pattern is the better form; consider updating the no-dauth step at line 697 to use the same anchored style for consistency, but this is not blocking.

32. [ ] **test-quality-reviewer** | `test/consumer_fixture.cpp:117` | missing-test
   touch_create_webserver_setters() pins use_ssl, basic_auth, and digest_auth via member-function pointers (proving their declarations are unconditional) and also calls TLS credential-material setters directly on a live create_webserver object. The digest_auth(bool) member-function pointer is captured but the setter is never called with `true` directly (unlike use_ssl(false) and basic_auth(false) in build_server()). This is intentional per the comment, but means a future removal of the digest_auth(bool) overload would only be caught via the member-function-pointer address, not via a direct call site — which is weaker evidence for consumers.
   *Recommendation:* This is acceptable for a compile-only gate. The member-function-pointer reference is sufficient to prove the symbol exists at link time. No change required.

33. [ ] **test-quality-reviewer** | `test/consumer_fixture.cpp:42` | test-isolation
   The fixture is compiled with `-DHTTPSERVER_COMPILATION` (inherited from `AM_CPPFLAGS` in `test/Makefile.am`), which unlocks private MHD-bound constructors and internal headers that a real consumer would never have access to. This means the fixture is not a true consumer simulation: a regression that breaks real consumer compilation (e.g., a missing public symbol) could theoretically be masked if the symbol is reachable via the internal `HTTPSERVER_COMPILATION` path. The comment in the fixture acknowledges this for `create_test_request.hpp` but does not flag the broader implication.
   *Recommendation:* Consider adding a second, minimal compile-only TU (e.g., `test/unit/consumer_public_only_test.cpp`) that omits `-DHTTPSERVER_COMPILATION` and exercises only the symbols reachable from `<httpserver.hpp>` (the umbrella header), similar to how `header_hygiene_test.cpp` and `webserver_pimpl_test.cpp` override `CPPFLAGS` with `header_hygiene_CPPFLAGS` / `webserver_pimpl_LDADD`. If this is out of scope for TASK-037, at minimum document the limitation explicitly in the fixture header comment.
