# Unworked Review Issues

**Run:** 2026-06-06 15:17:14
**Task:** TASK-067
**Total:** 24 (0 critical, 1 major, 23 minor)

## Major

1. [ ] **housekeeper** | `specs/tasks/M7-v2-cleanup/TASK-067.md:16` | action-item-not-marked-complete
   All five action items use unchecked '- [ ]' markers even though the task is marked Done and the implementation is verified complete. The four substantively completed items are: 'Identify every reader of registered_resources* (grep src/)...' (rewritten to consume v2 route table), 'Delete registered_resources* maps + mutex from webserver_impl' (confirmed via grep returning no matches), 'Delete namespace compat from create_webserver.hpp, the [[deprecated]] overload, and the paired -Wdeprecated-declarations push/pop' (confirmed via grep returning no matches), and 'RELEASE_NOTES.md: document the binary/source incompatibility under Removals' (two entries added at lines 68-93). The fifth item ('Drop the same -Wdeprecated-declarations suppression in src/http_resource.cpp:81-115 once the migration to std::atomic<std::shared_ptr<T>> (TASK-070) makes it unnecessary, or hand it off to TASK-070 explicitly if scope expands') was correctly handed off to TASK-070 (which already captures it in its action items), but the bullet itself was not marked complete or annotated to reflect the handoff decision.
   *Recommendation:* Mark the four completed action items as '- [x]' in specs/tasks/M7-v2-cleanup/TASK-067.md. For the fifth item (http_resource.cpp -Wdeprecated-declarations), either mark it '- [x]' with a note 'handed off to TASK-070' or annotate it inline to record the scope decision — so that a future reader understands it was deliberately deferred rather than forgotten.

## Minor

2. [ ] **architecture-alignment-checker** | `src/httpserver/detail/webserver_impl_dispatch.hpp:310` | pattern-violation
   Comment block references 'registered_resources_str' and 'registered_resources_regex' (the deleted v1 map names) inline inside a declaration comment for resolve_resource_for_request. While these are purely historical documentation, the names now appear nowhere else as actual identifiers, making the references slightly stale. The architecture doc (route-table.md §Implementation status) is the canonical place for this history.
   *Recommendation:* Consider trimming the comment at lines 310-313 to remove the deleted field names (keeping only the TASK attribution), or leave as-is — this is a documentation-only concern with no correctness impact.

3. [ ] **code-quality-reviewer** | `specs/tasks/M7-v2-cleanup/TASK-067.md:19` | task-completeness
   Action item 4 ('Drop the same -Wdeprecated-declarations suppression in src/http_resource.cpp:81-115 once the migration to std::atomic<shared_ptr<T>> (TASK-070) makes it unnecessary, or hand it off to TASK-070 explicitly') is acknowledged in the task as an explicit deferral to TASK-070, but the PR description does not record the decision. The suppression in src/http_resource.cpp:88-91 is unrelated to the compat shim (it suppresses the std::atomic_load_explicit deprecation for shared_ptr, a C++20 issue) and was correctly left alone. The task checkbox is left unchecked but no handoff note is present in the task document itself.
   *Recommendation:* Add a one-line note to the TASK-067.md action item 4 or in the TASK-070 task confirming the explicit deferral decision, so the open checkbox does not appear as forgotten work during the final audit.

4. [ ] **code-quality-reviewer** | `src/detail/webserver_routes.cpp:99` | code-readability
   The block comment at lines 99-115 (preceding for_each_requested_method) describes the numbered steps of on_methods_ but is orphaned above the anonymous namespace — it is a leftover from a pre-refactor state and no longer accurately introduces the code immediately below it. The comment describes steps 1-5 of a function that is split across the file, and the 'three storage maps' language still refers to the deleted v1 maps. This could confuse readers trying to understand the current code structure.
   *Recommendation:* Remove or rewrite lines 99-115 to describe the v2-only picture: the comment block's numbered steps and 'three storage maps' language are stale after TASK-067 removed the v1 maps and the lock structure changed.

5. [ ] **code-quality-reviewer** | `test/integ/basic.cpp:491` | test-coverage
   The CASE_INSENSITIVE branch at line 500 ('ws->register_path("ok", ok2)') under duplicate_endpoints is now a successful registration rather than a throw. The test correctly branches on the #ifdef but does not verify that the newly registered 'ok' route is actually reachable (i.e., a follow-up curl GET to '/ok' returns 200). This leaves the case-sensitive vs. case-insensitive disambiguation slightly under-pinned at the integration level.
   *Recommendation:* Consider adding a curl probe for '/ok' in the else branch to verify the route is reachable, confirming that case-sensitive path registration works end-to-end after the v1 map removal. This is optional since the unit-level route table tests cover lookup correctness.

6. [ ] **code-quality-reviewer** | `test/unit/no_v1_compat_shim_test.cpp:95` | code-readability
   The runtime LT_BEGIN_AUTO_TEST case 'sentinel_compiles' contains LT_CHECK_EQ(1, 1) as a trivial stub. The file's comment explains this exists purely so the LT runner sees the suite and prints a green line. While pragmatically justified, the intent would be clearer if the no-op assertion had an accompanying comment (e.g. '// LT runner requires at least one runtime assertion; the load-bearing checks above are static_asserts').
   *Recommendation:* Add a brief inline comment on the LT_CHECK_EQ(1, 1) line referencing the static_asserts so future readers do not mistake the trivial check for incomplete test logic.

7. [ ] **code-simplifier** | `src/detail/webserver_dispatch.cpp:288` | code-structure
   In `resolve_resource_for_request`, the `get_if` result is named `hp` — a two-character abbreviation for 'handler pointer'. Given the surrounding context where several pointer types coexist (`hrm`, `mr`, `dhr`), expanding `hp` to `handler_ptr` or `resource_ptr` would make its role immediately clear without consulting the type annotation.
   *Recommendation:* Rename `hp` to `resource_ptr` (or `handler_sp`) throughout `resolve_resource_for_request`. This is a single-function change with zero behavior impact.

8. [ ] **code-simplifier** | `src/detail/webserver_dispatch.cpp:97` | code-structure
   `invalidate_route_cache()` is a one-liner (`route_lru_cache.clear()`) that exists solely to give the public API a name. While the naming is good, the 8-line block comment above `lookup_v2` that explains the lock ordering (table BEFORE cache) is not mirrored in `invalidate_route_cache`, whose callers must know NOT to hold the table lock when they call it. This contract is currently stated in `unregister_impl_` but not on the function itself.
   *Recommendation:* Add a one-line contract comment to `invalidate_route_cache`: `// Caller must NOT hold route_table_mutex_ (lock is taken by route_lru_cache internally).` Keeps the lock-order contract co-located with the function that must obey it.

9. [ ] **code-simplifier** | `src/detail/webserver_register.cpp:155` | code-structure
   The `duplicate_throw` lambda captures `key` by reference and is a local function used only twice in `reject_duplicate_v2_entry_`. The lambda adds a layer of indirection that makes the reader ask 'is this stored somewhere?' before concluding it is always called immediately. A named helper or a direct `throw` statement would be clearer.
   *Recommendation:* Replace the lambda with a direct throw expression at the two call sites, or extract a private static helper `throw_duplicate(const std::string& key)` that is called by both branches. Eliminates the 'is this callable stored?' mental overhead.

10. [ ] **code-simplifier** | `src/detail/webserver_register.cpp:183` | naming
   `fresh_out` is an output-only parameter (bool&) used as a flag to communicate 'was this a fresh entry?' back to `on_methods_`. Boolean output parameters are a classic code smell (Clean Code: flag arguments). The name `fresh_out` encodes the output direction in a suffix, which is atypical for C++ and suggests the API boundary is slightly awkward.
   *Recommendation:* Return `bool` directly from `prepare_or_create_lambda_shim` (true = fresh entry, false = existing). The caller already uses the returned `shim` alongside `is_new_entry`; returning a `std::pair<std::shared_ptr<lambda_resource>, bool>` or a small struct with named fields would make both outputs explicit without a mutable reference parameter.

11. [ ] **code-simplifier** | `src/detail/webserver_register.cpp:246` | code-structure
   `reject_terminus_collision` builds the `collision` boolean in one expression that spans four lines with asymmetric branches for `want_is_prefix=true` vs `want_is_prefix=false`. The polarity inversion (`!want_is_prefix ? ... : ...`) means the first branch is for the 'not prefix / new exact' case and the second is for the 'want prefix' case, which is the opposite of the parameter's name. This is the type of negative-conditional the clean-code guide warns about.
   *Recommendation:* Split into two clearly-named guard expressions, one per incoming kind:
```cpp
if (!want_is_prefix) {
    // New exact: refuse if a prefix entry already exists at this key.
    if (param_and_prefix_routes_.has_terminus_at(key, /*is_prefix=*/true))
        throw_collision(key, "exact", "prefix");
} else {
    // New prefix: refuse if an exact entry already exists at this key.
    const bool exact_exists = exact_routes_.find(key) != exact_routes_.end()
        || param_and_prefix_routes_.has_terminus_at(key, /*is_prefix=*/false);
    if (exact_exists) throw_collision(key, "prefix", "exact");
}
```
This removes the negative-conditional and makes the collision direction readable without consulting the caller.

12. [ ] **code-simplifier** | `src/detail/webserver_register.cpp:292` | code-structure
   `unregister_impl_` uses a braced block to scope the `table_lock` unique_lock, with `invalidate_route_cache()` called immediately after the closing brace (outside the block). The pattern is correct (table lock released before cache invalidation) but the braced scope is implicit — a reader must notice the brace to understand the lock is already released. The same pattern in `on_methods_` (line 456) is explicit because a comment documents the lock window.
   *Recommendation:* Add a short comment before `invalidate_route_cache()` in both `unregister_impl_` and `unregister_resource`: `// table_lock released above; cache invalidation runs without the table lock (lock-order: table -> cache).` Mirrors the existing comment in on_methods_.

13. [ ] **code-simplifier** | `src/detail/webserver_register.cpp:340` | code-structure
   `update_existing_v2_entry` identifies the correct tier-based entry by walking `exact_routes_` first, then sweeping `regex_routes_` by shim pointer identity. The comment says 'pointer identity is the cheapest and most reliable discriminator', but the same code does not attempt the radix tier at all. A comment explaining that radix-tier paths always go through `upsert_v2_radix_route` (never here) would close the apparent gap for a reader who notices the missing tier.
   *Recommendation:* Add a one-line comment before the exact-tier lookup: `// Radix-tier updates are handled by upsert_v2_radix_route; only exact and regex entries reach here.` This makes the three-tier coverage explicit and prevents a future reader from adding a redundant radix probe.

14. [ ] **code-simplifier** | `test/unit/no_v1_compat_shim_test.cpp:57` | patterns
   The detection-idiom struct is named `accepts_legacy` but is actually a general 'does CW accept F as auth_handler argument' probe — it has no inherent connection to 'legacy'. The negative static_assert on line 70 and positive control on line 79 could use the same template with clearer names: `accepts_auth_handler<CW, F>::value`. The name `accepts_legacy` implies the template is only useful for the negative case, but the positive control reuses it too.
   *Recommendation:* Rename `accepts_legacy` to `accepts_auth_handler` and update both static_asserts. The template is general-purpose; the 'legacy' meaning is in the test name and the comment, not in the template name.

15. [ ] **performance-reviewer** | `src/detail/webserver_dispatch.cpp:312` | missing-caching
   resolve_resource_for_request populates mr->matched_path_template with mr->standardized_url (a copy of the already-interned string) on every request when any route_resolved or before_handler hook is registered. The comment correctly notes the v2 cache_value does not store the matched URL template, so the copy is unavoidable given the current cache_value shape. This is a latent minor cost: if hook registration becomes common the per-request copy accumulates, but the string is already owned by mr so the actual allocation is a std::string copy of an existing heap string (bounded by path length, typically <100 bytes).
   *Recommendation:* Consider extending cache_value to store the matched_path_template string alongside the entry and captured_params, so the hook-gated copy path can be skipped on cache hits. Impact is minor: path strings are short and the hook gate already avoids the copy when no hooks are registered.

16. [ ] **performance-reviewer** | `src/detail/webserver_register.cpp:177` | algorithmic-complexity
   reject_duplicate_v2_entry_ for the regex tier (route_tier_kind::pattern) does a linear scan of regex_routes_ by url_complete string equality to detect duplicates at registration time. This is O(n) in the number of registered regex patterns. Duplicate detection only runs during registration (not on the hot dispatch path), so the impact is limited to server startup or dynamic route registration. However, if an application registers many hundreds of regex routes dynamically, this scan could become noticeable.
   *Recommendation:* For typical deployments (tens of regex routes registered at startup) this is fine. If dynamic registration of many regex routes is expected, maintain a secondary std::set<std::string> of regex url_complete keys for O(log n) duplicate detection. Registration is already serialised under route_table_mutex_ so there is no concurrency complication.

17. [ ] **performance-reviewer** | `src/detail/webserver_register.cpp:272` | algorithmic-complexity
   unregister_impl_ for the exact/regex branch performs a linear scan of regex_routes_ (std::remove_if over a vector) to erase by url_complete key. This is O(n) in the number of registered regex routes. The same pattern exists in unregister_resource (line 309). For the expected workload (few regex routes, unregistration is a rare admin operation) this is entirely acceptable. It becomes a concern only if an application registers and unregisters thousands of regex routes at high frequency.
   *Recommendation:* No action needed for typical workloads. If high-frequency regex-route churn becomes a use case, consider switching regex_routes_ to a std::map<std::string, regex_route> keyed by url_complete to get O(log n) erasure. The trade-off is slightly higher lookup overhead (extra indirection vs the current cache-friendly vector scan).

18. [ ] **security-reviewer** | `src/detail/webserver_dispatch.cpp:181` | insecure-design
   route_lru_cache.find_by_view is called (line 181) outside the route_table_mutex_ shared_lock. The cache has its own internal mutex, so there is no data race on the cache structure itself. However, a cache hit returns a route_entry that was inserted when the table lock was held; after the cache hit is returned, the caller uses the route_entry without re-validating that the entry is still present in the route table (a concurrent unregister_impl_ could have removed it). The shared_ptr<http_resource> held by the cache entry keeps the resource alive, so there is no use-after-free, but the resource may serve requests after it has been logically unregistered.
   *Recommendation:* This is an accepted architectural trade-off (the cache is invalidated on unregister, but an in-flight cache-hit call may finish serving an old entry). Document this window explicitly in the cache invalidation contract or in the threading contract block in webserver.hpp to make the behavior auditable.

19. [ ] **security-reviewer** | `src/detail/webserver_routes.cpp:424` | insecure-design
   validate_on_methods_inputs_ rejects paths containing embedded null bytes (CWE-20, line 428-431), but register_impl_ / validate_register_inputs_ has no equivalent null-byte check. A path with an embedded null passed to register_path / register_prefix or the register_resource legacy forwarder would be canonicalized by http_endpoint and stored under a key that might silently truncate at the null in some downstream comparison contexts (e.g., C-string-based MHD URL matching).
   *Recommendation:* Add the same null-byte guard used in validate_on_methods_inputs_ to webserver::validate_register_inputs_ so all registration entry points reject paths with embedded nulls consistently.

20. [ ] **security-reviewer** | `src/detail/webserver_routes.cpp:526` | insecure-design
   The method_set overload of webserver::route() (line 520-536) documents that passing only the count_ sentinel bit produces a non-empty bitmask that for_each_requested_method iterates zero times, resulting in a silently empty lambda_resource shim that is stored but never dispatches anything. The single-method overload explicitly guards against count_ but the method_set overload does not. An attacker-controlled method_set with only the count_ bit set would register an unreachable route that occupies a slot without serving any request, potentially masking a legitimate registration attempt.
   *Recommendation:* Add a guard in validate_on_methods_inputs_ or on_methods_: iterate the method_set bits and reject any call where no valid method bit (i.e., bit index in [0, count_-1]) is set, regardless of whether the raw bitmask is zero. This closes the gap noted in the existing comment at webserver_routes.cpp line 528-535.

21. [ ] **security-reviewer** | `src/detail/webserver_websocket.cpp:109` | insecure-design
   In handle_close_frame, the close_reason is derived directly from the WebSocket CLOSE frame payload (frame_data+2, frame_len-2) with no length cap or UTF-8 validation before being passed to handler->on_close() and session.close(). RFC 6455 §5.5.1 requires the reason to be valid UTF-8 and does not cap its length; a malicious peer could send an oversized or invalid-UTF-8 CLOSE payload that user-supplied on_close implementations may not expect. libhttpserver cannot enforce how users handle this string.
   *Recommendation:* Document in the websocket_handler::on_close API contract that the close_reason parameter originates directly from the peer and may require UTF-8 validation and length capping in user implementations. Optionally add a defensive cap (e.g., 123 bytes, the RFC 6455 §5.5 CLOSE frame max payload minus 2) inside handle_close_frame before forwarding to the handler.

22. [ ] **spec-alignment-checker** | `/Users/etr/progs/libhttpserver/.worktrees/TASK-067/src/http_resource.cpp:86` | action-item
   The action item 'Drop -Wdeprecated-declarations suppression in src/http_resource.cpp:81-115' is not completed in this PR. The suppression pragmas remain at lines 86-92 and 111-114. The task definition itself provides the exit clause: 'or hand it off to TASK-070 explicitly if scope expands'. The comment in http_resource.cpp (line 81: 'TODO(C++20 cleanup): migrate hook_table_ to std::atomic<shared_ptr<T>>') makes the dependency on TASK-070 explicit, and the _index.md confirms TASK-070 is a tracked Backlog item. The deferral is intentional and correctly documented; this is a minor variance noting that one of the five action items is deferred rather than completed in TASK-067.
   *Recommendation:* No action required in this PR. Ensure TASK-070 (Migrate hook_table_ to std::atomic<shared_ptr<T>>) explicitly captures removal of the -Wdeprecated-declarations suppression block in src/http_resource.cpp as part of its acceptance criteria.

23. [ ] **test-quality-reviewer** | `test/unit/no_v1_compat_shim_test.cpp:95` | naming-convention
   The lone runtime test case is named `sentinel_compiles`, which accurately describes its purpose (the file comment explains this is a runner-visible case whose value is the compile-time assertion). However the detection-idiom template is named `accepts_legacy` even when it is used for both the negative (legacy) and positive (canonical) assertions (line 79). The positive control's name `accepts_legacy<..., canonical_sig>` is semantically contradictory — a reader must re-read the comment to understand why a struct called `accepts_legacy` is asserted true for the canonical shape.
   *Recommendation:* Rename the template to `accepts_auth_handler` or `is_auth_handler_accepted` so both the negative and positive static_asserts read naturally without requiring the surrounding comment.

24. [ ] **test-quality-reviewer** | `test/unit/no_v1_compat_shim_test.cpp:95` | unnecessary-test
   The runtime test case `sentinel_compiles` asserts `LT_CHECK_EQ(1, 1)` — a tautology that can never fail. The comment acknowledges this (its purpose is to give the littletest runner a visible suite line), but there is a small risk a future contributor might misread this as a meaningful assertion and copy the pattern. The file comment explaining the design rationale is thorough, which mitigates this risk.
   *Recommendation:* Consider replacing `LT_CHECK_EQ(1, 1)` with a trivially-passing but self-documenting assertion such as a boolean constant, e.g. `LT_CHECK(true)`, or add an inline comment directly on the assertion line restating that the runtime value is irrelevant and the compile-time static_asserts above are the load-bearing checks.
