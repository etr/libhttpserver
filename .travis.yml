language: cpp
os:
 - linux
 - osx
compiler: 
 - gcc
 - clang
env:
 - DEBUG="debug"
 - DEBUG="nodebug"
 - LINKING="static"
before_install:
 - export LDFLAGS="$LDFLAGS -L/usr/local/lib -L/usr/lib"
 - export PATH=$PATH:/usr/local/lib
 - export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:/usr/local/lib
 - export DYLD_LIBRARY_PATH=$DYLD_LIBRARY_PATH:/usr/local/lib
 - if [ "$TRAVIS_OS_NAME" = "linux" ]; then sudo apt-get install info install-info; fi
 - if [ "$TRAVIS_OS_NAME" = "linux" ]; then sudo pip install cpp-coveralls; fi
 - if [ "$TRAVIS_OS_NAME" = "linux" ]; then sudo pip install gcovr; fi
 - if [ "$TRAVIS_OS_NAME" = "osx" ]; then export CFLAGS='-mtune=generic'; fi
 - curl https://s3.amazonaws.com/libhttpserver/libmicrohttpd_releases/libmicrohttpd-0.9.59.tar.gz -o libmicrohttpd-0.9.59.tar.gz
 - tar -xvzf libmicrohttpd-0.9.59.tar.gz
 - cd libmicrohttpd-0.9.59
 - ./configure --disable-examples
 - make
 - sudo make install
 - cd ..
 - eval "${MATRIX_EVAL}"
install:
 - if [[ "$CROSS_COMPILE" == 1 ]] ; then
     if [[ "$ARM_ARCH_DIR" == "aarch64-linux-gnu" ]] ; then
       mkdir $HOME/linker_bin ;
       ln -s /usr/bin/aarch64-linux-gnu-ld $HOME/linker_bin/ld ;
       LD=$HOME/linker_bin/ld ;
       echo "SETTING GNU LINKER DIR" ;
       ls -al $HOME/linker_bin/ld ;
     fi
   fi
 - ./bootstrap
 - mkdir build
 - cd build
 - if [ $LINKING = "static" ]; then
     ../configure --enable-static --disable-fastopen;
   elif [ $DEBUG = "debug" ]; then
     ../configure --enable-debug --disable-shared --disable-fastopen;
   else ../configure --disable-fastopen;
   fi
 - cat config.log
 - cat Makefile
 - cat libtool
 - make
 - make check TESTS=
script:
 - if [[ "$CROSS_COMPILE" == 1 ]]; then
     cd test ;
     if [[ "$ARM_ARCH_DIR" == "aarch64-linux-gnu" ]]; then
       qemu-aarch64 -L /usr/aarch64-linux-gnu/ ./run_tests ;
     else
       qemu-arm -L /usr/arm-linux-gnueabi/ ./run_tests ;
     fi
   else
     make check ;
     cat test/test-suite.log ;
     ls -l /usr/local/lib/ ;
     ls -l /usr/lib/ ;
   fi
after_success:
  - if [ $DEBUG = "debug" ] && [ "$TRAVIS_OS_NAME" = "linux" ]; then ../ci-report-coverage; fi
matrix:
  exclude:
    - compiler: clang
      env: DEBUG="debug"
    - compiler: clang
      env: LINKING='static'
  include:
    - os: linux
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - g++-5
      env:
         - MATRIX_EVAL="CC=gcc-5 && CXX=g++-5"
    - os: linux
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - g++-6
      env:
        - MATRIX_EVAL="CC=gcc-6 && CXX=g++-6"
    - os: linux
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - g++-7
      env:
        - MATRIX_EVAL="CC=gcc-7 && CXX=g++-7"
    - os: linux
      compiler: clang
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - clang-3.9
            - g++-arm-linux-gnueabi
            - g++-multilib
            - gcc-multilib
            - qemu
            - qemu-system-arm
      env: CROSS_COMPILE=1 ARM_LD_PATH=$HOME/linker_bin ARM_ARCH_DIR=arm-linux-gnueabi ARM_GCC_VER=4.7.3 ARM_SETTINGS="armv7-a -mfpu=neon -mfloat-abi=softfp -target arm-linux-gnueabi"
    - os: linux
      compiler: clang
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - clang-3.9
            - g++-arm-linux-gnueabi
            - g++-multilib
            - gcc-multilib
            - qemu
            - qemu-system-arm
      env: CROSS_COMPILE=1 ARM_LD_PATH=$HOME/linker_bin ARM_ARCH_DIR=arm-linux-gnueabi ARM_GCC_VER=4.7.3 ARM_SETTINGS="armv8-a -mfpu=neon -mfloat-abi=softfp -target arm-linux-gnueabi"
    - os: linux
      compiler: clang
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - clang-3.9
            - g++-4.8-aarch64-linux-gnu
            - gcc-4.8-aarch64-linux-gnu
            - g++-4.8-multilib
            - gcc-4.8-multilib
            - qemu
            - qemu-system-arm
      env: CROSS_COMPILE=1 ARM_LD_PATH=$HOME/linker_bin ARM_ARCH_DIR=aarch64-linux-gnu ARM_GCC_VER=4.8.4 ARM_SETTINGS="armv8-a -target aarch64-linux-gnueabi"
  allow_failures:
    - env: CROSS_COMPILE=1 ARM_LD_PATH=$HOME/linker_bin ARM_ARCH_DIR=arm-linux-gnueabi ARM_GCC_VER=4.7.3 ARM_SETTINGS="armv7-a -mfpu=neon -mfloat-abi=softfp -target arm-linux-gnueabi"
    - env: CROSS_COMPILE=1 ARM_LD_PATH=$HOME/linker_bin ARM_ARCH_DIR=arm-linux-gnueabi ARM_GCC_VER=4.7.3 ARM_SETTINGS="armv8-a -mfpu=neon -mfloat-abi=softfp -target arm-linux-gnueabi"
    - env: CROSS_COMPILE=1 ARM_LD_PATH=$HOME/linker_bin ARM_ARCH_DIR=aarch64-linux-gnu ARM_GCC_VER=4.8.4 ARM_SETTINGS="armv8-a -target aarch64-linux-gnueabi"
